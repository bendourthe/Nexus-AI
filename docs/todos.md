# Nexus - Progress Dashboard

**Project:** Nexus (renamed from Gemma Code at v1.0.0). **Release branch:** `main`. **Active branch:** `feat/v2.4.1-field-reliability` (**v2.4.3** field density Phases 1-8 local complete; T040 waits on explicit push approval against `develop` via [PR 58](https://github.com/bendourthe/Nexus-AI/pull/58). Package remains **2.4.1** until `/update release`).

> **v2.4.8 DESKTOP CORRECTIONS, PHASES 1-5 LOCAL (2026-09-06)** -- [plan](v2/v2.4/plans/v2.4.8-desktop-token-split-persona-and-model-order.md) T001-T023 complete on `feat/v2.4.8-desktop-token-split-persona-and-model-order`: provider token total split by text proportion (no more double-counted reasoning), composer menus and Persona popover dismiss on outside click / Escape with app tokens, `Sessions` title in nav type on four pillars, Settings tabs and cards in the installer grammar (shared fixtures asserted from vitest and pytest), pickers and defaults follow catalog tier and the installer snapshot writer names the picker's own top pick. Phase 6: evidence, known gaps, CI filter, installer rebuild. No push until approved.

> **MODEL LOAD VISIBILITY (2026-09-07)** -- operator saw `Loading model...` for 20+ minutes at 0% GPU on Images with the chat model resident; a direct runtime run loads RealVisXL in ~10 s, so the wait sat upstream (GPU scheduler is single-slot FIFO with no timeout; a chat/agent turn holding it starves studio jobs). Landed: runtime `load_progress.py` counts weight bytes through the safetensors readers and emits `loading` with loadedBytes/totalBytes/etaS (proven live: 0.2 MB -> 6.8 GB of 6.94 GB); sidecar `queuedStage.ts` synthesizes a `queued` stage naming the GPU holder while a job has not reached the runtime; bubble shows `Waiting for GPU (Chatbot is using it)...` or `Loading model NN%` + byte bar + time left; GPU card names the studio model right of `Local model` and shows `GPU usage` / `NN%`; Sessions header orders New session, New folder, Archive, Delete flush right. Fourth installer rebuild (2026-09-07 14:30): `npm run build:shell` then `build-windows.ps1 -SkipSign` -> `dist/NexusSetup.exe` 239.5 MB, SHA256 CA5110CF...2415F0, payload `Nexus AI Studio_2.4.1_x64-setup.exe` sha256 25718660...; smoke-windows-exe.ps1 passed (5/5). Hub snapshot refused (local catalog 4.7.0 not latest), so the installer syncs at install time as before. Tests: +10 pytest (212 green), +9 vitest (touched suites green). Open: scheduler starvation itself (timeout or preemption for a parked chat/agent turn) is not fixed; `pendingCaptionRotator` hero-preset test has been red since the Phase 8 Loading-model change; `settings-control-sweep` ModelsSettings `<button` sweep is red on the branch.

> **GPU HANDOFF AND SESSION CONTINUITY (2026-09-07, round 2)** -- root cause of every Images stall found: `ChildProcessDiffusionRuntime.handleLine` read `jobId` off the top level while the Python runtime sends JSON-RPC notifications with it under `params`, so every loading/generating/heartbeat event was dropped and the bubble never left `Loading model...` (fixed + test). No eviction existed, so the chat model stayed resident and SDXL fell into CPU offload: the studio pump now evicts Ollama residents when free VRAM < 1.5x the media model (`sidecar/src/models/ollamaResidency.ts`), and image jobs release cached VRAM after each run (`vram_lifecycle.release_vram`). New IPC `models.resident` / `models.warm`. Chat: per-chat in-flight turns at module scope (`chatTurns.ts`) so switching session or tab keeps the pending orb and lands the reply; `Loading model NN%` on pending chat replies from Ollama residency + VRAM delta (`modelLoadWatch.ts`); model switch inside a session asks (`ConfirmDialog`), keeps the conversation (sidecar session restarts with replayed history), warms the new model, and the Context bar follows the new window. Sessions newest-first (chat store, in-memory client, studio store). Persona is a person-icon button opening the box directly, anchored right. Sessions header icons match row icon size/gap/right padding. Models settings header is one row: tabs, search, storage. Queued wording: `Waiting for the GPU to free up...` plus a plain second line. Open: the studio pages (Images/Videos) still lose their in-flight orb on a tab change (same pattern as chat before this round); the per-chat model override is not persisted to the chat record (no explorer API); Agents page untouched.

> **GPU BUSY DIALOG AND STUDIO CONTINUITY (2026-09-07, round 3)** -- `Another image is still being generated` was true but invisible: the studio pages dropped the pending orb on a session switch and persisted a job's completion into whichever session was open. Now `activeJob` carries its `sessionId`; completions persist to that session; the pending bubble is restored when the session is re-opened. Any new studio request while the scheduler has an active job asks first (`ConfirmDialog`: `Cancel it and start` / `Wait for it`), replacing the residency switch dialog for the active-job case; new IPC `generation.scheduler.cancelActive` (cancels the queue job, aborts its handle, restarts the Python runtime, or aborts a chat/agent turn via `GpuScheduler.cancelActive`). Chat `Loading model` state is centered like the studios. Models storage summary is compact (`182 GB used` | bar | `206 GB free`) so tabs, search, and storage share one row. Open: the studio pages still track one job per page instance (a second request in another session while the page's own job runs is blocked by `isGenerating`); cancelling a chat/agent turn aborts the scheduler slot but not the Ollama stream.

> **GHOST JOB ROOT CAUSE AND MODEL-SWITCH DIALOG (2026-09-07, round 4)** -- `~/.nexus/generations/studio.db` proved it: interactive job `job-mtrng1jm-1` (12:44) sat `running`, and `GenerationQueue.recover()` re-queued it on every launch (running -> interrupted -> queued), so the pump re-ran the ghost first and every new request queued behind it (three more were waiting). Fix: interactive jobs left queued/running/interrupted by a previous process are now failed with `Interrupted by app restart` (`INTERRUPTED_BY_RESTART`); batch jobs keep the re-queue. Studio pages no longer show a waiting message: any busy GPU (pre-submit via the scheduler prop, or a `queued` report on an already-submitted job) opens `Switch to <Y>?` naming the holder (`<model> (<tab>)` or `a <tab> task`), stating that generation in progress stops and the GPU is cleared; `Switch and start` calls `generation.scheduler.cancelActive`, which now also evicts Ollama residents. Persona button is available before the first message; the draft moves onto the chat the first send creates (ref mirror so the first turn carries it). Studio captions: pool of 9 words, random pick every tick, never the same word twice in a row. Not runnable here: the new `GenerationDatabase.test.ts` recovery case needs better-sqlite3 (host ABI baseline); CI runs it.

> **COLD-START DEADLOCK: THE REAL `Loading model...` CAUSE (2026-09-07, round 5)** -- measured on the operator's host, not inferred. Driving the INSTALLED sidecar bundle over JSON-RPC showed the image job accepted, Ollama evicted, scheduler active, 120 heartbeats over 4 minutes and NOT ONE stage event. Driving the INSTALLED runtime directly with a `health` call first: same job done in 27 s with stage + byte events. Same runtime, job as the FIRST request: 169 heartbeats (~340 s), zero stage events. Root cause: `_dispatch_with_heartbeat` runs job methods on worker threads, and the first `import torch` from a worker thread never completes on Windows; the sidecar always makes a job the first request, so every studio generation deadlocked before `emit_stage("loading")` -- which is exactly the endless `Loading model...` with no bar. Fix: `runtimes/diffusion/main.warm_accelerator()` imports torch on the main thread at startup (verified: job-as-first-request now completes in 15 s with full byte progress), plus a sidecar readiness gate that sends `health` (a control method, inline on the runtime's main thread) before the first job and honours a configured short timeout. Also: the switch dialog now resolves the GPU holder from the running job AND Ollama residency, so finishing a chat reply and moving to Images asks `Switch to RealVisXL V5.0?` naming Gemma 4 -- previously nothing was running, so nothing was asked. Open: the worker-thread import stall itself is worked around, not explained at the CPython/torch level.

> **FEEDBACK, PREFERENCES AND THE SWITCH DIALOG (2026-09-07, round 6)** -- measured first: the operator's image ran 1024px/14 steps in 64 s while their Wan video was 854x480, 30 steps, 96 frames and took ~20 min, so the cost model in `generationProgress.ts` is calibrated on those two real runs rather than invented, and it is dropped the moment the run's own step rate gives a measured estimate. Runtime now emits `callback_on_step_end` step counts (signature-guarded, so a pipeline that does not accept the callback is unaffected), which drives a real bar plus `Step 12 of 30 - about 3 min left` and a running clock. Bar is a styled `<progress>` (`.nexus-progress` in globals.css) as wide as the caption above it. Switch dialog reworded to the operator's text (`Switch to Image model: X?`, holder named from the catalog via `models.resident` displayName, not the raw `gemma4:12b` tag), carries a `Do not show this again` checkbox, and is used by Chat as well as the studios; a `clearing` stage shows `Clearing the GPU... / Unloading Gemma 4 12B.` between confirm and job start. Studio captions shuffle once per prompt then loop that order. New Settings > Preferences tab, first and default: ask-before-switch toggle, per-category default model and reorder (`nexus.ui.defaultModel.*` / `nexus.ui.modelOrder.*`), ranked above catalog tier in `pickerOrder`. Video bubble no longer renders a second full-width player under the clip it already plays (per-frame comments went with that previewer; the prompt suffix stays wired) and Enhance is an icon button beside Download. DEFERRED, not started: the Video2X enhancement panel redesign (compact layout, fewer options, `Resolution` instead of `General`, animation preset as a toggle).

> **INSTALLER WIZARD MERGE, ROUND 3 (2026-09-06)** -- Welcome prerequisites drop VS Code (the extension feature self-disables), order Disk / GPU / Python / Ollama with the GPU card styled like the others; Browse is a gray pill. Model cards: violet downloaded icon, hover texts, Document tab after Embeddings. Review: note under Components in a narrower facts column, card-head captions, fixed-width category pills in two fixed columns with aligned model display names. Every label in the wizard is mouse-selectable via an app-level filter.

> **INSTALLER WIZARD MERGE, ROUND 2 (2026-09-06)** -- Configuration also folds into Welcome (five steps: Welcome, Models, Review, Installing, Complete): install path + Ollama URL card and a Features card (Start Menu shortcut, Desktop shortcut, VS Code extension update, optional Unsloth); required components resolve on Review. Models page: totals and the OS-reserve warning count only pending downloads, cards tinted by provider with provider-colored names, steelblue Recommended pill, and size / compatibility icon / downloaded icon badges. Browse is a filled rounded button.

> **INSTALLER WIZARD MERGE (2026-09-06)** -- Welcome absorbs the Setup step: the prerequisites become per-item cards (icon tile, name, detail, status badge) with a wide GPU card, the install path and Start Menu shortcut move to Configuration (two surface cards), the Review page is rebuilt to the mockup (path box with copy, component check rows, GPU pill, ring gauges, category pills, stat tiles), downloaded models stay pre-selected across Models revisits, and the Ollama manifest fallback honors `OLLAMA_MODELS`. STEP_NAMES is six entries. Installer exe rebuilt unsigned via build-windows.ps1 -SkipSign. Next: field-test the six-step flow.

> **v2.4.3 FIELD DENSITY PHASE 8 LOCAL (2026-08-31)** -- [field density last-phase evidence](v2/v2.4/development/last-phase-evidence-v2.4.3-field-density.md) T032-T039 complete. T040 is one push that updates PR 58; merge, tag, and `/update release` wait on green integration. MT-1 through MT-7 remain Not observed.

> **v2.4.3 FIELD DENSITY PHASE 7 (2026-08-31)** -- [SANA-Video layout and executor](v2/v2.4/plans/v2.4.3-field-density-identity-and-runtime.md) T028-T031 complete. Complete Diffusers tree plus SanaVideoPipeline. Next: Phase 8 last-phase evidence.

> **v2.4.3 FIELD DENSITY PHASE 6 (2026-08-31)** -- [image restyle identity](v2/v2.4/plans/v2.4.3-field-density-identity-and-runtime.md) T024-T027 complete. Restyle strength 0.7 plus fail-closed without last PNG. Next: Phase 7 SANA-Video.

> **v2.4.3 FIELD DENSITY PHASE 5 (2026-08-31)** -- [picker rank and default](v2/v2.4/plans/v2.4.3-field-density-identity-and-runtime.md) T020-T023 complete. Empty agentic session defaults to Gemma 4 12B. Next: Phase 6 image restyle.

> **v2.4.3 FIELD DENSITY PHASE 4 (2026-08-31)** -- [Settings compact cards](v2/v2.4/plans/v2.4.3-field-density-identity-and-runtime.md) T016-T019 complete. Nowrap facts, Details pills, colored icon actions. Next: Phase 5 picker rank and default.

> **v2.4.3 FIELD DENSITY PHASE 3 (2026-08-31)** -- [thinking pill geometry](v2/v2.4/plans/v2.4.3-field-density-identity-and-runtime.md) T013-T015 complete. Fixed min-width from Searching... plus 12 px left inset. Next: Phase 4 Settings compact cards.

> **v2.4.3 FIELD DENSITY PHASE 2 (2026-08-31)** -- [history chrome, new-chat select, first-send titles, centered delete](v2/v2.4/plans/v2.4.3-field-density-identity-and-runtime.md) T007-T012 complete. Next: Phase 3 thinking pill.

> **v2.4.3 FIELD DENSITY PHASE 1 (2026-08-31)** -- [installer two-column layout and Unsloth lock](v2/v2.4/plans/v2.4.3-field-density-identity-and-runtime.md) T001-T006 complete. Configuration is Components | Features. Review is facts | models. Unsloth Incompatible cannot be ticked and refreshes after GPU detection. Next: Phase 2 history chrome.

> **v2.4.2 FIELD UI LAST PHASE LOCAL (2026-08-31)** -- [field UI, sidebar history, and generation follow-through](v2/v2.4/plans/v2.4.2-field-ui-history-and-generation.md) T001-T042 complete. Evidence: [last-phase-evidence-v2.4.2-field-ui.md](v2/v2.4/development/last-phase-evidence-v2.4.2-field-ui.md). Phases 1-6 committed locally (`dcb201f` through `bbe0f6d`). T043 is one push that updates PR 58; merge, tag, and `/update release` wait on green integration. MT-1 through MT-6 remain Not observed.

> **v2.4.1 GENERATION RECOVERY IMPLEMENTATION ACTIVE (2026-08-30)** -- The approved [generation recovery and UI corrections plan](v2/v2.4/plans/v2.4.1-generation-recovery-and-ui-corrections.md) finished Phase 7 local gates. Phases 1-6 are committed locally (`d51b14e` through `b11cec7`) and have not been pushed. Exact installer SHA-256 `E29725ADE5671271962CE9FB6DE66DCAA38BA182DC257802DFA835C78120490D` produced a validated 512x512 PNG and an H.264 848x480 MP4. T089-T096 are recorded; T097 waits on explicit push/PR approval against `develop`. Remaining visual/account/reboot checklist items stay Not observed.

> **v2.4.1 FIELD-CORRECTED INTEGRATION CANDIDATE (2026-08-29)** -- The approved [field reliability, chat archives, model library, and multi-root workspaces plan](v2/v2.4/plans/v2.4.1-field-reliability-chat-archives-models-workspaces.md) has T001-T047 complete and PR 58 open. Follow-up field feedback is corrected: EmbeddingGemma 300M is required/default app-wide, incompatible models sort last, explicitly selected gated models use direct browser device authorization with working manual/skip fallbacks, and the global progress bar has a stationary pill-shaped fill with one broad 12-second liquid-glass highlight, high-contrast percentage text, and complete visible step accounting. Fresh second-correction evidence includes 43 focused tests, installer 1,272 passed / 3 skipped, strict typing on the three changed modules, full installer Ruff, and frozen OAuth archive proof. The second replacement unsigned `dist/NexusSetup.exe` is 239.2 MB with SHA-256 `85350CE1FF3A1FBE9EC06D6332D7540B2A7AC1E889973E4623C45A803B653E58` and passes frozen version/registry/payload smoke. Tag, release, merge, and main integration remain unauthorized; packaged visual/live-account/GPU observations remain in [v2.4 known gaps](v2/v2.4/known-gaps.md).

> **v2.3.1 RELEASE PREPARED (2026-08-29)** -- [installer field-repair plan](v2/v2.3/plans/v2.3.1-installer-field-repair.md) is integrated on `develop` via [PR 53](https://github.com/bendourthe/Nexus-AI/pull/53) (merge `838268f`). Product version is **2.3.1**. T001-T027 complete. MT-1 (`host_detect.py` 40%) remains. Tag, GitHub Release, and `main` merge wait on confirmation.

> **v2.3.0 RELEASED (2026-08-29)** -- [local-video-enhancement adoption plan](v2/v2.3/plans/v2.3.0-adoption-qwen-video2x-openworker.md) is tagged `v2.3.0`. Product version **2.3.0**. Real Video2X/GPU/packaged measurement remains DF-3. Hub security-audit remains DF-2. First full-matrix Shell Build Windows/macOS desktop vitest miss is BG-1.

> **v2.3.1 PHASE 6 COMPLETE (2026-08-29)** -- [installer field-repair](v2/v2.3/plans/v2.3.1-installer-field-repair.md) T019-T027: last-phase evidence, PR 53 green (after vsce engines fix), merged to `develop` at `838268f`. Post-merge CI, Installer tests, and Shell Build (ubuntu) succeeded.

> **v2.3.0 PHASE 6 LOCAL GATE COMPLETE (2026-08-28)** -- [local-video-enhancement adoption plan](v2/v2.3/plans/v2.3.0-adoption-qwen-video2x-openworker.md) T017-T024 quote last-phase evidence, reconcile known gaps, compare CI without changing workflow files, and prove the full local suite. Exact local gates: root 5693 passed / 12 skipped (87.66% lines), isolated desktop 1820 passed / 1 skipped (87.39% lines), installer 1162 passed / 3 skipped, python 261 passed, lint and `tsc -b` clean. Human testing remains suggestions (DF-3). T025 publication is pending explicit approval. No pipeline file changed, and no v2.3.0 phase has been pushed.
>
> **v2.3.0 PHASE 5 COMPLETE (2026-08-28)** -- [local-video-enhancement adoption plan](v2/v2.3/plans/v2.3.0-adoption-qwen-video2x-openworker.md) T014-T016 add the fake-backend enhancement harness, support-copy SSOT, Settings > Video path, installer notes, and packaging parity tests. Exact local gates: 6 packaging tests, 33 focused desktop tests, installer pytest for support copy plus ConfigurationPage, desktop ESLint `--max-warnings=0`, `tsc -b` and desktop `tsc --noEmit`. File-level line coverage on new modules is at or above 80% (`videoEnhancementSupport.ts` 100%, `VideoSettings.tsx` 98.87%, `ipcVideoSettingsClient.ts` 100%). Fake-backend 8/8 fixture cases pass; real Video2X, GPU, and packaged detection remain DF-3. Hub security-audit remains DF-2. Phase 6 is last-phase evidence, full aggregate gates, and the first push. No pipeline file changed, and no v2.3.0 phase has been pushed or started remote CI; no human testing was requested.
>
> **v2.3.0 PHASE 4 COMPLETE (2026-08-28)** -- [local-video-enhancement adoption plan](v2/v2.3/plans/v2.3.0-adoption-qwen-video2x-openworker.md) T011-T013 add the Video Lab Enhance surface over durable child jobs. Completed identity-backed clips can open a capability-backed preset panel, enqueue typed sidecar requests, observe progress and cancellation, and receive a separate synthesized output with its own download and provenance action. Exact local gates are 61 focused desktop UI/client tests, desktop ESLint with `--max-warnings=0`, and green desktop TypeScript compilation. Evidence is internal-compatible for the React and IPC path and candidate for real Video2X, GPU, and packaged support until Phase 5. Session hydrate does not restore Enhance because studio turns persist a media path rather than output id and hash (DF-1). Phase 5 adds quality, performance, and packaging evidence. No pipeline file changed, and no v2.3.0 phase has been pushed or started remote CI; no human testing was requested.
>
> **v2.2.9 RELEASED (2026-08-28)** -- [field-chrome-catalog-and-generate](v2/v2.2/plans/v2.2.9-field-chrome-catalog-and-generate.md) is published at tag `v2.2.9`. Phases 1-7 are complete at automated/internal-compatible evidence and package metadata is **2.2.9**. The approved legacy exception records that feature work landed directly on `develop` without a pull request. Fresh root and desktop aggregate coverage passes provide release evidence; MT-4 remains open because per-phase coverage was not recorded. The final unsigned Windows installer passed version, registry, and embedded-desktop-payload probes, but operator checklist items 1-7 were explicitly deferred to DF-36; live GPU generation remains DF-4. Installer pytest passed 1160 tests with 3 skips. Release and CI-smoke VSIX packaging emit host-qualified artifacts, rebuild `better-sqlite3` 12.11.1 for Electron 42.8.1, and pin the optional extension to exact VS Code 1.134.0. The release contains exactly five attachments: three platform VSIXes, the complete Windows provisioning installer, and `SHA256SUMS.txt`. Raw Tauri bundles are withheld until they embed the exact Node runtime required by the sidecar (DF-38). The tag workflow, `main` integration, post-merge CI, Installer Tests, Shell Build, CodeQL, and semantic-release no-op all passed. Known gaps are [finalized](v2/v2.2/known-gaps.md). This cycle changes no opt-in capability or installer flag; the Windows desktop application is unaffected by the VS Code host pin.
>
> **v2.2.8 RELEASED (2026-08-25)** -- [working-local-studio](v2/v2.2/plans/v2.2.8-working-local-studio.md) plus v2.2.6 session-memory and v2.2.7 context-meter, one tag. Package **2.2.8**. Known-gaps [in-progress](v2/v2.2/known-gaps.md) (DF-2 closed observed; DF-4 live GPU; DF-32 packaged Hi observed 2026-08-25, closed in v2.2.9 Phase 7; DF-35 Active=latest observed with `v` prefix banner, closed in v2.2.9 Phase 7 with the banner fixed by tag normalize). This release changes no opt-in capability, installer flag, or host surface.
>
> **v2.2.5 RELEASED (2026-08-23)** -- field-repair cycle [v2.2.0](v2/v2.2/plans/v2.2.0-runtime-repair-and-ux-overhaul.md) through [v2.2.5](v2/v2.2/plans/v2.2.5-first-successful-generation.md). Package **2.2.5**. Known-gaps [in-progress](v2/v2.2/known-gaps.md) (DF-2 packaged Explorer, DF-4 live GPU generate). Cycle: sidecar start, honest catalog, Chatbot-first, alias-fold ids, fail-closed diffusion, Settings Models installer parity, chat explorer chrome, Hub latest. This release changes no opt-in capability, installer flag, or host surface.
>
> **v2.1.0 RELEASED (2026-08-20)** -- [open-local-ai-wave](v2/v2.1/plans/v2.1.0-adoption-open-local-ai-wave.md) Phases 1-7 plus known-gaps sweeps. Package **2.1.0**. Known-gaps [finalized](v2/v2.1/known-gaps.md) (6 open DF remain hardware/watch/live-GPU). Cycle: Muse Glimmer + Nemotron Lightning catalog/harness, cheap-first routing, studio provenance + queue, vision budgets + SAM2, Unsloth Core fine-tuning, signed audit log + JSON CLI + VRAM knobs. Develop follow-up closed code-completeable rows (unload, AgentLoop routing, SAM picker, MaskEditor, frame comments, installer Unsloth checkbox, golden-eval adapter). No retag.
>
> Opt-in: Settings > Fine-tuning Provision (`opt_in=False`); JSON CLI loopback on sidecar `sync()` (`/v1` still off until Local API); Settings > Security parse_document checkbox.

> **v2.0.0 RELEASED (2026-08-20)** -- [governed-autonomy-multimodal](v2/v2.0/plans/v2.0.0-adoption-governed-autonomy-multimodal.md) Phases 1-6. Convergence of v1.18.0 + v1.19.0/.1/.2 + this plan. Package **2.0.0**. Known-gaps [in-progress](v2/v2.0/known-gaps.md). Gates: root **5160 passed / 12 skipped / 0 failed**, coverage 88.28% / 83.84% / 90.96%, desktop **1036 passed**, python runtimes **231 passed**, lint + `tsc -b` + docs-layout clean.
>
> **v1.20.0 RELEASED (2026-08-19)** -- all 5 phases of [v1.20.0-adoption-docling.md](v1/v1.20/plans/v1.20.0-adoption-docling.md) landed on `develop`; package.json / CHANGELOG / README cut as **v1.20.0**. Known-gaps [in-progress](v1/v1.20/known-gaps.md) (v1.20.0: 6 DF). Cycle: wire `parse_document`, magic-byte Office ingest, Chat and Coding attach, Docling bake-off deferred.
> - **P1** sidecar + VS Code composition-root wiring (LSO.P4.B / LSO.P4.C).
> - **P2** format router + native DOCX/PPTX/XLSX + Chat `DOCUMENT_ACCEPT`.
> - **P3** Coding composer attach (parse-then-show; agent tool stays CONFIRM).
> - **P4** RapidOCR library smoke; A4 DEFER (DF-5).
> - **P5** keep `runtimes/ocr/` (DF-6); CI already covers Office wheels and desktop shell tests.
>
> Opt-in: `nexus.coding.parseDocument.enabled` / `NEXUS_PARSE_DOCUMENT` (agent tool only; attach UI is not this flag).

> **v1.19.2 RELEASED (2026-08-19)** -- Phase 1 of [v1.19.2-adoption-catalog-and-model-expansion.md](v1/v1.19/plans/v1.19.2-adoption-catalog-and-model-expansion.md) landed on `develop`; package.json / CHANGELOG / README cut as **v1.19.2**. Known-gaps [in-progress](v1/v1.19/known-gaps.md) (v1.19.2: 4 DF; file stays open for v2.0.0 ingest). Cycle: modalities + audioConditioning, official-only weight variants, Hermes 3, Inkling-Small patient-tier GGUF, calibrated patient-tier copy.
> - **P1** schema + puller variants + Hermes family/harness + Inkling GGUF + RAM presets + skip-if-absent determinism. Live Hermes generate is DF-1. GGUF multimodal is DF-2. AgentLoop Gemma XML is DF-3. Offload adapter not bundled is DF-4.
>
> Gates: root **5076 passed / 11 skipped / 0 failed** (466 files), coverage 87.77% / 83.95% / 91.35%, lint + `tsc -b` clean. Opt-in: `NEXUS_WEIGHTS_VARIANT` (official variant pick); `nexus.llm.patientTier.ramPreset` (copy only).

> **v1.19.1 RELEASED (2026-08-19)** -- both phases of [v1.19.1-adoption-agent-loop-and-guardrail-hardening.md](v1/v1.19/plans/v1.19.1-adoption-agent-loop-and-guardrail-hardening.md) landed on `develop`; package.json / CHANGELOG / README cut as **v1.19.1**. Known-gaps [in-progress](v1/v1.19/known-gaps.md) (v1.19.1: 5 DF; file stays open for v1.19.2). Cycle: Hub skill-native wins + agent-loop / guardrail hardening.
> - **P1** Hub grounded-citation, persona-card, avatar-prep / transcript-reasoning. Chat persona field is DF-1. Hub merge/sync is DF-2.
> - **P2** HARD_DENIALS, LoopGuards, edit noop / near-miss / spill scrub, user-message compaction tail, Strict/Standard/Unattended posture, origin screening, DNS pin, watch_path/hash_file, ToolPromptAssembler.
>
> Gates: root **5054 passed / 11 skipped / 0 failed** (466 files), coverage 87.71% / 84.02% / 91.27%, lint + `tsc -b` clean. Opt-in: `nexus.coding.securityPosture` (default `standard`; Unattended is not no-floor).
>
> **v1.19.0 RELEASED (2026-08-19)** -- all 4 phases of [v1.19.0-adoption-liquid-lfm-agentic.md](v1/v1.19/plans/v1.19.0-adoption-liquid-lfm-agentic.md) landed on `develop`; package.json / CHANGELOG / README cut as **v1.19.0**. Known-gaps [reconciled, in-progress](v1/v1.19/known-gaps.md) (8 open DF; file stays open for v1.19.1 / v1.19.2). Cycle: LFM2.5-2.6B low-VRAM Agentic catalog + license label, harness profile, 8B-A1B bake-off declined.
> - **P1** `lfm2.5:2.6b` catalog entry, cpu/8 agentic defaults, LFM Open License v1.0 use-restriction label (ungated).
> - **P2** `lfm-agentic` harness profile, pythonic parser, 128K context, Coding ModelCatalog row (DF-1/4/5 closed; DF-6 AgentLoop Gemma XML).
> - **P3** 8B-A1B **DECLINE** (win not demonstrated). Watchlist: VL variants, PII-extract Nano (DF-7/8/9).
> - **P4** layout audit (no moves), known-gaps reconcile, installer tests path-filtered, desktop vitest on develop CI.
>
> Gates: root **4947 passed / 11 skipped / 0 failed** (459 files), desktop **971 passed** (112 files), installer pytest green, lint + `tsc -b` clean. This release changes no opt-in capability.

> **v1.18.0 RELEASED (2026-08-17)** -- all 7 phases of [v1.18.0-adoption-agent-harness-and-governance.md](v1/v1.18/plans/v1.18.0-adoption-agent-harness-and-governance.md) landed on `develop`; package.json / CHANGELOG / README cut as **v1.18.0**. Known-gaps [finalized](v1/v1.18/known-gaps.md) (14 open DF). Cycle: skill-native Hub mappings + llama.cpp recipe, live harness selector, catalog/MCP governance, ask inbox + scheduler, loopback ACP, OS process sandbox.
> - **P1** Hub `agent-presets` / `browser-testing-with-devtools` coverage notes + llama.cpp loopback recipe.
> - **P2** live `HarnessSelector` overlay behind `nexus.coding.harnessSelector.enabled` (default off) + `/harness`.
> - **P3** `toolCallingVerified`, MoE active/total params, tightens-only MCP tool deny.
> - **P4** persistent ask inbox + local scheduler (no auto-approve; DF-9 closed).
> - **P5** ACP JSON-RPC on the shared serving loopback listener (`nexus.acp.enabled`).
> - **P6** `run_terminal` OS sandbox (`nexus.coding.execSandbox`; Windows `partial` = DF-11).
> - **P7** layout/known-gaps/CI path-filter; then `/update release`.
>
> Gates: root **4926 passed / 11 skipped / 0 failed** (459 files), coverage 87.61% / 84.09% / 91.14%, lint + `tsc -b` clean. Desktop **967 passed / 0 failed** (112 files). Tag / push / GitHub Release stay behind confirmation.
>
> **v1.17.0 RELEASED (2026-08-16)** -- all 6 phases landed on `develop`; package.json / CHANGELOG / README cut as **v1.17.0**. Plan: [v1.17.0-adoption-ui-motion-identity.md](v1/v1.17/plans/v1.17.0-adoption-ui-motion-identity.md) - [known-gaps](v1/v1.17/known-gaps.md) (finalized). Cycle: reverse-engineer thinking-orbs / border-beam / metal-fx into an internal motion system (no new npm deps).
> - **P1** motion tokens, centralized `prefers-reduced-motion` (halt, not slow), recede-when-active primitive.
> - **P2** agent-state Canvas orbs on coding / chat / image / video / model dock / retained generation canvas.
> - **P3** surface-liveness beam on composers, idle dock, generation-canvas frame.
> - **P4** hero-action metal ring on send, Generate, and New session (cap 3 WebGL instances).
> - **P5** one winner per surface (`orb > metal > beam > aurora`); recede once per group.
> - **P6** close-out: layout clean, gaps reconciled (incl. gigatoken N5 watch), CI verified. Gates: desktop **916 passed / 0 failed** (106 files), coverage 92.92% / 86.2% / 85.08%, lint + typecheck + docs-layout + naming clean.
>
> **Known follow-ups:** on-device GPU/visual pass (DF-8), Tailwind `@theme` compile (DF-1), installer motion (DF-3). Tag / push / GitHub Release stay behind confirmation gates.

> **v1.16.0 RELEASED (2026-08-16)** -- all 6 phases landed on `develop`; package.json / CHANGELOG / README cut as **v1.16.0**. Plan: [v1.16.0-adoption-local-serving-and-ocr.md](v1/v1.16/plans/v1.16.0-adoption-local-serving-and-ocr.md) - [known-gaps](v1/v1.16/known-gaps.md) (finalized). Cycle: local serving gateway + per-model analytics + document OCR + parse_document tool + MLX-via-adapters docs + model-library UX.
> - **P1** opt-in loopback OpenAI/Anthropic gateway in front of installed models (auth token, bind guard).
> - **P2** per-model tokens/sec, TTFT, memory on the Traces panel (no outbound calls).
> - **P3** RapidOCR (portable) + Unlimited-OCR (NVIDIA) in the catalog; sidecar parse via `core/documents`.
> - **P4** `parse_document` tool + optional memory ingest (tested; composition-root wiring is LSO.P4.B/C).
> - **P5** MLX how-to via `nexus.llm.localAdapters` (no bundled runtime) + Models page search/tier-fit + Chat/Coding quick switcher.
> - **P6** close-out: layout clean, gaps reconciled, CI verified. Gates: root **4813 passed / 0 failed** (434 files), desktop **824 passed** (95 files), Python runtimes **196 passed**, tsc + eslint + architecture (0 errors / 10 warnings) + docs-layout + naming clean.
>
> **Known follow-ups:** on-device gateway/OCR/MLX smokes (LSO.P1.C, LSO.P3.C, LSO.P5.A), wire `parse_document` at the composition root (LSO.P4.B), HF `sha256` pin rotation (LSO.P3.A / IRSC.P4.B). Tag / push / GitHub Release stay behind `/update release` confirmation gates.

> **v1.15.0 COMPLETE (2026-08-06) -- tagged `v1.15.0` on main 2026-08-11.** Plan: [v1.15.0-installer-registry-fixes-and-studio-chat.md](v1/v1.15/plans/v1.15.0-installer-registry-fixes-and-studio-chat.md) - [known-gaps](v1/v1.15/known-gaps.md) (finalized). Cycle driven by six user-reported defects from a v2.4.0 reinstall, **all resolved**:
> - **P1** (Issue 4) window controls un-buried (`.nexus-titlebar` stacking context above the opaque backdrop) + `maximized: true`.
> - **P2** (Issue 1) installer relaunch starts at Welcome: a terminal `state.json` no longer redirects cold launches (`interpret_startup` -> `DECISION_FRESH`), `CompletePage.on_finish` + both NSIS uninstallers clear it.
> - **P3** (Issue 2) the log's two failures were already fixed in-catalog, so the cycle added a content-invariant guard (`catalog_invariants` + `check-catalog.py` + FAIL-CLOSED spec + CI test) so a stale/regressed catalog cannot ship; plus clearer gated-token UX and a plain-language post-install summary + retry-failed-downloads.
> - **P4** (Issue 3) real `models.*` IPC on `NexusModelRegistry`, reconciled against Ollama `/api/tags` + the installer's `~/.nexus/models/weights/<id>/` tree, streaming in-app install, real `ipcModelsClient` wired in `App.tsx`. **Closes the long-tracked 5.P1.BB.**
> - **P5/P6** (Issue 5) Image Studio + Video Lab rebuilt as chat: mode tabs/sidebars gone, shared `MediaComposer` (+ button / drag-drop / paste), attachment-inferred intent, inline media in bubbles, parameters behind "Advanced settings", installed-only model selectors + "Get more models".
> - **P7** (Issue 6) extension activation made crash-proof (guarded memory subsystem + contained `activate()` + safe-mode fallbacks for every declared command/view), packaging hardened (parameterized Electron version, `package:quick` release-blocked), renamed to **Nexus Code**. The Claude Code-style UX rework (7.4) is deliberately deferred to its own plan (IRSC.P7.A).
> - **P8** close-out: layout clean, gaps reconciled, CI verified. Gates: root **4646 passed / 0 failed** (424 files), desktop **581 passed** (77 files), installer pytest green, tsc + eslint + ruff (touched files) + catalog guard clean, architecture 0 errors / 10 pre-existing warnings.
>
> **Known follow-ups:** the Claude Code-style extension UX (IRSC.P7.A) and an HF `sha256` pin rotation, which closes both the carried-forward ICR.P1.C/P2.C and the new IRSC.P4.B (in-app HF install digest verification).

> **Current state (2026-07-10):** New plan filed 2026-07-09 -- **v1.10.0 (`nexus-hub-consumption-rearchitecture`)** ([plan](v1/v1.10/plans/nexus-hub-consumption-rearchitecture.md), [known-gaps](v1/v1.10/known-gaps.md)) -- re-architects how Nexus-AI consumes the Nexus-Hub catalog: fetch the latest catalog into a single isolated subtree `~/.nexus-ai/catalog/` (read like `~/.claude/`), replacing the version-scoped `~/.nexus/skills/devai-hub/<tag>/` path + the broken bundled baseline; rename `DevAIHubSyncer` -> `NexusHubSyncer` with a subtree-scoped refresh, reroute all readers via the version-manifest `layout` map, add first-launch fetch + in-app update detection, one-shot migrate + guarded cleanup of the old cache (delete ONLY `~/.nexus/skills/devai-hub/`), scrub `DevAI`/`devai` naming, and canonicalize docs to Nexus-Hub's layout. App-data home consolidation (`~/.nexus/*` -> `~/.nexus-ai/*`) is a deliberately separate, tested follow-up plan. **Phase 1 (shared catalog-path + layout resolver, T001-T005) landed 2026-07-09** -- the pure resolver (`nexusAiHome`/`catalogRoot`/`hubLayoutDir` + frozen `HUB_LAYOUT` + `hubVersionManifestPath`) in [paths.ts](../core/storage/paths.ts) plus the deterministic `nexus-hub-version.json` I/O (`build`/`serialize`/`read`/`write`/`resolveHubLayout`) in the new fs-touching [hubVersionManifest.ts](../core/storage/hubVersionManifest.ts) (split out to preserve paths.ts's no-fs purity, a refinement of plan T003); subdir names resolve from the manifest `layout` map, never hardcoded. 24 new tests (byte-deterministic manifest, partial-layout fallback, paths.ts purity CI assertion) pass; storage suite 64/0; `tsc -b` clean; additive-only (no reader consumes it yet -- Phase 2/3). **Phase 2 (rename `DevAIHubSyncer` -> `NexusHubSyncer` + retarget syncer to `~/.nexus-ai/catalog/`, T006-T013) landed 2026-07-09** -- the syncer ([NexusHubSyncer.ts](../core/skills/NexusHubSyncer.ts)) now fetches into the single isolated catalog subtree (no `<tag>` subfolder, no ACTIVE pointer), keys its up-to-date short-circuit on the recorded `nexus-hub-version.json` version, writes that manifest deterministically (version from `.claude-plugin/plugin.json` else the tag), sparse-checkouts `["catalog", ".claude-plugin"]`, and scopes its destructive wipe/refetch to the catalog subtree via `assertScopedCatalogRoot` (a re-sync provably leaves sibling app data untouched). Every importer updated (bin CLI, AutoSync, SkillsReloader, SkillInstaller, ChatPanelBootstrap). T012 (the `"devai-hub"` provenance-token rename) is deferred to Phase 3 (coupled to the reader/loader reroute; `NHC.P2.A`); the old path helpers stay as transitional shims for the not-yet-rerouted readers (`NHC.P2.B`), so the syncer writes the new path while readers read the old one (inert -- nothing wired live yet). Gates: full root suite **4583 passed / 6 skipped / 0 failed**, `tsc -b` clean, eslint clean; rewritten NexusHubSyncer.test.ts (48 tests) incl. the adversarial subtree-scope test. **Phase 3 (reroute all readers to the catalog resolver, T014-T020) landed 2026-07-09** -- [ChatPanelBootstrap](../src/panels/ChatPanelBootstrap.ts) (rules/agents/commands), [SkillsReloader](../core/skills/SkillsReloader.ts) (now watches the `nexus-hub-version.json` sentinel instead of the ACTIVE pointer), and [codingBootstrap](../desktop/sidecar/src/runtime/codingBootstrap.ts) all read from `~/.nexus-ai/catalog/` via `hubLayoutDir` + the version-manifest `layout` map (inert when unsynced); new [hubMcpRegistry.ts](../modules/coding/mcp/hubMcpRegistry.ts) reads + policy-filters the Hub MCP registry (inert, never auto-connects); the now-unused legacy Hub path helpers were removed from NexusHubSyncer.ts (resolving `NHC.P2.B`; `defaultSkillsRoot` kept as the app-data user-skills root). SkillLoader is unchanged by design (Hub skills reach the runtime via the command/agent/rule loaders, not SkillLoader; `NHC.P3.A`). The `"devai-hub"` provenance-token rename stays deferred to Phase 7 (`NHC.P2.A`); the Hub mcp reader is not yet consumed by the live MCP flow (`NHC.P3.B`, Phase 6). Gates: full root suite **4580 passed / 6 skipped / 0 failed**, root `tsc -b` + desktop `tsc --noEmit` + eslint clean (desktop's stricter `noUnusedParameters` caught a dead param), desktop codingBootstrap 7 passed. **Phase 4 (rename AutoSync + one-shot migration + guarded cleanup, T021-T025) landed 2026-07-10** -- [NexusHubAutoSync.ts](../core/skills/NexusHubAutoSync.ts) (task id + setting key move to the `nexus-hub` namespace; a read-migration carries an existing `autoSync.devai-hub` opt-in forward to `autoSync.nexus-hub`), and new [migrateLegacyCatalog.ts](../core/skills/migrateLegacyCatalog.ts) does a guarded, idempotent one-shot cleanup of the legacy `~/.nexus/skills/devai-hub/` cache (removes ONLY that dir + an empty `skills/`, refuses empty/root homes; an adversarial test proves `settings.json`/`mcp.json`/`models/`/`session-artifacts/`/`skills/user/` survive). Both run through `bootstrapCoding` (best-effort), which the live sidecar wires in Phase 6 (`NHC.P4.A`). Gates: full root suite **4588 passed / 6 skipped / 0 failed**, root + desktop `tsc` + eslint clean. **Phase 5 (remove the installer bundled-baseline redundancy, T026-T030) landed 2026-07-10** -- deleted the broken bundled-baseline path (placeholder `devai-hub-baseline.json` + `devai_hub_provisioner.py` + its test + the `devai-hub` dispatch-chain entry/import/factory + the `fetch-payload`/`versions.lock` pins), removed the `devai_hub_gb` disk row from `storage.py` (6 rows -> 5), dropped the baseline-tag steps from the 3 installer workflows + the build doc, and marked `nexus-installer.spec` as bundling no Hub payload (fetched at runtime). Installer pytest green (0 failed / 2 skipped), ruff clean on changed src+tests; two pre-existing `build/fetch-payload.py` findings + one `storage.py` `refresh()` nit are the installer's existing baseline (`NHC.P5.A`). The uncommitted v1.9.0 `storage.py` font tweak was isolated via a single-file stash and restored. **Phase 6 (live first-launch fetch + `skills.*` IPC + in-app update detection, T031-T034/T036) landed 2026-07-10** -- the live sidecar ([main.ts](../desktop/sidecar/src/main.ts)) now runs the P4 one-shot cleanup + fetches the catalog into `~/.nexus-ai/catalog/` on first launch (fire-and-forget, offline-safe); new `skills.sync`/`skills.status`/`skills.upstreamLatest` IPC ([protocol.ts](../desktop/sidecar/src/protocol.ts) + [handlers.ts](../desktop/sidecar/src/handlers.ts)) back a real [ipcSkillsClient](../desktop/src/pages/settings/ipcSkillsClient.ts) wired into the live SettingsPage, and [SkillsSettings](../desktop/src/pages/settings/SkillsSettings.tsx) grew an "update available" banner + "not yet synced" empty state. Deferred: T035 wire-enum rename -> Phase 7 (`NHC.P2.A`); full skill-management IPC (`NHC.P6.B`); the weekly idle auto-sync worker needs a sidecar idle loop (`NHC.P6.C`); hermetic handler tests need an injectable home/deps seam (`NHC.P6.D`). **Phase 7 (DevAI/devai naming scrub, T037-T040) landed 2026-07-10** -- renamed the load-bearing `"devai-hub"` provenance/namespace token to `"nexus-hub"` across core/modules/src/desktop (SkillCatalog/SkillAuditor/SkillRenderLine/SlashAutocomplete/Tracer/AgentLoop/IPC `z.enum`/slash namespaces) plus every asserting test, scrubbed live-concept prose (SkillInstaller message, SkillsSettings labels, panelData/mock demo ids, DoctorReport/LanguageRuleBuilder/ToolActivationContext comments), and added a dependency-free `check:naming` CI gate that fails if the quoted `"devai-hub"` enum value reappears in JS/TS source (allowlisting `migrateLegacyCatalog.ts` + the deferred `bin/nexus.mjs` audit reader); root 1029 + desktop 533 tests green, tsc/lint/gate clean. Only the two allowed exceptions remain: old-path/migration code and dated `renamed from` notes (plus installer negative-guards + `NHC.P3.C` bin reroute, still deferred). **Phase 8 FINAL (docs architecture refactor + known-gaps reconciliation + CI, T041-T048) landed 2026-07-11** -- canonicalized the docs tree to the version-first scheme (`docs/versions/v1/v1.<m>.0/` -> `docs/v1/v1.<m>/`; `docs/archive/versions/v0/...` -> `docs/archive/v0/...`; 328 files `git mv`'d, `versions/` wrappers dropped, single patch segment collapsed), recomputed every move-affected reference (verified flat vs the pre-move HEAD baseline: 3549 -> 3553 broken links, all pre-existing historical rot in frozen archives), folded v1.6's two plan ledgers into one `known-gaps.md`, wrote the docs-cleanup-report + refreshed `archive/README.md`, and added a dependency-free `check:docs-layout` CI gate. T042 (filename release-prefixing) + T044 (ADR->specs/policy) deferred as churn-without-a-canonical-target (`NHC.P8.A`/`NHC.P8.B`); `docs/adr/` kept in place. Root 4588 + desktop 533 tests green; `tsc`/lint/naming/docs-layout gates clean. **v1.10.0 plan COMPLETE (all 8 phases)**; next: release-readiness via `/update release`. Post-cycle installer hotfixes landed direct-to-main 2026-07-12 (extension no-console spawn + banner `5341392`; model-pull source.url resolution + no-hang `5411260`; VSIX bloat 1.66GB->149MB + PS5.1 build fixes `fdf9013`; installing-page interim redesign + installer dark dialog titlebars `5aa156c`; desktop process-wide dark mode `ec05830`). **NEXT CYCLE PLANNED - v1.11.0 Installer Overhaul** ([plan](v1/v1.11/plans/installer-overhaul.md), 8 phases, not started): P1 download-engine root-cause (ollama exit -1 no-output from frozen app) + catalog URL audit (gated SANA 401s) + pin rotation + parallel per-model progress; P2 clean-machine harness (Windows Sandbox + Docker Linux + macOS checklist); P3 dependency self-sufficiency (Ollama/Python/CUDA/VS Code from scratch); P4 embed the desktop app in NexusSetup.exe (kills the release-asset 404); P5 progress UX v2 (sub-bars only when >1 sub-step, per-model rows w/ speed+ETA, resizable logs, copy feedback); P6 mockup shell (sidebar nav during install, Models category-tab flow, header sizing fix); P7 FULL background continuation (tray + persistent engine + reattach); P8 mandatory final (refactor + gaps + CI/CD). Operator decisions locked: open-mirror audit / embed desktop / Sandbox+Docker harness / full background mode. COORD: save the UI mockup to docs/v1/v1.11/design/installer-mockup.png before P5. **P1 (download engine, T101-T106) landed 2026-07-13** -- root cause PROVEN: a cp1252 decode bomb (ollama's braille spinner byte 0x8f killed the text-mode reader ~1.1s into any pull; the swallowed UnicodeDecodeError faked an EOF and wait(10) terminated healthy downloads as exit -1). Shipped: indestructible byte-mode reader (utf-8-replace, 
+
 split, ANSI strip), no_window_kwargs() + 20-site no-console sweep (kills the flashing command prompts), server-aware model step (managed hidden `ollama serve`; also fixed _verify_ollama's start-then-kill bug), live catalog audit (6 broken entries re-pointed to verified open paths -> 32/38 OK; 6 gated non-defaults documented as IO.P1.A), 26 HF pins rotated + build warning, parallel downloads (pool of 3) + per-model ModelProgress events/Qt signals for the P5 UI. Acceptance: the exact field-failure model (nomic-embed-text) pulls successfully from a windowed process. Installer suite + root registry suite green. Discovered for P3: clean-machine Ollama install broken (v0.3.6 + all-zero checksum pin, IO.P1.B). **P2 (clean-machine harness, T201-T205) landed 2026-07-14** -- one-command install verification on dependency-free machines: `--headless-smoke <profile.json>` + `--smoke-output` (versioned nexus-smoke-result/v1 contract; BOM-tolerant profiles), Windows Sandbox harness (wsb template + in-sandbox bootstrap with windowed-exe wait semantics + run-sandbox-test.ps1 host runner), Docker Linux harness (no-deps python:3.12-slim; headless path is Qt-free), macOS manual checklist, 14 harness self-tests + runbook README. Validated: source + FROZEN-exe contract runs green locally; runner degradation paths verified. First real sandbox/docker runs = operator actions (Sandbox feature not enabled on dev host; Docker absent) -- expected to reproduce IO.P1.B (IO.P2.A). **P3 (dependency self-sufficiency, T301-T304) landed 2026-07-14** -- audit table produced (venv=stub since v0.4.0; payload provisioner chain is UNWIRED scaffolding with zero live callers, IO.P3.A; GPU-less path code-verified clean); IO.P1.B CLOSED: Ollama pins are real (v0.32.0 + GitHub asset digests, Authenticode kept fail-closed) and Linux moved off unpinnable install.sh to the immutable tar.zst release asset installed user-local (no sudo) with PATH prepend + path-traversal-filtered zstd extraction; VS Code absent is now a clean SKIP with guidance (state.skipped_steps); T303 structured failures (plain-language summary + suggested action per step) recorded on state and included in the smoke result; IO.P1.C closed (metal/rocm/macos/linux provisioner spawns unified); build/check-ollama-pin.py advisory. Full suite + live headless-smoke regression green. Sandbox end-to-end gate pending operator + P4 (IO.P3.B). **P4 (embed the desktop app, T401-T404) landed 2026-07-14** -- the desktop step's GitHub-release 404 is dead: build-windows.ps1 stages the Tauri NSIS bundle (version-checked against package.json, fail-closed if absent) + a sha256 manifest into the PyInstaller payload; desktop_provisioner installs from the embedded payload (manifest-verified fail-closed, silent NSIS, health check; zero network for the step; override seam kept) and the entire release-fetch path (SHA256SUMS contract, resume downloader) is deleted; new --check-desktop-payload diagnostic wired into the packaging smoke (now 5 probes); T403 VSIX renamed to nexus-coding-<ver>.vsix (NAME.P1.A artifact remnant closed). Bundle is only ~1.7 MB (RISK.2 moot; NSIS stub may bootstrap WebView2 online -- IO.P4.B sandbox-verify). mac/linux staging deferred (IO.P4.A). Full suite + mypy/ruff green. **P5 (installing-page progress UX v2, T501-T506) landed 2026-07-16** -- phase_group v2 per operator feedback: ONE main accent bar per phase with thin translucent sub-bars ONLY for multi-step phases (T501); dynamic per-model rows (name + sub-bar + 'X GB / Y GB (Z%) - S MB/s - ETA' + Waiting/Downloading/Done/Failed:reason) driven by the P1 telemetry signals with rows pre-created from the selection (T502); log area drag-resizable via a visible grip (clamped 80-480px, T503); copy button flips to check+'Copied' for 1.5s (T504); failed steps auto-expand and render the T303 summary+suggestion block above the log actions, failed models auto-expand with inline reasons (T505); +15 widget tests incl. formatting helpers (T506). Full suite + ruff/mypy green. Visual pass = operator on the rebuilt exe (IO.P5.B). **P6 (mockup shell, T601-T605) landed 2026-07-16** -- adopted the mockup layout: window restructured into a `[title bar]` over a `[sidebar | content]` split. New left Sidebar is the primary nav surface (brand block + one row per section with done/current/pending/locked state icons + a viewed-section accent bar + a 'Need help?' docs block); the top stepper is kept and 'Step X of Y' + current-step name moved to the content top-right (T601). Free navigation during install (T602): the installing page emits started/finished, the shell locks the Models page read-only (set_interactive) and lets the sidebar review any earlier section WITHOUT stopping/restarting the run (idempotent start guard), stepper pinned to Installing, Complete gated until finish, footer inert mid-install. Models category flow (T603): a category is decided by a selection, an explicit 'Skip this category', or emptiness; validate() blocks + jumps to the first undecided tab; decided tabs get a check prefix and a category_progress signal drives 'Models (X/Y)' on the sidebar. Header root-cause fixed (T604): the 44px wordmark rendered tiny because the global QSS `QWidget{font-size:16px}` overrode setFont -- moved the size into the label stylesheet (hero 28px > 24px step counter), logo trimmed 120->84. +23 shell tests (T605); full suite + ruff green, mypy src at baseline, changed-surface coverage 86%. Visual pass = operator (IO.P6.B); gated-models picker decision carried to P8 (IO.P6.E). Next: P7 (full background continuation: persistent engine state, tray mode, reattach, crash-safe resume). Gates: desktop `tsc` + eslint clean, desktop suite **533 passed / 0 failed** (+10); no root/core files changed. Prior: A new v1.9.0 follow-up plan -- **installer + app UI rework** (type scale + legibility, static logo to kill the float-lag, taskbar-icon frozen-path fix, per-provider model colors, plain-language shared catalog copy, modern scrollbars + checkbox, an on-brand aurora generation animation in Image Studio + Video Lab, and a chat disclaimer) -- was filed 2026-07-07 ([plan](v1/v1.9/plans/installer-and-app-ui-rework.md)); **Phase 1 (shared design foundations, T001-T004) landed 2026-07-07** -- the installer type scale + weight tokens and the per-provider color palette (derived from `family`, no catalog-schema change) are in [constants.py](../scripts/installer/src/nexus_installer/constants.py), and the aurora animation spec + plain-language model-copy template are ratified in [ui-rework-design.md](v1/v1.9/ui-rework-design.md) (installer suite 657 passed / 2 skipped / 0 failed, +6 tests). **Phase 2 (shared catalog.json plain-language copy rewrite, T005-T007) landed 2026-07-07** -- all 34 user-facing model descriptions rewritten to the T004 template (byte-preserving), the 3 readers stay green, +1 regression guard. **Phase 3 (installer typography + hierarchy sweep, T008-T011) landed 2026-07-07** -- ~90 inline font sizes replaced with the FS_* scale tokens + pageTitle/sectionHead QSS classes (grep gate clean), installer suite 658/2/0. **Phase 4 (logo de-lag + two-tone wordmark + stepper legibility, T012-T017) landed 2026-07-07** -- FloatingLogo retired for a static StaticLogo, guide two-tone header wordmark, readable 14px stepper labels (STEP_BAR_HEIGHT 112), installer suite 656/2/0. **Phase 5 (installer chrome: frozen-path icon + scrollbars + checkbox, T018-T021) landed 2026-07-08** -- `_MEIPASS`-first icon resolver (frozen build proven: NexusSetup.exe boots + icon.ico bundled), pill scrollbars, custom-painted ModelCheckBox, installer suite 670/2/0. **Phase 6 (Models page: per-provider color + plain-language cards + intro copy, T022-T025) landed 2026-07-09** -- cards colored by provider (one color per model across tabs), description-led card layout with a full-width "Best for", per-provider legend, installer suite 672/2/0. **Phase 7 (whole-app copy/readability pass + end-to-end QA, T026-T028) landed 2026-07-09** -- jargon copy simplified across the pages, consistency pass green, installer PR (Phases 1-7) code-complete; on-device DoD 1-8 walk-through consolidated as `UIR.P7.A`. **Phase 8 (app aurora generation animation, T029-T032) landed 2026-07-09** -- reusable `GenerationCanvas` (drifting blurred aurora + shimmer, reduced-motion aware) mounted in Image Studio + Video Lab, progress-coupled; desktop suite 520 passed. **Phase 9 FINAL (app chat disclaimer + logo/icon parity + app QA, T033-T036) landed 2026-07-09** -- accuracy disclaimer under the shared composer, window-icon.png now regenerable, Dashboard logo confirmed transform-only; desktop suite 521 passed. **The `installer-and-app-ui-rework` cycle is COMPLETE across all 9 phases** -- code-complete + green; remaining work is the operator on-device visual QA (`UIR.P7.A`/`P8.A`/`P9.A`) and the merge-to-main release (semantic-release cuts the version + CHANGELOG + tag). the live development line is the **v1.x cycle series** summarized in the "v1.x line" section near the end of this file. The detailed per-task boards below (v0.1.0 - v0.7.0) are retained as history and predate the rename. Latest work: **v1.9.0 (`installer-and-app-experience-overhaul`) Phase 6 FINAL landed 2026-07-04** -- cross-platform rehearsal + docs + close-out (T601-T603): the v1.9.0 cycle is **COMPLETE across all 6 phases**. Phase 6 re-proved the Windows single-artifact build a second time (a clean-tree PyInstaller onefile rebuild produced exactly one `dist/NexusSetup.exe` ~72.1 MB; `smoke-windows-exe.ps1` all-green -- single artifact, no leftover `nexus-installer.exe`, `--version` + `--check-registry` exit 0 so the bundled `catalog.json`/`recommended.json` resolve inside the frozen bundle) and re-ran the installer suite (**651 passed / 2 skipped / 0 failed**, 653 collected). Every open known gap was dispositioned: the Windows build leg of `IAE.P1.B` is re-closed, and the remainder are environmentally blocked in this headless single-OS no-egress sandbox -- the 3-OS on-device visual/behavioral rehearsal (no mac/Linux hardware, no clean VM, no GUI surface: `IAE.P1.B` mac/Linux+VM, `IAE.P2.B`, `IAE.P3.A`, `IAE.P5.A`, `IAE.P5.B`) and the post-freeze CI legs (Actions freeze until 2026-08-01, no HF egress: `IAE.P4.C`, `IAE.P5.D`, T602) -- so they stay recorded operator/dispatch rehearsals (mirroring v1.8.0 `OSI006.P6.A/C`). No feature code changed in Phase 6 (verification + close-out only); whole-plan DoD (Section 0's 7 observables) met locally + by construction; version tag + CHANGELOG cut on merge to `main` via semantic-release. [plan](v1/v1.9/plans/installer-and-app-experience-overhaul.md), [known-gaps](v1/v1.9/known-gaps.md), [phase history](v1/v1.9/development/history/2026-07_phase-6-rehearsal-and-closeout.md). Prior: **v1.9.0 (`installer-and-app-experience-overhaul`) Phase 5 landed 2026-07-04** -- desktop app (Nexus AI Studio) full UI/UX overhaul (T501-T505): the Tauri app now reads as one product with the rebuilt installer. The window is **frameless** (`decorations: false`) behind a custom React [TitleBar](../desktop/src/components/TitleBar.tsx) (transparent glowing mark + "Nexus AI Studio" wordmark + minimize/maximize-restore/close; `data-tauri-drag-region` move + double-click-maximize) whose controls route through a Tauri-runtime-safe [windowControls.ts](../desktop/src/lib/windowControls.ts) wrapper (no-op in Vitest/`dev:web`); the app's first Tauri [capabilities/default.json](../desktop/src-tauri/capabilities/default.json) grants the `core:window` surface. The Phase 2 [`<ConstellationBackground/>`](../desktop/src/components/ConstellationBackground.tsx) mounts at 0.5 opacity over a fixed radial-glow layer behind the whole shell (foreground at `z-index:1`; the four transparent-root pillar pages inherit the backdrop). Rebrand complete: `productName` + window title + [index.html](../desktop/index.html) + [Sidebar](../desktop/src/components/Sidebar.tsx) (translucent glass, signature-gradient wordmark, glowing mark) + [Dashboard](../desktop/src/pages/Dashboard.tsx) (animated `FloatingLogo` hero + gradient heading) all read "Nexus AI Studio" (identifier `ai.nexus.shell` kept). The cohesive restyle uses the standardized inline-token + `globals.css` system (Tailwind v4 is not an installed dep; not introduced under the freeze); pillar-page internals keep their working layouts by the "cohesive restyle, not a feature rewrite" cut-line. Also closes `IAE.P4.B` (desktop DTO mirror gained `"audio"` + an Audio filter/icon in [ModelsSettings](../desktop/src/pages/settings/ModelsSettings.tsx)) and the desktop half of `NAME.P1.A`. Gates: desktop Vitest **515 passed / 0 failed** (+19); coverage lines 93.83% / branches 85.51% / functions 83.43% (>= 80/70/80) with TitleBar 100%, Sidebar/Dashboard 100% lines; `tsc --noEmit` + eslint clean; `cargo check` + `cargo clippy` clean; `release.yml` `desktop-bundle` staging is wildcard-based so the spaced `productName` does not break upload globs. New gaps `IAE.P5.A` (frameless per-OS behavior -> Phase 6), `IAE.P5.B` (edge-resize with decorations off -> Phase 6), `IAE.P5.C` (pillar-page internal polish deferred), `IAE.P5.D` (spaced-productName bundle verification -> Phase 6) in [known-gaps](v1/v1.9/known-gaps.md); [phase history](v1/v1.9/development/history/2026-07_phase-5-desktop-app-overhaul.md). Prior: **v1.9.0 (`installer-and-app-experience-overhaul`) Phase 4 landed 2026-07-04** -- model selector redesign + metadata + Gemma-4-agentic + audio pillar (T401-T407): the installer catalog is now scannable and complete. The catalog schema gains `origin` (publisher country) + an `agentic` capability flag + an optional `guardrails` label (installer derives Uncensored / Safety-tuned / N/A), and `"audio"` becomes a valid `ModelType` ([catalog.ts](../core/registry/catalog.ts), followed through `ModelStorage.ts` + `NexusModelRegistry.ts`); origin + agentic are populated across all 34 user-facing entries. The [model cards](../scripts/installer/pyqt/src/nexus_installer/pages/typed_catalog.py) are rebuilt to a compact chip row (Origin, Best-at, Agentic yes/no, Guardrails, context, license) + a single status badge (**Required** > incompatibility > **Recommended** > **Compatible**), a prominent disk accent, larger cyan selection boxes, a locked-on **Required** state for `nomic-embed-text`, and a **Refresh Models** footer. The "Agentic Coding" tab is renamed **Agentic** and now lists agentic-capable chat models (the Gemma 4 family) ranked Gemma-first; [recommended.json](../core/registry/recommended.json) + [tier_defaults.py](../scripts/installer/pyqt/src/nexus_installer/tier_defaults.py) make the agentic default a fitting Gemma 4 variant per tier, so one Gemma covers both chat and agentic (the coder is the in-list fallback). The **Audio** pillar is curated for the first time -- faster-whisper (STT, MIT) + Kokoro (TTS, Apache-2.0) default on every tier (CPU-capable), with Piper / MusicGen / Stable Audio Open opt-in. The legacy `ModelSelectionPage`/`RecommendedModelsPage` + the review `_MODEL_SIZES` table are removed (closes `OSI004.P4.D`). Gates: installer **651 passed / 2 skipped / 0 failed** (typed_catalog 96% / tier_defaults 94% / review 94% lines); root Vitest **4573 passed / 6 skipped / 0 failed** (+4); `tsc -b` + eslint + ruff clean. New gaps `IAE.P4.A` (audio runtime not implemented -- download-only), `IAE.P4.B` (desktop DTO mirror needs `"audio"` -> Phase 5), `IAE.P4.C` (audio weights pins are placeholders -> Phase 6) in [known-gaps](v1/v1.9/known-gaps.md); [phase history](v1/v1.9/development/history/2026-07_phase-4-model-selector-metadata-audio.md). Prior: **v1.9.0 (`installer-and-app-experience-overhaul`) Phase 3 landed 2026-07-04** -- installer visual overhaul + naming + NexusAI path (T301-T307): the PyQt wizard now matches the north-star guide and finishes the installer rebrand. The window is **frameless** (`Qt.FramelessWindowHint`) behind a custom [TitleBar](../scripts/installer/pyqt/src/nexus_installer/widgets/title_bar.py) (transparent glowing mark + "Nexus AI Studio" wordmark + minimize/maximize-restore/close; drag-move via `startSystemMove` + manual fallback; double-click-maximize; bottom-corner `QSizeGrip`s), with a `NEXUS_NATIVE_TITLEBAR` / `frameless=False` fallback to native decorations. A new [BackgroundWidget](../scripts/installer/pyqt/src/nexus_installer/widgets/background.py) paints the guide's two radial-glow pools fading into `BG_DEEP` and hosts the Phase 2 `ConstellationBackground` at 0.55 opacity behind the (now-transparent) content band -- the chrome bands + step indicator stay opaque for readability; `resolve_reduced_motion()` wires the `NEXUS_REDUCED_MOTION` env var **and** the Windows `SPI_GETCLIENTAREAANIMATION` query (partially closes `IAE.P2.A`). The [header](../scripts/installer/pyqt/src/nexus_installer/widgets/header.py) + [welcome](../scripts/installer/pyqt/src/nexus_installer/pages/welcome.py) hero swap the black-box icon for the `FloatingLogo` primitive, and the [step indicator](../scripts/installer/pyqt/src/nexus_installer/widgets/step_indicator.py) gets glowing completed dots + a highlighted current dot. Rebrand complete: window/taskbar title, `QApplication` name/displayName, argparse description, and `--version` (`nexus-ai-studio-installer`) all read "Nexus AI Studio"; default install path is `NexusAI` on every OS ([installer_state.py](../scripts/installer/pyqt/src/nexus_installer/installer_state.py)); the "Gemma model" callout is "Nexus models"; a grep confirms zero user-visible Gemma/old-name strings in the wizard UI (remaining `gemma-code` refs are intentional compat + the Phase 4 legacy-page removal). Gates: installer suite **671 passed / 2 skipped / 0 failed** (+39); new/changed widget coverage title_bar 91% / background 97% / header 100% / step_indicator 99% / window 83% lines; ruff check + format clean on changed files; offscreen full-wizard composition smoke green. No TS/desktop change (app parity is Phase 5). New gaps `IAE.P3.A` (frameless per-OS behavior -> Phase 6), `IAE.P3.B` (mac/Linux reduced-motion native query), `IAE.P3.C` (Inter/JetBrains Mono TTF bundling deferred, supersedes `OSI005.P5.B`) in [known-gaps](v1/v1.9/known-gaps.md); [phase history](v1/v1.9/development/history/2026-07_phase-3-installer-visual-overhaul.md). Prior: **v1.9.0 (`installer-and-app-experience-overhaul`) Phase 2 landed 2026-07-04** -- shared brand foundation: icons + glow tokens + constellation/float-logo primitives (T201-T204). The v1.8.0 icon set was opaque black (the logo read as a black box in the header/taskbar/dock) even though the designer source was transparent. [generate-icons.py](../scripts/desktop/generate-icons.py) already resized straight from the transparent source (the black icons were stale), so a superellipse (squircle) alpha mask (`_superellipse_alpha_mask` per-row span fill, supersampled + LANCZOS-smoothed + cached; `_apply_rounded_corners` multiplies the mask into existing alpha) was added and `render_square(..., rounded=True)` now rounds every frame -- re-ran it, and `assets/icon.{png,ico,svg}` + the whole `desktop/src-tauri/icons/` set are now transparent + rounded (corner alpha 0, opaque ratio ~0.19 vs. the previous 1.00). The guide's glow layer was added additively to both palettes ([tokens.css](../desktop/src/styles/tokens.css) `--bg-deep`/`--glow-cyan`/`--glow-sm|md|lg`/`--grad-signature`/`--bg-radial-glow`; installer [constants.py](../scripts/installer/pyqt/src/nexus_installer/constants.py) `BG_DEEP`/`CONSTELLATION_*`/`GLOW_*`/`SIGNATURE_GRADIENT_STOPS`/`RADIAL_GLOW_POOLS`) + [design-tokens.md](v1/v1.9/design-tokens.md). The reusable constellation + floating-glow-logo primitives shipped in both stacks: React [constellation.ts](../desktop/src/components/constellation.ts) (pure engine) + [`<ConstellationBackground/>`](../desktop/src/components/ConstellationBackground.tsx) + [FloatingLogo.tsx](../desktop/src/components/FloatingLogo.tsx) + `@keyframes nexus-float`; PyQt [constellation.py](../scripts/installer/pyqt/src/nexus_installer/widgets/constellation.py) + [float_logo.py](../scripts/installer/pyqt/src/nexus_installer/widgets/float_logo.py) -- both reduced-motion aware (React `matchMedia`; PyQt `NEXUS_REDUCED_MOTION`) and hidden-pause wired, node-count max(18,min(46,w/34)), dpr cap 2, `#38bdf8` links / `#7dd3fc` nodes. `Pillow>=10.0` added to installer dev deps so the T204 icon assertions run in CI. Gates: installer suite **632 passed / 2 skipped / 0 failed** (+41), new widgets 97% lines; desktop **496 passed / 0 failed** (+34), new files 100% lines (suite 93.75% / 85.31% / 82.96% >= the 80/80/70/80 gate); `tsc --noEmit` + eslint + ruff clean. New gaps `IAE.P2.A` (PyQt reduced-motion env-var signal -> wire in Phase 3) + `IAE.P2.B` (on-device icon rendering visual check -> Phase 6) in [known-gaps](v1/v1.9/known-gaps.md); the primitives are built + unit-tested but not yet mounted (consumption is Phase 3 T302/T303 + Phase 5 T501/T502, by design). [phase history](v1/v1.9/development/history/2026-07_phase-2-shared-brand-foundation.md). Prior: **v1.9.0 (`installer-and-app-experience-overhaul`) Phase 1 landed 2026-07-04** -- single-artifact installer build (drop NSIS) (T101-T105): the v1.8.0 two-installer experience is retired. The PyInstaller onefile IS the distributable now -- the Windows [spec](../scripts/installer/pyqt/build/nexus-installer.spec) `APP_NAME` becomes `NexusSetup` (mac `Nexus AI Studio Setup`, linux `nexus-setup`), so double-clicking the download opens exactly one modern window (no generic NSIS pre-wizard dialog). [build-windows.ps1](../scripts/installer/pyqt/build/build-windows.ps1) is rewritten from the 6-step PyInstaller+NSIS+hand-copy flow to a 4-step single-onefile flow writing straight to the repo-root `dist/NexusSetup.exe`; [build-macos.sh](../scripts/installer/pyqt/build/build-macos.sh) / [build-linux.sh](../scripts/installer/pyqt/build/build-linux.sh) follow the same one-artifact-to-one-location contract (`NexusSetup.dmg` / `NexusSetup-x86_64.AppImage`). The NSIS shell is retired to [scripts/installer/legacy/nexus-setup.nsi](../scripts/installer/legacy/nexus-setup.nsi) and the four workflows ([installer-build](../.github/workflows/installer-build.yml)/[-macos](../.github/workflows/installer-macos.yml)/[-linux](../.github/workflows/installer-linux.yml)/[release](../.github/workflows/release.yml)) upload the single repo-root `dist/` artifact (NSIS-verify + Fetch-payload + `include_payload` steps removed). [docs/install.md](install.md) documents the single-window flow + direct `NexusSetup.exe --headless` (no `/S /D=`) + no self-registered uninstaller/Start-menu shortcut. [smoke-windows-exe.ps1](../scripts/installer/pyqt/build/smoke-windows-exe.ps1) now asserts one artifact then boots the frozen exe. Local DoD proof: `dist/NexusSetup.exe` **65.3 MB** built end-to-end, smoke all-green (single artifact, no leftover `nexus-installer.exe`, `--version` + `--check-registry` exit 0); installer suite **591 passed / 2 skipped / 0 failed**; ruff clean on the changed test; no TS touched. New gaps `IAE.P1.A` (offline payload embed dropped -- NSIS-only, supersedes v1.8.0 `OSI006.P6.D`) + `IAE.P1.B` (mac/linux + clean-VM build proof -> Phase 6) in [known-gaps](v1/v1.9/known-gaps.md); [phase history](v1/v1.9/development/history/2026-07_phase-1-single-artifact-installer.md). Prior: **v1.8.0 (`one-shot-installer`) Phase 6 in-session legs landed 2026-07-03** -- the Windows .exe completion closes gap G5's buildable half (T601 + T605; T602/T603/T604 recorded as operator / post-freeze rehearsals `OSI006.P6.A/B/C`): [installer-build.yml](../.github/workflows/installer-build.yml) rewritten from the v1.0.0 TODO skeleton to the real dispatch-gated pipeline (NSIS verify -> pinned versions -> root-workspace VSIX build matching release.yml -> optional pin-rotation-blocked `include_payload` embed (`OSI006.P6.D`) -> build -> smoke -> upload); the stale v1.1.0 [nexus-setup.nsi](../scripts/installer/build/nsis/nexus-setup.nsi) rewritten as a slim per-user shell (extract wizard + finish-page launch + HKCU uninstall entry + silent extract-only mode + `~/.nexus` always preserved on silent uninstall; the unsupported `--verify-only` invocation and the misplaced `nexus://` / file-association registrations are gone) -- the exe is slim by design per the fetch-at-install-time operator decision; [build-windows.ps1](../scripts/installer/pyqt/build/build-windows.ps1) gains the makensis stage (spec wizard renamed `nexus-installer.exe`; NSIS owns `NexusSetup.exe` at release.yml's existing upload path) and the renamed-VSIX glob; new [smoke-windows-exe.ps1](../scripts/installer/pyqt/build/smoke-windows-exe.ps1) silent install/probe/uninstall round-trip; `installer-macos.yml` / `installer-linux.yml` aligned to the canonical spec-driven build scripts + root VSIX packaging (their hand-rolled PyInstaller bypassed the spec's datas; `cd extensions/nexus-coding` never existed; macOS DMG now re-created after stapling). **`OSI004.P4.C` closed**: new [registry_paths.py](../scripts/installer/pyqt/src/nexus_installer/registry_paths.py) (`sys._MEIPASS`-first resolver, 100% lines) backs the model router + typed catalog, the spec bundles `core/registry/{catalog,recommended}.json`, and the new `--check-registry` CLI diagnostic asserts them inside the frozen exe. **T605**: end-user [docs/install.md](install.md) (per-OS steps, SmartScreen/Gatekeeper unsigned click-throughs, FUSE/glibc caveats, SHA256SUMS verification, silent/headless usage) + README Quick Start pointer. Local DoD proof: `NexusSetup.exe` 64.7 MB built end-to-end on the dev box; smoke all-green (silent install exit 0 -> wizard `--version` + `--check-registry` exit 0 -> silent uninstall clean, HKCU + Start Menu verified). Gates: installer suite **590 passed / 2 skipped / 0 failed** (+26); ruff 0 new findings; `tsc -b` clean; root Vitest **4569 / 6 / 0** unchanged. The whole-plan DoD rides the recorded rehearsals in [known-gaps](v1/v1.8/known-gaps.md); [phase history](v1/v1.8/development/history/2026-07_phase-6-windows-exe-completion.md). Prior: **v1.8.0 Phase 5 landed 2026-07-03** -- the desktop-token restyle + per-phase progress UX closes gap G4 (T501-T503): the installer palette ([constants.py](../scripts/installer/pyqt/src/nexus_installer/constants.py)) becomes a direct port of the desktop app's [tokens.css](../desktop/src/styles/tokens.css) and the documented single palette source (bg `#0a0d14`/`#11151f`/`#181d2a` + elevated, four-tier fg incl. new `TEXT_BODY #d6dbe7`, white-alpha borders composited to solid `#191c22`/`#272a30`, lead accent -> chatbot cyan `#22d3ee`, module accents + `SECTION_ACCENTS`); [theme.py](../scripts/installer/pyqt/src/nexus_installer/theme.py) adds QTabWidget/QTabBar styling + dark-on-bright button/checkmark treatment (its mojibake comments are ASCII again); the typed catalog carries per-section accents (chat cyan / agentic magenta / image orange / video green on tab rules, Recommended pills, size labels, "Why this one" lines, checked checkboxes) and the `_ModelCard` boxed-pill stylesheet wart is fixed via a scoped `QWidget#modelCard` selector. The installing page ([installing.py](../scripts/installer/pyqt/src/nexus_installer/pages/installing.py)) regroups into **Dependencies -> VS Code Extension -> Models -> Nexus Desktop** phase cards (new [phase_group.py](../scripts/installer/pyqt/src/nexus_installer/widgets/phase_group.py): status icon, per-group bar, collapsible Details log; overall bar on top; active-group log routing; component-driven rebuild) over three new engine signals (`step_started` / `step_progress` / `step_failed`; existing signals unchanged; model + desktop stream real within-step progress). Welcome/complete polished to product quality: desktop-icon lockup + four pillar accent chips + real product copy (the stale "Gemma Code ... 5-15 minutes" text is gone), "Managing Nexus", `nexus-install.log`; the header brand mark's dead fixed-depth asset path (landed on nonexistent `scripts/assets/`) replaced with walk-up resolution. Eight before/after captures archived in [assets/2026-07_phase-5/](v1/v1.8/development/history/assets/2026-07_phase-5) (the after-installing capture is the DoD's four-groups-progressing proof; card bg pixel-verified `#181d2a`). Gates: installer suite **564 passed / 2 skipped / 0 failed** (+26); `phase_group` 96% / `engine/installer` 93% / `installing` 82% lines; ruff 0 new findings; `tsc -b` clean; root Vitest **4569 / 6 / 0** unchanged. New gaps `OSI005.P5.A/B` (dependency-step bars completion-quantized; Inter/JetBrains Mono faces not bundled) + dispositions on `OSI002.P2.D` (flow not reworked; standalone pages stay unwired -> Phase 6) and `OSI004.P4.D` (no page reuse confirmed; retirement rides `NAME.P1.A`) in [known-gaps](v1/v1.8/known-gaps.md); [phase history](v1/v1.8/development/history/2026-07_phase-5-desktop-restyle.md). Prior: **v1.8.0 Phase 4 landed 2026-07-03** -- catalog curation + selection UX closes gap G3 (T401-T404): the catalog schema gains `task` (chat / agentic / image / video / audio / embed) + per-model copy fields (`strengths[]`, `whyRecommended`, `differentiators`, `provenance`) typed and validated in [catalog.ts](../core/registry/catalog.ts) (invalid-task rejection; **`uncensored: true` without `provenance` throws**); the installer's Text tab splits into **Chat** and **Agentic Coding** sections ([typed_catalog.py](../scripts/installer/pyqt/src/nexus_installer/pages/typed_catalog.py), tabs Chat / Agentic Coding / Image / Video / Audio, embed under Chat) with the new copy rendered on the model cards; all 13 text-side entries carry curated copy (chat = gemma4 ladder by VRAM tier, agentic = qwen2.5-coder -> deepseek-coder-v2); four **uncensored image/video entries curated with web-verified license + provenance** (`juggernaut-xl-v9` CreativeML Open RAIL-M, `realvisxl-v5` OpenRAIL++, `wan2.1-t2v-1.3b` + `wan2.2-ti2v-5b` Apache-2.0; placeholder pins per the Phase 3 convention); [recommended.json](../core/registry/recommended.json) rewritten to a v2 **per-VRAM-tier defaults matrix** (cpu/8/12/16/24) with uncensored image + video selected by default on every GPU tier, resolved + fit-gated by the new pure [tier_defaults.py](../scripts/installer/pyqt/src/nexus_installer/tier_defaults.py) (chat + agentic guaranteed, image/video fit-gated without substitution, cpu never selects image/video, disk reserve honored); **`TypedCatalogPage` wired into the GUI chain** at step 4 ("Models"), replacing `ModelSelectionPage` and producing the section-ordered `selected_model_ids` + legacy `selected_model` chat pick + `selected_models_gb` (closes `OSI003.P3.D`; defaults recompute on show until the user touches a checkbox; the smoke also caught + fixed the page's latent `parents[5]` registry-path bug); review page renders the multi-selection. T404 matrix suite runs the REAL catalog per simulated tier (8/12/16/24 GB + CPU-only) asserting VRAM/disk fit, one-chat + one-agentic + embed composition, and uncensored image/video defaults. Gates: installer suite **538 passed / 2 skipped / 0 failed** (+46); `tier_defaults` 94% / `typed_catalog` 93% / `review` 91% lines; ruff 0 new findings; `tsc -b` clean; root Vitest **4569 / 6 / 0** (+4); offscreen 5-tier smoke green. New gaps `OSI004.P4.A-E` (pin rotation + manifest enumeration for the new entries, GPU-box runtime-load verification, PyInstaller registry-file packaging -> Phase 6/T601, legacy page retirement, size/VRAM true-up) in [known-gaps](v1/v1.8/known-gaps.md); [phase history](v1/v1.8/development/history/2026-07_phase-4-catalog-curation.md). Prior: **v1.8.0 Phase 3 landed 2026-07-03** -- the Hugging Face weights downloader closes gap G2 (T301-T304): image/video catalog selections stop being dead ends. Per-model `weights` manifests (path + sha256 per file, versions.lock.json discipline) on all 16 HF entries in [catalog.json](../core/registry/catalog.json), with the runtime model-path contract **defined + documented** (`<models_root>/weights/<model-id>/{repo-relative path}`, default `~/.nexus/models`, the directory diffusers `from_pretrained` loads; the runtime's real executors remain OA-09 stubs) and a new stdlib [pin-hf-weights.py](../scripts/installer/build/pin-hf-weights.py) rotation script (API `lfs.oid` / `--from-dir` local-hash / `--check` gate; all 16 pins are still documented placeholders -- no HF egress from the dev sandbox, `OSI003.P3.A`). New [hf_weights_puller.py](../scripts/installer/pyqt/src/nexus_installer/engine/hf_weights_puller.py): per-file `.partial`+Range resume (206 append / 200 restart / 416 promote; cancel keeps partials), 3-attempt backoff retry, SHA-256 verify (real-pin mismatch deletes + fails closed; placeholder pin warns + logs the computed digest, the fetch-payload.py discipline), traversal-safe manifest paths, 10 GB-reserve disk pre-check. New [model_router.py](../scripts/installer/pyqt/src/nexus_installer/engine/model_router.py) routes the GUI **and** headless model step by catalog protocol (`ollama` -> `ModelPuller.pull_model` unchanged; `huggingface` -> weights puller; unknown ids -> ollama verbatim) with sizeGB-weighted mixed progress, per-model failure isolation onto new `InstallerState.failed_models` + named complete-page bullets, and the new `selected_model_ids` multi-select state (wizard UI producer = Phase 4, `OSI003.P3.D`). 62 new tests incl. an env-gated `NEXUS_HF_WEIGHTS_SMOKE=1` real-download smoke (operator GPU-box action with the runtime-load leg, `OSI003.P3.C`): installer suite **494 passed / 2 skipped / 0 failed** (+61); `hf_weights_puller` 94% / `model_router` 98% lines; ruff 0 new findings; `tsc -b` clean; root Vitest **4565 / 6 / 0** unchanged. Full multi-file diffusers manifests (text encoder / tokenizer / scheduler / VAE) ride Phase 4/T403 (`OSI003.P3.B`). See the [v1.8.0 plan](v1/v1.8/plans/one-shot-installer.md), [known-gaps](v1/v1.8/known-gaps.md), and [phase history](v1/v1.8/development/history/2026-07_phase-3-hf-weights-downloader.md). Prior: **v1.8.0 Phase 2 landed 2026-07-02** -- the installer installs Nexus (T201-T204): a new fail-closed [desktop_provisioner.py](../scripts/installer/pyqt/src/nexus_installer/engine/desktop_provisioner.py) (pinned tag `v2.1.0` -> per-OS/arch asset resolution against the Phase 1 canonical names -> resumable `.partial`+Range download that survives wizard restarts -> `SHA256SUMS.txt` verification **before any install dispatch**, mismatches deleted -> per-OS install: NSIS `/S`[+`/D=`], `hdiutil`/`ditto` to `/Applications`, AppImage to `~/.local/bin` + `.desktop` entry; Windows exe resolution handles the bundle's actual `nexus-shell.exe`; `--desktop-bundle` local-override for the freeze-era path, warn-logged), engine **step 5 "Nexus Desktop"** with its own progress band + cancel wiring (also fixed the model step's hardcoded final-step progress base), `"desktop"` default-on in the components (ConfigurationPage toggle, friendly Review labels, headless step + `--skip-desktop` threaded through the 3 smoke scripts), a **first-run launch-probe health check** (exit-0 or alive-past-5s-grace; failure warns on the complete page's new "Nexus Desktop" services row without failing the step -- the sidecar-ping tier is `OSI002.P2.B`, blocked on the shell-only-bundle carryover `OSI001.P1.B`), and a default-checked "Launch Nexus when I click Finish" checkbox via a new window Finish hook. 39 new tests + suite updates: installer suite **433 passed / 1 skipped / 0 failed** (+42); the env-gated fixture integration test ran green on the dev box (real silent install -> Start-menu `Nexus.lnk` -> app launches, health check passes -> clean uninstall); `desktop_provisioner` 89% lines; ruff 0 new findings; root Vitest **4565 / 6 / 0** unchanged. New gaps `OSI002.P2.A-D` (live release-fetch rehearsal deferred to T603; sidecar-ping tier; mac/linux hardware legs deferred to T604; unwired standalone wizard pages) in [known-gaps](v1/v1.8/known-gaps.md); [phase history](v1/v1.8/development/history/2026-07_phase-2-desktop-provisioner.md). Prior: **v1.8.0 Phase 1 landed 2026-07-02** -- release pipeline produces desktop bundles (T101-T104): a `desktop-bundle` 3-OS Tauri job set in `release.yml` staging canonical `Nexus-Desktop_{version}_*` assets (tag-triggered only under the Actions freeze; CI execution deferred to the Phase 6 post-freeze rehearsal), a build-time `tauri.conf.json` version sync from the semantic-release-owned package.json ([scripts/sync-tauri-version.mjs](../scripts/sync-tauri-version.mjs) + 7 tests; the conf was stale at 1.5.0 vs 2.1.0), a single fail-loud `SHA256SUMS.txt` release asset covering every attached artifact (the Phase 2 provisioner's fail-closed verification source), and the grep-audited `GemmaCodeSetup.*` -> `NexusSetup.*` rename (`release.yml`'s upload paths were dead-broken vs the build scripts' actual `NexusSetup.*` output; the wizard's `setApplicationName`, the `gemma-code-*.vsix` asset name, and the "Gemma Code v..." release title were also fixed). Local T104 proof on the dev box: rustup stable-msvc 1.96.1 installed (MSVC + NSIS were present), `Nexus_2.1.0_x64-setup.exe` (1.6 MB NSIS) + `Nexus_2.1.0_x64_en-US.msi` (2.1 MB) built in 2m27s, silent install/uninstall round-trip clean, NSIS bundle stashed at `.local-fixtures/` (new gitignored dir) as the Phase 2 T204 fixture. Root suite **4565 passed / 6 skipped / 0 failed** (+7 new); `tsc -b` + lint clean. Key carryovers: the bundle ships the Tauri shell only -- no sidecar dist packaged (`OSI001.P1.B`, the v1.7.0 `RT.P7.A`/`RT.P7.B` chain) -- pinned to Phase 2's DoD; the operator-raised residual `gemma-code` compat-shim retirement (removal was promised "until v1.2.0", now ~5 majors overdue) recorded as `NAME.P1.A`. See the [v1.8.0 plan](v1/v1.8/plans/one-shot-installer.md), [known-gaps](v1/v1.8/known-gaps.md), and [phase history](v1/v1.8/development/history/2026-07_phase-1-release-pipeline-desktop-bundles.md). Prior: **v1.7.0 post-cycle -- vscode-free headless agent runtime + optimizer production wiring (2026-07-02)**: built the deferred `modules/coding/runtime/` (workdir-scoped headless tool set + `HeadlessAgentSession` loop reusing the pure `Gemma4ToolFormat` codec + `HeadlessAgentDriver`) and the production optimizer seams (`HeadlessOptimizerRollout`, `HeadlessCandidateProducer`/`Scorer`/`Promoter`) + a `nexus golden run` CLI -- resolving five forward-tier gaps (`SO001.P1.A` real AgentDriver, `SO003.P3.B` production rollout, `SO005.P4.A` producer+scorer, `SO005.P4.B` promoter, `SO001.P1.B` golden-run CLI). 35 new tests; full suite **4533 passed / 6 skipped / 0 failed**; lint + `tsc -b` + check-architecture (0 errors / 10 pre-existing warnings) + security:check + check:tampering + catalog + prod-audit all green. Deferred (v1.7.0 known-gaps): `RT.P7.A` desktop-sidecar wiring (blocked on `RT.P7.B` relocating the shared tool-call primitives into `modules/coding` so the desktop typecheck stays `src/`-free, + the Rust toolchain for the Tauri e2e), `RT.P7.C` optimize/frontier CLIs. Commits `b2ba301`/`5240fa9`/`2fb0a75`. Prior: **v1.7.0 (`adoption-self-optimizing-skills`) COMPLETE -- Phase 6 FINAL closed 2026-07-02** (SO007-SO009): the whole-plan acceptance gate passed with **no feature-code change** (verification + close-out only). All seven Definition-of-pass items verified -- the six delivered modules (S1 golden-task live runner; S4 split + held-out gate + rejected-edit buffer; S2+S6 bounded-edit optimizer + A/B; S3 Pareto frontier; O-A shell introspection) confirmed physically present, item 7 (testing + docs) delivered. Gate green: root suite **4498 passed / 6 skipped / 0 failed**; coverage lines 88.22% / branches 84.01% / functions 91.35% (all above the 80/75/80 CI gate); `tsc -b` + lint (0 errors) + check-architecture (0 errors / 10 pre-existing warnings) + security:check (in sync) + check:tampering (0 findings) all clean. Docs finalized (known-gaps ledger flipped to COMPLETE 6/6, DEVLOG, todos, plan); README/ARCHITECTURE/CHANGELOG narrative + the npm version tag remain semantic-release-owned (cut on merge to `main`). Demand-gated backlog recorded (S5; opencode O-B/O-D/O-E). Nexus-Hub touchpoint assessed **not warranted** as an in-repo change (`SO009.P6.A`; precedent v1.6.0 `OF015.P5.A`/`AS010.P6.A`). See the [v1.7.0 plan](v1/v1.7/plans/adoption-self-optimizing-skills.md) and [known-gaps](v1/v1.7/known-gaps.md). Prior: **v1.7.0 (`adoption-self-optimizing-skills`) Phase 5 landed 2026-07-01** -- O-A tree-sitter shell-command introspection for permission gating (SO006): the terminal permission gate now enumerates the paths/cwd a shell command will touch (bash/PowerShell/cmd) via the vscode-free, dependency-free [shellIntrospection.ts](../modules/coding/guardrails/shellIntrospection.ts) `introspectShellCommand` -- a quote-aware structural parser (redirection detection + per-dialect file-command tables + `cd`-family cwd changes) reverse-engineered per the MCP Registry Policy with **no new dependency** (the bundled `tree-sitter-wasms` ships a bash grammar only; PowerShell/cmd have none, so the structural tokenizer is the always-available core) -- and matches each write/delete/read path against the `.nexus/permissions.deny` file-tool rules in the [ToolRegistry](../src/tools/ToolRegistry.ts) deny gate (`_denyByTouchedPath`: write->`write_file`, delete->`delete_file`, read->`read_file`, blanket `*:` honored), so `write_file: secrets/**` now also blocks `echo x > secrets/prod.env`, not just a literal `write_file` call. The gate **fails closed**: an un-parseable command (command/process substitution, `$`/`%` variable expansion, backtick, `Invoke-Expression`, an unbalanced quote) logs the fallback and fabricates no path rule -- the DANGEROUS-tier confirmation + command-string denylist still gate, never auto-allowing -- so the introspection only ever *tightens* the surface; the `run_terminal` dry-run report gains a `Touched paths:` line. 37 new tests; root suite **4498 passed / 6 skipped / 0 failed**; new-module coverage shellIntrospection 100% lines / 92.59% branches / 100% functions; lint + `tsc -b` + check-architecture (0 errors / 10 pre-existing warnings) + check:tampering (0) + security:check all clean. Forward-tier carryovers `SO006.P5.A` (tree-sitter-bash AST upgrade over the bundled grammar), `SO006.P5.B` (opt-in workspace-escape hard-block), `SO006.P5.C` (glob-expansion / read-scope precision) in [v1.7.0 known-gaps](v1/v1.7/known-gaps.md). See the [v1.7.0 plan](v1/v1.7/plans/adoption-self-optimizing-skills.md). Prior: **v1.7.0 (`adoption-self-optimizing-skills`) Phase 4 landed 2026-07-01** -- S3 the evolutionary layer (SO005): a **`CandidateFrontier`** (three vscode-free [`modules/coding/skilloptimizer/`](../modules/coding/skilloptimizer/) modules) reverse-engineers the GEPA/EvoSkill genetic-Pareto selector as a lean local module (no DSPy/EvoSkill dependency) on the Phase 3 spine -- the pure [pareto.ts](../modules/coding/skilloptimizer/pareto.ts) selector (`dominates` / `paretoFrontier` non-dominated set / deterministic held-out extremes) plus the [CandidateFrontier.ts](../modules/coding/skilloptimizer/CandidateFrontier.ts) orchestrator: produce >= 2 candidates -> materialize each on its **own git branch** (the real [frontierWorktree.ts](../modules/coding/skilloptimizer/frontierWorktree.ts) `WorktreeCandidateManager`, reusing the v1.5.0 worktree-swarm `GitRunner` contract) -> score across the diverse tasks -> auto-clean each ephemeral worktree (one live at a time, the single-GPU discipline; the branch ref survives) -> keep a **bounded population under a hard candidate cap** (the EvoSkill replacement rule: a challenger displaces the lowest-held-out incumbent only when it strictly beats it on the held-out split) -> select the non-dominated (Pareto) set -> surface the winner for **explicit human approval**, reusing the Phase 3 `SkillEditApprovalGate` (a winning branch is **never** auto-merged -- the `CandidatePromoter` is structurally unreachable without approval). 30 new tests; root suite **4461 passed / 6 skipped / 0 failed**; new-module coverage pareto 100/100/100, CandidateFrontier 100/94.11/100, frontierWorktree 96.61/90/100 (skilloptimizer subtree 99.47/91.51/100); lint + `tsc -b` + check-architecture (0 errors / 10 pre-existing warnings) + check:tampering (0) + security:check all clean. Forward-tier carryovers `SO005.P4.A` (production candidate producer + scorer over the real agent, deferred behind `SO001.P1.A`/`SO003.P3.B`), `SO005.P4.B` (live branch->catalog promoter over `GitSafetyNet`), `SO005.P4.C` (no frontier CLI + tier-cap wiring) in [v1.7.0 known-gaps](v1/v1.7/known-gaps.md). See the [v1.7.0 plan](v1/v1.7/plans/adoption-self-optimizing-skills.md). Prior: **v1.7.0 (`adoption-self-optimizing-skills`) Phase 3 landed 2026-06-30** -- S2 + S6 the headline capability (SO003 + SO004): the bounded-edit **`SkillOptimizer`** (eight vscode-free [`modules/coding/skilloptimizer/`](../modules/coding/skilloptimizer/) modules) runs the reverse-engineered SkillOpt/GEPA loop -- rollout the train split -> reflect on failing trajectories via the existing `ReflexionEngine`/`CriticAgent` (trajectories `redactSecrets`-scanned) -> propose a **bounded** add/delete/replace skill edit under a textual learning-rate budget -> accept ONLY on the Phase 2 held-out `validationGate` -> buffer rejects in the `RejectedEditBuffer` -> overwrite the skill file **only after explicit human approval** (`classifyAction` + a fail-closed catalog-root path resolver gate the write; the loop is capped by the v0.3.0 runaway `BudgetMiddleware`) -- plus the **optimizer-quality A/B** ([SkillOptimizerAb.ts](../modules/coding/skilloptimizer/SkillOptimizerAb.ts) reusing the v1.6.0 Fusion F4 `PanelAbHarness` + `decideSkillOptimizerDefault`). New opt-in flag `nexus.coding.skillOptimizer.enabled` ships **default off** (no live local A/B run here, so no measured net win to flip it; `SO003.P3.A`). 48 new tests; root suite **4431 passed / 6 skipped / 0 failed**; new-module coverage 99.62% lines / 89.61% branches / 100% functions across the `skilloptimizer` subtree; lint + `tsc -b` + check-architecture (0 errors / 10 pre-existing warnings) + check:tampering (0) + security:check all clean. Forward-tier carryovers `SO003.P3.A` (opt-in default-off pending a live A/B), `SO003.P3.B` (production rollout over the real `AgentLoop`, deferred behind `SO001.P1.A`), `SO003.P3.C` (live `ConfirmationGate`/`pathGuard` adapters), `SO003.P3.D` (no optimize CLI) in [v1.7.0 known-gaps](v1/v1.7/known-gaps.md). See the [v1.7.0 plan](v1/v1.7/plans/adoption-self-optimizing-skills.md). Prior: **v1.7.0 (`adoption-self-optimizing-skills`) Phase 2 landed 2026-06-30** -- S4 regression-safety scaffolding (SO002): a locked train/validation/test **split** (additive `split` field on `GoldenTaskSpec` + a deterministic default-by-category assignment in [goldenSplit.ts](../modules/coding/evaluation/goldenSplit.ts)) whose **test** split is structurally unreachable from the optimizer loader (`optimizerVisibleTasks` + `assertNoTestSplit` guard); a pure held-out **validation gate** ([validationGate.ts](../modules/coding/evaluation/validationGate.ts) -- accepts only on a strict aggregate validation-pass-rate improvement with per-task regressions within tolerance, reusing `RegressionReport`); and a content-addressed, redaction-on-write **rejected-edit buffer** ([core/memory/RejectedEditBuffer.ts](../core/memory/RejectedEditBuffer.ts), keyed by `skillId + editHash`, edit/trajectory text stored via `ArtifactStore` so it never lands on disk unredacted). 29 new tests; root suite **4382 passed / 6 skipped / 0 failed**; new-module coverage 100% lines/branches/functions across goldenSplit / validationGate / RejectedEditBuffer; lint + `tsc -b` + check-architecture (0 errors / 10 pre-existing warnings) + check:tampering (0) + security:check all clean. Forward-tier carryovers `SO002.P2.A` (latency/token regression gating), `SO002.P2.B` (buffer orphan GC), `SO002.P2.C` (difficulty-stratified splits) in [v1.7.0 known-gaps](v1/v1.7/known-gaps.md). Phases 3-6 pending. See the [v1.7.0 plan](v1/v1.7/plans/adoption-self-optimizing-skills.md). Prior: **v1.7.0 Phase 1 landed 2026-06-29** -- S1 TS-native golden-task **live runner** (SO001): four vscode-free [`modules/coding/evaluation/`](../modules/coding/evaluation/) modules -- a declarative `success_criteria` evaluator ([goldenCriteria.ts](../modules/coding/evaluation/goldenCriteria.ts), TS port of `evaluator.py`), a copy-into-temp + `git init` snapshot materializer ([goldenSnapshot.ts](../modules/coding/evaluation/goldenSnapshot.ts)), a dependency-free YAML-subset task loader ([goldenTaskLoader.ts](../modules/coding/evaluation/goldenTaskLoader.ts)), and a runner that materializes -> runs the agent via an injected `AgentDriver` seam (kept vscode-free per `no-llm-outside-llm-folder`) -> evaluates -> emits a scored `GoldenTaskResult` ([GoldenTaskRunner.ts](../modules/coding/evaluation/GoldenTaskRunner.ts)), with a per-task timeout. Revives the live golden runs deleted by ADR-0001 (the Python `_run_live()` returned "backend call failed" since v0.4.0). 45 new tests (unit + real-snapshot dry/mock-live integration + env-gated Ollama live smoke); root suite **4354 passed / 6 skipped / 0 failed**; new-module coverage 97.11% lines / 83.25% branches / 100% functions; lint + `tsc -b` + check-architecture (0 errors / 10 pre-existing warnings) + check:tampering (0) + security:check all clean. Forward-tier carryovers (`SO001.P1.A` full-loop driver, `SO001.P1.B` `nexus golden run` CLI, `SO001.P1.C` Windows shell criteria) in [v1.7.0 known-gaps](v1/v1.7/known-gaps.md). Prior: **v1.6.0 companion plan `adoption-openrouter-fusion` COMPLETE -- Phase 5 FINAL closed 2026-06-17** (all 5 phases). The whole-plan acceptance gate passed: all six definition-of-pass items verified, no feature code changed (verification + close-out, OF013-OF015). Routing default ships opt-in (off) per the no-degradation gate (the A/B was not run on live models here, `OF010.P4.A`); live route wiring deferred (`OF011.P4.A`); Nexus-Hub touchpoint assessed not-warranted (`OF015.P5.A`); pre-existing `dompurify`/`protobufjs` prod advisories recorded as not-owned by this plan (`ENV.P5.A`). Full suite 4291 passed / 5 skipped / 0 failed; lint + `tsc -b` + check-architecture (0 errors) + security:check + check:tampering (0) all clean. README/ARCHITECTURE/CHANGELOG narrative + the npm version tag are semantic-release-owned and cut on merge to `main`. Prior: **Phase 4 landed 2026-06-17** -- F4 budget-panel routing + local A/B: a [`PanelAbHarness`](../modules/coding/orchestration/PanelAbHarness.ts) (pure aggregation + `decidePanelRoutingDefault` gate + the `scripts/run-panel-ab.mjs` live runner) that measures a small-model panel against the single best resident model on coding tasks; an opt-in [`PanelRouter`](../modules/coding/llm/PanelRouter.ts) heuristic (`nexus.llm.panelRouting`, default off) that escalates a reliability-flagged task to the panel; and the Phase 3 co-residency primitive wired into [`PanelExecutor`](../modules/coding/orchestration/PanelExecutor.ts) (optional `concurrency` backend through `GpuScheduler.enqueuePanel`, closing `OF007.P3.A`) (OF010-OF012). Routing default ships **opt-in (off)** per the no-degradation gate -- the A/B was not run on live models in this environment, so no measured net win exists to flip it (`OF010.P4.A`); the live composition-root route wiring is deferred (`OF011.P4.A`). Full suite 4291 passed / 5 skipped / 0 failed; `tsc -b` + lint + check:tampering (0) clean; check-architecture 0 errors (10 pre-existing warnings); new-module coverage PanelRouter 100% / 100%, PanelAbHarness 100% / 100%, PanelExecutor 100% lines / 100% funcs. Phase 5 (FINAL acceptance gate + docs + Hub sync) pending. Prior: **Phase 3 landed 2026-06-16** -- F3 concurrent multi-model VRAM residency: [`GpuScheduler.enqueuePanel`](../core/scheduler/GpuScheduler.ts) admits a bounded panel as one scheduler job that runs its members concurrently when their summed VRAM fits free VRAM and degrades to sequential fan-out (peaking at the largest single member, never the sum) when it does not -- no OOM, no rejection, single-active-job ceiling preserved for non-panel workloads; a hard panel-size cap (`DEFAULT_PANEL_SIZE_CAP = 3`, overridable) bounds co-residency; [`ModelPinRegistry.holdForPanel`](../core/registry/ModelPinRegistry.ts) keep-alive-holds the panel's models (ref-counted, in-memory, never persisted) for the run and releases them after, leaving a user's explicit pin untouched (OF007-OF009). Full suite 4256 passed / 5 skipped / 0 failed; `tsc -b` + check:tampering (0) clean; new-code coverage GpuScheduler 99.07% lines / 100% funcs, ModelPinRegistry 100% / 100%; check-architecture 0 errors (10 pre-existing warnings). Scheduler primitive + keep-alive are built ahead of their PanelExecutor/route consumer (Phase 4 routing wires them; `OF007.P3.A`). Prior: **Phase 2 landed 2026-06-16** -- F2 + F5 local multi-model panel orchestration + eval-integrity hardening: a [`FusionAgent`](../modules/coding/orchestration/FusionAgent.ts) (the judge, consuming the Phase 1 `fuse` schema, hardened as an untrusted-input boundary -- candidates redacted + treated as data, single-judge SPOF documented) and a [`PanelExecutor`](../modules/coding/orchestration/PanelExecutor.ts) that fans one prompt across N distinct registry models (sequential fan-out MVP), collects labeled candidates, survives a panelist failing, and fuses the survivors; panelists get no per-panelist tools (OF004-OF006). Routing + A/B (F4) is Phase 4. Prior: **Phase 1 landed 2026-06-16** -- F1 structured judge-fusion synthesis: the [`fuse` skill](../modules/coding/skills/catalog/fuse/SKILL.md) emitting the consensus / contradictions / partial-coverage / unique-insights / blind-spots -> grounded-answer schema + `council` synthesis upgrade + an 8-case conformance test. See the [openrouter-fusion plan](v1/v1.6/plans/adoption-openrouter-fusion.md) and its [known-gaps](v1/v1.6/known-gaps.md). Prior: **v1.6.0 (aisuite-harness) cycle COMPLETE -- all 6 phases closed (Phase 6 FINAL whole-plan acceptance gate passed 2026-06-16)**: every quality gate is green (root suite 4197 passed / 5 skipped / 0 failed, desktop 449 passed / 0 failed, `tsc -b` + lint + check-architecture (0 errors / 10 pre-existing warnings) + security:check + check:tampering all clean) and all six definition-of-pass items are verified; no feature code changed (verification + cycle closure). The Nexus-Hub touchpoint was assessed and judged not warranted (carried as `AS010.P6.A`). README/ARCHITECTURE/CHANGELOG narrative sync + the npm version tag are semantic-release-owned and cut on merge to `main`. Phases 4 (A2 hierarchical sub-run trace nesting) and 5 (A3 manifest-driven loopback-only local-runtime adapter registry) also closed 2026-06-15. Earlier in the cycle: **Phase 3 (A1 session-state artifact dehydration) closed 2026-06-15** -- large session message fields (>20KB) are now moved out-of-line to a content-addressed, redaction-on-write artifact store under `<nexusHome>/session-artifacts/` ([core/memory/ArtifactStore.ts](../core/memory/ArtifactStore.ts) + [sessionArtifacts.ts](../core/memory/sessionArtifacts.ts) + a one-way v1->v2 migration [migrateSessionsDehydrate.ts](../core/memory/migrateSessionsDehydrate.ts) + `.mjs` wrapper) and rehydrated on resume, wired into the sidecar [JsonFileSessionStore](../desktop/sidecar/src/coding/sessionStore.ts) at the disk I/O boundary (public `messages: string[]` unchanged; pre-v2 files load verbatim); 26 core + 4 sidecar tests + a size-delta benchmark, new-module coverage 97.81% lines, root suite 4155 passed / 5 skipped, desktop 449 passed / 0 failed. Prior: **v1.6.0 Phase 2 (A4 session/trace viewer) closed 2026-06-15** -- a standalone shareable, self-contained HTML trace export ([modules/coding/observability/TraceHtmlExport.ts](../modules/coding/observability/TraceHtmlExport.ts) + a vscode-free [TraceDbReader.ts](../modules/coding/observability/TraceDbReader.ts) + a `nexus trace export` CLI) reusing the Phase 1 design tokens and the `InteractiveArtifact` sanitisation rules; static-first timeline + inline JSON, verified by [TraceHtmlExport.test.ts](../tests/unit/observability/TraceHtmlExport.test.ts) (26 cases) + [TraceDbReader.test.ts](../tests/unit/observability/TraceDbReader.test.ts) (5 cases); root suite 4130 passed / 5 skipped / 0 failed, new-module coverage 100% lines. Prior: **v1.6.0 Phase 1 (interactive guide) closed 2026-06-15** -- the AS003 offline-integrity + reduced-motion Vitest test ([tests/unit/guides/nexus-ai-guide.offline.test.ts](../tests/unit/guides/nexus-ai-guide.offline.test.ts), 18 cases) closed the self-contained Nexus-AI interactive guide (AS001/AS002 delivered 2026-06-14). See the [v1.6.0 plan](v1/v1.6/plans/adoption-aisuite-harness.md) and [known-gaps](v1/v1.6/known-gaps.md). Prior: **v1.5.0 "Local Agent Maturity" Phase 7 (FINAL) landed 2026-06-15** -- Nexus-Hub sync + whole-plan acceptance gate (T023-T024): published the 2 Phase 2 skills to Hub `develop` and integrated all six Hub consumption surfaces (skills + data-index + rules + agents + commands + hooks + MCP-registry-policy-filter); also fixed the failing v1.5.0 CI/Nightly/Installer-smoke workflows + Dependabot CI hygiene. Whole-plan acceptance gate green (root suite 4080, desktop 445, all static gates clean). v1.5.0 complete across all 7 phases; ready for release. Prior: **Phase 5 landed 2026-06-14** -- the Bucket 4 `re-partial` desktop / model-layer set (T015-T020): multimodal image input gated by `isVisionCapableModel` at both prompt-assembly sites (item 33), the `PreviewPane` side-by-side renderer reusing `InteractiveArtifact` (item 24), the vault-only `CredentialsSettings` surface over new `credentials.*` sidecar IPC (item 25), and cross-surface session resume via a persistent `JsonFileSessionStore` + resume-with-history (item 26); the local cron scheduler (item 38) was deferred on the demand gate (`T019.P3.A`). Root suite 4035 / desktop suite 445, all gates clean. Prior: **Phase 4 landed 2026-06-12** -- the Bucket 3 `re-full` planner/critic/worker swarm orchestration (item 36, opt-in default off); closed three v1.4.0 P3 deferrals (`T018.P3.A`, `T018.P3.B`, `T016.P3.A`). Prior: **Phase 3 landed 2026-06-10** -- the inbound prompt-injection classifier (warn-then-allow). Prior: **Phase 2 landed 2026-06-10** -- the two Bucket 2 `skill-native` adoptions. Prior: **Phase 1 landed 2026-06-10** -- the three Bucket 1 `local-only` foundations; see the [v1.5.0 plan](v1/v1.5/plans/adoption-ecosystem-2026-06.md) and [known-gaps](v1/v1.5/known-gaps.md). Prior: **v1.4.0 complete (Phases 1-9)**, closed 2026-06-09. (v1.4.0 + v1.5.0 work is not yet merged to `main`.)

---

## Scores (update after each sprint)

| Metric | Current | Target | Delta |
|--------|---------|--------|-------|
| Tasks done (v0.1.0) | 13 / 21 | 21 / 21 | -8 |
| Tasks done (v0.2.0) | 44 / 44 | 44 / 44 | 0 |
| Tasks done (v0.3.0) | 55 / 55 | 55 / 55 | 0 |
| Tasks done (v0.4.0) | 16 (P1) + 17 (P2) + 21 (P3) + 21 (P4) + 22 (P5) | ~135 / ~135 | in-progress |

---

## Sprint 1 - Completed Work [DONE]

- [x] Bootstrap VS Code extension scaffold with TypeScript, tsconfig, ESLint, and Vitest (Phase 1)
- [x] Implement OllamaClient with streaming chat, health check, and model listing (Phase 1)
- [x] Build chat engine with ConversationManager and streaming webview UI (Phase 2)
- [x] Implement agentic tool layer with 10 tool handlers and confirmation gate (Phase 3)
- [x] Add skill loader, command router, and plan mode with slash command autocomplete (Phase 4)
- [x] Add persistent SQLite chat history, auto-compact, and edit modes (Phase 5)
- [x] Implement Python inference backend with FastAPI and Gemma chat template (Phase 6)
- [x] Build BackendManager with auto-start and graceful fallback to direct Ollama (Phase 6)
- [x] Build Windows NSIS installer with Ollama, VSIX, venv, and model download (Phase 7)
- [x] Set up CI/CD workflows: ci.yml, release.yml, nightly.yml (Phase 7)
- [x] Fix SSRF vulnerability in FetchPageTool and harden terminal blocklist (Phase 8)
- [x] Add performance benchmark suite and error handling hardening (Phase 8)
- [x] Write comprehensive README, CHANGELOG, and architecture documentation (Phase 8)

## Sprint 2 - v0.1.x maintenance [SUPERSEDED]

*Pre-rename (Gemma Code) maintenance backlog, superseded by the v1.0.0 four-pillar Nexus rebuild. Retained as history; not active work. The unchecked items below were never shipped under the v0.1.x line.*

- [ ] Implement ripgrep-backed GrepCodebaseTool to replace slow workspace.findFiles approach (CHANGELOG Known Limitations)
- [x] Build macOS and Linux installer packages (resolved in v0.3.0 Phase 7 PyQt5 installer)
- [ ] Publish extension to VS Code Marketplace (CHANGELOG Unreleased)
- [ ] Implement Rust performance components for file indexing and grep (CHANGELOG Unreleased)
- [ ] Implement Go CLI tooling for project scaffolding (CHANGELOG Unreleased)

## v0.2.0 - Planned (local Claude Code equivalent)

Full plan: `docs/archive/versions/v0/v0.2.0/development/implementation-plan.md`

### Phase 0 - Gemma 4 Native Protocol Migration
- [x] Create `src/tools/Gemma4ToolFormat.ts` (tool declaration serializer, tool call parser, result formatter)
- [x] Migrate `ToolCallParser.ts` from XML `<tool_call>` regex to Gemma 4 native `<|tool_call>` tokens
- [x] Update `AgentLoop.ts` tool result injection to use `<|tool_result>` format
- [x] Update `ConversationManager.ts` system prompt for native system role
- [x] Update `settings.ts` defaults: model `gemma4:e4b`, maxTokens 131072, temperature 1.0, topP 0.95, topK 64
- [x] Update `prompt.py` to use native system role and Gemma 4 turn tokens
- [x] Update `client.ts` to pass `tools` parameter in Ollama API requests
- [x] Add thinking mode support (`<|think|>` token in system prompt)

### Phase 1 - Dynamic PromptBuilder with Token Budgeting
- [x] Create `src/chat/PromptBuilder.ts` with section-based assembly and greedy packing
- [x] Create `src/chat/PromptBuilder.types.ts` (PromptContext, PromptSection, PromptStyle)
- [x] Create `src/config/PromptBudget.ts` (centralized budget calculator)
- [x] Refactor `ConversationManager.ts` to use PromptBuilder instead of static SYSTEM_PROMPT
- [x] Wire PromptBuilder into `GemmaCodePanel.ts`
- [x] Add `promptStyle` and `systemPromptBudgetPercent` settings

### Phase 2 - Multi-Strategy Context Compaction
- [x] Create `src/chat/CompactionStrategy.ts` with interface and 5 strategy implementations
- [x] Implement ToolResultClearing strategy (regex-based, zero LLM cost)
- [x] Implement SlidingWindow strategy (keep anchors + recent messages)
- [x] Implement CodeBlockTruncation strategy (replace large code blocks with placeholders)
- [x] Refactor `ContextCompactor.ts` to use CompactionPipeline
- [x] Add pre-compaction save hook (wires to MemoryStore in Phase 3)

### Phase 3 - Persistent Memory System
- [x] Create `src/storage/MemoryStore.ts` with SQLite FTS5 schema
- [x] Create `src/storage/EmbeddingClient.ts` wrapping Ollama `/api/embed`
- [x] Add FTS5 virtual table and sync triggers to `ChatHistoryStore.ts`
- [x] Add `buildMemorySection()` to PromptBuilder with budget cap
- [x] Wire memory retrieval into chat flow (GemmaCodePanel)
- [x] Implement pre-compaction memory extraction (`extractAndSave`)
- [x] Add `/memory` slash command (search, save, clear, status)

### Phase 4 - Conditional Tool Activation and MCP Support
- [x] Add enabled/disabled state to `ToolRegistry.ts`
- [x] Update PromptBuilder to only declare enabled tools
- [x] Create `src/mcp/McpServer.ts` (expose tools via MCP stdio)
- [x] Create `src/mcp/McpClient.ts` (consume external MCP servers)
- [x] Create `src/mcp/McpManager.ts` (lifecycle, config from mcp.json)
- [x] Add `/mcp` slash command (status, connect, disconnect)

### Phase 5 - Sub-Agent Orchestration [COMPLETED]
- [x] Create `src/agents/SubAgentManager.ts` (isolated ConversationManager + AgentLoop)
- [x] Create `src/agents/SubAgentPrompts.ts` (verification, research, planning templates)
- [x] Add file-edit counter and auto-verification trigger to AgentLoop
- [x] Add `buildForSubAgent()` to PromptBuilder
- [x] Wire sub-agent status to webview
- [x] Add `/verify` and `/research` slash commands

### Phase 6 - Integration, Polish, and Backend Alignment [COMPLETED]
- [x] Align Python backend prompt.py with multi-strategy compaction
- [x] Update webview UI (memory status, sub-agent spinner, MCP badge)
- [x] Create root-level `SECURITY.md` and `ARCHITECTURE.md`
- [x] Run end-to-end verification checklist (11 items in plan)
- [x] Bump version to 0.2.0, update CHANGELOG

## v0.3.0 - Harness Engineering Infrastructure

Full plan: `docs/archive/versions/v0/v0.3.0/implementation-plan.md`

### Phase 1 - GPU Detection & Hardware-Aware Foundation [COMPLETED]
- [x] GPU/VRAM detection service (nvidia-smi, rocm-smi, system_profiler, WMI fallbacks)
- [x] Hardware tier classification (3 tiers: constrained/balanced/full)
- [x] Tier-aware context budget calculator (expanded BudgetOverrides, calculateTierBudget)
- [x] Token and iteration budget middleware (BudgetMiddleware in AgentLoop)
- [x] Wire GPU detection into extension lifecycle (status bar, detectGpu command, panel integration)

### Phase 2 - Advanced Context Engineering [COMPLETED]
- [x] Lazy tool loading with progressive disclosure
- [x] Output redirection for large tool results
- [x] Enhanced compaction with regenerate-from-source
- [x] Context budget middleware chain
- [x] Token estimation accuracy improvements

### Phase 3 - Graph-Vector Hybrid Memory [COMPLETED]
- [x] 4-layer memory stack (working/episodic/semantic/graph)
- [x] Entity extraction and provenance tracking
- [x] Memory-aware context assembly
- [x] Memory pruning and consolidation

### Phase 4 - Safety, Budgeting & Runaway Prevention [COMPLETED]
- [x] Hash-based loop detection
- [x] Irreversible action classification
- [x] Git safety net
- [x] Permission escalation system

### Phase 5 - Plan-and-Execute Orchestration [COMPLETED]
- [x] Task DAG data model and PlannerAgent (LLM-based request decomposition)
- [x] DAG executor with GPU-aware semaphore scheduling
- [x] Reflexion pattern for error recovery (analyze, constrain, retry)
- [x] Structured output contracts for sub-agent communication
- [x] Orchestrator integration with GemmaCodePanel (plan mode + complexity heuristic)
- [x] Dynamic replanning on divergence (>30% failure threshold)

### Phase 6 - Local Observability & Trace Dashboard [COMPLETED]
- [x] SQLite trace store (TraceStore with spans, traces, events)
- [x] Tracer singleton with no-op mode and core component instrumentation
- [x] Metrics collector and golden task evaluation framework
- [x] Webview-based trace dashboard with waterfall visualization
- [x] Optional OTLP export (off by default, minimal fetch-based exporter)

### Phase 7 - Cross-Platform PyQt5 Installer [COMPLETED]
- [x] PyQt5 project scaffold with dark theme engine and custom widgets (7.1)
- [x] Welcome, Prerequisites, GPU Detection wizard pages with background detection (7.2)
- [x] Install Path, Model Selection, Configuration, Review wizard pages (7.3)
- [x] Installation engine with Ollama/extension/venv/model installers and real-time log (7.4)
- [x] Completion page with services table, management commands, and navigation polish (7.5)
- [x] Cross-platform packaging: PyInstaller for Windows/macOS/Linux (7.6)
- [x] Comprehensive test suite (184 tests, 83% coverage) and NSIS migration to legacy (7.7)
- [x] Testing stabilization to 80%+ coverage (7.T)

### Phase 8 - Golden Task Suite & Integration Stabilization [COMPLETED]
- [x] Golden task framework: types, loader, runner, evaluator, reporter, snapshot (8.1)
- [x] 24 golden task YAMLs + snapshots across 5 categories (8.2)
- [x] Per-tier model-matrix, memory-recall, and golden-task-perf benchmarks (8.3)
- [x] Baseline save/load + regression detection framework (8.3)
- [x] Installer CLI flags (`--headless`, `--model`, `--install-path`, `--skip-model`, `--json-output`) (8.4)
- [x] Cross-platform smoke tests (Windows PowerShell, macOS/Linux bash) with verify + cleanup helpers (8.4)
- [x] E2E integration tests: full-pipeline, memory-across-sessions, compaction-under-load, sub-agent, MCP, prompt-budget (8.5)
- [x] Comparison framework (`compare_versions` + markdown report) (8.6)
- [x] v0.3.0 architecture doc, performance benchmarks, performance comparison template (8.6)
- [x] `.github/workflows/golden-tasks.yml` + `installer-smoke.yml` (conservative, workflow_dispatch + weekly cron) (8.7)
- [x] Release checklist and CI pipeline documentation (8.7)
- [x] ARCHITECTURE.md, CHANGELOG.md, docs/todos.md updates (8.6)

## Backlog

- [ ] *(suggested)* Improve web search backend to reduce rate-limiting and improve result quality
- [ ] *(suggested)* Add E2E test suite to the standard CI matrix for pre-merge validation
- [ ] *(deferred)* Tree-sitter AST parsing for semantic code understanding (from Graphify comparison)
- [ ] *(deferred)* Knowledge graph generation (from Graphify/MemPalace comparisons)
- [ ] *(deferred)* Chat format normalization for importing Claude/ChatGPT history (from MemPalace comparison)
- [ ] *(deferred)* Retrieval quality benchmarks (from MemPalace comparison)

### v0.5 follow-ups (deferred from v0.4.0 Phase 6)

- [ ] Complete the GemmaCodePanel split: extract `ChatController` (agent loop + orchestration mediator) and `ChatWebviewHost` (webview provider + message translation) from `src/panels/GemmaCodePanel.ts`. Reduce the panel to <100 lines or delete it; thin `extension.ts` to a lifecycle adapter. Seam already in place via `GemmaRuntime`.
- [ ] Finish settings injection: eliminate the 12 `_getSettings()` reads inside `panels/GemmaCodePanel.ts` and the activation-time `getSettings()` in `extension.ts`. Reach the strict acceptance criterion of "exactly one `getSettings()` call, in `GemmaRuntime`."
- [ ] Expand Zod adoption beyond the LLM boundary: `panels/messages.ts` (webview payloads), `storage/GraphMemory.ts` (persisted entity attributes), `observability/TraceStore.ts` (persisted span attributes).
- [ ] Upgrade `marked` from v4 to v12: adapt the renderer API break, snapshot-test the rendered HTML against the existing fixture set, keep DOMPurify in the pipeline. See the `NOTE(v0.5)` comment in `src/utils/MarkdownRenderer.ts:1`.

---

## Functionality Matrix

### Tool Handlers

| Feature | Status | File/Location | Sprint |
|---------|--------|---------------|--------|
| read_file | Done | `src/tools/handlers/` | -- |
| write_file | Done | `src/tools/handlers/` | -- |
| create_file | Done | `src/tools/handlers/` | -- |
| delete_file | Done | `src/tools/handlers/` | -- |
| edit_file | Done | `src/tools/handlers/` | -- |
| list_directory | Done | `src/tools/handlers/` | -- |
| grep_codebase | Partial | `src/tools/handlers/` | Sprint 2 |
| run_terminal | Done | `src/tools/handlers/terminal.ts` | -- |
| web_search | Done | `src/tools/handlers/webSearch.ts` | Sprint 3 |
| fetch_page | Done | `src/tools/handlers/webSearch.ts` | -- |

### Platform Installers

| Platform | Status | File/Location | Sprint |
|----------|--------|---------------|--------|
| Windows (NSIS) | Done | `scripts/installer/setup.nsi` | -- |
| macOS | Missing | -- | Sprint 2 |
| Linux | Missing | -- | Sprint 2 |
| VS Code Marketplace | Missing | -- | Sprint 2 |

---

## v0.4.0 - Code Review Remediation

Driven by [docs/archive/versions/v0/v0.3.0/review.md](archive/v0/v0.3/review.md) (129 findings: 14 P0, 46 P1, 42 P2, 27 P3). Plan: [docs/archive/versions/v0/v0.4.0/implementation-plan.md](archive/v0/v0.4/implementation-plan.md).

### Phase 1 - Critical Hotfix (P0 Unblock) [COMPLETED 2026-04-18]

All 14 P0 findings closed plus version bump. See [docs/DEVLOG.md](DEVLOG.md) and [docs/adr/0001-python-backend-disposition.md](adr/0001-python-backend-disposition.md) for detail.

- [x] 1.1 DOMPurify-sanitize markdown + tighten webview CSP
- [x] 1.2 Restrict `run_terminal` cwd to workspace root (via `src/tools/handlers/pathGuard.ts`)
- [x] 1.3 Add AFTER UPDATE FTS5 trigger to ChatHistoryStore + switch `saveMessage` to UPDATE-or-INSERT
- [x] 1.4 Delete dead in-degree loop in TaskDAG.hasCycle
- [x] 1.5 Fix GraphQueryEngine path reconstruction
- [x] 1.6 Add Float32 embedding cache + FTS5 candidate filter to MemoryStore.searchSemantic
- [x] 1.7 Batch tracer writes; eliminate SELECT in endSpan
- [x] 1.8 Add end-to-end safety-pipeline integration test
- [x] 1.9 Add McpToolHandler unit tests
- [x] 1.10 Add SessionListPanel unit tests + attribute-context escaping (closes #87)
- [x] 1.11 Wire benchmark threshold gating in nightly.yml
- [x] 1.12 Wire golden-task live-Ollama matrix CI (e2b + e4b)
- [x] 1.13 Delete Python backend; record ADR-0001
- [x] 1.14 Extract MemorySubsystem factory from GemmaCodePanel (-84 LOC)
- [x] 1.15 Version bump to 0.4.0 + CHANGELOG seed
- [x] 1.16 Testing & stabilization (build + lint + test green on touched files)

### Phase 2 - Security Hardening [COMPLETED 2026-04-19]

20 non-P0 security findings closed (6 P1 + 9 P2 + 5 P3). 2.2 and 2.13 closed N/A per ADR-0001. See [docs/archive/versions/v0/v0.4.0/development/history/2026-04_phase-2-security-hardening.md](archive/v0/v0.4/development/history/2026-04_phase-2-security-hardening.md).

### Phase 3 - Correctness & Code Quality [COMPLETED 2026-04-19]

24 findings closed (8 P1 + 10 P2 + 6 P3). See [docs/archive/versions/v0/v0.4.0/development/history/2026-04_phase-3-correctness.md](archive/v0/v0.4/development/history/2026-04_phase-3-correctness.md).

### Phase 4 - Performance Optimization [COMPLETED 2026-04-19]

20 findings closed across seven waves; 5 closed N/A. See [docs/archive/versions/v0/v0.4.0/development/history/2026-04_phase-4-performance.md](archive/v0/v0.4/development/history/2026-04_phase-4-performance.md).

### Phase 5 - Testing Pipeline Completeness [COMPLETED 2026-04-19]

22 findings closed; 2 closed N/A. 1166 Vitest cases at 89.07% line / 82.78% branch coverage. See [docs/archive/versions/v0/v0.4.0/development/history/2026-04_phase-5-testing-pipeline.md](archive/v0/v0.4/development/history/2026-04_phase-5-testing-pipeline.md).

### Phase 6 - Restructuring (Architecture) [COMPLETED 2026-04-24]

14 of 17 sub-tasks landed; 3 scoped down with documented v0.5 deferrals (panel split, full settings injection, full Zod boundary coverage). See the [DEVLOG entry](DEVLOG.md) for the full breakdown.

- [x] 6.1 Record ADR-0001 Python backend disposition
- [x] 6.2 GemmaRuntime composition root extracted (full ChatController / ChatWebviewHost split deferred to v0.5)
- [x] 6.3 src/safety/ -> src/guardrails/ (all 5 modules + BLOCKED_PATTERNS extracted to policy.ts)
- [x] 6.4 src/llm/ port with vendor-neutral types + OllamaClient driver (10 consumers migrated; src/ollama/ deleted)
- [x] 6.5 src/llm/OllamaHttp.ts shared client; OllamaClient + EmbeddingClient compose over it
- [x] 6.6 GoldenTaskSuite + goldenTasksYaml.generated.ts moved to src/evaluation/
- [x] 6.7 src/modes/PlanMode.ts inlined into src/chat/PlanMode.ts; src/modes/ deleted
- [x] 6.8 Tracer singleton retired; constructor injection via GemmaRuntime; tests parallel-safe
- [x] 6.9 Settings injection in ContextCompactor + RegenerateFromSource (panel reads deferred)
- [x] 6.10 src/utils/logger.ts (vscode.OutputChannel wrapper); 25 console.* calls migrated; ESLint no-console -> error
- [x] 6.11 src/utils/errors.ts (formatForUser with redaction; formatForLog); 21 ad-hoc patterns replaced
- [x] 6.12 Zod schemas at LLM boundary (stream chunks + listModels); webview/storage/observability deferred
- [x] 6.13 docs/adr/ scaffolding (README index + MADR template)
- [x] 6.14 scripts/dev-setup.{sh,ps1} + CONTRIBUTING.md + npm run dev
- [x] 6.15 Already satisfied via Phase 5 sub-task 5.19 (no-op)
- [x] 6.16 marked v12 deferred with NOTE(v0.5) (renderer API break; DOMPurify already provides sanitization)
- [x] 6.17 Lint + test + build stabilization (1165 pass / 0 fail; 0 lint errors)

### Phase 7 - Simplification & Release [SUPERSEDED]

*Never shipped under v0.4.0: the release + simplification work rolled into the v0.5.0 Unified Adoption Release (tag `v0.5.4`). Retained as history; not active work.*

17 simplification findings; ~800 LOC target deletion; v0.4.0 tag + VSIX publish.

---

## v0.5.0 - Unified Adoption Release [SHIPPED 2026-04-26]

12-phase consolidation cycle adopting five external comparisons (Claude Code, Routa, Foundry Vault, Token Optimizer MCP, agent-friendly CLI rubric). All phases landed; tag `v0.5.4` cut. Plan: [docs/archive/versions/v0/v0.5.0/plans/implementation-plan.md](archive/v0/v0.5/plans/implementation-plan.md). Architecture: [docs/archive/versions/v0/v0.5.0/architecture.md](archive/v0/v0.5/architecture.md).

- [x] Phase 1 - Identity & Naming (AGENTS.md canonical; CLAUDE.md removed)
- [x] Phase 2 - Tool Surface Hardening (max_bytes, range_start/end, next_offset)
- [x] Phase 3 - Compression Foundation
- [x] Phase 4 - Persistent Cache + Diff-Based Reads
- [x] Phase 5 - Semantic Recall + Budgeting
- [x] Phase 6 - Mutation Safety + Structured Outputs (dry_run, format=json)
- [x] Phase 7 - Memory Hygiene + Corroboration
- [x] Phase 8 - Generic Harness + Specialist Externalization
- [x] Phase 9 - Coverage + Observability
- [x] Phase 10 - Local-Dev Hygiene + CI Hardening
- [x] Phase 11 - Documentation Discipline (ADRs, catalog, governance)
- [x] Phase 12 - Advanced Fallbacks (PredictiveCache, eviction strategies, HeuristicEmbedder, semantic-release)

---

## v0.6.0 - Review-Driven Cycle [SUPERSEDED]

*Closed out by the v1.0.0 four-pillar Nexus rebuild, which superseded the remaining v0.x roadmap. Phases 1, 5, and 6 landed (below); Phases 2, 3, 4, 7, and 8 were never shipped under v0.6.0. Retained as history; not active work.*

Hygiene/ratchet cycle closing the v0.6.0 review pass (1 P0, 6 P1, 9 P2, 11 P3). No new product surface; pays down v0.5.0 technical debt. Plan: [docs/archive/versions/v0/v0.6.0/plans/v0.6.0-cycle.md](archive/v0/v0.6/plans/v0.6.0-cycle.md).

### Phase 1 - Security chain closure [COMPLETED 2026-04-26]

Closes pen-test F-001, F-003, F-004; both legs of Attack Path A refuse the operation. See [docs/archive/versions/v0/v0.6.0/development/history/2026-04_phase-1-security-chain-closure.md](archive/v0/v0.6/development/history/2026-04_phase-1-security-chain-closure.md).

- [x] 1.1 Unify path resolution behind realpath-aware `pathGuard.resolveInsideWorkspace` (with ancestor walk for non-existent leaves) + 7-tool symlink regression test
- [x] 1.2 Clamp `permissionOverrides` so confirmation-tier tools cannot drop to AUTO_APPROVE (with logger warning + dedupe)
- [x] 1.3 Tag MCP-originated tool calls with `source: "mcp"` peer attribution in ConfirmationGate; add `gemma-code.mcpExposedTools` allowlist (read-only by default)
- [x] 1.4 Phase 1 testing and stabilization (17 new tests + lint + deps:check + catalog:check green)

### Phase 2 - Test pipeline reliability + release-gate baselines [SUPERSEDED]
### Phase 3 - Defense-in-depth ratchets [SUPERSEDED]
### Phase 4 - Module-boundary ratchet [SUPERSEDED]

### Phase 5 - Doc/code drift + dead-code cleanup [COMPLETED 2026-05-03]

Closes pen-test F-007, F-008, F-014; known-gaps 4.2, 4.3, 5.1, 5.3, 5.4, sections 8 + 9.7; codebase-review #4, #7, #13, #20. See [docs/archive/versions/v0/v0.6.0/development/history/2026-05_phase-5-doc-code-drift.md](archive/v0/v0.6/development/history/2026-05_phase-5-doc-code-drift.md).

- [x] 5.1 Decide PredictiveCache: deleted (Option B). Hard constraint #1 forbids new product surface; setting was unwired; bench measured latency, not hit-rate.
- [x] 5.2 Decide threshold elevation: implemented (Option A). Per-row provenance threshold in `searchByEmbedding`; `gemma-code.ollamaEmbeddingThreshold` (0.85) + `gemma-code.heuristicEmbeddingThreshold` (0.95) settings; 3 real heuristic-fallback tests replace the `it.todo`.
- [x] 5.3 Delete legacy `gemma-code.gpuTier` setting fallback; CHANGELOG `### Removed` entry.
- [x] 5.4 Architecture-doc inaccuracies: meta-test path corrected; v0.4.0 ship date bumped to 2026-04-25; permission-tier table now generated from `PermissionTiers.ts` via `scripts/generate-tool-permission-table.mjs` and CI-gated in `catalog-sync` (also fixed two inverted tier rows -- delete_file is tier 1, web_search is tier 2).
- [x] 5.5 FIFO-vs-LRU reconciled: `accessed_at` column + index added; `lookup()` bumps it; `_enforceCapacity()` orders by it; hot-vs-cold regression test.
- [x] 5.6 Migration-ordering regression test (4 cases) seeds v0.4.0 schema, asserts all four migrations land cleanly + are idempotent.
- [x] 5.7 Lint + test + build + deps stabilization (1579 pass / 0 fail; 0 lint errors).

### Phase 6 - Panel decomposition [COMPLETED 2026-05-03]

Closes codebase-review #2, #3, #16 (deferred), #23. See [docs/archive/versions/v0/v0.6.0/development/history/2026-05_phase-6-panel-decomposition.md](archive/v0/v0.6/development/history/2026-05_phase-6-panel-decomposition.md).

- [x] 6.1 Extract `ChatController.ts` (agent-loop wiring + slash-command dispatch composition). Routes `submitUserMessage`, `cancelInFlight`, `approveStep`, plan detection, and pre-prompt memory injection. Tests: 12 cases, 90% coverage.
- [x] 6.2 Extract `ChatWebviewHost.ts` (sidebar view + editor panel + postMessage routing + focus tracking). Tests: 7 cases, 99% coverage.
- [x] 6.3 Extract `ChatCommandHandlers.ts` (12 slash commands moved out of the panel). Tests: 33 cases, 86% coverage.
- [x] 6.4 Source-level webview split (Option B): `scaffold.ts` / `bodyMarkup.ts` / `runtime.ts` / `styles.ts`. `index.ts` shrank from 1,573 to 12 lines. Tests: 9 scaffold + 4 CSP cases.
- [ ] 6.5 Optional: split `filesystem.ts` per-tool. **Deferred per plan note** ("Lower-priority; defer if Phase 6 is already large").
- [x] 6.6 Stabilization: lint clean, typecheck clean, deps:check clean, catalog regenerated. Manual flow verification (5 paths) is on the operator -- see history file section 4.

**Partial deviation**: `GemmaCodePanel.ts` is 935 lines (down from 1,724); the plan's < 400 target requires further factory work (PanelComposition + post-helper move) tracked as v0.7.0 follow-up. See history file section 3.1.

### Phase 7 - Polish + simplification [SUPERSEDED]
### Phase 8 - Release gate + ADRs + CHANGELOG [SUPERSEDED]

---

## v0.7.0 - Multi-Source Adoption Cycle [SUPERSEDED]

*Closed out by the v1.0.0 four-pillar Nexus rebuild, which superseded the remaining v0.x roadmap. Phases 0-5 landed (below); Phases 6, 7, and 8 were never shipped under v0.7.0. Retained as history; not active work.*

Phased adoption of comparison-multi-source findings across skills, memory architecture, compaction, render protocol, and per-model context overrides. Plan: [docs/archive/versions/v0/v0.7.0/plans/v0.7.0-cycle.md](archive/v0/v0.7/plans/v0.7.0-cycle.md).

### Phase 0 - Close-out + carryovers [COMPLETED 2026-05-05]
### Phase 1 - Skill expansion (zero-code first) [COMPLETED 2026-05-05]
### Phase 2 - Memory file architecture [COMPLETED 2026-05-05]

### Phase 3 - Compaction stack expansion [COMPLETED 2026-05-05]

Adopts C12 / C13 / C14 / C15 / C16. Adds two deterministic strategies (deduplication, purgeErrors), a model-callable compress tool with two modes (range, message-experimental), per-session CompressionState, six `/compact` verbs, per-model context overrides, and ADR-0012. See [docs/DEVLOG.md](DEVLOG.md) for details.

- [x] 3.1 Deduplication compaction strategy (`src/chat/strategies/deduplication.ts` + 8 tests)
- [x] 3.2 Purge-errors compaction strategy (`src/chat/strategies/purgeErrors.ts` + 5 tests)
- [x] 3.3 CompressionState module + block ID allocation (`src/chat/state/CompressionState.ts` + 6 tests)
- [x] 3.4 `compress_range` tool handler (`src/tools/handlers/compress.ts` + prompt md + 6 tests)
- [x] 3.5 `compress_message` tool handler (experimental, flag-gated; 3 tests)
- [x] 3.6 `/compact <verb>` commands (`src/commands/compactCommand.ts` + 9 tests; sweep auto-issue deferred to Phase 4)
- [x] 3.7 Per-model context-limit overrides (`gemma-code.contextLimitsPerModel` + `resolveModelContextLimit` + 6 tests)
- [x] 3.8 ADR-0012 model-callable compress tool design (renumbered from plan's ADR-0006 because 0006 is taken)

### Phase 4 - Webview render protocol expansion [COMPLETED 2026-05-06]

Adopt the seven Claude-Code-style chat-UI primitives (S7 / C21-C27): inline diff cards, action-type tags, numbered permission prompts, structured todo blocks, "Thought for Ns" meta-rows, queued-message fields, and end-of-task completion reports. History: [docs/archive/versions/v0/v0.7.0/development/history/2026-05_phase-4-webview-render-protocol.md](archive/v0/v0.7/development/history/2026-05_phase-4-webview-render-protocol.md).

- [x] 4.1 Inline diff card (`src/panels/webview/render/diffCard.ts` + 6 tests)
- [x] 4.2 Action-type tag (`src/panels/webview/render/actionTag.ts` + 10 tests)
- [x] 4.3 Numbered permission prompt (`src/panels/webview/render/permissionPrompt.ts` + 10 tests; `ConfirmationGate.requestPrompt`)
- [x] 4.4 Todo block + `update_todos` tool (5 + 4 tests; `src/tools/handlers/todos.ts` permission tier 0)
- [x] 4.5 Thought-for-Ns meta-row (`src/panels/webview/render/thoughtMetaRow.ts` + 5 tests; StreamingPipeline emits)
- [x] 4.6 Queued-message field (renderer 6 tests + ConversationManager queue 4 tests; UX wiring deferred to v0.8.0)
- [x] 4.7 Completion-report block (`src/panels/webview/render/completionReport.ts` + 7 tests; `buildCompletionReport`)
- [x] 4.8 ADR-0013 webview render protocol (renumbered from plan's ADR-0008 because 0006-0012 are taken)

**Follow-ups (deferred to v0.8.0 Phase 1)**: panel-host adoption of the new render protocol -- (a) replace input row with queued-message field during streaming, (b) route `permissionPromptResponse` through `ChatMessageRouter` to `gate.resolvePrompt`, (c) wire `ToolRegistryBuilder.todos` in the panel bootstrap. All three share the same surface; bundling is intentional.

### Phase 5 - Memory commands + manual memory page UI + per-model context limits [COMPLETED 2026-05-07]

Polish the memory experience: complete `/memory` slash-command surface, ship a sidebar `MemoryPanel` webview, and confirm the per-model context-limit override is wired. Plan: [docs/archive/versions/v0/v0.7.0/plans/v0.7.0-cycle.md](archive/v0/v0.7/plans/v0.7.0-cycle.md) Phase 5.

- [x] 5.1 `/memory forget`, `/memory export`, `/memory import` slash commands (extends `ChatCommandHandlers.dispatch("memory", ...)`; new helpers `parseForgetArgs`, `parseImportArgs`, `forgetMatchingSqlRows`; new `MemoryStore.deleteById`; 13 cases + 4 parser cases in `tests/unit/panels/ChatCommandHandlers.test.ts`)
- [x] 5.2 MemoryPanel webview tab (`src/panels/MemoryPanel.ts`, `src/panels/webview/memoryView.ts`, view registered in `package.json`, wired in `src/extension.ts` via `chatPanel.getMemoryFiles()` / `chatPanel.getMemoryStore()`; 13 cases in `tests/unit/panels/MemoryPanel.test.ts`)
- [x] 5.3 ADR-0014 memory file architecture (renumbered from plan's ADR-0007 because 0007 is taken; same pattern as ADR-0013)
- [x] Per-model context limits finalised (no new code; Phase 3 sub-task 3.7 already shipped `resolveModelContextLimit` + 6 tests; tracked as in-cycle gap 10.O.6 for the audit trail)

**Follow-ups carried into v0.8.0**: same three Phase 4 panel-host items (queued-field swap, permissionPromptResponse routing, todos opt-in wiring); no new follow-ups from Phase 5.

### Phase 6 - Multi-harness skill packaging [SUPERSEDED]
### Phase 7 - HNSW vector index (optional) [SUPERSEDED]
### Phase 8 - Release gate + ADRs + CHANGELOG [SUPERSEDED]

---

## v1.x line - Nexus (post-rename)

The project was renamed from Gemma Code to Nexus at v1.0.0 (the four-pillar desktop AI Studio pivot). The detailed boards above (v0.1.0 - v0.7.0) are retained as history. The v1.x cycles are tracked here at cycle/phase altitude; per-task detail lives in each cycle's plan and `known-gaps.md` under `docs/versions/v1/<version>/`.

| Cycle | Theme | Status | Source plan |
|---|---|---|---|
| v1.0.0 | Four-pillar desktop pivot (Coding/Chat/Image/Video) + cross-OS installer + MCP harness + skill catalog | Shipped | `versions/v1/v1.0.0/` |
| v1.1.0 | Stabilization + expansion (hybrid retrieval, memory CLI, session replay, SANA image tier) | Closed 2026-05-26 | `versions/v1/v1.1.0/` |
| v1.2.0 | 2026-05 ecosystem adoption (code-graph MCP, command compressor, AST chunker, PrunedDenseIndex, LSP) | Phases 1-7 landed | `versions/v1/v1.2.0/plans/adoption-ecosystem-2026-05.md` |
| v1.3.0 | skill-cleaner adoption (`nexus skills audit` token-budget report) | All 7 phases landed 2026-05-29 | `versions/v1/v1.3.0/plans/adoption-skill-cleaner.md` |
| v1.4.0 | claude-code-harness adoption + known-gaps closure + Nexus-Hub sync | **Complete (Phases 1-9)**; closed 2026-06-09 | `versions/v1/v1.4.0/plans/adoption-claude-code-harness.md` |
| v1.5.0 | Local Agent Maturity (2026-06 scan: energy telemetry, credential vault, swarm orchestration, multimodal input) + Nexus-Hub surface integration | **Complete (Phases 1-7)** -- Phases 1-3 landed 2026-06-10, Phase 4 2026-06-12, Phases 5-6 2026-06-14, Phase 7 (Nexus-Hub sync + 6-surface integration + acceptance gate) 2026-06-15; ready for release | `versions/v1/v1.5.0/plans/adoption-ecosystem-2026-06.md` |
| v1.6.0 | aisuite harness adoptions + Nexus-AI interactive guide (H1 guide, A4 session viewer, A1 dehydration, A2 trace nesting, A3 adapter registry) | **Complete (Phases 1-6)** -- Phases 1-5 closed 2026-06-15 (H1 interactive guide AS001-AS003; A4 session/trace viewer AS004; A1 session-state artifact dehydration AS005; A2 hierarchical sub-run trace nesting AS006; A3 manifest-driven local-runtime adapter registry AS007 -- demand-gated forward-looking refactor, ADR-0019); Phase 6 FINAL (whole-plan acceptance gate + Hub-sync assessment, AS008-AS010) closed 2026-06-16, all gates green, all 6 definition-of-pass items verified; ready for release | `versions/v1/v1.6.0/plans/adoption-aisuite-harness.md` |
| v1.6.0 (companion) | OpenRouter Fusion technique reverse-engineered local-only (F1 fuse skill, F2 panel executor, F3 VRAM co-residency, F4 budget-panel routing + A/B) | **Complete (Phases 1-5)** -- Phase 1 (F1 `fuse` skill + `council` synthesis upgrade + schema-conformance test, OF001-OF003) landed 2026-06-16; Phase 2 (F2 `PanelExecutor`/`FusionAgent` distinct-model fan-out + F5 untrusted-input/tool-isolation hardening, OF004-OF006) landed 2026-06-16; Phase 3 (F3 `GpuScheduler.enqueuePanel` VRAM-gated panel co-residency + degrade-to-sequential + `ModelPinRegistry.holdForPanel` keep-alive, OF007-OF009) landed 2026-06-16; Phase 4 (F4 `PanelAbHarness` local A/B + opt-in `PanelRouter`/`nexus.llm.panelRouting` heuristic + `PanelExecutor` `GpuScheduler.enqueuePanel` concurrency wiring, OF010-OF012) landed 2026-06-17; Phase 5 FINAL (whole-plan acceptance gate + close-out, OF013-OF015) closed 2026-06-17 -- all six definition-of-pass items verified, no feature code changed. Routing default ships opt-in (off) per the no-degradation gate since the A/B was not run on live models here (`OF010.P4.A`); live route wiring deferred (`OF011.P4.A`); Hub touchpoint not warranted (`OF015.P5.A`); pre-existing `dompurify`/`protobufjs` prod advisories recorded as not-owned (`ENV.P5.A`). README/ARCHITECTURE/CHANGELOG narrative + version tag are semantic-release-owned at merge to `main` | `versions/v1/v1.6.0/plans/adoption-openrouter-fusion.md` |
| v1.7.0 | Self-optimizing skills adoption (golden-task live runner, train/validation/test split + held-out gate, bounded-edit SkillOptimizer + A/B, Pareto frontier, shell introspection) + post-cycle headless runtime | **Complete (Phases 1-6)** -- closed 2026-07-02; post-cycle headless agent runtime + optimizer production wiring landed 2026-07-02 | `versions/v1/v1.7.0/plans/adoption-self-optimizing-skills.md` |
| v1.8.0 | One-shot end-user installer (desktop bundles in releases, desktop provisioner + health check, HF weights downloader, catalog curation + uncensored defaults, desktop-token restyle, Windows .exe completion) | **In-session work complete (Phases 1-6)** -- Phases 1-2 landed 2026-07-02, Phases 3-6 on 2026-07-03; whole-plan acceptance rides the recorded rehearsals (T602 Windows clean-VM + T604 mac/linux = operator `OSI006.P6.A/C`; T603 CI legs + pre-release tag = post-freeze >= 2026-08-01 `OSI006.P6.B`; pin rotation + GPU-box chain `OSI003.P3.A/B/C` + `OSI004.P4.A/B/E`) | `versions/v1/v1.8.0/plans/one-shot-installer.md` |
| v1.9.0 | Installer + Nexus AI Studio experience overhaul (single-artifact installer / drop NSIS, shared brand foundation: transparent+rounded icons + glow tokens + constellation/float-logo primitives, installer visual overhaul + frameless dark title bar + NexusAI path, model-selector metadata (origin/guardrails/agentic) + Gemma-4-agentic + audio pillar, desktop app full UI/UX overhaul) | **COMPLETE (Phases 1-6)** -- all in-session work landed 2026-07-04; Phase 6 FINAL (rehearsal + docs + close-out) re-proved the Windows single-artifact build a second time (`dist/NexusSetup.exe` 75.6 MB, smoke 4/4 green, installer suite 651/2/0). Whole-plan DoD met locally + by construction; the 3-OS on-device visual/behavioral rehearsal (`IAE.P1.B` mac/Linux+VM, `IAE.P2.B`, `IAE.P3.A`, `IAE.P5.A`, `IAE.P5.B`) + post-freeze CI legs (`IAE.P4.C` pin rotation, `IAE.P5.D` bundle build, T602 >= 2026-08-01) are recorded operator/dispatch rehearsals. Version tag + CHANGELOG cut on merge to `main` via semantic-release | `versions/v1/v1.9.0/plans/installer-and-app-experience-overhaul.md` |
| v1.9.0 (UI rework) | Installer + app UI rework: type scale + legibility, static logo (kill float-lag), taskbar/window icon frozen-path fix, two-tone wordmark, modern scrollbars + checkbox, per-provider model colors + plain-language shared catalog copy, whole-app copy pass; desktop app aurora generation animation (Image Studio + Video Lab), chat disclaimer, logo/icon parity | **PROPOSED (2026-07-07)** -- Phases 1-9 / T001-T036 not started; installer PR (P1-7) off the integration branch + app PR (P8-9); operator decisions: animation in the studios (not chat), rewrite shared `catalog.json`, one combined plan | `versions/v1/v1.9.0/plans/installer-and-app-ui-rework.md` |
| v2.2.3 | Glass chrome, thinking orbs, durable pillar runtime, occupancy, workspace-aware Hub harness, installer field fixes | **Implemented on develop (2026-08-23)** -- Phases 1-8 landed; package remains 2.1.0 until `/update release` | `v2/v2.2/plans/v2.2.3-glass-orbs-and-pillar-runtime.md` |
| v2.2.4 | Chatbot-first shell, installed-catalog honesty, compact studio media, Settings Models catalog, Hub update timeout copy | **Implemented on develop (2026-08-23)** -- Phases 1-7 landed; DF-2 packaged Explorer and DF-4 live GPU remain open | `v2/v2.2/plans/v2.2.4-chatbot-first-and-runtime-honesty.md` |
| v2.2.5 | First successful generation: alias-fold chat ids, fail-closed diffusion, Settings Models installer parity, chat explorer chrome, Hub latest | **Released (2026-08-23)** -- package 2.2.5; DF-2 and DF-4 remain open | `v2/v2.2/plans/v2.2.5-first-successful-generation.md` |
| v2.2.6 | Session memory and studio history | **Released in v2.2.8 (2026-08-24)** -- Phases 1-6 landed | `v2/v2.2/plans/v2.2.6-session-memory-and-studio-history.md` |
| v2.2.7 | Context meter and transcript chrome | **Released in v2.2.8 (2026-08-24)** -- Phases 1-5 landed; DF-28 through DF-31 packaged/live soak remain open | `v2/v2.2/plans/v2.2.7-context-meter-and-transcript-chrome.md` |
| v2.2.8 | Working local studio: minutes-class RPCs, shared FolderTree, Models sort, Hub quarantine apply | **Released (2026-08-25)** -- package 2.2.8 tagged; packaged Hi observed; DF-33/34 and DF-4 remain; DF-32/35 closed in v2.2.9 Phase 7 | `v2/v2.2/plans/v2.2.8-working-local-studio.md` |
| v2.2.9 | Field chrome, catalog identity, honest generate | **Released (2026-08-28)** -- package 2.2.9; legacy no-PR exception approved; optional extension pinned to VS Code 1.134.0; packaged field QA deferred to DF-36; live GPU stays DF-4 | `v2/v2.2/plans/v2.2.9-field-chrome-catalog-and-generate.md` |
| v2.3.0 | Optional local video enhancement, Qwen admission gates, and Hub security-workflow handoff | **Released (2026-08-29)** -- package 2.3.0 tagged; DF-1..4, BG-1, WN-1, MT-1, QG-1/QG-2 remain | `v2/v2.3/plans/v2.3.0-adoption-qwen-video2x-openworker.md` |
| v2.3.1 | Windows installer field repair | **Release prepared (2026-08-29)** -- package 2.3.1; PR 53 merged; tag pending; MT-1 remains | `v2/v2.3/plans/v2.3.1-installer-field-repair.md` |
| v2.4.1 | Installer/runtime reliability, transcript reasoning UX, archives, model parity, and multi-root Agents workspaces | **Integration candidate (2026-08-29)** -- T001-T047 complete; package 2.4.1; inherited gates remediated/baselined; T048 authorized for first push/PR and packaged rebuild, with field checks still explicit | `v2/v2.4/plans/v2.4.1-field-reliability-chat-archives-models-workspaces.md` |

### v1.4.0 status (COMPLETE -- closed 2026-06-09)

- Adoptions A1-A12: all 12 landed (Phases 1-6).
- Carryforward: 34 of 36 gaps resolved (6 in Phase 7, 22 in Phase 8, 2 in Phase 9: `1.1.P2.A` + `1.1.P3.B`). Re-justified as Hub-owned (open against the Nexus-Hub repo, not closeable from Nexus-AI): `T017.P3.E`, `T002.P2.A`.
- Phase 9 (FINAL, `T032-T035`) complete: Nexus-Hub integration delta written ([development/nexus-hub-integration-delta.md](v1/v1.4/development/nexus-hub-integration-delta.md)); `DEFAULT_UPSTREAM` fixed `bendourthe/DevAI-Hub` -> `bendourthe/Nexus-Hub`; whole-plan acceptance gate green (build/lint/arch/check/audit-prod/test+coverage; 87.19% lines; the `hono` prod advisory fixed in-phase to 4.12.25); known-gaps finalized; [RELEASE_NOTES.md](v1/v1.4/RELEASE_NOTES.md) written; desktop product version bumped 1.3.0 -> 1.4.0.
- Carried to v1.5.0: P3 deferrals `T016.P3.A`, `T018.P3.A`, `T018.P3.B`, `T022.P3.A` (all four now closed -- the first three in v1.5.0 Phase 4, `T022.P3.A` in Phase 6); the `HUB.P3.*` Hub-v3.x integration items; `T034.P2.A` (dev-only npm advisories).

### v1.5.0 plan (newly filed this session, 2026-06-09)

Derived from the [2026-06 ecosystem comparison](v1/v1.5/comparison-ecosystem-2026-06.md) (8 sources; the scan largely validated the existing v1.x direction). RE-first ordered; 7 phases:

- [x] Phase 1 - Local-only foundations: Gemma 4 GGUF quant ladder, OS-keychain credential vault, intelligence-per-watt telemetry (landed 2026-06-10; T001-T004; [known-gaps](v1/v1.5/known-gaps.md), [session history](v1/v1.5/development/history/2026-06_phase-1-local-only-foundations.md))
- [x] Phase 2 - Skill-native: DCI search-discipline skill + agent presets (landed 2026-06-10; T005-T007; authored in Nexus-Hub on branch `feat/dci-discipline-and-agent-presets` -- `developer-experience/direct-corpus-interaction` + `workflow/agent-presets`; merge/publish/`nexus skills sync` deferred to Phase 7/T023; [session history](v1/v1.5/development/history/2026-06_phase-2-skill-native.md))
- [x] Phase 3 - Inbound security: prompt-injection classifier (warn-then-allow) (landed 2026-06-10; T008-T009; `modules/coding/security/InboundClassifier.ts` routed over `fetch_page`/`web_search` in `AgentLoop`; toggles `nexus.coding.inboundClassifier.enabled`/`.deepScan`; [session history](v1/v1.5/development/history/2026-06_phase-3-inbound-security.md))
- [x] Phase 4 - Swarm/DAG orchestration over worktree sub-agents (landed 2026-06-12; T010-T014; `CriticAgent` + `DAGExecutor` isolate/critic options + `Orchestrator` `swarmEnabled` flag + read-tool worktree rooting + live PreCompact hook; opt-in `nexus.coding.swarmOrchestration.enabled` default off; closes v1.4.0 `T018.P3.A`, `T018.P3.B`, `T016.P3.A`; [session history](v1/v1.5/development/history/2026-06_phase-4-swarm-orchestration.md))
- [x] Phase 5 - Model-layer & desktop re-partials (landed 2026-06-14; T015-T020; multimodal image input + `isVisionCapableModel` vision gate (item 33), `PreviewPane` side-by-side pane (item 24), vault-only `CredentialsSettings` + `credentials.*` IPC (item 25), persistent `JsonFileSessionStore` + resume-with-history (item 26); item 38 local cron DEFERRED -- `T019.P3.A`; partially closes `T001.P3.A`; [session history](v1/v1.5/development/history/2026-06_phase-5-desktop-re-partials.md))
- [x] Phase 6 - Carryforward: Tree-sitter `.wasm` packaging (landed 2026-06-14; T021-T022; scanner bundled-wasm-dir override `setTreeSitterWasmDir` + `Parser.init({locateFile})`, `build:sidecar` esbuild config copies the 4 grammar `.wasm` + web-tree-sitter runtime `.wasm` into `sidecar/dist/wasm` with a CJS dist marker + startup warm-up, VSIX `.vscodeignore` grammar trim; closes v1.4.0 `T022.P3.A`; full Tauri `build:shell` not run here (cargo absent) -- `T021.P3.A`; [session history](v1/v1.5/development/history/2026-06_phase-6-tree-sitter-packaging.md))
- [x] Phase 7 - FINAL: Nexus-Hub sync + whole-plan acceptance gate (landed 2026-06-15; T023-T024; published the 2 Phase 2 skills to Hub `develop` (`fe8eb68`, no tag); integrated all four net-new non-skill Hub surfaces -- `HUB.P3.DATA` (sync index enrichment), `HUB.P3.RULES` (language rules -> prompt), `HUB.P3.AGENT` (persona dispatch), `HUB.P3.CMD` (hub-command routing), `HUB.P3.HOOK` (sparse-checkout + installer), `HUB.P3.MCPCFG` (policy-gated registry filter); whole-plan acceptance gate green (root 4080 / desktop 445 / lint / tsc / arch / tampering / prompts / security / audit-prod all clean); see [integration delta](v1/v1.5/development/nexus-hub-integration-delta.md). Also fixed the v1.5.0 CI/Nightly/Installer-smoke workflows (docs-sync regen, installer `[dev]` extra + importlib.metadata, smoke `--skip-extension` + venv/extension drift, Windows ollama auto-start/IPv4 + missing httpx dep) and the Dependabot CI hygiene (branch `fix/dependabot-ci-hygiene`).)

**Branch**: `feat/v1.5.0-phase-3-inbound-security` (chained off the Phase 2 branch; v1.5.0 not yet merged to `main`). All 7 phases complete; ready for release.

### v1.9.0 UI rework plan (newly filed this session, 2026-07-07)

Follow-up to the COMPLETE `installer-and-app-experience-overhaul`, authored from an operator review of the shipped v1.9.0 installer + app (screenshots 2026-07-06/07). Fixes readability, animation-lag, taskbar-icon, model-copy, per-tab-color, scrollbar/checkbox, and wordmark issues in the PyQt installer, and adds a generation animation + chat disclaimer to the Tauri app. Operator decisions (2026-07-07): the aurora animation lives in the Image Studio + Video Lab preview boxes (not chat); rewrite the shared `core/registry/catalog.json` copy; one combined plan, separate PRs per workstream. Additional Phase-1 operator decisions (2026-07-07): confirmed type scale (Display 34 / H1 28 / H2 20 / H3 17 / Body 16 / Caption 14, 14px floor), per-provider color **derived from `family`** (no catalog-schema change), and the proposed provider palette. Progress: 9 / 9 phases (T001-T036) COMPLETE; code-complete + green, pending on-device visual QA (`UIR.P7.A`/`P8.A`/`P9.A`) + the merge-to-main release (semantic-release-owned version/tag). Full detail + per-phase DoD in the [plan](v1/v1.9/plans/installer-and-app-ui-rework.md).

- [x] Phase 1 - Shared design foundations: type scale tokens, provider color palette, aurora/shimmer spec, plain-language copy template (landed 2026-07-07; T001-T004; `FS_*`/`FW_*`/`TYPE_SCALE` + `PROVIDER_COLORS`/`FAMILY_TO_PUBLISHER`/`provider_color()` in [constants.py](../scripts/installer/src/nexus_installer/constants.py), aurora spec + copy template ratified in [ui-rework-design.md](v1/v1.9/ui-rework-design.md); installer suite 657 passed / 2 skipped / 0 failed (+6 regression tests in test_brand_tokens.py); ruff clean; no page/widget wiring yet -- that is Phases 3/6/8)
- [x] Phase 2 - Shared `catalog.json` plain-language copy rewrite + keep the 3 readers/schema green (TS validator, Python loader, app types) (landed 2026-07-07; T005-T007; rewrote all 34 user-facing `description`s to the T004 template (maker + kind + origin country; jargon relocated to `differentiators`), byte-preserving edit so only 35 desc/diff lines changed; T005 needed no `publisher` field (Phase 1 derive-from-family); readers green -- TS validateSpec 32, Python loader 46, desktop typecheck clean; +1 regression guard in catalog.test.ts; 4 task=null components left unchanged (`UIR.P2.A`))
- [x] Phase 3 - Installer typography + hierarchy sweep: replace ~90 inline font sizes with scale tokens, fix one-off sizes, enforce a 14px floor (landed 2026-07-07; T008-T011; theme.py gains pageTitle/sectionHead scale-classes + scale-wired secondaryLabel/mutedLabel; all active pages/widgets consume FS_* tokens (grep gate: 0 literal font-sizes in active files); one-offs mapped (19->H2, 18->H3, 13->caption, 11pt/12pt->px); height docstrings reconciled; installer suite 658 passed / 2 skipped / 0 failed; +1 T008 guard; offscreen composition + objectName wiring verified; visual per-page QA -> Phase 7 (`UIR.P3.A`))
- [x] Phase 4 - Installer logo de-lag + two-tone wordmark + stepper legibility (landed 2026-07-07; T012-T017; retired FloatingLogo (deleted widget+test) for a new StaticLogo (no animation); removed the Welcome logo lockup; header two-tone wordmark (Nexus #eaf6f8/700 + AI Studio #6f8990/600); step counter -> FS_BODY; stepper labels 8pt -> 14px with LABEL_GAP 14 + LABEL_Y_OFFSET 36 + STEP_BAR_HEIGHT 112 + word-wrap; installer suite 656 passed / 2 skipped / 0 failed; +test_static_logo.py; min-width label fit + wordmark render -> Phase 7 visual QA (`UIR.P4.A`))
- [x] Phase 5 - Installer chrome: frozen-path taskbar/window icon fix + PyInstaller spec datas, transparent pill scrollbars, modern per-model checkbox (landed 2026-07-08; T018-T021; `asset_file`/`resolve_window_icon` `_MEIPASS`-first resolver in registry_paths, main.py + window.py setWindowIcon; spec stages icon.ico/.png/mark; pill scrollbars; custom-painted `ModelCheckBox` (crisp glyph + all states) + modern base checkbox QSS; **frozen build proven**: NexusSetup.exe 73.7MB boots + --check-registry resolves _MEIPASS + icon.ico in bundle TOC; installer suite 670/2/0; +14 tests; visual taskbar/scrollbar/checkbox render -> Phase 7 (`UIR.P5.A`))
- [x] Phase 6 - Installer Models page: per-provider color (not per-tab), plain-language card layout with a readable "Best for", simplified intro/total copy (landed 2026-07-09; T022-T025; CatalogModel gains `family`, cards colored by `provider_color(family)` (neutral tab bar) so a model shows one color across Chat/Agentic; card rebuilt to lead with the plain description + a full-width "Best for" from strengths + a slim fact-pill row (Uncensored flag only when applicable); subtitle/totals simplified + "Reset to recommended" button; per-provider legend when >1 provider; installer suite 672/2/0; +2 tests; offscreen verified no model shows >1 color; card-color readability -> Phase 7 (`UIR.P6.A`))
- [x] Phase 7 - Installer whole-app copy/readability pass + end-to-end frozen-build visual QA against DoD 1-8 (landed 2026-07-09; T026-T028; simplified jargon copy (gpu tier labels drop MoE/Dense/codenames, install_path callout drops command/venv/package-manager wording, config toggles drop chain-of-thought/cross-session); consistency pass green (grep gate: 0 literal font-size in active files); installer suite 672/2/0; installer PR (Phases 1-7) code-complete; on-device DoD 1-8 walk-through consolidated as `UIR.P7.A` (subsumes P3.A/P4.A/P5.A/P6.A) for operator rehearsal)
- [x] Phase 8 - App generation animation: reusable aurora `GenerationCanvas` in the Image Studio + Video Lab preview boxes, progress-coupled, reduced-motion aware (landed 2026-07-09; T029-T032; new `desktop/src/components/GenerationCanvas.tsx` (3 drifting blurred radial layers + shimmer, mix-blend screen, staggered 9-11s loops) + aurora keyframes in globals.css gated behind prefers-reduced-motion (static cyan glow fallback); live latent overlaid + materializing with progress; mounted in Image Studio (tint image) + Video Lab (tint video, backs the thumbnail strip, TimelinePreviewer handoff); desktop suite 520 passed (+5); tsc + eslint clean; live animation + reduced-motion render -> `UIR.P8.A`)
- [x] Phase 9 - App chat disclaimer under the shared composer + logo/icon parity audit + end-to-end visual QA against DoD 9-11 (landed 2026-07-09; T033-T036; accuracy disclaimer under the shared ChatInput (shows under chat + coding); generate-icons.py now emits window-icon.png (no longer stale-prone); Dashboard FloatingLogo confirmed transform-only (low-lag by construction, no swap needed); desktop suite 521 passed (+1); tsc + eslint clean; app on-device QA (DoD 9-11) -> `UIR.P9.A`). **All 9 phases code-complete; cycle done.**

**Branch**: all v1.9.0 UI-rework work is on `feat/v1.9.0-installer-phase-1` (operator kept both workstreams on one branch; can split at push). Phases 1-4 landed 2026-07-07, Phase 5 landed 2026-07-08, Phases 6-9 landed 2026-07-09 -- **all 9 phases code-complete**. Ready to merge to `main` (semantic-release cuts the version/CHANGELOG/tag) once the on-device visual QA passes. Open on-device items: `UIR.P7.A` (installer DoD 1-8 walk-through), `UIR.P8.A` (app aurora render + reduced-motion), `UIR.P9.A` (app DoD 9-11: disclaimer + both auroras + taskbar icon + Dashboard logo).

---

## Recurring Obligations

- [ ] Review AGENTS.md against current model behavior every 6 months -- next review due 2026-11-26. (Older prompt workarounds may now block beneficial new model behaviors; see the `## AGENTS.md review cadence` section in [AGENTS.md](../AGENTS.md). This date is the canonical source referenced by AGENTS.md. Added in v1.2.0 Phase 1.4.)
