# Cross-Project Comparison: Nexus vs. the August 2026 Open Local-AI Wave

**Version**: v2.1.0
**Adoption target**: v2.1.0 (confirmed by user; v1.16 in flight, v1.17-v1.19 and v2.0.0 hold committed plans, and the user chose a fresh minor after the v2.0 convergence over the next v2.0.x patch slot)
**Generated**: 2026-08-12
**Analyzer**: Claude Code -- /compare command (multi-source)
**External Sources** (6):

| # | Source | Type |
|---|---|---|
| S1 | [block/buzz](https://github.com/block/buzz) -- Block's self-hosted workspace with built-in AI agents | Repository |
| S2 | [Meta Muse Glimmer](https://research.meta.ai/blog/introducing-muse-glimmer-open-agentic-model) -- open-weight ~30B agentic model | Article + HF model card |
| S3 | [invoke-ai/InvokeAI](https://github.com/invoke-ai/InvokeAI) -- open-source creative studio on Stable Diffusion | Repository |
| S4 | [NVIDIA Nemotron 3.5 Lightning 30B-A3B](https://huggingface.co/nvidia/NVIDIA-Nemotron-3.5-Lightning-30B-A3B-NVFP4) + [NeMo Switchyard](https://github.com/NVIDIA-NeMo/Switchyard) router | Model page + repository |
| S5 | [unslothai/unsloth](https://github.com/unslothai/unsloth) -- fine-tuning library + new desktop app | Repository |
| S6 | [google/diffusiongemma-26B-A4B-it](https://huggingface.co/google/diffusiongemma-26B-A4B-it) -- discrete-diffusion multimodal LM | Model page |

**Pre-ingest source-security verdict**: CLEAR for S2, S3, S4, S6; PROCEED-WITH-CAUTION for S1 and S5 (benign, quoted in Section 5.0). No BLOCK verdicts; ingestion proceeded for all six.

---

## Section 1: Executive Summary

Six simultaneous releases sketch where the local-AI ecosystem moved in mid-2026, and they cluster into three signals for Nexus.

**Signal 1 -- the 30B-total / ~3-4B-active MoE class is now the local agentic sweet spot.** Muse Glimmer (S2, Apache 2.0, SWE-Bench Verified 76.0, Ollama-supported), Nemotron 3.5 Lightning (S4, OpenMDW-1.1, 1M context, official ggml GGUF, Ollama >= 0.32.9), and DiffusionGemma (S6, Apache 2.0, discrete diffusion, runtime not yet mainlined) are all 26-30B MoE models aimed at consumer GPUs. Two of the three are drop-in catalog candidates through Nexus's existing Ollama channel with zero new infrastructure; the third is a watch item until llama.cpp PR #24423 mainlines. Muse Glimmer in particular would likely displace Qwen 2.5 Coder as the strongest local coding model in the catalog's 24 GB tier.

**Signal 2 -- routing is the new differentiator, and it is pure control logic.** NeMo Switchyard (S4, Apache 2.0) ships three tuning-free routing heuristics (LLM-classifier with session affinity, tool-activity stage routing, cheap-first escalation on sustained difficulty) that map one-to-one onto signals Nexus already collects in `core/telemetry/TelemetryBus.ts`, `core/observability/InferenceMetrics.ts`, and `src/tools/AgentLoop.ts`. The Rust proxy itself duplicates Nexus's v1.16.0 serving gateway and is rejected; the heuristics are the highest-leverage reverse-engineering candidate in this report.

**Signal 3 -- the "local AI studio" category is converging, so defend the moats and close the two real gaps.** Unsloth Desktop (S5) and InvokeAI (S3) show competitors converging on chat + serve + generate; Buzz (S1) shows agents becoming first-class identities. Nexus's durable moats (hardware-aware installer/catalog, quant ladder, per-model harness selector, GPU scheduler, skill self-optimization loop) appear in none of the six sources. The two genuine capability gaps: (a) **local fine-tuning** -- Unsloth Core (Apache 2.0) as an installer-provisioned runtime, like Ollama, under a Nexus-original train-on-your-repos/chats UI gated by the existing golden-task runner; and (b) **image-pillar depth** -- InvokeAI's queue/batch, embedded-metadata recall, and SAM2 segmentation-driven inpainting are all reverse-engineerable onto Nexus's existing SANA/SDXL pipelines without adopting InvokeAI's Python stack.

**Overall recommendation**: adopt two catalog models (Muse Glimmer, Nemotron Lightning) plus their harness profiles as the quick wins; reverse-engineer the Switchyard routing heuristics into the harness selector / planner-critic-worker DAG as the headline v2.1.0 engineering item; plan the Unsloth-powered fine-tuning pillar as the one large addition; harvest InvokeAI's queue, metadata-recall, and SAM2 patterns for the Image Studio; and take Buzz's audit-log / agent-identity ideas as small hardening items. Drop everything multi-user, hosted, or wrapper-shaped (Section 7).

## Section 2: Source Profiles

| Aspect | Nexus (this project) | S1 buzz | S2 Muse Glimmer | S3 InvokeAI | S4 Lightning + Switchyard | S5 unsloth | S6 DiffusionGemma |
|---|---|---|---|---|---|---|---|
| Kind | Local-first desktop AI Studio (4 pillars) | Self-hosted human+agent workspace on a Nostr relay | Open-weight ~29.6B agentic LLM | Self-hosted SD creative studio | 30B-A3B MoE LLM + Rust model router | Fine-tuning library + Tauri desktop app | 26B-A4B discrete-diffusion multimodal LM |
| License | MIT | Apache 2.0 | Apache 2.0 | Apache 2.0 (+ model-component licenses) | OpenMDW-1.1 (model); Apache 2.0 (router) | Apache 2.0 core; AGPL-3.0 optional UI components | Apache 2.0 (not Gemma ToU) |
| Stack | TypeScript + Tauri + Node sidecar + Python sidecars | Rust (Axum) + React/Tauri + Flutter; Postgres/Redis/MinIO | Weights (HF + GGUF); Ollama >= 0.32.7 | Python (diffusers) + React WebUI; node-graph engine | Weights (HF + ggml GGUF, Ollama >= 0.32.9); Rust router | Python + Triton kernels; Tauri desktop | Weights (HF + unsloth GGUF); needs llama.cpp diffusion branch (PR #24423) |
| Local-model posture | Core identity (Ollama + local diffusion) | None -- agents bring external runtimes (Goose/Codex/Claude Code) | Local-first by design (< 20 GB compressed) | Fully local | Local-capable (GGUF); NVFP4 native only on Blackwell | Fully local | Local, but runtime bleeding-edge |
| Relevance | -- | Agent identity, audit log, workflows, ACP | Coding-pillar catalog candidate | Image Studio depth | Router logic + worker-model candidate | Fine-tuning pillar | Multimodal chat + watch-item model |

## Section 3: Insight Classification (per source)

Classification statuses: **Already** (Nexus does this; evidence cited), **Partial**, **Missing**, **N/A** (out of scope or conflicts with design principles).

### S1 -- block/buzz

| ID | Insight | Status in Nexus |
|---|---|---|
| BZ1 | Unified append-only signed event log spanning chat, coding actions, workflows (Nostr events, per-actor keys) | Partial -- `core/telemetry/TelemetryBus.ts` and session replay exist, but no signed, unified, cross-pillar audit log |
| BZ2 | Agents as first-class members: per-agent identity, membership, action attribution | Partial -- sub-agent dispatch exists (planner/critic/worker DAG) but actions are not attributed to distinct agent identities |
| BZ3 | Agent-first JSON-in/JSON-out CLI designed for LLM tool calls | Missing -- Nexus is GUI-centric; the sidecar has no headless JSON CLI surface |
| BZ4 | YAML workflow engine with message/reaction/schedule triggers | Missing -- no declarative trigger-driven automation |
| BZ5 | ACP harness adapter letting external agent runtimes (Goose/Codex/Claude Code) drive the workspace | Partial -- the v1.16.0 serving gateway exposes models to external tools, but not sessions/tools |
| BZ6 | Frame-anchored comments on media (pin feedback to a video frame) | Missing -- Video Lab has a timeline previewer but no anchored-annotation iteration loop |
| BZ7 | Unified search across conversations, patches, workflow runs | Partial -- hybrid retrieval (BM25+dense+graph RRF) is stronger per-corpus but scoped to chat |
| BZ8 | Multi-user rooms, multi-tenant relay, Nostr git collaboration | N/A -- conflicts with single-user local-first design |

### S2 -- Muse Glimmer

| ID | Insight | Status in Nexus |
|---|---|---|
| MG1 | Apache-2.0 30B agentic model, GGUF + Ollama 0.32.7 support, SWE-Bench Verified 76.0 | Missing from catalog -- would be the strongest local coding model at the 24 GB tier |
| MG2 | Meta-published quant tiers (K-Quant-17GB for 24 GB VRAM at 1.0% degradation; K-Quant-Dynamic for 32 GB at 0.2%) | Missing -- maps directly onto the hardware-aware per-VRAM-tier catalog; model exceeds 8-12 GB tiers entirely |
| MG3 | Adjustable reasoning strength + scaffold-compatibility conventions needing a harness profile | Missing -- per-model harness selector exists (v1.12.0) but has no Muse Glimmer profile |
| MG4 | Strong MCP/tool-workflow scores (MCP Atlas 75.5) suggesting a default agentic route + local LLM-as-judge for the skill optimizer | Partial -- routing tables exist; judge model is configurable |
| MG5 | Built-in perception encoder (screenshots/charts/docs) counting against the VRAM envelope | Missing -- and contingent on the Ollama GGUF shipping the vision projector (unverified) |
| MG6 | DFlash speculative decoding (3.1x on RTX 5090) | N/A today -- upstream-dependent; 5090 is above the stated hardware ceiling |

### S3 -- InvokeAI

| ID | Insight | Status in Nexus |
|---|---|---|
| IV1 | Session queue + batch generation (seed/prompt sweeps, fire-and-forget) | Missing -- GPU scheduler exists but no persistent generation job queue or batch expansion |
| IV2 | Generation metadata embedded in image files + recall actions (Use Prompt / Use Seed / Use All / Remix) | Missing -- cheap and high-fit for the chat-style Studio ("regenerate from this image") |
| IV3 | SAM/SAM2 segmentation-driven masking ("replace the car" without manual masks) | Missing -- arguably fits Nexus's chat UX better than InvokeAI's own canvas |
| IV4 | Layer-based Unified Canvas (raster/control/regional-reference/inpaint-mask layers, typed drag-drop, bounding-box region) | Missing -- Nexus infers mode from attachments but offers no spatial control |
| IV5 | Low-VRAM partial loading knobs (`max_cache_ram_gb`, `max_cache_vram_gb`, `device_working_mem_gb`, RAM-copy toggle) | Partial -- DiffusionTier policy gates resolutions, but no explicit layer-streaming budget knobs for diffusion |
| IV6 | Regional prompting (region-scoped prompts via attention masking) | Missing -- needs backend pipeline work; depends on IV4 |
| IV7 | Boards/gallery organization (polymorphic image+video boards, auto-add, virtual By-Date) | Partial -- chat-thread history organizes generations; no cross-pillar boards |
| IV8 | Node-graph workflow editor + community node plugins | N/A -- conflicts with chat-first, parameters-behind-Advanced-settings philosophy; the reproducible-pipeline idea survives as "saved presets" |
| IV9 | Model ecosystem breadth (SD1.5/2.0/SDXL/SD3.5/FLUX/CogView4 + LoRA/TI/ControlNet management) | Partial -- deliberate lean curation; selective LoRA support is the only piece worth taking |

### S4 -- Nemotron Lightning + NeMo Switchyard

| ID | Insight | Status in Nexus |
|---|---|---|
| NL1 | Lightning as a fast "routine-step" worker model: 3B active, 1M context, official GGUF, Ollama >= 0.32.9; Q4_K_M 22.5 GB (24 GB tier native, 16 GB via expert offload) | Missing from catalog -- the MoE expert-offload profile is unusually favorable for the existing disk-offload quant-ladder rung |
| NL2 | Switchyard's three tuning-free routing heuristics: LLM-classifier with session affinity, tool-activity stage router (error streaks, unproductive loops, prolonged exploration), cheap-first escalation on sustained difficulty | Missing -- Nexus's DAG assigns roles statically; all needed signals already exist in TelemetryBus / InferenceMetrics / AgentLoop traces |
| NL3 | Session affinity + hysteretic (anti-thrash) escalation as a single-GPU scheduling concern (model-swap cost) | Missing -- directly relevant to the GPU scheduler when worker and strong model cannot coexist in VRAM |
| NL4 | Switchyard Rust proxy (OpenAI+Anthropic translation server) | Already -- duplicates the v1.16.0 serving gateway (`desktop/sidecar/src/serving/`) |
| NL5 | Prefill router (residual-stream complexity prediction, needs training data) | N/A -- not viable through Ollama's API |

### S5 -- unsloth

| ID | Insight | Status in Nexus |
|---|---|---|
| UN1 | Local no-code fine-tuning (QLoRA "2x faster / 70% less VRAM") on single consumer GPUs, validated by Unsloth Desktop's existence | Missing -- Nexus has no training capability at all; its golden-task runner + skill-optimization loop is a ready-made post-fine-tune eval gate Unsloth lacks |
| UN2 | Fine-tune -> GGUF -> Ollama export path closing the loop back into existing serving | Missing -- but lands on infrastructure Nexus already has |
| UN3 | Unsloth Dynamic 2.0 GGUF quants on HF as best-in-class low-bit artifacts | Partial -- the catalog can reference these as data (no code adoption); complements the extreme-low-bit ladder |
| UN4 | Training-recipe knowledge (hyperparameters, dataset formats from PDF/CSV/JSON) | Missing -- encodable as a Nexus skill/preset with zero dependencies |
| UN5 | Unsloth Desktop app itself (chat + serve + train + search + tunnels) | N/A -- direct product competitor; UI layer likely AGPL-3.0; reference only |

### S6 -- DiffusionGemma

| ID | Insight | Status in Nexus |
|---|---|---|
| DG1 | Discrete-diffusion text generation (15-20 accepted tokens/forward pass, ~4x throughput) at 26B-A4B, Apache 2.0 | N/A today -- requires the llama.cpp diffusion branch (PR #24423) / `llama-diffusion-cli`; stock Ollama support incomplete; watch item |
| DG2 | Image AND video input to text output in Nexus's VRAM class (Q4_K_M 16.8 GB) | Missing -- the Chat pillar has no image/video attachment path; the plumbing pays off for any VLM regardless of this model |
| DG3 | Quality deltas are steep exactly where agentic coding lives (AIME -19 pts vs Gemma 4; Google positions it as experimental) | Insight only -- if ever adopted, gate to Chat drafting, never the coding harness; encode as catalog capability flags (`diffusion=true`, `coding-eligible=false`) |
| DG4 | Would demand a second installer-provisioned inference runtime (custom llama.cpp branch or vLLM) | N/A -- contradicts single-runtime simplicity until upstream mainlines |

## Section 4: Gap Analysis Summary

### 4a: Strengths to preserve (Nexus-only, appearing in none of the six sources)

- Hardware-aware per-VRAM-tier model catalog + one-shot cross-OS installer (S3 leaves VRAM tuning to manual YAML; S5's app has no tiering; S1 has no local inference at all).
- Per-model harness selector (`modules/coding/orchestration/HarnessSelector.ts`) and the quant ladder with extreme-low-bit + disk-offload tiers.
- GPU/VRAM telemetry + cross-pillar GPU scheduler.
- Local skill self-optimization loop (golden-task runner, bounded-edit optimizer) -- the eval discipline none of the six ship.
- Privacy plumbing: `redactSecrets` pre-index filter, credential vault, local-only telemetry.
- Governance note: InvokeAI's hosted business collapsed into Adobe and Buzz outsources inference to cloud agent runtimes -- both validate the local-first, originality-over-wrappers posture.

### 4b: The two structural gaps

1. **No training capability** (UN1/UN2): every competitor motion in S5 points at fine-tune-on-your-own-data as the next studio pillar; Nexus's eval loop makes its version defensibly better.
2. **Image Studio depth** (IV1-IV5): the chat-first UX is ahead of InvokeAI ergonomically but behind it in queueing, provenance/recall, segmentation-assisted editing, and VRAM-budget knobs.

## Section 5: Security and Risk Assessment

### 5.0 Pre-ingest source-security scan (Step 1.5)

| Source | Verdict | Findings |
|---|---|---|
| S1 buzz | PROCEED-WITH-CAUTION | Repo contains agent-directed instruction files by design (`AGENTS.md`, `CLAUDE.md`, `VISION_AGENT.md`, `.claude/skills`, `.goose/skills`) instructing AI *contributors* (e.g. "run `. ./bin/activate-hermit`", "git commit -s mandatory"). Treated as data, not followed. Anyone cloning it for study must keep those files out of an active agent's instruction path. Hermit binary bootstrap noted as a mild supply-chain surface. No injection payloads. |
| S2 Muse Glimmer | CLEAR | No agent-directed instructions on the blog or HF card. |
| S3 InvokeAI | CLEAR | Ordinary product documentation; no injection content. |
| S4 Lightning/Switchyard | CLEAR | One superficial pattern-match in the Switchyard README ("The coding agent you launch must also be installed and on your PATH") is human installation documentation, not agent-directed. |
| S5 unsloth | PROCEED-WITH-CAUTION | Docs page advertises a dynamic docs-query HTTP endpoint addressed to agents ("you can query the documentation dynamically by asking a question" via GET to `unsloth.ai/docs/desktop.md`). Benign affordance, not an injection; the endpoint was NOT called. Treated as data. |
| S6 DiffusionGemma | CLEAR | HF card and unsloth GGUF page are developer documentation only. |

### 5.1 Threat-model comparison

| Vector | Nexus today | If the adoption plan below is executed |
|---|---|---|
| New runtime dependencies | Ollama, bundled embedder, local diffusion pipelines | +Unsloth Core (Apache 2.0, installer-provisioned Python env, like Ollama); +SAM2 weights (local). No library dependency on Switchyard, InvokeAI, or Buzz -- patterns only. |
| Outbound destinations | HF weight pulls at install time (opt-in), Nexus-Hub catalog sync | Unchanged in kind: only additional one-time HF weight downloads (Muse Glimmer, Lightning, SAM2, Unsloth base models). No new telemetry, no hosted inference. |
| Credentials / API keys | Local bearer token for the loopback gateway; credential vault | None added. Gated-model HF tokens reuse the existing v1.14.0 auth flow. |
| Data leaving the machine | None without opt-in | None added. Fine-tuning data (user repos/chats) stays local; hosted Muse Glimmer providers (Together/Fireworks/OpenRouter) are explicitly rejected. |
| New commercial relationships | None | None. All adopted artifacts are Apache 2.0 / OpenMDW-1.1 open weights. |

### 5.2 Per-item risk scorecard and 5.3 RE classification

| Item | Risk | RE class | Notes |
|---|---|---|---|
| MG1/MG2 Muse Glimmer catalog entry + tiers | Low | vendor-intrinsic weights, skill-native integration | Weight download only; Apache 2.0; Meta-reported benchmarks must be re-verified by the local harness eval before default-route promotion |
| MG3/MG4 Muse Glimmer harness profile + routing | None | skill-native | Prompt/template/config work from public docs |
| NL1 Lightning catalog entry (dual-tier) | Low | vendor-intrinsic weights, skill-native integration | OpenMDW-1.1 permits reference-in-catalog; GGUF via ggml-org |
| NL2/NL3 Switchyard routing heuristics + anti-thrash escalation | None | re-full | Pure local control logic over existing telemetry; Apache 2.0 reference readable; MCP Registry Policy bucket 3 |
| UN1/UN2 Fine-tuning pillar on Unsloth Core | Medium | vendor-intrinsic provisioned runtime | Triton kernels not reverse-engineerable; bundle like Ollama per policy bucket 4 rationale in Section 6; AGPL boundary must be verified to Apache-2.0-only components before shipping |
| UN3 Dynamic GGUF quant references | Low | catalog data | Data, not dependency |
| UN4 Training-recipe skill | None | skill-native | |
| IV1 Generation job queue + batch | None | re-full | Extends existing GPU scheduler |
| IV2 Embedded metadata + recall | None | re-full | Local PNG metadata |
| IV3 SAM2 segmentation-driven inpainting | Low | re-full | Local SAM2 weights (~modest VRAM); Apache 2.0 |
| IV4 Layer canvas | None | re-partial | UI patterns only; large frontend effort |
| IV5 Diffusion VRAM budget knobs | None | re-full | Config surface + layer offload in local pipelines |
| IV6 Regional guidance | None | re-partial | Depends on IV4; SANA pipeline work unproven |
| BZ1 Signed local audit log | None | re-full | Local keys + SQLite; no Nostr network |
| BZ2 Per-agent identity for sub-agents | None | re-full | |
| BZ3 Headless JSON CLI over the sidecar | Low | re-full | New local attack surface; reuse gateway token auth |
| BZ6 Frame-anchored media comments | None | re-partial | UI pattern |
| DG2 Chat multimodal attachment plumbing | None | re-full | Model-agnostic; pays off for any VLM |
| DG1/DG4 DiffusionGemma catalog entry | Medium | defer (watch) | Requires non-mainlined runtime; revisit when llama.cpp PR #24423 ships in an Ollama release AND sub-16 GB quants appear |

## Section 6: Prioritized Adoption Plan (Step 5.4 ordering: skill-native -> RE builds -> vendor-intrinsic; P-tiers within buckets)

### Bucket 1 -- skill-native

| # | Item | Value | Effort | Priority | Rationale |
|---|---|---|---|---|---|
| A1 | MG1+MG2: add Muse Glimmer to `core/registry/catalog.json` -- K-Quant-17GB at the 24 GB tier, K-Quant-Dynamic at 32 GB, excluded below 16 GB | High | Low | P0 | Likely strongest local coding model in class; existing Ollama channel; MCP Registry Policy: local-only weight download, no new processor |
| A2 | MG3: Muse Glimmer harness profile (reasoning-strength plumbing, function-calling template, long-horizon conventions) | High | Low | P0 | Prereq for A1 being useful; public docs |
| A3 | NL1: Nemotron Lightning dual-tier catalog entry (24 GB native Q4_K_M / 16 GB expert-offload rung) + Qwen3-Coder-parser harness profile | High | Low | P1 | Purpose-built routine-step worker for A5; Ollama >= 0.32.9 |
| A4 | UN3+UN4: reference Unsloth Dynamic 2.0 GGUFs as catalog data where they beat current picks; encode training recipes as a skill (pre-work for C1) | Med | Low | P1 | Data + prompt content only |
| A4b | DG3: add `diffusion` / `coding-eligible` capability flags to the catalog schema; record DiffusionGemma as a known-gaps watch item with its two flip conditions | Med | Low | P2 | Cheap future-proofing; keeps the watch item actionable |

### Bucket 2 -- reverse-engineer (re-full first, then re-partial)

| # | Item | Value | Effort | Priority | Rationale |
|---|---|---|---|---|---|
| A5 | NL2+NL3: Switchyard-style router stage in the harness selector / planner-critic-worker DAG -- tool-activity signals (error streaks, repeated identical actions, progress-free step counts) trigger per-turn escalation worker->strong, with session affinity and hysteretic anti-thrash thresholds wired into the GPU scheduler | High | Med | P1 (headline) | All signals already local (TelemetryBus, InferenceMetrics, AgentLoop); policy bucket 3: build the logic, not the Rust proxy |
| A6 | IV2: embed generation metadata in output files + chat recall actions (Use Prompt / Use Seed / Remix) | High | Low | P0 | Immediate provenance win for the chat-style Studio |
| A7 | IV1: persistent generation job queue + batch expansion (N seeds, prompt matrix) on the GPU scheduler | High | Med | P1 | Fire-and-forget batches; foundation for sweeps |
| A8 | DG2: Chat-pillar image/video attachment path + visual-token budgeting, model-agnostic (serves Muse Glimmer's perception encoder and any future VLM, not just DiffusionGemma) | High | Med | P1 | Unblocks multimodal chat regardless of which VLM wins |
| A9 | IV3: SAM2-assisted inpainting -- "replace the X" resolves to a segmentation mask, no manual masking | High | Med | P1 | Chat-native editing beyond InvokeAI's own UX; local weights |
| A10 | IV5: explicit diffusion VRAM-budget knobs (cache RAM/VRAM caps, working-mem reserve, layer streaming) folded into DiffusionTier | Med | Med | P2 | Proven config surface; deepens the single-GPU moat |
| A11 | BZ1+BZ2: locally-signed unified audit log (SQLite, per-sub-agent keys, cross-pillar action attribution) | Med | Med | P2 | Governance hardening; no Nostr network |
| A12 | BZ3: headless JSON-in/JSON-out CLI over the sidecar (reusing gateway token auth) | Med | Med | P2 | Makes Nexus itself agent-drivable; natural extension of the v1.16.0 gateway |
| A13 | IV4 (re-partial): minimal mask-layer canvas (paint/erase mask + bounding-box region) behind the existing Advanced settings, NOT the full four-layer-type system | Med | High | P3 | Only after A9 proves demand for manual mask refinement |
| A14 | BZ6 (re-partial): frame-anchored comments in Video Lab for iterating on generations | Low | Med | P3 | Backlog |

### Bucket 3 -- vendor-intrinsic (justified)

| # | Item | Value | Effort | Priority | Rationale |
|---|---|---|---|---|---|
| C1 | UN1+UN2: local fine-tuning pillar -- installer provisions Unsloth Core via `uv` into a managed per-GPU-vendor Python env (exactly the Ollama precedent); Nexus builds original orchestration: dataset builder from user repos/chats (through `redactSecrets`), tier-gated base-model picker, QLoRA runs, golden-task eval gate, GGUF->Ollama re-import | High | High | P1 (plan it) | Policy bucket 4 justification, all three prongs: (1) the Triton kernels ARE the value and are not reverse-engineerable; (2) no local equivalent is viable; (3) the feature is category-defining (S5 proves demand). No vendor account, no outbound calls, Apache 2.0 core only. **Gate: verify the Apache/AGPL component boundary before any bundling; conservative hardware-tier gating for AMD-on-Windows and Intel/Vulkan training paths.** |

### Sequencing

1. **Wave 1 (catalog + quick wins, parallel)**: A1, A2, A6, then A3, A4, A4b. Includes local harness re-evaluation of Meta's and NVIDIA's vendor-reported benchmarks before either model gets a default route.
2. **Wave 2 (routing headline)**: A5 (depends on A3 for the worker model), then A7.
3. **Wave 3 (multimodal + editing)**: A8, A9 (independent of each other; A9 independent of A8).
4. **Wave 4 (fine-tuning pillar)**: C1 after its AGPL-boundary gate; A4's recipe skill feeds it.
5. **Backlog**: A10-A14 as capacity allows; DG watch item reviewed each cycle against its flip conditions.

## Section 7: NOT Recommended (drop-outright, with policy grounds)

| Item | Source | Grounds |
|---|---|---|
| Hosted Muse Glimmer inference (Together / Fireworks / OpenRouter) | S2 | MCP Registry Policy hard-no: generation-as-a-service; violates local-first |
| Switchyard Rust proxy as a dependency | S4 | Duplicates the v1.16.0 serving gateway; policy buckets 2/3 say build the routing logic locally (done as A5) |
| Prefill router | S4 | Needs residual-stream access + training pipeline; not viable via Ollama |
| TensorRT-LLM / NIM deployment paths, NVFP4-native serving | S4 | Blackwell/datacenter scope; violates single-consumer-GPU ceiling |
| Buzz multi-user rooms, multi-tenant relay, Nostr git hosting (NIP-34) | S1 | Nexus is single-user local-first; a collaboration server inverts the architecture |
| Buzz Railway one-click deploy pattern | S1 | Third-party hosting; irrelevant to a desktop app |
| InvokeAI node-graph editor + community node plugins (wholesale) | S3 | Conflicts with chat-first philosophy; plugin supply-chain surface; survives only as "saved presets" (A13-adjacent) |
| InvokeAI full model-ecosystem breadth | S3 | Contradicts lean hardware-tiered curation; selective LoRA support only |
| Unsloth Desktop app (bundle or imitate) | S5 | Direct competitor; UI layer likely AGPL-3.0 (license hazard for MIT-licensed Nexus); core library as provisioned runtime (C1) is the clean path |
| DiffusionGemma as a shipped catalog entry today | S6 | Needs a non-mainlined llama.cpp branch = second installer-provisioned runtime for one experimental model; 24 GB-tier-only at Q4_K_M; quality below Gemma 4 where agentic work lives. Tracked as a watch item (A4b), not adopted |

## Section 8: Verification Checklist

- [x] Step 1.5 security scan ran per source BEFORE ingestion; verdicts in 5.0; no BLOCK
- [x] Source types identified (2 repos, 1 article+card, 2 model pages, 1 model+router pair); strategy per type
- [x] Every insight cites its source artifact; statuses cite Nexus files where applicable
- [x] Step 5 threat model (5.1), risk scorecard (5.2), RE classification (5.3) complete
- [x] Step 5.4 ordering applied: skill-native (A1-A4b) -> RE builds (A5-A14) -> vendor-intrinsic justified (C1) -> drops in Section 7
- [x] Adoption target resolved and user-confirmed: v2.1.0
- [x] MCP Registry Policy cited for every item with a download, new runtime, or rejected outbound surface

---

*Next step: `/plan from-comparison` ingests Section 6 into a phased v2.1.0 implementation plan (reverse-engineer-first ordering), co-located under `docs/archive/v2/v2.1/plans/`.*
