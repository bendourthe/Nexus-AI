# Plan -- Open Local-AI Wave Adoption (Catalog, Routing, Studio Depth, Fine-Tuning)

**Project**: Nexus (Nexus-AI)
**Version**: v2.1.0
**Slug**: open-local-ai-wave
**Filename**: v2.1.0-adoption-open-local-ai-wave.md
**Plan Type**: Feature (adoption plan)
**Created**: 2026-08-13
**Seeded from**: docs/archive/v2/v2.1/comparisons/v2.1.0-comparison-open-local-ai-wave.md (Adoption target: v2.1.0)
**Goal**: Ship the six-source adoption plan end to end: two new top-tier catalog models with verified local benchmarks, Switchyard-derived adaptive routing in the coding DAG, Image Studio provenance + queueing, multimodal chat attachments with SAM2-assisted editing, a local fine-tuning pillar on an installer-provisioned Unsloth Core runtime, and governance hardening -- with zero new outbound surfaces beyond one-time open-weight downloads.

## Overview

This plan operationalizes the August 2026 multi-source comparison (`v2.1.0-comparison-open-local-ai-wave.md`). The comparison found three signals: the 30B-total / 3-4B-active MoE class is now the local agentic sweet spot (Muse Glimmer, Nemotron Lightning); routing is the new differentiator and is pure control logic over telemetry Nexus already collects (NeMo Switchyard); and the "local AI studio" category is converging, so Nexus must defend its moats (installer, catalog, harness selector, eval loop) while closing its two real gaps: local fine-tuning and Image Studio depth.

Delivery is entirely inside the existing desktop app and installer. No new outbound destinations, credentials, or third-party processors are introduced: adopted artifacts are Apache-2.0 / OpenMDW-1.1 open weights pulled through the existing installer/HF channel, and every external design (Switchyard heuristics, InvokeAI queue/metadata/SAM2 patterns, Buzz audit-log ideas) is reverse-engineered into lean local modules per the MCP Registry Policy. The one vendor-intrinsic addition, Unsloth Core as a provisioned training runtime, follows the Ollama precedent and is gated on an Apache/AGPL component-boundary verification before anything ships.

Success looks like: a 24 GB-tier user gets Muse Glimmer as their coding default (after local eval confirms vendor claims), long agentic sessions run routine steps on Lightning and escalate to the strong model only on measured difficulty without VRAM thrash, Image Studio generations carry recallable provenance and queue as batches, chat accepts screenshots and short videos, "replace the X" edits work without manual masking, and a user can fine-tune a small model on their own repos/chats and have it appear in their Ollama-served catalog -- all offline after weight download.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at docs/v2.1.0/constitution.md (or any `docs/**/constitution.md`) - skipping check. Recommend running /constitution to establish project principles.

The plan was instead checked against the six README Design Principles (local-first, originality over wrappers, single-GPU ceiling, installer carries the burden, privacy by construction, OS parity): all phases PASS; the fine-tuning pillar's Unsloth runtime is justified under the originality-over-wrappers exception for non-reverse-engineerable GPU kernels, mirroring the Ollama precedent, and dataset extraction routes through `redactSecrets`.

## Current model map

**Model map status**: fresh as of 2026-08-13; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|------|-----------|--------|--------|--------|
| frontier | claude-fable-5 | gpt-5.6-sol | gemini-3.1-pro-preview | grok-4.6 |
| strong | claude-opus-5 | gpt-5.3-codex | gemini-3.6-flash | grok-4.5 |
| standard | claude-sonnet-5 | gpt-5.6-terra | gemini-3.5-flash | composer-2.5 |
| fast | claude-haiku-4-5-20251001 | gpt-5.6-luna | gemini-3.5-flash-lite | composer-2.5 (standard variant) |

Notes: Google's next Pro (3.5 Pro) is announced but not yet in the API; `gemini-3.1-pro-preview` is the newest available Pro. Cursor's pool is Grok 4.6 / Grok 4.5 / Composer 2.5; Composer 2.5 fast mode is the interactive default.

### Model map sources

- Anthropic: https://www.anthropic.com/news/claude-sonnet-5 and https://benchlm.ai/anthropic/api-pricing
- OpenAI: https://help.openai.com/en/articles/9624314-model-release-notes
- Google: https://ai.google.dev/gemini-api/docs/changelog and https://ai.google.dev/gemini-api/docs/models
- Cursor: https://cursor.com/docs/models-and-pricing and https://cursor.com/docs/models/cursor-composer-2-5

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|-------|-------|---------|------------------------|--------------------------|
| 1 | Model Catalog Expansion + Harness Profiles | Muse Glimmer + Nemotron Lightning shipped in the catalog with harness profiles, Unsloth Dynamic quant references, capability flags, and locally re-verified benchmarks | strong | medium |
| 2 | Adaptive Model Routing (Switchyard-derived) | Per-turn worker-to-strong escalation from tool-activity signals, session affinity, and anti-thrash scheduling in the coding DAG | frontier | high |
| 3 | Image Studio Provenance + Generation Queue | Embedded generation metadata with chat recall actions; persistent job queue with batch/seed sweeps | strong | medium |
| 4 | Multimodal Chat + Segmentation-Assisted Editing | Chat-pillar image/video attachments with visual-token budgets; SAM2-driven maskless inpainting | strong | high |
| 5 | Local Fine-Tuning Pillar (Unsloth Core) | License-gated, installer-provisioned QLoRA fine-tuning on user data with golden-task eval gate and GGUF-to-Ollama re-import | frontier | max |
| 6 | Hardening + Depth Backlog | Signed local audit log with per-agent identity, headless JSON CLI, diffusion VRAM-budget knobs | strong | medium |
| 7 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Clean layout, reconciled gaps, complete and optimized CI/CD | frontier | max |

---

## Phase 1: Model Catalog Expansion + Harness Profiles

**Goal**: Ship Muse Glimmer 30B and Nemotron 3.5 Lightning 30B-A3B as tier-gated catalog models with harness profiles, add Unsloth Dynamic quant references and catalog capability flags, and re-verify vendor benchmarks with the local golden-task harness before any default-route promotion.
**Prerequisites**: None.
**Stability Gate**: Both models install, load, and complete the golden-task suite on a 24 GB-tier machine; catalog schema migration passes validation; no model is promoted to a default route without a recorded local eval result.
**Recommended model tier**: strong
**Recommended effort level**: medium
**Rationale**: Mostly catalog data, harness configuration, and eval runs on existing infrastructure; the risk is in tier gating and schema migration, not novel code.

### Sub-tasks

#### 1.1 -- Muse Glimmer catalog entry + quant tiers (A1)

**Objective**: Add Muse Glimmer 30B to `core/registry/catalog.json` with Meta's published quant tiers mapped to Nexus VRAM tiers.

**Failure modes**: If the GGUF manifest or HF download is unreachable or slow, the installer surfaces the existing retry-failed-downloads flow (v1.15.0) rather than marking the model installed; if a catalog entry references a quant absent from the tier's manifest, the v1.15.0 content-invariant guard fails the build; if a user's VRAM tier is below 16 GB, the entry is hidden by the visibility gate rather than shown-and-failing at load.

**Prompt**:
> Add Muse Glimmer 30B (Apache 2.0, `meta-models/Muse-Glimmer-30B-GGUF`, Ollama >= 0.32.7) to `core/registry/catalog.json`. Map K-Quant-17GB to the 24 GB VRAM tier and K-Quant-Dynamic to the 32 GB tier; exclude the model from all tiers below 16 GB. Record vendor-reported SWE-Bench Verified 76.0 in the entry's metadata flagged `vendor_reported: true` (local verification happens in sub-task 1.5). Wire the entry through the installer's per-VRAM catalog curation and the v1.14.0 release-date/best-of-family collapse so it appears correctly in the model picker. Ensure the catalog content-invariant guard covers the new entry shape. Acceptance: on a 24 GB profile the model appears, installs via the existing weights puller, and loads in Ollama; on a 12 GB profile it does not appear.

---

#### 1.2 -- Muse Glimmer harness profile (A2)

**Objective**: Give the per-model harness selector a Muse Glimmer profile: reasoning-strength parameter plumbing, function-calling template, and long-horizon/failure-recovery conventions from Meta's public docs.

**Failure modes**: If the harness profile is missing at selection time, the selector falls back to the generic profile and logs a warning (never blocks a session); if the reasoning-strength parameter is rejected by the served model version, the harness drops the parameter and records the downgrade in InferenceMetrics; if two sessions request conflicting profile variants, per-session profile resolution keeps them isolated.

**Prompt**:
> Add a Muse Glimmer profile to `modules/coding/orchestration/HarnessSelector.ts` (and its profile registry): reasoning-strength control surfaced as a session option, the model's function-calling template, and prompting conventions for long-horizon agentic work per Meta's Muse Glimmer developer docs (dev.meta.ai/docs/muse-glimmer). Follow the existing v1.12.0 per-model profile structure. Acceptance: with Muse Glimmer active, the coding pillar uses the new profile (assert via harness-selection unit tests mirroring the existing per-model profile tests), and an unknown-model fallback test still passes.

---

#### 1.3 -- Nemotron Lightning dual-tier catalog entry + harness profile (A3)

**Objective**: Add Nemotron 3.5 Lightning 30B-A3B as a dual-tier entry (24 GB native Q4_K_M; 16 GB via the existing MoE expert-offload rung) with a harness profile using its Qwen3-Coder-style tool-call parser.

**Failure modes**: If the ggml-org GGUF pull fails, the retry flow applies as in 1.1; if the host's Ollama is older than 0.32.9, the entry is hidden with an actionable "update Ollama" note rather than failing at load; if expert-offload is selected on a machine without sufficient disk headroom, the existing disk-offload preflight rejects the configuration before download.

**Prompt**:
> Add Nemotron 3.5 Lightning 30B-A3B (OpenMDW-1.1, `ggml-org/NVIDIA-Nemotron-3.5-Lightning-30B-A3B-GGUF`, Ollama >= 0.32.9) to `core/registry/catalog.json` as a dual-tier entry: Q4_K_M (22.5 GB) native at the 24 GB tier, and a 16 GB-tier variant riding the existing disk-offload/expert-offload quant-ladder rung (`core/registry/` offload tier plumbing). Gate on Ollama >= 0.32.9 with a visible version check. Add a harness profile configured for its tool-calling parser and its role as a fast routine-step worker (this profile is consumed by Phase 2 routing). Tag the entry `role: worker-candidate`. Acceptance: installs and loads at both tiers on matching profiles; version gate hides it on older Ollama; harness profile selected under test.

---

#### 1.4 -- Unsloth Dynamic quant references + capability flags + watch item (A4, A4b)

**Objective**: Reference Unsloth Dynamic 2.0 GGUF artifacts as catalog data where they beat current picks; extend the catalog schema with `diffusion` and `coding_eligible` capability flags; record the DiffusionGemma watch item with its two flip conditions.

**Failure modes**: If a referenced Dynamic GGUF is removed upstream, the installer's live-reachability check (v1.14.0) marks it unavailable instead of failing the whole catalog; if the schema migration meets an entry missing the new flags, defaults apply (`diffusion: false`, `coding_eligible: true`) so old entries remain valid; if two schema versions coexist during rollout, the registry loader accepts both and normalizes.

**Prompt**:
> (1) Audit the current catalog's low-bit picks against Unsloth Dynamic 2.0 GGUF equivalents on Hugging Face; where the Dynamic quant is strictly better for a tier, swap the artifact reference (data-only change, no new dependency). (2) Add `diffusion: boolean` and `coding_eligible: boolean` to the catalog schema with backward-compatible defaults, updating the content-invariant guard and schema validation tests. (3) Append a DiffusionGemma watch item to `docs/archive/v2/v2.1/known-gaps.md` (create the file if missing) with flip conditions: llama.cpp PR #24423 mainlined into a shipped Ollama release, AND sub-16 GB quants published; cite the v2.1.0 comparison Section 7. Acceptance: catalog validates, swapped entries install, schema tests pass, watch item recorded.

---

#### 1.5 -- Local benchmark re-verification of vendor claims

**Objective**: Run both new models through the golden-task suite and record local results; only then decide default-route placement.

**Build class**: load-bearing -- this eval gate is the mechanism that keeps vendor-reported numbers out of routing decisions, here and in Phase 2.

**Failure modes**: If a model OOMs or times out mid-suite, the run records a partial result marked `incomplete` and the model keeps `vendor_reported`-only status (never silently passes); if the suite is run on a machine below the model's tier, the runner refuses to start with a clear message; if two eval runs target the same GPU concurrently, the GPU scheduler serializes them.

**Prompt**:
> Using `modules/coding/evaluation/GoldenTaskRunner.ts`, run the full golden-task suite against Muse Glimmer (K-Quant-17GB) and Nemotron Lightning (Q4_K_M) on a 24 GB-tier machine. Persist per-model results next to the existing eval outputs and surface them in the catalog entries as `local_eval` blocks alongside the `vendor_reported` figures. Based on results, propose (do not silently apply) default-route changes for the coding pillar; apply only after recording the decision in this plan's session history. Acceptance: both models have `local_eval` blocks; any default-route change references a recorded local result.

---

#### 1.6 -- Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until the phase is stable before advancing to Phase 2.

**Prompt**:
> Generate comprehensive tests for everything built in Phase 1: catalog schema validation (new flags, dual-tier entries, version gates), harness-profile selection unit tests for both new profiles, installer tier-curation tests (model visible/hidden per VRAM profile), reachability and retry-path tests, and a golden-task runner smoke test with a stubbed model. Run the tests, fix failures, and iterate until green. Then create or update the CI/CD pipeline to cover this phase's changes and optimize it to reduce CI action minutes (path filters, concurrency cancel-in-progress, dependency caching, gating expensive-OS or matrix jobs to merges/schedule) while keeping comprehensive coverage. Do not proceed to Phase 2 until stable.
> After all tests pass, run `/generate-session-history` to document Phase 1.

---

### Phase 1 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 2

---

## Phase 2: Adaptive Model Routing (Switchyard-derived)

**Goal**: Reverse-engineer NeMo Switchyard's tuning-free routing heuristics into a local router stage: per-turn worker-to-strong escalation from tool-activity signals, cheap-first session policy, session affinity, and hysteretic anti-thrash scheduling on a single GPU.
**Prerequisites**: Phase 1 (Lightning worker model + harness profiles; local eval results).
**Stability Gate**: A long agentic session demonstrably starts routine steps on the worker model, escalates on induced difficulty (seeded error streaks), de-escalates after recovery, and never swaps models more than the hysteresis budget allows; all routing decisions are visible in telemetry.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Concurrency-sensitive control logic spanning the DAG, harness selector, GPU scheduler, and telemetry, where subtle threshold bugs degrade every coding session.

### Sub-tasks

#### 2.1 -- Routing signal extraction from existing telemetry

**Objective**: Derive the three Switchyard-style signal families from data Nexus already collects: tool-error streaks, repeated-identical-action (unproductive-loop) detection, and progress-free step counts / prolonged exploration.

**Failure modes**: If telemetry events are missing or malformed for a turn (e.g. a crashed tool call), the signal extractor treats the turn as neutral rather than escalating on absent data; if TelemetryBus lags behind the AgentLoop, signals are computed on the last consistent window and marked stale; if two concurrent sessions interleave events, per-session scoping keys prevent cross-contamination.

**Build class**: load-bearing.

**Prompt**:
> Create a `RoutingSignals` module (suggested: `modules/coding/orchestration/routing/RoutingSignals.ts`) that consumes `core/telemetry/TelemetryBus.ts`, `core/observability/InferenceMetrics.ts`, and `src/tools/AgentLoop.ts` traces to produce per-session, per-turn signals: consecutive tool-error count, repeated-identical-action detection (hash of tool+args over a sliding window), and steps-without-progress (no file mutation, no test-state change, no new plan node over N steps). Pure functions over event windows; no new event sources. Unit-test each signal against synthetic event streams, including malformed/missing-event cases. Acceptance: signals computed deterministically from recorded fixture streams; stale/missing data yields neutral signals.

---

#### 2.2 -- Escalation policy engine (stage router + cheap-first + hysteresis)

**Objective**: Implement the decision layer: sessions start on the worker model (cheap-first); sustained-difficulty thresholds escalate a turn (stage router) or the session (escalation router) to the strong model; hysteresis and cooldown prevent thrash.

**Failure modes**: If the strong model is unavailable (not installed, tier-excluded), escalation degrades to staying on the worker with a surfaced notice instead of erroring; if signals conflict (error streak says escalate, cooldown says hold), cooldown wins and the deferred escalation is queued for the next eligible turn; if the policy config is malformed, the engine loads compiled defaults and logs the rejection.

**Build class**: load-bearing.

**Prompt**:
> Implement `EscalationPolicy` (suggested: `modules/coding/orchestration/routing/EscalationPolicy.ts`) consuming RoutingSignals: (1) cheap-first default -- planner/critic roles pin to the strong model, worker roles start on the `role: worker-candidate` model from Phase 1; (2) per-turn escalation when thresholds trip (defaults: 3 consecutive tool errors, 2 identical-action repeats, 8 progress-free steps -- all configurable); (3) session escalation on sustained difficulty (2 per-turn escalations within a window); (4) hysteresis: minimum turns-on-model before de-escalation, plus a session-level swap budget. Session affinity: a routed step stays on its model for KV-cache reuse until policy says otherwise. Expose every decision as a telemetry event (`routing.decision` with signal snapshot and reason). Wire into the planner/critic/worker DAG's model selection alongside the harness selector. Acceptance: decision unit tests cover escalate/hold/de-escalate/conflict/unavailable-model paths; DAG integration test shows role pinning + worker escalation end to end against stubbed models.

---

#### 2.3 -- GPU scheduler integration (single-GPU swap cost)

**Objective**: Teach the GPU scheduler that escalation events have a VRAM swap cost: batch escalations hysteretically, prefetch/evict model weights around predicted swaps, and refuse swaps that would OOM concurrent diffusion work.

**Failure modes**: If VRAM telemetry is unavailable mid-decision, the scheduler assumes worst-case occupancy and defers the swap; if a swap stalls (Ollama slow to load), the session continues on the incumbent model with the swap retried in the background rather than blocking the turn; if a routing swap and a diffusion job request VRAM simultaneously, the existing coding-over-diffusion priority applies and the loser is queued, not killed.

**Prompt**:
> Extend the GPU scheduler (`core/scheduler/`) with a model-swap cost model for routing escalations: consult live VRAM telemetry before honoring an EscalationPolicy decision; batch near-simultaneous escalation requests; keep the worker resident when headroom allows, evict when it does not; defer (never drop) decisions that would OOM active work. Emit `scheduler.swap` telemetry with outcome (honored / deferred / degraded). Acceptance: simulated single-GPU scenarios (worker+strong fit; worker+strong don't fit; diffusion job concurrent) each produce the documented outcome in integration tests with a mocked telemetry source.

---

#### 2.4 -- Routing observability panel

**Objective**: Surface routing decisions in the existing trace/telemetry UI so users can see which model handled each step and why.

**Failure modes**: If routing events are absent (feature off or old session replay), the panel renders an empty state, not an error; if the event stream is large, the panel virtualizes rather than blocking the UI thread; if decision reasons reference a model since uninstalled, the label degrades to the model id string.

**Prompt**:
> Extend the trace dashboard (`desktop/src/modules/coding/panels/TraceDashboardPanel.tsx` and the v1.16.0 `ModelAnalyticsSection.tsx`) with a routing lane: per-step model attribution, escalation markers with reason tooltips (signal snapshot), and per-session swap counts. Follow existing panel test patterns (`desktop/tests/`). Acceptance: component tests cover populated, empty, and uninstalled-model states; a replayed fixture session renders the full routing story.

---

#### 2.5 -- Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until the phase is stable before advancing to Phase 3.

**Prompt**:
> Generate comprehensive tests for Phase 2: signal-extraction units (including malformed streams), escalation-policy decision matrix, scheduler swap-cost integration under simulated VRAM states, DAG end-to-end with induced error streaks (assert escalation, de-escalation, affinity, and swap-budget enforcement), and UI component tests for the routing lane. Add a soak-style test replaying a long recorded session to assert the swap count stays within budget. Run, fix, iterate until green. Then create or update CI/CD for this phase's changes and optimize action minutes (path filters, concurrency cancel-in-progress, caching, gating expensive jobs) while keeping coverage comprehensive. Do not proceed to Phase 3 until stable.
> After all tests pass, run `/generate-session-history` to document Phase 2.

---

### Phase 2 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 3

---

## Phase 3: Image Studio Provenance + Generation Queue

**Goal**: Every generation carries embedded, recallable provenance; generations run through a persistent job queue supporting fire-and-forget batches and seed/prompt sweeps.
**Prerequisites**: None (independent of Phases 1-2; scheduled here per RE-first wave order).
**Stability Gate**: A generated image re-opened days later can reproduce its prompt/seed/settings via recall actions; a 20-job batch survives an app restart and completes; queue and scheduler never deadlock under concurrent coding load.
**Recommended model tier**: strong
**Recommended effort level**: medium
**Rationale**: Well-scoped features on existing pipelines; persistence-across-restart and scheduler interplay are the only elevated-risk seams.

### Sub-tasks

#### 3.1 -- Embedded generation metadata + recall actions (A6)

**Objective**: Embed full generation parameters (prompt, negative prompt, seed, model, sampler, tier, mode) in output image/video files and expose chat recall actions: Use Prompt / Use Seed / Use All / Remix.

**Failure modes**: If an output format cannot carry metadata (or a writer fails), the parameters are persisted in the local generation index keyed by content hash so recall still works; if a user imports an external image with no metadata, recall actions are hidden rather than erroring; if metadata schema versions differ between app versions, the reader accepts all known versions and ignores unknown fields.

**Prompt**:
> Embed generation metadata in Image Studio and Video Lab outputs (PNG tEXt/iTXt chunks; sidecar or container metadata for video), mirrored into the local generation index. Add recall actions to generated-media messages in the chat-style Studio UI: Use Prompt, Use Seed, Use All, and Remix (prefills a new message with all parameters editable). Metadata never leaves the machine; it is written to local files only. Acceptance: round-trip test (generate -> read back -> byte-equal parameters), external-image graceful degradation, recall actions produce the expected prefilled state, and `redactSecrets` is applied to any free-text fields before indexing.

---

#### 3.2 -- Persistent generation job queue + batch expansion (A7)

**Objective**: A durable queue in front of the diffusion pipelines: jobs persist across restarts, batches expand (N seeds, prompt matrix), progress streams into chat, and the GPU scheduler arbitrates against coding load.

**Failure modes**: If the app crashes mid-job, on restart the job is marked interrupted and re-queued (idempotent by job id), never duplicated; if the diffusion pipeline rejects a job (model uninstalled since queuing), the job fails with a per-job error and the queue continues; if a batch and an interactive generation contend, interactive jobs get priority within the existing coding-over-diffusion global policy.

**Build class**: load-bearing.

**Prompt**:
> Implement a persistent generation queue (SQLite-backed, suggested under `core/` alongside existing local stores): job schema (id, pillar, parameters, batch-expansion spec, state machine queued/running/interrupted/done/failed), batch expansion for seed ranges and prompt matrices, restart recovery, and cancellation. Integrate with the GPU scheduler's existing priority policy; stream per-job progress into the originating chat thread. Add queue-management UI affordances in the Studio (pending count, cancel, reorder). Acceptance: restart-recovery integration test, batch-expansion unit tests, priority test under simulated coding load, cancellation mid-run leaves consistent state.

---

#### 3.3 -- Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until the phase is stable before advancing to Phase 4.

**Prompt**:
> Generate comprehensive tests for Phase 3: metadata round-trip and degradation tests across PNG/video paths, recall-action UI tests, queue state-machine units, restart-recovery and crash-simulation integration tests, batch expansion, and scheduler-contention scenarios. Run, fix, iterate until green. Then create or update CI/CD for this phase's changes and optimize action minutes while keeping coverage. Do not proceed to Phase 4 until stable.
> After all tests pass, run `/generate-session-history` to document Phase 3.

---

### Phase 3 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 4

---

## Phase 4: Multimodal Chat + Segmentation-Assisted Editing

**Goal**: The Chat pillar accepts image and video attachments with visual-token budgeting (model-agnostic, serving any VLM in the catalog); Image Studio gains SAM2-driven maskless editing ("replace the X").
**Prerequisites**: Phase 1 (catalog capability flags identify vision-capable models); Phase 3 recommended for the editing UX (recall/queue) but not blocking.
**Stability Gate**: A screenshot pasted into chat is answered by a vision-capable local model with the visual-token budget enforced; "replace the car with a truck" produces a SAM2 mask and a correct inpaint without manual masking; non-vision models cleanly reject attachments with guidance.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: New ingestion path touching memory/retrieval layers plus a new local model integration (SAM2); broad surface but established patterns.

### Sub-tasks

#### 4.1 -- Chat attachment ingestion + visual-token budgeting (A8)

**Objective**: Image/video attachment path for the Chat pillar: ingestion, per-model capability negotiation (via the Phase 1 flags plus a `vision` flag), visual-token budget selection, and memory/retrieval handling of non-text turns.

**Failure modes**: If an attachment is malformed or an unsupported format, ingestion rejects it at the boundary with a user-visible reason (never passes bytes to the model); if the active model lacks vision, the UI offers switching to an installed vision-capable model or proceeding text-only; if an attachment would blow the context/VRAM budget, the budget selector downsamples or truncates video frames and says so, rather than OOMing.

**Build class**: load-bearing.

**Prompt**:
> Add a `vision: boolean` capability flag plus per-model visual-token budget metadata to the catalog schema (extending Phase 1's flag work). Build the Chat attachment path: paste/drag/attach images and short videos, format validation, frame sampling for video, visual-token budget selection (per-model presets with an Advanced override), and handoff to vision-capable models through the existing Ollama channel. Teach the four-layer memory and hybrid retrieval to store and index non-text turns (caption/summary text as the indexable surrogate, generated locally). Verify whether the Muse Glimmer Ollama GGUF ships its vision projector; if not, gate its `vision` flag off and document in known-gaps. Acceptance: end-to-end screenshot Q&A against a stubbed vision model, rejection paths for malformed files and non-vision models, budget enforcement test, retrieval returns the multimodal turn via its surrogate text.

---

#### 4.2 -- SAM2 catalog integration (local segmentation runtime)

**Objective**: SAM2 as an installer-provisioned local capability: weights in the catalog (tier-gated by its modest VRAM), loaded on demand for segmentation.

**Failure modes**: If SAM2 weights are missing at edit time, the feature degrades to the existing manual inpaint flow with an install prompt; if segmentation exceeds a latency budget or VRAM headroom, the request is queued behind the GPU scheduler rather than freezing the Studio; if segmentation returns no confident mask for the referenced object, the user is told and offered manual selection.

**Prompt**:
> Add SAM2 (Apache 2.0) to the catalog and installer as a utility model (not a chat model): tier-gated weights download, local runtime integration in the Python diffusion sidecar, and a segmentation service API (image + point/box/text-derived hint -> mask) mediated by the GPU scheduler. Acceptance: segmentation smoke test on fixture images, missing-weights degradation path, scheduler-mediated concurrency test.

---

#### 4.3 -- "Replace the X" chat-native editing (A9)

**Objective**: Resolve a natural-language edit request to a SAM2 mask and route through the existing inpaint pipeline, inline in the Studio chat.

**Failure modes**: If the object phrase resolves ambiguously (multiple instances), the UI shows candidate masks for a one-tap choice instead of guessing; if the inpaint result fails to generate (pipeline error), the original image is untouched and the error is a normal chat message; if mask + prompt conflict with the inferred mode (e.g. outpaint request with an object mask), mode inference asks rather than silently picking.

**Prompt**:
> Implement segmentation-assisted editing in Image Studio's chat flow: parse edit intents referencing objects ("replace/remove/recolor the X"), obtain the mask via the 4.2 segmentation service (using the local LLM to ground the phrase to a point/box hint), preview the mask overlay for confirmation, then run the existing inpaint pipeline with the confirmed mask. Multi-candidate disambiguation UI for ambiguous phrases. Acceptance: fixture-driven e2e (image + phrase -> mask -> inpaint) with stubbed diffusion, ambiguity flow test, failure leaves the original intact.

---

#### 4.4 -- Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until the phase is stable before advancing to Phase 5.

**Prompt**:
> Generate comprehensive tests for Phase 4: attachment validation and rejection units, visual-token budget enforcement, multimodal memory/retrieval integration, SAM2 service tests (fixtures, degradation, scheduling), and the full "replace the X" e2e with stubbed models. Run, fix, iterate until green. Then create or update CI/CD for this phase's changes and optimize action minutes while keeping coverage. Do not proceed to Phase 5 until stable.
> After all tests pass, run `/generate-session-history` to document Phase 4.

---

### Phase 4 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 5

---

## Phase 5: Local Fine-Tuning Pillar (Unsloth Core)

**Goal**: A license-gated fine-tuning capability: the installer provisions Unsloth Core (Apache 2.0 components only) as a managed runtime, Nexus provides original orchestration -- dataset builder from user repos/chats through `redactSecrets`, tier-gated base-model picker, QLoRA runs, golden-task eval gate, and GGUF-to-Ollama re-import.
**Prerequisites**: Phase 1 (catalog flags, quant references); Phase 2 recommended (routing benefits from tuned workers) but not blocking.
**Stability Gate**: On a 16 GB+ machine, a user fine-tunes a small base model on a local dataset end to end offline (post-provisioning), the eval gate blocks a regressed adapter, and the exported model serves through Ollama and appears in Settings > Models.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: New pillar spanning installer, a new provisioned Python runtime, GPU-heavy long-running jobs, privacy-sensitive dataset extraction, and a licensing gate -- the highest blast radius in the plan.

### Sub-tasks

#### 5.1 -- License-boundary verification gate (GATE: blocks all shipping sub-tasks)

**Objective**: Verify the exact Apache-2.0 / AGPL-3.0 component boundary of the Unsloth project and record which artifacts Nexus may provision.

**Build class**: load-bearing -- this decision record is the legal ground the pillar stands on.

**Prompt**:
> Audit the Unsloth repository and PyPI artifacts: enumerate which packages/components are Apache 2.0 (core library) vs AGPL-3.0 (Studio/UI components), pinning exact package names and versions. Produce a decision record at `docs/archive/v2/v2.1/development/unsloth-license-boundary.md` listing: provisioned artifacts (Apache-only), excluded artifacts (AGPL), and the invocation surface Nexus uses (library API calls from Nexus-owned orchestration code only; no bundled Unsloth UI). If ANY required runtime component turns out AGPL-licensed, STOP and surface the finding for a user decision before any further Phase 5 work. Acceptance: decision record exists, cites upstream license files per component, and names pinned versions.

---

#### 5.2 -- Installer provisioning of the training runtime

**Objective**: The installer provisions Unsloth Core via `uv` into a managed per-GPU-vendor Python environment (the Ollama precedent), tier-gated to hardware that can realistically train.

**Failure modes**: If provisioning fails (network, resolver, CUDA mismatch), the pillar is marked unavailable with a retry action and the rest of Nexus is unaffected; if the GPU vendor path is unsupported for training (conservative gating: Intel/Vulkan and AMD-on-Windows excluded initially), the pillar is hidden for that hardware with a documented reason; if a provisioned env is later corrupted, a preflight check detects it and offers re-provisioning rather than failing mid-training-run.

**Prompt**:
> Extend the installer to optionally provision the fine-tuning runtime: a dedicated `uv`-managed Python env containing the Apache-2.0 Unsloth Core packages pinned per the 5.1 decision record, per-GPU-vendor torch builds reusing the existing CUDA/ROCm/Metal provisioning, gated to NVIDIA (all OS) and AMD-on-Linux tiers with >= 16 GB VRAM initially. Add a runtime preflight (import check + kernel smoke) surfaced in Settings. Provisioning is opt-in during install and available post-install from Settings. Acceptance: clean-machine harness run provisions and passes preflight on an NVIDIA profile; unsupported profiles hide the pillar with the documented reason; failure mid-provision leaves a resumable state.

---

#### 5.3 -- Dataset builder from user repos and chats

**Objective**: Original Nexus UX for assembling training datasets from local sources -- repositories, chat threads, and dropped files (PDF/CSV/JSON) -- with every record passing through `redactSecrets`.

**Failure modes**: If a source file is unreadable or oversized, it is skipped with a per-file report, never aborting the whole dataset; if redaction flags a record, the record is shown redacted in preview and the raw form never reaches the dataset on disk; if two dataset builds target the same output, builds are serialized and versioned rather than interleaved.

**Build class**: load-bearing.

**Prompt**:
> Build the dataset builder: select local sources (repo paths, chat folders, dropped PDF/CSV/JSON), extraction to instruction-tuning formats (chat-template JSONL; leverage the Phase 1 A4 training-recipe skill for format presets), mandatory `redactSecrets` (core/observability/redactSecrets.ts) pass over every record with a preview UI showing redactions, dataset versioning on local disk, and size/token estimates per VRAM tier. Nothing leaves the machine. Acceptance: extraction units per source type, redaction integration test proving a seeded secret never lands in the output file, preview shows redacted records, oversized-file skip path.

---

#### 5.4 -- QLoRA training orchestration + eval gate + re-import

**Objective**: The training loop: tier-gated base-model picker, QLoRA run management (long-running, GPU-scheduled, cancellable, crash-recoverable), golden-task eval gate comparing adapter vs base, and GGUF export re-imported into Ollama and the registry.

**Failure modes**: If training crashes or the machine sleeps, checkpoints allow resume and the job state machine mirrors Phase 3's queue semantics (interrupted, resumable, never duplicated); if the eval gate shows regression beyond threshold, the adapter is quarantined (kept on disk, not imported) with the comparison shown; if GGUF export or Ollama import fails, the run is marked export-failed with artifacts retained for retry -- the registry never lists a model Ollama cannot serve (reusing the v1.15.0 reconciliation).

**Prompt**:
> Implement training orchestration in a new `modules/tuning/` (or equivalent) module: base-model picker filtered by tier and `coding_eligible`/`vision` flags; QLoRA job runner invoking the provisioned runtime (5.2) as a subprocess with structured progress events into the GPU scheduler and chat-style progress UI; checkpoint/resume; cancellation. Post-training: run the golden-task eval gate (GoldenTaskRunner) comparing adapter vs base on the relevant suite, quarantine on regression; on pass, export GGUF via the Unsloth export path, import into Ollama, and register through the v1.15.0 registry-reconciliation flow with provenance metadata (base model, dataset version, eval delta). Acceptance: end-to-end fine-tune on a tiny base model + tiny fixture dataset completes in CI-feasible time (or behind a hardware-gated integration tag), regression quarantine test, export-failure retains artifacts, imported model appears in Settings > Models and serves a completion.

---

#### 5.5 -- Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until the phase is stable before advancing to Phase 6.

**Prompt**:
> Generate comprehensive tests for Phase 5: license-boundary record lint (pinned versions present), provisioning preflight units, clean-machine provisioning run in the installer harness, dataset extraction/redaction suite, training job state-machine units (crash/resume/cancel), eval-gate decision tests, export/import integration with a stubbed Ollama, and the hardware-gated end-to-end tag. Run, fix, iterate until green. Then create or update CI/CD for this phase's changes (hardware-gated jobs scheduled, not per-PR) and optimize action minutes while keeping coverage. Do not proceed to Phase 6 until stable.
> After all tests pass, run `/generate-session-history` to document Phase 5.

---

### Phase 5 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 6

---

## Phase 6: Hardening + Depth Backlog

**Goal**: Governance and agent-surface hardening from the Buzz and InvokeAI comparisons: a locally-signed unified audit log with per-sub-agent identity, a headless JSON CLI over the sidecar, and explicit diffusion VRAM-budget knobs.
**Prerequisites**: Phase 2 (routing events feed the audit log), Phase 5 (training events feed it too).
**Stability Gate**: Every cross-pillar action (chat turn, tool call, routing decision, generation, training run) appears in the signed local audit log attributable to a distinct actor identity; the JSON CLI drives a coding session headlessly with token auth; diffusion pipelines honor explicit VRAM/RAM budget knobs.
**Recommended model tier**: strong
**Recommended effort level**: medium
**Rationale**: Additive, well-bounded modules over existing event streams; cryptographic signing and a new local API surface warrant care but not frontier-level novelty.

### Sub-tasks

#### 6.1 -- Signed local audit log + per-agent identity (A11, BZ1/BZ2)

**Objective**: An append-only, locally-signed (per-actor local keys) SQLite audit log unifying events across pillars, with sub-agents (planner/critic/worker) getting distinct identities and attribution. No Nostr, no network.

**Failure modes**: If signing keys are missing on first run, they are generated locally and stored via the credential vault (never plaintext); if an event burst exceeds write throughput, events buffer with backpressure and a dropped-event counter (auditable gap, never silent loss); if verification of a log segment fails (tampering/corruption), the UI marks the segment untrusted rather than hiding it.

**Prompt**:
> Implement a local audit log: append-only SQLite store subscribed to TelemetryBus event families (chat, tool calls, routing decisions, generations, training runs), each event signed with a per-actor Ed25519 key (app identity + one per sub-agent role) generated locally and stored in the credential vault. Provide a verification API and a simple viewer surface (filter by actor/pillar/time). Secrets pass through `redactSecrets` before persistence. Local-only by construction. Acceptance: signature round-trip and tamper-detection tests, per-actor attribution test across a replayed session, backpressure test with counted drops.

---

#### 6.2 -- Headless JSON CLI over the sidecar (A12, BZ3)

**Objective**: A `nexus` CLI subcommand surface (JSON-in/JSON-out) exposing sidecar operations -- start/continue coding sessions, list models, queue generations -- authenticated with the existing local bearer token.

**Failure modes**: If the sidecar is not running, the CLI reports a structured error with a start hint (exit code contract, JSON error body); if input JSON is malformed or fails schema validation, the CLI rejects with the validation detail before touching the sidecar; if two CLI calls mutate the same session concurrently, sidecar-side session locking serializes them.

**Prompt**:
> Add headless JSON commands to the existing `nexus` CLI: `session new/send/list`, `models list`, `generate queue/status`, speaking to the sidecar over the loopback gateway using the v1.16.0 bearer-token auth (reuse, do not invent a second auth path). Strict JSON schemas for input/output, stable exit codes, `--json` guaranteed machine-parseable output. Document the surface for agent consumption. Acceptance: schema-validation tests, auth-failure and sidecar-down paths, an end-to-end scripted session against a stubbed model, concurrent-mutation serialization test.

---

#### 6.3 -- Diffusion VRAM-budget knobs (A10, IV5)

**Objective**: Explicit budget knobs for the diffusion pipelines -- cache RAM/VRAM caps, working-memory reserve, layer streaming toggle -- folded into the DiffusionTier policy with tier-derived defaults.

**Failure modes**: If a user sets contradictory budgets (VRAM cap below model minimum), validation rejects with the computed minimum instead of OOMing at generation time; if layer streaming is enabled on hardware where it thrashes (slow disk), a preflight measurement warns and suggests defaults; if the knobs are absent (older config), tier defaults apply unchanged.

**Prompt**:
> Add explicit budget knobs to the diffusion pipeline configuration surfaced under Advanced settings: `max_cache_vram_gb`, `max_cache_ram_gb`, `working_mem_reserve_gb`, and a partial-loading/layer-streaming toggle, with defaults derived from the detected DiffusionTier. Implement layer streaming in the local pipelines where the underlying framework supports it; validate knob combinations against model minimums. Acceptance: validation units, tier-default derivation tests, a constrained-VRAM integration test demonstrating a previously-OOM configuration completing with streaming enabled.

---

#### 6.4 -- Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until the phase is stable before advancing to Phase 7.

**Prompt**:
> Generate comprehensive tests for Phase 6: audit-log signing/tamper/attribution/backpressure suite, CLI schema and auth contract tests plus the scripted end-to-end session, and diffusion budget validation + constrained-VRAM integration. Run, fix, iterate until green. Then create or update CI/CD for this phase's changes and optimize action minutes while keeping coverage. Do not proceed to Phase 7 until stable.
> After all tests pass, run `/generate-session-history` to document Phase 6.

---

### Phase 6 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 7

---

## Phase 7: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, and its CI/CD complete and optimized.
**Prerequisites**: All prior phases.
**Stability Gate**: The layout is clean (no deprecated/obsolete files, empty dirs, redundant files/dirs, or overcomplicated structure left un-triaged); the version's known gaps are reconciled; CI/CD covers every change and is optimized; project validation/tests pass.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, reference repair, known-gap reconciliation, and release gating carry high context volume and blast radius.

### Sub-tasks

#### 7.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure, then refactor toward a clean, intuitive layout via [[project-refactor]] and [[docs-layout-refactor]] (propose-then-apply, with confirmation; repair every reference for anything that moves).

#### 7.2 - Known-gaps reconciliation
**Objective**: Reconcile the version's open gaps.
**Prompt**:
> Reconcile this version's known gaps via [[known-gaps-tracker]]: resolve, defer, or transfer each open item, and finalize the per-minor known-gaps file for the version. Include the deferred comparison items (A13 minimal mask canvas, A14 frame-anchored video comments) and the DiffusionGemma watch item with its flip conditions.

#### 7.3 - CI/CD create/update/optimize
**Objective**: CI/CD covers all changes and is optimized.
**Prompt**:
> Create or update the CI/CD pipeline so it covers every change in this plan, then optimize it to reduce action minutes (path filters, concurrency cancel-in-progress, dependency caching, gating expensive-OS or matrix jobs to merges/schedule) while keeping comprehensive testing. Keep it platform-agnostic; GitHub Actions is the primary example.

#### 7.4 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and CI/CD is green.
**Prompt**:
> Run the full validation/test suite, confirm the refactor changed no behavior, confirm CI/CD passes and the action-minute reduction is real, and iterate until clean. Generate a session-history entry for this phase.

---

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|

---

### Phase 7 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] Session history generated for this phase
- [x] Ready to release v2.1.0
