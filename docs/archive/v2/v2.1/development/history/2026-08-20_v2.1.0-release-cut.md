# Session history - v2.1.0 release cut

**Date**: 2026-08-20
**Scope**: `/update release` after Phases 1-7 and the known-gaps sweep

## Version surfaces

- `package.json` / lockfile root entries / `desktop/src-tauri/tauri.conf.json`: **2.1.0**
- `desktop/package.json` and `desktop/src-tauri/Cargo.toml` stay **1.5.0** (desktop package number, same as the v2.0.0 cut)

## Docs

README What's new is no longer in-progress. Ledger row Landed. CHANGELOG `[2.1.0]` plus three opt-in surfaces. DEVLOG cut entry. Known-gaps status **finalized**. ARCHITECTURE stale Phase 3/4/6 sentences updated for iTXt, Chat ffmpeg sampling, JSON CLI bind, and Video Lab VRAM knobs.

## Not claimed

Live golden-task eval (`localEval.status: not_run`), DiffusionGemma catalog entry, live Unsloth GPU train, and remaining hardware DFs. `recommended.json` unchanged.

## Release mechanics

Commit on `develop`, tag `v2.1.0`, push, GitHub Release from the CHANGELOG section. This repo has no Hub `check_version_sync.py` or `MANIFEST.sha256`; those steps are a no-op.
