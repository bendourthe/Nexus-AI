# v2.4.1 Windows and GPU Operator Checklist

Use this checklist only after automated Phase 8 gates pass. Record `Observed`, `Failed`, or `Not observed` for every item. Attach screenshots and logs beside the result. Do not infer a pass from automated tests.

## Host Record

- Date/time: 2026-08-29 20:10 PDT
- Nexus commit and version: Phase 8 pre-push candidate on `feat/v2.4.1-field-reliability`, version 2.4.1
- Windows edition/build: Windows 11 Pro, build 26200
- GPU and VRAM: NVIDIA GeForce RTX 3080 Ti Laptop GPU, 16,384 MiB
- NVIDIA driver/CUDA runtime: driver 596.08; packaged diffusion torch 2.3.0+cu121, CUDA 12.1, Python 3.12.10, `cuda_available=true`
- Installer artifact SHA-256: `E29725ADE5671271962CE9FB6DE66DCAA38BA182DC257802DFA835C78120490D` (unsigned generation-recovery Phase 7 freeze)
- Existing install before run: 2.3.1 reported by the user; not modified during automated verification
- Evidence directory: `docs/archive/v2/v2.4/development/`; rebuilt artifact will be `dist/NexusSetup.exe`

## Checklist

1. **Clean install progress**: Start the frozen installer on a clean Windows host. Confirm the overall bar is visually larger than phase bars, shows a live percentage, keeps the filled segment stationary and pill-shaped, moves one broad liquid-glass highlight smoothly through that fill over about 12 seconds, never decreases, reaches 100%, and remains understandable with Windows reduced motion enabled.
2. **Repair install progress**: Run the same artifact over an existing Nexus install. Confirm the same progress hierarchy and that retained/repair steps do not trigger the old `unsloth-pins.json` FileNotFoundError. Save `Downloads/nexus-install-nexus-desktop-log.txt`.
3. **Optional Unsloth**: Repeat with Unsloth enabled on a supported NVIDIA host. Confirm the pinned manifest is present in the frozen artifact, provisioning is opt-in, and failure is contained in the wizard with a retryable summary.
4. **SANA and CUDA runtime**: Select SANA/image support. Confirm the diffusion venv is created or repaired, its readiness response reports CUDA torch available, and Image Studio generates one real PNG. Record Python, torch, CUDA, model id, and elapsed time.
5. **Pending and response bubbles**: In Chatbot and Agents, send a message. Confirm the generating animation appears directly on the transcript canvas without a bubble; only the completed assistant response receives a bubble. Confirm user and assistant bubbles use subtly different backgrounds.
6. **Token and reasoning metadata**: Confirm time is left-aligned, `(X tokens)` is italic and right-aligned, hover and keyboard focus reveal input/reasoning/output counts, and a thinking-capable model shows a collapsed reasoning disclosure above the answer bubble that expands and collapses without mixing reasoning into the answer.
7. **Archive/delete matrix**: For Chatbot, Agents, Images, and Videos, delete one active session and archive another. Confirm both actions warn that the active view will reset, delete warns it cannot be undone, archive is reversible, late generation events do not repopulate the reset view, Settings > Archived chats groups by pillar, and Restore returns the session.
8. **Models and storage**: Compare installer and Settings model roster/order. Confirm downloaded cards use a restrained highlight, every catalog card shows a one-sentence description, size appears as a pill, VRAM appears beside compatibility, expanded details add information, and external weights stay in a separate honest group. Record the disk meter before install, after install, and after remove.
9. **Multi-root Agents workspace**: Confirm first launch defaults to the OS home directory. Replace the primary root, add a second root, cancel a picker without changing state, remove an extra root, and restart the app to prove persistence. Create a session, read/write inside both roots, attempt and observe denial outside both roots, then verify history groups the session under `Primary +1` with full-root tooltip. Confirm changing roots during an active run requires stop-and-switch confirmation.
10. **Gated Hugging Face model sign-in**: Select a gated model, accept its publisher terms in the browser, then use `Sign in with Hugging Face`. Confirm browser device authorization returns to the installer, validates repository access, and continues without copying a token. Confirm manual read-token entry and `Skip this model` remain usable fallbacks.

## Recorded Status

| Item | Status | Evidence and disposition |
|---:|---|---|
| 1 | Failed on two superseded artifacts; second replacement not observed | User screenshots 4-5 showed no perceptible motion, low-contrast percentage text, and 73% after every visible group said Done. The next candidate replaced that with a fast traveling rectangular segment, which the follow-up field screenshot also rejected. The second replacement is determinate from 0%, keeps its fill stationary and pill-shaped, moves one broad glass sheen on a 12-second cycle, retains the dark percentage capsule, and maps `runtime` / `hub-catalog` / `unsloth` into visible groups. Automated render/lifecycle tests and frozen smoke pass; packaged appearance still needs confirmation. |
| 2 | Not observed | Frozen-resource and repair-state tests passed. The existing 2.3.1 installation was not overwritten during automated verification. |
| 3 | Not observed | Frozen Unsloth manifest resolution and optional-failure containment tests passed. No opt-in packaged provisioning run was performed. |
| 4 | Observed (PNG and MP4); reboot persistence Not observed | Exact installer `E29725ADE5671271962CE9FB6DE66DCAA38BA182DC257802DFA835C78120490D` after packaged repair. Runtime schema 3 / ready / torch 2.3.0+cu121 / CUDA 12.1. Image `realvisxl-v5` 512x512 PNG 330437 bytes SHA-256 `1db719ce7693e918e958e22f6b7de34fe9bede4b5e9b128bdad4fc96db35fa80`. Video `wan2.1-t2v-1.3b` H.264 848x480 (16-aligned from advertised 854x480) 13 frames 1.083 s SHA-256 `60bb5cb281b7845ec7839467ba4d3ababd3a15cea52dd43e4cdfbb54a9a22422`. Report: `docs/archive/v2/v2.4/development/installed-media-smoke-report.json`. |
| 5 | Not observed | Chatbot and Agents component tests passed for pending and completed states. No packaged visual comparison was captured. |
| 6 | Not observed | Token hover/focus, collapsed reasoning, hydration, and absent-reasoning tests passed. No thinking-capable local-model turn was run. |
| 7 | Not observed | Store, IPC, cancellation, late-event, reset, and restore tests passed for all four pillars. No packaged four-pillar matrix was run. |
| 8 | Failed on the pre-correction artifact; replacement not observed | User screenshots 1-2 showed Nomic incorrectly locked as required and incompatible models interleaved above compatible rows. The replacement makes EmbeddingGemma 300M the sole required/default embedder and partitions incompatible rows to the bottom in installer and Settings. Automated policy tests pass; packaged parity and disk changes still need observation. |
| 9 | Not observed | Home default, picker, persistence, workspace grouping, and union-of-roots denial tests passed. No packaged two-folder operator walkthrough was run. |
| 10 | Failed on the superseded replacement; second replacement not observed | The superseded replacement called `huggingface_hub.login()`, which waits for a hidden terminal login-method choice in the windowed executable; the field screenshot showed the resulting stuck dialog and inert browser actions. The second replacement calls the device-code request/poll primitives directly, opens URLs on Qt's main thread with an OS-browser fallback, keeps Skip/manual controls enabled, validates repository access, and bundles both OAuth modules. Unit/UI worker tests and frozen archive checks pass, but a live account and gated repository were not exercised. |

## Result

- Overall verdict: Item 4 (CUDA PNG and MP4) is Observed on installer `E29725ADE5671271962CE9FB6DE66DCAA38BA182DC257802DFA835C78120490D`. Items 1, 8, and 10 failed on superseded artifacts and remain unobserved on this freeze. Reboot persistence is Not observed.
- Failed item numbers: 1, 8, and 10 across superseded artifacts; no failure has been claimed against this generation-recovery freeze.
- Deferred item numbers and reason: 1-3 and 5-10 remain pending for visual, account, destructive session, and reboot observation.
- Logs/screenshots: installed-media report `docs/archive/v2/v2.4/development/installed-media-smoke-report.json`; artifact SHA-256 values are quoted in last-phase evidence.
- Operator name: Cursor session continuing the Codex Phase 7 run; human release operator still pending for T097.
