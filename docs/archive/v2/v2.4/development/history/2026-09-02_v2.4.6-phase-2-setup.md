# Development Log: v2.4.6 Phase 2 - Installer Setup compactness

**Date**: 2026-09-02
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: Setup fits prerequisites, GPU, and install path without a tall empty card.
**Outcome**: Compact Setup uses two-column prereqs, a title-row refresh icon, one-line GPU text, and packed vertical spacing.

---

## 1. Starting State

- **Branch**: `feat/v2.4.6-field-delivery-density-and-session-identity` at Phase 1 commit `fb7d8ddd`.
- **Environment**: Windows 11, Python 3.12, pytest, ruff
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.6-field-delivery-density-and-session-identity.md`
- **Field evidence**: packaged screenshot 1 still showed one-column prereqs, a full-width Re-check bar, a two-line GPU card, and empty stretch below Install Path.

---

## 2. Chronological Steps

### 2.1 Two-column prerequisites and refresh icon (T006, T007)

**What happened**: Compact `PrerequisitesPage` puts VS Code and Python on row 1, Disk and Ollama on row 2. Width below 520 px restacks to one column. Re-check is a title-row `QToolButton` (`↻`) with accessible name and tooltip `Re-check`. The full-width `SecondaryButton("Re-check")` remains only on the standalone page. A second click disconnects, quits, and waits on the previous detection worker before starting a new one.

### 2.2 One-line GPU card and packed Setup spacing (T008, T009)

**What happened**: Compact GPU card padding is 8 px. On detect, one `QLabel` shows `{name} | Vendor: {Vendor} | {display_vram_gb} GB VRAM`. Long names elide with a full-string tooltip when the label is at least 80 px wide; narrower labels keep the full text so unshown widgets do not collapse to ellipses. No GPU hides the card and keeps the warning status line. Compact mode hides the recommended-model callout. `SetupPage` spacing is 6 px with no trailing stretch. Install path and the available-space caption stay.

### 2.3 Testing (T010)

**What happened**: `test_pages_qt.py` asserts no compact `QPushButton` text `Re-check`, one `QToolButton` named `Re-check`, two-column then stacked grid, worker replace, one-line GPU including vendor and VRAM, hidden GPU card on miss, elision, and no trailing stretch.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| ruff check + format on installer `src`/`tests` | passed |
| pytest `tests/test_pages_qt.py` plus GPU/prereq coverage files | passed |
| full installer pytest | passed (3 skipped) |
| compactness module coverage | setup 97%, prerequisites 84%, gpu_detection 70% (OS probe branches not taken on this Windows host) |
| Remote CI | NOT RUN (non-final phase is commit-only) |

---

## 4. Known Issues

- MT-2: packaged Setup compactness is not observed on a frozen NexusSetup.exe.
- gpu_detection.py file coverage stays at 70% because Linux/macOS/AMD probe helpers are unchanged and not executed here.

---

## 5. Plan Discrepancies

- GPU elide skips when the label is narrower than 80 px so Qt tests on unshown widgets still see the full one-line string. The plan asked for elision of long names, which still runs once the card has a real width.

---

## 6. Assumptions Made

- 520 px is the compact two-column breakpoint.
- Compact Setup does not need the recommended-model callout; Configuration still owns the model choice.
- Package metadata stays 2.4.1 until `/update release`.
- Cursor session model already matches the plan's frontier cell; Phase 2's strong/high recommendation is met by staying on that session.

---

## 7. Testing Summary

Automated only. Human QA of Setup packing is deferred to Phase 8.

---

## 8. TODO Tracker

- T006-T011 done
- Phase 3 is next (Configuration, VS Code 1.136, Review groups)

---

## 9. Summary and Next Steps

Phase 2 removes the empty Setup card the operator screenshot showed. Next: Phase 3 Unsloth auto-check, VS Code Features including 1.136, drop Gemma sampling and Video2X from Configuration, and group Review by catalog section.
