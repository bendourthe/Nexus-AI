# Development Log: v2.4.3 Phase 2 - History Chrome, Selection, Titles, and Delete Overlay

**Date**: 2026-08-31
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: Tabs and chats are visually split, New chat opens empty and selected, first Image prompt names the session, and delete is centered.
**Outcome**: Hairline under the four module tabs. Toolbar New chat selects and opens the row. Pre-created untitled Image/Video sessions title on first send. Delete and archive dialogs portal to `document.body`.

---

## 1. Starting State

- **Branch**: `feat/v2.4.1-field-reliability`
- **Starting tag/commit**: `b384027` (v2.4.3 Phase 1)
- **Environment**: Windows 10, Node 20, vitest, tsc
- **Prior session reference**: `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-1-installer-layout.md`
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.3-field-density-identity-and-runtime.md`

---

## 2. Chronological Steps

### 2.1 Hairline (T007)

**What happened**: `sidebar-history-hairline` sits between `sidebar-module-nav` and `sidebar-history-host`. Settings keeps the spacer and has no hairline.

### 2.2 New chat selects (T008)

**What happened**: `FolderTree.onCreateChat` calls `selectNode` and `onOpenChat` instead of entering rename. Image/Video hydrate an empty transcript. ChatPage `handleOpenChat` loads zero messages.

### 2.3 First-send titles (T009)

**What happened**: `shouldTitleOnFirstSend` plus Image/Video `ensureSession` schedule `applyImmediateFallbackTitle` when the active session already exists and is still "New chat" with turnCount 0.

### 2.4 Centered delete (T010)

**What happened**: Delete and archive dialogs `createPortal` to `document.body`. Backdrop z-index 2000.

### 2.5 Testing (T011-T012)

**Verification**: See section 3.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| ESLint on touched desktop files | PASS |
| `tsc --noEmit` | PASS |
| Focused vitest (5 files, 93 tests) | PASS |
| Remote CI | NOT RUN (non-final phase, commit-only) |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| jsdom cannot prove hairline pixels or dialog centering in a packaged window | P2 | v2.4.3 MT-2 |
| v2.4.3 MT-1 packaged installer screenshot | P2 | unchanged |

---

## 5. Plan Discrepancies

- Immediate title for "Generate image of a puppy" is the six-word fallback of that prompt, not the later model string "Image puppy generation". Refine still runs after generate.

---

## 6. Assumptions Made

- Dropping auto-rename-on-create is what "select instead of only entering rename" required.

---

## 7. Testing Summary

### Automated Tests
- Hairline present on /images, absent on /settings.
- New chat toolbar calls onSelect + onOpenChat.
- Delete dialog parent is document.body.
- Pre-created Image "New chat" becomes "Generate image of a puppy" on first send.

### Manual Testing Performed
- None; human QA is deferred to the last phase.

### Manual Testing Still Needed
- human QA is deferred to the last phase

---

## 8. TODO Tracker

### Completed This Session
- [x] T007-T012

### Remaining
- [ ] Phase 3 thinking pill geometry (T013-T015)

### Out of Scope
- [ ] Push / PR (final phase only)

---

## 9. Summary and Next Steps

Phase 2 selects new chats, titles untitled studio sessions on first send, and centers delete over the window. Next: Phase 3 thinking-pill geometry.
