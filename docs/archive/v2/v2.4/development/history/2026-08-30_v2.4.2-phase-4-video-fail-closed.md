# Development Log: v2.4.2 Phase 4 - Video Fail-Closed Output

**Date**: 2026-08-31
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: A video turn that cannot produce a playable clip shows why, never an empty success bubble.
**Outcome**: Completing without a usable mp4 path, or with a path the player cannot resolve, writes a non-empty error. Persist never stores empty assistant content without `mediaRef`. Missing weights quote Settings > Models.

---

## 1. Starting State

- **Branch**: `feat/v2.4.1-field-reliability`
- **Starting tag/commit**: `dfbaa80` (Phase 3 image follow-up identity)
- **Environment**: Windows 10, Node 24, desktop Vitest 2.1.9, jsdom
- **Prior session reference**: `docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.2-phase-3-image-followup.md`
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.2-field-ui-history-and-generation.md`

Context: Packaged 2.4.1 could finish a first puppy-in-grass video as `pending: false` plus empty `content` and no `media`. Installer dirty files stay unstaged for Phase 6.

---

## 2. Chronological Steps

### 2.1 Never-empty assistant (T020)

**Branch**: `feat/v2.4.1-field-reliability` | **PR**: none | **Merged to**: n/a

**Plan specification**: Forbid `pending: false` + empty `content` + no `media`. Replace persist of empty assistant content without `mediaRef`. Media-runtime recovery may keep empty bubble text only when `mediaRecovery` actually renders.

**What happened**: `videoFailClosed.ts` owns `EMPTY_VIDEO_CLIP`, `formatVideoFailure`, and `persistableAssistant`. `VideoLabPage` routes assistant persist through that helper. Complete without a usable path, or with a path whose `resolveMp4Url` is empty, patches and persists the playable-clip sentence. Decode failure and empty continuation plans use distinct `formatVideoFailure` sentences. Media-runtime failures persist the recovery message (or the formatted error if the card cannot render).

**Key files changed**: `desktop/src/modules/video/videoFailClosed.ts`, `desktop/src/modules/video/VideoLabPage.tsx`

**Verification**: VideoLabPage tests for persist of empty-complete, missing-weights copy, and unresolved player URL.

### 2.2 Instant SANA-Video failure (T021)

**Plan specification**: Trace SANA-Video 2B 720p. Fail-closed mapping from each terminal job state. Weights-not-installed must say Settings > Models. Fake dispatcher that resolves in one tick with no path.

**What happened**: `requireUsableVideoPath` already throws the Python envelope (or `VIDEO_RUNTIME_NOT_READY`) before `complete`. SANA-Video routes to `diffusion.video.sana.text2video`. Added a one-tick regression: `ok: false` plus weights-not-installed rejects instead of completing empty. UI maps that envelope through `formatVideoFailure`.

**Key files changed**: `desktop/tests/videoDispatcher.test.ts` (product dispatcher already fail-closed)

**Verification**: Dispatcher test throws `/weights are not installed/` for `sana-video-2b-720p`.

### 2.3 Testing and stabilization (T022-T023)

**What happened**: New `desktop/tests/videoFailClosed.test.ts` plus VideoLabPage persist / weights / unresolved-URL cases. First coverage run timed out in `video2x-adapter` and `video-enhancement-integration` (ENV). Isolation passed in 6.56s. Coverage retry passed.

**Verification**: See section 3.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Desktop ESLint (`npm run lint` in `desktop/`) | PASS |
| Desktop `tsc --noEmit` | PASS |
| Desktop vitest with coverage (retry after ENV isolation) | PASS (208 files, 1918 passed, 1 skipped; lines 86.78%, branches 81.42%, functions 82.05%) |
| Isolation `video2x-adapter` + `video-enhancement-integration` | PASS (73 passed, 1 skipped) after coverage timeouts |
| Remote CI | NOT RUN (non-final phase, commit-only) |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| jsdom cannot prove thinking-pill pixel containment | P2 | v2.4.2 MT-1 |
| Stick-to-bottom on a live overflow pane is not observed | P2 | v2.4.2 MT-2 |
| Packaged img2img identity is not observed | P2 | v2.4.2 MT-3 |
| Packaged empty-video fail-closed is not observed | P2 | v2.4.2 MT-4; Phase 7 operator screenshot |
| Coverage load can time out 5s video2x/enhancement tests | P3 | ENV; isolation then retry |

---

## 5. Plan Discrepancies

- Dispatcher already threw on missing `mp4Path`; Phase 4 added the UI persist contract and a SANA one-tick weights regression rather than rewriting `videoDispatcher.ts`.
- Decode failure patches the live bubble through `formatVideoFailure` but does not append a second explorer turn (the success persist may already hold a `mediaRef`).

---

## 6. Assumptions Made

- **Installer dirty files stay out of this commit**: Phase 6 T032 owns Setup/Reset/SANA installer UI still uncommitted on this branch.
- **`isUsableVideoPath` stays any non-empty trimmed string**: player resolve returning `""` is the extra fail-closed gate.

---

## 7. Testing Summary

### Automated Tests
- Desktop vitest + coverage: 1918 passed, 1 skipped, 0 failed. Aggregate lines 86.78% (threshold 80%).
- Focused VideoLabPage (29), videoFailClosed (2), and videoDispatcher SANA one-tick test passed.

### Manual Testing Performed
- None; human QA is deferred to the last phase.

### Manual Testing Still Needed
- human QA is deferred to the last phase

---

## 8. TODO Tracker

### Completed This Session
- [x] T020 Forbid empty video success bubbles
- [x] T021 Surface the real SANA-Video failure path
- [x] T022 Empty-output and instant-fail video tests
- [x] T023 Stabilize and record local evidence

### Remaining (Not Started or Partially Done)
- [ ] Phase 5 Settings Models Density (T024-T027)

### Out of Scope (Deferred)
- [ ] Installer VRAM / Unsloth / VS Code merge (Phase 6)
- [ ] v2.4.1 T097 push/PR

---

## 9. Summary and Next Steps

Phase 4 landed fail-closed video output: no empty success bubble, persist refuses empty assistant content without `mediaRef`, and missing weights point at Settings > Models. Next: `/implement v2.4.2-field-ui-history-and-generation in-full` continues at Phase 5.
