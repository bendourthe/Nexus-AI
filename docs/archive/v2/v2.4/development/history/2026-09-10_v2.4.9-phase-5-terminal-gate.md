# Session history -- 2026-09-10 -- v2.4.9 Phase 5: Architecture refactor, known-gaps reconciliation, and CI/CD

**Plan**: [v2.4.9-adoption-voicestudio-field-discipline.md](../../plans/v2.4.9-adoption-voicestudio-field-discipline.md), Phase 5 (renumbered from 6 by D1)
**Evidence**: [v2.4.9-last-phase-evidence.md](../v2.4.9-last-phase-evidence.md)
**Mode**: `/implement ... full`
**Branch**: `develop`, one local commit, **nothing pushed**

## What this phase was for

Leave the repository clean, reconcile the gaps, reconcile the pipeline, prove the Goal landed by inspecting artifacts rather than ticking checkboxes, and stop at the approval gate before anything leaves the machine.

## Duties

| Duty | Outcome |
|---|---|
| 5.1 Architecture refactor | Report-only. Both predicted findings present. |
| 5.2 Known-gaps reconciliation | 30 files globbed; 5 in-progress listed and left. 2 resolved, 10 open in v2.4.9. |
| 5.3 Living docs | One finding found and fixed: `feature-inventory.md` was orphaned. |
| 5.4 Git-tree hygiene | Clean tree. `protected_checkout=yes` changes the publication mechanics. |
| 5.5 Terminal CI/CD reconciliation | PASS on this plan's checks; four contract differences recorded, none applied. |
| 5.6 Tier 3 deep pass | Weighted run. Bounded clean. |
| 5.7 Goal-vs-codebase review | Four clauses met, one deferred by D1, **one not met and not claimed**. |
| 5.8 Human testing suggestions | Six, all beyond automated reach. |
| 5.9 Full local gate | Zero new failures across four suites. |
| 5.10 Publication | **Not started. Awaiting approval.** |

## The three things worth reading

**1. Clause 4 of the Definition of Done is not met, and is not claimed.** "CodeQL reports Python results with a non-zero scanned-file count." The configuration is in place and the count does not exist, because the CodeQL CLI was deliberately not installed. Calling this satisfied would be precisely the satisfied-in-intent failure the Goal review exists to catch, so it is recorded as deferred-and-named with the check that settles it: the first CodeQL run on the integration pull request, where a count in the low hundreds against 308 tracked Python files is a pass and a count in the thousands means the extractor found a virtualenv and is a miss.

**2. Branch protection changed how this plan can publish.** Protection was applied in Phase 2 while the work sat on `develop` itself, so a direct push to `develop` is now blocked by its own required checks. Publication goes through a feature branch created at the current HEAD. No reset, no history rewrite; local `develop` converges when the pull request merges. This is a consequence of an operator decision taken mid-plan, not a defect, but it had to be discovered before the push rather than by a rejected push.

**3. The pull request carries 14 commits, not 6.** Eight predate this session, including the whole v2.4.8 range. Measured against the remote, as the runbook insists, rather than against local `develop`: 98 files and 12,814 insertions. They cannot be unbundled because `develop` requires a pull request and every unpushed commit travels with the branch, so the disposition is disclosure in the pull-request description.

## Plan delta

**Disposition: No delta.** Considered, not omitted.

Phase 5's duties executed as written. The two findings the plan predicted by name for 5.1 were both present and both real: the root-level `.vsix` artifacts (untracked local litter, left alone) and `core/storage/StorageMigration.ts`, whose line 18 says "Removed in v1.1.0" while the file and its test still exist and nothing calls it.

Two instructions were satisfied by a state the plan allowed for rather than by an action. 5.4 asks for the Phase 1.3 staging guard to be removed; 1.3 was cut at Phase 1 because the tree was clean, so `.husky/pre-commit` is untouched at its original single line, and the plan's parenthetical "(If 1.3 was cut, record that instead)" is what applies. 5.4 also asks whether the installer rework is still uncommitted; it is committed, having landed through `f36afd9c` before Phase 1 began.

**Consequence for remaining phases**: none. This is the last phase.

## Deviations

- **`docs/reference/feature-inventory.md` was linked from `CONTRIBUTING.md` and `AGENTS.md`** during 5.3. That is a content edit inside a duty the plan frames as a check, but the check is specifically "reachable rather than orphaned" and it failed; leaving a document the plan asked for unreachable would have satisfied the letter of the duty and defeated it.
- **One Tier 3 limit was recorded in the evidence rather than filed as a gap**: with two identically-named `## ` headings the drift checker parses only the first slice, leaving names in the second unenforced. Duplicate H2 headings in one README is pathological input, and filing it would add noise to a gap log that already carries ten open items.

## Verification

Full local gate, against the measured `f36afd9c` baseline: root vitest 61 failed / 476 passed (baseline 61 / 473), desktop 9 failed / 216 passed (baseline 9 / 215), python 313 passed (baseline 310), installer python pass. **Zero new failures**; every failure is the `better-sqlite3` Electron-ABI baseline, confirmed by a two-run stashed comparison earlier in the session. Lint exit 0, `nexus-check` 0 errors with the baseline matched and 0 stale, tampering 0, docs-layout OK, naming clean, drift checker OK.

54 tests were added across the cycle, every one confirmed collected by running it rather than assumed, and none written as `.test.mjs`, which `configs/vitest.config.ts` would silently skip.

The Tier 3 deep pass weighted by blast radius: full procedure for the out-of-plan GPU fixes (both proven to fail against pre-change code), the drift checker (three adversarial probes covering duplicate headings, regex metacharacters in a feature name, and a traversal-shaped evidence path), and the two gating security checks (re-run terminally: 750 commits with no leaks, 180 packages with no advisories). Reduced path for the additive documents and templates.

## Next steps

`5.10` is the only remaining step and it is the plan's single remote event: create the feature branch, push once with explicit approval, open the pull request into `develop`, execute the eight deferred observations, wait for every required check, and merge only on green. A red check reopens this phase and is reproduced locally before any re-push.

Two things need the operator's answer at that gate: approval for a pull request carrying 14 commits rather than 6, and a decision on the version outcome, because two `feat` commits in this plan compute a **minor** under `semantic-release` while the plan expects a patch.

`/update release` runs only after the integration result is green and merged.
