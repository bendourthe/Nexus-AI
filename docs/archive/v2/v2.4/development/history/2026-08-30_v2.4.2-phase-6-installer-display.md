# Development Log: v2.4.2 Phase 6 - Installer VRAM, Unsloth, and VS Code Merge

**Date**: 2026-08-31
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: GPU totals read as whole GB, Unsloth compatibility is obvious, and VS Code is not its own wizard step.
**Outcome**: Display ceil helper (16384 and 15360 both show 16 GB). Unsloth shows Compatible or Incompatible before opt-in. STEP_NAMES is Welcome, Setup, Models, Configuration, Review, Installing, Complete. The VS Code checkbox lives on Configuration. Prior Setup/Reset/SANA installer UI is included.

---

## 1. Starting State

- **Branch**: `feat/v2.4.1-field-reliability`
- **Starting tag/commit**: `f9ed98d` (Phase 5 Settings Models density)
- **Environment**: Windows 10, Python 3.11, uv, pytest, PyQt5
- **Prior session reference**: `docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.2-phase-5-settings-density.md`
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.2-field-ui-history-and-generation.md`

Context: Installer dirty files from the Setup-step collapse, Reset-on-tab-row, and SANA selectable work were held until this phase (T032).

---

## 2. Chronological Steps

### 2.1 VRAM display ceil (T028)

**Plan specification**: `display_vram_gb(vram_mb) -> int` using ceil. GPU Detection copy and typed catalog subtitle. Do not change `tier_defaults` math. Pin 16384 -> 16 and 15360 -> 16.

**What happened**: New `vram_display.py`. GPU detection prints `N GB VRAM` instead of megabytes. Catalog subtitle uses the helper; `_current_defaults` still passes `int(vram_mb / 1024)`.

### 2.2 Unsloth badge (T029)

**Plan specification**: Compatible / Incompatible pill using the same NVIDIA + 16 GB floor rule as the warning. Incompatible still allows opt-in. NVIDIA 16 GB+ hides the incompatible badge.

**What happened**: Badge is visible before the checkbox is ticked. Warning still appears only after opt-in on an incompatible host.

### 2.3 VS Code on Configuration (T030-T031)

**Plan specification**: Move detection + checkbox onto Configuration. Remove VS Code from STEP_NAMES. Unsupported CLI cannot check the box. `--skip-extension` still works. Review/Installing still list the extension when selected.

**What happened**: Compact `VsCodeExtensionPage` is nested on Configuration. Wizard route is 7 steps. Smoke flag remains `--skip-extension` (the product name; the plan's `--skip-vscode` alias does not exist).

### 2.4 Uncommitted Setup/Reset/SANA (T032)

**What happened**: Included `setup.py` and the already-dirty GPU/prereq/path/catalog/navigation tests that collapse three machine panels onto Setup.

### 2.5 Testing (T033-T034)

**Verification**: See section 3.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| `uv run ruff check` on touched installer files | PASS |
| Full installer pytest (`uv run pytest tests` in `scripts/installer`) | PASS (3 skipped) |
| Remote CI | NOT RUN (non-final phase, commit-only) |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Prior desktop MT-1 through MT-5 | P2 | unchanged |
| Live wizard GPU/Unsloth/VS Code screenshot is not observed | P2 | v2.4.2 MT-6; Phase 7 operator item 9 |

---

## 5. Plan Discrepancies

- Smoke skip flag is `--skip-extension`, not `--skip-vscode`.
- 15360 MiB displays 16 GB by a 15 GiB-class band, because `math.ceil(15360/1024)` is 15 and the plan pins 15360 -> 16.

---

## 6. Assumptions Made

- Nested compact VS Code widget plus Configuration `set_interactive` is enough for the install-lock path.
- Setup-step collapse from the held dirty tree is in scope via T032.

---

## 7. Testing Summary

### Automated Tests
- Full installer pytest: passed, 3 skipped.
- New `test_vram_display.py` (4). Navigation 7-step, Unsloth badge, VS Code-on-Configuration, catalog subtitle 15360 -> 16 GB.

### Manual Testing Performed
- None; human QA is deferred to the last phase.

### Manual Testing Still Needed
- human QA is deferred to the last phase

---

## 8. TODO Tracker

### Completed This Session
- [x] T028 Display VRAM ceil
- [x] T029 Unsloth Compatible / Incompatible badge
- [x] T030-T031 VS Code on Configuration, 7-step route
- [x] T032 Include Setup / Reset / SANA installer UI
- [x] T033-T034 Tests and local evidence

### Remaining (Not Started or Partially Done)
- [ ] Phase 7 last-phase evidence + publication (T035-T043)

### Out of Scope (Deferred)
- [ ] `/update release` until green integration

---

## 9. Summary and Next Steps

Phase 6 landed display-only VRAM ceil, Unsloth badges, a 7-step installer with VS Code on Configuration, and the held Setup-step work. Next: `/implement v2.4.2-field-ui-history-and-generation in-full` continues at Phase 7, which waits on explicit approval before any push.
