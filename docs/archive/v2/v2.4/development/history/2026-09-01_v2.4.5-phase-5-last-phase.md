# Development Log: v2.4.5 Phase 5 - Evidence, known gaps, CI/CD, integration, installer rebuild

**Date**: 2026-09-01
**Operator**: Benjamin Dourthe
**Assisted by**: Claude Opus 5
**Objective**: Quote every last-phase duty as evidence, reconcile gaps, integrate, and rebuild the installer for field testing.
**Outcome**: Local gate green after two environment findings were diagnosed. Publication and rebuild follow this record.

---

## 1. Starting State

- **Branch**: `feat/v2.4.5-installer-downloaded-models`
- **Starting commit**: `afe86686` (v2.4.5 Phase 4)
- **Environment**: Windows 11 Pro, Node 24.13.0, Python 3.12
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.5-installer-already-downloaded-models.md`

---

## 2. Chronological Steps

### 2.1 Architecture refactor (T019)

**What happened**: The empty-directory scan returned the same two `SkillLoader` test byproducts as v2.4.4. Neither is tracked; both are recreated by the next suite run. No deprecated files, no redundant directories, nothing moved. Finding empty, scan quoted in the evidence file.

### 2.2 Known-gaps reconciliation (T020)

**What happened**: A `## v2.4.5` section with 2 MT rows, 1 WN row, and one resolved BG row.

BG-1 is closed on code evidence: the guard demonstrably sizes the remaining download, with the exact field reproduction passing and the old `204.4 GB` refusal pinned so a regression is unambiguous. Packaged confirmation is still owed and is MT-1, so the defect is resolved while the observation stays open.

Every v2.4.4 row carries forward unchanged, and the reason is worth stating rather than implying: v2.4.4's work has still never been observed **because the installer it produced could not be installed**. That is the defect this cycle fixes, so this rebuild is the first opportunity to observe any of it. v2.4.4 DF-1 (Wan and SDXL not re-smoked on the 0.36.0 Diffusers pin) remains the widest-blast-radius open item.

The glob covered 32 files; the seven still in-progress were read and none had a row this cycle observed.

### 2.3 Living docs and git hygiene (T021, T022)

**What happened**: Required living tree present and unchanged. `docs/handbooks/technical/installer-runtime.md` covers the provisioning runtime, whereas this cycle changed wizard logic, so no handbook edit was warranted. `docs/testing/` and `docs/validation/` were not created. `check_release_preconditions.py` quoted verbatim, report only, `working_tree=clean`.

### 2.4 CI/CD conformance re-check (T023)

**What happened**: A re-check, not an authoring pass. v2.4.4's QG-1 fix is confirmed still in force, so this branch's develop-targeted pull request will run the full merge-result gate rather than commitlint alone. `installer-tests.yml` already scopes `scripts/installer/**`, which covers every file this plan touched.

Two v2.4.4 findings remain open and are carried, not re-litigated: `ci.yml` has no always-resolving aggregate required check and no top-level `permissions:` block. No pipeline file was edited this cycle. Cross-installer parity was not executed; this cycle changes wizard logic shared by all three installers, so parity is unaffected by construction, but that is reasoning rather than an executed gate and is recorded as such.

### 2.5 Goal-vs-codebase review (T024)

**What happened**: All nine Goal slices have a named artifact verified against the tree, each pinned by a test that fails against the previous behavior. No slice is missing.

The honest limit is the same one v2.4.4 hit: the Goal describes an installer a person runs, and nothing here has run in the packaged wizard. Recorded as MT-1, not counted as a pass, and it is why this plan ends in a rebuild rather than a release.

### 2.6 Human testing suggestions (T025)

**What happened**: Ten operator items. Four are this cycle's; six are v2.4.4's, unobserved because that installer could not be installed. Item 4 deliberately asks the operator to confirm the guard still *refuses* an oversized new selection, because "the install button now works" is not by itself evidence that the check was fixed rather than removed.

### 2.7 Full-suite testing (T026)

**What happened**: Green, after diagnosing two environment findings.

**The VSIX build leaves `node_modules` unusable by the Node suite.** The first root-vitest run reported 495 failures across 64 files, all `NODE_MODULE_VERSION 146` vs required `137` on `better_sqlite3.node`. This is not a defect in this cycle's code: `scripts/build-vsix.ps1` deliberately rebuilds the native module for Electron 42.8.1 (ABI 146) so the packaged extension works, and Node 24 needs ABI 137. Building an installer therefore breaks the Node suite until `npm rebuild better-sqlite3`. This retroactively explains the 33 failures seen at the start of the v2.4.4 session, which followed an earlier VSIX build. After rebuilding: 495 failures became 3.

**The remaining 3 are contention-sensitive performance budgets.** A 15-second consolidation budget, a p50/p99 retrieval latency budget, and one golden-runner case. Re-run in isolation on the same tree, all 18 tests in those three files pass; they failed only while heavy builds ran concurrently. Not treated as product defects, and not silently omitted from the evidence either.

Final: root vitest 5734 passed, desktop vitest 1963 passed, `tests/python` 292 passed, installer suite 100% with no fatal exception, root lint / `tsc -b` / installer ruff all clean.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Root ESLint | PASS |
| Root `tsc -b` | PASS |
| Desktop ESLint + `tsc --noEmit` | PASS |
| Root vitest | 526 files / 5734 passed (3 load-sensitive, pass in isolation) |
| Desktop vitest | 210 files / 1963 passed, 1 skipped |
| pytest `tests/python` | 292 passed |
| pytest `scripts/installer/tests` | 100%, 0 failed, no fatal exception |
| ruff (installer) | All checks passed |
| Unicode safety on new docs | PASS |
| Remote CI | Pending publication approval |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Nothing in this cycle observed in the packaged wizard | P1 | v2.4.5 MT-1; the rebuild is the first opportunity |
| Every v2.4.4 operator item still unobserved | P1 | Carried; this rebuild finally makes them testable |
| Wan and SDXL not re-smoked on the 0.36.0 pin | P1 | v2.4.4 DF-1; widest blast radius |
| Ollama `/api/tags` not exercised against a live daemon | P2 | v2.4.5 MT-2 |
| `ci.yml` lacks an aggregate required check and top-level permissions | P2 | Carried from v2.4.4 |
| VSIX build breaks the Node suite until `npm rebuild better-sqlite3` | P3 | Newly diagnosed; documented in the evidence file |

---

## 5. Plan Discrepancies

- The plan did not anticipate the Electron/Node ABI interaction. It cost a full misleading suite run and is now written down so the next cycle recognizes it in seconds rather than re-diagnosing 495 failures.

---

## 6. Assumptions Made

- The three load-sensitive failures are environmental. Evidence: all 18 tests in those files pass in isolation on the identical tree.

---

## 7. Testing Summary

### Automated Tests
- Full local gate green; see section 3.

### Manual Testing Performed
- None on the packaged wizard.

### Manual Testing Still Needed
- Ten operator items in the evidence file.

---

## 8. CI Impact

No pipeline file edited in this phase or any earlier one. `installer-tests.yml` already collects the new module and its tests. No new dependency, job, or artifact.
