# Development Log: v2.4.2 Phase 1 - Sidebar History Host and Shared Chrome

**Date**: 2026-08-30
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: Put Chat, Agents, Images, and Videos history under the four module tabs in the left sidebar, uncrop the thinking pill, and restyle overflow scrollbars once for the whole app.
**Outcome**: History portals into `sidebar-history-host`. The main pane is transcript or workspace only. The pending pill no longer applies `border-radius: 999px` to the canvas host. App scrollbars use a transparent track and a muted-foreground thumb.

---

## 1. Starting State

- **Branch**: `feat/v2.4.1-field-reliability` (ahead of `origin/feat/v2.4.1-field-reliability` by 7)
- **Starting tag/commit**: `48daf5f`
- **Environment**: Windows 10, Node 24, desktop Vitest 2.1.9, jsdom
- **Prior session reference**: `docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.1-correction-phase-7-release.md`
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.2-field-ui-history-and-generation.md`

Context: Packaged 2.4.1 still spent a second history column (or an Agents History band), cropped the thinking orb, and used opaque OS scrollbars. This phase is chrome only; installer dirty files stay unstaged for Phase 6.

---

## 2. Chronological Steps

### 2.1 Sidebar history host (T001-T004)

**Branch**: `feat/v2.4.1-field-reliability` | **PR**: none | **Merged to**: n/a

**Plan specification**: One left pane owns module nav plus a history slot. Chat `FolderTree`, studio history, and Agents sessions fill that slot. Settings and Approvals show no slot. Compact icon-rail hides titles and must not open a second column.

**What happened**: Added `SidebarHistoryProvider` and `SidebarHistorySlot` (`createPortal` into a host under `sidebar-module-nav`). Module routes default expanded at 280px. Chat, Image, Video, and Agents register existing trees. Removed `CollapsibleHistoryAside`. Pages rendered without App keep an in-place fallback so unit tests still see `chats-pane` / `coding-history-pane`.

**Key files changed**: `desktop/src/components/SidebarHistoryHost.tsx`, `desktop/src/components/Sidebar.tsx`, `desktop/src/App.tsx`, `desktop/src/modules/chat/ChatPage.tsx`, `desktop/src/modules/coding/CodingPage.tsx`, `desktop/src/shared/explorer/StudioHistoryPane.tsx`, `desktop/src/modules/image/ImageStudioPage.tsx`, `desktop/src/modules/video/VideoLabPage.tsx`

**Troubleshooting**:
- **Problem**: `shell-phase6.test.tsx` still expected the compact-by-default rail (`nav-chatbot` text empty).
- **Root cause**: Phase 1 changed the no-preference default to expanded so session titles fit.
- **Resolution**: Updated those tests to the new default and stored-compact behavior.

**Verification**: `npx vitest run tests/sidebar-history-host.test.tsx` 8 passed.

### 2.2 Uncrop thinking pill (T005)

**Plan specification**: Left dots of the 48px bubble orb must sit inside the visible pill. Do not wrap pending status in a message bubble.

**What happened**: Pill chrome is a sibling layer with `border-radius: 999px`. The host uses `overflow: visible` and no capsule radius. Canvas stays a direct child.

**Key files changed**: `desktop/src/components/agentState/AgentStateOrb.tsx`, `desktop/src/components/agentState/orbEngine.ts`

**Verification**: AgentStateOrb and sidebar-history-host geometry tests passed. jsdom cannot prove real pixels (known-gaps MT-1).

### 2.3 Scrollbar tokens (T006)

**Plan specification**: Transparent track, scheme-matched thumb, `::-webkit-scrollbar` plus `scrollbar-color` / `scrollbar-width`.

**What happened**: Shared tokens in `desktop/src/styles/tokens.css`.

**Verification**: Token file assertions in `desktop/tests/sidebar-history-host.test.tsx`.

### 2.4 Testing and stabilization (T007-T008)

**What happened**: New host tests plus updates to Chat, Studio, Coding, Sidebar, and shell-phase6 tests. Deleted unused `CollapsibleHistoryAside.tsx`.

**Troubleshooting**:
- **Problem**: Full coverage run timed out `tests/video2x-adapter.test.ts` at 5s.
- **Classification**: ENV / load flake. Isolation re-run passed (69 passed, 1 skipped).
- **Resolution**: No product change.

**Verification**: See section 3.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Desktop ESLint (`npm run lint` in `desktop/`) | PASS |
| Desktop `tsc --noEmit` | PASS |
| Desktop vitest with coverage | PASS (205 files, 1890 passed, 1 skipped; lines 86.59%, branches 81.44%, functions 82.02%) |
| Remote CI | NOT RUN (non-final phase, commit-only) |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| jsdom cannot prove thinking-pill pixel containment | P2 | Recorded as v2.4.2 MT-1; Phase 7 operator zoom check |
| `video2x-adapter` 5s timeout under coverage load | Cosmetic | Accepted flake; isolation pass |

---

## 5. Plan Discrepancies

- History uses a portal into a sidebar host rather than a React context that stores `ReactNode` (avoids an update loop). Same observable slot.
- `CollapsibleHistoryAside.tsx` was deleted after nothing imported it, not kept as a unused wrapper.

---

## 6. Assumptions Made

- **Installer dirty files stay out of this commit**: Phase 6 T032 owns Setup/Reset/SANA installer UI still uncommitted on this branch.
- **Default expanded sidebar is the title-readable default**: users with `nexus.sidebar.compact=true` already stored keep the icon rail.

---

## 7. Testing Summary

### Automated Tests
- Desktop vitest + coverage: 1890 passed, 1 skipped, 0 failed. Aggregate lines 86.59% (threshold 80%).
- Focused host/orb/sidebar/chat/coding/studio/App tests passed after the compact-default fix.

### Manual Testing Performed
- None; human QA is deferred to the last phase.

### Manual Testing Still Needed
- human QA is deferred to the last phase

---

## 8. TODO Tracker

### Completed This Session
- [x] T001 Sidebar history host
- [x] T002 Move Chatbot FolderTree
- [x] T003 Move Image and Video history
- [x] T004 Remove Agents horizontal History band
- [x] T005 Uncrop thinking pill
- [x] T006 Scrollbar tokens
- [x] T007 Regression tests
- [x] T008 Stabilize and record local evidence

### Remaining (Not Started or Partially Done)
- [ ] Phase 2 Transcript Honesty (T009-T015)

### Out of Scope (Deferred)
- [ ] Installer VRAM / Unsloth / VS Code merge (Phase 6)
- [ ] v2.4.1 T097 push/PR

---

## 9. Summary and Next Steps

Phase 1 landed the sidebar history host, uncropped pending pill chrome, and shared scrollbar tokens. Next: `/implement v2.4.2-field-ui-history-and-generation` continues in-full at Phase 2 (auto-scroll, honest context, generic multi-delete, concise titles).
