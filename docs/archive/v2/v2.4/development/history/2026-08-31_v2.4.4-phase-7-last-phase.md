# Development Log: v2.4.4 Phase 7 - Architecture refactor, known gaps, and CI/CD

**Date**: 2026-08-31
**Operator**: Benjamin Dourthe
**Assisted by**: Claude Opus 5
**Objective**: Quote every last-phase duty as evidence, reconcile known gaps, compare the pipeline against the canonical contract, and stop at explicit push approval.
**Outcome**: Evidence file written with one section per duty. Local gate green. The CI/CD comparison did not pass, and publication waits on two operator decisions.

---

## 1. Starting State

- **Branch**: `feat/v2.4.1-field-reliability`
- **Starting tag/commit**: `887afe6d` (v2.4.4 Phase 6)
- **Environment**: Windows 11 Pro, Node 24.13.0, Python 3.12, vitest, pytest, ruff
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.4-field-chrome-restyle-sana-and-density.md`

---

## 2. Chronological Steps

### 2.1 Architecture refactor (T028)

**What happened**: The empty-directory scan returned two paths, both under `modules/coding/skills/catalog/`. Neither is obsolete: `git ls-files` does not list them, and both names appear in the test suite as deliberately-nonexistent paths passed to `SkillLoader`. They are created by running the suite and would return on the next run, so deleting them is churn, not cleanup. No deprecated files, no redundant directories, no structural complication introduced, and nothing moved, so no reference needed repair. The scan output is quoted in the evidence file.

### 2.2 Known-gaps reconciliation (T029)

**What happened**: A `## v2.4.4` section was added with 6 MT rows, 1 DF row, 1 WN row, and nothing closed. That last part is the substance of this sub-task. Five v2.4.3 rows (MT-2, MT-3, MT-4, MT-6, MT-7) each have a v2.4.4 successor stating the same limit against the new code - superseded, not resolved, because this cycle produced no rebuilt installer and the plan explicitly forbids closing a row because a fix exists. v2.4.3 MT-1 and MT-5 are carried forward untouched; this screenshot set does not cover them.

Two rows are genuinely new. **DF-1** records that Wan and SDXL were not re-smoked on the 0.36.0 Diffusers pin, which is the highest-blast-radius unobserved change in the cycle: the pin is shared by every Image and Video model and no automated test loads a real pipeline. **WN-1** records two pre-existing ruff F401 findings in files this plan did not touch, recorded rather than fixed.

The glob covered 31 files. Seven have Status `in-progress` or remaining Open Items; each was read against this cycle's observations and none had a row this cycle observed, so all are unchanged. The full disposition is written into the file.

### 2.3 Living docs architecture (T030)

**What happened**: The required shape is present - markdown source of truth, generated `html/` mirror, one atlas walkthrough, three technical companions, `docs/decisions/`, and the three living roots. `docs/testing/` and `docs/validation/` do not exist and were not created. Markdown and HTML did not disagree because this cycle changed neither; every change is code plus release-scoped evidence, so no regeneration was needed. All seven new or edited documents pass `validate_unicode_safety.py --strict`.

### 2.4 Git-tree hygiene (T031)

**What happened**: `check_release_preconditions.py --branches --repo-settings` quoted verbatim. Report only; no branch deleted. `working_tree=dirty` is the evidence file being written plus a benchmark-fixture timing rewrite inherited from a prior session, which was deliberately left unstaged because no line of it traces to this plan.

### 2.5 Terminal CI/CD reconciliation (T032)

**What happened**: Provider detected as GitHub Actions, 19 workflows. Thirteen contract fields pass. Three do not, and one of them is load-bearing for this plan's own final sub-task.

Every substantive workflow filters `pull_request` to `main`. PR 58 targets `develop`. On a develop-targeted pull request, `commitlint` is the only workflow that runs on the pull_request event - CI, CodeQL, coverage-diff, installer-tests, and shell-build all sit out. The push to the feature branch does trigger `ci.yml` through its push trigger, so the branch head is tested, but the **merge result** is not, and testing the merge result is the whole purpose of the integration gate the plan describes. This is v2.4.3 QG-5, and it is why the plan's step 7.9 phrasing ("wait until every required check reaches a terminal state") would otherwise be vacuous here.

The other two failures: `ci.yml` has 17 top-level jobs and no always-resolving aggregate required check, and it declares no top-level `permissions:` block so it inherits the repository default.

Per the plan, QG-5 stays proposed and not applied pending explicit approval. No workflow file was edited in this phase or in any earlier one. The smallest closing change is named with its cost and risk in the evidence file.

Cross-installer parity: the repository ships four installer workflows. No declarative parity gate was added and the real installers were not executed on their target operating systems. This is material precisely because this cycle changed the Diffusers pin those installers provision, so it is recorded as DF-1 rather than claimed as covered.

**Comparison verdict: not PASS.** A green run would not change that, because none of the three failing fields is exercised by a run.

### 2.6 Independent Goal-vs-codebase review (T033)

**What happened**: The Goal was re-read and the working tree inspected as an outside reader, against the code rather than the checklists. All ten Goal slices have a named artifact, verified by grep against the tree, and each is pinned by a test that fails against the previous behavior.

The honest finding is not a missing artifact. The Goal is written about "a 16 GB NVIDIA packaged studio", and six of the ten slices are proven only against jsdom, stubs, or distribution metadata. The Goal as literally stated is therefore not yet demonstrated, and for the restyle slice this is the third consecutive cycle in which that is true. That is recorded as MT-1 through MT-6 plus DF-1, all open, none closed - not as a pass.

### 2.7 Human/manual testing suggestions (T034)

**What happened**: Six operator items, each written as something a person actually does rather than a restatement of a code change, and each naming what a failure would look like. Item 4 carries the addition this cycle earned: after checking SANA, re-smoke Wan and one SDXL model, because the pin change is not SANA-local. Item 5 says what to conclude if the window still stalls while the caption keeps updating, since that distinguishes a runtime problem from compositor VRAM. No walkthrough was invented; nothing below was observed here.

### 2.8 Testing and stabilization (T035)

**What happened**: The complete local gate ran green - root lint, `tsc -b`, `nexus-check` (0 errors, baseline matched, exit 0), root vitest (5737 passed), desktop vitest (1963 passed), `tests/python` (292 passed), the installer pytest suite, and ruff across the installer and every file this cycle touched. No nexus-check finding falls in a file this cycle changed.

One environment repair is disclosed rather than buried: 33 sidecar tests were failing on this host before any edit, all from `better-sqlite3` compiled against a different Node ABI. `npm rebuild better-sqlite3` cleared all 33 and touched `node_modules` only. The green numbers above are not comparable to a run on an unrepaired host, which is why it is stated.

### 2.9 Publication and integration (T036)

**What happened**: Not performed. Six local commits plus this phase's commit exist on the branch. No push, no pull request, and no remote CI run has occurred at any point in this plan. Two decisions were put to the operator together rather than one per turn: whether to push at all (which updates PR 58 and would carry unpushed v2.4.3 T040 work alongside v2.4.4), and whether to apply QG-5 first so the integration pull request actually validates the merge result.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Root ESLint | PASS |
| Root `tsc -b` | PASS |
| `npm run check` (nexus-check) | 0 error, 54 warning, baseline 56 matched / 0 stale, exit 0 |
| Root vitest | 529 files / 5737 passed, 12 skipped |
| Desktop vitest | 210 files / 1963 passed, 1 skipped |
| pytest `tests/python` | 292 passed |
| pytest `scripts/installer/tests` | PASS |
| ruff (installer + files touched) | PASS |
| Unicode safety on new docs | 7 files, 0 failures |
| Remote CI | NOT RUN - waiting on explicit approval |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Six packaged operator observations outstanding | P1/P2 | v2.4.4 MT-1 through MT-6 |
| Wan and SDXL not re-smoked on the 0.36.0 pin | P1 | v2.4.4 DF-1; highest blast radius in the cycle |
| Develop-targeted PRs run only commitlint | P1 | QG-5; proposed, not applied, awaiting approval |
| `ci.yml` has no aggregate required check | P2 | Recorded in the CI/CD comparison |
| `ci.yml` declares no top-level permissions | P2 | Recorded in the CI/CD comparison |
| Two pre-existing ruff F401 findings | P3 | v2.4.4 WN-1 |

---

## 5. Plan Discrepancies

- The plan allowed the CI/CD comparison to conclude PASS only when every required field has observable evidence. It does not, so it is recorded as not PASS rather than softened.
- The plan's step 7.9 assumes required checks will run against the merge result. On a develop-targeted PR they currently do not, which is why QG-5 was raised as a decision rather than left as a note.

---

## 6. Assumptions Made

- One push carries v2.4.3 and v2.4.4 together unless the operator splits them, since both are unpushed on the same branch.
- An approved push updates PR 58 rather than opening a second pull request.

---

## 7. Testing Summary

### Automated Tests
- Full local gate green; see section 3.

### Manual Testing Performed
- None.

### Manual Testing Still Needed
- Six operator items in the evidence file, all requiring a rebuilt installer carrying the 0.36.0 pin.

---

## 8. CI Impact

No pipeline file was edited in this phase or in phases 1 through 6. The QG-5 change is proposed with its cost and risk and awaits approval. No remote run was started.
