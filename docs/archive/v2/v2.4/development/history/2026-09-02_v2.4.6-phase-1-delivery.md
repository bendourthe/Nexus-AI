# Development Log: v2.4.6 Phase 1 - Packaged delivery and installer window

**Date**: 2026-09-02
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: A rebuilt installer installs this tree's desktop, overwrites the previous app, shows a matching fingerprint, and opens maximized.
**Outcome**: Staging fails closed on a stale or version-mismatched NSIS bundle; Settings shows the installer payload hash; the wizard's first show is maximized.

---

## 1. Starting State

- **Branch**: `develop` at `3fb8a04f` (v2.4.5 integration). Feature branch cut as `feat/v2.4.6-field-delivery-density-and-session-identity`.
- **Environment**: Windows 11, Python 3.12, pytest, ruff, Node 20, vitest
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.6-field-delivery-density-and-session-identity.md`
- **Field evidence**: packaged post-v2.4.5 screenshots showed a new wizard Review page with a pre-density desktop. `build-windows.ps1` Copy-Item'd whatever NSIS already sat on disk when package.json was still 2.4.1.

---

## 2. Chronological Steps

### 2.1 Fail closed on a stale desktop payload (T001)

**What happened**: The freeze step used to Copy-Item the first matching `Nexus AI Studio_${Version}_x64-setup.exe` and write `manifest.json` in PowerShell. Version equality was necessary and not sufficient: last week's binary still matched 2.4.1.

`nexus_installer.engine.desktop_payload.stage_desktop_payload` now copies, hashes, and writes the manifest. It refuses a missing file, a filename that does not encode the product version (`2.4.1` does not match `2.4.10`), and a bundle whose mtime is older than `desktop/src`, `desktop/sidecar/src`, `desktop/src-tauri/src`, and the pin files. `build-windows.ps1` calls `stage-desktop-payload.py` and exits 1 on that failure. It does not invoke `npm run build:shell` (too long for this gate); the error tells the operator to run it.

A freshly copied stale NSIS whose LastWriteTime is newer than source can still pass the mtime check. That is recorded as WN-1. The field case (old tauri output left in `desktop/src-tauri/target/...`) is the mtime miss the tests pin.

### 2.2 Desktop fingerprint (T002)

**What happened**: After a successful desktop install, the provisioner writes `~/.nexus/desktop-payload.json` (version + sha256) from the embedded manifest. A local `--desktop-bundle` override writes `version: local-override` and does not consult the embedded payload (so the override test still proves the payload is skipped). Settings shows `Desktop payload {version} ({12-char hash})` or `Desktop payload unknown`. Sidecar method `runtime.desktopPayload` reads the file; a missing file is null, never a guessed 2.4.1.

### 2.3 First presentation is maximized (T003)

**What happened**: `present_installer_window` calls `showMaximized`. `main.py` no longer calls `window.show()`. `show_and_raise` still uses `showNormal` for tray reattach.

### 2.4 Testing (T004)

Installer pytest for staging, identity, maximize helper, and packaging script shape. Desktop vitest for Settings fingerprint and sidecar dispatch. `npm rebuild better-sqlite3` was required before Node tests after an earlier Electron ABI rebuild (v2.4.5 WN).

---

## 3. Verification Gate

| Check | Result |
|---|---|
| pytest `test_desktop_payload.py` + `test_desktop_provisioner.py` | passed (one skip: Windows fixture integration) |
| desktop_payload / desktop_provisioner line coverage | 89% / 88% |
| ruff check + format | passed |
| vitest `desktopPayload.test.ts` | 6 passed |
| vitest SettingsPage + sidecar-handlers | 30 passed after `npm rebuild better-sqlite3` |
| Remote CI | NOT RUN (non-final phase is commit-only) |

---

## 4. Known Issues

- WN-1: mtime freshness cannot catch a stale NSIS that was copied (new timestamp) after source last changed.
- MT-1: packaged installer first-show maximize and Settings fingerprint are not observed on a frozen NexusSetup.exe.

---

## 5. Plan Discrepancies

- T004's plan path named `test_desktop_provisioner.py`; staging tests live in `test_desktop_payload.py` and provisioner tests were extended. Both run in this phase.
- `npm run build:shell` is documented as a prerequisite, not invoked from `build-windows.ps1`.

---

## 6. Assumptions Made

- Package metadata stays 2.4.1 until `/update release`.
- NSIS `/S` overwrites a previous same-product install.
- Cursor session model already matches the plan's frontier cell (`cursor-grok-4.6`); no picker switch.

---

## 7. Testing Summary

Automated only. Human QA of maximize and the Settings fingerprint is deferred to Phase 8.

---

## 8. TODO Tracker

- T001-T005 done
- Phase 2 is next (Setup compactness)

---

## 9. Summary and Next Steps

Phase 1 makes a rebuilt wizard fail if it would embed last week's desktop, and gives the operator a hash they can read in Settings. Next: Phase 2 Setup two-column prerequisites, refresh icon, one-line GPU.
