# Development Log: v2.4.6 Phase 4 - VS Code owned agentic models

**Date**: 2026-09-03
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: The Nexus VS Code extension selects among installer-or-Settings-owned agentic models and keeps Hub agentic capability.
**Outcome**: Status bar, `nexus.coding.selectModel`, and `/model` list the AD-13 allowlist only. Empty snapshot and leftover installed ids are absent. Switching model keeps tool dispatch and ConfirmationGate.

---

## 1. Starting State

- **Branch**: `feat/v2.4.6-field-delivery-density-and-session-identity` at Phase 3 commit `71cdec11`.
- **Environment**: Windows 11, Node 20, vitest, tsc
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.6-field-delivery-density-and-session-identity.md`
- **Inventory (4.1)**: No `nexus.coding.selectModel`. `/model` called `client.listModels()` (every Ollama tag). AgentLoop, SkillCatalog, HookBus, plan mode, ConfirmationGate, and terminal already load in the extension-only host. Remaining gap is allowlist + picker + default, not a new agent runtime.

---

## 2. Chronological Steps

### 2.1 vscode-free allowlist (T018)

**What happened**: `core/registry/ownedSelection.ts` parses `~/.nexus/selected-models.json` and fail-closes to an empty owned set when the snapshot is missing. `core/registry/ownedAgentic.ts` intersects catalog agentic (`task === "agentic"` or `agentic === true`), owned ids, and InstalledProbe presence. Default is `recommendedByTask.agentic` when that id is owned (16 GB: `gemma-4-12b-it-gguf`). `modules/coding/config/settings.ts` re-exports the enum helpers. Plan path `src/config/settings.ts` does not exist.

### 2.2 Picker and `/model` (T019)

**What happened**: Command `nexus.coding.selectModel`, status bar, and Chat `/model` share the allowlist. Registered in extension-only, proxy (local snapshot, not the daemon placeholder), and safe-mode `DECLARED_COMMAND_IDS`. Empty owned set posts the Settings message. Unknown id posts a structured Settings error.

### 2.3 Hub loop stays on switch (T020)

**What happened**: `AgentLoop.setModelName` and `StreamingPipeline.setModelName` follow `nexus.llm.modelName`. Skills, hooks, ToolRegistry, ConfirmationGate, and pathGuard stay on the same loop. No network LLM providers.

### 2.4 Tests (T021)

**What happened**: Fixture leftover-coder is installed but unowned and absent from the enum. Empty snapshot is empty, not `ollama list`. Tool dispatch and ConfirmationGate remain after switching `gemma-4-12b-it-gguf` to `qwen2.5-coder:14b`. Full vitest: 5781 passed, 12 skipped. `tsc -b` clean.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| eslint on changed src/modules | passed |
| prettier on changed TS | passed |
| tsc -b | passed |
| vitest run (unit + integration) | 5781 passed, 12 skipped |
| Remote CI | NOT RUN (non-final phase is commit-only) |

---

## 4. Known Issues

- MT-4: packaged VSIX picker on VS Code 1.136 is not observed.
- WN-2: Electron ABI for 1.136 still inferred (42.8.1). After a VSIX rebuild, `npm rebuild better-sqlite3` before Node tests (v2.4.5 Electron ABI 146 vs Node 137).

---

## Plan delta

**Incomplete** (non-blocking). The plan named `src/config/settings.ts` and `src/extension.ts`; the coding settings live at `modules/coding/config/settings.ts` and the picker at `src/activation/ownedAgenticPicker.ts`. Tests live under `tests/unit/core/registry`, `tests/unit/activation`, and `tests/unit/tools`, not `tests/unit/src`. Inventory showed Hub skills/hooks/commands already load, so this phase did not start a second agent runtime. Desktop `ownedIdSet(null)` pass-through is unchanged; Phase 7 must reuse this fail-closed helper on Chat/Agents/Image/Video.

---

## 5. Assumptions Made

- Cursor session model already matches the plan's frontier cell (`cursor-grok-4.6`).
- Package metadata stays 2.4.1 until `/update release`.

---

## 6. Testing Summary

Automated only. Human QA of the VSIX picker is deferred to Phase 8.

---

## 7. TODO Tracker

- T018-T022 done
- Phase 5 is next (Sessions History chrome)

---

## 8. Summary and Next Steps

Phase 4 makes the VS Code extension pick only models this install owns. Next: Phase 5 always shows Sessions History and uses Session copy except the Chatbot tab.
