# Development Log: v2.4.3 Phase 3 - Thinking Pill Geometry

**Date**: 2026-08-31
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: The pending pill keeps a constant width across Thinking / Searching / Working / Solving, and the glow is inset so it is not cropped by the transcript edge.
**Outcome**: Min-width is driven by `Searching...` (longest caption) plus orb and padding. Left inset is 12 px. Overflow stays visible on the pill host and the chat pending row.

---

## 1. Starting State

- **Branch**: `feat/v2.4.1-field-reliability`
- **Starting tag/commit**: `357ce3e` (v2.4.3 Phase 2)
- **Environment**: Windows 10, Node 20, vitest, tsc
- **Prior session reference**: `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-2-history-chrome.md`
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.3-field-density-identity-and-runtime.md`

---

## 2. Chronological Steps

### 2.1 Fixed width and inset (T013)

**What happened**: `longestPendingCaption()` and `pendingPillMinWidthExpr(orbPx)` live in `captionRotator.ts`. Rotating pills set `minWidth` to that expression, `marginLeft` to `PENDING_PILL_INSET_PX` (12), and keep `overflow: visible`. The caption span also uses the longest caption in `ch` so the word column does not shrink. `PendingMessage` adds the same inset as left padding so the glow is not flush to the pane.

### 2.2 Tests (T014-T015)

**Verification**: See section 3.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| ESLint on touched desktop files | PASS |
| `tsc --noEmit` | PASS |
| Focused vitest (2 files, 23 tests) | PASS |
| Remote CI | NOT RUN (non-final phase, commit-only) |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| jsdom cannot prove glow pixels inside a packaged transcript | P2 | v2.4.3 MT-3; v2.4.2 MT-1 unchanged |
| v2.4.3 MT-1 packaged installer screenshot | P2 | unchanged |
| v2.4.3 MT-2 hairline / dialog centering | P2 | unchanged |

---

## 5. Plan Discrepancies

- None. Width driver is `Searching...` as specified.

---

## 6. Assumptions Made

- A 12 px left inset is enough to keep the 14-16 px glow inside the transcript padding.

---

## 7. Testing Summary

### Automated Tests
- Longest caption is `Searching...`.
- Pill `minWidth` contains that length in `ch` and matches `pendingPillMinWidthExpr(ORB_SIZE_BUBBLE)`.
- `marginLeft` is 12 px; host overflow is visible.
- Chat pending row padding includes the inset; studio hero pending is unchanged.

### Manual Testing Performed
- None; human QA is deferred to the last phase.

### Manual Testing Still Needed
- human QA is deferred to the last phase

---

## 8. TODO Tracker

### Completed This Session
- [x] T013-T015

### Remaining
- [ ] Phase 4 Settings compact cards (T016-T019)

### Out of Scope
- [ ] Push / PR (final phase only)

---

## 9. Summary and Next Steps

Phase 3 pins thinking-pill width and insets the glow. Next: Phase 4 Settings Models compact cards.
