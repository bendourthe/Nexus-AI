# Development Log: v2.4.3 Phase 1 - Installer Two-Column Layout and Unsloth Lock

**Date**: 2026-08-31
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: Configuration and Review use two columns; Unsloth cannot be selected when Incompatible; this 16 GB NVIDIA host shows Compatible after detection.
**Outcome**: Components sit left of Features. Review facts sit left of the model list. GPU is whole GB. Estimated installation time is the time label. Unsloth refreshes on showEvent and is disabled when Incompatible.

---

## 1. Starting State

- **Branch**: `feat/v2.4.1-field-reliability`
- **Starting tag/commit**: `83a7a90` (v2.4.2 Phase 7 last-phase evidence)
- **Environment**: Windows 10, Python 3.12, uv, pytest, PyQt5
- **Prior session reference**: `docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.2-phase-7-last-phase.md`
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.3-field-density-identity-and-runtime.md`

Context: Packaged v2.4.2 still left Unsloth tickable under an Incompatible badge (badge computed at construct with `vram_mb=0`) and stacked Configuration/Review in one column.

---

## 2. Chronological Steps

### 2.1 Configuration two columns (T001)

**Plan specification**: Components left, Features right. Ollama URL, VS Code, and the rest full width below. Narrow window stacks columns.

**What happened**: `QGridLayout` hosts `config-components-column` and `config-features-column`. `resizeEvent` uses the event width and restacks Features under Components below 560 px.

### 2.2 Review two columns and copy (T002, T004)

**Plan specification**: Facts left (path, components, GPU, disk, Estimated installation time), models right. GPU via `display_vram_gb`. Zero VRAM omits a GB suffix.

**What happened**: Two card labels (`review-facts-column`, `review-models-column`). 16384 MB prints 16 GB VRAM. Empty selection still reads "none selected" on the right. `_summary_text()` concatenates both columns for older tests.

### 2.3 Unsloth lock and refresh (T003)

**Plan specification**: Refresh on `showEvent`. Host-ok is NVIDIA plus `display_vram_gb >= 16`. Incompatible disables and clears the checkbox. No opt-in warning.

**What happened**: Construct with empty GPU stays Incompatible and disabled. Applying NVIDIA 16384 then `showEvent` becomes Compatible and enabled. 8 GB NVIDIA cannot check. Reverses the v2.4.2 opt-in warning.

### 2.4 Testing (T005-T006)

**Verification**: See section 3.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| `uv run ruff check` on touched installer files | PASS |
| Focused page tests | PASS (16 Configuration/Review) |
| Full installer pytest (`uv run pytest tests --cov=src`) | PASS (3 skipped), 85% lines |
| `configuration.py` / `review.py` line coverage | 94% / 84% (>= 80%) |
| Remote CI | NOT RUN (non-final phase, commit-only) |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Packaged wizard two-column + Unsloth lock screenshot | P2 | v2.4.3 MT-1; last-phase operator item |
| v2.4.2 MT-1 through MT-6 | P2 | unchanged; several are now observed packaged failures this plan addresses later |

---

## 5. Plan Discrepancies

- None. Event-size restack was required so tests (and real resizes) do not read a stale `self.width()`.

---

## 6. Assumptions Made

- GPU detection still finishes on Setup before Configuration is shown, so `showEvent` is the refresh point.
- 15360 MiB remains Compatible via `display_vram_gb` 16 GB-class band.

---

## 7. Testing Summary

### Automated Tests
- Full installer pytest: passed, 3 skipped.
- New/updated: two-column object names, Unsloth lock, showEvent refresh, Review 16 GB + time label, zero-VRAM suffix omitted, narrow restack.

### Manual Testing Performed
- None; human QA is deferred to the last phase.

### Manual Testing Still Needed
- human QA is deferred to the last phase

---

## 8. TODO Tracker

### Completed This Session
- [x] T001 Configuration two columns
- [x] T002 Review two columns and time label
- [x] T003 Unsloth lock and showEvent refresh
- [x] T004 Review GPU as whole GB
- [x] T005-T006 Tests and local evidence

### Remaining (Not Started or Partially Done)
- [ ] Phase 2 history chrome, new-chat select, titles, delete overlay (T007-T012)

### Out of Scope (Deferred)
- [ ] Push / PR (final phase only)
- [ ] `/update release` until green integration

---

## 9. Summary and Next Steps

Phase 1 locked Unsloth to Compatible hosts and split Configuration/Review into two columns. Next: `/implement v2.4.3-field-density-identity-and-runtime in-full` continues at Phase 2.
