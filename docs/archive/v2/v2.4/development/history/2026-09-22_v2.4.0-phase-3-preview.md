# v2.4.0 Phase 3 session history

**Plan**: [v2.4.0-adoption-unsloth-qwen38-gaussian-splatting.md](../../plans/v2.4.0-adoption-unsloth-qwen38-gaussian-splatting.md)
**Phase**: 3, Image Studio 3D Preview Experience
**Date**: 2026-09-22

## What landed

- Finished Image Studio rows gain a "3D preview" button beside Download. The 2D download stays.
- `SplatPreviewPanel` shows the honesty sentence, keeps generate disabled with the reason "generator not wired", and opens either an injected local reader or a user-picked `.splat` / `.ply` file. Remote paths are rejected before decode.
- `core/image/SplatProvenance.ts` stores a hash, format, count, and optional backend fields beside the file. It does not write splat bytes into a PNG `tEXt` chunk. Download and screenshot names differ from the source PNG and from each other.

## Plan delta

Incomplete, and not blocking. The phase asked for a typed sidecar file client. No image-file read IPC exists, so the panel reads a picked `File` and tests pass `OpenLocalSplat`. Phase 4 can supply that reader from the runtime without changing the panel props. Generate stays disabled on purpose until that phase.

## CI impact

New paths: `desktop/tests/SplatPreviewPanel.test.tsx`, one case in `desktop/tests/ImageStudioPage.test.tsx`, and `tests/unit/core/image/SplatProvenance.test.ts`. Desktop Vitest and root Vitest already include those globs. `ci.yml` and `shell-build.yml` already run them. No new command, dependency, environment variable, or pipeline file. No remote CI was started from this phase.

## Gitignore

0 patterns added.

## Known gaps

0 entries added.

## Docs cleanup

Audit only. No scratch document was created, and no file was moved.

## Functional smoke

- Input: a finished Image Studio message, then "3D preview", then an injected local `C:/studio/preview.splat`. Expected: the honesty sentence, the Download button still present, a `.splat` name, and no `fetch`. Observed: `ImageStudioPage.test.tsx` matched that on 2026-09-22.
- Input: a picked `room.splat` with no sidecar reader. Expected: a provenance name and no `fetch`. Observed: `SplatPreviewPanel` test "reads a picked local file".
- Comparison: expected and observed matched. A real GPU canvas was not observed.

## Verification

`npx vitest run --config vitest.config.ts tests/SplatPreviewPanel.test.tsx` passed: 5 tests. The new Image Studio case passed. `npx vitest run --config configs/vitest.config.ts tests/unit/core/image/SplatProvenance.test.ts` passed: 2 tests. Desktop `tsc --noEmit` exited 0.
