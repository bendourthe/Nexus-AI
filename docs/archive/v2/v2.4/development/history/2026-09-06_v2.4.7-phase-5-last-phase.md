# Development Log: v2.4.7 Phase 5 - Evidence, known gaps, CI/CD, integration, installer rebuild

**Date**: 2026-09-06
**Operator**: Benjamin Dourthe
**Assisted by**: Claude Opus 5
**Objective**: Quote every last-phase duty, reconcile gaps, integrate, and rebuild the installer for field testing.
**Outcome**: Local gate green with zero failures across every suite. Publication and rebuild follow this record.

---

## 1. Starting State

- **Branch**: `feat/v2.4.7-installer-wizard-density-and-scope`
- **Starting commit**: `04b7ef12` (v2.4.7 Phase 4)
- **Environment**: Windows 11 Pro, Node 24.13.0, Python 3.12
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.7-installer-wizard-density-and-scope.md`

---

## 2. Chronological Steps

### 2.1 Architecture refactor (T020)

**What happened**: The empty-directory scan returned the same two `SkillLoader` test byproducts as every cycle since v2.4.4; neither is tracked and both are recreated by the next run.

One real finding: `known-gaps.md` carried a stray empty `## v2.4.5` heading immediately above the real one, left by an earlier cycle. A duplicate heading in the ledger the next plan reads is a navigation defect, so it was removed. Two untracked files under `docs/v2/v2.5/` from a prior session were deliberately left untracked rather than swept into this plan's commits.

### 2.2 Known-gaps reconciliation (T021)

**What happened**: A `## v2.4.7` section with 1 MT, 2 DF and 1 resolved BG row.

BG-1 records the defect and names its origin plainly: this project introduced it in v2.4.5, and v2.4.5's own tests missed it because every fixture made the report cover exactly the selection. The row also names the property that prevents a third occurrence, because "we fixed it" is not a guard - "every fixture uses a catalog wider than the selection" is.

The two DF rows record deliberate omissions rather than oversights, each with the reason it was left: the Ollama "will be installed" wording is imprecise but not wrong, and colouring category labels carries a palette decision with no correctness content.

Every earlier row carries forward, for the reason that has held since v2.4.4: no installer built since then has been successfully field-tested.

### 2.3 Living docs and git hygiene (T022, T023)

**What happened**: Required living tree present and unchanged. The installer handbook covers provisioning, which this cycle did not change - the engine contract (`components_to_install` as the executed step list) is identical - so no handbook edit was warranted. `check_release_preconditions.py` quoted verbatim, report only.

### 2.4 CI/CD conformance re-check (T024)

**What happened**: A re-check, not an authoring pass. v2.4.4's QG-1 fix is confirmed still in force, so this branch's develop-targeted pull request runs the full merge-result gate. `installer-tests.yml` already scopes `scripts/installer/**`, covering every file and both new test modules. Two v2.4.4 findings carry forward unchanged. No pipeline file was edited.

### 2.5 Goal-vs-codebase review (T025)

**What happened**: All eleven Goal slices have a named artifact verified against the tree, each pinned by a test that fails against the previous behavior.

The limit is unchanged from every cycle since v2.4.4: the Goal describes a wizard a person operates, and Qt geometry and rich-text layout do not resolve headlessly. Recorded as MT-1, not counted as a pass.

### 2.6 Human testing suggestions (T026)

**What happened**: Eleven operator items; five are this cycle's and six are inherited. Item 2 deliberately asks the operator to confirm the guard still *refuses* an oversized selection, and says why it matters more than item 1: a relaxation that always says yes would pass item 1 perfectly.

### 2.7 Full-suite testing (T027)

**What happened**: Zero failures across every suite - the first clean first-attempt root run in the 2.4 series. Root vitest 5782 passed, desktop vitest 1979 passed, `tests/python` 292 passed, installer suite 100%, and root lint, `tsc -b`, `npm run check` and installer ruff all clean.

`npm rebuild better-sqlite3` was run up front rather than after a confusing failure, applying the v2.4.5 lesson: `build-vsix.ps1` leaves the native module built for Electron (ABI 146) while Node 24 needs 137.

One self-inflicted failure during Phase 4 is recorded rather than smoothed over: test helpers returned `ReviewPage` objects, and the suite died at teardown with a COM `RPC_E_DISCONNECTED` - the identical trap v2.4.5 Phase 2 hit by storing a `QWidget` on a card record. The helper now returns extracted text.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Root ESLint | PASS |
| Root `tsc -b` | PASS |
| Desktop ESLint + `tsc --noEmit` | PASS |
| `npm run check` | 0 error, 54 warning, baseline 56 matched / 0 stale |
| Root vitest | 534 files / 5782 passed, 12 skipped |
| Desktop vitest | 212 files / 1979 passed, 1 skipped |
| pytest `tests/python` | 292 passed |
| pytest `scripts/installer/tests` | 100%, 0 failed, no fatal exception |
| ruff `scripts/installer` | All checks passed |
| Unicode safety on new docs | PASS |
| Remote CI | Pending publication approval |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| The reworked wizard is unobserved in the packaged window | P1 | v2.4.7 MT-1 |
| Every v2.4.4 through v2.4.6 operator item still unobserved | P1 | Carried; this rebuild is the first opportunity |
| Wan and SDXL not re-smoked on the 0.36.0 pin | P1 | v2.4.4 DF-1; widest blast radius |
| Already-installed Ollama reads as "will be installed" | P3 | v2.4.7 DF-1 |
| Category labels are not coloured chips | P3 | v2.4.7 DF-2 |
| `ci.yml` lacks an aggregate required check and top-level permissions | P2 | Carried from v2.4.4 |

---

## 5. Plan Discrepancies

- The plan did not anticipate the duplicate known-gaps heading. It was found by the architecture scan and fixed, since it lives in the file the next plan ingests.

---

## 6. Assumptions Made

- The two untracked `docs/v2/v2.5/` files belong to a separate effort and are not this plan's to commit.

---

## 7. Testing Summary

### Automated Tests
- Full local gate green, zero failures; see section 3.

### Manual Testing Performed
- None on the packaged wizard.

### Manual Testing Still Needed
- Eleven operator items in the evidence file.

---

## 8. CI Impact

No pipeline file edited in this phase or any earlier one. `installer-tests.yml` already collects both new test modules. No new dependency, job, or artifact.
