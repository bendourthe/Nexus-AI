# Development Log: v2.4.6 Phase 7 - Four-tab runtime chrome

**Date**: 2026-09-03
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: Chat, Agents, Image, and Video match the operator chrome: allowlisted pickers, gutters, tokens, order, Stop, captions, Advanced row, image default, GPU busy.
**Outcome**: Pickers fail closed on installer-or-Settings ownership, recommend order, persistent assistant tokens, Stop while in flight, Creating/Generating captions, Advanced on the context row, RealVis default when owned, and GPU Busy while a scheduler job is active.

---

## 1. Starting State

- **Branch**: `feat/v2.4.6-field-delivery-density-and-session-identity` at Phase 6 commit `8ece62fd`.
- **Environment**: Windows 11, Node 20, desktop vitest, eslint, tsc
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.6-field-delivery-density-and-session-identity.md`
- **Inventory**: `ownedIdSet(null)` still passed through every installed model. QuickModelSwitcher sorted with VRAM family collapse. Assistant tokens could drop on persist. Studio captions still used Shaping. GPU widget idled at 0% while Chat/Agents ran outside the scheduler. Image default did not pin `realvisxl-v5`.

---

## 2. Chronological Steps

### 2.1 Gutters and assistant tokens (T032, T033)

**What happened**: `MessageList` already used one list `paddingInline` of `TRANSCRIPT_GUTTER_PX` (12). No extra pill margin. `persistMessage` forwards `requestUsage` and `messageUsage`. Assistant complete prefers provider `outputTokens`, else `estimatedMessageUsage`. Stop increments a turn epoch so a late reply cannot overwrite the stopped bubble. ChatPage tests assert the assistant `message-tokens-` node.

### 2.2 Allowlist then recommend order (T034, T051)

**What happened**: `ownedIdSet(null)` is an empty set. `installedForTask` / `installedModelsForType` never treat a missing snapshot as pass-through. `recommendOrderForTask` is recommended id, then `orderedIds`, then `downloadedSinceInstall`. QuickModelSwitcher filters owned intersect catalog tab and sorts by that order. `hostVramGB` is accepted but unused so a missing VRAM reading cannot shuffle owned rows. Chat/Agents/Image/Video always pass `ownedIds={ownedIdSet(selection)}`. Image/Video ipc-unavailable fallbacks own the sentinel id through `snapshotForOwnedIds` so FALLBACK_MODEL cannot appear while the sidecar is up.

### 2.3 Stop, captions, Advanced, Image default (T035-T038)

**What happened**: `MediaComposer` and `CodingInput` replace Send with Stop while streaming (`media-composer-stop`, `coding-input-stop`). Image/Video cancel the queue job and patch pending to Stopped. `mapping.ts` image/video labels are Creating and Generating; orb `state` remains `shaping` (animation enum). Advanced lives in `ComposerContextRow` `trailing` (`composer-advanced-slot`). Image default uses `recommendedByTask.image`. A 16 GB snapshot that owns both Juggernaut and RealVis selects `realvisxl-v5` and lists RealVis first.

Juggernaut is catalog id `juggernaut-xl-v9`. The wizard can auto-tick it because those weights were already on disk. That is not a ghost picker row. It appears in Chat/Agents/Image/Video only when it is in `orderedIds` or `downloadedSinceInstall`.

### 2.4 GPU busy (T039)

**What happened**: `queryHostGpu` retries `C:\Windows\System32\nvidia-smi.exe` on Windows after PATH `nvidia-smi` fails. `createLiveTelemetryStream` polls the scheduler snapshot. `idle` is false whenever `active` is non-null, including utilization 0%. Chat and Agents `sendMessage` wrap work in `scheduler.enqueue` (`jobType: "tokens"`). Composer Context 0% on an empty session is unchanged.

### 2.5 Tests (T040)

**What happened**: `owned-picker-allowlist.test.tsx` hides leftover LLM/image ids from all four pickers, pins Gemma 4 12B before gpt-oss on Agents, and still shows unowned catalog cards in Settings. ImageStudioPage pins RealVis default. gpu-runtime, live-telemetry, telemetryStream, and GpuStatusFooter pin System32 fallback and Busy at 0%. Fail-closed fixtures add `lastSelection` wherever a picker must list a model.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| desktop eslint | passed (max-warnings 0) |
| prettier on changed TS | applied |
| desktop tsc --noEmit | passed |
| desktop vitest with coverage | 1979 passed, 1 skipped, 212 files, lines 86.89% |
| Functional smoke (jsdom four-tab leftover exclusion, RealVis default, footer not Idle at 0% with idle false) | observed in vitest |
| Packaged desktop four-tab chrome | not observed (MT-7) |
| Remote CI | NOT RUN (non-final phase is commit-only) |

---

## 4. Known Issues

- MT-7: packaged four-tab chrome is not observed (needs Phase 8 installer rebuild plus desktop launch).
- v2.4.4 MT-1..MT-6 and v2.4.5 MT-1 remain open until that rebuild.
- `gpuRuntime.ts` production `execFile` path stays thinly covered; the System32 fallback is unit-tested through the exec seam.

---

## Plan delta

**Incomplete** (non-blocking). Image empty-session default ignores an owned localStorage favorite so a leftover favorite cannot beat `recommendedByTask.image`. The plan text allowed a favorite to win when it exists; AD-13 leftover exclusion is the stronger rule. Phase 8 should not treat a click-to-restore owned favorite as a Goal miss. Video Lab still exposes host VRAM as `vramGB`, not `hostVramGB`. `MessageList` gutters were already equal, so T032 needed no source edit. Consequence for Phase 8: human items must use a rebuilt desktop payload; do not close MT-7 on these unit tests.

---

## 5. Assumptions Made

- Cursor session model already matches the plan's frontier cell (`cursor-grok-4.6`).
- Package metadata stays 2.4.1 until `/update release`.
- `tests/fixtures/memory-tier-benchmark-results/2026-05-26/results.json` is unrelated dirty state and is not part of this phase.

---

## 6. Testing Summary

Automated only. Human QA of four-tab chrome, leftover absence, RealVis default, Stop, captions, and GPU busy is deferred to Phase 8.

---

## 7. TODO Tracker

- T032-T041 and T051 done
- Next: Phase 8 architecture refactor, known-gaps, CI/CD, publication after approval, installer rebuild with a fresh `desktop` `npm run build:shell`

---

## 8. Next Actions

`/implement v2.4.6-field-delivery-density-and-session-identity in-full` from Phase 8.
