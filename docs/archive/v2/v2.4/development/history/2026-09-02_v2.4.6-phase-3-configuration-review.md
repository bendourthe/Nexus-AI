# Development Log: v2.4.6 Phase 3 - Configuration, VS Code host, and Review groups

**Date**: 2026-09-02
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: Configuration defaults match a 16 GB Compatible host, and Review is categorized and dense.
**Outcome**: Unsloth starts checked on Compatible NVIDIA 16 GB+; VS Code 1.136 is a Features checkbox default-on; Gemma sampling and Video2X notes are gone; Review groups models by catalog section.

---

## 1. Starting State

- **Branch**: `feat/v2.4.6-field-delivery-density-and-session-identity` at Phase 2 commit `2637eb6e`.
- **Environment**: Windows 11, Python 3.12, pytest, ruff, operator host VS Code 1.136.0
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.6-field-delivery-density-and-session-identity.md`
- **Field evidence**: screenshot 2 Unsloth Compatible but unchecked; VS Code 1.136 disabled; Gemma Temperature/Top-P/Top-K; Video2X note; screenshot 4 uncategorized two-column model list.

---

## 2. Chronological Steps

### 2.1 Unsloth Compatible is selected (T012)

**What happened**: `_apply_unsloth_host_lock` checks Unsloth and sets `install_unsloth` when the host is Compatible and the user has not touched the box. `_on_unsloth` sets `_user_touched_unsloth` so an uncheck survives `showEvent`. Incompatible still forces unchecked and locked. Help text drops "Off by default" on Compatible hosts.

### 2.2 VS Code Features checkbox and 1.136 (T013)

**What happened**: `SUPPORTED_VSCODE_MAX_EXCLUSIVE` is `1.137.0`. `SUPPORTED_VSCODE_MINORS` includes `(1, 136)`. Copy uses `1.134, 1.135, or 1.136`. The compact extension control moved into Configuration Features (checkbox plus detection, no nested card). Replace-copy stays on that one checkbox. The separate VS Code Extension Settings segment is gone. `engines.vscode` stays `^1.134.0` (vsce cannot take a compound range). Electron rebuild pin stays 42.8.1 (WN-2). Insiders/Cursor/Windsurf stay unsupported.

### 2.3 Remove Gemma preview and Video2X; categorize Review (T014, T015)

**What happened**: Configuration no longer renders Model/Temperature/Top-P/Top-K or the Optional video enhancement block. Review groups `selected_model_ids` by catalog tab order (Embeddings, Chat, Agentic, Image, Video, Audio, Document), omits empty groups, and keeps downloaded/pending marks. Unknown ids land under Other. Headless `--model` still renders by name. Facts card aligns to the top and uses a Maximum vertical size policy so it hugs content.

### 2.4 Testing (T016)

**What happened**: Qt and review tests cover Unsloth default-on, user uncheck, 1.136 enabled, absent Gemma/Video2X strings, Features parent for the VS Code checkbox, and Image heading containing `juggernaut-xl-v9` without mixing `gemma4:e4b`.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| ruff check + format on installer `src`/`tests` | passed |
| pytest configuration, review, extension, vscode page, packaging | passed |
| full installer pytest | passed (3 skipped) |
| Remote CI | NOT RUN (non-final phase is commit-only) |

---

## 4. Known Issues

- MT-3: packaged Configuration/Review and a VSIX load on VS Code 1.136 are not observed.
- WN-2: Electron ABI for 1.136 is inferred from 1.134/1.135 (42.8.1); vscode-versions had not listed 1.136 at implementation time.

---

## 5. Plan Discrepancies

- Compact VS Code control is checkbox plus detection caption, not a nested card. The plan asked to merge replace-copy into one Features checkbox; that is what shipped.
- `engines.vscode` was already `^1.134.0` and was not changed (vsce rejects compound ranges).

---

## 6. Assumptions Made

- VS Code 1.136.0 keeps Electron 42.8.1 because 1.134 and 1.135 did. If that is wrong, Phase 8 rebuilds `better-sqlite3`.
- Package metadata stays 2.4.1 until `/update release`.
- Cursor session model already matches the plan's frontier cell.

---

## 7. Testing Summary

Automated only. Human QA of Configuration, Review groups, and a 1.136 VSIX load is deferred to Phase 8.

---

## 8. TODO Tracker

- T012-T017 done
- Phase 4 is next (VS Code agentic allowlist picker)

---

## 9. Summary and Next Steps

Phase 3 makes a 16 GB Compatible host start with Unsloth and VS Code 1.136 on, and Review lists models by catalog section. Next: Phase 4 VS Code picker uses installer-or-Settings-owned agentic models only.
