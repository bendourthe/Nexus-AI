# Development Log: v2.4.3 Phase 5 - Shared Picker Rank and Default

**Date**: 2026-08-31
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: Chat, Agents, Image, and Video dropdowns use installer recommend order; an empty session defaults to the required/recommended model for that task.
**Outcome**: Switchers receive host VRAM and `recommendOrder`. Empty sessions use `snapshot.recommendedByTask` and ignore leftover favorites until the user changes the picker in that session. 16 GB agentic default is `gemma-4-12b-it-gguf`.

---

## 1. Starting State

- **Branch**: `feat/v2.4.1-field-reliability`
- **Starting tag/commit**: `3f22d87` (v2.4.3 Phase 4)
- **Environment**: Windows 10, Node 20, vitest, tsc
- **Prior session reference**: `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-4-settings-cards.md`
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.3-field-density-identity-and-runtime.md`

---

## 2. Chronological Steps

### 2.1 Switcher sort (T020)

**What happened**: App passes `hostVramGB` into Chat, Agents, and Image. Video uses `vramGB` when it is greater than 0 so a missing probe is not treated as CPU-none. `QuickModelSwitcher` forwards `recommendOrder` into `visibleModelsOnTab`.

### 2.2 Empty-session default (T021)

**What happened**: `resolveDefaultId` prefers recommended unless `applyFavorite` is true. Four pages hydrate without `readFavorite`. `userChangedModelRef` still protects a manual pick from a later list refresh.

### 2.3 Tests (T022-T023)

**Verification**: See section 3.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| ESLint on touched desktop files | PASS |
| `tsc --noEmit` | PASS |
| Focused vitest (selection-policy, QuickModelSwitcher, CodingPage, ChatPage, Image, Video) | PASS |
| Remote CI | NOT RUN (non-final phase, commit-only) |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Packaged Agents picker order on this 16 GB install is not observed here | P2 | v2.4.3 MT-5 |
| Prior v2.4.3 MT-1 through MT-4 | P2 | unchanged |

---

## 5. Plan Discrepancies

- Tests live in `desktop/tests/selection-policy.test.ts` (existing filename), not `selectionPolicy.test.ts`.

---

## 6. Assumptions Made

- `recommendedByTask` plus snapshot `orderedIds` is enough recommend order without loading `recommended.json` in the renderer.

---

## 7. Testing Summary

### Automated Tests
- Empty agentic default is `gemma-4-12b-it-gguf` even with a gpt-oss favorite.
- Switcher options put Gemma 4 12B before gpt-oss at 16 GB.
- CodingPage leftover favorite is ignored; a manual pick survives refresh.

### Manual Testing Performed
- None; human QA is deferred to the last phase.

### Manual Testing Still Needed
- human QA is deferred to the last phase

---

## 8. TODO Tracker

### Completed This Session
- [x] T020-T023

### Remaining
- [ ] Phase 6 image restyle identity (T024-T027)

### Out of Scope
- [ ] Push / PR (final phase only)

---

## 9. Summary and Next Steps

Phase 5 shares picker rank and empty-session default. Next: Phase 6 image restyle identity.
