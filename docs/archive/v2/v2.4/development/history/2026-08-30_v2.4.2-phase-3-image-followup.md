# Development Log: v2.4.2 Phase 3 - Image Follow-up Identity

**Date**: 2026-08-31
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: A follow-up that refers to the last image edits that image; missing SAM2 is a recovery path, not a brick wall.
**Outcome**: "Make that puppy black" with no attachment dispatches img2img against last PNG bytes. Object replace still uses SAM2 when weights exist. Missing SAM2 shows Install / Paint a mask / Settings, then Retry after install.

---

## 1. Starting State

- **Branch**: `feat/v2.4.1-field-reliability`
- **Starting tag/commit**: `f06cd5e` (Phase 2 transcript honesty)
- **Environment**: Windows 10, Node 24, desktop Vitest 2.1.9, jsdom
- **Prior session reference**: `docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.2-phase-2-transcript-honesty.md`
- **Plan reference**: `docs/archive/v2/v2.4/plans/v2.4.2-field-ui-history-and-generation.md`

Context: Packaged 2.4.1 generated a new black puppy on "Make that puppy black" and left SAM2-missing as a text dead-end. Installer dirty files stay unstaged for Phase 6.

---

## 2. Chronological Steps

### 2.1 Follow-up is an edit (T016)

**Branch**: `feat/v2.4.1-field-reliability` | **PR**: none | **Merged to**: n/a

**Plan specification**: Empty attachments plus a usable last output never take txt2img. Load the path to the protocol the sidecar already accepts. Cap strength so restyles keep pose and subject. Expand "make (that|the|this) X color" as restyle, not only `recolor X to Y`.

**What happened**: `parseReplaceIntent` adds `scope: image` for make-that-color. `resolveFollowUpSourceImage` prefers in-session PNG bytes as a data URL. img2img follow-ups send `strength: 0.45`. Python `decode_source_image` also opens an existing filesystem path so remounted sessions still work.

**Key files changed**: `core/image/replaceIntent.ts`, `desktop/src/modules/image/followUpSource.ts`, `desktop/src/modules/image/ImageStudioPage.tsx`, `runtimes/diffusion/pipelines/real_execute.py`

**Verification**: ImageStudioPage: "Make that puppy black" is img2img, `lastSegment` is null, source is `data:image/png;base64,...`. replaceIntent and imageIntent fixtures pass.

### 2.2 SAM2 recovery (T017)

**Plan specification**: weights_missing renders Install `sam2:hiera-tiny`, Paint a mask, optional Settings. Whole-image restyles must not call segment. Auto-install is forbidden. Retry uses the parked prompt.

**What happened**: `Sam2RecoveryCard` in the assistant bubble. Install is disabled when the sidecar is down. Paint a mask opens Advanced with the last source. After install completes, Retry replays `mediaRetryRef`.

**Key files changed**: `desktop/src/components/GenerationCanvas.tsx`, `desktop/src/shared/chat/types.ts`, `desktop/src/shared/chat/MessageBubble.tsx`, `desktop/src/shared/chat/MessageList.tsx`, `desktop/src/modules/image/ImageStudioPage.tsx`

**Verification**: weights_missing shows `sam2-recovery`. Install then Retry reaches inpaint. "Make that puppy black" does not call `segment`.

### 2.3 Testing and stabilization (T018-T019)

**What happened**: New `desktop/tests/followUpSource.test.ts` plus ImageStudioPage, imageIntent, GenerationCanvas, replaceIntent, and Python decode tests.

**Verification**: See section 3.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Desktop ESLint (`npm run lint` in `desktop/`) | PASS |
| Desktop `tsc --noEmit` | PASS |
| Desktop vitest with coverage | PASS (207 files, 1912 passed, 1 skipped; lines 86.8%, branches 81.41%, functions 82.03%) |
| Core replaceIntent unit tests | PASS (4) |
| Python `test_decode_source_image_opens_an_existing_path` | PASS |
| Remote CI | NOT RUN (non-final phase, commit-only) |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| jsdom cannot prove thinking-pill pixel containment | P2 | v2.4.2 MT-1 |
| Stick-to-bottom on a live overflow pane is not observed | P2 | v2.4.2 MT-2 |
| Packaged img2img identity is not observed | P2 | v2.4.2 MT-3; Phase 7 operator screenshot |

---

## 5. Plan Discrepancies

- Follow-up source prefers in-session PNG bytes, not only `event.outputPath`, because the old decoder rejected filesystem paths.
- Python path decode is extra defense for remounted sessions.
- SAM2 recovery is a dedicated card (`sam2-recovery`), not the media-runtime repair card.

---

## 6. Assumptions Made

- **Installer dirty files stay out of this commit**: Phase 6 T032 owns Setup/Reset/SANA installer UI still uncommitted on this branch.
- **0.45 strength is enough to keep identity** for color/style follow-ups; a packaged proof waits for Phase 7.

---

## 7. Testing Summary

### Automated Tests
- Desktop vitest + coverage: 1912 passed, 1 skipped, 0 failed. Aggregate lines 86.8% (threshold 80%).
- Focused ImageStudioPage (28), imageIntent (9), followUpSource (3), GenerationCanvas (11), replaceIntent (4), and one Python decode test passed.

### Manual Testing Performed
- None; human QA is deferred to the last phase.

### Manual Testing Still Needed
- human QA is deferred to the last phase

---

## 8. TODO Tracker

### Completed This Session
- [x] T016 Unattached follow-up edits the last image
- [x] T017 SAM2-missing recovery path
- [x] T018 Intent, img2img, and SAM2 tests
- [x] T019 Stabilize and record local evidence

### Remaining (Not Started or Partially Done)
- [ ] Phase 4 Video Fail-Closed Output (T020-T023)

### Out of Scope (Deferred)
- [ ] Installer VRAM / Unsloth / VS Code merge (Phase 6)
- [ ] v2.4.1 T097 push/PR

---

## 9. Summary and Next Steps

Phase 3 landed last-output img2img, restyle-without-SAM2, and a SAM2 recovery card. Next: `/implement v2.4.2-field-ui-history-and-generation in-full` continues at Phase 4 (no empty video success bubble).
