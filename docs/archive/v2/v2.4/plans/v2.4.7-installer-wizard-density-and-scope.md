# Plan -- Installer Wizard Density and Scope

**Project**: Nexus AI Studio
**Version**: v2.4.7
**Slug**: installer-wizard-density-and-scope
**Filename**: v2.4.7-installer-wizard-density-and-scope.md
**Plan Type**: Bug fix / UX enhancement
**Status**: Complete. PR 63 green (42 checks pass, 0 fail), merged to develop as 2b7b9811. Installer rebuilt for field testing (sha256 8B546AE4...). No release performed.
**Created**: 2026-09-06
**Field evidence**: Packaged v2.4.6 installer, operator screenshots 1-4 (2026-09-06). Screenshot 3 shows the Review page reporting `Models: 7 selected (7 already downloaded, 0 to download)` beside `Estimated disk usage: ~157 GB to download / 233.2 GB already downloaded` -- two mutually exclusive claims on one card. Screenshots 1, 2 and 4 are layout and scope feedback on Install Path, Configuration, and Review.
**Goal**: The installer states one consistent size for a selection, asks only the questions a user can meaningfully answer, and presents Install Path, Configuration and Review at a density that fits the window. The Review estimate and the disk guard both size the selected models rather than the whole catalog; the Configuration step drops preference toggles that are not install decisions and derives the components a selection requires; Install Path uses its full width with Browse inside the field; and Review presents a model summary with counters, category chips, and a single-line storage figure.

## Overview

v2.4.6 shipped and merged (PR 62, 42 checks green, `8499c05d`). Field-testing its installer produced four screenshots. Three are density and scope feedback. One is a defect, and it is a defect this project introduced in v2.4.5.

**The defect.** The Review card claims `7 already downloaded, 0 to download` and `~157 GB to download` at the same time. The counts are computed per id in `review.py` against `installed_report.is_downloaded`, so they are scoped to the selection and correct. The sizes are read straight off `installed_report.pending_gb` and `downloaded_gb`, which are **catalog-wide**, because the picker deliberately probes every catalog entry:

```python
# typed_catalog.py:1188
report = probe_installed_models(
    selection=list(self._catalog),   # every model, not the selection
    ...
)
```

Probing the whole catalog is correct -- every card needs to know whether to show its Downloaded pill. Reading that report's *sizes* as if they described the selection is not. `~157 GB` is every un-downloaded catalog model; `233.2 GB` is every downloaded one.

This is not confined to the Review text. `pending_download_gb` returns the same catalog-wide `report.pending_gb`, and that value feeds the install guard, the picker footer, and `can_select_model`. The guard is therefore demanding headroom for models the user did not select. The operator had 201.7 GB free so it passed; with less free space it would have refused an install that downloads nothing, which is the exact v2.4.5 defect wearing a different mask.

v2.4.5's tests missed it because every fixture built a report whose `downloaded`/`pending` sets matched the selection, so catalog-wide and selection-scoped sizes coincided. A fixture with a catalog wider than the selection is the missing case.

`InstallerState.pending_models_gb` was declared in v2.4.5 and never written. It is the correct home for the selection-scoped figure.

**The scope question.** The operator asked whether all eight Configuration options are needed. They are not, and two of them are not install decisions at all. Tracing what each gates:

| Option | What it gates | Verdict |
|---|---|---|
| Install Ollama | Every `ollama`-protocol model (all chat/agentic) | Load-bearing |
| Create Python virtual environment | The diffusion runtime for every Image/Video/Audio model | Load-bearing |
| Install the Nexus desktop app | The product itself | Load-bearing |
| Add Start Menu shortcut | A shortcut | Optional |
| Replace the VS Code extension | The extension install | Optional |
| Install Unsloth Core | QLoRA fine-tuning runtime; already opt-in and VRAM-gated | Optional |
| Enable thinking mode | `state.enable_thinking` -- a config value, no install step | **Not an install decision** |
| Enable persistent memory | `state.enable_memory` -- a config value, no install step | **Not an install decision** |

Unchecking a load-bearing component silently breaks models the user selected two steps earlier, and the wizard says nothing. The two settings toggles ask about runtime preferences at the worst possible moment and are changeable in Settings afterwards.

This plan derives the three load-bearing components from the model selection and presents them as a "will be installed" list, drops the two settings toggles, and leaves the three genuine choices as choices.

This is a v2.4 cycle on a new branch off `develop` at `8499c05d`. It does not reopen the v2.4.6 delivery, density, or session-identity work. Nothing in the 2.4 series is released; the package version stays 2.4.1 and no version bump belongs to this plan.

No phase except the last pushes, opens a pull request, or starts remote CI. Each earlier phase ends with automated tests, session history, and one local commit. The final phase rebuilds the installer so the operator can field-test it.

Discovery interview: skipped as a live questionnaire. The operator supplied four annotated screenshots, a target mockup, and an explicit scope question in one message, and approved the recommendation that answered it. Assumptions are listed under Architecture Decisions.

## Goals First

**Target problem**: The installer reports two contradictory sizes for one selection and sizes its disk guard from the wrong one; it asks eight configuration questions of which two are not install decisions and three are load-bearing enough that answering them wrongly breaks the install; and three wizard pages waste horizontal space.

**Target persona**: The operator re-running the packaged installer on the same Windows / NVIDIA host across plan cycles (RTX 3080 Ti Laptop, 16 GB VRAM, ~200 GB free), who has most models already downloaded and needs the wizard to be both correct and quick to read.

**Observable definition of done**:

- With 7 models selected and all 7 already on disk, the Review card reads `0 to download` **and** an estimated download of ~2 GB (overhead only). The two figures never disagree.
- With a catalog wider than the selection, `pending_download_gb` returns the size of the un-downloaded **selected** models, not of the un-downloaded catalog.
- The install guard, the picker footer, and `can_select_model` all use that same selection-scoped figure.
- Estimated installation time is derived from the selection-scoped pending size, so an all-downloaded selection does not read as a 10-15 minute install.
- Install Path: the field spans the full page width and Browse sits inside it.
- Configuration: "Enable thinking mode" and "Enable persistent memory" are gone from the installer. Ollama, Python environment, and desktop app appear as a derived "will be installed" list, not as checkboxes. The VS Code extension, Start Menu shortcut, and Unsloth remain user choices.
- Configuration: the Ollama URL field sits in the left column at single-column width, directly beneath the Ollama row, with no separate "Ollama URL" heading. The blue VS Code detection paragraph is gone.
- Review: install path, components, GPU, estimated storage and estimated installation time occupy the left column. Estimated storage renders on one line.
- Review: the model summary is visually distinct from the per-category lists -- counters for selected / ready / to download, and category chips -- per the screenshot 4 mockup.
- A user whose selection needs Ollama is told so; the wizard never silently installs a chat model with no runtime to serve it.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at docs/archive/v2/v2.4/constitution.md - skipping check. Recommend running /constitution to establish project principles.

Governing contracts used instead: `AGENTS.md` (core must not import modules; local-only models; evidence tiers `not_observed != absent`), module authorship, and `docs/archive/v2/v2.4/known-gaps.md`. This plan opens no new outbound network dependency and adds no new install step.

## Knowledge-base grounding

No `STRATEGY.md` and no `docs/solutions/` store exist in this tree. Grounding is the four operator screenshots, the v2.4.5 plan and its `installed_models` module, the v2.4.6 plan and last-phase evidence, and `docs/archive/v2/v2.4/known-gaps.md`.

## Current model map

**Model map status**: carried from the v2.4.6 plan; no refresh performed for this cycle.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.5-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

## Architecture Decisions

1. **The probe keeps its catalog-wide scope; the sizes become selection-scoped at the point of use.** Narrowing the probe to the selection would break the picker, which needs a Downloaded pill on every card including unselected ones. The report's `downloaded` and `pending` sets stay catalog-wide and remain correct for that job. What changes is that nothing reads `report.pending_gb` as a selection size.

2. **`InstallerState.pending_models_gb` becomes the single selection-scoped figure.** It already exists and is dead. The picker computes it in `_update_selection_state`, which is the one place that already knows both the selection and the per-model sizes, and writes both it and `selected_models_gb` together so they cannot drift.

3. **`pending_download_gb` reads the state field, not the report.** Its unknown-report fallback is unchanged: a report the probe never populated still falls back to the full selection total, because the safe reading of unknown remains "assume nothing is present".

4. **The regression test uses a catalog wider than the selection.** That single property is what v2.4.5's fixtures lacked. Every new sizing test builds a report covering more models than the user selected, so catalog-wide and selection-scoped values are forced apart and a reversion cannot pass.

5. **Load-bearing components are derived, not asked.** `requires_ollama(selection)` and `requires_python_env(selection)` resolve from each selected model's protocol and type, and the desktop app is always installed. They render as a read-only "Will be installed" list with a one-line reason each. This removes the failure mode where unchecking a box two steps after picking a model breaks that model with no warning. The engine contract is untouched: `components_to_install` is still the list the installer executes; it is now populated from the selection instead of from checkboxes.

6. **The two settings toggles leave the installer entirely.** `enable_thinking` and `enable_memory` gate no install step. Their state fields and their defaults stay exactly as they are, so first-run behavior does not change and the app's Settings surface still owns them. Only the installer's questions go.

7. **Unsloth, the VS Code extension, and the shortcut stay checkboxes.** Each is a genuine choice with a real cost the user may decline, and none of them breaks a selected model when unchecked.

8. **Review's left column carries the facts; the right column carries the models.** Estimated storage and installation time move left because they are install facts alongside path, components and GPU. Storage renders on one line, with the already-downloaded total as secondary text rather than a second full-width row.

9. **The model summary gets its own visual level.** Screenshot 4's mockup is the target: a counter row (selected / ready / to download) reading as a summary, then category chips. The chips reuse the existing category vocabulary; nothing new is invented. This is styling over the same data the page already renders, so it carries no new state.

10. **Install-time heuristic keys off the corrected pending size.** With the size bug fixed, the existing "under 5 minutes" band finally fires for an all-downloaded selection, which is what screenshot 3's `10-15 minutes` got wrong.

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|---|---|---|---|---|
| 1 | Selection-scoped sizing | One size per selection; guard and Review agree | frontier | max |
| 2 | Configuration scope | Derived components; two toggles removed | frontier | high |
| 3 | Install Path and Configuration layout | Full-width path, Browse inside, URL in column | strong | high |
| 4 | Review density and model summary | Facts left, one-line storage, counters and chips | strong | high |
| 5 | Evidence, known gaps, CI/CD, integration, installer rebuild | Green integration, then a rebuilt installer to test | frontier | max |

- [x] T001 Compute selection-scoped pending and downloaded sizes in the picker scripts/installer/src/nexus_installer/pages/typed_catalog.py
- [x] T002 Read the state field in pending_download_gb instead of the report scripts/installer/src/nexus_installer/engine/installed_models.py
- [x] T003 Scope the Review estimate to the selection scripts/installer/src/nexus_installer/pages/review.py
- [x] T004 Add wider-catalog regression tests across guard, footer and Review scripts/installer/tests/test_selection_scoped_sizing.py
- [x] T005 Stabilize Phase 1 and record local evidence docs/archive/v2/v2.4/development/history/2026-09-06_v2.4.7-phase-1-selection-sizing.md
- [x] T006 Derive required components from the model selection scripts/installer/src/nexus_installer/engine/required_components.py
- [x] T007 Render a read-only Will be installed list scripts/installer/src/nexus_installer/pages/configuration.py
- [x] T008 Remove the thinking-mode and persistent-memory toggles scripts/installer/src/nexus_installer/pages/configuration.py
- [x] T009 Add derived-component and removed-toggle tests scripts/installer/tests/test_required_components.py
- [x] T010 Stabilize Phase 2 and record local evidence docs/archive/v2/v2.4/development/history/2026-09-06_v2.4.7-phase-2-config-scope.md
- [x] T011 Put Browse inside a full-width path field scripts/installer/src/nexus_installer/pages/install_path.py
- [x] T012 Move the Ollama URL under Ollama at column width scripts/installer/src/nexus_installer/pages/configuration.py
- [x] T013 Drop the VS Code detection paragraph scripts/installer/src/nexus_installer/pages/vscode_extension.py
- [x] T014 Add Install Path and Configuration layout tests scripts/installer/tests/test_pages_qt.py
- [x] T015 Stabilize Phase 3 and record local evidence docs/archive/v2/v2.4/development/history/2026-09-06_v2.4.7-phase-3-page-layout.md
- [x] T016 Move storage and time to the left column on one line scripts/installer/src/nexus_installer/pages/review.py
- [x] T017 Render the model summary as counters plus category chips scripts/installer/src/nexus_installer/pages/review.py
- [x] T018 Add Review density and summary tests scripts/installer/tests/test_review.py
- [x] T019 Stabilize Phase 4 and record local evidence docs/archive/v2/v2.4/development/history/2026-09-06_v2.4.7-phase-4-review-density.md
- [x] T020 Refactor release-touched architecture and quote the scan docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.7-wizard-density.md
- [x] T021 Reconcile all active known gaps docs/archive/v2/v2.4/known-gaps.md
- [x] T022 Verify living documentation architecture docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.7-wizard-density.md
- [x] T023 Report git-tree hygiene docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.7-wizard-density.md
- [x] T024 Reconcile terminal CI/CD coverage docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.7-wizard-density.md
- [x] T025 Perform an independent Goal-vs-codebase review docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.7-wizard-density.md
- [x] T026 Execute last-phase human testing suggestions docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.7-wizard-density.md
- [x] T027 Run full local testing and stabilization docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.7-wizard-density.md
- [x] T028 Publish and integrate only after explicit approval docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.7-wizard-density.md
- [x] T029 Rebuild the installer for operator field testing dist/NexusSetup.exe

## Field map (screenshots to code)

| Shot | Symptom | Primary cause in tree |
|---|---|---|
| 3 | `0 to download` beside `~157 GB to download` | `review.py:267` reads `report.pending_gb`, which `typed_catalog.py:1188` populated from `list(self._catalog)` |
| 3 | `10-15 minutes` for a zero-byte download | `_estimate_html` keys its heuristic off the same wrong number |
| 3 | Storage and time on the right, storage over two lines | `_estimate_html` is appended to the models column |
| 1 | Browse outside a narrow path field | `install_path.py:63-67` puts the input and button in a shared row |
| 2 | Ollama URL full width under both columns | `configuration.py:190-193` adds it to the page layout, not the components column |
| 2 | Blue VS Code detection paragraph | `VsCodeExtensionPage` compact mode renders its detection line |
| 2 | Eight options, two of which are not install decisions | `_thinking_toggle` and `_memory_toggle` set `enable_thinking` / `enable_memory` and gate no step |
| 4 | Model summary reads like another category | Summary and categories share one text level in `_rebuild_summary` |

---

## Phase 1: Selection-Scoped Sizing

**Goal**: One number describes a selection, and every consumer reads it.
**Prerequisites**: None.
**Stability Gate**: Tests built on a catalog wider than the selection prove `pending_download_gb`, the Review estimate, the picker footer and `can_select_model` all report selection-scoped sizes, and that the guard neither over-refuses nor under-refuses.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: This is a correctness defect in the gate that decides whether an install may start, and the previous fix for the same symptom was itself incomplete.

### Sub-tasks

#### 1.1 - Selection-scoped sizes on state

**Objective**: The picker publishes `pending_models_gb` alongside `selected_models_gb`.

**Failure modes**: A selected id absent from the catalog contributes 0 and must not raise. The two fields must be written together so they cannot disagree. `installed_report` keeps its catalog-wide sets, since the picker's pills depend on them.

**Prompt**:
> In `_update_selection_state`, compute the pending total as the sum of `size_gb` for selected ids not in `installed_report.downloaded`, and assign `state.pending_models_gb` in the same statement group that assigns `state.selected_models_gb`. Do not narrow the probe's `selection` argument; the picker needs the catalog-wide report.

#### 1.2 - Consumers read the selection-scoped figure

**Objective**: `pending_download_gb`, the Review estimate, the footer and `can_select_model` agree.

**Failure modes**: The unknown-report fallback must survive: a report the probe never populated still falls back to the full selection total. The headless `--model` path never opens the picker, so `pending_models_gb` is 0 there and must not be read as "nothing to download".

**Prompt**:
> Change `pending_download_gb` to return `state.pending_models_gb` when the report is populated, keeping the existing unknown fallback. Point `review.py` at the same helper rather than at `report.pending_gb` / `report.downloaded_gb`, deriving the already-downloaded total as `selected_models_gb - pending`. Verify `can_select_model` credits back only selected downloaded models.

#### 1.3 - Testing and Stabilization

**Prompt**:
> Generate `scripts/installer/tests/test_selection_scoped_sizing.py`. Every fixture MUST use a catalog strictly wider than the selection, which is the property v2.4.5's tests lacked. Cover: all-selected-downloaded reports ~0 pending; a partially-downloaded selection; the field case (7 selected, 7 downloaded, a catalog holding 157 GB of other un-downloaded models) asserting the estimate is overhead-only and the guard passes; an unknown report still falling back; and an oversized genuinely-new selection still blocking. Run installer pytest and ruff. CI IMPACT: `installer-tests.yml` already scopes `scripts/installer/**`. Session history `docs/archive/v2/v2.4/development/history/2026-09-06_v2.4.7-phase-1-selection-sizing.md`. One local commit. Do not push.

### Phase 1 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] CI impact recorded against the canonical contract, with no remote CI run
- [ ] Session history generated for this phase
- [ ] One local commit created for this phase
- [ ] No branch push, pull request, or remote CI run occurred
- [ ] Ready to advance to Phase 2

---

## Phase 2: Configuration Scope

**Goal**: The installer asks only what a user can meaningfully decide, and never lets a component choice silently break a selected model.
**Prerequisites**: Phase 1.
**Stability Gate**: Tests prove Ollama and the Python environment are derived from the selection, the desktop app is always present, the two settings toggles are gone from the page while their state defaults are unchanged, and `components_to_install` still drives the engine.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: This changes what the installer installs, not merely how it looks; a wrong derivation silently removes a runtime a selected model needs.

### Sub-tasks

#### 2.1 - Derive required components

**Objective**: A pure resolver maps a model selection onto the components it requires.

**Failure modes**: An uncatalogued id routes to ollama verbatim and therefore requires Ollama. An empty selection still requires the desktop app. A host with Ollama already installed still lists it as required but satisfied. Image/Video/Audio models require the Python environment; chat-only selections do not.

**Prompt**:
> Add `scripts/installer/src/nexus_installer/engine/required_components.py` exposing `required_components(selection, catalog) -> RequiredComponents` with the component ids and a one-line human reason each. Route on `model_router.protocol_for` and the catalog `type`. No Qt import. Keep `components_to_install` as the engine's contract; populate it from this resolver plus the user's optional choices.

#### 2.2 - Will be installed list

**Objective**: Required components are shown, with their reason, not asked.

**Failure modes**: The list must update when the selection changes. Optional choices (extension, shortcut, Unsloth) stay checkboxes and must not move into the list. An already-installed Ollama should read as satisfied rather than as pending work.

**Prompt**:
> Replace the Ollama / venv / desktop checkboxes with a read-only "Will be installed" list rendering each required component and its reason. Keep the section heading. Recompute on show, since the picker precedes this page.

#### 2.3 - Remove the settings toggles

**Objective**: The installer stops asking about runtime preferences.

**Failure modes**: `state.enable_thinking` and `state.enable_memory` keep their current defaults and are still written to the app config, so first-run behavior is unchanged. Any test asserting the toggles exist is rewritten to assert the state defaults instead.

**Prompt**:
> Delete `_thinking_toggle` and `_memory_toggle` and their rows. Do not touch the state fields, their defaults, or the config writer.

#### 2.4 - Testing and Stabilization

**Prompt**:
> Generate `scripts/installer/tests/test_required_components.py` and update the configuration page tests. Run installer pytest and ruff. CI IMPACT: `scripts/installer/**` already covered. Session history `docs/archive/v2/v2.4/development/history/2026-09-06_v2.4.7-phase-2-config-scope.md`. One local commit. Do not push.

### Phase 2 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] CI impact recorded against the canonical contract, with no remote CI run
- [ ] Session history generated for this phase
- [ ] One local commit created for this phase
- [ ] No branch push, pull request, or remote CI run occurred
- [ ] Ready to advance to Phase 3

---

## Phase 3: Install Path and Configuration Layout

**Goal**: Both pages use their full width and group related controls together.
**Prerequisites**: Phase 2.
**Stability Gate**: Tests prove the path input spans the row with Browse inside it, the Ollama URL is a child of the components column with no separate heading, and the VS Code detection paragraph is absent.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Contained Qt layout work across two pages with existing tests to keep green.

### Sub-tasks

#### 3.1 - Browse inside a full-width path field

**Objective**: One control that reads as a path field with an action.

**Failure modes**: The free-space line and the validation error line stay under the field. Keyboard focus and tab order must still reach Browse. The field must remain editable by typing.

**Prompt**:
> Make the path input span the full row and place Browse inside it as a trailing action (`QLineEdit.addAction` with a right position, or an overlaid button). Remove the separate row cell.

#### 3.2 - Ollama URL in the components column

**Objective**: The URL sits with the thing it configures.

**Failure modes**: It must not stretch to page width. It must remain readable at the narrowest wizard width. The accessible name survives the removal of the visible heading.

**Prompt**:
> Move the Ollama URL field into the components column directly beneath the Ollama row, drop the visible "Ollama URL" heading, and keep an `accessibleName` or placeholder carrying the same meaning.

#### 3.3 - Drop the VS Code detection paragraph

**Objective**: The checkbox stands alone.

**Failure modes**: Detection logic itself is unchanged; only its rendered paragraph goes. The not-detected path must still be discoverable, so if a user cannot install the extension the checkbox is disabled with a tooltip rather than silently ticked.

**Prompt**:
> Remove the blue detection line from the compact `VsCodeExtensionPage`. Keep detection driving the checkbox's enabled state.

#### 3.4 - Testing and Stabilization

**Prompt**:
> Extend `scripts/installer/tests/test_pages_qt.py`. Run installer pytest and ruff. CI IMPACT: `scripts/installer/**` already covered. Session history `docs/archive/v2/v2.4/development/history/2026-09-06_v2.4.7-phase-3-page-layout.md`. One local commit. Do not push.

### Phase 3 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] CI impact recorded against the canonical contract, with no remote CI run
- [ ] Session history generated for this phase
- [ ] One local commit created for this phase
- [ ] No branch push, pull request, or remote CI run occurred
- [ ] Ready to advance to Phase 4

---

## Phase 4: Review Density and Model Summary

**Goal**: Review reads as facts on the left and models on the right, with a summary that is visually distinct from the category lists.
**Prerequisites**: Phase 3.
**Stability Gate**: Tests prove storage and time render in the facts column, storage occupies one line, the counter row reports selected / ready / to download, and category chips are present and distinct from the summary.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Single-file rich-text restructuring with an existing narrow-width restack path to preserve.

### Sub-tasks

#### 4.1 - Facts column carries storage and time

**Objective**: Install facts sit together.

**Failure modes**: The narrow-width restack must still collapse the split. Storage must fit one line at the packaged width, with the already-downloaded total as secondary text rather than a second row.

**Prompt**:
> Move the estimate block from the models column into `facts_html`, under GPU. Render storage as a single line with the already-downloaded figure as muted trailing text. Keep the install-time line beneath it.

#### 4.2 - Counters and category chips

**Objective**: The summary reads as a summary.

**Failure modes**: Counters must agree with the per-category rows they summarize. A category with no selected model is omitted, not shown empty. Marks stay distinguishable without color alone.

**Prompt**:
> Render a counter row (selected / ready / to download) above the category groups, styled distinctly from the category headings, then the categories as labelled chips per the screenshot 4 mockup. Reuse the existing category vocabulary and the existing downloaded/pending marks; add no new state.

#### 4.3 - Testing and Stabilization

**Prompt**:
> Extend `scripts/installer/tests/test_review.py`. Run installer pytest and ruff. CI IMPACT: `scripts/installer/**` already covered. Session history `docs/archive/v2/v2.4/development/history/2026-09-06_v2.4.7-phase-4-review-density.md`. One local commit. Do not push.

### Phase 4 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] CI impact recorded against the canonical contract, with no remote CI run
- [ ] Session history generated for this phase
- [ ] One local commit created for this phase
- [ ] No branch push, pull request, or remote CI run occurred
- [ ] Ready to advance to Phase 5

---

## Phase 5: Evidence, Known Gaps, CI/CD, Integration, and Installer Rebuild

**Goal**: Leave the project well-organized and its gaps reconciled, publish and integrate, then rebuild the installer so the operator can field-test this cycle and every one before it.
**Prerequisites**: All prior phases.
**Stability Gate**: `docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.7-wizard-density.md` exists with one quoted section per duty. A missing Goal, a Goal-review miss without a recorded known-gap, or a missing evidence section blocks the rebuild.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, Goal-vs-codebase review, known-gap reconciliation, release gating, and the installer rebuild that is this cycle's deliverable.

### Sub-tasks

#### 5.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout and quote the scan.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure, then refactor via [[project-refactor]] and [[docs-layout-refactor]] (propose-then-apply, with confirmation; repair every reference for anything that moves). Write the detector output into the evidence file under `## Architecture refactor`. An empty finding is allowed only when the scan output is quoted.

#### 5.2 - Known-gaps reconciliation
**Objective**: Reconcile this version and every other still-open known-gaps file.
**Prompt**:
> Via [[known-gaps-tracker]], reconcile this version's known gaps AND every other `docs/**/known-gaps.md` whose Status is in-progress or whose Open Items remain. Add a `## v2.4.7` section. Record the selection-scoped sizing defect as a resolved BG row naming v2.4.5 as its origin and the wider-catalog fixture as the regression guard. Carry forward every v2.4.4, v2.4.5 and v2.4.6 row that this cycle did not observe. Write the disposition under `## Known-gaps reconciliation`.

#### 5.3 - Living docs architecture
**Objective**: Prove handbooks, decisions, and generated HTML match the required living tree.
**Prompt**:
> Check `docs/handbooks/`, `docs/decisions/`, and living `docs/README.md` / `docs/DEVLOG.md` / `docs/todos.md`. Self-gated: never invent `docs/testing/` or `docs/validation/`. Markdown wins if HTML disagrees. This cycle changes installer wizard behavior; if a handbook documents the Configuration questions, refresh it. Quote the check under `## Living docs architecture`.

#### 5.4 - Git-tree hygiene
**Objective**: Report branch and repository-settings hygiene without deleting anything.
**Prompt**:
> Run `python scripts/check_release_preconditions.py --branches --repo-settings`. Quote the output under `## Git-tree hygiene`. Report only; never delete branches.

#### 5.5 - Terminal CI/CD reconciliation
**Objective**: The pipeline is compared field by field against the canonical contract.
**Prompt**:
> Invoke [[cicd-architect]] in existing-pipeline comparison mode and run its six steps in order: DETECT, COMPARE against every canonical field, PROPOSE each difference with cost and risk, obtain explicit APPROVAL per change, APPLY approved changes and re-run the local gate, and RECORD declined or environment-only differences as known gaps. Two findings are already open and carry forward unless fixed here: `ci.yml` has no always-resolving aggregate required check, and it declares no top-level `permissions:` block. Confirm v2.4.4 QG-1 (develop-targeted PR coverage) is still in force. Quote the per-field result under `## CI/CD coverage`.

#### 5.6 - Independent Goal-vs-codebase review
**Objective**: Prove the plan Goal landed in the codebase, not that checkboxes were ticked.
**Prompt**:
> Re-read the plan header Goal and the Goals First / Definition of Done. Inspect the codebase as if this agent had not implemented the phases. Write `## Goal-vs-codebase review` listing the Goal restated, the artifacts that satisfy it, and any gap. A miss is a blocking finding or a new known-gap, not a pass.

#### 5.7 - Human/manual testing suggestions (last phase only)
**Objective**: Ask a human to exercise only what automated tests cannot cover.
**Prompt**:
> Emit operator items for the rebuilt installer. This cycle: (1) with everything already downloaded, Review reads `0 to download` and an overhead-only storage figure, and the install proceeds; (2) an oversized genuinely-new selection still refuses, so the guard was not disabled; (3) Install Path spans the window with Browse inside it; (4) Configuration shows a Will-be-installed list plus three real choices, with the Ollama URL beneath Ollama and no thinking-mode or memory toggles; (5) Review shows facts left with one-line storage, and a counter row visually distinct from the category chips. Carry forward every open operator item from v2.4.4, v2.4.5 and v2.4.6, including the Wan and SDXL re-smoke on the 0.36.0 Diffusers pin.

#### 5.8 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and the complete LOCAL gate is green before anything is published.
**Prompt**:
> Run the full local validation/test suite (root lint, `tsc -b`, `npm run check`, root vitest, desktop vitest, `tests/python` pytest, installer pytest, ruff), confirm the refactor changed no behavior, and iterate until clean. Remember that a VSIX build leaves `better-sqlite3` compiled for Electron; run `npm rebuild better-sqlite3` before trusting a Node-side suite. Quote the suite under `## Full-suite testing and stabilization`. Generate a session-history entry.

#### 5.9 - Publication and integration
**Objective**: Publish once, validate the merge result remotely, and merge on green.
**Prompt**:
> Create the final local commit. Present the resolved branching model, remote, branch name, and pull-request target, and obtain EXPLICIT approval before the plan's first branch push. Wait until every required check reaches a terminal state. A red check REOPENS this phase: classify, reproduce locally, apply the narrow fix, re-run the local gate, update known gaps and session history, and push again with approval. Never re-run a red check without a local reproduction. Merge only after every required check is green and the user approves. Quote the required-check results under `## Publication and integration`.

#### 5.10 - Installer rebuild for field testing
**Objective**: Hand the operator a rebuilt installer carrying every 2.4 cycle.
**Prompt**:
> After the integration merges green, rebuild from the integrated branch: `powershell -File scripts/build-vsix.ps1 -SkipTests`, then `cd desktop; npm run build:shell`, then `powershell -File scripts/installer/build/build-windows.ps1 -SkipSign`, then `powershell -File scripts/installer/build/smoke-windows-exe.ps1`. Verify the artifact by extracting the embedded PyInstaller archive rather than grepping the compressed exe: confirm the lock files still pin `diffusers==0.36.0` and that this cycle's modules are present in `PYZ.pyz`. Record the artifact path, byte size, SHA-256, source commit, and staged payload hash under `## Installer rebuild`. Do NOT bump the package version, create a tag, or run `/update release`.

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| | | |

### Phase 5 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] CI impact recorded against the canonical contract, with no remote CI run until T028
- [ ] Session history generated for this phase
- [ ] One local commit created for this phase
- [ ] Branch push, pull request, and remote CI occurred only after explicit approval
- [ ] Installer rebuilt from the integrated branch and its hash recorded
- [ ] No version bump, tag, or release performed
