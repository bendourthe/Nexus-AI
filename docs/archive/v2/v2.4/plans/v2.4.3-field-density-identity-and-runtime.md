# Plan -- Field Density, Identity, and Runtime

**Project**: Nexus AI Studio
**Version**: v2.4.3
**Slug**: field-density-identity-and-runtime
**Filename**: v2.4.3-field-density-identity-and-runtime.md
**Plan Type**: Feature / corrective enhancement
**Status**: In progress (Phase 8 local complete; T040 waits on explicit push approval)
**Created**: 2026-08-31
**Field evidence**: Packaged v2.4.2 operator screenshots 1-9 (installer Unsloth still selectable, Configuration/Review single column, untitled Image chat, tan-puppy follow-up, cropped Searching pill, left-cropped delete dialog, SANA-Video model-layout-invalid, Settings pills wrapping, Agents picker order)
**Goal**: A 16 GB NVIDIA install refuses Unsloth when it is incompatible, uses two-column installer summaries, selects and names a new chat on first send, restyles the last puppy instead of reprinting it, keeps the thinking glow inside a fixed-width pill, centers delete confirmation, lists models in installer recommend order with icon actions, and either plays a SANA-Video clip or explains a real missing-file reason.

## Overview

v2.4.2 landed on this packaged host: history lives in the left sidebar, Unsloth shows an Incompatible badge, VS Code sits on Configuration, video fails with a written error, Settings no longer prints a catalog hash, and delete copy is generic. The same pass still wastes vertical space on installer pages, lets Unsloth stay ticked while labelled Incompatible (the badge is computed at page construct, before GPU detection finishes), leaves Image sessions titled "New chat", reprints the tan puppy at img2img strength 0.45, clips the Searching glow, parks the delete dialog in the sidebar, wraps Settings pills, shuffles the Agents picker, and probes SANA-Video as if it were a complete Wan Diffusers tree.

This plan is a second field pass on `feat/v2.4.1-field-reliability`. It does not reopen the v2.4.2 sidebar host, empty-video honesty gate, or VS Code merge. Phase 1 fixes installer layout and Unsloth lock. Phase 2 fixes history chrome, new-chat selection, first-send titles, and the delete overlay. Phase 3 fixes the thinking pill. Phase 4 densifies Settings cards. Phase 5 shares picker order and default. Phase 6 raises restyle identity. Phase 7 gives SANA-Video a real layout and executor. Phase 8 is the mandatory fail-closed closeout.

No phase except the last pushes, opens a pull request, or starts remote CI. Each earlier phase ends with automated tests, session history, and one local commit. v2.4.2 T043 remains a separate publication hold.

## Goals First

**Target problem**: After v2.4.2, the packaged studio still lets the operator opt into work the machine cannot do (Unsloth, incomplete SANA-Video), wastes a column of installer and Settings space, and fails the first Image follow-up and first untitled session in ways a person can see in one sitting.

**Target persona**: A privacy-focused Windows user with a single consumer NVIDIA GPU (this host: RTX 3080 Ti Laptop, 16384 MB) who expects installer choices, sidebar chats, Image restyles, Video errors, Settings cards, and the four pickers to match the models they just installed.

**Observable definition of done**:

- Configuration is two columns (Components | Features). Unsloth is Compatible on this 16 GB NVIDIA host after GPU detection, and Incompatible hosts cannot tick it.
- Review is two columns (facts | models). GPU reads whole GB. The time line is "Estimated installation time".
- A hairline sits between the four module tabs and the Chats list.
- New chat selects the empty session immediately.
- First Image prompt "Generate image of a puppy" replaces "New chat" with a short title (model title preferred; prompt fallback is not left as "New chat").
- "Make the puppy black" with no re-attach restyles the last PNG so fur reads black, same composition.
- Thinking pill has a fixed width that fits Searching... / Thinking... / Working... / Solving..., inset so the glow is not cropped.
- Delete confirmation is centered in the window, not flush to the left chrome.
- Settings model facts fit on one line. Downloaded is green. Remove is a red trash icon. Download is a blue download icon. Description uses the card width beside the actions.
- Chat, Agents, Image, and Video pickers default to the installer required/recommended model for that task and list models in that recommend order. Agents empty session is not gpt-oss 20B when Gemma 4 12B is the 16 GB agentic default.
- A SANA-Video turn either produces a playable clip or names the actual missing Diffusers file; it does not probe SANA weights with WanPipeline.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at docs/archive/v2/v2.4/constitution.md - skipping check. Recommend running /constitution to establish project principles.

Governing contracts used instead: `AGENTS.md` (core must not import modules; local-only models; evidence tiers `not_observed != absent`), module authorship, and `docs/archive/v2/v2.4/known-gaps.md`. Carry v2.4.2 MT-1 through MT-6 as now-observed field items where this cycle produces a screenshot.

## Knowledge-base grounding

No `STRATEGY.md` and no `docs/solutions/` store exist in this tree, so grounding is the v2.4.2 plan, its last-phase evidence, and `docs/archive/v2/v2.4/known-gaps.md`. This cycle does not reopen the sidebar history host, empty-video honesty gate, or VS Code merge.

## Current model map

**Model map status**: fresh as of 2026-08-31; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.5-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/about-claude/models/overview
- OpenAI: https://developers.openai.com/api/docs/models
- Google: https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/, https://ai.google.dev/gemini-api/docs/changelog
- Cursor: https://cursor.com/docs/models-and-pricing, https://cursor.com/docs/models/grok-4-6

## Architecture Decisions

1. **Unsloth Incompatible is not selectable.** v2.4.2 AD allowed opt-in after a warning. Screenshot 1 reverses that: if the badge is Incompatible, the checkbox is disabled and cleared. Refresh the badge on `showEvent` so it is not computed at construct with `vram_mb=0`. Host-ok is NVIDIA plus `display_vram_gb(vram_mb) >= 16` (the 15 GiB-class band still counts as 16). This 16 GB laptop must show Compatible.
2. **Installer summaries are two columns.** Configuration: Components left, Features right. Review: install path, components, GPU, disk, estimated installation time on the left; the model list on the right. GPU on Review uses `display_vram_gb`, never raw MB.
3. **Delete and archive dialogs portal to `document.body`.** `position: fixed` inside the sidebar history host is why screenshot 6 sits flush left and clips. Center over the full window.
4. **New chat selects.** `FolderTree.onCreateChat` calls the same `onSelect` path as a row click and shows an empty transcript. Creating a session is not enough.
5. **Untitled sessions title on first send.** Image/Video `ensureSession` returns early when a "New chat" row already exists, so `titleJobRef` never runs. Chat already titles on first send of an untitled chat. Studio must do the same, including `applyImmediateFallbackTitle` plus `refineGeneratedTitle`.
6. **Thinking pill width is constant.** Min-width is the longest caption (`Searching...`) plus orb and padding. Add left inset (or host margin) so `box-shadow` / drop-shadow is inside the transcript, not against the pane edge.
7. **Settings facts are one nowrap row.** Origin / license / long metadata move into Details. The dense row keeps size, VRAM compatibility, and recommendation. Actions are icon-only: green Downloaded, red trash Remove, blue Download.
8. **Picker order is installer recommend order.** `QuickModelSwitcher` today lists `visibleModelsOnTab` without `hostVramGB` or `recommendOrder`, so Agents looks shuffled and can default to gpt-oss 20B. Pass host VRAM, sort with `catalog_tab_sort` / snapshot `recommendedByTask`, and default an empty session to the installer required/recommended id for that task. A later user change still sticks on that session.
9. **Whole-image restyle uses stronger img2img.** "Make the puppy black" already parses as `scope: "image"`. Strength 0.45 preserves tan fur. Restyle strength is 0.70 (object inpaint stays 0.45). The img2img prompt must say keep composition and change fur color. Fail closed if `lastOutputRef` is missing (do not silently txt2img).
10. **SANA-Video is not Wan.** `sana_video.py` calls `real_execute.video_execute`, which requires `model_index.json` and `WanPipeline`. Catalog `sana-video-2b-720p` only lists transformer shards. Expand the weights manifest (or snapshot) for a complete Diffusers SANA-Video tree, and execute with the SANA-Video pipeline class, not Wan. 16 GB default video remains `wan2.1-t2v-1.3b`; this phase still has to make the opted-in SANA id honest.

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|---|---|---|---|---|
| 1 | Installer two-column layout and Unsloth lock | Configuration and Review use two columns; Unsloth cannot be ticked when Incompatible; this 16 GB host shows Compatible | standard | medium |
| 2 | History chrome, selection, titles, delete overlay | Hairline, new-chat select, first-send titles, centered delete | standard | medium |
| 3 | Thinking pill geometry | Fixed width and inset glow | standard | medium |
| 4 | Settings Models compact cards | One facts line and icon actions | standard | medium |
| 5 | Shared picker rank and default | Four tabs match installer recommend order and default | strong | high |
| 6 | Image restyle identity | Last puppy recolors, same shot | frontier | high |
| 7 | SANA-Video layout and executor | Playable clip or a real missing-file error | frontier | max |
| 8 | Architecture refactor, known-gaps, CI/CD | Evidence file, local gate, publication after approval | frontier | max |

- [x] T001 Put Configuration Components and Features in two columns scripts/installer/src/nexus_installer/pages/configuration.py
- [x] T002 Put Review facts and models in two columns and rename estimated installation time scripts/installer/src/nexus_installer/pages/review.py
- [x] T003 Refresh Unsloth on show and disable the checkbox when Incompatible scripts/installer/src/nexus_installer/pages/configuration.py
- [x] T004 Show Review GPU as whole GB via display_vram_gb scripts/installer/src/nexus_installer/pages/review.py
- [x] T005 Add installer two-column and Unsloth-lock tests scripts/installer/tests/test_pages_qt.py
- [x] T006 Stabilize Phase 1 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-1-installer-layout.md
- [x] T007 Add a hairline between module tabs and Chats desktop/src/components/Sidebar.tsx
- [x] T008 Select the new chat after create desktop/src/modules/chat/FolderTree.tsx
- [x] T009 Title untitled Image and Video sessions on first send desktop/src/modules/image/ImageStudioPage.tsx
- [x] T010 Portal delete and archive dialogs to document.body desktop/src/modules/chat/FolderTree.tsx
- [x] T011 Add sidebar chrome, select, title, and dialog tests desktop/tests/sidebar-history-host.test.tsx
- [x] T012 Stabilize Phase 2 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-2-history-chrome.md
- [x] T013 Fix thinking-pill min-width and left inset desktop/src/components/agentState/AgentStateOrb.tsx
- [x] T014 Add thinking-pill geometry tests desktop/tests/sidebar-history-host.test.tsx
- [x] T015 Stabilize Phase 3 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-3-thinking-pill.md
- [x] T016 Put Settings facts on one nowrap line desktop/src/pages/settings/ModelsSettings.tsx
- [x] T017 Replace Download / Remove text with colored icons desktop/src/pages/settings/ModelsSettings.tsx
- [x] T018 Add Settings compact-card tests desktop/tests/settings-models-density.test.tsx
- [x] T019 Stabilize Phase 4 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-4-settings-cards.md
- [x] T020 Sort QuickModelSwitcher with host VRAM and recommend order desktop/src/shared/models/QuickModelSwitcher.tsx
- [x] T021 Default empty sessions to snapshot.recommendedByTask desktop/src/shared/models/selectionPolicy.ts
- [x] T022 Add picker-order tests for chat, agentic, image, and video desktop/tests/selectionPolicy.test.ts
- [x] T023 Stabilize Phase 5 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-5-picker-order.md
- [x] T024 Raise whole-image restyle img2img strength and identity prompt desktop/src/modules/image/followUpSource.ts
- [x] T025 Fail closed when lastOutputRef is missing on an unattached restyle desktop/src/modules/image/ImageStudioPage.tsx
- [x] T026 Add restyle strength and fail-closed tests tests/unit/core/image/replaceIntent.test.ts
- [x] T027 Stabilize Phase 6 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-6-image-restyle.md
- [x] T028 Expand sana-video-2b-720p weights to a complete Diffusers tree core/registry/catalog.json
- [x] T029 Execute SANA-Video with the SANA-Video pipeline, not WanPipeline runtimes/diffusion/pipelines/real_execute.py
- [x] T030 Add SANA-Video layout and executor tests tests/python/diffusion/test_real_execute.py
- [x] T031 Stabilize Phase 7 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-7-sana-video.md
- [x] T032 Refactor release-touched architecture and quote the scan docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.3-field-density.md
- [x] T033 Reconcile all active known gaps docs/archive/v2/v2.4/known-gaps.md
- [x] T034 Verify living documentation architecture docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.3-field-density.md
- [x] T035 Report git-tree hygiene docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.3-field-density.md
- [x] T036 Reconcile terminal CI/CD coverage docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.3-field-density.md
- [x] T037 Perform an independent Goal-vs-codebase review docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.3-field-density.md
- [x] T038 Execute last-phase human testing suggestions docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.3-field-density.md
- [x] T039 Run full local testing and stabilization docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.3-field-density.md
- [ ] T040 Publish and integrate only after explicit approval docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.3-field-density.md

## Field map (screenshots to code)

| Shot | Symptom | Primary cause in tree |
|---|---|---|
| 1 | Unsloth Incompatible still ticked; single-column Configuration | `ConfigurationPage` builds the badge in `__init__` before GPU detection; checkbox stays enabled; Components and Features share one `QVBoxLayout` |
| 2 | Review is a tall single list; GPU is 16384 MB; "Estimated time" | `review.py` `_rebuild_summary` is one HTML column; raw `s.vram_mb` |
| 3 | Image chat stays "New chat" after first puppy | `ensureSession` returns the already-created id and never sets `titleJobRef` |
| 4 | "Make the puppy black" reprints tan fur | Restyle path uses `FOLLOWUP_IMG2IMG_STRENGTH = 0.45` |
| 5 | Searching glow cropped; pill width jumps | `AgentStateOrb` width is `auto`; pill flush to transcript left; parent clip |
| 6 | Delete dialog flush left and cropped | Dialog rendered inside sidebar-portaled `FolderTree`; `position:fixed` containing block |
| 7 | Video fails instantly: missing model_index.json | `video_execute` Wan layout check; SANA catalog files omit the Diffusers index |
| 8 | Settings pills wrap; gray Download/Remove | `pillRowStyle.flexWrap = wrap`; text buttons |
| 9 | Agents picker shuffled; gpt-oss selected | `QuickModelSwitcher` without `hostVramGB`; favorite/last id beats `recommendedByTask.agentic` |

---

## Phase 1: Installer Two-Column Layout and Unsloth Lock

**Goal**: Configuration and Review use the horizontal space, Unsloth cannot be selected when Incompatible, and this 16 GB NVIDIA host shows Compatible after detection.
**Prerequisites**: None.
**Stability Gate**: Pytest proves two-column widgets, Unsloth disabled when Incompatible, Compatible after a 16384 MB NVIDIA state, Review GPU as 16 GB, and the renamed time label.
**Recommended model tier**: standard
**Recommended effort level**: medium
**Rationale**: Qt layout and a stale-badge refresh; no new runtime.

### Sub-tasks

#### 1.1 - Configuration two columns

**Objective**: Components left, Features right.

**Failure modes**: Malformed state still renders both columns with empty lists. A missing VS Code widget must not drop Features. Narrow window stacks columns vertically rather than clipping checkboxes.

**Prompt**:
> In `scripts/installer/src/nexus_installer/pages/configuration.py`, put Components and Features in a two-column `QHBoxLayout` (Components left, Features right: thinking, memory, Unsloth). Keep Ollama URL, VS Code, and the rest full width below. Do not change provisioning flags. Add a pytest that the two section heads sit in different columns.

#### 1.2 - Review two columns and copy

**Objective**: Facts left, models right. Rename estimated installation time. GPU is whole GB.

**Failure modes**: Zero models still show the right column as "none selected". Missing GPU name prints None detected without crashing. `vram_mb` 0 must not display 0 GB as if it were 16384.

**Prompt**:
> In `scripts/installer/src/nexus_installer/pages/review.py`, rebuild the summary as two columns: left is install path, components, GPU (`display_vram_gb` plus "GB VRAM"), estimated disk usage, and the label "Estimated installation time"; right is the selected model list. Pin 16384 -> 16 GB. Keep the internet callout.

#### 1.3 - Unsloth lock and refresh

**Objective**: Badge follows live GPU state. Incompatible cannot be ticked.

**Failure modes**: Construct-time `vram_mb=0` must not stick after detection. A Compatible host that later reports 0 VRAM unchecks and disables. Checking must be a no-op when disabled (state.install_unsloth stays false).

**Prompt**:
> In `configuration.py`, refresh Unsloth on `showEvent` (and after GPU fields change). Host-ok: vendor nvidia and `display_vram_gb(vram_mb) >= 16`. If not host-ok: badge Incompatible, checkbox disabled, `install_unsloth` false, no "you can still tick this" warning. If host-ok: Compatible and enabled. Pytest: construct with empty GPU then apply NVIDIA 16384 and show the page -> Compatible and enabled. Pytest: 8 GB NVIDIA -> Incompatible, cannot check.

#### 1.4 - Testing and Stabilization

**Objective**: Installer tests green; local commit only.

**Prompt**:
> Generate automated tests for Phase 1 (two-column layout, Unsloth lock, Review GB and time label). Run `uv run pytest tests` in `scripts/installer` and ruff on touched files. Record CI IMPACT: existing `installer-tests.yml` already covers `scripts/installer/**`. Do not ask for human testing. Run session history to `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-1-installer-layout.md`, then one local commit. Do not push.

### Phase 1 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 2

---

## Phase 2: History Chrome, Selection, Titles, and Delete Overlay

**Goal**: Tabs and chats are visually split, New chat opens empty and selected, first Image prompt names the session, and delete is centered.
**Prerequisites**: Phase 1.
**Stability Gate**: Desktop tests prove the hairline, create-and-select, first-send title on a pre-created untitled studio session, and a dialog node under `document.body`.
**Recommended model tier**: standard
**Recommended effort level**: medium
**Rationale**: Sidebar and FolderTree behavior with a known portal bug.

### Sub-tasks

#### 2.1 - Hairline under module tabs

**Objective**: Subtle horizontal rule between Chatbot/Agents/Images/Videos and Chats.

**Failure modes**: Compact icon rail still shows a short rule or hides it without overlapping icons. Settings route has no Chats list so no extra gap. Missing theme token falls back to `--border-1`.

**Prompt**:
> In `desktop/src/components/Sidebar.tsx`, add a 1px hairline (`color-mix` on `--border-1`, low opacity) between the four module tabs and the history slot. Do not add a second title. Test that the rule is present on `/images` and absent or collapsed on `/settings`.

#### 2.2 - New chat selects

**Objective**: Plus-button create selects the new row and shows an empty transcript.

**Failure modes**: Create failure must not leave a stale selection. Creating while another chat is generating must not steal that job. Empty CTA "Start a new chat" uses the same select path.

**Prompt**:
> In `FolderTree.onCreateChat`, after a successful create, select that chat (call `onSelect` / the same path as a row click) instead of only entering rename. Image and Video `StudioHistoryPane` must hydrate an empty transcript. ChatPage already `setActiveChat` on its own create; do not double-create. Test: click new chat, active id is the created id, message list is empty.

#### 2.3 - Title untitled studio sessions on first send

**Objective**: "Generate image of a puppy" does not stay "New chat".

**Failure modes**: User-renamed titles must not be overwritten. Empty prompt keeps "New chat". Sidecar title timeout keeps the immediate fallback, not "New chat". A pre-created untitled session must title on first send.

**Prompt**:
> Image and Video currently set `titleJobRef` only when `ensureSession` creates a session. Screenshot 3 created the session first, so the first send skipped titling. On first user send, if the session title is still `DEFAULT_SESSION_TITLE` and `userRenamed` is not true, run `applyImmediateFallbackTitle` then `refineGeneratedTitle` (same as ChatPage `isFirstTitledSend`). Video Lab gets the same. Test a pre-created "New chat" Image session whose first prompt becomes a non-default title.

#### 2.4 - Centered delete overlay

**Objective**: Confirm delete/archive is a full-window modal.

**Failure modes**: Missing `document.body` in jsdom renders in place without throwing. Escape and Cancel still close. Multi-select copy is unchanged.

**Prompt**:
> In `FolderTree.tsx`, `createPortal` the delete and archive dialogs to `document.body`. Keep `modalBackdropStyle` (`position:fixed; inset:0; justifyContent:center`). Test `folder-tree-confirm-delete` is a child of `document.body` and is not clipped to the sidebar host.

#### 2.5 - Testing and Stabilization

**Prompt**:
> Generate automated tests for Phase 2. Run desktop lint, `tsc --noEmit`, and focused plus coverage as in prior v2.4.2 phases. Record CI IMPACT: existing `shell-build.yml` covers `desktop/**`. Session history `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-2-history-chrome.md`. One local commit. Do not push.

### Phase 2 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 3

---

## Phase 3: Thinking Pill Geometry

**Goal**: The pending pill does not change width as captions rotate, and the glow is fully visible.
**Prerequisites**: Phase 2.
**Stability Gate**: Tests pin min-width to the longest caption and a non-zero left inset; overflow remains visible on the pill host.
**Recommended model tier**: standard
**Recommended effort level**: medium
**Rationale**: CSS geometry; jsdom cannot prove pixels (record MT if needed).

### Sub-tasks

#### 3.1 - Fixed width and inset

**Objective**: Constant pill width; glow inside the transcript.

**Failure modes**: Reduced-motion first caption still uses the same min-width. Hero studio pending (no rotate) is unchanged. Missing caption list falls back to `Searching...` as the width driver.

**Prompt**:
> Screenshot 5 shows Searching... glow cropped and width jumping vs Thinking. In `AgentStateOrb.tsx` pending-pill mode, set min-width from the longest `PENDING_CAPTIONS` entry plus orb and horizontal padding so width does not change. Add left margin/inset (do not put `overflow:hidden` on an ancestor that clips `box-shadow`). Keep overflow visible on the host. MessageBubble pending row must not clip. Export the min-width constant for tests.

#### 3.2 - Testing and Stabilization

**Prompt**:
> Assert min-width uses `Searching...` (longest caption) and inset is non-zero. Do not claim pixel containment in jsdom; if a real `getBoundingClientRect` crop cannot be proven, keep/update v2.4.2 MT-1. Desktop lint, tsc, coverage. CI IMPACT: `desktop/**` already in shell-build. History `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-3-thinking-pill.md`. One local commit. Do not push.

### Phase 3 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 4

---

## Phase 4: Settings Models Compact Cards

**Goal**: Each model card is one facts line plus a full-width description and icon actions.
**Prerequisites**: Phase 3.
**Stability Gate**: Tests prove one nowrap facts row, no wrapping origin pills in the dense row, green downloaded, red trash, blue download, description next to actions.
**Recommended model tier**: standard
**Recommended effort level**: medium
**Rationale**: Card CSS and lucide icons; no catalog schema change.

### Sub-tasks

#### 4.1 - One facts line and full-width description

**Objective**: Stop wrapping Company/Country/Context onto extra rows.

**Failure modes**: Missing display fields omit chips rather than leaving empty pills. Long names ellipsize, they do not wrap the facts row. Details still exposes the full metadata.

**Prompt**:
> In `ModelsSettings.tsx`, the dense card row is nowrap: size, VRAM compatibility, recommendation (and installed state). Move origin, license, agentic, context, multimodal, guardrails, released into Details. Description uses remaining width in a grid beside actions (`minmax(0,1fr)`), not a short column that wraps early. Keep every fact accessible in Details.

#### 4.2 - Icon actions

**Objective**: Green downloaded, red trash, blue download.

**Failure modes**: Icon-only buttons keep accessible names (Downloaded, Remove, Download). Disabled Downloaded is not clickable. Remove still confirms through the existing path.

**Prompt**:
> Replace the gray Downloaded / Remove / Download text buttons with lucide icons already used in the desktop app: a muted green Downloaded (non-button or disabled), a slight-red trash for Remove, a slight-blue download icon for Download. Accessible names stay the verbs. Test colors via style or test ids, not screenshots.

#### 4.3 - Testing and Stabilization

**Prompt**:
> Extend `desktop/tests/settings-models-density.test.tsx`. Lint, tsc, coverage. CI IMPACT: desktop tests already covered. History `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-4-settings-cards.md`. One local commit. Do not push.

### Phase 4 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 5

---

## Phase 5: Shared Picker Rank and Default

**Goal**: Chat, Agents, Image, and Video dropdowns use installer recommend order; an empty session defaults to the required/recommended model for that task.
**Prerequisites**: Phase 4.
**Stability Gate**: Tests prove 16 GB agentic default is `gemma-4-12b-it-gguf` (not gpt-oss 20B) when that id is installed, and option order matches recommend order.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Four pickers plus `selected-models.json` ordering; easy to break a session's remembered model.

### Sub-tasks

#### 5.1 - Switcher sort

**Objective**: `QuickModelSwitcher` is not catalog-file order.

**Failure modes**: Null `hostVramGB` must not treat the host as CPU-none. Missing snapshot falls back to `visibleModelsOnTab` with detected VRAM, not shuffle. Get more models stays last.

**Prompt**:
> Screenshot 9 lists LFM, Gemma, Qwen, gpt-oss in non-recommend order because `CodingPage` passes `listedModels` into `QuickModelSwitcher` without `hostVramGB`. Pass host VRAM (from the models client / GPU probe already used by Settings) and sort with `visibleModelsOnTab` / `catalog_tab_sort` recommend order. Chat, Image, and Video switchers get the same props. Do not reorder by `installedForTask` snapshot install order when a recommend order exists.

#### 5.2 - Empty-session default

**Objective**: Default is installer required/recommended, not a sticky favorite of gpt-oss.

**Failure modes**: An existing session keeps `session.modelId`. User change in that session still writes favorite. Missing recommended id falls back to the first recommend-ordered ready model. Uninstalled recommended id skips to the next ready recommend.

**Prompt**:
> `resolveDefaultId` prefers `favorite` over `recommended`, and Agents screenshot 9 selected gpt-oss 20B on an empty workspace. For a new empty session, default is `snapshot.recommendedByTask[task]` (16 GB agentic: `gemma-4-12b-it-gguf` from `core/registry/recommended.json`). Do not apply a leftover favorite until the user changes the picker in that session (`userChangedModelRef`). Confirm installer write of `recommendedByTask` matches `recommended.json` for the detected tier. Test empty agentic session default.

#### 5.3 - Testing and Stabilization

**Prompt**:
> Add tests for recommend-order options and empty-session default across chat, agentic, image, and video. Lint, tsc, coverage. CI IMPACT: desktop already covered. History `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-5-picker-order.md`. One local commit. Do not push.

### Phase 5 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 6

---

## Phase 6: Image Restyle Identity

**Goal**: Unattached "Make the puppy black" restyles the last PNG so fur is black and composition stays.
**Prerequisites**: Phase 5.
**Stability Gate**: Tests prove restyle strength 0.70, identity prompt, no SAM2, and fail-closed without `lastOutputRef` (no txt2img).
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Field proof that 0.45 img2img is a no-op on color; packed NVIDIA observation stays last-phase.

### Sub-tasks

#### 6.1 - Stronger restyle

**Objective**: Color change is visible.

**Failure modes**: Object-local inpaint stays 0.45. Missing last PNG writes an error, does not txt2img a new dog. Malformed strength is ignored in favor of the constant.

**Prompt**:
> Screenshot 4 is the same tan puppy after "Make the puppy black". `parseReplaceIntent` already marks that as `scope: "image"`. In `followUpSource.ts` split strengths: `FOLLOWUP_IMG2IMG_STRENGTH` remains 0.45 for generic follow-up; add `RESTYLE_IMG2IMG_STRENGTH = 0.70` for whole-image restyle. Build the img2img prompt from the restyle intent (keep composition, change fur to black), not the raw user line alone. `usesSegment` stays false. If `lastOutputRef` is missing, fail closed with a written error. Do not sample txt2img.

#### 6.2 - Testing and Stabilization

**Prompt**:
> Extend replace-intent and ImageStudioPage tests: phrase "Make the puppy black", strength 0.70, no SAM2, missing last ref errors. Lint, tsc, coverage. Packaged NVIDIA before/after is last-phase only (update MT-3). History `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-6-image-restyle.md`. One local commit. Do not push.

### Phase 6 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 7

---

## Phase 7: SANA-Video Layout and Executor

**Goal**: Opted-in `sana-video-2b-720p` either runs a SANA-Video pipeline on a complete local tree or names the missing file; it never uses WanPipeline.
**Prerequisites**: Phase 6.
**Stability Gate**: Python tests prove SANA-Video does not take the Wan `model_index.json` path; a complete fixture runs the SANA executor (stubbed); an incomplete tree raises `model-layout-invalid` that names the missing path. Wan `wan2.1-t2v-1.3b` still uses WanPipeline.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Wrong pipeline class plus an incomplete weights manifest; high blast radius for every video id.

### Sub-tasks

#### 7.1 - Complete SANA-Video weights list

**Objective**: Catalog files include the Diffusers index and required components, not only transformer shards.

**Failure modes**: Unknown extra files on disk are ignored. A listed file with empty sha is still required to exist. Snapshot download, if used, stays `local_files_only` at generate time.

**Prompt**:
> Screenshot 7: `sana-video-2b-720p is missing model_index.json and complete Diffusers components`. `core/registry/catalog.json` `weights.files` for that id lists only two transformer shards. Inspect the Hugging Face repo `Efficient-Large-Model/SANA-Video_2B_720p_diffusers` and add every file the SANA-Video Diffusers loader needs (`model_index.json`, scheduler, tokenizer, VAE, transformer index, and so on) with sha256. Do not pretend Wan files satisfy SANA. Keep 16 GB default video as `wan2.1-t2v-1.3b`.

#### 7.2 - Dedicated SANA-Video execute path

**Objective**: `sana_video.py` must not call Wan `video_execute`.

**Failure modes**: Missing CUDA still `RuntimeNotReady` accelerator. Missing weights `weights-missing`. Missing index names the path. Wan ids must not enter the SANA branch. Import failure of the SANA class is `RuntimeNotReady`, not a traceback complete event.

**Prompt**:
> `runtimes/diffusion/pipelines/sana_video.py` registers `real=real_execute.video_execute`, and `video_execute` always requires `model_index.json` then `WanPipeline.from_pretrained`. Add `sana_video_execute` (or branch inside `video_execute` on `sana-video*` ids) that loads the SANA-Video Diffusers class for that repo, `local_files_only=True`. Do not use WanPipeline for SANA. Fail closed with `model-layout-invalid` that names the missing file. Wan 1.3B stays on WanPipeline. Wire `select_executor` so CI keeps the stub.

#### 7.3 - Testing and Stabilization

**Prompt**:
> Tests: SANA incomplete tree names `model_index.json` (or the actual missing file); SANA complete fixture does not call WanPipeline; Wan fixture still does. Catalog digest tests if catalog.json hashes are gated. Python pytest `tests/python/diffusion`, installer catalog invariants if listed. CI IMPACT: root CI pytest runtimes plus catalog paths in installer-tests. History `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.3-phase-7-sana-video.md`. One local commit. Do not push. Packaged NVIDIA SANA clip is last-phase observation.

### Phase 7 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 8

---

## Phase 8: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, its living docs architecture current, and its pipeline reconciled against the canonical contract, with independent evidence that the plan Goal landed, then publish and integrate the completed branch.
**Prerequisites**: All prior phases.
**Stability Gate**: `docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.3-field-density.md` exists with one quoted section per duty below. A missing Goal, a Goal-review miss without a recorded known-gap, or a missing evidence section blocks `/update release`.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, Goal-vs-codebase review, known-gap reconciliation, and release gating carry high context volume and blast radius.

### Sub-tasks

#### 8.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout and quote the scan.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure, then refactor toward a clean, intuitive layout via [[project-refactor]] and [[docs-layout-refactor]] (propose-then-apply, with confirmation; repair every reference for anything that moves). Write the detector output into `docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.3-field-density.md` under `## Architecture refactor`. An empty finding is allowed only when the scan output is quoted.

#### 8.2 - Known-gaps reconciliation
**Objective**: Reconcile this version and every other still-open known-gaps file.
**Prompt**:
> Via [[known-gaps-tracker]], reconcile this version's known gaps AND every other `docs/**/known-gaps.md` whose Status is in-progress or whose Open Items remain. Glob both canonical (`docs/releases/v<MAJOR>/v<MAJOR>.<MINOR>/known-gaps.md`) and legacy layouts. If a file is unreachable, record the glob result and continue with what was found. Write the disposition into the evidence file under `## Known-gaps reconciliation`. Close or rewrite v2.4.2 MT-1 through MT-6 only with new observation from this cycle. Carry v2.4.1 DF-1 and DF-3 through DF-7 unless new evidence.

#### 8.3 - Living docs architecture
**Objective**: Prove handbooks, decisions, and generated HTML match the required living tree.
**Prompt**:
> Check `docs/handbooks/` (markdown source of truth, generated `html/`, one atlas walkthrough, technical companions for key components), `docs/decisions/`, and living `docs/README.md` / `docs/DEVLOG.md` / `docs/todos.md`. Self-gated: never invent `docs/testing/` or `docs/validation/`. Markdown wins if HTML disagrees; regenerate or fail the check. Quote the check in the evidence file under `## Living docs architecture`. Details live in [[docs-layout-refactor]].

#### 8.4 - Git-tree hygiene
**Objective**: Report branch and repository-settings hygiene without deleting anything.
**Prompt**:
> Run `python scripts/check_release_preconditions.py --branches --repo-settings`. Quote the output in the evidence file under `## Git-tree hygiene`. Report only; never delete branches.

#### 8.5 - Terminal CI/CD reconciliation
**Objective**: The repository's pipeline is compared field by field against the canonical contract, and every difference is either applied with approval or recorded as a known gap.
**Prompt**:
> Invoke [[cicd-architect]] in existing-pipeline comparison mode and run its six steps in order: DETECT the active CI provider (recording "none detected" rather than assuming one); COMPARE the existing pipeline against every field of the canonical contract - repository-native profiles, event separation, runner selection, the always-resolving aggregate required check, permissions, immutable action or plugin references, caching, concurrency, path scoping, artifact retention, structured reports, deployment boundaries, and failure recovery; PROPOSE each difference with its cost, its risk, and the smallest change that closes it; obtain explicit APPROVAL per change (silence is not approval); APPLY the approved changes and re-run the local gate; and RECORD every declined or environment-only difference as a known gap with an owner and a next step via [[known-gaps-tracker]]. The comparison concludes PASS only when every required field has observable evidence; a green pipeline run is not evidence, because a pipeline that checks nothing is green for the wrong reason. If this repository ships more than one installer, run or add a declarative cross-installer parity gate covering distributed artifacts, supported platforms, named capability counterparts, and external-tool fallbacks, then execute the real installers on their target operating systems with identical postconditions. Cross-link [[platform-contract-verification]] so discovery and delivery are checked in the same pass. If the repository ships zero or one installer, record a silent no-op and continue. Quote the per-field comparison result in the evidence file under `## CI/CD coverage`. QG-5 from v2.4.2 (develop-targeted PRs) remains unless this phase obtains approval to apply it.

#### 8.6 - Independent Goal-vs-codebase review
**Objective**: Prove the plan Goal landed in the codebase, not that checkboxes were ticked.
**Prompt**:
> Re-read the plan header Goal and the Goals First / Definition of Done. Inspect the codebase as if this agent had not implemented the phases. Write `## Goal-vs-codebase review` in `docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.3-field-density.md` listing: the plan Goal restated; the code/docs artifacts that satisfy it; and any gap. A missing or malformed Goal is a fail-closed finding that names the missing Goal. Completing every prior-phase sub-task is not evidence the Goal landed. A miss is a blocking finding or a new known-gap, not a pass.

#### 8.7 - Human/manual testing suggestions (last phase only)
**Objective**: Ask a human to exercise only what automated tests cannot cover, now that the feature is complete.
**Prompt**:
> Emit human/manual testing suggestions. Do not invent a fake walkthrough. Operator items: (1) Configuration two columns and Unsloth Compatible on this 16 GB NVIDIA host, Incompatible checkbox disabled on a simulated 8 GB host if available; (2) Review two columns, 16 GB GPU line, Estimated installation time; (3) hairline under tabs; (4) New chat selects empty; (5) Image first prompt titles the row; (6) Make the puppy black restyles fur; (7) thinking pill glow at 100% and 150% zoom; (8) delete dialog centered; (9) Settings one facts line and icon colors; (10) Agents empty session defaults to Gemma 4 12B on this install; (11) SANA-Video either plays or names a real missing file.

#### 8.8 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and the complete LOCAL gate is green before anything is published.
**Prompt**:
> Run the full local validation/test suite, confirm the refactor changed no behavior, and iterate until clean. This gate is local by construction: it must pass before the branch is published, so it cannot depend on a remote run. Quote the suite in the evidence file under `## Full-suite testing and stabilization`. Generate a session-history entry for this phase.

#### 8.9 - Publication and integration
**Objective**: Publish the completed branch once, validate the merge result remotely, and hand a green integration to the release flow.
**Prompt**:
> Create the final local commit for this phase. Then present the resolved branching model, remote, branch name, and pull-request target, and obtain EXPLICIT approval before the plan's first branch push. This branch already has [PR 58](https://github.com/bendourthe/Nexus-AI/pull/58) against `develop`; an approved push updates that PR rather than opening a second one unless 58 is closed. Wait until every required check reaches a terminal state. A red check REOPENS this phase: classify the failure, reproduce it locally, apply the narrow fix, re-run the local gate, update known gaps and session history, then amend the final commit or add one narrowly scoped stabilization commit and push again with approval. Never re-run a red check without a local reproduction; a re-run without one is a guess. Merge only after every required check is green and the user approves. Confirm the post-merge workflow performed only its intended smoke, publication, or provenance work and did not rerun the complete suite. Only then hand off to `/update release`, which owns the version bump, changelog, tag, push, and GitHub Release behind its own confirmation gates. Quote the required-check results in the evidence file under `## Publication and integration`.

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| | | |

### Phase 8 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run until T040
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [ ] Branch push, pull request, and remote CI occurred only after explicit approval
- [ ] Ready for `/update release` after green integration
