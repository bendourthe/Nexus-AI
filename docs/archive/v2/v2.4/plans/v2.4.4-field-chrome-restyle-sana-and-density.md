# Plan -- Field Chrome, Restyle, SANA, and Density

**Project**: Nexus AI Studio
**Version**: v2.4.4
**Slug**: field-chrome-restyle-sana-and-density
**Filename**: v2.4.4-field-chrome-restyle-sana-and-density.md
**Plan Type**: Feature / corrective enhancement
**Status**: Phases 1-7 complete. Published and integrated: PR 58 is green (42 checks pass, 0 fail) on head 4b1771da. Merge and /update release await separate explicit approval.
**Created**: 2026-08-31
**Field evidence**: Packaged post-v2.4.3 operator screenshots 1-6 (2026-08-31 rebuild): Chat/Agents Searching pill too far right with left glow cropped; hairline not vertically centered and selected-row title inset; Image restyle reprints the tan puppy; SANA-Video ImportError for SanaVideoPipeline then Wan Shaping freeze; Settings search on a second row with tall cards and scattered icons; Details dumps redundant ID / Also agentic / License / Backend model text
**Goal**: A 16 GB NVIDIA packaged studio keeps Chat/Agents pending chrome aligned with equal transcript gutters and an uncropped glow, centers the sidebar hairline and pulls history titles to the selection rail, offers Delete All and Archive All, restyles the last puppy to black fur instead of reprinting it, runs SANA families on a Diffusers pin that actually exports their pipeline classes, keeps Image/Video pending animation alive until a clip or a written error, and densifies Settings model cards so tabs sit with search, actions share one row, height hugs content, and Details is pills plus a Best for list.

## Overview

v2.4.3 landed locally (Phases 1-8, T040 still waiting on an explicit push). The rebuilt unsigned installer was smoked, then field-tested on this host. The installer two-column work, first-send titles, centered delete dialogs, picker order, and SANA-Video *executor* path are not the misses in these screenshots. What failed in the packaged window is the next layer: pill geometry over-corrected to the right, history chrome still uneven, restyle still a reprint, SANA-Video still an import error because `diffusers==0.34.0` has no `SanaVideoPipeline`, Wan generation froze the shell, and Settings cards still waste a row.

This plan is a third field pass on `feat/v2.4.1-field-reliability`. It does not reopen the v2.4.2 sidebar host, empty-video honesty gate, VS Code merge, installer two-column layout, Unsloth lock, or picker recommend order unless a later screenshot forces it. v2.4.3 T040 remains a separate publication hold until the user approves a push. Package version stays 2.4.1 until `/update release` after a green integration.

No phase except the last pushes, opens a pull request, or starts remote CI. Each earlier phase ends with automated tests, session history, and one local commit.

Discovery interview: skipped as a live questionnaire. The operator named the version, the six screenshot goals, and the 16 GB NVIDIA Windows host in one message. Assumptions are listed under Architecture Decisions.

## Goals First

**Target problem**: After the v2.4.3 rebuild, the packaged studio still looks uneven in Chat and the sidebar, reprints the last Image instead of restyling it, cannot import SANA-Video from the installed venv, freezes the shell while Wan runs, and wastes Settings card space with redundant Details copy.

**Target persona**: A privacy-focused Windows user with a single consumer NVIDIA GPU (this host: RTX 3080 Ti Laptop, 16384 MB) who expects Chat chrome, sidebar history, Image follow-ups, Video generation, and Settings cards to stay usable and honest in one sitting.

**Observable definition of done**:

- Chat/Agents pending pill sits on the same left gutter as completed assistant bubbles. That gutter matches the right gutter of user bubbles. The glow is fully visible at 100% and 150% zoom (not cropped on the left).
- The hairline between Videos and Chats has equal space above and below the 1px rule.
- Selected history titles sit close to the blue rail (no 12px dummy indent on chat rows).
- Chats header offers Archive All and Delete All, each behind one centered confirm, covering the chats in that pillar list.
- "Make the puppy black" with no re-attach produces a new PNG whose fur reads black, same composition. The new turn's image bytes are not a byte-for-byte copy of the previous PNG. The job is img2img (or a documented fail-closed reason), not a silent txt2img of the first prompt.
- Opt-in SANA-Video either plays a clip or fails closed with `diffusers-missing` / a real missing-file path. It does not raise a raw `ImportError: cannot import name 'SanaVideoPipeline'`. Image SANA is probed the same way for `SanaPipeline`.
- Wan (and any long Image/Video job): the window stays interactive, the pending orb keeps animating, and the caption stays live until a playable output or a written error. A few minutes of GPU work is not a frozen shell.
- Image/Video pending copy is one of Creating... / Crafting... / Generating..., picked per prompt (not Shaping...).
- Settings Models: category tabs and the search field share one row. Star, downloaded/check, and delete sit on one centered horizontal row. Card height hugs its content.
- Details shows fact pills (label visually distinct from value), then a Best for heading with strength bullets. No ID/task line, no "Also agentic", no repeated License, no Backend model line.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at docs/archive/v2/v2.4/constitution.md - skipping check. Recommend running /constitution to establish project principles.

Governing contracts used instead: `AGENTS.md` (core must not import modules; local-only models; evidence tiers `not_observed != absent`), module authorship, and `docs/archive/v2/v2.4/known-gaps.md`. Carry v2.4.3 MT-1 through MT-7: several are now field-observed as still failing and become this cycle's work. Do not close a known gap without a new observation.

## Knowledge-base grounding

No `STRATEGY.md` and no `docs/solutions/` store exist in this tree, so grounding is the v2.4.3 plan, its last-phase evidence, `docs/archive/v2/v2.4/known-gaps.md`, and the six packaged screenshots. This cycle does not reopen the sidebar history host, empty-video honesty gate, VS Code merge, installer two-column pages, or picker recommend order.

## Current model map

**Model map status**: fresh as of 2026-08-31; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.5-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/about-claude/models/overview
- OpenAI: https://developers.openai.com/api/docs/models
- Google: https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/, https://ai.google.dev/gemini-api/docs/changelog
- Cursor: https://cursor.com/docs/models-and-pricing, https://cursor.com/docs/models/grok-4-6

## Architecture Decisions

1. **Equal transcript gutters, not stacked insets.** Screenshot 1 is asymmetry, not only glow crop. User bubbles already sit `flex-end` with a max width. Assistant pending currently adds `paddingInline`, extra `paddingLeft` (`space-2` + `PENDING_PILL_INSET_PX`), and `AgentStateOrb` `marginLeft: 12`. Collapse to one transcript gutter token used on both sides of the pane. The pending pill starts on that left gutter. Glow lives inside the gutter (padding on the list, overflow visible on the row). Do not keep a second 12px pill margin.

2. **Hairline is vertically centered in the gap.** Today the rule uses `margin: var(--space-1) 0` while FolderTree's Chats header adds `padding-top: var(--space-2)`, so the Videos-to-line gap is smaller than the line-to-Chats gap. Equal padding above and below the 1px rule. Reduce FolderTree header padding-top so the title does not add a second top gap.

3. **Selected chat titles sit next to the rail.** Expanded chat rows reserve a 12px dummy span (folder chevron slot) plus `gap` plus `padding`. Drop the dummy span on chat rows. Keep the 4px `borderLeft` rail. Folder rows keep the chevron.

4. **Archive All / Delete All are header actions.** One confirm each, portaled to `document.body` like the per-row dialogs. They act on the chats in the current pillar tree (not other pillars, not already-archived Settings rows). Implementation loops the existing `archiveChat` / `deleteChat` clients. Empty list disables the actions.

5. **Restyle must change pixels, not only raise strength.** v2.4.3 already set `RESTYLE_IMG2IMG_STRENGTH = 0.7` and `restylePromptFor`. Screenshot 3 is an exact reprint, which is not explained by a mild strength miss alone. This phase traces mode, prompt, source digest, strength, and output digest through Image Studio, the sidecar, and `image_execute`. Fail closed if source bytes are missing. Never attach the previous PNG to the new assistant turn. Pass `generator`/`seed` into the pipeline (today `image_execute` ignores seed). Fix `strength=ctx.params.strength or 0.75` so a real 0 is not rewritten. Parser accepts trailing punctuation on "Make the puppy black". If img2img still returns a near-clone, raise restyle strength (target 0.85) and keep the identity prompt. Unit tests assert output hash != source hash on the restyle path (stub pipeline that echoes the source must fail).

6. **SANA families require a Diffusers pin that exports their classes.** Field error: `cannot import name 'SanaVideoPipeline' from 'diffusers'` in the packaged venv. Catalog layout and `sana_video_execute` are not the miss. `runtimes/diffusion/runtime-lock.json` and `scripts/installer/build/versions.lock.json` pin `diffusers==0.34.0`. Hugging Face documents `SanaVideoPipeline` on the 0.37.1 docs. Bump the lock to a verified release that imports both `SanaPipeline` and `SanaVideoPipeline` against the existing torch 2.3.0 + cu121 stack, or record a known gap if that pair cannot coexist. Missing class stays `RuntimeNotReady` kind `diffusers-missing` with a human sentence, never a raw ImportError traceback in the bubble. Image SANA is included in the same pin probe.

7. **Generation RPC must not run CUDA on the stdin loop.** `runtimes/diffusion/main.py` `dispatch` calls the handler to completion before reading the next line. `PipelineRunner.execute` / `VideoPipelineRunner` run `pipe(...)` inline, so Node `call("diffusion.video.text2video")` waits for the whole job, Python cannot emit heartbeats, and a 16 GB laptop GPU load then starves the compositor (Shaping freezes, shell unresponsive). Accept the job, return `jobId` immediately, run CUDA on a worker thread, emit progress notifications on stdout, and keep the stdin reader alive for `drainEvents` / cancel. The UI pending state lasts until complete or a written error. CSS animation plus progress events is the liveness signal. If the worker dies, fail closed in the transcript.

8. **Studio pending captions are Creating / Crafting / Generating.** Chat/Agents keep Thinking / Searching / Working / Solving. Image and Video pending shuffle the studio list once per prompt (same rotator pattern, different copy). No "Shaping...".

9. **Settings cards hug content; actions are one row; search shares the tab row.** `ModelCard` actions are a column (star top, check/trash bottom), which stretches the grid and leaves empty description space. One horizontal `alignItems: center; justifyContent: center` action row. No min-height on the card beyond padding. Category tabs and `SearchInput` sit on one wrap row (`models-chrome`). Hidden "Search" label stays for a11y.

10. **Details is pills plus Best for.** Drop `ID: ...; task:`, `Also agentic`, repeated `License:`, and `Backend model:`. Pills keep the v2.2.9 grammar but render `Company:` (muted/weight) distinct from `Google` (foreground). Best for is a heading plus a `<ul>` of `item.strengths`. Optional one-line Why this one may stay if it is not a restatement of the pills; default is omit when it only repeats strengths or license.

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|---|---|---|---|---|
| 1 | Transcript gutters and thinking pill | Equal left/right gutters; uncropped glow; pill not drifted right | strong | high |
| 2 | History hairline, title inset, bulk actions | Centered hairline; titles next to the rail; Archive All / Delete All | frontier | high |
| 3 | Image restyle identity on the GPU | Follow-up changes fur; output bytes != source bytes | frontier | max |
| 4 | SANA family Diffusers pin | SanaPipeline and SanaVideoPipeline import, or a written diffusers-missing | frontier | max |
| 5 | Generation liveness and studio captions | Non-blocking jobs, live animation, Creating/Crafting/Generating | frontier | max |
| 6 | Settings Models density and Details | Tabs+search row; one action row; hug height; pills + Best for | strong | high |
| 7 | Architecture refactor, known-gaps, CI/CD | Evidence file, local gate, publication after approval | frontier | max |

- [x] T001 Put equal gutters on the transcript and pending pill desktop/src/shared/chat/MessageList.tsx
- [x] T002 Keep thinking-pill glow inside one inset desktop/src/components/agentState/captionRotator.ts
- [x] T003 Add transcript-gutter and pill tests desktop/tests/AgentStateOrb.test.tsx
- [x] T004 Stabilize Phase 1 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.4-phase-1-transcript-gutters.md
- [x] T005 Center the sidebar hairline between tabs and Chats desktop/src/components/Sidebar.tsx
- [x] T006 Pull expanded chat titles to the selection rail desktop/src/modules/chat/FolderTree.tsx
- [x] T007 Add Archive All and Delete All on the Chats header desktop/src/modules/chat/FolderTree.tsx
- [x] T008 Add history chrome and bulk-action tests desktop/tests/Sidebar.test.tsx
- [x] T009 Stabilize Phase 2 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.4-phase-2-history-chrome.md
- [x] T010 Trace restyle mode, source digest, strength, and output digest desktop/src/modules/image/ImageStudioPage.tsx
- [x] T011 Wire seed and non-clone restyle through image_execute runtimes/diffusion/pipelines/real_execute.py
- [x] T012 Add restyle identity tests tests/unit/core/image/replaceIntent.test.ts
- [x] T013 Stabilize Phase 3 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.4-phase-3-image-restyle.md
- [x] T014 Pin Diffusers to a release that exports SanaVideoPipeline runtimes/diffusion/runtime-lock.json
- [x] T015 Fail closed when SanaPipeline or SanaVideoPipeline is missing runtimes/diffusion/pipelines/real_execute.py
- [x] T016 Add SANA pin and import tests scripts/installer/tests/test_media_runtime_contract.py
- [x] T017 Stabilize Phase 4 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.4-phase-4-sana-diffusers-pin.md
- [x] T018 Run diffusion jobs off the JSON-RPC stdin loop runtimes/diffusion/main.py
- [x] T019 Keep Video/Image pending live until complete or a written error desktop/src/modules/video/VideoLabPage.tsx
- [x] T020 Rotate Creating/Crafting/Generating on studio pending desktop/src/components/agentState/captionRotator.ts
- [x] T021 Add liveness and studio-caption tests tests/python/diffusion/test_pipelines.py
- [x] T022 Stabilize Phase 5 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.4-phase-5-generation-liveness.md
- [x] T023 Put model category tabs and search on one row desktop/src/pages/settings/ModelsSettings.tsx
- [x] T024 Align star/downloaded/delete and hug card height desktop/src/pages/settings/ModelsSettings.tsx
- [x] T025 Render Details as labeled pills plus Best for bullets desktop/src/pages/settings/ModelsSettings.tsx
- [x] T026 Add Settings density and Details tests desktop/tests/settings-models-density.test.tsx
- [x] T027 Stabilize Phase 6 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.4-phase-6-settings-density.md
- [x] T028 Refactor release-touched architecture and quote the scan docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.4-field-chrome.md
- [x] T029 Reconcile all active known gaps docs/archive/v2/v2.4/known-gaps.md
- [x] T030 Verify living documentation architecture docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.4-field-chrome.md
- [x] T031 Report git-tree hygiene docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.4-field-chrome.md
- [x] T032 Reconcile terminal CI/CD coverage docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.4-field-chrome.md
- [x] T033 Perform an independent Goal-vs-codebase review docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.4-field-chrome.md
- [x] T034 Execute last-phase human testing suggestions docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.4-field-chrome.md
- [x] T035 Run full local testing and stabilization docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.4-field-chrome.md
- [x] T036 Publish and integrate only after explicit approval docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.4-field-chrome.md

## Field map (screenshots to code)

| Shot | Symptom | Primary cause in tree |
|---|---|---|
| 1 | Searching pill too far right; left glow cropped | `PendingMessage` stacks `paddingInline` + extra `paddingLeft` + `AgentStateOrb` `marginLeft: 12`; parent overflow still clips the shadow |
| 2 | Hairline closer to Videos than to Chats; large gap before the title | Hairline `margin` is `space-1` while FolderTree header adds `space-2` padding-top; chat rows keep a 12px chevron spacer |
| 2 | No Delete All / Archive All | FolderTree toolbar is new-folder + new-chat only |
| 3 | "Make the puppy black" reprints the tan puppy | Strength 0.7 landed in tests, not in observed pixels; `image_execute` ignores seed and uses `strength or 0.75`; possible silent txt2img or reused PNG bytes |
| 4 | Instant SANA-Video ImportError | Packaged venv is `diffusers==0.34.0`; `SanaVideoPipeline` is documented at 0.37.1 |
| 4 | Wan Shaping then frozen unresponsive shell | Python runs `pipe()` on the stdin JSON-RPC loop; no heartbeats; laptop GPU starves the compositor |
| 5 | Search under tabs; tall empty cards; icons scattered | Search is a sibling below `tablist`; actions are a column in a two-column grid |
| 6 | Details repeats ID, Also agentic, License, Backend model | `ModelCard` details still print those paragraphs under the pills |

---

## Phase 1: Transcript Gutters and Thinking Pill

**Goal**: Chat/Agents pending chrome uses the same left margin as assistant replies and the same right margin as user replies, with the glow fully visible.
**Prerequisites**: None.
**Stability Gate**: Desktop tests pin equal gutter tokens, a single inset (not stacked), overflow visible on the pending row, and min-width still driven by the longest chat caption.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Layout plus clipping across MessageList, MessageBubble, and AgentStateOrb; screenshot 1 is a field miss of v2.4.3 Phase 3.

### Sub-tasks

#### 1.1 - Equal gutters

**Objective**: Assistant pending and completed assistant bubbles share one left gutter; user bubbles share the matching right gutter.

**Failure modes**: Studio hero pending (Image/Video) must stay centered and must not inherit chat gutters. Tool cards stay left-aligned under assistant content. Compact media max-width is unchanged.

**Prompt**:
> Introduce one transcript gutter (reuse an existing space token or a named constant). Apply it as padding on the message list (or as matching margin on user `flex-end` and assistant `flex-start` rows). Remove stacked `paddingLeft` on `PendingMessage` that adds `PENDING_PILL_INSET_PX` on top of `paddingInline`. Chat pending aligns to the assistant gutter, not several inches into the pane.

#### 1.2 - Single uncropped glow

**Objective**: Pill glow is inside the gutter; no second 12px `marginLeft` on the orb.

**Failure modes**: 150% zoom still shows the full halo. Reduced-motion keeps a static caption. Studio hero orb has no pill chrome and must not pick up chat min-width.

**Prompt**:
> Keep `overflow: visible` on the pill host, pending row, and list item. Move leftover glow inset into the list gutter from 1.1. `PENDING_PILL_INSET_PX` becomes 0 or is deleted if the gutter is enough. Min-width still uses `longestPendingCaption()` (`Searching...`). Tests that pinned 12px inset are rewritten to the gutter constant.

#### 1.3 - Testing and Stabilization

**Prompt**:
> Generate automated tests for Phase 1. Run desktop lint, `tsc --noEmit`, and focused tests. CI IMPACT: existing `shell-build.yml` covers `desktop/**`. Session history `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.4-phase-1-transcript-gutters.md`. One local commit. Do not push.

### Phase 1 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] CI impact recorded against the canonical contract, with no remote CI run
- [ ] Session history generated for this phase
- [ ] One local commit created for this phase
- [ ] No branch push, pull request, or remote CI run occurred
- [ ] Ready to advance to Phase 2

---

## Phase 2: History Hairline, Title Inset, and Bulk Actions

**Goal**: The tab/history split is visually centered, selected titles sit next to the blue rail, and the operator can archive or delete the whole list from the header.
**Prerequisites**: Phase 1.
**Stability Gate**: Tests prove equal hairline spacing tokens, chat rows without the 12px dummy indent, and header Archive All / Delete All with a `document.body` confirm.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: One high signal is destructive bulk delete of the pillar's chats.

### Sub-tasks

#### 2.1 - Center the hairline

**Objective**: Distance from the bottom of Videos to the rule equals distance from the rule to the top of the Chats label.

**Failure modes**: Compact icon rail still shows a short rule that does not overlap icons. Settings has no history slot. Do not add a second "Chats" title.

**Prompt**:
> In `Sidebar.tsx`, give `sidebar-history-hairline` equal vertical margin (or a wrapper with equal padding). In `FolderTree` header, drop extra `padding-top` that doubles the gap under the rule. Tests can pin the style tokens; pixel-perfect packaged layout stays a last-phase operator item (updates v2.4.3 MT-2).

#### 2.2 - Pull titles to the rail

**Objective**: Selected chat title starts just after the 4px blue bar.

**Failure modes**: Folder chevrons remain. Nested folder depth spacer (`depth * 12`) remains. Collapsed rail marks are unchanged. Action icons (archive/edit/delete) stay right-aligned.

**Prompt**:
> Remove the 12px dummy `<span>` on expanded chat rows in `FolderTree.tsx`. Keep `borderLeft` 4px for selection. Reduce row `padding-left` if a gap remains. Test the selected chat label is the first text node after the rail, not after a 12px spacer.

#### 2.3 - Archive All and Delete All

**Objective**: Header actions for the current pillar history list.

**Failure modes**: Confirm copy is generic (no internal ids). Cancel leaves the list unchanged. Empty list disables both. Archive All must not delete. Delete All must not leave orphan folders unless that is existing per-row behavior. Coding / Image / Video / Chatbot each operate only on their client. Confirm dialogs portal to `document.body`.

**Prompt**:
> Add Archive All and Delete All next to the existing new-folder / new-chat toolbar (visible when the list is non-empty). Reuse `archiveChat` / `deleteChat`. One confirm each, same portal as per-row delete. Tests: click Archive All, confirm, every chat in the fixture is archived; Delete All confirm removes them; cancel is a no-op.

#### 2.4 - Testing and Stabilization

**Prompt**:
> Generate automated tests for Phase 2. Run desktop lint, `tsc --noEmit`, and focused tests. CI IMPACT: `desktop/**` already covered. Session history `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.4-phase-2-history-chrome.md`. One local commit. Do not push.

### Phase 2 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] CI impact recorded against the canonical contract, with no remote CI run
- [ ] Session history generated for this phase
- [ ] One local commit created for this phase
- [ ] No branch push, pull request, or remote CI run occurred
- [ ] Ready to advance to Phase 3

---

## Phase 3: Image Restyle Identity on the GPU

**Goal**: "Make the puppy black" restyles the last PNG (black fur, same shot) instead of reprinting it.
**Prerequisites**: Phase 2.
**Stability Gate**: Tests prove restyle is img2img with identity prompt, source bytes present, strength applied (not `or 0.75` clobber), seed forwarded, trailing punctuation parsed, and output digest != source digest. Packaged NVIDIA before/after remains a last-phase operator item.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Cross-cutting Image Studio, sidecar, and Python execute; two prior cycles still reprinted in the field.

### Sub-tasks

#### 3.1 - Trace the packaged path

**Objective**: Know whether screenshot 3 was txt2img, img2img with ignored strength, or reused PNG bytes.

**Failure modes**: `lastPngB64Ref` empty after a successful first generate would skip fail-closed only if `restyle` is false. `values.prompt` must not win over `restylePromptFor`. New assistant `media.src` must not be the previous data URL.

**Prompt**:
> Instrument the restyle send path with testable fields: `mode`, `strength`, `prompt`, source digest, output digest. Persist them on the job extra / a debug-only test hook, not in the user-visible bubble. If the UI currently copies the last `media.src` onto the pending turn, stop. Fail closed with `MISSING_RESTYLE_SOURCE_TEXT` when source bytes are missing.

#### 3.2 - Execute restyle as a real img2img

**Objective**: Python applies prompt, strength, source image, and seed.

**Failure modes**: `strength=ctx.params.strength or 0.75` treats 0 as missing; use an explicit None check. INT4 SANA img2img remains unsupported. Do not call SAM2 for `scope: "image"`. Raise restyle strength to 0.85 if stub/hash tests show 0.7 still echoing the source.

**Prompt**:
> In `real_execute.image_execute`, pass `generator` from `ctx.params.seed` and `strength` without falsy `or`. Expand `MAKE_RESTYLE_RE` to allow a trailing period. Keep `restylePromptFor` as the img2img prompt. Tests: parser accepts "Make the puppy black."; a restyle request includes strength and source; a pipeline that returns the source PNG unchanged fails the digest assertion.

#### 3.3 - Testing and Stabilization

**Prompt**:
> Generate automated tests for Phase 3 (replaceIntent, followUpSource, ImageStudioPage, `test_real_execute`). Run TS and pytest slices. CI IMPACT: existing unit + `tests/python/diffusion`. Session history `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.4-phase-3-image-restyle.md`. One local commit. Do not push.

### Phase 3 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] CI impact recorded against the canonical contract, with no remote CI run
- [ ] Session history generated for this phase
- [ ] One local commit created for this phase
- [ ] No branch push, pull request, or remote CI run occurred
- [ ] Ready to advance to Phase 4

---

## Phase 4: SANA Family Diffusers Pin

**Goal**: The packaged venv can import `SanaPipeline` and `SanaVideoPipeline`, or the bubble names `diffusers-missing` without a traceback. CUDA work still does not run on an incomplete pin.
**Prerequisites**: Phase 3.
**Stability Gate**: Lockfiles and installer contract tests name a Diffusers version whose documented API includes `SanaVideoPipeline`. Runtime tests prove missing class -> `kind="diffusers-missing"` and present class -> SANA path not WanPipeline. A live NVIDIA SANA clip is last-phase only.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Highest technical risk. A bad pin can break every Image/Video model on the host. Field screenshot 4 is this miss, not the v2.4.3 layout work.

### Sub-tasks

#### 4.1 - Bump the media-runtime Diffusers pin

**Objective**: Installer venv and `runtime-lock.json` agree on a release that exports SANA video.

**Failure modes**: Torch 2.3.0 + cu121 + transformers 4.53.2 may not accept the newest Diffusers. If 0.37.1 (documented `SanaVideoPipeline`) cannot coexist, try the oldest release that exports the class. Do not float an unpinned `diffusers`. Placeholder HF hashes stay skip-verify. Both `versions.lock.json` and `runtime-lock.json` must move together.

**Prompt**:
> Update `runtimes/diffusion/runtime-lock.json`, `scripts/installer/build/versions.lock.json`, and any provisioner copy of `diffusers==0.34.0`. Record the chosen version and the import probe (`from diffusers import SanaPipeline, SanaVideoPipeline`) in the phase history. If the pair cannot be installed on torch 2.3.0, stop and open a known gap rather than shipping a broken venv.

#### 4.2 - Honest import errors

**Objective**: Missing class is `RuntimeNotReady` kind `diffusers-missing`, including Image SANA.

**Failure modes**: Do not leak a Python traceback into the chat bubble. Incomplete weight trees still name the missing path (`model-layout-invalid`). 16 GB default video remains `wan2.1-t2v-1.3b`.

**Prompt**:
> Keep the v2.4.3 `sana_video_execute` / `SanaPipeline` loaders. Catch ImportError and map to `diffusers-missing` with a sentence that names the class and the installed diffusers version. Add an Image SANA import probe next to the video one. Tests already cover a fake module without `SanaVideoPipeline`; extend to `SanaPipeline`.

#### 4.3 - Testing and Stabilization

**Prompt**:
> Update `test_media_runtime_contract.py` and `test_real_execute.py`. Run installer pytest for the lock. CI IMPACT: installer workflow already runs these tests; lockfile change is in-scope. Session history `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.4-phase-4-sana-diffusers-pin.md`. One local commit. Do not push.

### Phase 4 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] CI impact recorded against the canonical contract, with no remote CI run
- [ ] Session history generated for this phase
- [ ] One local commit created for this phase
- [ ] No branch push, pull request, or remote CI run occurred
- [ ] Ready to advance to Phase 5

---

## Phase 5: Generation Liveness and Studio Captions

**Goal**: Long Image/Video jobs keep the shell interactive and the pending orb moving, with Creating/Crafting/Generating instead of Shaping.
**Prerequisites**: Phase 4.
**Stability Gate**: Tests prove the JSON-RPC accept returns before `pipe()` finishes, progress/heartbeat events enqueue, cancel still works, and studio pending captions are the new list shuffled per bubble.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Stdin-loop CUDA plus GPU compositor starvation. Two or more high signals (runtime threading, production freeze).

### Sub-tasks

#### 5.1 - Job worker and heartbeats

**Objective**: Python reads the next RPC line while CUDA runs.

**Failure modes**: Two concurrent GPU jobs still serialize through GpuScheduler / one worker. Crash on the worker fails the job with a written error. stdout writes stay line-delimited JSON. Health/version stay synchronous.

**Prompt**:
> Change `runtimes/diffusion/main.py` (and runners as needed) so `diffusion.video.text2video` / image generate accept with `jobId` immediately, execute on a worker thread, and emit progress notifications (`kind: progress`) on a timer even when Diffusers has no step callback yet. Keep `drainEvents` able to run on the Node side during the job. Tests: a fake slow `pipe` does not block a second `health` call; a heartbeat event appears before complete.

#### 5.2 - UI stays pending and alive

**Objective**: Video Lab and Image Studio do not freeze the orb; they wait for complete or error.

**Failure modes**: `await client.text2video` must not be the long wait if 5.1 returns immediately; polling `drainEvents` continues. If the worker is silent longer than a watchdog, keep the animation and show the latest caption, then fail closed if the process is dead. Do not mark the turn complete with empty media.

**Prompt**:
> Confirm VideoLabPage / ImageStudioPage pending remains true until complete/error events. CSS animation must not set `animation-play-state: paused` unless reduced-motion. Add a test that a pending video message still has `data-orb-paused=false` while a job is active. Document GPU headroom (`workingMemReserveGB` / offload) so a 16 GB laptop leaves compositor VRAM; do not silently lower quality without a comment in phase history.

#### 5.3 - Studio caption pool

**Objective**: Image/Video pending uses Creating... / Crafting... / Generating..., shuffled per prompt.

**Failure modes**: Chat/Agents keep the existing four captions. Accessible name can stay "Generating media". Reduced-motion shows the first studio caption.

**Prompt**:
> Split studio captions from `PENDING_CAPTIONS` in `captionRotator.ts`. Map `image-generation` / `video-generation` to the studio rotator (not mapping.ts `label: "Shaping"`). Shuffle once per pending bubble. Tests pin the three strings and assert Shaping is absent.

#### 5.4 - Testing and Stabilization

**Prompt**:
> Generate automated tests for Phase 5. Run Python diffusion tests and desktop AgentStateOrb / VideoLab tests. CI IMPACT: both `desktop/**` and `runtimes/diffusion/**` / `tests/python/diffusion`. Session history `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.4-phase-5-generation-liveness.md`. One local commit. Do not push.

### Phase 5 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] CI impact recorded against the canonical contract, with no remote CI run
- [ ] Session history generated for this phase
- [ ] One local commit created for this phase
- [ ] No branch push, pull request, or remote CI run occurred
- [ ] Ready to advance to Phase 6

---

## Phase 6: Settings Models Density and Details

**Goal**: Models chrome is one tabs+search row, each card hugs its content with one centered action row, and Details is pills plus Best for.
**Prerequisites**: Phase 5.
**Stability Gate**: Tests prove search is inside `models-chrome` on the tab row, actions are a single horizontal group, no extra min-height, Details omits ID/Also agentic/License/Backend paragraphs, pills split label/value, and Best for is a list of strengths.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Several related files in one Settings surface; no new runtime. Three or more medium signals.

### Sub-tasks

#### 6.1 - Tabs and search on one row

**Objective**: Category tabs and the search field share one row.

**Failure modes**: Narrow Settings width wraps search below tabs rather than overflowing the page. The visible "Search" label can hide; `aria-label` stays. Tab selection still filters the list.

**Prompt**:
> Move `SearchInput` into `models-chrome` beside the `tablist` (flex row, wrap allowed). Tests: `models-search` is a descendant of `models-chrome` and shares a row container with `role=tablist`.

#### 6.2 - One action row and hug height

**Objective**: Star, downloaded/check or download, and delete are one centered row; card height follows content.

**Failure modes**: Installing progress still fits. Over-budget rows stay muted. Favorite star color unchanged. Do not restore text Download/Remove buttons.

**Prompt**:
> In `ModelCard`, put favorite + `RowActions` in one `flex-direction: row` group, `alignItems: center`, `justifyContent: center` (or end if that keeps the description width). Description and facts stay left. Remove any implicit stretch that leaves empty space under the copy. `cardStyle` has no fixed min-height.

#### 6.3 - Details copy

**Objective**: Pills, then Best for.

**Failure modes**: Pill order stays the v2.2.9 grammar (WN-7 fixture). Missing pill sources still omit the pill. Strengths empty: omit Best for. Why this one omitted when it only repeats pills.

**Prompt**:
> Split each pill into label + value (e.g. `Company:` muted, `Google` normal). Remove the ID/task paragraph, Also agentic, License paragraph, Backend model, and components dump if they duplicate pills. Render `Best for` as a heading and `item.strengths` as bullets. Tests: details for Gemma 4 12B has pills + best-for list and does not contain `ID: gemma-4-12b` or `Also agentic`.

#### 6.4 - Testing and Stabilization

**Prompt**:
> Extend `desktop/tests/settings-models-density.test.tsx`. Run desktop lint and focused tests. CI IMPACT: `desktop/**`. Session history `docs/archive/v2/v2.4/development/history/2026-08-31_v2.4.4-phase-6-settings-density.md`. One local commit. Do not push.

### Phase 6 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] CI impact recorded against the canonical contract, with no remote CI run
- [ ] Session history generated for this phase
- [ ] One local commit created for this phase
- [ ] No branch push, pull request, or remote CI run occurred
- [ ] Ready to advance to Phase 7

---

## Phase 7: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, its living docs architecture current, and its pipeline reconciled against the canonical contract, with independent evidence that the plan Goal landed, then publish and integrate the completed branch.
**Prerequisites**: All prior phases.
**Stability Gate**: `docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.4-field-chrome.md` exists with one quoted section per duty below. A missing Goal, a Goal-review miss without a recorded known-gap, or a missing evidence section blocks `/update release`.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, Goal-vs-codebase review, known-gap reconciliation, and release gating carry high context volume and blast radius.

### Sub-tasks

#### 7.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout and quote the scan.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure, then refactor toward a clean, intuitive layout via [[project-refactor]] and [[docs-layout-refactor]] (propose-then-apply, with confirmation; repair every reference for anything that moves). Write the detector output into `docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.4-field-chrome.md` under `## Architecture refactor`. An empty finding is allowed only when the scan output is quoted.

#### 7.2 - Known-gaps reconciliation
**Objective**: Reconcile this version and every other still-open known-gaps file.
**Prompt**:
> Via [[known-gaps-tracker]], reconcile this version's known gaps AND every other `docs/**/known-gaps.md` whose Status is in-progress or whose Open Items remain. Glob both canonical (`docs/releases/v<MAJOR>/v<MAJOR>.<MINOR>/known-gaps.md`) and legacy layouts. If a file is unreachable, record the glob result and continue with what was found. Write the disposition into the evidence file under `## Known-gaps reconciliation`. Carry forward v2.4.3 MT-1 (packaged installer two-column still not in this screenshot set) and any v2.4.2 MTs that this cycle still does not observe. Close v2.4.3 MT-2/MT-3/MT-4/MT-6/MT-7 only with new observation, not because this plan exists.

#### 7.3 - Living docs architecture
**Objective**: Prove handbooks, decisions, and generated HTML match the required living tree.
**Prompt**:
> Check `docs/handbooks/` (markdown source of truth, generated `html/`, one atlas walkthrough, technical companions for key components), `docs/decisions/`, and living `docs/README.md` / `docs/DEVLOG.md` / `docs/todos.md`. Self-gated: never invent `docs/testing/` or `docs/validation/`. Markdown wins if HTML disagrees; regenerate or fail the check. Quote the check in the evidence file under `## Living docs architecture`. Details live in [[docs-layout-refactor]].

#### 7.4 - Git-tree hygiene
**Objective**: Report branch and repository-settings hygiene without deleting anything.
**Prompt**:
> Run `python scripts/check_release_preconditions.py --branches --repo-settings`. Quote the output in the evidence file under `## Git-tree hygiene`. Report only; never delete branches.

#### 7.5 - Terminal CI/CD reconciliation
**Objective**: The repository's pipeline is compared field by field against the canonical contract, and every difference is either applied with approval or recorded as a known gap.
**Prompt**:
> Invoke [[cicd-architect]] in existing-pipeline comparison mode and run its six steps in order: DETECT the active CI provider (recording "none detected" rather than assuming one); COMPARE the existing pipeline against every field of the canonical contract - repository-native profiles, event separation, runner selection, the always-resolving aggregate required check, permissions, immutable action or plugin references, caching, concurrency, path scoping, artifact retention, structured reports, deployment boundaries, and failure recovery; PROPOSE each difference with its cost, its risk, and the smallest change that closes it; obtain explicit APPROVAL per change (silence is not approval); APPLY the approved changes and re-run the local gate; and RECORD every declined or environment-only difference as a known gap with an owner and a next step via [[known-gaps-tracker]]. The comparison concludes PASS only when every required field has observable evidence; a green pipeline run is not evidence, because a pipeline that checks nothing is green for the wrong reason. If this repository ships more than one installer, run or add a declarative cross-installer parity gate covering distributed artifacts, supported platforms, named capability counterparts, and external-tool fallbacks, then execute the real installers on their target operating systems with identical postconditions. Cross-link [[platform-contract-verification]] so discovery and delivery are checked in the same pass. If the repository ships zero or one installer, record a silent no-op and continue. Quote the per-field comparison result in the evidence file under `## CI/CD coverage`. v2.4.3 QG-5 (`develop` merge-result workflows) stays proposed-not-applied unless the user approves it here.

#### 7.6 - Independent Goal-vs-codebase review
**Objective**: Prove the plan Goal landed in the codebase, not that checkboxes were ticked.
**Prompt**:
> Re-read the plan header Goal and the Goals First / Definition of Done. Inspect the codebase as if this agent had not implemented the phases. Write `## Goal-vs-codebase review` in `docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.4-field-chrome.md` listing: the plan Goal restated; the code/docs artifacts that satisfy it; and any gap. A missing or malformed Goal is a fail-closed finding that names the missing Goal. Completing every prior-phase sub-task is not evidence the Goal landed. A miss is a blocking finding or a new known-gap, not a pass.

#### 7.7 - Human/manual testing suggestions (last phase only)
**Objective**: Ask a human to exercise only what automated tests cannot cover, now that the feature is complete.
**Prompt**:
> Emit human/manual testing suggestions. Do not invent a fake walkthrough. Operator items after a rebuilt installer: (1) Chat/Agents Searching pill on the assistant gutter, glow uncropped at 100% and 150% zoom; (2) hairline centered under Videos; selected title tight to the blue rail; Archive All / Delete All confirm and act; (3) Generate a puppy then Make the puppy black: fur reads black, not a reprint; (4) SANA-Video: clip or diffusers-missing sentence, no raw ImportError; Image SANA import honesty; (5) Wan 1.3B: orb keeps moving, window accepts clicks, ends with a clip or a written error; caption is Creating/Crafting/Generating; (6) Settings tabs+search one row, one action row, hug height, Details pills + Best for only.

#### 7.8 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and the complete LOCAL gate is green before anything is published.
**Prompt**:
> Run the full local validation/test suite, confirm the refactor changed no behavior, and iterate until clean. This gate is local by construction: it must pass before the branch is published, so it cannot depend on a remote run. Quote the suite in the evidence file under `## Full-suite testing and stabilization`. Generate a session-history entry for this phase.

#### 7.9 - Publication and integration
**Objective**: Publish the completed branch once, validate the merge result remotely, and hand a green integration to the release flow.
**Prompt**:
> Create the final local commit for this phase. Then present the resolved branching model, remote, branch name, and pull-request target, and obtain EXPLICIT approval before the plan's first branch push. This branch already has [PR 58](https://github.com/bendourthe/Nexus-AI/pull/58) against `develop`; an approved push updates that PR rather than opening a second one unless 58 is closed. v2.4.3 T040 is the same hold if that work is still unpushed: one push should carry v2.4.3 + v2.4.4 unless the user splits them. Wait until every required check reaches a terminal state. A red check REOPENS this phase: classify the failure, reproduce it locally, apply the narrow fix, re-run the local gate, update known gaps and session history, then amend the final commit or add one narrowly scoped stabilization commit and push again with approval. Never re-run a red check without a local reproduction; a re-run without one is a guess. Merge only after every required check is green and the user approves. Confirm the post-merge workflow performed only its intended smoke, publication, or provenance work and did not rerun the complete suite. Only then hand off to `/update release`, which owns the version bump, changelog, tag, push, and GitHub Release behind its own confirmation gates. Quote the required-check results in the evidence file under `## Publication and integration`.

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| | | |

### Phase 7 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] CI impact recorded against the canonical contract, with no remote CI run until T036
- [ ] Session history generated for this phase
- [ ] One local commit created for this phase
- [ ] Branch push, pull request, and remote CI occurred only after explicit approval
- [ ] Ready for `/update release` after green integration
