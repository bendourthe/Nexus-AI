# Plan -- Generation Recovery and UI Corrections

**Project**: Nexus AI Studio
**Version**: v2.4.1
**Slug**: generation-recovery-and-ui-corrections
**Filename**: v2.4.1-generation-recovery-and-ui-corrections.md
**Plan Type**: Corrective enhancement / release blocker
**Status**: Approved - implementation complete locally (Phases 1-6 committed, Phase 7 local gates green; T097 push/PR waits on explicit approval)
**Created**: 2026-08-30
**Field evidence**: User screenshots 1-9, `C:/Users/bdour/Downloads/nexus-install.log`, `C:/Users/bdour/.nexus/runtime.json`, and `C:/Users/bdour/AppData/Local/Nexus/python/.diffusion-repair.lock`
**Goal**: Before v2.4.1 is released, a clean or repair installation on a supported Windows NVIDIA host must recover safely from interrupted runtime work and produce one valid local image and one valid local video from the packaged application; the installer and desktop must then present truthful, compact, and consistent authorization, warnings, model ordering, transcript metadata, archived data, training capability, and Agents workspace controls.

## Overview

This is a corrective continuation of the completed [v2.4.1 field-reliability plan](v2.4.1-field-reliability-chat-archives-models-workspaces.md), not a rewrite of its T001-T047 evidence. The latest field run proves that the desktop and selected models install, but it also exposes a release-blocking runtime invariant: `runtime.json` remains in `repairing` with `REPAIR_BUSY`, while the lock file contains PID `18096` and no such process is alive. The current PID-only exclusive file has no owner start-time validation or stale-lease reclamation, so an interrupted installer can disable image and video generation indefinitely while falsely claiming another repair is active.

The same log proves that the repeated `Could not update the installer window.` messages are not download failures. All selected models completed, but background model callbacks enqueue work back to the `InstallEngine` thread while its synchronous `run()` method is occupying that thread. Failed UI marshalling is then written into `model_failures`, turning a telemetry defect into misleading product warnings.

The Hugging Face 404 is catalog drift. `sana-1.6b-int4` points to a removed repository and is marked gated, while the maintained INT4 repository `nunchaku-ai/nunchaku-sana` is public and contains `svdq-int4_r32-sana1.6b.safetensors`. That model should not ask for authorization. A clearer guided flow is still required for genuinely gated repositories and must derive its steps from live, build-validated catalog metadata.

Desktop issues share two causes: policy is duplicated or applied at the wrong layer, and request-level telemetry is displayed as if it belonged to one message. Model Settings does not partition downloaded rows ahead of compatible catalog rows. Assistant token chrome adds request prompt tokens, estimated reasoning, and backend completion totals even when those totals include hidden reasoning. The plan separates durable contracts first, then changes presentation.

## Goals First

**Target problem**: Repeated v2.4.x iterations have not produced a single image or video on the field host, while several screens report misleading readiness, warnings, ordering, and token details that make it difficult to distinguish a real failure from a display defect.

**Target persona**: A privacy-focused Windows user with a consumer NVIDIA GPU who expects a local installer and desktop application to work without manual runtime archaeology, while retaining honest degraded behavior on unsupported hosts.

**Observable definition of done**:

- A dead or reused repair-lock owner is detected and reclaimed safely; a genuinely live repair remains mutually exclusive.
- `runtime.json` transitions atomically through explicit states and cannot remain `repairing` after its owner exits without a recoverable diagnostic.
- The packaged installer repairs the field host's stale state and the packaged desktop generates a decodable PNG and a playable video using installed local models.
- Image proof records dimensions, format, non-zero pixels, and SHA-256; video proof records codec, duration, frame count, dimensions, and SHA-256 through `ffprobe` or an equivalent local parser.
- The installer never calls the run successful when a selected media capability is unavailable; it offers a bounded repair/retry path with the actual failure code.
- Successful model downloads cannot become failures because a UI progress callback was missed.
- Warning summaries use warning color and icons, distinguish required failures, optional skips, and telemetry notices, and never show internal marshalling language to users.
- SANA uses a working, pinned, public repository and does not open an authorization dialog. Every genuinely gated row passes an automated repository/access-page preflight before packaging.
- The gated dialog is a numbered flow: optional free account creation, required license authorization, required sign-in, then a prominent monospace device code with Copy and accessible status.
- Settings Models groups downloaded and ready rows first, compatible not-downloaded rows second, and incompatible rows last; each group uses the same recommendation and tie-break ordering as the installer.
- Archived chats are inside Settings > Data. Fine-tuning is not a dead top-level tab: eligible hosts see an actionable Training surface; unsupported or unprobed hosts get an honest capability state without a fake `0 GB` reading.
- User bubble metadata shows only that message's input-token count and no hover-only breakdown. Assistant bubble metadata shows reasoning plus visible output only; request/context input remains available in Activity telemetry, not the bubble.
- Token counts are tokenizer-backed when the runtime supports it; estimates are explicitly labelled and never presented as exact. The supplied reasoning and final-response fixture produces counts consistent with the visible text.
- Time and token total render inside each bubble above the text. The pending animation has a safe left inset and no enclosing message bubble. Plain message clicks do not open a duplicate right panel.
- Agents resolves its initial model from the installed recommendation snapshot before presenting a selection, places workspace folders and Chat/Memory/Activity in one compact row, and renders session history beneath that row in the same main pane.
- Chat, Memory, and Activity remain only when they have actionable content and include concise purpose text; unknown legacy tab ids fall back to Chat.

**Success metrics**:

- Zero stale-lock false positives across dead PID, reused PID, malformed lease, crash, cancel, and genuine concurrent-repair fixtures.
- Zero successful model installs classified as failed because of UI telemetry.
- 100% catalog URL/file preflight success for every selectable Hugging Face row in the release artifact.
- 100% Python/TypeScript ordering parity over installed, compatible, incompatible, recommendation, date, and name fixtures.
- Exact role-correct token display in protocol, persistence, hydration, and UI tests.
- One observed packaged PNG and one observed packaged video on the target host before publication.

**Strategy grounding**: No `STRATEGY.md` or version constitution was found. The plan follows the repository's local-first product identity, truthful evidence tiers, module authorship contract, and rule against generation-as-a-service. No `docs/solutions/` entry was found for the stale repair lease or token-provenance issue.

## Non-Goals

- Do not add a cloud image, video, embedding, scraping, or generation fallback.
- Do not bypass Hugging Face access controls or accept a publisher license on the user's behalf.
- Do not expose hidden chain-of-thought beyond reasoning text already emitted by the selected local runtime.
- Do not infer exact tokenizer counts from UTF-8 byte length while labelling them exact.
- Do not redesign all Settings navigation or all desktop chrome.
- Do not remove Memory or Activity merely to save space if their current data is actionable; compact and explain them instead.
- Do not implement Video2X installation as part of core video generation. Video2X remains an optional post-processing capability.
- Do not publish, merge, tag, or create a release before the final phase and its explicit approvals.

## Constitution Check

*GATE: Must pass before Phase 1 implementation and be re-checked before Phase 7.*

No version constitution was found. Implementation must therefore use `AGENTS.md`, the module authorship contract, evidence/support tiers, and the active v2.4 known-gaps file as the governing contract.

## Current Model Map

**Model map status**: fresh as of 2026-08-30; the local helper returned `valid: true`, four tiers, and four providers.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.5-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

### Model Map Sources

- Anthropic: https://platform.claude.com/docs/en/models/overview
- OpenAI: https://developers.openai.com/api/docs/models
- Google: https://ai.google.dev/gemini-api/docs/changelog
- Cursor: https://cursor.com/docs/models-and-pricing and https://cursor.com/help/models-and-usage/available-models

## Architecture Decisions

1. **Owner-aware repair lease**: Replace the PID-only sentinel with a versioned JSON lease containing PID, process start identity, creation time, host/boot identity when available, target environment, and nonce. Reclaim only after proving the owner is absent or mismatched. Never delete a lock based only on age.
2. **Atomic runtime state machine**: `unavailable -> repairing -> ready | failed` is written atomically with lease identity, attempt id, timestamps, smoke evidence, and actionable failure code. Startup reconciles a stale `repairing` record before the UI consumes it.
3. **Packaged artifact proof is the release bar**: Unit and mock-CUDA tests are necessary but insufficient. The release remains blocked until the exact installer and installed desktop produce validated local image and video artifacts on the field host.
4. **Catalog access is build-validated data**: Hugging Face repository, revision, exact file paths, license, and gated status are checked by a read-only preflight. A public repository never opens the gated dialog. A missing or 404 row is not selectable in a release build.
5. **UI telemetry cannot determine install outcome**: Download/install result is owned by the model worker result. Qt signals report progress only. Missed visual updates may produce a diagnostic log entry, never `failed_models`.
6. **One model rank tuple**: Settings rank is `(availability bucket, recommendation rank, release date descending, folded display name, id)`. Installer rank omits only the availability bucket and uses the identical remaining tuple and shared fixture.
7. **Message usage is role-local**: User messages own visible input tokens. Assistant messages own visible reasoning and visible answer tokens. Request prompt/context totals live in turn telemetry and Activity. Estimated values carry provenance and never masquerade as exact counts.
8. **One-pane Agents composition**: The workspace control, tabs, history, and active tab content share the main pane. History is a collapsible section below the header, not a second permanent rail.
9. **Capability-aware Training surface**: Fine-tuning is renamed to Training. It is shown in primary Settings navigation only when hardware detection is valid and the host is eligible or the user enables advanced capabilities. Unknown hardware is `Detecting` or `Unavailable`, never `0 GB`.

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort |
|---|---|---|---|---|
| 1 | Installer Transaction Integrity and Repair Lease | Interrupted media repair recovers safely and model/UI telemetry cannot corrupt install outcomes | frontier | max |
| 2 | Hugging Face Catalog and Completion UX | Public SANA installs without auth, real gates use a clear flow, and completion warnings are truthful | frontier | high |
| 3 | Working Image and Video Generation | The installed sidecar loads the pinned runtime and produces validated local image and video artifacts | frontier | max |
| 4 | Models, Data, and Training Settings | Settings ordering, archives placement, and capability-aware training become coherent | strong | high |
| 5 | Transcript Layout and Token Provenance | Bubble chrome, pending animation, reasoning, and role-local exact/estimated usage are correct | frontier | high |
| 6 | Compact Agents Workspace | Recommended model selection, compact header, and in-pane history reduce wasted space without losing capability | strong | high |
| 7 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Independent evidence, packaged field qualification, full gates, and controlled publication complete the correction | frontier | max |

## Task Inventory

- [x] T049 Reproduce stale repair ownership and preserve field fixtures scripts/installer/tests/test_diffusion_venv_provisioner.py
- [x] T050 Implement an owner-aware, crash-recoverable diffusion lease scripts/installer/src/nexus_installer/engine/diffusion_venv_provisioner.py
- [x] T051 Reconcile runtime state atomically across installer and sidecar scripts/installer/src/nexus_installer/engine/runtime_provisioner.py
- [x] T052 Separate model results from Qt progress delivery scripts/installer/src/nexus_installer/engine/installer.py
- [x] T053 Add concurrency, crash, cancel, and stale-owner regression tests scripts/installer/tests/test_installer_engine.py
- [x] T054 Stabilize Phase 1 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.1-correction-phase-1-runtime-integrity.md
- [x] T055 Migrate SANA INT4 to a maintained public pinned source core/registry/catalog.json
- [x] T056 Add release-blocking Hugging Face repository and file preflight scripts/installer/build/check-hf-catalog.py
- [x] T057 Redesign the gated authorization dialog as explicit numbered steps scripts/installer/src/nexus_installer/widgets/gated_auth_dialog.py
- [x] T058 Make the device code prominent, copyable, and accessible scripts/installer/src/nexus_installer/widgets/gated_auth_dialog.py
- [x] T059 Classify completion outcomes and warning visuals truthfully scripts/installer/src/nexus_installer/pages/complete.py
- [x] T060 Add auth, catalog drift, clipboard, browser, and completion tests scripts/installer/tests/test_gated_auth_dialog.py
- [x] T061 Stabilize Phase 2 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.1-correction-phase-2-auth-completion.md
- [x] T062 Define one media-runtime readiness and repair API desktop/sidecar/src/diffusion/runtimeFactory.ts
- [x] T063 Verify image model manifests, ABI, and runtime load paths desktop/sidecar/src/image/diffusionRuntime.ts
- [x] T064 Verify video model manifests, decoder, and output finalization desktop/sidecar/src/video/videoRuntime.ts
- [x] T065 Add a bounded in-app Repair media runtime action desktop/src/components/GenerationCanvas.tsx
- [x] T066 Build an installed-artifact image and video acceptance harness scripts/installer/build/smoke-installed-media.ps1
- [x] T067 Add fake-backend, interrupted-repair, and packaged-contract automated tests desktop/sidecar/tests/diffusion-runtime.test.ts
- [x] T068 Stabilize Phase 3 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.1-correction-phase-3-media-generation.md
- [x] T069 Define the cross-language availability and recommendation rank fixture core/registry/model-display-order.fixture.json
- [x] T070 Apply downloaded-compatible-incompatible ordering in Settings desktop/src/pages/settings/ModelsSettings.tsx
- [x] T071 Move archived chats into the Data settings surface desktop/src/pages/settings/DataSettings.tsx
- [x] T072 Replace the dead Fine-tuning tab with capability-aware Training desktop/src/pages/settings/FineTuningSettings.tsx
- [x] T073 Unify Settings hardware detection with the live host snapshot desktop/src/pages/settings/SettingsPage.tsx
- [x] T074 Add ordering, archives, training, and hardware-state tests desktop/tests/settings-v2.4.1-corrections.test.tsx
- [x] T075 Stabilize Phase 4 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.1-correction-phase-4-settings.md
- [x] T076 Split request usage from per-message usage in the sidecar protocol desktop/sidecar/src/protocol.ts
- [x] T077 Count user, reasoning, and visible answer text with honest provenance core/chat/tokenUsage.ts
- [x] T078 Render role-correct metadata inside each bubble above content desktop/src/shared/chat/MessageBubble.tsx
- [x] T079 Remove ordinary-message preview selection while preserving media lightboxes desktop/src/modules/chat/ChatPage.tsx
- [x] T080 Give pending status a safe inset and uncropped responsive lane desktop/src/shared/chat/MessageBubble.tsx
- [x] T081 Add token, hydration, preview-removal, reasoning, and layout regression tests desktop/tests/transcript-v2.4.1-corrections.test.tsx
- [x] T082 Stabilize Phase 5 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.1-correction-phase-5-transcript.md
- [x] T083 Resolve the initial Agents model from the installed recommendation snapshot desktop/src/modules/coding/CodingPage.tsx
- [x] T084 Put workspace controls and tabs on one compact header row desktop/src/modules/coding/CodingPage.tsx
- [x] T085 Move coding history beneath the header into the main pane desktop/src/modules/coding/CodingPage.tsx
- [x] T086 Audit and clarify Chat, Memory, and Activity actionability desktop/src/modules/coding/CodingPage.tsx
- [x] T087 Add Agents startup, compact-layout, history, and legacy-tab tests desktop/tests/CodingPage.test.tsx
- [x] T088 Stabilize Phase 6 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.1-correction-phase-6-agents.md
- [x] T089 Refactor release-touched architecture and quote the scan docs/archive/v2/v2.4/development/last-phase-evidence-generation-recovery.md
- [x] T090 Reconcile all active known gaps docs/archive/v2/v2.4/known-gaps.md
- [x] T091 Verify living documentation architecture docs/archive/v2/v2.4/development/last-phase-evidence-generation-recovery.md
- [x] T092 Report git-tree hygiene docs/archive/v2/v2.4/development/last-phase-evidence-generation-recovery.md
- [x] T093 Reconcile terminal CI/CD coverage docs/archive/v2/v2.4/development/last-phase-evidence-generation-recovery.md
- [x] T094 Perform an independent Goal-vs-codebase review docs/archive/v2/v2.4/development/last-phase-evidence-generation-recovery.md
- [x] T095 Execute the last-phase packaged operator checklist docs/archive/v2/v2.4/development/last-phase-evidence-generation-recovery.md
- [x] T096 Run full local testing and stabilization docs/archive/v2/v2.4/development/last-phase-evidence-generation-recovery.md
- [ ] T097 Publish and integrate only after explicit approval docs/archive/v2/v2.4/development/last-phase-evidence-generation-recovery.md

---

## Phase 1: Installer Transaction Integrity and Repair Lease

**Goal**: Make interrupted diffusion repair self-healing and keep UI telemetry failures from changing installation results.
**Prerequisites**: Preserve copies of the field `runtime.json`, stale lock, and installer log as sanitized test fixtures; retain the current installed environment until the reproduction is captured.
**Stability Gate**: A dead or reused lease owner is reclaimed; a live owner is not; runtime state reaches one terminal value per attempt; successful model results remain successful even if every UI callback is dropped.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Cross-process ownership, crash recovery, atomic state, and Qt threading are release-critical concurrency boundaries.

### 1.1 - Field reproduction contract (T049)

**Objective**: Turn the exact stale `REPAIR_BUSY` field state into deterministic regressions.

**Prompt**:
> Add sanitized fixtures for a stale PID-only lock, a versioned lease whose PID is dead, a PID reused by a different process start identity, a malformed lease, and a genuinely live owner. Reproduce startup reconciliation and an interrupted installer between `repairing` and `ready`. Assert current behavior fails for the stale cases before changing production code.

**Failure modes**: Missing or malformed fixtures fail loudly; slow process inspection is bounded; tests never terminate unrelated processes and use child processes created by the fixture only.

### 1.2 - Owner-aware repair lease (T050)

**Objective**: Replace the permanent exclusive sentinel with safe mutual exclusion and stale-owner recovery.

**Prompt**:
> Replace `environment_lock` with a versioned JSON lease written through exclusive create and flushed before work begins. Record PID, process start identity, creation time, target path, attempt id, and random nonce; include boot/host identity where the platform exposes it. On collision, read and validate the lease, verify both PID and start identity, and reclaim only when the owner is absent or mismatched. Use compare-before-delete so one process cannot remove another process's replacement lease. Preserve a bounded wait for a confirmed live owner and remove the lease in `finally` only when nonce ownership still matches.

**Failure modes**: Malformed leases are quarantined with a diagnostic and replaced atomically; process-inspection denial returns an actionable `REPAIR_OWNER_UNKNOWN` without deletion; concurrent stale reclaim has one winner; cancel/crash cannot delete a newer owner's lease.

### 1.3 - Atomic runtime-state reconciliation (T051)

**Objective**: Ensure installer and sidecar see one truthful repair state.

**Prompt**:
> Version the runtime contract and write it by temp-file plus atomic replace. Persist attempt id, lease nonce, owner identity, state, timestamps, component readiness, and smoke evidence. On installer or sidecar startup, reconcile `repairing`: if the matching owner is live, report busy; if stale, move to a recoverable failed state and allow a new repair; if the environment already passes smoke, promote to ready. Never persist `diffusionPython: null` with a ready media capability.

**Failure modes**: Truncated JSON preserves the last valid backup and reports `RUNTIME_CONTRACT_INVALID`; slow smoke checks time out; concurrent readers never observe a partially written file; stale attempts cannot overwrite a newer terminal result.

### 1.4 - Qt result/telemetry separation (T052)

**Objective**: Stop dropped progress notifications from creating fake model failures.

**Prompt**:
> Replace `QMetaObject.invokeMethod(self, ..., QueuedConnection)` callbacks targeting the blocked engine thread with a dedicated QObject signal bridge whose receiver lives on the GUI thread, or emit thread-safe Qt signals directly from workers. Model completion/failure state must be committed exactly once from the worker result before UI delivery. A missed progress render logs an internal diagnostic only; it never writes `model_failures`, `failed_models`, or completion warnings.

**Failure modes**: Malformed worker events are rejected without changing a prior terminal result; a closed window drops telemetry safely; simultaneous cancel/completion resolves once; high-frequency progress is coalesced without losing terminal events.

### 1.5 - Concurrency and crash regression suite (T053)

**Objective**: Prove lease ownership, state transitions, and model result integrity under adverse timing.

**Prompt**:
> Add process-level and Qt tests for dead owner, reused PID, malformed lease, live owner timeout, two reclaimers, crash before cleanup, cancel during repair, stale contract with ready environment, UI receiver destruction, 100 parallel model completions, and delayed progress after completion. Assert no fake `Could not update the installer window.` user outcome remains.

### 1.6 - Testing and Stabilization (T054)

**Prompt**:
> Run focused installer process/Qt tests, Ruff, typing for touched modules, and a local repair smoke against a disposable environment. Record commands, counts, support tier, and CI IMPACT in `docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.1-correction-phase-1-runtime-integrity.md`. Create one local ASCII-only commit. Do not push or start remote CI.

### Phase 1 Exit Checklist

- [x] T049-T054 complete
- [x] Stale repair state recovers without weakening live-owner exclusion
- [x] Runtime state is atomic and attempt-owned
- [x] UI delivery cannot corrupt model outcomes
- [x] Focused checks pass and one local commit exists
- [x] Nothing was pushed

---

## Phase 2: Hugging Face Catalog and Completion UX

**Goal**: Remove the false SANA gate, prevent catalog 404s from shipping, and make genuine authorization and warning states explicit.
**Prerequisites**: Phase 1 terminal-state and model-result contracts.
**Stability Gate**: SANA downloads from a pinned public source without a dialog; all Hugging Face rows pass repository/file/gating preflight; a genuinely gated fixture completes sign-up/license/login/code/token/skip flows; completion severity reflects real outcomes only.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Catalog migration, frozen payload integrity, external access semantics, and installer UX cross Python, JSON, network validation, and packaging.

### 2.1 - Maintained public SANA source (T055)

**Objective**: Replace the removed SANA repository and eliminate unnecessary authorization.

**Prompt**:
> Migrate `sana-1.6b-int4` to the maintained public `nunchaku-ai/nunchaku-sana` source and its exact `svdq-int4_r32-sana1.6b.safetensors` path. Update repository, revision, license, source URL, weight manifest, size, runtime loader expectation, and real SHA-256 pins together. Remove `gated` and `requiresLicense` only after the preflight confirms anonymous access. Update affected lockfiles, catalog descriptions, tests, and installer documentation without changing the user-facing model identity unnecessarily.

**Failure modes**: Missing exact files fail catalog validation; a moved repository cannot silently fall back to latest; digest mismatch is terminal; runtime incompatibility keeps the row unavailable rather than downloading unusable weights.

### 2.2 - Hugging Face catalog preflight (T056)

**Objective**: Stop missing repositories, wrong file paths, and false gating metadata at build time.

**Prompt**:
> Add a read-only build preflight using the Hugging Face model API and exact resolve URLs for every selectable Hugging Face row. Validate repository existence, pinned revision, each weight path, anonymous access status, license metadata, and catalog `gated`/`requiresLicense` consistency. Cache a signed or hashed result for offline deterministic packaging; stale cache must be visibly dated and release-blocking beyond its approved window. Never send user credentials during build.

**Failure modes**: Network unavailable uses only a still-valid cache; 404, 401/403 mismatch, rate limit, malformed JSON, or moved files produce distinct diagnostics; concurrent checks are bounded; no secret appears in logs.

### 2.3 - Numbered authorization flow (T057)

**Objective**: Make genuine gated access understandable and sequential.

**Prompt**:
> Redesign `GatedAuthDialog` into visible numbered step cards. Step 0 (Optional) explains that a Hugging Face account is free and opens `https://huggingface.co/join`. Step 1 (Required) names the selected model, explains authorization, and provides a primary `Open license page` button using the build-validated URL. Step 2 (Required) provides a primary `Sign in with Hugging Face` device-flow button. Keep `Paste an existing read token` as an advanced fallback and `Skip this model` as a secondary action. Use concise copy, visible completion states, keyboard order, and accessible names.

**Failure modes**: Missing license URL disables the model before this dialog; browser-open failure exposes a copyable URL; cancel leaves the queue deterministic; slow device authorization is cancellable; skip cannot install partial files.

### 2.4 - Prominent device code and Copy (T058)

**Objective**: Make the eight-character authorization code impossible to miss.

**Prompt**:
> After device authorization starts, reveal a dedicated high-contrast code panel with a large monospace code, grouped exactly as returned, a `Copy code` button, and live `Copied` status. Show the verification URL beside an `Open sign-in page` fallback. Do not place the code in a prose status line. Clear it on cancel, expiry, or model change and never log access tokens.

**Failure modes**: Clipboard denial selects the code and explains manual copy; expired codes restart cleanly; polling failures retain the code with retry guidance; concurrent requests cannot display one model's code in another dialog.

### 2.5 - Truthful completion severity (T059)

**Objective**: Make required failure, optional skip, warning, and diagnostic visually and semantically distinct.

**Prompt**:
> Replace the generic blue `CalloutBox` for issues with semantic warning/error components using warning color, icon, border, and `role=alert`. Group required failures, optional skipped items, and non-blocking notices under separate headings. Remove internal transport language from user summaries. If a selected image/video capability is not ready, say installation needs repair and make `Repair media runtime` or retry the primary action; do not show a clean-success subtitle.

**Failure modes**: Unknown failure codes render a sanitized generic reason plus log link; duplicate outcomes deduplicate by component id; large lists collapse with counts but keep all details accessible; no HTML from logs is injected unsafely.

### 2.6 - Auth/catalog/completion tests (T060)

**Objective**: Cover the field 404 and every dialog state without live-account dependence.

**Prompt**:
> Add tests for public SANA bypass, genuine gated metadata, repo 404, missing weight, anonymous/private mismatch, browser launch failure, clipboard unavailable, code copy, expiry, cancellation, manual token, skip, focus order, screen-reader labels, warning colors/roles, and absence of UI-marshalling text. Use a local HTTP fake and frozen preflight fixture; never require a real account in automated tests.

### 2.7 - Testing and Stabilization (T061)

**Prompt**:
> Run catalog invariants, Hugging Face preflight fixtures, Qt dialog/completion tests, Ruff, typing, and frozen-resource probes. Record evidence and CI IMPACT in `docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.1-correction-phase-2-auth-completion.md`. Create one local ASCII-only commit. Do not push or start remote CI.

### Phase 2 Exit Checklist

- [ ] T055-T061 complete
- [ ] Public SANA no longer asks for authorization
- [ ] Missing or stale Hugging Face artifacts cannot ship silently
- [ ] Genuine gates have clear numbered steps and copyable code
- [ ] Completion severity reflects real component state
- [ ] Focused checks pass and one local commit exists

---

## Phase 3: Working Image and Video Generation

**Goal**: Make the installed media stack produce real, validated output and provide a recoverable user path when readiness fails.
**Prerequisites**: Phases 1-2, pinned media manifests, and a disposable test generation directory.
**Stability Gate**: Automated installed-contract tests pass; on the target packaged Windows host, the exact installed desktop creates one validated PNG and one validated video after clean or repair setup. Failure to obtain both blocks publication and cannot be deferred as a cosmetic known gap.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: CUDA/Python ABI compatibility, multi-gigabyte manifests, sidecar process lifecycle, media encoding, cancellation, and packaged hardware behavior form the highest-risk release path.

### 3.1 - One readiness and repair API (T062)

**Objective**: Give Image Studio and Video Lab the same truthful runtime contract.

**Prompt**:
> Add a sidecar media-runtime service that reads and reconciles the versioned installer contract, validates executable and manifest paths, exposes typed `ready`, `repairable`, `repairing`, and `failed` states, and launches one bounded repair attempt through the installed provisioner. Image and Video handlers must consume this service instead of interpreting files independently. Return actionable codes and progress; do not tell the user to rerun the entire installer when an in-place repair is safe.

**Failure modes**: Missing/malformed contracts fail closed; a live repair reports progress; stale repair triggers Phase 1 reconciliation; repair cancellation terminates only owned children; concurrent studio requests share one attempt.

### 3.2 - Image runtime and manifest proof (T063)

**Objective**: Prove the selected image model can actually load with the installed Python/CUDA stack.

**Prompt**:
> Trace the selected image id through catalog alias folding, weight directory, runtime adapter, Python entry point, CUDA device, dtype, and output finalization. Add a preflight that imports pinned torch/diffusers/nunchaku modules, verifies CUDA and compute capability where required, resolves every manifest file, loads the model, and performs a minimal deterministic decode. Keep errors typed by dependency, ABI, missing weight, out-of-memory, and decode stage.

**Failure modes**: CPU-only torch, incompatible CUDA wheel, missing DLL, unsupported GPU architecture, digest mismatch, OOM, corrupt output, and cancellation each clean up children and partial files without marking ready.

### 3.3 - Video runtime and finalized output proof (T064)

**Objective**: Prove Video Lab generates and finalizes a playable clip independently of optional Video2X.

**Prompt**:
> Trace the recommended installed video model from picker through weights, runtime adapter, frame generation, encoder availability, temporary frame directory, final MP4/WebM write, and provenance persistence. Validate codec/encoder at preflight. Ensure core generation never depends on Video2X. Commit the output atomically only after the container is finalized and probeable.

**Failure modes**: Missing encoder, zero frames, corrupt frame, OOM, disk-full, cancellation, sidecar restart, and finalization failure remove only owned temporary artifacts and retain a retryable job record.

### 3.4 - In-app repair action (T065)

**Objective**: Replace dead-end generation errors with a bounded, observable recovery flow.

**Prompt**:
> When media state is repairable, render an inline error card with the plain-language cause, `Repair media runtime`, `View details`, and `Open log location`. Show repair progress using the existing retained canvas without creating a message bubble. On success, re-run the user's pending generation once after confirmation; on failure, preserve the prompt and attachments. Never start repair automatically in a loop.

**Failure modes**: Sidecar unavailable offers restart first; a live external repair disables duplicate action; slow repair remains cancellable; late success after page navigation updates shared runtime state but does not inject output into the wrong chat.

### 3.5 - Installed-artifact acceptance harness (T066)

**Objective**: Turn real generation into a repeatable release command.

**Prompt**:
> Add a Windows PowerShell acceptance harness that locates the exact installed desktop/runtime, records installer hash, app/runtime/catalog versions, GPU/driver/CUDA details, clears only a disposable output directory, invokes one deterministic image prompt and one short deterministic video prompt through the same sidecar APIs as the UI, and validates outputs. PNG checks include signature, decode, dimensions, non-zero variance, bytes, and SHA-256. Video checks use the bundled or discovered `ffprobe` to record codec, duration, dimensions, frame count, bytes, and SHA-256. Emit one machine-readable report and return non-zero on either failure.

**Failure modes**: Missing tools or models fail with exact prerequisites; timeouts terminate owned jobs; existing user generations are never deleted; output from a prior run cannot satisfy the current attempt id.

### 3.6 - Automated media contract tests (T067)

**Objective**: Cover failure recovery without replacing the required hardware proof.

**Prompt**:
> Add fake-backend and process tests for clean ready, stale lease repair, live repair, CPU-only torch, missing DLL, missing/corrupt weights, OOM, encoder missing, zero-frame video, cancellation, disk-full, restart hydration, one-shot retry, and output validation. Add packaging tests proving all entry points, manifests, and probe tools expected by the installed harness are present.

### 3.7 - Testing and Stabilization (T068)

**Prompt**:
> Run focused sidecar, Python runtime, installer-manifest, TypeScript, lint, and packaging-contract tests. Do not claim field success or ask for manual testing in this phase. Record automated evidence, remaining prerequisites for Phase 7, and CI IMPACT in `docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.1-correction-phase-3-media-generation.md`. Create one local ASCII-only commit. Do not push or start remote CI.

### Phase 3 Exit Checklist

- [x] T062-T068 complete
- [x] Image and video share a typed, repairable readiness contract
- [x] Image load/decode and video frame/encode/finalize paths are covered
- [x] Installed-artifact harness validates real outputs
- [x] Automated checks pass and one local commit exists
- [x] Packaged hardware proof remains explicitly pending until Phase 7

---

## Phase 4: Models, Data, and Training Settings

**Goal**: Make Settings follow the installer's recommendation contract while giving archives and local training intuitive homes.
**Prerequisites**: Phase 2 catalog metadata and the existing archive store/API.
**Stability Gate**: All Settings model tabs use the three availability groups with shared recommendation order; Data contains archived chats and transfer tools; unsupported/unprobed training never renders as an empty `0 GB` feature.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: The UI work is bounded, but cross-language order parity, hardware hydration, and archive restoration require coordinated state and accessibility behavior.

### 4.1 - Shared rank fixture (T069)

**Objective**: Make installer and Settings order mechanically comparable.

**Prompt**:
> Add a language-neutral fixture that assigns recommendation rank and expected order for required/default/recommended rows, release-date and name ties, installed/ready state, compatible state, and incompatible state. Python and TypeScript must consume the same fixture in parity tests. Define Settings buckets as downloaded+ready, compatible not downloaded, incompatible; within each bucket use the installer recommendation tuple exactly.

**Failure modes**: Unknown installed readiness is not treated as ready; missing recommendation rank falls behind ranked peers; malformed dates use stable name/id ties; asynchronous install/remove cannot reorder using stale state.

### 4.2 - Settings model grouping (T070)

**Objective**: Put usable owned models first without losing recommendation quality.

**Prompt**:
> Update `ModelsSettings` and shared display policy to render downloaded and runtime-ready rows first, compatible non-downloaded rows second, and incompatible rows last. Add subtle group headings only when more than one group is present. Preserve category tabs, search filtering, external-model separation, and recommendation order inside each group. A downloaded but broken model belongs in a visible `Needs repair` subgroup before compatible downloads, not silently among ready models.

**Failure modes**: Missing readiness is labelled unknown; slow reconciliation shows skeleton order then one stable update; concurrent install/remove uses latest request; incompatible rows remain visible and disabled appropriately.

### 4.3 - Archived chats inside Data (T071)

**Objective**: Remove the top-level Archived chats tab and make Data the home for retention and transfer.

**Prompt**:
> Remove `Archived chats` from the Settings tab strip and compose `ArchivedChatsSettings` as an `Archived chats` section within `DataSettings`, above export/import or behind a local Data sub-navigation when vertical space requires it. Preserve pillar grouping, restore behavior, errors, keyboard access, and refresh after restore. Deep links or persisted `archives` tab values migrate to `data#archived-chats`.

**Failure modes**: Missing archive backend shows only that section unavailable; slow list is cancellable on tab change; restoring while export runs is serialized by operation type; duplicate restore events merge by session id.

### 4.4 - Capability-aware Training (T072)

**Objective**: Explain local QLoRA and stop advertising a non-actionable empty tab.

**Prompt**:
> Rename the product label to `Training` and add a one-sentence explanation: it adapts a copy of a supported local model to user data through local QLoRA. Show the primary Settings tab only when hardware detection is valid and the host is eligible, or when Advanced capabilities is enabled. For unsupported hosts, place a compact capability card under Models with actual detected GPU/VRAM, minimum requirement, supported platforms, and no action. For eligible hosts, retain provision, dataset, model, job, cancellation, redaction, and license detail in a guided sequence.

**Failure modes**: Unknown telemetry says detection unavailable with Retry, never `0 GB`; slow status keeps controls disabled; provision failures retain datasets and jobs; unsupported hosts cannot start hidden IPC actions.

### 4.5 - Hardware snapshot unification (T073)

**Objective**: Use the same detected host facts in status dock, Models, Training, and compatibility.

**Prompt**:
> Add one cached host-capability snapshot to the desktop composition root with GPU vendor, total/free VRAM, driver, platform, measured timestamp, and state (`loading`, `ready`, `unavailable`). Pass it to Settings and pickers instead of defaulting missing values to zero. Refresh on sidecar reconnect and explicit retry; coalesce concurrent consumers.

**Failure modes**: Missing/malformed telemetry stays unavailable; slow requests time out to the last known snapshot with age; reconnect cannot apply an older response; no surface infers incompatibility from unknown VRAM.

### 4.6 - Settings regression tests (T074)

**Objective**: Pin order, navigation, capability, and accessibility behavior.

**Prompt**:
> Add cross-language order parity, install/remove reorder, broken downloaded model, search/category stability, archives deep-link migration, restore refresh, training hidden/advanced/eligible states, telemetry loading/unavailable/reconnect, focus order, and narrow-window tab overflow tests.

### 4.7 - Testing and Stabilization (T075)

**Prompt**:
> Run focused desktop tests with coverage for touched modules, Python order parity, TypeScript build, ESLint, and accessibility assertions. Record evidence and CI IMPACT in `docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.1-correction-phase-4-settings.md`. Create one local ASCII-only commit. Do not push or start remote CI.

### Phase 4 Exit Checklist

- [x] T069-T075 complete
- [x] Settings uses downloaded, compatible, incompatible groups with shared recommendation order
- [x] Archived chats live under Data
- [x] Training is explanatory and actionable only when appropriate
- [x] Unknown hardware is never displayed as zero
- [x] Focused checks pass and one local commit exists

---

## Phase 5: Transcript Layout and Token Provenance

**Goal**: Make transcript chrome compact and trustworthy by displaying only the tokens attributable to each visible message.
**Prerequisites**: Existing separate `reasoningText`, token fields, session persistence, and shared MessageBubble.
**Stability Gate**: The supplied `Hi`/reasoning/answer fixture produces role-correct counts; time/tokens are inside the bubble above content; pending status is fully visible; plain messages have no selection/preview behavior.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Protocol and persistence compatibility plus provider-specific usage semantics make this more than a visual adjustment.

### 5.1 - Request versus message usage schema (T076)

**Objective**: Stop attaching context-window prompt tokens to the assistant message.

**Prompt**:
> Version the chat protocol and persistence so a turn records request usage separately from message usage. Request usage may contain system, history, attachment, cache, and current-user prompt totals for Activity analytics. User message usage contains only tokens for that user message. Assistant message usage contains reasoning and visible answer tokens only, plus provenance (`provider`, `tokenizer`, `exact|estimated`, raw counters). Migrate older rows without rewriting their uncertain totals as exact.

**Failure modes**: Missing/malformed usage omits chrome; older cumulative totals retain a legacy/estimated marker; slow tokenizer work cannot block streaming text; concurrent chunks merge monotonically by turn id.

### 5.2 - Honest token accounting (T077)

**Objective**: Count the text actually displayed and explain unavoidable estimates.

**Prompt**:
> Prefer runtime/provider tokenizer counts for current user text, emitted reasoning text, and visible final output. When the backend reports only cumulative `prompt_eval_count`/`eval_count`, retain those as request telemetry and separately tokenize the split reasoning/output text. If no tokenizer is available, estimate each visible segment independently, set `tokensEstimated=true`, and use copy that says `estimated`. Never derive reasoning from byte length while displaying it as exact, and never include reasoning tokens again in visible output.

**Failure modes**: Unicode, emoji, empty output, missing reasoning, streaming revisions, provider totals that include reasoning, and tokenizer failure preserve text and mark usage unavailable/estimated honestly.

### 5.3 - Metadata inside bubbles (T078)

**Objective**: Put the time and discrete token label above text inside each bubble.

**Prompt**:
> Move `BubbleMeta` to the top of the `<article>` before content. Keep time left and italic `(X tokens)` right. For user messages, total equals only user input and has no title, hover breakdown, or focus stop. For assistant messages, total equals reasoning plus visible output; hover/focus detail lists only those two fields and whether either is estimated. Keep reasoning disclosure collapsed above the assistant bubble and do not count request input in its label.

**Failure modes**: Missing time or tokens leaves the other aligned; one-token grammar remains correct; narrow bubbles wrap metadata without overlapping text; assistive names do not duplicate visible content.

### 5.4 - Remove redundant message preview (T079)

**Objective**: Stop ordinary clicks from opening a duplicate right panel.

**Prompt**:
> Remove `onSelectMessage`, plain-message button semantics, selected preview state, and the ChatPage `PreviewPane` path for text messages. Preserve explicit media click-to-lightbox, fullscreen/download/copy, and any true file/artifact preview opened by a dedicated action. Delete obsolete tests or components only when no other surface consumes them.

**Failure modes**: Links, code selection, and reasoning disclosure remain clickable without opening a panel; media clicks open one lightbox; keyboard focus no longer lands on the whole bubble.

### 5.5 - Uncropped pending status (T080)

**Objective**: Keep the no-bubble generation animation fully inside the transcript at every pane width.

**Prompt**:
> Add a dedicated assistant status lane with at least one spacing token of left inset, `box-sizing: border-box`, safe overflow, and a bounded max width. Keep the orb/pill background owned only by the status component and no article/bubble wrapper. Verify collapsed history, expanded history, narrow window, 100-200% zoom, and reduced motion. Do not center the status like media generation.

**Failure modes**: Long localized captions truncate or wrap without clipping the orb; reduced motion retains a visible static state; rapid pending-to-answer transition does not shift the transcript horizontally.

### 5.6 - Transcript regression suite (T081)

**Objective**: Pin the user's exact example and message interaction contract.

**Prompt**:
> Add a fixture with user `Hi`, the supplied reasoning text, and final `Hello! How can I help you today?`. Assert user-only input metadata, assistant reasoning/output total, tokenizer or explicit estimated provenance, exact absence of assistant input in the tooltip, metadata position inside article, reasoning collapsed by default, no whole-message button, no PreviewPane, and non-negative bounding rectangles for the pending orb at narrow widths.

### 5.7 - Testing and Stabilization (T082)

**Prompt**:
> Run focused sidecar/protocol/storage/component tests with coverage, TypeScript builds, ESLint, hydration compatibility, and accessibility checks. Record evidence and CI IMPACT in `docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.1-correction-phase-5-transcript.md`. Create one local ASCII-only commit. Do not push or start remote CI.

### Phase 5 Exit Checklist

- [x] T076-T082 complete
- [x] Request context is separate from per-message usage
- [x] Exact and estimated counts are distinguished honestly
- [x] Metadata is inside bubbles above content
- [x] Pending animation is visible and plain clicks open no preview panel
- [x] Focused checks pass and one local commit exists

---

## Phase 6: Compact Agents Workspace

**Goal**: Use one main pane for workspace selection, tabs, history, and session content while selecting the recommended installed agentic model deterministically.
**Prerequisites**: Existing WorkspaceScope, native picker, coding session explorer, and model selection snapshot.
**Stability Gate**: First render resolves to the recommended installed model; header controls fit one row at normal widths; history expands below the header; each retained tab has actionable content and purpose copy.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: The layout change is bounded React work, but startup selection, active-session state, responsive behavior, and history navigation must remain coherent.

### 6.1 - Deterministic recommended model (T083)

**Objective**: Prevent `gpt-oss` or a fallback id from flashing or persisting as the initial selection.

**Prompt**:
> Hydrate the selected-model snapshot and installed agentic catalog before committing an initial picker value. Resolve in order: explicit valid session model, explicit user favorite, installer `recommendedByTask.agentic`, then first ready model in canonical recommendation order. Treat `DEFAULT_MODEL_ID` and static fallback rows as loading placeholders only, never user-visible selections when live catalog data is expected. Persist a choice only after the user changes it.

**Failure modes**: Missing snapshot falls back to canonical ready order; recommended but unavailable falls to the next ready row; slow catalog shows `Loading models` rather than a wrong selection; late responses cannot override a user change.

### 6.2 - One compact header row (T084)

**Objective**: Place workspace folders left and Chat/Memory/Activity right on one line.

**Prompt**:
> Compose `WorkspaceSelector` and the tablist in one responsive header. Folder chips use the left flexible region; tabs align right. At narrower widths, chips scroll or wrap before the tabs move to a second line. Keep add/replace/remove semantics, full-path tooltips, native picker cancellation, accessible tab relationships, and a minimum chat-content height.

**Failure modes**: Long paths truncate without hiding remove/add controls; many roots remain keyboard reachable; 200% zoom wraps cleanly; a pending picker result cannot change the active tab.

### 6.3 - History inside the main pane (T085)

**Objective**: Replace the permanent second rail with a collapsible workspace/session section under the main header.

**Prompt**:
> Remove `CollapsibleHistoryAside` from the Agents page and render the existing workspace-grouped `FolderTree` as a collapsible `History` section below the header and above active tab content. Preserve selected session, automatic workspace directories, archive/delete callbacks, refresh, collapse persistence, and active-session reset. Default the section to a compact height with independent scrolling; allow users to collapse it fully.

**Failure modes**: Empty history uses one concise start action; slow loading preserves header/chat space; selecting a session closes or compacts history on narrow windows; delete/archive cannot leave a stale active transcript.

### 6.4 - Tab actionability and explanation (T086)

**Objective**: Retain only useful Chat, Memory, and Activity surfaces and explain them plainly.

**Prompt**:
> Audit the mounted content and data source for each tab. Keep Chat for conversation/output. Keep Memory only if it shows scoped indexed knowledge or actionable empty/index controls; label it `Memory` with `Knowledge saved or indexed for these workspace folders.` Keep Activity only if it shows tools, approvals, runtime events, or model metrics; label it `Activity` with `Tools, approvals, and runtime events for this workspace.` Hide empty implementation-only controls. Preserve `trace -> activity` and unknown-id -> `chat` migration.

**Failure modes**: Unavailable memory/activity backends show retryable empty states; tab changes do not duplicate streams; session switches retain the active tab only when valid for the new session.

### 6.5 - Agents regression suite (T087)

**Objective**: Verify model hydration, compact layout, history, and tab behavior.

**Prompt**:
> Add tests for delayed catalog/snapshot, recommended unavailable, user favorite, late user change, long/many folder chips, narrow/zoom layout, history expand/collapse/select/archive/delete/reset, first workspace session, Memory/Activity unavailable states, tab descriptions, and legacy id migration. Use DOM geometry assertions where jsdom permits and a browser screenshot test for the compact row.

### 6.6 - Testing and Stabilization (T088)

**Prompt**:
> Run focused desktop tests with coverage, TypeScript build, ESLint, Tauri picker tests, session explorer tests, and accessibility assertions. Record evidence and CI IMPACT in `docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.1-correction-phase-6-agents.md`. Create one local ASCII-only commit. Do not push or start remote CI.

### Phase 6 Exit Checklist

- [x] T083-T088 complete
- [x] Recommended installed agentic model is deterministic
- [x] Workspace and tabs share one responsive header
- [x] History is a collapsible main-pane section
- [x] Retained tabs are actionable and explained
- [x] Focused checks pass and one local commit exists

---

## Phase 7: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, its living docs architecture current, and its pipeline reconciled against the canonical contract, with independent evidence that the plan Goal landed, then publish and integrate the completed branch.
**Prerequisites**: All prior phases.
**Stability Gate**: `docs/archive/v2/v2.4/development/last-phase-evidence-generation-recovery.md` exists with one quoted section per duty below. A missing Goal, a Goal-review miss without a recorded known-gap, a missing real PNG/video result, or a missing evidence section blocks publication.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, Goal-vs-codebase review, packaged GPU qualification, known-gap reconciliation, and release gating carry high context volume and blast radius.

### Sub-tasks

#### 7.1 - Architecture refactor (T089)
**Objective**: Refactor toward a well-organized, intuitive layout and quote the scan.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure, then refactor toward a clean, intuitive layout via [[project-refactor]] and [[docs-layout-refactor]] (propose-then-apply, with confirmation; repair every reference for anything that moves). Write the detector output into `docs/archive/v2/v2.4/development/last-phase-evidence-generation-recovery.md` under `## Architecture refactor`. An empty finding is allowed only when the scan output is quoted.

#### 7.2 - Known-gaps reconciliation (T090)
**Objective**: Reconcile this version and every other still-open known-gaps file.
**Prompt**:
> Via [[known-gaps-tracker]], reconcile this version's known gaps AND every other `docs/**/known-gaps.md` whose Status is in-progress or whose Open Items remain. Glob both canonical (`docs/releases/v<MAJOR>/v<MAJOR>.<MINOR>/known-gaps.md`) and legacy layouts. If a file is unreachable, record the glob result and continue with what was found. The unresolved image/video field blocker cannot be downgraded or deferred for v2.4.1. Write the disposition into the evidence file under `## Known-gaps reconciliation`.

#### 7.3 - Living docs architecture (T091)
**Objective**: Prove handbooks, decisions, and generated HTML match the required living tree.
**Prompt**:
> Check `docs/handbooks/` (markdown source of truth, generated `html/`, one atlas walkthrough, technical companions for key components), `docs/decisions/`, and living `docs/README.md` / `docs/DEVLOG.md` / `docs/todos.md`. Self-gated: never invent `docs/testing/` or `docs/validation/`. Markdown wins if HTML disagrees; regenerate or fail the check. Quote the check in the evidence file under `## Living docs architecture`. Details live in [[docs-layout-refactor]].

#### 7.4 - Git-tree hygiene (T092)
**Objective**: Report branch and repository-settings hygiene without deleting anything.
**Prompt**:
> Run `python scripts/check_release_preconditions.py --branches --repo-settings`. Quote the output in the evidence file under `## Git-tree hygiene`. Report only; never delete branches.

#### 7.5 - Terminal CI/CD reconciliation (T093)
**Objective**: Compare the pipeline field by field against the canonical contract and cover the corrected release paths.
**Prompt**:
> Invoke [[cicd-architect]] in existing-pipeline comparison mode and run its six steps in order: DETECT the active CI provider; COMPARE repository-native profiles, event separation, runner selection, aggregate required check, permissions, immutable references, caching, concurrency, path scoping, retention, structured reports, deployment boundaries, failure recovery, installer manifest/preflight, runtime lease tests, catalog parity, token protocol compatibility, and packaging contract; PROPOSE each difference with cost/risk/smallest fix; obtain explicit APPROVAL per change; APPLY approved changes and re-run the local gate; RECORD declined/environment-only differences via [[known-gaps-tracker]]. A green pipeline is not evidence if it skips the packaged contract. Record one-installer parity as a silent no-op and cross-link [[platform-contract-verification]]. Quote the result under `## CI/CD coverage`.

#### 7.6 - Independent Goal-vs-codebase review (T094)
**Objective**: Prove the plan Goal landed in the codebase, not that checkboxes were ticked.
**Prompt**:
> Re-read the plan header Goal and the Goals First / Definition of Done. Inspect the codebase as if this agent had not implemented the phases. Write `## Goal-vs-codebase review` in the evidence file listing the Goal restated, code/docs artifacts that satisfy it, and every gap. Trace all nine supplied screenshot groups and the stale runtime/log evidence. A missing Goal or unproven required media path is fail-closed. Completing prior tasks is not evidence.

#### 7.7 - Human/manual testing suggestions (last phase only) (T095)
**Objective**: Execute the field workflow automated tests cannot prove, after implementation is complete.
**Prompt**:
> Run a numbered operator checklist on the target Windows NVIDIA host using the exact rebuilt installer: record installer SHA-256; reproduce and recover the stale lease; complete clean and repair installs; verify no false model-window warnings; verify warning colors/copy; verify public SANA bypass and one genuine gated fixture's sign-up/license/login/code/copy flow; launch the installed app; run the acceptance harness and retain one validated PNG plus one validated video; inspect Settings model groups, Data archives, and Training capability; inspect the supplied chat token fixture, metadata placement, reasoning, pending animation, and absence of preview pane; inspect Agents recommended model, compact header, history, and tabs; reboot and repeat one image and one video to prove persistence. Record host, versions, commands, screenshots, hashes, probe metadata, and observed/not-observed status under `## Human testing`. Both generated artifacts are required for GO.

#### 7.8 - Testing and Stabilization (T096)
**Objective**: Prove the refactor preserved behavior and the complete local gate is green before publication.
**Prompt**:
> Run the full root, isolated desktop, installer, Python runtime, Rust, lint, format/check, TypeScript build, dependency-cruiser, docs/link, Unicode safety, catalog preflight/cache, packaging manifest, installed-media contract, and coverage gates. Confirm the refactor changed no unrelated behavior and iterate until clean. Quote commands, counts, coverage, skips, and failures under `## Full-suite testing and stabilization`. Generate a session-history entry for this phase. A failed required gate or either missing media artifact blocks publication.

#### 7.9 - Publication and integration (T097)
**Objective**: Publish the completed branch once, validate the merge result remotely, and hand a green integration to the release flow.
**Prompt**:
> Create the final local commit for this phase. Then present the resolved branching model, remote, branch name, and pull-request target, and obtain EXPLICIT approval before the plan's first branch push. Push once, open or update the integration pull request, and report the exact required checks expected. Wait until every required check reaches a terminal state. A red check reopens this phase: reproduce locally, apply the narrow fix, re-run the local gate, update known gaps/history, then amend or add one narrow stabilization commit and push again with approval. Merge only after checks are green and the user approves. Confirm post-merge workflows perform only intended smoke/publication/provenance work. Hand off to `/update release` for changelog, tag, push, and GitHub Release behind its own confirmation gates.

### Phase 7 Exit Checklist

- [x] T089-T096 complete; T097 waits on explicit push/PR approval
- [x] Architecture, living docs, and active known gaps are reconciled
- [x] Independent review traces every reported issue to code and evidence
- [x] Exact packaged installer produces one validated PNG and one validated video after repair. Reboot persistence is Not observed.
- [x] Full local suite, coverage, packaging, catalog, docs, and Unicode gates pass
- [x] One Phase 7 commit exists
- [ ] First push/PR/remote CI occurs only after all local gates and explicit approval
- [ ] Merge, tag, release, and main integration follow separate explicit authorization

## Verification Matrix

| Requested outcome | Primary automated evidence | Required final field evidence |
|---|---|---|
| Recover interrupted media runtime | Process lease and atomic contract tests | Stale field lease repaired by exact installer |
| Generate images | Manifest/ABI/load/decode and PNG validator tests | Validated PNG from installed desktop |
| Generate videos | Frame/encoder/finalize and video probe tests | Validated playable video from installed desktop |
| Clear Hugging Face steps | Catalog preflight and dialog state/a11y tests | Working gated fixture with code copy; SANA bypass |
| Truthful completion warnings | Result/telemetry isolation and semantic callout tests | Completion screenshot and log without false model failures |
| Correct Settings model order | Shared Python/TypeScript rank fixture | Installer/Settings visual comparison |
| Archives and Training information architecture | Navigation/deep-link/capability tests | Data archive restore and honest host capability display |
| Correct token attribution | Protocol/tokenizer/hydration/component fixtures | Supplied `Hi` transcript inspected in packaged app |
| Compact transcript | DOM/a11y/layout tests | Pending animation uncut; no ordinary-message preview pane |
| Compact Agents workspace | Selection/layout/history/tab tests | Recommended model and one-pane workflow observed |

## Residual Risks and Cut Lines

- Image or video generation failure on the supported field host is a release blocker for v2.4.1, not a deferrable documentation gap.
- If the maintained SANA INT4 artifact fails runtime compatibility, remove that variant from selectable defaults and ship a proven public variant; do not retain a 404 or false gate.
- If a provider/runtime cannot tokenize visible segments exactly, label the counts estimated or omit them. Never show request totals as message totals.
- If Training cannot obtain a valid hardware snapshot, hide it from primary navigation and show detection unavailable under Models; do not render an empty actionable tab.
- If Activity or Memory has no actionable data after the audit, remove that tab and migrate its legacy id to Chat rather than keeping placeholder chrome.
- If a real concurrent repair cannot be distinguished safely from an inaccessible owner, fail closed with `REPAIR_OWNER_UNKNOWN` and an explicit recovery instruction; never delete an uncertain live owner's lease.

## Commit and CI Contract

- Each phase ends with exactly one local ASCII-only commit after its scoped automated verification passes.
- Phases 1-6 do not push, open a pull request, or start remote CI.
- Phase 7 performs the only human/operator checklist, complete local gate, first push, and remote validation.
- No force push, branch deletion, tag, GitHub Release, merge, or main integration occurs without the explicit authorization required by repository policy.
