# Plan -- Field Reliability, Chat Archives, Model Library, and Multi-Root Workspaces

**Project**: Nexus AI Studio
**Version**: v2.4.1
**Slug**: field-reliability-chat-archives-models-workspaces
**Filename**: v2.4.1-field-reliability-chat-archives-models-workspaces.md
**Plan Type**: Feature / Enhancement / Bug Fix
**Status**: Approved - implementation in progress
**Created**: 2026-08-29
**Confirmed**: 2026-08-29
**Field evidence**: Screenshots 1-7 supplied by the user and `C:/Users/bdour/Downloads/nexus-install-nexus-desktop-log.txt`
**Goal**: By the end of v2.4.1, a Windows user can complete a truthful Nexus installation with an obvious overall percentage and a verified CUDA image runtime; read cleaner chat transcripts with optional model-provided reasoning; safely archive, restore, or permanently delete sessions without stale active state; see one consistent, informative model library in the installer and Settings; and work in an Agents workspace made from one or more local folders with sessions grouped beneath that workspace.

## Overview

This release is a field-repair and product-coherence cycle. The supplied installer log proves that Nexus Desktop 2.3.1, the sidecar health check, Node, runtime sources, the selected-model snapshot, and Nexus-Hub completed successfully. The unhandled failure starts at the optional Unsloth step because `UnslothVenvProvisioner._pins_path()` falls back to the checkout-relative `core/tuning/unsloth-pins.json`, while `scripts/installer/build/nexus-installer.spec` does not bundle that file or expose it through the frozen-resource resolver.

The image-generation failure has a separate root cause. The active `RuntimeProvisioner` copies `runtimes/` and writes `runtime.json`, but it never invokes the existing `DiffusionVenvProvisioner`. It therefore records `diffusionPython: null` on a clean host, even though the inactive `provisioner_dispatch.py` chain already knows how to create the environment. v2.4.1 reconnects the provisioner to the shipping path, downloads only the pinned platform payload needed by the host, verifies every artifact, and refuses to claim image readiness until a CUDA smoke test passes.

The desktop work is deliberately split by durable boundary. Transcript rendering becomes quieter without inventing or exposing reasoning that the local backend did not provide. Session archive state is persisted in each existing store and aggregated for Settings. Model ordering becomes a tested shared contract instead of two similar algorithms. Multi-root coding introduces an explicit workspace identity and union-of-roots path policy before changing the Agents UI.

## Goals First

**Target problem**: New users can reach a partially working desktop while the installer reports a terminal exception, Image Studio advertises downloaded models without a usable CUDA runtime, and session/model/workspace surfaces behave differently enough to undermine trust.

**Target persona**: A privacy-focused Windows developer or creator using local models on a consumer NVIDIA GPU, with the same contracts retained for supported macOS and Linux paths.

**Observable definition of done**:

- The installer shows a larger overall bar with a moving gradient, readable percentage, reduced-motion fallback, and unchanged smaller phase bars.
- A frozen installer resolves bundled Unsloth pins and contains optional-step exceptions without misreporting the whole installation as failed.
- Selecting an image or video model provisions a pinned diffusion environment; NVIDIA installs pass `torch.cuda.is_available()` before `runtime.json` marks CUDA image generation ready.
- The pending assistant animation has no message bubble; completed user and assistant bubbles use subtly different neutral backgrounds.
- Bubble metadata shows time at the left and italic `(X tokens)` at the right, with keyboard-accessible input/reasoning/output detail on hover or focus.
- Model-provided reasoning content, when available, is persisted separately and rendered collapsed above the response bubble; no disclosure appears when only a token count exists.
- Delete is explicitly irreversible, Archive is available beside Delete, both actions reset an active transcript only after storage succeeds, and Settings can restore archived chats grouped by Chatbot, Agents, Images, and Videos.
- Installer and Settings show the same selectable catalog roster in the same default order: recommendation tier first, release date newest first, then stable name order. Compatibility changes badges and actions, not visibility.
- Model cards always show a one-sentence description, storage-size pill, VRAM in the compatibility label, expanded technical details, and a muted downloaded state.
- Settings shows dynamic downloaded-model disk usage as a progress meter with free space and percentage, refreshing after model changes, window focus, and a bounded polling interval.
- Agents supports a native folder picker, one primary root plus additional roots, the OS home directory as the initial default, and sessions grouped under the normalized workspace in Chat History.
- Multi-root tools cannot read, write, execute, or traverse outside the selected root union, including through symlinks, relative traversal, or a conflicting requested working directory.
- The redundant Agents Sessions tab is removed; Chat, Memory, and Activity have clear labels and explanatory empty states.

**Success metrics**:

- Zero unhandled installer exceptions in clean-install and repair-install frozen smoke tests.
- 100% parity for selectable catalog ids and sort order between Python installer and TypeScript Settings fixtures.
- 100% archive/delete active-session reset coverage across all four tabs.
- 100% multi-root escape-fixture rejection and selected-root fixture acceptance.
- No runtime readiness claim without an observed environment smoke result.
- WCAG keyboard/focus behavior for token detail, reasoning disclosure, archive/delete dialogs, and workspace controls.

**Strategy grounding**: No `STRATEGY.md` or version-scoped strategy anchor was found. This plan uses the repository's local-first product identity, module authorship contract, evidence tiers, and no-generation-as-a-service policy. No `docs/solutions/` store was found, so no prior solution changed the design.

## Non-Goals

- Do not implement the v2.4.0 Gaussian-splat plan or change its model-admission decisions.
- Do not expose hidden chain-of-thought, synthesize reasoning text from token counts, or label ordinary output as reasoning.
- Do not add cloud sync, remote archives, remote workspaces, or network filesystem discovery.
- Do not automatically delete model weights found outside the Nexus catalog.
- Do not bundle a multi-gigabyte universal Python wheel payload into the slim installer; fetch the pinned host-specific payload and retain a verified local cache.
- Do not redesign the global navigation or replace the Chat History explorer.

## Constitution Check

*GATE: Must pass before Phase 1 implementation. Re-check after Phase 2 runtime design.*

No constitution file found at docs/<version>/constitution.md - skipping check. Recommend running /constitution to establish project principles.

## Current Model Map

**Model map status**: fresh as of 2026-08-29; machine validation returned `valid: true`, four tiers, and four providers.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.5-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

### Model Map Sources

- Anthropic: https://platform.claude.com/docs/en/models/overview
- OpenAI: https://developers.openai.com/api/docs/models, https://developers.openai.com/api/docs/guides/latest-model
- Google: https://ai.google.dev/gemini-api/docs/models
- Cursor: https://cursor.com/docs/models-and-pricing

## Architecture Decisions

1. **Slim, pinned runtime repair**: Reuse `DiffusionVenvProvisioner` from the active `RuntimeProvisioner`. Download a host-specific, versioned manifest with SHA-256 verification into the existing per-user cache. Never silently fall back from CUDA to CPU when a selected feature requires CUDA.
2. **Truthful runtime contract**: Bump `runtime.json` and record component status, smoke evidence, provisioner version, and actionable failure code. `diffusionPython` alone is not evidence of CUDA readiness.
3. **Provider-exposed reasoning only**: Add a separate optional reasoning stream/persisted field. Render it only when the model runtime supplies content or a provider-supplied summary. Token counts remain usage metadata.
4. **Archive in place**: Add nullable `archived_at`/`archivedAt` to existing session records. Default lists exclude archived rows. Restore preserves the original pillar and parent when possible.
5. **One catalog display contract**: User-selectable catalog rows are visible on both surfaces. Dependency-only rows remain in expanded component details. Discovered non-catalog weights remain in a clearly separated Settings-only section because they were not installer choices.
6. **Workspace identity by normalized root set**: A workspace id is derived from sorted, canonical, case-normalized roots. UI order retains the primary root, but identity does not change when chips are reordered.
7. **Union-of-roots authorization**: The primary root supplies the default working directory. Every filesystem or terminal path must resolve inside at least one selected root. Existing single-root sessions migrate to a one-element root array.

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort |
|---|---|---|---|---|
| 1 | Installer Completion and Progress UX | Frozen installs resolve Unsloth data, contain optional failures, and show an obvious overall percentage | strong | high |
| 2 | Diffusion Runtime Provisioning and Readiness | Clean installs create and verify the correct local Python/CUDA runtime before media readiness is claimed | frontier | max |
| 3 | Transcript Chrome and Reasoning Disclosure | Pending state, bubbles, token metadata, and model-provided reasoning become quieter and clearer | strong | high |
| 4 | Archive, Restore, Delete, and Active-State Reset | Every pillar has durable archives, explicit delete safety, and consistent transcript reset behavior | frontier | max |
| 5 | Canonical Model Library and Disk Meter | Installer and Settings share roster/order, richer cards, subdued downloaded styling, and dynamic disk use | strong | high |
| 6 | Multi-Root Workspace Core and Security | Coding sessions persist a normalized root set and tools are confined to its union | frontier | max |
| 7 | Agents Workspace and History Experience | Native folder chips, workspace history directories, and simplified tabs ship as one coherent flow | strong | high |
| 8 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Evidence, docs, full gates, human field checks, publication, and integration complete the release | frontier | max |

## Task Inventory

- [x] T001 [US1] Add the prominent animated overall progress widget scripts/installer/src/nexus_installer/widgets/overall_progress.py
- [x] T002 [US1] Bundle and resolve Unsloth pins in frozen and checkout modes scripts/installer/build/nexus-installer.spec
- [x] T003 [US1] Contain optional Unsloth failures and preserve truthful completion state scripts/installer/src/nexus_installer/engine/installer.py
- [x] T004 [US1] Add frozen-resource and overall-progress installer tests scripts/installer/tests/test_installing_page.py
- [x] T005 [US1] Stabilize Phase 1 and record evidence docs/archive/v2/v2.4/development/history/2026-08-29_v2.4.1-phase-1-installer.md
- [x] T006 [US2] Define pinned host-specific diffusion artifacts scripts/installer/build/versions.lock.json
- [x] T007 [US2] Wire diffusion environment creation into the active runtime provisioner scripts/installer/src/nexus_installer/engine/runtime_provisioner.py
- [x] T008 [US2] Persist smoke evidence and typed readiness in runtime.json scripts/installer/src/nexus_installer/engine/runtime_provisioner.py
- [x] T009 [US2] Fail closed in Image and Video runtime launchers desktop/sidecar/src/image/diffusionRuntime.ts
- [x] T010 [US2] Add clean-host, repair, offline-cache, and CUDA smoke tests scripts/installer/tests/test_runtime_provisioner.py
- [x] T011 [US2] Stabilize Phase 2 and record evidence docs/archive/v2/v2.4/development/history/2026-08-29_v2.4.1-phase-2-diffusion.md
- [x] T012 [P] [US3] Separate pending assistant state from response bubbles desktop/src/shared/chat/MessageBubble.tsx
- [x] T013 [P] [US3] Add subtle semantic user and assistant bubble colors desktop/src/styles.css
- [x] T014 [US3] Replace verbose token copy with an accessible metadata footer desktop/src/shared/chat/transcriptChrome.ts
- [x] T015 [US3] Add optional model-provided reasoning stream and persistence desktop/sidecar/src/protocol.ts
- [x] T016 [US3] Render a collapsed reasoning disclosure above assistant output desktop/src/shared/chat/ReasoningDisclosure.tsx
- [x] T017 [US3] Stabilize Phase 3 and record evidence docs/archive/v2/v2.4/development/history/2026-08-29_v2.4.1-phase-3-transcript.md
- [x] T018 [P] [US4] Add archive migrations to Chatbot and Studio session stores modules/chat/storage/ChatExplorerStore.ts
- [x] T019 [P] [US4] Add archive migration to Coding JSON sessions desktop/sidecar/src/coding/sessionStore.ts
- [x] T020 [US4] Add unified archive list and restore IPC desktop/sidecar/src/handlers.ts
- [x] T021 [US4] Add Archive beside Delete and explicit confirmation dialogs desktop/src/shared/explorer/FolderTree.tsx
- [x] T022 [US4] Reset active views only after successful archive or delete desktop/src/modules/chat/ChatPage.tsx
- [x] T023 [US4] Add Settings Archived Chats grouped by pillar desktop/src/pages/settings/ArchivedChatsSettings.tsx
- [x] T024 [US4] Stabilize Phase 4 and record evidence docs/archive/v2/v2.4/development/history/2026-08-29_v2.4.1-phase-4-archives.md
- [x] T025 [US5] Define the canonical selectable roster and sort contract core/registry/modelDisplayPolicy.ts
- [x] T026 [US5] Enforce descriptions and installer/Settings parity core/registry/catalog.json
- [x] T027 [US5] Redesign model card summary, pills, compatibility, and downloaded state desktop/src/pages/settings/ModelsSettings.tsx
- [x] T028 [US5] Replace disk copy with a dynamic model-storage meter desktop/sidecar/src/models/modelsService.ts
- [x] T029 [US5] Apply the same roster, ordering, and card facts in the installer scripts/installer/src/nexus_installer/pages/typed_catalog.py
- [x] T030 [US5] Stabilize Phase 5 and record evidence docs/archive/v2/v2.4/development/history/2026-08-29_v2.4.1-phase-5-models.md
- [x] T031 [US6] Add normalized WorkspaceScope and single-root migration core/project/WorkspaceScope.ts
- [x] T032 [US6] Extend coding protocol and persistence to workspace roots desktop/sidecar/src/protocol.ts
- [x] T033 [US6] Enforce union-of-roots filesystem and terminal authorization desktop/sidecar/src/coding/headlessAgentRunner.ts
- [x] T034 [US6] Add native folder picker capability desktop/src-tauri/src/lib.rs
- [x] T035 [US6] Stabilize Phase 6 security and migration behavior docs/archive/v2/v2.4/development/history/2026-08-29_v2.4.1-phase-6-workspace-core.md
- [x] T036 [US7] Replace the workspace text field with primary and additional folder chips desktop/src/modules/coding/CodingPage.tsx
- [x] T037 [US7] Group coding sessions under automatic workspace directories desktop/src/shared/explorer/codingSessionsAsChatExplorer.ts
- [x] T038 [US7] Remove Sessions and clarify Chat, Memory, and Activity desktop/src/modules/coding/CodingPage.tsx
- [x] T039 [US7] Stabilize end-to-end Agents workspace flows docs/archive/v2/v2.4/development/history/2026-08-29_v2.4.1-phase-7-agents-ui.md
- [x] T040 Refactor architecture and record evidence docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.1.md
- [x] T041 Reconcile known gaps across active releases docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.1.md
- [x] T042 Verify living documentation architecture docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.1.md
- [x] T043 Report git-tree hygiene docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.1.md
- [x] T044 Reconcile terminal CI/CD coverage docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.1.md
- [x] T045 Perform an independent Goal-vs-codebase review docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.1.md
- [x] T046 Run last-phase-only human testing docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.1.md
- [x] T047 Run full local stabilization docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.1.md
- [ ] T048 Publish and integrate the completed branch docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.1.md

---

## Phase 1: Installer Completion and Progress UX

**Goal**: Make installation progress visually dominant and prevent the optional Unsloth provisioner from turning an otherwise successful install into an unhandled engine exception.
**Prerequisites**: Clean `develop`, the supplied installer log retained as field evidence, and a host-matched desktop payload for frozen tests.
**Stability Gate**: Frozen-resource probes find catalog, runtimes, and Unsloth pins; optional Unsloth failure produces an actionable failed sub-step while the completion page accurately distinguishes core success from optional failure; reduced motion is honored.
**Recommended model tier**: strong
**Recommended effort**: high
**Rationale**: The root causes are localized, but PyInstaller resource resolution and Qt animation lifetime require careful packaged testing.

### 1.1 -- Prominent overall progress widget (T001)

**Objective**: Give overall installation progress stronger hierarchy than phase bars.

**Prompt**:
> Create `scripts/installer/src/nexus_installer/widgets/overall_progress.py` and replace the generic overall `QProgressBar` in `pages/installing.py`. Use an 18-20 px track, readable `%p%` text, high-contrast foreground, and a timer-driven gradient offset while work is active. Keep subordinate phase/model bars at their current 4-8 px sizes. Stop timers when hidden or complete. When reduced motion is enabled, show the gradient without movement. Add a weighted progress aggregator whose stable denominator comes from the selected install steps; long downloads contribute byte progress when content length is known, and completed steps contribute their full weight. Preserve indeterminate behavior only before the first numeric event or for an individual unknown-length operation; once overall progress is known, clamp to 0-100, keep it monotonic for one install attempt, and expose it to accessibility APIs.

**Failure modes**: Missing or malformed progress events leave the last valid value and log a warning; a slow engine keeps the animation bounded without creating timers per event; concurrent phase events are serialized through the existing UI signal and cannot move overall progress backward unless the engine explicitly starts a new install attempt.

### 1.2 -- Frozen Unsloth pins resolution (T002)

**Objective**: Make `core/tuning/unsloth-pins.json` a required frozen resource with one resolver contract.

**Prompt**:
> Add `unsloth-pins.json` to `nexus-installer.spec` with a build-time existence and JSON-schema check. Extend `nexus_installer.registry_paths` with a generic frozen/check-out resource resolver and make `UnslothVenvProvisioner._pins_path()` use it. Remove the relative-CWD fallback. Add a frozen-mode test using a temporary `_MEIPASS` and a build-spec assertion. Do not duplicate pin content inside Python.

**Failure modes**: Missing or malformed pins fail the build before packaging; a missing frozen resource returns a typed optional-component failure rather than a raw `FileNotFoundError`; concurrent installer instances remain governed by the existing single-instance lock and never rewrite the pin file.

### 1.3 -- Optional-step containment and truthful completion (T003)

**Objective**: Keep optional Unsloth provisioning isolated from required desktop/runtime success.

**Prompt**:
> Refactor the install engine step runner so every step reports required/optional, terminal status, error code, and retryability. Catch unexpected exceptions at the step boundary, log a sanitized traceback, mark only that step failed, and compute final state from required-step success plus optional warnings. Unsloth remains opt-in. The completion page must say which optional component failed and offer retry/details; it must never display a clean-success claim if a required component failed. Preserve idempotent rerun behavior.

**Failure modes**: Malformed step results become failed steps with `INVALID_STEP_RESULT`; unavailable/slow dependencies time out with retry guidance; simultaneous cancel and exception resolve once, with cancellation winning before any new step starts.

### 1.4 -- Installer resource and UI tests (T004)

**Objective**: Add deterministic coverage for the field failures and visual contract.

**Prompt**:
> Add Qt tests for overall size hierarchy, percentage text, monotonic updates, timer cleanup, and reduced motion. Add installer tests for checkout and frozen Unsloth resolution, malformed pins, optional exception containment, and accurate completion summaries. Add a build manifest probe that opens the produced executable's collected data manifest without launching external installers.

### 1.5 -- Testing and Stabilization (T005)

**Prompt**:
> Run focused installer pytest, ruff, frozen-data probes, and the smallest Windows onefile smoke that can prove bundled resources. Fix failures. Record observed support tier, exact commands, counts, and CI IMPACT in `docs/archive/v2/v2.4/development/history/2026-08-29_v2.4.1-phase-1-installer.md`. Do not run remote CI. Create one local ASCII-only commit for Phase 1.

### Phase 1 Exit Checklist

- [x] T001-T005 complete
- [x] Overall progress is larger, animated, percentage-bearing, and reduced-motion safe
- [x] Unsloth pins resolve in a frozen build
- [x] Optional failures no longer crash the engine
- [x] Focused checks pass and evidence is recorded
- [x] One local commit exists; nothing was pushed

---

## Phase 2: Diffusion Runtime Provisioning and Readiness

**Goal**: Provision the Python environment that Image Studio actually uses and prove CUDA readiness before enabling local media generation.
**Prerequisites**: Phase 1 and the existing `DiffusionVenvProvisioner` test baseline.
**Stability Gate**: A clean supported NVIDIA fixture creates a venv, installs only pinned verified artifacts, passes import and CUDA smoke tests, and writes ready status; CPU-only or failed hosts remain usable for chat but cannot enter a false image-ready state.
**Recommended model tier**: frontier
**Recommended effort**: max
**Rationale**: Python ABI, CUDA wheel selection, resumable downloads, security verification, and packaged Windows behavior form a high-risk installation boundary.

### 2.1 -- Pinned diffusion artifact manifest (T006)

**Objective**: Define the slim installer's authoritative host-specific Python and torch inputs.

**Prompt**:
> Extend `scripts/installer/build/versions.lock.json` with Python ABI, torch/CUDA or platform backend, runtime requirements, source URL, filename, size, and SHA-256 per supported OS/architecture/GPU path. Use official vendor download origins only. Update the existing payload fetcher to download into a content-addressed per-user cache, resume partial files safely, verify before use, and never execute an unverified artifact. Keep CPU and platform alternatives explicit; do not silently choose CPU for a catalog selection that requires CUDA.

**Failure modes**: Missing/malformed pins fail the provision step before environment mutation; unavailable or slow downloads retain verified cache entries and return typed retryable errors; concurrent downloads use an atomic lock and temp file so one process cannot consume another's partial artifact.

### 2.2 -- Active diffusion environment provisioning (T007)

**Objective**: Reconnect `DiffusionVenvProvisioner` to the active `RuntimeProvisioner`.

**Prompt**:
> In `runtime_provisioner.py`, determine whether selected catalog tasks require diffusion, then invoke the existing `DiffusionVenvProvisioner` before writing `runtime.json`. Reuse an environment only when its manifest fingerprint matches. On mismatch, build a staging venv and atomically swap it after smoke success so repair cannot destroy the last working environment. Emit numeric sub-progress through the Phase 1 progress contract. Keep Node and chat setup independently usable if diffusion fails.

**Failure modes**: Missing/corrupt venvs trigger repair from verified inputs; unavailable/slow package installation times out and preserves the previous healthy environment; concurrent repair attempts serialize by environment lock and never swap a half-built venv.

### 2.3 -- Runtime readiness evidence (T008)

**Objective**: Make `runtime.json` an honest capability contract, not a path snapshot.

**Prompt**:
> Bump the runtime schema and record `diffusion.status`, backend, Python/torch versions, CUDA version, `torch.cuda.is_available`, GPU name, smoke timestamp, manifest fingerprint, and typed failure code. Write atomically. The legacy `diffusionPython` field may remain for one compatibility cycle, but sidecar readiness must use the new evidence. Scrub user paths and hardware identifiers from logs according to existing observability policy.

**Failure modes**: Malformed legacy config migrates to unknown/not-ready; unavailable or slow smoke processes are killed with a bounded timeout; concurrent config writers use replace semantics and the sidecar reads either the old complete file or new complete file.

### 2.4 -- Sidecar fail-closed launch and recovery copy (T009)

**Objective**: Prevent downloaded weights from being mistaken for a working image runtime.

**Prompt**:
> Update the diffusion launcher and capability DTO so Image and Video actions are enabled only for a matching successful smoke. Return an actionable error that distinguishes missing runtime, CPU-only torch, unsupported GPU, corrupt config, and busy repair. Link the user to rerun/repair the installer; do not claim NVIDIA VRAM telemetry proves diffusion readiness. Preserve existing generation job identity and cancellation behavior.

**Failure modes**: Missing/malformed runtime config yields not-ready; unavailable/slow child startup times out and terminates the child; conflicting launch/repair requests make repair exclusive and return busy without queuing duplicate runtimes.

### 2.5 -- Runtime field matrix tests (T010)

**Prompt**:
> Add clean-host, compatible-cache, checksum-failure, offline-no-cache, stale-venv, CPU-only torch, CUDA-smoke-success, timeout, cancel, and concurrent-repair tests. Add a packaged probe that launches the installed Python with `-c` and the exact imports used by the sidecar. Mock downloads in CI; reserve live NVIDIA execution for Phase 8 human/hardware evidence.

### 2.6 -- Testing and Stabilization (T011)

**Prompt**:
> Run installer pytest, runtime Python tests, sidecar tests, lint, and TypeScript build for changed paths. Record exact evidence and CI IMPACT in `docs/archive/v2/v2.4/development/history/2026-08-29_v2.4.1-phase-2-diffusion.md`. Re-check the Constitution Check. Do not run remote CI. Create one local ASCII-only commit for Phase 2.

### Phase 2 Exit Checklist

- [x] T006-T011 complete
- [x] Clean selected-media installs provision a real environment
- [x] CUDA readiness is observed, persisted, and enforced
- [x] Failed repair preserves chat and any prior healthy environment
- [x] Focused checks pass and one local commit exists

---

## Phase 3: Transcript Chrome and Reasoning Disclosure

**Goal**: Make chat generation state, bubble styling, token metadata, and optional model-provided reasoning discreet and comprehensible across all pillars.
**Prerequisites**: Existing shared `MessageBubble`, `AgentStateOrb`, and usage-token contracts.
**Stability Gate**: Pending rows are bubble-free; completed messages have subtle role separation; token total and tooltip are accurate and accessible; reasoning is collapsed, separate, persisted, and absent when not supplied.
**Recommended model tier**: strong
**Recommended effort**: high
**Rationale**: Shared UI changes are moderate risk, while stream/persistence evolution needs backward compatibility across four tabs.

### 3.1 -- Bubble-free pending state (T012)

**Prompt**:
> Refactor `MessageBubble` and `AgentStateOrb` so an assistant pending message renders as an inline status row without article background, border, or padded bubble. The actual assistant bubble appears only when output content or a terminal error exists. Keep the accessible status announcement, cancellation state, retained animation cap, and reduced-motion behavior.

**Failure modes**: Missing status renders a stable generic generating label; a stalled stream keeps one status instance and no growing DOM; concurrent pending messages retain unique keys and do not share animation state.

### 3.2 -- Subtle semantic bubble colors (T013)

**Prompt**:
> Add neutral `--bubble-user` and `--bubble-assistant` tokens with a small luminance difference and measured text/border contrast. Apply them through shared classes rather than pillar-specific colors. Avoid green, cyan, or success-state washes. Verify high-contrast and reduced-transparency modes remain legible.

**Failure modes**: Missing theme tokens fall back to existing neutral surfaces; no external dependency is involved; simultaneous theme changes update both roles without transient unreadable inline styles.

### 3.3 -- Token metadata footer (T014)

**Prompt**:
> Replace verbose token prose with an outside-the-bubble metadata row directly below the bubble: time aligned to the start and italic `(X tokens)` aligned to the end. Total is the sum of reported input, reasoning, and output counts, without double-counting provider totals. Add a keyboard-focusable accessible tooltip with input, reasoning, and output details and unavailable labels where a provider omits a field. Mark estimates explicitly. Keep timestamps localized.

**Failure modes**: Malformed/negative counts are ignored; there is no remote dependency; late usage events update the existing footer atomically and never produce `NaN` or a decreasing final total.

### 3.4 -- Separate reasoning stream and persistence (T015)

**Prompt**:
> Extend the sidecar protocol with an optional `reasoning_delta` stream event and optional persisted `reasoningText`/provider summary field. Capture only a reasoning channel explicitly returned by the local runtime; never infer hidden chain-of-thought from ordinary output. Add bounded size, local redaction rules, schema migration, and hydration support for Chatbot, Coding, Image, and Video session records. Preserve count-only providers without creating fake content.

**Failure modes**: Malformed reasoning events are dropped and logged without corrupting output; a provider that omits or delays the channel still completes normally; concurrent reasoning/output deltas are ordered by stream sequence and persisted once on completion.

### 3.5 -- Collapsed reasoning disclosure (T016)

**Prompt**:
> Create `ReasoningDisclosure.tsx` above the assistant output bubble. Default it collapsed with a discreet chevron, `Reasoning`, and optional token count/duration. Expanded content is selectable, scroll-bounded, keyboard operable, and screen-reader labeled. Hide the control when there is no provider-supplied content. Errors in reasoning rendering must not hide the final response.

**Failure modes**: Missing/malformed content hides the disclosure; no external dependency is required; expansion state is local to each message and remains stable while late deltas append.

### 3.6 -- Testing and Stabilization (T017)

**Prompt**:
> Add shared component, protocol, persistence migration, hydration, keyboard, tooltip, contrast, reduced-motion, partial-stream, count-only, and reasoning-channel tests. Run focused desktop tests, ESLint, TypeScript builds, and sidecar tests. Record evidence and CI IMPACT in the Phase 3 history file. Create one local ASCII-only commit; do not push or run remote CI.

### Phase 3 Exit Checklist

- [x] T012-T017 complete
- [ ] Pending animation has no bubble
- [ ] Metadata and role colors match the requested hierarchy
- [ ] Reasoning content is honest, separate, collapsed, and optional
- [ ] Accessibility and migration tests pass

---

## Phase 4: Archive, Restore, Delete, and Active-State Reset

**Goal**: Give every chat-bearing tab one durable archive/delete lifecycle and eliminate stale active transcripts after either action.
**Prerequisites**: Phase 3 persistence schema decisions.
**Stability Gate**: Archive/restore/delete behavior is transactionally correct across Chatbot, Agents, Images, and Videos; active state resets only after backend success; Settings lists and restores archives by pillar.
**Recommended model tier**: frontier
**Recommended effort**: max
**Rationale**: Three persistence engines, cascade deletion, in-flight jobs, and shared explorer callbacks create high data-loss and partial-failure risk.

### 4.1 -- SQLite archive migrations (T018)

**Prompt**:
> Add nullable `archived_at` to ChatExplorer and Studio session tables with idempotent migration. Default tree, search, recent, and session-open queries exclude archives. Add archive and restore methods in transactions. Preserve messages, attachments, media provenance, folder identity, and original parent. Studio rows retain pillar so Image and Video remain separate in Settings.

**Failure modes**: Missing/corrupt rows return not-found without table-wide mutation; a locked/slow database yields a typed retryable error; concurrent archive/delete/restore on one id uses a transaction and exactly one terminal disposition.

### 4.2 -- Coding JSON archive migration (T019)

**Prompt**:
> Bump the coding session JSON schema and add optional `archivedAt`, retaining tolerance for the current v2 records. Write atomically through a temp file and replace. Default list excludes archived sessions; archive list includes only them. Preserve unknown forward-compatible fields and never rewrite unrelated sessions during a failed mutation.

**Failure modes**: Malformed files are quarantined/read-only with an actionable error, not overwritten; unavailable/slow filesystem writes leave the old file intact; concurrent mutations serialize through the store lock and re-read before replace.

### 4.3 -- Unified archive service and IPC (T020)

**Prompt**:
> Add typed IPC requests to archive, list archived, and restore. Aggregate the three stores into DTOs with pillar, title, archived time, original parent/workspace, and stable id. Restore to the original parent when it exists; otherwise restore to the pillar root and report that fallback. Keep permanent delete as the existing store operation with a stronger confirmation contract.

**Failure modes**: Malformed ids/pillars fail validation; an unavailable/slow store returns a per-pillar error without hiding results from other stores; concurrent aggregate requests preserve store transaction semantics and deduplicate stable ids.

### 4.4 -- Explorer actions and confirmations (T021)

**Prompt**:
> Add an Archive icon immediately left of Delete for chat/session rows. Archive opens a confirmation explaining it can be restored from Settings. Delete opens a danger dialog that says the action cannot be undone and offers `Archive instead`, `Cancel`, and `Delete permanently`. Disable actions while the request is pending, restore focus on cancel/error, and announce success or failure. Folder deletion behavior remains unchanged unless it represents a single session row.

**Failure modes**: Missing row ids disable actions; a slow backend shows pending state and prevents double-submit; simultaneous archive/delete clicks resolve through one in-flight action token.

### 4.5 -- Active transcript reset and in-flight work (T022)

**Prompt**:
> Add an `onSessionDisposition` callback from FolderTree adapters to each page. If the affected id is active, request cancellation of its stream/job, wait for terminal cancellation or bounded failure, perform storage mutation, then clear active id, messages, attachments, composer state, and context meter only after success. On failure, keep the active transcript intact. Apply to ChatPage, CodingPage, ImageStudioPage, and VideoLabPage.

**Failure modes**: Missing active ids are no-ops; unavailable/slow cancellation blocks destructive mutation and explains why; competing completion and disposition events are guarded by session generation/id so late events cannot repopulate the reset view.

### 4.6 -- Settings Archived Chats (T023)

**Prompt**:
> Add an `Archived chats` Settings section grouped by Chatbot, Agents, Images, and Videos. Show title, archived date, and original folder/workspace. Restore is the only mutation in this release and is disabled while pending. After restore, remove the row and refresh the corresponding history explorer. Include empty, partial-store-error, and loading states.

**Failure modes**: Malformed entries are skipped with a count; unavailable/slow pillar queries show partial results plus retry; concurrent restore/list merges by stable id without resurrecting duplicates.

### 4.7 -- Testing and Stabilization (T024)

**Prompt**:
> Add migration, cascade-preservation, archive filtering, restore fallback, irreversible copy, archive-instead, focus, in-flight cancellation, backend-failure, late-event, and active-reset tests for all pillars. Run focused root/desktop tests, lint, TypeScript builds, and SQLite tests. Record evidence and CI IMPACT in the Phase 4 history file. Create one local ASCII-only commit; do not push.

### Phase 4 Exit Checklist

- [x] T018-T024 complete
- [ ] Archive and delete are explicit and durable
- [ ] Active pages reset only after success
- [ ] Settings restores archives by pillar
- [ ] Data-loss, migration, and concurrency tests pass

---

## Phase 5: Canonical Model Library and Disk Meter

**Goal**: Make installer and Settings present the same current selectable models in the same useful order, with quieter downloaded styling and clearer resource facts.
**Prerequisites**: Existing 38-row catalog, recommendation metadata, Python model router, and TypeScript Settings catalog adapter.
**Stability Gate**: Golden fixtures prove identical ids/order; every selectable row has a visible sentence; cards have no raw summary line; disk meter uses live values and correct arithmetic.
**Recommended model tier**: strong
**Recommended effort**: high
**Rationale**: The behavior is well bounded, but cross-language parity and external installed weights need explicit rules.

### 5.1 -- Canonical roster and ordering (T025)

**Prompt**:
> Add a typed display policy consumed by the TypeScript registry and mirrored by the Python router: include every user-selectable catalog row (`task` present), exclude dependency-only rows from primary cards while listing them under components/details, never filter incompatible models, and sort within each category by required/recommended tier, `releaseDate` descending, then case-folded name and id. Remove family collapse and downloaded-first ordering. Add golden id/order fixtures shared by Python and TypeScript tests. Keep discovered non-catalog weights in a separate Settings-only `Installed outside catalog` group.

**Failure modes**: Missing/malformed dates sort last with stable name fallback; unavailable catalog loading fails closed with an explicit empty/error state; concurrent installs update status without changing the canonical relative sort.

### 5.2 -- Catalog description and parity invariants (T026)

**Prompt**:
> Require at least one complete sentence in `description` for every selectable catalog row. Add or improve factual local-use descriptions for rows such as Gemma 4 E4B and SANA 1.5. Validate installer and desktop source the same bundled catalog version/hash. Discovered external rows use a clearly labeled metadata-unavailable fallback rather than fabricated capabilities.

**Failure modes**: Missing/malformed descriptions fail catalog build invariants; unavailable catalog files fail the build; concurrent catalog refreshes swap whole validated snapshots and never mix versions.

### 5.3 -- Model card hierarchy and downloaded styling (T027)

**Prompt**:
> Remove the raw `id - task - size - VRAM` line. Always show the description sentence. Add storage size as a pill. Render compatibility as `Compatible - N GB VRAM` or `Incompatible - needs N GB VRAM`. Put id, task, strengths, recommendation explanation, license, backend, components, and technical metadata in expanded Details. Replace the green full-card downloaded wash with a neutral card, muted downloaded pill, and subtle low-saturation indicator. Preserve keyboard and screen-reader behavior.

**Failure modes**: Missing facts omit only that pill and never show `undefined`; there is no external dependency; install/remove state changes update one card by id and cannot recolor unrelated rows.

### 5.4 -- Dynamic model-storage meter (T028)

**Prompt**:
> Extend `DiskUsageDto` with capacity/free/model bytes, measurement path, and timestamp. Deduplicate model files by resolved path. In Settings, replace the sentence with an accessible progress meter whose numerator is downloaded model bytes and denominator is model bytes plus currently free bytes (the space that would be free if models were removed). Label exact model size, free space, and percentage. Refresh after install/remove, on window focus, and every 30 seconds while visible; stop polling when hidden.

**Failure modes**: Missing/malformed filesystem stats show unavailable without a fake zero; slow disk measurement is timeout/cached and never blocks cards; concurrent polls use latest-request-wins and install/remove triggers one coalesced refresh.

### 5.5 -- Installer model-page parity (T029)

**Prompt**:
> Apply the canonical roster/order and the same description, size, and VRAM facts in the installer. Keep installer-specific selection controls and compatibility disabling, but do not hide incompatible rows or collapse families. Ensure the displayed catalog hash matches Settings. Update screenshots/golden widget tests at common and narrow window sizes.

**Failure modes**: Malformed catalog rows are rejected by build invariants; unavailable catalog data fails the packaged build rather than showing a partial stale list; concurrent selection changes do not reorder rows or lose checked ids.

### 5.6 -- Testing and Stabilization (T030)

**Prompt**:
> Run cross-language parity fixtures, catalog invariants, installer model-page tests, Settings component/accessibility tests, disk arithmetic and polling tests, lint, and builds. Record evidence and CI IMPACT in the Phase 5 history file. Create one local ASCII-only commit; do not push or run remote CI.

### Phase 5 Exit Checklist

- [x] T025-T030 complete
- [x] Installer and Settings roster/order are identical
- [x] Model cards show description, size, VRAM, and quieter status
- [x] Disk meter is live, accurate, and accessible
- [x] Parity and catalog gates pass

---

## Phase 6: Multi-Root Workspace Core and Security

**Goal**: Introduce a durable multi-root coding workspace without widening tool access beyond the selected folders.
**Prerequisites**: Phase 4 coding session schema migration and existing path guards/sandbox contracts.
**Stability Gate**: Single-root sessions migrate; normalized root-set identity is stable; all filesystem/terminal escape fixtures fail; primary-root commands and explicit selected-root commands pass.
**Recommended model tier**: frontier
**Recommended effort**: max
**Rationale**: A multi-root workspace changes the security perimeter of every coding tool and scheduled/headless path.

### 6.1 -- WorkspaceScope and migration (T031)

**Prompt**:
> Create `core/project/WorkspaceScope.ts` with `workspaceId`, primary root, ordered roots, normalized identity roots, display label, and timestamps. Canonicalize absolute existing directories, apply Windows case rules, deduplicate equivalents, and reject parent traversal. Initialize a new install to the OS home directory. Migrate persisted `workspacePath` to a one-element root array without changing old session ids. Workspace identity is a hash of sorted normalized roots; chip order does not affect it.

**Failure modes**: Missing/malformed roots fail validation and retain the prior scope; unavailable/slow filesystem canonicalization is bounded and reports the offending root; concurrent workspace creation deduplicates by identity and atomically updates last-used metadata.

### 6.2 -- Protocol and session persistence (T032)

**Prompt**:
> Extend coding start/list DTOs and persisted sessions with `workspaceId`, `workspaceRoots`, and `primaryRoot`, retaining `workspacePath` input for one compatibility cycle. Hydration returns the original root set. Scheduled/headless runs snapshot an immutable scope at creation; later UI changes do not silently expand an active run. Persist workspace records in the existing local Nexus storage tree.

**Failure modes**: Malformed root arrays fail schema validation; unavailable/slow workspace storage prevents session start rather than using process cwd; concurrent starts for one workspace receive distinct session ids but the same workspace id.

### 6.3 -- Union-of-roots tool authorization (T033)

**Prompt**:
> Refactor headless runner, path guards, terminal cwd validation, attachments, rule discovery, MCP workspace config, memory scope, and durable sandbox inputs to accept `WorkspaceScope`. Use primary root as default cwd. Resolve real paths before authorization; a path is allowed only under at least one root. Cross-root copy/move is allowed only when both endpoints are selected. Do not scan every root implicitly for rules: load workspace-global user config plus each root's local rules in deterministic root order and label provenance.

**Failure modes**: Missing/malformed paths are denied; unavailable/slow roots fail only operations targeting that root while session scope stays intact; concurrent root operations retain per-call resolved authorization and cannot exploit a root-set update race.

### 6.4 -- Native folder picker capability (T034)

**Prompt**:
> Add the official Tauri 2 dialog plugin or a minimal native Rust directory picker command, with directory-only, multi-select, cancellation, and capability allowlisting. Return canonical absolute paths to the frontend. Do not use browser upload inputs or grant general shell access. Pin the dependency consistently in Cargo and npm lockfiles.

**Failure modes**: Missing/malformed picker results are ignored; an unavailable/slow native dialog can be canceled without blocking the sidecar; concurrent picker requests are serialized so results cannot attach to the wrong action.

### 6.5 -- Testing and Stabilization (T035)

**Prompt**:
> Add migration, home-default, normalization, case, duplicate, ancestor/child, symlink/junction, traversal, terminal cwd, cross-root move, removed root, scheduled snapshot, picker cancel, and concurrent-change tests. Run focused root/desktop/Rust tests, dependency boundaries, lint, and builds. Record evidence and CI IMPACT in the Phase 6 history file. Create one local ASCII-only commit; do not push.

### Phase 6 Exit Checklist

- [x] T031-T035 complete
- [x] Workspace identity and migration are durable
- [x] Tools are confined to the selected union
- [x] Native picker is least privilege and cancellable
- [x] Security and migration tests pass

---

## Phase 7: Agents Workspace and History Experience

**Goal**: Present multi-root workspaces as a clear folder-chip control and make Chat History the single session-navigation surface.
**Prerequisites**: Phase 6 and Phase 4 archive callbacks.
**Stability Gate**: A user can replace the primary root, add/remove roots, switch to a root-set workspace, see sessions grouped under its workspace directory, and understand the remaining tabs.
**Recommended model tier**: strong
**Recommended effort**: high
**Rationale**: The UI is bounded but must coordinate active session state, history grouping, archive behavior, and native picker results.

### 7.1 -- Workspace folder chips (T036)

**Prompt**:
> Replace the path input with a workspace row: clickable primary folder chip, `+` add-folder button, additional removable chips, and concise path tooltips. Clicking the primary chip replaces it through the native picker; `+` appends selected directories. Default to OS home when no saved workspace exists. Before changing an active busy workspace, explain that the running session is fixed to its current roots and require stop/switch; do not mutate it in place. Persist and restore last-used workspace.

**Failure modes**: Missing/malformed picker paths are rejected without clearing current roots; a slow/canceled picker leaves state untouched; concurrent replace/add actions use request ids and ignore stale dialog results.

### 7.2 -- Workspace directories in Chat History (T037)

**Prompt**:
> Make coding Chat History synthesize one top-level workspace directory per persisted root-set identity and nest every coding session beneath it. Use a readable primary-folder label plus `+N` for additional roots and show full roots in tooltip/details. Creating the first session creates the workspace node automatically. Archive filtering from Phase 4 applies inside each workspace. Remove the localStorage-only folder overlay for coding workspace grouping or migrate user folders beneath a stable `Unsorted` node without losing sessions.

**Failure modes**: Missing workspace metadata migrates to a `Legacy workspace` node; unavailable/slow session listing shows the workspace shell with retry; concurrent session creation and refresh merge by workspace/session id and do not duplicate nodes.

### 7.3 -- Simplified Agents tabs (T038)

**Prompt**:
> Remove the redundant `Sessions` tab because Chat History owns session navigation. Keep `Chat`. Keep `Memory` with an empty-state explanation that it shows knowledge scoped to the selected workspace. Rename `Trace` to `Activity` and explain that it shows tools, approvals, and runtime events. Add accessible tab descriptions/tooltips and preserve deep-link migration from `sessions` to `chat` and `trace` to `activity`.

**Failure modes**: Missing/unknown legacy tab ids fall back to Chat; there is no external dependency; concurrent history selection and tab changes preserve the selected session and do not mount duplicate streams.

### 7.4 -- End-to-end Agents stabilization (T039)

**Prompt**:
> Add end-to-end component tests for first launch at home, replace primary, add multiple roots, dedupe, cancel picker, start session, workspace grouping, switch during idle/busy generation, archive active session, restore from Settings, remove Sessions, tab deep-link migration, and restart hydration. Run desktop tests, Rust tests, lint, builds, and a local packaged picker smoke where available. Record evidence and CI IMPACT in the Phase 7 history file. Create one local ASCII-only commit; do not push.

### Phase 7 Exit Checklist

- [x] T036-T039 complete
- [x] Folder picker and chips replace the raw path field
- [x] Sessions are grouped under automatic workspace directories
- [x] Sessions tab is gone; remaining tabs explain themselves
- [x] End-to-end state, archive, and restart tests pass

---

## Phase 8: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well organized, reconcile every open gap touched by v2.4.1, prove the complete Goal against the codebase and packaged Windows field path, then publish once.
**Prerequisites**: Phases 1-7 complete with one local commit each and no remote CI runs.
**Stability Gate**: `docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.1.md` contains one quoted section per duty below. A missing Goal, an unrecorded review miss, an unverified required installer path, or a missing evidence section blocks publication.
**Recommended model tier**: frontier
**Recommended effort**: max
**Rationale**: Repo-wide evidence, packaged hardware validation, architecture reconciliation, and release integration carry the largest context and blast radius.

### 8.1 -- Architecture refactor (T040)

**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/directories, and overcomplicated structure created or exposed by v2.4.1. Use `project-refactor` and `docs-layout-refactor` only within confirmed scope, propose before moves, and repair every reference. Do not clean unrelated legacy code. Quote detector output under `## Architecture refactor` in the evidence file. An empty finding is allowed only with quoted scan output.

### 8.2 -- Known-gaps reconciliation (T041)

**Prompt**:
> Via `known-gaps-tracker`, create/update the v2.4 known-gaps file and reconcile every active `docs/**/known-gaps.md`. Close only observed items. Record live-GPU, non-Windows picker, or packaged checks that could not be run as explicit deferred field items with owners and next evidence. Quote the disposition under `## Known-gaps reconciliation`.

### 8.3 -- Living docs architecture (T042)

**Prompt**:
> Update living user/architecture documentation for installer repair behavior, runtime readiness, transcript metadata/reasoning, archives, model display policy, and WorkspaceScope. Verify lifespan placement, links, generated HTML parity where applicable, AGENTS module boundaries, and `docs/todos.md`. Quote checks under `## Living docs architecture`.

### 8.4 -- Git-tree hygiene (T043)

**Prompt**:
> Run `python scripts/check_release_preconditions.py --branches --repo-settings`. Quote output under `## Git-tree hygiene`. Report only; never delete branches or rewrite history. Confirm every prior phase has exactly one local commit and no phase pushed.

### 8.5 -- Terminal CI/CD reconciliation (T044)

**Prompt**:
> Via `cicd-architect`, compare repository workflows field by field against the canonical lifecycle. Ensure path filters cover installer Python/spec/data, runtime manifests, desktop/Rust/sidecar, storage migrations, catalog parity, and workspace security tests. Apply pipeline changes only if required by this plan and record justification. Quote the comparison under `## Terminal CI/CD reconciliation`. Do not trigger remote CI yet.

### 8.6 -- Independent Goal-vs-codebase review (T045)

**Prompt**:
> Run an independent read-only review against the exact Goal and observable definition of done, not merely task completion. Trace every requested screenshot issue to code and automated evidence. Classify misses by severity and add every accepted miss to known gaps before proceeding. Quote findings and verdict under `## Goal-vs-codebase review`.

### 8.7 -- Last-phase-only human testing (T046)

**Prompt**:
> After automated review is green, run or provide a numbered operator checklist for a clean Windows install and repair install: visible progress hierarchy/percentage/motion; opt-in Unsloth pins; selected SANA/image runtime creation; real CUDA generation; pending/response bubble transition; token hover/focus; reasoning disclosure on a thinking-capable local model; archive/delete/reset/restore in all pillars; model parity/card/disk meter; and multi-root folder picker/security/history behavior. Record host, versions, screenshots/logs, observed/not-observed status, and support tier under `## Human testing`. Human testing is suggested only in this last phase.

### 8.8 -- Full local stabilization (T047)

**Prompt**:
> Run the full root, isolated desktop, installer, Python runtime, Rust, lint, format/check, TypeScript build, dependency-cruiser, docs/link, Unicode safety, packaging manifest, and coverage gates using quiet output. Attempt `python scripts/validate_unicode_safety.py --strict --fix --root . --path docs/archive/v2/v2.4/plans/v2.4.1-field-reliability-chat-archives-models-workspaces.md`; if the helper is still absent, record that tooling gap and run a deterministic UTF-8/ASCII scan that fails on unsafe punctuation before publication. Quote commands, counts, coverage, skips, and failures under `## Full local stabilization`. Any required failure blocks publication.

### 8.9 -- Publish and integrate (T048)

**Prompt**:
> Only after 8.1-8.8 are green, reconcile version/release notes and create the Phase 8 local ASCII-only commit. Push once, open the plan's first pull request against the integration branch, and let remote CI validate the merge result. Resolve in-scope failures without bypassing gates. After approval and green integration, follow the declared branching strategy for release. Tag/GitHub Release/main integration remain behind the user's explicit release authorization. Record PR, checks, merge SHA, and any authorized publication under `## Publish and integrate`.

### Phase 8 Exit Checklist

- [ ] T040-T048 complete
- [ ] Architecture and living docs match the implemented boundaries
- [ ] Known gaps contain every unobserved hardware/platform claim
- [ ] Independent Goal review is green or every miss is dispositioned
- [ ] Last-phase operator evidence covers the supplied field failures
- [ ] Full local suite, coverage, packaging, docs, and Unicode gates pass
- [ ] One Phase 8 commit exists
- [ ] First push/PR/remote CI occurred only after all local phases completed
- [ ] Release publication followed explicit authorization

## Verification Matrix

| Requested outcome | Primary automated evidence | Last-phase field evidence |
|---|---|---|
| Larger animated overall progress with percentage | Qt widget hierarchy, animation, monotonicity, and reduced-motion tests | Clean/repair installer screenshot and observed percentage |
| Unsloth pins crash fixed | Frozen `_MEIPASS`, spec-manifest, optional exception tests | Opt-in Unsloth packaged run |
| Image generation runtime fixed | Pinned download, venv staging, runtime schema, CUDA smoke mocks | Real NVIDIA torch/CUDA smoke and one generated image |
| Bubble/token/reasoning UX | Shared component, stream, persistence, a11y, and hydration tests | Thinking-capable model transcript walkthrough |
| Archive/delete/reset/restore | Store migrations, transaction, cancellation, late-event, and page reset tests | Four-pillar operator matrix |
| Model parity and presentation | Cross-language golden roster/order, catalog invariants, card tests | Installer vs Settings visual comparison |
| Dynamic disk meter | Disk arithmetic, dedupe, polling/focus tests | Free-space change observed after install/remove |
| Multi-root Agents workspace | Workspace identity/migration, picker, path escape, session grouping tests | Two-folder workspace with read/write in both and denied outside path |

## Residual Risks and Cut Lines

- If live NVIDIA hardware is unavailable, v2.4.1 may ship only after the image-runtime claim is downgraded to candidate and the missing live check is recorded as a release-blocking known gap for any public CUDA-ready claim.
- If multi-root sandbox confinement cannot be proven on Windows, keep the UI behind a default-off feature flag and ship the remaining v2.4.1 repairs; do not weaken path guards.
- If provider reasoning content cannot be separated from output for a runtime, ship the token tooltip but omit the disclosure for that runtime.
- If external installed weights lack catalog metadata, keep them in the separate external group and do not invent description, size, or compatibility.
- If a catalog row cannot be made installable from the same artifact shown in Settings, fail its selectable-status invariant and keep it dependency-only until corrected.

## Commit and CI Contract

- Each phase ends with exactly one local ASCII-only commit after its scoped verification passes.
- Phases 1-7 do not push, open a pull request, or start remote CI.
- Phase 8 reconciles CI, runs all local gates, then performs the first push and pull request.
- No destructive git action, force push, tag, GitHub Release, or main integration occurs without the authorization required by repository policy.
