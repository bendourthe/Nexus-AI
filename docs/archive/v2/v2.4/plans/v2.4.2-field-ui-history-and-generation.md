# Plan -- Field UI, Sidebar History, and Generation Follow-through

**Project**: Nexus AI Studio
**Version**: v2.4.2
**Slug**: field-ui-history-and-generation
**Filename**: v2.4.2-field-ui-history-and-generation.md
**Plan Type**: Feature / corrective enhancement
**Status**: Phase 7 local complete; T043 publication pending explicit approval
**Created**: 2026-08-30
**Field evidence**: Packaged 2.4.1 operator screenshots 1-8 (thinking crop, studio context, follow-up image, SAM2 dead-end, delete copy, empty video bubble, Agents history band, Settings density)
**Goal**: After one send, the user always sees the latest turn, a concise session title, honest context, and a truthful result or error, while history lives under the four module tabs and installer GPU / Unsloth / VS Code choices match the rest of the product.

## Overview

v2.4.1 made image generation work on this host, but the same packaged pass still wastes a second history column, clips the thinking pill, names chats after the raw first prompt, and treats studio follow-ups as new txt2img jobs. Video can finish as an empty assistant bubble with no error. Settings still shows an internal catalog fingerprint and tall model cards. The installer still truncates VRAM and hides Unsloth compatibility behind a late warning, with VS Code on its own wizard step.

This plan is a field-driven UI and generation-follow-through cycle, not a rewrite of the v2.4.1 runtime lease or catalog pins. Phase 1 moves history into the left sidebar and fixes chrome that every tab inherits (thinking inset, scrollbars). Phase 2 makes transcripts honest (auto-scroll, context copy, titles, delete). Phases 3-4 close image edit identity and silent video failure. Phase 5 densifies Settings Models. Phase 6 finishes installer display and merges VS Code onto Configuration. Phase 7 is the mandatory fail-closed closeout.

No phase except the last pushes, opens a pull request, or starts remote CI. Each earlier phase ends with automated tests, session history, and one local commit.

## Goals First

**Target problem**: The packaged studio still spends horizontal space on a second history rail (or a horizontal Agents band), clips pending status, names chats after the first prompt, and fails follow-up image and first video turns without a usable explanation.

**Target persona**: A privacy-focused Windows user with a single consumer NVIDIA GPU who expects Chat, Agents, Images, and Videos to share one left pane, keep the latest turn in view, and either edit the last image or say why they cannot.

**Observable definition of done**:

- History for Chat, Agents, Images, and Videos is an independent segment of the left sidebar under the four module tabs and swaps with the active module. The main pane is transcript or workspace only.
- The thinking pill's orb is fully visible (no left crop) at default zoom, 100-200% zoom, and with history expanded or collapsed.
- After the user sends on any of the four tabs, the transcript sticks to the bottom unless they have scrolled up to read earlier turns. A new send from the composer always jumps to the latest turn.
- App scrollbars use a transparent track and a thumb that matches the dark scheme.
- Image follow-up "Make that puppy black" (no re-attach) edits the last generated image rather than sampling a new dog. Missing SAM2 does not dead-end: the bubble offers in-product install or paint-mask, and whole-image restyles fall back to img2img.
- A video turn that cannot produce a playable clip never paints an empty success bubble; it shows the real failure.
- Delete copy is generic ("Delete the selected chat(s)?") with the irreversible warning on its own line. Multi-select of chats and folders is supported.
- Session titles are a short model-generated name from the first prompt. The raw prompt is not the durable title.
- Settings Models has no catalog hash under the title, category tabs sit close to the title, and model rows keep every fact with tighter spacing.
- Installer GPU VRAM displays as a whole number rounded up (16 GB, not 15.x). Unsloth shows Compatible / Incompatible like model cards. The VS Code extension checkbox lives on the Configuration page; the separate VS Code wizard step is gone.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at docs/archive/v2/v2.4/constitution.md - skipping check. Recommend running /constitution to establish project principles.

Governing contracts used instead: `AGENTS.md` (core must not import modules; local-only models; evidence tiers `not_observed != absent`), module authorship, and `docs/archive/v2/v2.4/known-gaps.md`.

## Current model map

**Model map status**: fresh as of 2026-08-30; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.5-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/about-claude/models/overview
- OpenAI: https://developers.openai.com/api/docs/models
- Google: https://ai.google.dev/gemini-api/docs/changelog, https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/
- Cursor: https://cursor.com/docs/models-and-pricing, https://cursor.com/help/models-and-usage/available-models

## Architecture Decisions

1. **Sidebar history host supersedes v2.4.1 AD-8.** History is not a second full-width rail and not a horizontal band in the main pane. It is a pillar-scoped segment of the single left sidebar, directly under Chatbot / Agents / Images / Videos, swapping as the module changes. Settings and Approvals stay at the foot. Compact icon-rail mode may hide titles; default for module routes is wide enough to read session names.
2. **Studio context is a session visual budget, not `maxImages: 1`.** Catalog `maxImages` remains a per-request encoder cap. The footer bar must not jump to 100% after one puppy PNG and 200% after the edit. Warning copy must match the number on the bar.
3. **Follow-up without an attachment uses the last usable output as img2img.** Object-local replace / remove still may call SAM2. Phrases like "make that puppy black" are whole-image restyles and must not require SAM2. When SAM2 is actually required and missing, the bubble offers install `sam2:hiera-tiny` or paint a mask; it never looks like success.
4. **Titles are generated, not sliced prompts.** Create the session as "New chat" (or equivalent), fire `chat.generateTitle` in the background from the first user prompt, then rename unless the user already renamed. Truncated-prompt fallback is only for timeout or no resident model.
5. **Empty media bubbles are errors.** Assistant turns with `pending: false`, no playable `media`, and empty `content` are forbidden. Persist the error string.
6. **Installer VRAM ceil is display-only.** `math.ceil(vram_mb / 1024)` for human-facing GPU labels. Tier selection and compatibility math keep the existing fractional GB path so a 15.4 GB part does not silently become a 16 GB tier.
7. **VS Code joins Configuration.** `STEP_NAMES` drops the "VS Code" step. Detection, checkbox, and unsupported-CLI copy move onto the Configuration page with Unsloth. Provisioning behavior does not change.
8. **One scrollbar grammar.** Transparent track, scheme-matched thumb, no opaque OS chrome. Tokens live in desktop CSS and apply to transcript, Settings, and other overflow panes.

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|---|---|---|---|---|
| 1 | Sidebar History Host and Shared Chrome | History sits under the four tabs; thinking orb is uncropped; scrollbars match the theme | frontier | max |
| 2 | Transcript Honesty | Auto-scroll, matching context copy, generic multi-delete, concise titles | frontier | high |
| 3 | Image Follow-up Identity | Follow-up edits the last image; SAM2 missing is recoverable | frontier | max |
| 4 | Video Fail-Closed Output | No empty success bubble; real error is visible | frontier | max |
| 5 | Settings Models Density | Catalog hash gone; tighter header and cards; same scrollbar | standard | medium |
| 6 | Installer VRAM, Unsloth, and VS Code Merge | Rounded VRAM, Unsloth badges, VS Code on Configuration | strong | high |
| 7 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Evidence file, local gate, and publication only after approval | frontier | max |

Highest implementation risk is Phase 3 (wrong image identity and SAM2 dead-ends). Highest layout risk is Phase 1 (every module's chrome).

## Task Inventory

- [x] T001 Add a sidebar history host that swaps with the active module desktop/src/components/Sidebar.tsx
- [x] T002 [P] Move Chatbot FolderTree out of the second column desktop/src/modules/chat/ChatPage.tsx
- [x] T003 [P] Move Image and Video history into the sidebar host desktop/src/shared/explorer/StudioHistoryPane.tsx
- [x] T004 Remove the Agents horizontal History band desktop/src/modules/coding/CodingPage.tsx
- [x] T005 Stop the thinking pill from clipping the orb desktop/src/components/agentState/AgentStateOrb.tsx
- [x] T006 Apply transparent, scheme-matched scrollbars app-wide desktop/src/styles/tokens.css
- [x] T007 Add sidebar-host, crop, and scrollbar regression tests desktop/tests/sidebar-history-host.test.tsx
- [x] T008 Stabilize Phase 1 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.2-phase-1-sidebar-chrome.md
- [x] T009 Stick transcripts to the latest turn on send desktop/src/shared/chat/useStickToBottom.ts
- [x] T010 Make studio context percent and warning one story core/chat/sessionContextUsage.ts
- [x] T011 Generic delete copy with a separate irreversible warning desktop/src/modules/chat/FolderTree.tsx
- [x] T012 Multi-select chats and folders for bulk delete desktop/src/modules/chat/FolderTree.tsx
- [x] T013 Assign concise model titles from the first prompt desktop/sidecar/src/chat/titleGenerator.ts
- [x] T014 Add scroll, context, delete, and title tests desktop/tests/transcript-v2.4.2-honesty.test.tsx
- [x] T015 Stabilize Phase 2 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.2-phase-2-transcript-honesty.md
- [x] T016 Treat unattached follow-ups as edits of the last image desktop/src/modules/image/intent.ts
- [x] T017 Recover when SAM2 weights are missing desktop/src/modules/image/ImageStudioPage.tsx
- [x] T018 Add replace-intent, img2img, and SAM2 recovery tests tests/unit/core/image/replaceIntent.test.ts
- [x] T019 Stabilize Phase 3 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.2-phase-3-image-followup.md
- [x] T020 Forbid empty video success bubbles desktop/src/modules/video/VideoLabPage.tsx
- [x] T021 Surface the real SANA-Video failure path desktop/sidecar/src/diffusion/videoDispatcher.ts
- [x] T022 Add empty-output and instant-fail video tests desktop/tests/VideoLabPage.test.tsx
- [x] T023 Stabilize Phase 4 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.2-phase-4-video-fail-closed.md
- [x] T024 Remove the catalog fingerprint and tighten the Models header desktop/src/pages/settings/ModelsSettings.tsx
- [x] T025 Compact model cards without dropping fields desktop/src/pages/settings/ModelsSettings.tsx
- [x] T026 Add Settings density and header tests desktop/tests/settings-models-density.test.tsx
- [x] T027 Stabilize Phase 5 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.2-phase-5-settings-density.md
- [x] T028 Round displayed installer GPU VRAM up to whole GB scripts/installer/src/nexus_installer/pages/typed_catalog.py
- [x] T029 Badge Unsloth Compatible / Incompatible like model cards scripts/installer/src/nexus_installer/pages/configuration.py
- [x] T030 Merge the VS Code extension prompt onto Configuration scripts/installer/src/nexus_installer/pages/vscode_extension.py
- [x] T031 Drop the VS Code wizard step and keep a 7-step route scripts/installer/src/nexus_installer/constants.py
- [x] T032 Include still-uncommitted Setup / Reset / SANA installer UI scripts/installer/src/nexus_installer/pages/setup.py
- [x] T033 Add installer VRAM, Unsloth, and 7-step navigation tests scripts/installer/tests/test_navigation.py
- [x] T034 Stabilize Phase 6 and record local evidence docs/archive/v2/v2.4/development/history/2026-08-30_v2.4.2-phase-6-installer-display.md
- [x] T035 Refactor release-touched architecture and quote the scan docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.2-field-ui.md
- [x] T036 Reconcile all active known gaps docs/archive/v2/v2.4/known-gaps.md
- [x] T037 Verify living documentation architecture docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.2-field-ui.md
- [x] T038 Report git-tree hygiene docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.2-field-ui.md
- [x] T039 Reconcile terminal CI/CD coverage docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.2-field-ui.md
- [x] T040 Perform an independent Goal-vs-codebase review docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.2-field-ui.md
- [x] T041 Execute last-phase human testing suggestions docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.2-field-ui.md
- [x] T042 Run full local testing and stabilization docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.2-field-ui.md
- [ ] T043 Publish and integrate only after explicit approval docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.2-field-ui.md

## Field map (screenshots to code)

| Shot | Symptom | Primary cause in tree |
|---|---|---|
| 1 | Thinking orb cropped on the left | `AgentStateOrb` capsule `border-radius: 999px` clips the 48px canvas; v2.4.1 lane inset did not move the orb inside the straight section of the pill |
| 2 | Context 100% after one image; no auto-scroll | `sessionContextUsage` uses `visualTokenBudget.maxImages` (catalog 1); no `scrollIntoView` / stick-to-bottom helper exists under `desktop/src` |
| 3 | "Make that puppy black" is a new dog | `parseReplaceIntent` misses that phrasing; SAM2 path only runs with attachments; img2img last-ref may be unused or denoise too high |
| 4 | SAM2 weights missing | `ImageStudioPage` + `runtimes/diffusion/pipelines/sam2.py`; error is text-only |
| 5 | Delete dialog quotes the full prompt title | `FolderTree` confirm copy interpolates `confirmDelete.label`; studio titles use `sessionTitleFromPrompt` (45-char slice) |
| 6 | Empty video bubble, no error | `VideoLabPage` can `persistTurn` empty content without media; pending cleared |
| 7 | Agents History is a horizontal panel | v2.4.1 AD-8 `coding-history-pane` in `CodingPage.tsx` |
| 8 | Catalog hash, tall cards, OS scrollbar | `ModelsSettings` header `catalogHash.slice(0, 12)`; no `::-webkit-scrollbar` tokens |

## Out of scope

- Cloud or remote generation fallback.
- Changing catalog `maxImages` as a generation-quality cap.
- v2.4.1 T097 push/PR (still waits on its own explicit approval).
- Windows kernel sandbox confinement (known-gap DF-7).
- Rounding live "GB free" in the GPU footer (that is remaining VRAM, not the advertised total).

---

## Phase 1: Sidebar History Host and Shared Chrome

**Goal**: Put pillar history under the four module tabs in the left sidebar, uncrop the thinking pill, and restyle overflow scrollbars once for the whole app.
**Prerequisites**: None.
**Stability Gate**: Switching Chatbot / Agents / Images / Videos swaps only the history segment; the main pane has no second history column and no Agents horizontal History band; the pending orb is fully inside the pill in layout tests; overflow panes use the new scrollbar tokens.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Cross-cutting App/Sidebar/page composition, visual clipping, and theme tokens; any high-risk layout miss ships to every tab.

### Sub-tasks

#### 1.1 -- Sidebar history host (T001)

**Objective**: One left pane owns module nav plus a history slot that the active route fills.

**Failure modes**: Settings / Approvals routes show no history slot. A down sidecar shows the same empty/error copy FolderTree already uses. Rapid tab switches must not leave the previous module's tree mounted as the visible list. Compact icon-rail hides session titles and offers expand; it must not duplicate a second column.

**Prompt**:
> Add a `SidebarHistoryHost` (context or outlet) owned by `desktop/src/App.tsx` and `desktop/src/components/Sidebar.tsx`. Place it under `sidebar-module-nav` and above the admin/GPU foot. The slot content is the active module's existing `FolderTree` / studio history tree, not a new data model. Default width on module routes must fit titles (about the current expanded `FULL_WIDTH` or slightly wider). Do not add a second permanent rail beside the sidebar. Document that this supersedes v2.4.1 architecture decision 8.

#### 1.2 -- Move Chatbot history (T002)

**Objective**: Chatbot no longer spends a `CollapsibleHistoryAside` column next to the transcript.

**Failure modes**: Missing client still renders the empty CTA. Selecting a chat still hydrates the transcript. Collapse of the whole sidebar does not delete chats.

**Prompt**:
> Register Chatbot's `FolderTree` into the sidebar host from `desktop/src/modules/chat/ChatPage.tsx`. Remove `CollapsibleHistoryAside` from that page's flex row so the transcript is the only main-pane child besides the composer. Keep `data-testid="chats-pane"` on the hosted tree for existing tests.

#### 1.3 -- Move Image and Video history (T003)

**Objective**: Studio history uses the same sidebar segment.

**Failure modes**: Image and Video lists stay isolated (an Images chat must not appear under Videos). Empty studio still has Start / New chat in the segment.

**Prompt**:
> Point `StudioHistoryPane` at the sidebar host instead of `CollapsibleHistoryAside` beside the canvas. Update `ImageStudioPage.tsx` and `VideoLabPage.tsx` layouts so the transcript/canvas is full width of the main pane.

#### 1.4 -- Move Agents history (T004)

**Objective**: Agents History is not a horizontal panel in the workspace.

**Failure modes**: "No sessions yet" and Start a new session remain, but in the sidebar segment. Chat / Memory / Activity tabs stay in the main header.

**Prompt**:
> Remove `coding-history-pane` from `desktop/src/modules/coding/CodingPage.tsx`. Host the session list in the sidebar slot when the Agents route is active. Keep workspace chips and Chat/Memory/Activity in the main pane header.

#### 1.5 -- Uncrop the thinking pill (T005)

**Objective**: Screenshot 1 cannot recur: left dots of the orb stay inside the visible pill.

**Failure modes**: Reduced motion still shows a static complete orb. Hero Image/Video orbs are unchanged. Narrow width and 200% zoom still show the full ring.

**Prompt**:
> The crop is the capsule `border-radius: 999px` clipping a 48px canvas whose left padding (`--space-2`) is smaller than the rounded cap. Fix in `AgentStateOrb.tsx` (and `PendingMessage` in `MessageBubble.tsx` if needed): either increase left padding so the canvas sits in the straight section, shrink the pill orb, or paint the orb on an unclipped inner layer. Add a DOM geometry test that the canvas's `getBoundingClientRect()` is fully inside the pill's content box. Do not wrap pending status in a message bubble.

#### 1.6 -- Modern scrollbars (T006)

**Objective**: Transcript and Settings overflow match the dark theme.

**Failure modes**: Overlay scrollbars on macOS stay usable. Keyboard scrolling still works. Contrast of the thumb against `--bg-0` meets a visible-not-invisible bar.

**Prompt**:
> Add shared scrollbar tokens in `desktop/src/styles/tokens.css` (`::-webkit-scrollbar` plus `scrollbar-color` / `scrollbar-width`). Transparent track, thumb using `color-mix` of `--fg-muted` or `--accent-chatbot` on transparent. Apply globally so Settings Models and chat transcripts inherit them. Do not invent a custom JS scrollbar.

#### 1.7 -- Testing and Stabilization (T007, T008)

**Objective**: Prove the host, crop, and scrollbars; commit Phase 1 locally.

**Prompt**:
> Generate automated tests for everything built in Phase 1. Include: route-switch history isolation (Chat vs Agents vs Image vs Video); absence of `coding-history-pane` and Chat `CollapsibleHistoryAside` in the main pane; pending-orb geometry; scrollbar CSS presence. Run the tests, fix failures, iterate until stable. Do not proceed to Phase 2 until this phase is fully tested. Then record this phase's CI IMPACT against `[[cicd-architect]]` without running remote CI: name any new command, dependency, environment variable, test path, or artifact this phase introduced, and state whether the existing pipeline already covers it. Changing a pipeline file is out of scope. After all automated tests pass, run `/generate-session-history` for Phase 1, then create ONE local commit scoped to this phase. Do NOT push, do NOT open a pull request, and do NOT start remote CI.

### Phase 1 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 2

---

## Phase 2: Transcript Honesty

**Goal**: After send, the user sees the latest turn, a title that is not the raw prompt, a context story that matches the bar, and a generic multi-delete confirm.
**Prerequisites**: Phase 1.
**Stability Gate**: Composer send on each of the four tabs scrolls the new user turn into view; Image/Video percent after one success is not 100% solely because `maxImages` is 1; delete dialog has no chat title in the question; Ctrl/Shift multi-select deletes many rows; first-prompt title becomes a 3-6 word model title (or short fallback), never the full prompt.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Four-module transcript behavior plus persistence of titles and usage; one high-scope signal.

### Sub-tasks

#### 2.1 -- Stick to bottom (T009)

**Objective**: Follow-up messages are not hidden under the composer.

**Failure modes**: If the user scrolled up to read history, incoming assistant tokens do not yank them down; sending from the composer always jumps to bottom. Empty transcript does not throw.

**Prompt**:
> There is no `scrollIntoView` / stick-to-bottom helper under `desktop/src` today. Add `desktop/src/shared/chat/useStickToBottom.ts` and use it from Chat, Agents, Image, and Video message lists (`ChatPage.tsx`, `CodingPage.tsx`, `ImageStudioPage.tsx`, `VideoLabPage.tsx`). On composer submit, always `scrollTop = scrollHeight`. While the user is pinned to bottom, keep pinning as pending/media arrives.

#### 2.2 -- Honest context meter (T010)

**Objective**: Bar fill, percent text, and "80%" warning are one number.

**Failure modes**: LLM chats with a real window keep using that window. Visual sessions use a session budget (default at least 8 generated visuals, not catalog `maxImages: 1`). Over-budget percent may exceed 100% but the warning must quote the actual percent, not a hardcoded 80.

**Prompt**:
> Stop using per-request `visualTokenBudget.maxImages` as the session denominator in `core/chat/sessionContextUsage.ts` / `studioTurnUsage.ts`. Introduce a session visual cap used only for the footer. Update `ContextUsageBar.tsx` and `ComposerContextRow.tsx` so the suggestion reads the live percent (or hides until the threshold). Tests: one Image success is not 100%; two successes are 2/N; warning string includes that percent; LLM window path unchanged.

#### 2.3 -- Generic delete copy (T011)

**Objective**: Screenshot 5 copy is gone.

**Failure modes**: Folder delete still warns that contents go with it, without interpolating long names. Cancel and Archive instead remain for chats.

**Prompt**:
> In `FolderTree.tsx` confirm dialog: question line is "Delete the selected chat?" / "Delete the selected chats?" / "Delete the selected folder?" / "Delete the selected items?" with no title interpolation. Second line: "This action cannot be undone." Keep Archive instead for chat targets.

#### 2.4 -- Multi-select delete (T012)

**Objective**: User can select several chats or folders and delete once.

**Failure modes**: Range-select must not select across folder boundaries unless intended. Delete of mixed selection uses "selected items". Escape clears the selection. Drag-and-drop still moves a single row unless the drag started on a selected set.

**Prompt**:
> Add Ctrl/Cmd click and Shift range select on `FolderTree` rows, with a visible selected set. Context menu Delete and toolbar/keyboard Delete operate on the set. Confirm uses T011 copy. Add tests in `desktop/tests/FolderTree.test.tsx`.

#### 2.5 -- Concise model titles (T013)

**Objective**: Rails show a short generated name, not the first prompt.

**Failure modes**: No resident model or 5s timeout keeps `fallbackTitle` (about 6 words), never `sessionTitleFromPrompt`'s 45-character prompt slice. User rename (`userRenamed`) is never overwritten. Title RPC must not evict the diffusion/chat model the user is using if that would stall generation; prefer the already-loaded chat model or skip to fallback.

**Prompt**:
> Chatbot already calls `generateTitle` after the first turn (`ChatPage.tsx`) but studio `ensureSession` persists `sessionTitleFromPrompt`. Change Image, Video, and Agents session create to start as "New chat", then reuse `desktop/sidecar/src/chat/titleGenerator.ts` (prompt: 3 to 6 words from the first user message). Replace studio `sessionTitleFromPrompt` durable use. Tests: fallback is short; success rename; user rename wins.

#### 2.6 -- Testing and Stabilization (T014, T015)

**Objective**: Prove honesty rules; commit Phase 2 locally.

**Prompt**:
> Generate automated tests for Phase 2 (scroll helper, context denominator, delete copy, multi-select, title generate/fallback). Run, fix, iterate. Record CI IMPACT without remote CI. Run `/generate-session-history` for Phase 2, then ONE local commit. Do NOT push, PR, or start remote CI.

### Phase 2 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 3

---

## Phase 3: Image Follow-up Identity

**Goal**: A follow-up that refers to the last image edits that image; missing SAM2 is a recovery path, not a brick wall.
**Prerequisites**: Phase 2.
**Stability Gate**: "Make that puppy black" with no attachment dispatches img2img (or equivalent) against `lastOutputRef`, not txt2img; SAM2-missing shows install or paint-mask actions; object replace still can use SAM2 when weights exist.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Intent parsing, diffusion modes, and a missing-weight path; two-plus high signals (structure, risk, reasoning).

### Sub-tasks

#### 3.1 -- Follow-up is an edit (T016)

**Objective**: Screenshot 3 does not recur.

**Failure modes**: No last output (first turn) stays txt2img. Unreadable last output keeps the existing fail-closed message. High denoise must not turn img2img into a new identity by default; cap strength so color/style follow-ups keep pose and subject unless the user asks for a different subject.

**Prompt**:
> Expand `core/image/replaceIntent.ts` for "make (that|the|this) X <color|adj>" as recolor/restyle, not only `recolor X to Y`. In `inferImageIntent` / `ImageStudioPage` handleSubmit: when attachments are empty and `lastOutputRef` is usable, never take the txt2img branch for a follow-up. Wire img2img `sourceImage` from the path (load to the protocol the sidecar already accepts). Keep painted masks as inpaint. Add fixtures for "Make that puppy black" and "replace the sky with sunset".

#### 3.2 -- SAM2 recovery (T017)

**Objective**: Screenshot 4 is actionable in-product.

**Failure modes**: Install CTA disabled when sidecar is down. Paint-mask path does not call SAM2. Auto-install without a click is forbidden. After install completes, Retry uses the parked prompt.

**Prompt**:
> When `client.segment` reports SAM2 weights missing, render the existing recovery-card pattern (cause, primary Install `sam2:hiera-tiny`, secondary Paint a mask, optional Open Settings > Models). Whole-image restyles from T016 must not call `segment` at all. Do not swallow the error into an empty bubble.

#### 3.3 -- Testing and Stabilization (T018, T019)

**Objective**: Prove intent and recovery; commit Phase 3 locally.

**Prompt**:
> Tests for new replace/recolor phrases, img2img last-ref, SAM2-missing card, and "do not segment" for make-that-color. Run focused desktop + core image tests. Record CI IMPACT. Session history, one local commit. No push/PR/CI.

### Phase 3 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 4

---

## Phase 4: Video Fail-Closed Output

**Goal**: A video turn that cannot produce a playable clip shows why, in under a second or after a long job alike.
**Prerequisites**: Phase 3 (shared pending/message helpers only; may start after Phase 1 if image work is isolated, but inventory order is 4 after 3).
**Stability Gate**: Completing without an mp4 path always patches a non-empty error; `persistTurn` never stores empty assistant content without `mediaRef`; the SANA-Video instant-empty path has a regression with a fake dispatcher.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Silent success is a production lie; debug plus dispatcher contract.

### Sub-tasks

#### 4.1 -- Never-empty assistant (T020)

**Objective**: Screenshot 6 cannot be a success state.

**Failure modes**: Sidecar timeout, missing weights, empty job payload, and decode failure each have a distinct user-visible sentence. `mediaRecovery` still shows when the runtime is repairing; if recovery UI is absent, `content` must still explain.

**Prompt**:
> In `VideoLabPage.tsx`, forbid `pending: false` + empty `content` + no `media`. Replace `persistTurn({ content: "" })` without mediaRef with the existing "completed without a playable clip" (or a more specific `formatInferenceError`). If `markMediaRuntimeFailure` sets `content: ""`, it must set `mediaRecovery` that `MessageBubble` actually renders; otherwise set content.

#### 4.2 -- Instant SANA-Video failure (T021)

**Objective**: Find why the first puppy-in-grass turn returned immediately empty.

**Failure modes**: Missing SANA-Video weights, dispatcher returning accepted then completing with empty path, and thrown errors that the UI swallowed.

**Prompt**:
> Trace `dispatchSegment` / `videoDispatcher.ts` / runtime factory for SANA-Video 2B 720p. Add a fail-closed mapping from each terminal job state to a message. If weights are not installed, the bubble must say to install from Settings > Models (same grammar as image). Tests with a fake client that resolves in one tick with no path.

#### 4.3 -- Testing and Stabilization (T022, T023)

**Prompt**:
> Add Video Lab tests for instant empty, missing weights, and persist of error text. Record CI IMPACT. Session history, one local commit. No push/PR/CI.

### Phase 4 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 5

---

## Phase 5: Settings Models Density

**Goal**: More models fit on screen; the catalog hash is gone; the list uses the Phase 1 scrollbar.
**Prerequisites**: Phase 1 (scrollbar tokens).
**Stability Gate**: Header has title + disk summary only; category tabs are one spacing token under the title; cards keep publisher, VRAM, size, description, actions, and Details, with reduced padding/gaps; no `Catalog <hex>` substring in the page.
**Recommended model tier**: standard
**Recommended effort level**: medium
**Rationale**: Localized Settings CSS; one medium structural signal.

### Sub-tasks

#### 5.1 -- Header (T024)

**Objective**: Screenshot 8 fingerprint and empty gap are gone.

**Failure modes**: Disk summary stays visible. Tabs remain keyboard-accessible.

**Prompt**:
> Remove `client.catalogHash` chrome from `ModelsSettings.tsx` header. Reduce `pageStyle` / header / tabList gaps so Embeddings/Chat/... sit close under "Models". Keep the hash available to diagnostics logs if needed, not the UI.

#### 5.2 -- Compact cards (T025)

**Objective**: Two downloaded cards should not fill the viewport.

**Failure modes**: Description, pills, Remove, and Details remain. Do not hide compatibility.

**Prompt**:
> Tighten `cardStyle` padding, inner stack gaps, and description line-height. Prefer a denser metadata grid over dropping fields. Search and group headings stay.

#### 5.3 -- Testing and Stabilization (T026, T027)

**Prompt**:
> Assert the fingerprint is absent, tabs follow the title with a small gap, and card padding tokens shrank. Record CI IMPACT. Session history, one local commit. No push/PR/CI.

### Phase 5 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 6

---

## Phase 6: Installer VRAM, Unsloth, and VS Code Merge

**Goal**: GPU totals read as whole GB, Unsloth compatibility is obvious, and VS Code is not its own wizard step.
**Prerequisites**: None relative to desktop phases 3-5 (may overlap after Phase 1). Inventory keeps it after Settings.
**Stability Gate**: Detected 3080-class 16 GB class displays as 16 GB via ceil; Unsloth row has Compatible or Incompatible before opt-in; `STEP_NAMES` is Welcome, Setup, Models, Configuration, Review, Installing, Complete; VS Code checkbox works on Configuration; prior uncommitted Setup/Reset/SANA/sort installer UI is included if still dirty.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Wizard route change plus several Qt pages; three medium signals.

### Sub-tasks

#### 6.1 -- VRAM display ceil (T028)

**Objective**: Compare GPU line to model "16 GB VRAM" badges easily.

**Failure modes**: Tier picker still uses precise GB (`vram_mb / 1024` float). 8.1 GB displays 9 GB on the label only. Tests pin 16384 -> 16 and 15360 -> 16 display.

**Prompt**:
> Add a single helper (e.g. `display_vram_gb(vram_mb) -> int`) using ceil, used by GPU Detection copy, typed catalog subtitle (`Detected ... (N GB VRAM)`), and any other human-facing installer GPU total. Do not change `tier_defaults` selection math.

#### 6.2 -- Unsloth compatibility badge (T029)

**Objective**: Same Compatible / Incompatible language as model cards.

**Failure modes**: Incompatible still lets the user opt in (Unsloth is optional) but the warning is visible before check, not only after. NVIDIA 16 GB+ hides the incompatible badge.

**Prompt**:
> On `configuration.py`, show a pill next to Unsloth: Compatible or Incompatible using the same NVIDIA + 16 GB rule as `_refresh_unsloth_warning`. Keep license help text.

#### 6.3 -- Merge VS Code onto Configuration (T030, T031)

**Objective**: No separate VS Code step.

**Failure modes**: Unsupported CLI still cannot check the box. Smoke `--skip-vscode` still works. Review and Installing still list the extension when selected.

**Prompt**:
> Move detection + checkbox + reason copy from `vscode_extension.py` onto `configuration.py` (there is already a "VS Code Extension Settings" block). Remove "VS Code" from `STEP_NAMES` and `_register_gui_pages`. Update `test_navigation.py` and `test_vscode_extension_page.py` (page object may become a mixin or Configuration assertions).

#### 6.4 -- Uncommitted Setup/Reset/SANA work (T032)

**Objective**: Do not leave the earlier installer UX pass stranded.

**Prompt**:
> If Setup-step collapse, Reset-on-tab-row, SANA selectable, and recommended-first sort are still uncommitted, include them in this phase after tests. Do not regress the 7-step route when merging VS Code.

#### 6.5 -- Testing and Stabilization (T033, T034)

**Prompt**:
> Run installer pytest for navigation, configuration, typed catalog VRAM labels, Unsloth badges, and VS Code-on-configuration. Record CI IMPACT. Session history, one local commit. No push/PR/CI.

### Phase 6 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 7

---

## Phase 7: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, its living docs architecture current, and its pipeline reconciled against the canonical contract, with independent evidence that the plan Goal landed, then publish and integrate the completed branch.
**Prerequisites**: All prior phases.
**Stability Gate**: `docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.2-field-ui.md` exists with one quoted section per duty below. A missing Goal, a Goal-review miss without a recorded known-gap, or a missing evidence section blocks `/update release`.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, Goal-vs-codebase review, known-gap reconciliation, and release gating carry high context volume and blast radius.

### Sub-tasks

#### 7.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout and quote the scan.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure, then refactor toward a clean, intuitive layout via [[project-refactor]] and [[docs-layout-refactor]] (propose-then-apply, with confirmation; repair every reference for anything that moves). Write the detector output into `docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.2-field-ui.md` under `## Architecture refactor`. An empty finding is allowed only when the scan output is quoted.

#### 7.2 - Known-gaps reconciliation
**Objective**: Reconcile this version and every other still-open known-gaps file.
**Prompt**:
> Via [[known-gaps-tracker]], reconcile this version's known gaps AND every other `docs/**/known-gaps.md` whose Status is in-progress or whose Open Items remain. Glob both canonical (`docs/releases/v<MAJOR>/v<MAJOR>.<MINOR>/known-gaps.md`) and legacy layouts. If a file is unreachable, record the glob result and continue with what was found. Write the disposition into the evidence file under `## Known-gaps reconciliation`. Carry forward v2.4.1 DF-1 through DF-7 unless this cycle produced new evidence. Add BG rows for any Phase 7 Goal miss.

#### 7.3 - Living docs architecture
**Objective**: Prove handbooks, decisions, and generated HTML match the required living tree.
**Prompt**:
> Check `docs/handbooks/` (markdown source of truth, generated `html/`, one atlas walkthrough, technical companions for key components), `docs/decisions/`, and living `docs/README.md` / `docs/DEVLOG.md` / `docs/todos.md`. Self-gated: never invent `docs/testing/` or `docs/validation/`. Markdown wins if HTML disagrees; regenerate or fail the check. Quote the check in the evidence file under `## Living docs architecture`. Details live in [[docs-layout-refactor]].

#### 7.4 - Git-tree hygiene
**Objective**: Report branch and repository-settings hygiene without deleting anything.
**Prompt**:
> Run `python scripts/check_release_preconditions.py --branches --repo-settings`. Quote the output in the evidence file under `## Git-tree hygiene`. Report only; never delete branches.

#### 7.5 - Terminal CI/CD reconciliation
**Objective**: The repository's pipeline is compared field by field against the canonical contract, and every difference is either applied with approval or recorded as a known gap.
**Prompt**:
> Invoke [[cicd-architect]] in existing-pipeline comparison mode and run its six steps in order: DETECT the active CI provider (recording "none detected" rather than assuming one); COMPARE the existing pipeline against every field of the canonical contract - repository-native profiles, event separation, runner selection, the always-resolving aggregate required check, permissions, immutable action or plugin references, caching, concurrency, path scoping, artifact retention, structured reports, deployment boundaries, and failure recovery; PROPOSE each difference with its cost, its risk, and the smallest change that closes it; obtain explicit APPROVAL per change (silence is not approval); APPLY the approved changes and re-run the local gate; and RECORD every declined or environment-only difference as a known gap with an owner and a next step via [[known-gaps-tracker]]. The comparison concludes PASS only when every required field has observable evidence; a green pipeline run is not evidence, because a pipeline that checks nothing is green for the wrong reason. If this repository ships more than one installer, run or add a declarative cross-installer parity gate covering distributed artifacts, supported platforms, named capability counterparts, and external-tool fallbacks, then execute the real installers on their target operating systems with identical postconditions. Cross-link [[platform-contract-verification]] so discovery and delivery are checked in the same pass. If the repository ships zero or one installer, record a silent no-op and continue. Quote the per-field comparison result in the evidence file under `## CI/CD coverage`.

#### 7.6 - Independent Goal-vs-codebase review
**Objective**: Prove the plan Goal landed in the codebase, not that checkboxes were ticked.
**Prompt**:
> Re-read the plan header Goal and the Goals First / Definition of Done. Inspect the codebase as if this agent had not implemented the phases. Write `## Goal-vs-codebase review` in `docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.2-field-ui.md` listing: the plan Goal restated; the code/docs artifacts that satisfy it; and any gap. A missing or malformed Goal is a fail-closed finding that names the missing Goal. Completing every prior-phase sub-task is not evidence the Goal landed. A miss is a blocking finding or a new known-gap, not a pass.

#### 7.7 - Human/manual testing suggestions (last phase only)
**Objective**: Ask a human to exercise only what automated tests cannot cover, now that the feature is complete.
**Prompt**:
> Emit human/manual testing suggestions for user-facing workflows, external integrations, and environment-specific cases that automated tests cannot cover. Do not invent a fake walkthrough. This duty exists only on the last phase. Suggested operator items: (1) thinking pill at 100% and 150% zoom; (2) switch all four tabs and confirm history lists swap in the left pane; (3) Image: generate then "Make that puppy black" without re-attach; (4) Image: trigger SAM2-missing and use Install or paint-mask; (5) Video: one prompt must not yield an empty bubble; (6) delete multi-select copy; (7) first-prompt title is short; (8) Settings Models density; (9) installer GPU line shows whole GB and Unsloth badge on the same page as VS Code.

#### 7.8 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and the complete LOCAL gate is green before anything is published.
**Prompt**:
> Run the full local validation/test suite, confirm the refactor changed no behavior, and iterate until clean. This gate is local by construction: it must pass before the branch is published, so it cannot depend on a remote run. Quote the suite in the evidence file under `## Full-suite testing and stabilization`. Generate a session-history entry for this phase.

#### 7.9 - Publication and integration
**Objective**: Publish the completed branch once, validate the merge result remotely, and hand a green integration to the release flow.
**Prompt**:
> Create the final local commit for this phase. Then present the resolved branching model, remote, branch name, and pull-request target, and obtain EXPLICIT approval before the plan's first branch push. Push once, open the integration pull request against the integration branch, and report the exact required checks expected. Wait until every required check reaches a terminal state. A red check REOPENS this phase: classify the failure, reproduce it locally, apply the narrow fix, re-run the local gate, update known gaps and session history, then amend the final commit or add one narrowly scoped stabilization commit and push again with approval. Never re-run a red check without a local reproduction; a re-run without one is a guess. Merge only after every required check is green and the user approves. Confirm the post-merge workflow performed only its intended smoke, publication, or provenance work and did not rerun the complete suite. Only then hand off to `/update release`, which owns the version bump, changelog, tag, push, and GitHub Release behind its own confirmation gates. Quote the required-check results in the evidence file under `## Publication and integration`.

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| | | |

### Phase 7 Exit Checklist

- [x] All sub-tasks completed (T043 remains the publication gate)
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run until T043
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [ ] Branch push, pull request, and remote CI occurred only after explicit approval
- [ ] Ready for `/update release` after green integration
