# Plan -- Field Delivery, Density, and Session Identity

**Project**: Nexus AI Studio
**Version**: v2.4.6
**Slug**: field-delivery-density-and-session-identity
**Filename**: v2.4.6-field-delivery-density-and-session-identity.md
**Plan Type**: Feature / corrective enhancement
**Status**: Complete. PR 62 green (42 checks pass, 0 fail), merged to develop as 8499c05d. Installer rebuilt for field testing (sha256 17DD4CA5...). No release performed.
**Created**: 2026-09-02
**Field evidence**: Packaged post-v2.4.5 operator screenshots 1-10 (2026-09-02): installer Setup still one-column with a full-width Re-check bar; Configuration still shows Unsloth Compatible but unchecked, a separate VS Code segment disabled on VS Code 1.136, Gemma sampling bullets, and a Video2X note; Review lists 19 models in two uncategorized columns with empty left-column space; desktop sidebar empty-state hides the history title; Settings model cards still use Details and stacked actions; Chat pending glow, missing assistant token counts, shuffled picker; Image still says Shaping..., defaults to Juggernaut XL v9, has no Stop, and parks Advanced settings on its own row; GPU footer stays Idle at 0% during a live request
**Goal**: The installer opens maximized and is dense on Setup, Configuration, and Review; Unsloth is checked when Compatible; the VS Code extension is a default-on feature that installs on current VS Code and uses the same installer-or-Settings-owned agentic models as the desktop; the packaged desktop that this installer writes is the desktop from this tree; Sessions History is always labeled Session (except the Chatbot tab); Settings cards are compact with no Details accordion; Chat, Agents, Image, Video, and VS Code pickers show only models the user selected in the installer or installed later in Settings > Models, in recommend order, plus a Stop control, equal gutters, assistant token counts, rotating studio captions, Advanced settings on the Context/Model row, and a GPU footer that is busy when a request is running.

## Overview

v2.4.4 and v2.4.5 did not ignore these screenshots by accident. They closed a different contract: automated tests against jsdom and pytest, plus an installer rebuild that could ship a stale desktop payload. The operator then ran that wizard, saw the v2.4.5 Review downloaded-marks (so the wizard was new), launched the desktop, and saw the pre-density UI. That split is the first defect this plan fixes. After the payload is honest, every remaining screenshot is implemented in this cycle. Nothing in the operator list is deferred, parked as a known-gap, or marked "already done" because a prior plan ticked a checkbox.

The wizard and the desktop are one product. Phase 1 makes the installer embed and overwrite this tree's desktop and VSIX, and opens maximized. Phases 2-3 densify the wizard and put VS Code plus Unsloth on the Features list. Phase 4 makes the VS Code extension pick from installer-or-Settings-owned agentic models and keep Hub skills, commands, hooks, and the agent loop. Phases 5-7 fix the desktop the operator actually sees, including the same allowlist on Chat, Agents, Image, and Video. Phase 8 is the fail-closed closeout, including a rebuilt installer whose payload fingerprint matches the desktop About line.

No phase except the last pushes, opens a pull request, or starts remote CI. Each earlier phase ends with automated tests, session history, and one local commit. Package metadata stays 2.4.1 until `/update release`; the desktop still shows a build fingerprint so a 2.4.1 package cannot hide a stale payload.

Discovery interview: skipped as a live questionnaire. The operator named every screenshot, forbade deferral, and asked why v2.4.4 and v2.4.5 did not show up. Assumptions are listed under Architecture Decisions.

## Why v2.4.4 and v2.4.5 looked ignored

This is the answer the operator asked for first. It is also the reason Phase 1 exists.

1. **The wizard and the desktop are not the same artifact.** `scripts/installer/build/build-windows.ps1` embeds a Tauri NSIS bundle from `desktop/src-tauri/target/release/bundle/nsis/` (or a downloaded `Nexus-Desktop_${Version}_x64-setup.exe`). Rebuilding only PyInstaller produces a new wizard (v2.4.5 downloaded-marks, two-column Review) while still installing whatever NSIS file was already staged. The operator screenshots prove that split: Review is the v2.4.5 wizard, Settings Details still print `ID:` / `Also agentic` / `Backend model:`, which v2.4.4 T025 deleted in source.

2. **Prior plans explicitly refused this work.** v2.4.3: "does not reopen the VS Code merge". v2.4.4: "does not reopen ... VS Code merge, installer two-column layout, Unsloth lock, or picker recommend order". v2.4.5: "does not reopen the v2.4.4 transcript, restyle, SANA-pin, liveness, or Settings work". Session rename, Stop on all four tabs, GPU occupancy, installer maximize, Setup compactness, Unsloth auto-check, VS Code-on-1.136, categorized Review, and removing the Gemma/Video2X Configuration block were never in those Goals.

3. **The written Goal sometimes contradicted the operator.** v2.4.3 AD7 and v2.4.4 AD10 moved origin, license, and long metadata *into* Details. Today's screenshot 6 asks to *delete* Details and keep a three-line card. Tests then pinned the old Details accordion (`settings-models-density.test.tsx` still requires Details). That is how a "density" phase can be green and still look like screenshot 6.

4. **Empty-state layout was not the layout the hairline tests used.** `FolderTree` only renders `copy.paneTitle` ("Chats") when the tree is non-empty. Screenshot 5 is the empty state: toolbar icons, "No chats yet.", no title. The hairline therefore sits against Videos with nothing named below it. v2.4.4 T005 equalized `marginBlock` assuming the Chats header existed.

5. **Tests asserted the wrong absence.** `ChatPage.test.tsx` comments that assistant token counts are omitted when the fake session reports no usage, and only asserts `message-tokens-` length `>= 1` (the user bubble). `estimatedMessageUsage` is computed, but a persist/reload or a mapping hole can drop `messageUsage` without failing CI. Screenshot 8 is that hole in the field.

6. **GPU Idle is a scheduler hole, not a dead nvidia-smi.** `GpuStatusFooter` shows Idle when `sample.idle` is true. `createTelemetryStream` sets idle from `scheduler().active === null`. Chat and many Image/Video turns never enqueue `GpuScheduler`, so the footer stays Idle. `nvidia-smi utilization.gpu` can also read 0 on this laptop even while VRAM moves; the sidecar query has no `C:\Windows\System32\nvidia-smi.exe` fallback (the installer GPU page does). The composer "0%" next to Context is context-window fill, not GPU; an empty "Hi" session at 0% is expected. The defect to fix is the sidebar GPU line.

7. **Juggernaut is a real catalog model, and Review already listed it.** `core/registry/catalog.json` id `juggernaut-xl-v9`. `recommended.json` uses it as the 8 GB and 12 GB image default; 16 GB image default is `realvisxl-v5`. v2.4.5 auto-selects anything already on disk; the host had `~/.nexus/models/weights` for Juggernaut (6.7 GB) and Review screenshot 4 column 2 includes `juggernaut-xl-v9`. The Image tab then resolved a default from `installedModelsForType` + `recommendedByTask.image` or `ready[0]`. An uncategorized Review made that selection easy to miss.

8. **Known-gaps recorded "not observed" and then the next plan moved on.** v2.4.4 MT-1 through MT-6 and v2.4.5 MT-1 are still open as packaged observation. A later plan that does not reopen those rows will look like the work was skipped. This plan treats an unobserved desktop screenshot as *not landed*, even if source and tests exist.

## Goals First

**Target problem**: After installing the latest wizard, the operator still cannot trust that the desktop matches the plan they just paid for, and every remaining installer and desktop screenshot still wastes space, names the wrong thing, or hides a running GPU.

**Target persona**: A privacy-focused Windows user with a single consumer NVIDIA GPU (this host: RTX 3080 Ti Laptop, 16384 MB, VS Code 1.136.0) who re-runs the installer across plan cycles and expects the wizard, the VS Code extension, and the four desktop tabs to show the models they just installed.

**Observable definition of done**:

- Installer window opens maximized.
- Setup: prerequisites in two columns; Re-check is a refresh icon on the Prerequisites title row; GPU name, vendor, and VRAM on one line; no large empty card padding.
- Configuration: Unsloth is checked when the badge is Compatible (unchecked and locked when Incompatible). VS Code extension is a Features checkbox, default on for current Microsoft VS Code including 1.136. No Gemma temperature/top-p/top-k preview. No Video2X paragraph on this page.
- Review: facts column hugs its content; models are grouped by category (agentic, image, video, audio, document, embed) with the existing downloaded/pending marks.
- The desktop this installer writes is the desktop from this tree. Settings or About shows a fingerprint that matches the installer payload manifest. Re-installing overwrites the previous app.
- VS Code extension lists only installer-or-Settings-owned agentic models, defaults to the recommended agentic id for this host, and runs Hub skills, commands, hooks, and the agent loop against that model (same local agentic capability as the desktop Agents tab; not a cloud Codex clone).
- Sidebar: hairline is equally spaced between Videos and the always-visible "Sessions History" title. New-folder and new-session icons sit on that title row, left-aligned after the title. Every chat-noun string is Session except the Chatbot tab label.
- Settings > Models: star, downloaded, and delete on one vertically centered row. No Details accordion. Card height hugs:

```
Gemma 4 12B
Requirements: Storage (7.6 GB) VRAM (11 GB) Company: Google Country: USA Released: May 2026
Agentic: Yes Context window: 262k tokens Multimodal: Yes Guardrails: Censored License: Gemma Terms of Use
Google's Gemma 4 12B is a mid-size multimodal agentic model from the USA that can read both text and images. It also has a large enough context window to hold book-length material in a single session.
```

- Chat pending glow: left edge of the glow aligns with the assistant bubble left edge; that gutter matches the user-bubble right gutter; glow is not cropped.
- Assistant bubbles show a token count next to the clock (estimate if the provider omitted usage).
- Chat, Agents, Image, Video, and VS Code model selectors list only models the user selected in the installer (`selected-models.json` `orderedIds`) or later installed from Settings > Models (`downloadedSinceInstall`). An Ollama tag, Hugging Face directory, fallback SANA id, or catalog card that is merely on disk and was never chosen does not appear. Missing snapshot means an empty picker plus Get more models, not every installed model. Settings > Models itself remains the catalog browser used to add more. Within that allowlist, pickers use installer recommend order for that task.
- Image/Video pending copy rotates Creating... / Crafting... / Generating... (never Shaping...).
- Image default on a 16 GB NVIDIA host is the 16 GB recommended image model (`realvisxl-v5` when installed), not Juggernaut unless the user picked it.
- Stop is available on Chat, Agents, Image, and Video while a turn is running.
- Image and Video: Context, Model, and Advanced settings share one row.
- GPU footer is not Idle at 0% while a request is in flight; utilization or a busy state tracks the active job. Composer Context 0% on an empty session is unchanged.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at docs/archive/v2/v2.4/constitution.md - skipping check. Recommend running /constitution to establish project principles.

Governing contracts used instead: `AGENTS.md` (core must not import modules; local-only models; evidence tiers `not_observed != absent`), module authorship, and `docs/archive/v2/v2.4/known-gaps.md`. Carry every v2.4.4 and v2.4.5 MT row as still-open until this cycle's packaged rebuild is observed. Do not close a known gap without a new observation.

## Knowledge-base grounding

No `STRATEGY.md` and no `docs/solutions/` store exist in this tree. Grounding is the operator screenshots, the v2.4.3-v2.4.5 plans, their last-phase evidence, and `docs/archive/v2/v2.4/known-gaps.md`. This cycle *does* reopen VS Code, installer density, Settings cards, picker allowlist and order, studio captions, and GPU occupancy, because the operator forbade deferral.

## Current model map

**Model map status**: fresh as of 2026-09-02; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5-1` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.5-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/about-claude/models/overview
- OpenAI: https://developers.openai.com/api/docs/guides/latest-model
- Google: https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/, https://ai.google.dev/gemini-api/docs/changelog
- Cursor: https://cursor.com/docs/models-and-pricing, https://cursor.com/docs/models/grok-4-6

## Architecture Decisions

1. **A rebuilt wizard that installs a stale desktop is a failed build.** `build-windows.ps1` must run (or require) a current `desktop` `npm run build:shell` whose NSIS version and hash are written into `desktop-payload/manifest.json`. The frozen installer refuses to install desktop if the payload manifest is missing. The running app shows that fingerprint. Tests compare manifest version to the staged bundle, not to a hardcoded 2.4.1 string.

2. **First show is maximized.** `main.py` currently calls `window.show()`. Change the first presentation to `showMaximized()`. `show_and_raise` (tray reattach) stays `showNormal` only when the user had restored; a fresh launch is maximized. Title-bar maximize glyph stays in sync via the existing `changeEvent`.

3. **Setup compactness is layout, not fewer checks.** Prerequisites render in a two-column grid inside the card. Re-check is an icon button on the title row (`aria-label="Re-check"`). GPU card is one line: `{name} | Vendor: {vendor} | {N} GB VRAM`. Drop `layout.addStretch()` on Setup or give it a small maximum so the three sections pack to the top.

4. **Compatible Unsloth is selected.** `_apply_unsloth_host_lock` today only *clears* the box when incompatible. When host-ok, set checked and `state.install_unsloth = True` on first `showEvent` unless the user already unchecked (`_user_touched_unsloth`). Incompatible remains disabled and unchecked.

5. **VS Code is a Features row, default on for current stable.** Lift `SUPPORTED_VSCODE_MAX_EXCLUSIVE` past 1.136 (support current Microsoft stable; keep excluding Insiders, Cursor, Windsurf). Merge install/replace into one Features checkbox default-checked when `detect_vscode_cli().supported`. The separate "VS Code Extension Settings" segment and the Gemma sampling preview go away. Temperature 1.0 / Top-P 0.95 / Top-K 64 are engine defaults, not installer copy.

6. **Video2X copy leaves the wizard.** `INSTALLER_NOTE` is not helpful on Configuration. Settings > Video already owns the executable path. Delete the Configuration block.

7. **Review models are grouped by catalog section.** Use the same section order as the Models step (embed, chat/agentic, image, video, audio, document). Each group is a heading plus the existing two-column marks. Facts column stays path, components, GPU; no 50% empty min-height.

8. **The VS Code extension is the same local agent as desktop Agents, with a model picker.** `nexus.llm.modelName` default `gemma4:e4b` and the installer preview of a single Gemma tag are the lie on screenshot 3. Enumerate the AD-13 allowlist for agentic ids only. Default is `recommendedByTask.agentic` for this host (16 GB: `gemma-4-12b-it-gguf`) when that id is owned. Status-bar / command-palette switcher matches Claude Code's model switch, not a second runtime. Skills, commands, hooks, rules, and the agent loop already live in `src/` and `modules/coding/`; they must keep working when the model id changes. Out of scope: cloud Codex/Claude accounts, remote MCP marketplaces, and a new agent framework.

9. **Sessions History always has a header.** Empty and non-empty trees share one header: title "Sessions History", then left-aligned new-folder and new-session icons. Hairline `marginBlock` equals the gap from Videos to the rule and from the rule to that title. Do not hide the title when `isEmpty`.

10. **Chatbot tab stays Chatbot; every session noun is Session.** Replace `CHAT_FOLDER_TREE_COPY` and sibling copy (empty hint, CTA, delete/archive dialogs, aria, test ids' visible strings). Keep route `/chat`, module id `chatbot`, and the tab label. Flip tests that currently pin "No chats yet" / "Start a new chat" (`StudioHistoryPane.test.tsx`).

11. **Settings cards have no Details.** Delete the `<details>` block. Render the three-line compact card in AD/DoD. Star, downloaded check, and delete stay one `alignItems: center` row. Description is one rewritten sentence from catalog fields (multimodal + agentic + origin + context), not the current two-line plus Best-for list. Update `settings-models-density.test.tsx` so it fails if Details returns.

12. **Assistant token counts are mandatory chrome.** `bubbleTokenMetadata` already supports them. Field miss is persist/mapping plus a weak test. After every assistant turn, `messageUsage` from `estimatedMessageUsage` (or provider usage) must be on the in-memory message *and* the explorer record. Tests must `getByTestId('message-tokens-...')` on the assistant bubble. Unknown counts still omit rather than show 0.

13. **Picker allowlist is installer selection union Settings installs; order is recommend order.** `installedForTask` already has this rule, but `ownedIdSet(null)` returns null and the filter then keeps every `installed` model of that type, including Ollama leftovers and `source: "external"` tags the user never chose. Fail closed: no snapshot means no model rows (only Get more models). The allowlist is `orderedIds` (what the wizard wrote from the user's ticks, including v2.4.5 auto-ticks the user left checked) union `downloadedSinceInstall` (Settings > Models installs after setup). Deselecting a model in the wizard, or never installing it in Settings, removes it from every Chat, Agents, Image, Video, and VS Code selector. Settings > Models still lists the catalog so the user can add one. After the allowlist, sort with `recommendOrderForTask`. Chat uses the chat list; Agents uses agentic.

14. **Image default is the 16 GB recommended image model among owned ids.** `resolveDefaultId` must prefer `recommendedByTask.image` (`realvisxl-v5` on tier 16) when that id is owned. Juggernaut remains selectable only when it is in the AD-13 allowlist (installer tick or a later Settings install). On-disk weights without that membership do not put it in the Image picker and must not become the default. Do not keep `FALLBACK_MODEL` as a fake installed SANA on a live sidecar.

15. **Studio captions rotate; mapping.ts must not say Shaping.** `rotateCaptions={!studioPending}` already routes Image/Video to `useStudioCaptionRotator`. Packaged Shaping... is the old bundle plus `mapping.ts` labels still `"Shaping"`. Change those labels; assert ImageStudioPage pending text is one of `STUDIO_PENDING_CAPTIONS`.

16. **Stop is a first-class composer action on all four tabs.** `MediaComposer` `streaming` today only restyles Send. Add Stop that aborts the in-flight Chat/Agents session and cancels the Image/Video job (`activeJob` / queue cancel). Disabled when idle.

17. **Advanced settings sit on the Context/Model row.** Lift the Image and Video Advanced toggle into `ComposerContextRow` (Context | Model | Advanced). The expanded panel may still open above the composer; the toggle must not consume its own row.

18. **GPU busy tracks jobs, not only nvidia-smi percent.** Register Chat, Agents, Image, and Video in-flight work with the same snapshot `createTelemetryStream` reads. Query `nvidia-smi` with the installer System32 fallback. If a job is active and utilization is 0, show Busy (model name) rather than Idle 0%. VRAM free may still update from smi.

19. **A model on disk is not a selected model.** Probe-discovered weights, `ollama list` extras, Image `FALLBACK_MODEL`, and catalog-only rows are not selector rows. Tests must plant an installed id that is absent from the snapshot and assert it is missing from all four desktop pickers and the VS Code picker.

## Operator item to phase map

Every screenshot item has a phase. None are DF.

| Operator item | Phase |
|---|---|
| Installer opens maximized | 1 |
| Setup two-column prereqs, refresh icon, GPU one-liner, less empty space | 2 |
| Unsloth auto-selected when Compatible | 3 |
| VS Code in Features, default install newest (1.136) | 3 |
| Remove Gemma params and Video2X from Configuration | 3 |
| VS Code uses installer-or-Settings-owned agentic models, Hub skills/commands/hooks | 4 |
| Review categorized, less empty space | 3 |
| Hairline centered, Sessions History always visible, icons left on that row | 5 |
| Chat -> Session except Chatbot tab | 5 |
| Settings card compact copy, no Details, icons one row, hug height | 6 |
| Chat gutters / uncropped glow | 7 |
| Assistant token counts restored | 7 |
| Picker recommend order on four tabs | 7 |
| Selectors show only installer-or-Settings-owned models (no Ollama leftovers) | 4 and 7 |
| Image captions Creating/Crafting/Generating | 7 |
| Juggernaut default explained and corrected | 7 |
| Stop on all four tabs | 7 |
| Advanced settings on Context/Model row (Image and Video) | 7 |
| GPU not Idle 0% during a request | 7 |
| Packaged desktop actually contains this work | 1 and 8 |

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|---|---|---|---|---|
| 1 | Packaged delivery and installer window | Fingerprinted desktop payload; overwrite on reinstall; wizard maximized | frontier | max |
| 2 | Installer Setup compactness | Two-column prereqs, refresh icon, one-line GPU, packed sections | strong | high |
| 3 | Configuration, VS Code host, and Review groups | Unsloth on; VS Code default-on for 1.136; no Gemma/Video2X; categorized Review | frontier | high |
| 4 | VS Code agentic model surface | Allowlisted agentic picker; Hub skills/commands/hooks on that model | frontier | max |
| 5 | Sessions History chrome and Session nouns | Always-on title, centered hairline, left icons, Session copy | strong | high |
| 6 | Settings model cards | Compact three-line cards, no Details, one action row | strong | high |
| 7 | Four-tab runtime chrome | Allowlisted recommend-order pickers, gutters, tokens, Stop, captions, Advanced row, image default, GPU busy | frontier | max |
| 8 | Architecture refactor, known-gaps, CI/CD | Evidence file, local gate, publication after approval, installer rebuild | frontier | max |

- [x] T001 Fail closed when the installer desktop payload is missing or stale scripts/installer/build/build-windows.ps1
- [x] T002 Show a matching desktop build fingerprint desktop/src/pages/settings/SettingsPage.tsx
- [x] T003 Open the installer maximized on first show scripts/installer/src/nexus_installer/main.py
- [x] T004 Add delivery and maximize tests scripts/installer/tests/test_desktop_provisioner.py
- [x] T005 Stabilize Phase 1 docs/archive/v2/v2.4/development/history/2026-09-02_v2.4.6-phase-1-delivery.md
- [x] T006 Put Setup prerequisites in two columns scripts/installer/src/nexus_installer/pages/prerequisites.py
- [x] T007 Move Re-check to a title-row refresh icon scripts/installer/src/nexus_installer/pages/prerequisites.py
- [x] T008 Render GPU name, vendor, and VRAM on one line scripts/installer/src/nexus_installer/pages/gpu_detection.py
- [x] T009 Pack Setup vertical spacing scripts/installer/src/nexus_installer/pages/setup.py
- [x] T010 Add Setup compactness tests scripts/installer/tests/test_pages_qt.py
- [x] T011 Stabilize Phase 2 docs/archive/v2/v2.4/development/history/2026-09-02_v2.4.6-phase-2-setup.md
- [x] T012 Auto-check Unsloth when Compatible scripts/installer/src/nexus_installer/pages/configuration.py
- [x] T013 Move VS Code into Features and support 1.136 scripts/installer/src/nexus_installer/engine/extension_installer.py
- [x] T014 Remove Gemma sampling preview and Video2X from Configuration scripts/installer/src/nexus_installer/pages/configuration.py
- [x] T015 Group Review models by catalog category scripts/installer/src/nexus_installer/pages/review.py
- [x] T016 Add Configuration and Review tests scripts/installer/tests/test_review.py
- [x] T017 Stabilize Phase 3 docs/archive/v2/v2.4/development/history/2026-09-02_v2.4.6-phase-3-configuration-review.md
- [x] T018 Enumerate installer-or-Settings-owned agentic models for the VS Code extension src/config/settings.ts
- [x] T019 Add a VS Code model picker (status bar + command) src/extension.ts
- [x] T020 Keep Hub skills, commands, hooks, and the agent loop on the selected model src/tools/AgentLoop.ts
- [x] T021 Add VS Code model-surface tests tests/unit/src
- [x] T022 Stabilize Phase 4 docs/archive/v2/v2.4/development/history/2026-09-02_v2.4.6-phase-4-vscode-models.md
- [x] T023 Always render Sessions History with left-aligned create actions desktop/src/modules/chat/FolderTree.tsx
- [x] T024 Equalize the Videos/history hairline against that title desktop/src/components/Sidebar.tsx
- [x] T025 Rename visible chat nouns to Session except the Chatbot tab desktop/src/modules/chat/FolderTree.tsx
- [x] T026 Add sidebar Session tests desktop/tests/Sidebar.test.tsx
- [x] T027 Stabilize Phase 5 docs/archive/v2/v2.4/development/history/2026-09-02_v2.4.6-phase-5-sessions.md
- [x] T028 Replace Details with the compact three-line Settings card desktop/src/pages/settings/ModelsSettings.tsx
- [x] T029 Keep star, downloaded, and delete on one centered row desktop/src/pages/settings/ModelsSettings.tsx
- [x] T030 Rewrite density tests so Details fails the suite desktop/tests/settings-models-density.test.tsx
- [x] T031 Stabilize Phase 6 docs/archive/v2/v2.4/development/history/2026-09-02_v2.4.6-phase-6-settings-cards.md
- [x] T032 Equal transcript gutters and uncropped pending glow desktop/src/shared/chat/MessageList.tsx
- [x] T033 Persist and render assistant token counts desktop/src/modules/chat/ChatPage.tsx
- [x] T034 Apply installer recommend order on four pickers desktop/src/shared/models/QuickModelSwitcher.tsx
- [x] T051 Fail closed: unselected installed models never appear in selectors desktop/src/shared/models/selectionPolicy.ts
- [x] T035 Add Stop to Chat, Agents, Image, and Video desktop/src/shared/chat/MediaComposer.tsx
- [x] T036 Rotate Creating/Crafting/Generating and delete Shaping desktop/src/components/agentState/mapping.ts
- [x] T037 Put Advanced settings on the Context/Model row desktop/src/shared/chat/ComposerContextRow.tsx
- [x] T038 Default Image to the 16 GB recommended image model desktop/src/modules/image/ImageStudioPage.tsx
- [x] T039 Mark GPU busy for in-flight pillar jobs desktop/sidecar/src/telemetry/gpuRuntime.ts
- [x] T040 Add four-tab runtime tests desktop/tests/ChatPage.test.tsx
- [x] T041 Stabilize Phase 7 docs/archive/v2/v2.4/development/history/2026-09-02_v2.4.6-phase-7-runtime.md
- [x] T042 Architecture refactor docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.6-field-delivery.md
- [x] T043 Known-gaps reconciliation docs/archive/v2/v2.4/known-gaps.md
- [x] T044 Living docs architecture docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.6-field-delivery.md
- [x] T045 Git-tree hygiene docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.6-field-delivery.md
- [x] T046 Terminal CI/CD reconciliation docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.6-field-delivery.md
- [x] T047 Independent Goal-vs-codebase review docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.6-field-delivery.md
- [x] T048 Last-phase-only human testing docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.6-field-delivery.md
- [x] T049 Full-suite local stabilization docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.6-field-delivery.md
- [x] T050 Publication, integration, and installer rebuild with a fresh desktop payload docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.6-field-delivery.md

---

## Phase 1: Packaged Delivery and Installer Window

**Goal**: A rebuilt installer installs this tree's desktop, overwrites the previous app, shows a matching fingerprint, and opens maximized.
**Prerequisites**: None.
**Stability Gate**: Tests prove a missing or stale desktop payload fails the build or the install step; About/Settings exposes the payload fingerprint; first `show` is maximized.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Cross-cutting installer plus desktop identity; a miss here makes every later desktop phase invisible in the field again.

### Sub-tasks

#### 1.1 -- Fail closed on a stale desktop payload

**Objective**: `build-windows.ps1` stages a bundle built from this tree, not an older NSIS sitting on disk.

**Failure modes**: Missing bundle already exits 1. A bundle whose file mtime or hash does not match a just-built `desktop` output must also fail, rather than silently embedding last week's exe. A corrupt payload at install time already fail-closes in `desktop_provisioner.py`; keep that path.

**Build class**: load-bearing.

**Prompt**:
> Trace `scripts/installer/build/build-windows.ps1` staging and `scripts/installer/src/nexus_installer/engine/desktop_provisioner.py`. Require a current `desktop` shell build (document `npm run build:shell` as a prerequisite, or invoke it). Write version plus sha256 into `desktop-payload/manifest.json`. Refuse to freeze NexusSetup.exe when the staged NSIS is missing. Add a check that the original_name version matches product version. Do not fetch GitHub releases. Acceptance: a deliberately stale staged exe fails the script.

#### 1.2 -- Desktop fingerprint the operator can see

**Objective**: Settings (or About) shows the same version/hash the installer embedded.

**Failure modes**: If the sidecar cannot read the fingerprint, show "unknown" rather than inventing 2.4.1. Do not bump `package.json` (that is `/update release`).

**Prompt**:
> Surface the embedded desktop payload version and a short hash in Settings. Tests pin the string is present and equals the staged manifest in unit fixtures. This is how the operator confirms the app they launched is the one the wizard just wrote.

#### 1.3 -- First presentation is maximized

**Objective**: Fresh installer launch is maximized.

**Failure modes**: Tray reattach must not force maximize over a user restore. `show_and_raise` stays restore-aware. Title-bar glyph stays synced.

**Prompt**:
> In `scripts/installer/src/nexus_installer/main.py`, replace the first `window.show()` with `showMaximized()`. Keep `_toggle_maximized` and `changeEvent` glyph sync. Add a Qt test that the initial show path calls maximize. Do not change desktop (Tauri) window state unless it already has an equivalent first-launch maximize that is broken; this operator item is the installer.

#### 1.4 -- Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until the phase is stable before advancing to Phase 2.

**Prompt**:
> Generate automated tests for payload fail-closed, fingerprint rendering, and maximized first show. Run installer pytest and the desktop settings test you add. CI IMPACT: existing `scripts/installer/**` and desktop vitest paths; no new pipeline file. Do not ask the user to manually exercise an incomplete feature. After tests pass, run `/generate-session-history` for Phase 1, then one local commit. Do NOT push, do NOT open a pull request, and do NOT start remote CI.

### Phase 1 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 2

---

## Phase 2: Installer Setup Compactness

**Goal**: Setup fits prerequisites, GPU, and install path without a tall empty card.
**Prerequisites**: Phase 1.
**Stability Gate**: Qt tests prove two-column prereqs, a title-row refresh control, one-line GPU text, and no full-width Re-check button.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Several related Qt files and visual packing; previous density work passed tests and still looked like screenshot 1.

### Sub-tasks

#### 2.1 -- Two-column prerequisites and refresh icon

**Objective**: Four prereq rows occupy two columns; Re-check is an icon on the title row.

**Failure modes**: Narrow width stacks to one column rather than clipping. Refresh with a dead worker still replaces the previous worker. Icon has an accessible name "Re-check".

**Prompt**:
> Edit `scripts/installer/src/nexus_installer/pages/prerequisites.py` compact mode. Put VS Code + Python on row 1, Disk + Ollama on row 2. Put a small refresh button next to the Prerequisites label. Remove the full-width SecondaryButton("Re-check"). Keep detection workers and status colors.

#### 2.2 -- One-line GPU card and packed Setup spacing

**Objective**: GPU name and characteristics share one line; Setup sections sit at the top.

**Failure modes**: Long GPU names elide rather than grow the card to half the page. Missing GPU still shows the failure callout, not an empty tall box.

**Prompt**:
> Edit `gpu_detection.py` compact card to one line `{name} | Vendor: {vendor} | {display_vram_gb} GB VRAM`. Edit `setup.py`: reduce spacing from 10 if needed, remove or cap the trailing stretch. Do not drop the install-path row or the available-space caption.

#### 2.3 -- Testing and Stabilization

**Prompt**:
> Extend `scripts/installer/tests/test_pages_qt.py` and GPU tests. Assert no widget text "Re-check" as a full-width button; assert an accessible refresh exists; assert GPU label has name and VRAM on one QLabel. Run installer pytest and ruff. CI IMPACT: existing installer tests. Session history for Phase 2, one local commit, no push.

### Phase 2 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 3

---

## Phase 3: Configuration, VS Code Host, and Review Groups

**Goal**: Configuration defaults match a 16 GB Compatible host, and Review is categorized and dense.
**Prerequisites**: Phase 2.
**Stability Gate**: Tests prove Unsloth checked when Compatible; VS Code 1.136 is supported and default-on; Gemma bullets and Video2X note are gone; Review HTML contains category headings.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Installer policy plus VS Code ABI pin; a wrong relaxation installs an extension that cannot start, and a wrong Unsloth default burns a long provision.

### Sub-tasks

#### 3.1 -- Unsloth Compatible is selected

**Objective**: Compatible hosts start with Unsloth checked; Incompatible hosts cannot check it.

**Failure modes**: User uncheck must stick across showEvent. Incompatible still forces unchecked. GPU not yet detected on first construct must refresh on showEvent (already the v2.4.3 lock).

**Prompt**:
> In `configuration.py` `_apply_unsloth_host_lock`, when `_unsloth_host_ok()` and the user has not touched the box, set checked and `install_unsloth = True`. Track `_user_touched_unsloth` from `_on_unsloth`. Keep the Compatible/Incompatible badge. Update help text so it no longer says "Off by default" when the host is Compatible.

#### 3.2 -- VS Code Features checkbox and 1.136

**Objective**: Extension is a Features item, default on for Microsoft VS Code 1.136.

**Failure modes**: Insiders/Cursor/Windsurf stay unsupported. A missing CLI stays visible and unchecked. `engines.vscode` in package.json is `^1.134.0` and already allows 1.136; the installer pin is the blocker (`SUPPORTED_VSCODE_MAX_EXCLUSIVE = 1.136.0`). Raise the exclusive max to the next known-bad ABI, not to "any version forever". Re-probe Electron ABI for 1.136 before claiming support; if the VSIX truly cannot load, that is a fail-closed finding that still must be resolved in this phase (rebuild VSIX for that Electron, do not leave the checkbox disabled).

**Prompt**:
> Update `extension_installer.py` version window to include Microsoft VS Code 1.136.x. Move the checkbox into Configuration Features. Default checked when supported. Merge replace-copy into that one checkbox. Delete the separate VS Code Extension Settings segment. Update `test_extension_installer.py` and `test_vscode_extension_page.py` that currently parametrize 1.136 as unsupported.

#### 3.3 -- Remove Gemma preview and Video2X note; categorize Review

**Objective**: Configuration no longer pretends Gemma is the only extension model; Review lists models by category.

**Failure modes**: Empty category omitted, not shown as "none". Downloaded/pending marks stay. Facts column has no min-height spacer. Headless `--model` still renders.

**Prompt**:
> Delete the Model/Temperature/Top-P/Top-K QLabel and the Optional video enhancement block from `configuration.py`. In `review.py`, group `selected_model_ids` by catalog section using the same order as the Models step. Render a heading per non-empty group, then the existing mark table. Hug the facts card to its content.

#### 3.4 -- Testing and Stabilization

**Prompt**:
> Tests for Unsloth default, 1.136 enabled+checked, absent Gemma/Video2X strings, Review category headings including image containing juggernaut-xl-v9 under Image not mixed with gemma. Installer pytest + ruff. CI IMPACT: existing installer tests; VSIX ABI may need `scripts/build-vsix.ps1` noted, not a new workflow. Session history, one local commit, no push.

### Phase 3 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 4

---

## Phase 4: VS Code Agentic Model Surface

**Goal**: The Nexus VS Code extension selects among downloaded agentic models and keeps Hub agentic capability.
**Prerequisites**: Phase 3.
**Stability Gate**: Tests prove the model enum is the installer-or-Settings-owned agentic set only, an installed-but-unselected id is absent, default is the recommended owned agentic id, and a model switch does not drop tools/skills/hooks.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Extension host plus catalog plus agent loop; uncertainty about Codex-parity gaps must not downshift.

### Sub-tasks

#### 4.1 -- decision: what "Codex-like" means in this tree

**Objective**: Inventory the existing extension against the operator bar before adding a second agent.

**Failure modes**: Do not invent a cloud Codex. If skills/hooks/commands already load, the remaining gap is the model picker plus settings enum. If they do not load in the extension host, wiring them is this phase, not a later plan.

**Prompt**:
> Inventory `src/extension.ts`, `src/tools/AgentLoop.ts`, `src/tools/ToolCatalog.ts`, `src/skills`, hooks, and `package.json` contributes. Write a short decision record in the session history: (1) model picker missing/present; (2) skills/hooks/commands load or not; (3) what Claude Code/Codex UX is already present (plan mode, confirmation gate, terminal). Then implement 4.2-4.3 against that record. Do not start a new agent runtime.

#### 4.2 -- Downloaded agentic model enum and picker

**Objective**: `nexus.llm.modelName` is not a single hardcoded Gemma tag.

**Failure modes**: Empty owned set falls back to a settings message, not every Ollama tag. A catalog-only id cannot be selected. An installed id missing from `orderedIds` and `downloadedSinceInstall` cannot be selected. Default on a 16 GB host is `gemma-4-12b-it-gguf` when that id is owned.

**Prompt**:
> Reuse desktop `installedForTask` / catalog agentic rules from a vscode-free module (`core/` or shared). The enum is the AD-13 allowlist, not `ollama list`. Populate a status-bar item and a `nexus.coding.selectModel` command listing owned agentic models in recommend order. Default `nexus.llm.modelName` from `selected-models.json` `recommendedByTask.agentic` only if that id is owned. Plant a fixture model that is installed but not owned and assert it is absent. Remove installer writing of a single Gemma sampling block (already gone in Phase 3).

#### 4.3 -- Hub skills, commands, hooks stay on the selected model

**Objective**: Changing model does not disable the agentic coding module.

**Failure modes**: Unknown model id returns a structured error pointing at Settings, not a hang. Tools still go through ConfirmationGate and pathGuard.

**Prompt**:
> Confirm AgentLoop, ToolCatalog, SkillCatalog, and hooks resolve against the selected Ollama/HF id. Add a test that switching from gemma-4-12b-it-gguf to another installed agentic id keeps tool dispatch. Do not add network LLM providers.

#### 4.4 -- Testing and Stabilization

**Prompt**:
> Unit tests for enum, default, empty-set fallback, unselected-installed exclusion, and tool dispatch after switch. CI IMPACT: existing extension tests; VSIX rebuild still ABI-sensitive (document the better-sqlite3 Electron rebuild footgun from v2.4.5 evidence). Session history, one local commit, no push.

### Phase 4 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 5

---

## Phase 5: Sessions History Chrome and Session Nouns

**Goal**: The sidebar always says Sessions History, centers the hairline, and uses Session copy except the Chatbot tab.
**Prerequisites**: Phase 4.
**Stability Gate**: Tests prove empty and non-empty trees render the title; hairline gap token is symmetric; visible strings use Session except Chatbot.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Many copy sites and an empty-state layout hole that previous hairline tests missed.

### Sub-tasks

#### 5.1 -- Shared header for empty and non-empty trees

**Objective**: Screenshot 5 cannot hide the title.

**Failure modes**: Compact icon-rail still hides the text title (icons only) but keeps create actions. Empty CTA stays. Archive All / Delete All remain on the header when the list is non-empty.

**Prompt**:
> Refactor `FolderTree.tsx` so empty state uses the same `<header>` as the tree: `paneTitle` default "Sessions History", then left-aligned folder+session buttons (`justifyContent: flex-start`, not `space-between` with icons on the right). Screenshot 8's right-aligned icons on "Chats" is the wrong alignment.

#### 5.2 -- Hairline equal to Videos and the title

**Objective**: The rule sits in the middle of the Videos-to-title gap.

**Failure modes**: FolderTree header padding-top stays 0. Do not add a second spacer under Videos.

**Prompt**:
> Keep `HISTORY_HAIRLINE_GAP` as the only vertical gap. Add a test that empty FolderTree still renders `paneTitle` immediately below the hairline host. Tune if `space-2` is visually unequal against nav item padding; tests should pin one token used on both sides.

#### 5.3 -- Session nouns

**Objective**: Chatbot tab label unchanged; all session copy says Session.

**Failure modes**: `itemNoun`, empty hint, empty CTA, new-item aria, delete/archive dialogs, "No chats yet", "Start a new chat", "New chat" all change. Do not rename module id `chatbot` or route `/chat`.

**Prompt**:
> Update `CHAT_FOLDER_TREE_COPY` and `StudioHistoryPane` copy. Grep desktop for user-visible "chat" strings in history chrome and dialogs. Flip `StudioHistoryPane.test.tsx` and FolderTree tests that pin the old words.

#### 5.4 -- Testing and Stabilization

**Prompt**:
> Sidebar and FolderTree tests for empty header, left-aligned actions, Session strings, Chatbot tab label unchanged. Desktop vitest. CI IMPACT: existing desktop tests. Session history, one local commit, no push.

### Phase 5 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 6

---

## Phase 6: Settings Model Cards

**Goal**: Each model card matches the compact three-line spec with no Details accordion.
**Prerequisites**: Phase 5.
**Stability Gate**: Tests prove no `models-row-*-details` element, one action row, and the requirements/facts/description structure.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Prior density phases implemented a different card; tests must be rewritten so they cannot go green on Details.

### Sub-tasks

#### 6.1 -- Compact card, no Details

**Objective**: Screenshot 6's accordion and empty description space are gone.

**Failure modes**: Missing catalog fields omit the chip, not "undefined". Description is one sentence; do not keep Best for / Why this one / Backend model. Family components that currently live in Details need a one-line Components row if they are required for install, not an accordion.

**Prompt**:
> Rewrite `ModelCard` in `ModelsSettings.tsx`. Remove `<details>`. Layout: title; requirements line (Storage, VRAM, Company, Country, Released); capability line (Agentic, context, Multimodal, Guardrails, License); one-sentence description using origin + multimodal + agentic + context. Star, downloaded, delete: one horizontal centered row. No card min-height beyond padding. Update description copy for Gemma 4 12B to the operator sentence (multimodal agentic, book-length context).

#### 6.2 -- Testing and Stabilization

**Prompt**:
> Rewrite `settings-models-density.test.tsx` and `ModelsSettings.test.tsx` so Details presence fails. Pin action row `flexDirection: row` and `alignItems: center`. Pin absence of "Also agentic", "Backend model:", and `ID:`. Desktop vitest. CI IMPACT: existing desktop tests. Session history, one local commit, no push.

### Phase 6 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 7

---

## Phase 7: Four-Tab Runtime Chrome

**Goal**: Chat, Agents, Image, and Video match the operator chrome: allowlisted pickers, gutters, tokens, order, Stop, captions, Advanced row, image default, GPU busy.
**Prerequisites**: Phase 6.
**Stability Gate**: Tests prove assistant tokens, Stop, studio captions without Shaping, allowlisted recommend order, an unselected installed id absent from all four pickers, Image default `realvisxl-v5` on a 16 GB snapshot when that id is owned, GPU busy while a job is active, and Advanced on the context row.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Cross-cutting UI plus sidecar telemetry plus four pillars; previous cycles green-tested pieces that still failed in the packaged window.

### Sub-tasks

#### 7.1 -- Gutters and assistant tokens

**Objective**: Pending glow aligns with assistant bubbles; assistant shows `(N tokens)`.

**Failure modes**: Provider usage preferred; estimate used when provider omitted; never show 0 for unknown. Persist `messageUsage` on explorer records so reload keeps the count. `ChatPage.test.tsx` must assert the assistant `message-tokens-` node, not `>= 1` total.

**Prompt**:
> Re-read `MessageList.tsx` gutter work from v2.4.4. Keep one list `paddingInline`; no extra pill margin. Trace `patchMessage` / `persistMessage` / explorer DTO for `messageUsage`. Fix any drop. Add timestamp on assistant complete so clock and tokens share the meta row. Flip the ChatPage test that allowed assistant tokens to be missing.

#### 7.2 -- Allowlist then recommend order on four pickers

**Objective**: Dropdowns show only installer-or-Settings-owned models, in installer/Settings recommend order, never `list()` leftovers.

**Failure modes**: `ownedIdSet(null)` must not mean "show everything". Missing snapshot yields only Get more models. Missing `hostVramGB` must not shuffle the allowlist. Agents uses agentic order (Gemma 4 12B first on 16 GB among owned ids), not gpt-oss first. `+ Get more models...` stays last. Image `FALLBACK_MODEL` must not appear when the sidecar is up. An Ollama tag installed outside Nexus does not appear.

**Prompt**:
> Change `ownedIdSet` / `installedForTask` / `installedModelsForType` so a missing snapshot is an empty allowlist, not a pass-through. Audit ChatPage, CodingPage, ImageStudioPage, VideoLabPage `QuickModelSwitcher` props so `ownedIds` is always the snapshot set (empty set, never omitted). Pass `hostVramGB` and `recommendOrderForTask`. Add tests: (1) an installed id absent from `orderedIds` and `downloadedSinceInstall` is missing from all four pickers; (2) a 16 GB snapshot lists Gemma 4 12B before LFM and gpt-oss on Chat/Agents among owned ids, RealVis before Juggernaut on Image when both are owned; (3) Settings > Models still shows unowned catalog cards so the user can install more.

#### 7.3 -- Stop, studio captions, Advanced row, Image default

**Objective**: The Image-tab screenshot items, plus Stop on every tab.

**Failure modes**: Stop on idle is hidden or disabled, not a no-op Send. Caption rotator uses `STUDIO_PENDING_CAPTIONS`. `mapping.ts` image-generation/video-generation labels are not Shaping. Advanced toggle lives in `ComposerContextRow`. Image default uses `recommendedByTask.image` (`realvisxl-v5` on tier 16) when installed.

**Prompt**:
> Add `onStop` to `MediaComposer`; wire Chat/Agents abort and Image/Video `queueClient.cancel` / job cancel. Change `mapping.ts` labels. Assert Image pending text matches `STUDIO_PENDING_CAPTIONS`. Move Advanced into `ComposerContextRow` for Image and Video (Context | Model | Advanced). Fix `ImageStudioPage` default so a 16 GB snapshot with both Juggernaut and RealVis selects RealVis unless the user changed it or a favorite exists. Document in session history that Juggernaut is catalog id `juggernaut-xl-v9`, auto-selected in the wizard because it was already on disk, and is not a ghost model.

#### 7.4 -- GPU busy during a request

**Objective**: Sidebar is not Idle 0% while Chat/Image/Video/Agents is generating.

**Failure modes**: nvidia-smi missing from PATH uses System32 fallback (same as installer). Utilization 0 with an active job shows Busy, not Idle. Composer Context 0% on an empty session stays; do not reuse that bar as GPU.

**Prompt**:
> Wire Chat, Agents, Image, and Video in-flight jobs into the snapshot `createTelemetryStream` already reads (`scheduler.active`). Add `C:\\Windows\\System32\\nvidia-smi.exe` fallback in `gpuRuntime.ts`. When `active` is non-null, `idle` is false even if `utilizationPct` is 0. Tests: footer not Idle while a fake active job exists; PATH-less smi still parses a fixture via System32 override.

#### 7.5 -- Testing and Stabilization

**Prompt**:
> Tests listed in 7.1-7.4 including T051 unselected-installed exclusion. Desktop vitest plus sidecar tests for gpuRuntime. CI IMPACT: existing desktop/sidecar tests. Session history, one local commit, no push.

### Phase 7 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 8

---

## Phase 8: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, its living docs architecture current, and its pipeline reconciled against the canonical contract, with independent evidence that the plan Goal landed, then publish and integrate the completed branch.
**Prerequisites**: All prior phases.
**Stability Gate**: `docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.6-field-delivery.md` exists with one quoted section per duty below. A missing Goal, a Goal-review miss without a recorded known-gap, or a missing evidence section blocks `/update release`.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, Goal-vs-codebase review, known-gap reconciliation, and release gating carry high context volume and blast radius.

### Sub-tasks

#### 8.1 -- Architecture refactor

**Objective**: Refactor toward a well-organized, intuitive layout and quote the scan.

**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure, then refactor toward a clean, intuitive layout via [[project-refactor]] and [[docs-layout-refactor]] (propose-then-apply, with confirmation; repair every reference for anything that moves). Write the detector output into `docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.6-field-delivery.md` under `## Architecture refactor`. An empty finding is allowed only when the scan output is quoted.

#### 8.2 -- Known-gaps reconciliation

**Objective**: Reconcile this version and every other still-open known-gaps file.

**Prompt**:
> Via [[known-gaps-tracker]], reconcile this version's known gaps AND every other `docs/**/known-gaps.md` whose Status is in-progress or whose Open Items remain. Glob both canonical (`docs/releases/v<MAJOR>/v<MAJOR>.<MINOR>/known-gaps.md`) and legacy layouts. Carry v2.4.4 MT-1..MT-6 and v2.4.5 MT-1 as still open until the rebuilt installer is observed; do not close them on unit tests. If a file is unreachable, record the glob result and continue with what was found. Write the disposition into the evidence file under `## Known-gaps reconciliation`.

#### 8.3 -- Living docs architecture

**Objective**: Prove handbooks, decisions, and generated HTML match the required living tree.

**Prompt**:
> Check `docs/handbooks/` (markdown source of truth, generated `html/`, one atlas walkthrough, technical companions for key components), `docs/decisions/`, and living `docs/README.md` / `docs/DEVLOG.md` / `docs/todos.md`. Self-gated: never invent `docs/testing/` or `docs/validation/`. Markdown wins if HTML disagrees; regenerate or fail the check. Quote the check in the evidence file under `## Living docs architecture`. Details live in [[docs-layout-refactor]].

#### 8.4 -- Git-tree hygiene

**Objective**: Report branch and repository-settings hygiene without deleting anything.

**Prompt**:
> Run `python scripts/check_release_preconditions.py --branches --repo-settings`. Quote the output in the evidence file under `## Git-tree hygiene`. Report only; never delete branches.

#### 8.5 -- Terminal CI/CD reconciliation

**Objective**: The repository's pipeline is compared field by field against the canonical contract, and every difference is either applied with approval or recorded as a known gap.

**Prompt**:
> Invoke [[cicd-architect]] in existing-pipeline comparison mode and run its six steps in order: DETECT the active CI provider (recording "none detected" rather than assuming one); COMPARE the existing pipeline against every field of the canonical contract - repository-native profiles, event separation, runner selection, the always-resolving aggregate required check, permissions, immutable action or plugin references, caching, concurrency, path scoping, artifact retention, structured reports, deployment boundaries, and failure recovery; PROPOSE each difference with its cost, its risk, and the smallest change that closes it; obtain explicit APPROVAL per change (silence is not approval); APPLY the approved changes and re-run the local gate; and RECORD every declined or environment-only difference as a known gap with an owner and a next step via [[known-gaps-tracker]]. The comparison concludes PASS only when every required field has observable evidence; a green pipeline run is not evidence, because a pipeline that checks nothing is green for the wrong reason. If this repository ships more than one installer, run or add a declarative cross-installer parity gate covering distributed artifacts, supported platforms, named capability counterparts, and external-tool fallbacks, then execute the real installers on their target operating systems with identical postconditions. Cross-link [[platform-contract-verification]] so discovery and delivery are checked in the same pass. If the repository ships zero or one installer, record a silent no-op and continue. Quote the per-field comparison result in the evidence file under `## CI/CD coverage`.

#### 8.6 -- Independent Goal-vs-codebase review

**Objective**: Prove the plan Goal landed in the codebase, not that checkboxes were ticked.

**Prompt**:
> Re-read the plan header Goal and the Goals First / Definition of Done. Inspect the codebase as if this agent had not implemented the phases. Write `## Goal-vs-codebase review` in `docs/archive/v2/v2.4/development/last-phase-evidence-v2.4.6-field-delivery.md` listing: the plan Goal restated; the code/docs artifacts that satisfy it; and any gap. Completing every prior-phase sub-task is not evidence the Goal landed. A miss is a blocking finding or a new known-gap, not a pass. In particular, verify the operator-item table in this plan still has an artifact per row.

#### 8.7 -- Human/manual testing suggestions (last phase only)

**Objective**: Ask a human to exercise only what automated tests cannot cover, now that the feature is complete.

**Prompt**:
> Emit human/manual testing suggestions for the rebuilt installer and desktop. Do not invent a fake walkthrough. Operator items: (1) wizard opens maximized; (2) Setup two-column prereqs, refresh icon, one-line GPU; (3) Unsloth checked on this 16 GB host; VS Code feature checked on 1.136; no Gemma bullets; no Video2X note; (4) Review grouped by category; (5) after install, desktop fingerprint matches payload; (6) Sessions History visible empty and filled, hairline centered, Session copy, Chatbot tab unchanged; (7) Settings Gemma card matches the three-line spec, no Details; (8) Chat glow gutters, assistant tokens, recommend order, and no selector row for a model the installer did not select; (9) Image captions rotate, default RealVis among owned ids (Juggernaut only if owned), Stop works, Advanced on the context row; (10) GPU footer Busy/non-zero while generating. Rebuild the installer with a fresh `desktop` `build:shell` so this list is observable.

#### 8.8 -- Testing and Stabilization

**Objective**: Prove the refactor preserved behavior and the complete LOCAL gate is green before anything is published.

**Prompt**:
> Run the full local validation/test suite, confirm the refactor changed no behavior, and iterate until clean. This gate is local by construction: it must pass before the branch is published, so it cannot depend on a remote run. Quote the suite in the evidence file under `## Full-suite testing and stabilization`. Generate a session-history entry for this phase. After `scripts/build-vsix.ps1`, run `npm rebuild better-sqlite3` before Node tests (v2.4.5 ABI footgun).

#### 8.9 -- Publication and integration

**Objective**: Publish the completed branch once, validate the merge result remotely, and hand a green integration to the release flow.

**Prompt**:
> Create the final local commit for this phase. Then present the resolved branching model, remote, branch name, and pull-request target, and obtain EXPLICIT approval before the plan's first branch push. Push once, open the integration pull request against the integration branch, and report the exact required checks expected. Wait until every required check reaches a terminal state. A red check REOPENS this phase: classify the failure, reproduce it locally, apply the narrow fix, re-run the local gate, update known gaps and session history, then amend the final commit or add one narrowly scoped stabilization commit and push again with approval. Never re-run a red check without a local reproduction; a re-run without one is a guess. Merge only after every required check is green and the user approves. Confirm the post-merge workflow performed only its intended smoke, publication, or provenance work and did not rerun the complete suite. Rebuild `NexusSetup.exe` with a current desktop NSIS payload before asking the operator to field-test. Only then hand off to `/update release`, which owns the version bump, changelog, tag, push, and GitHub Release behind its own confirmation gates. Quote the required-check results in the evidence file under `## Publication and integration`.

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| | | |

### Phase 8 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] CI impact recorded against the canonical contract, with no remote CI run until T050
- [ ] Session history generated for this phase
- [ ] One local commit created for this phase
- [ ] Branch push, pull request, and remote CI occurred only after explicit approval
- [ ] Ready for `/update release` after green integration
