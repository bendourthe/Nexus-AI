# Session history: v2.2.4 Phase 7 - architecture, gaps, and CI

**Date**: 2026-08-23
**Operator**: Project owner
**Objective**: Reconcile architecture, known gaps, and CI after Phases 1-6. Do not bump package.json.
**Outcome**: Pickers stay on `installedForTask` plus `~/.nexus/selected-models.json`. Chat/Agents compact switchers no longer forward tool-calling badges. Settings Models documents leftover weights. DF-23 and DF-24 record unproven packaged Hub update and Unix installer snapshot write. CI already runs desktop vitest, installer pytest, and docs-layout; comments now name the v2.2.4 contracts. No live Hub clone in PR CI.

---

## 1. Starting state

- **Branch**: `develop`
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.4-chatbot-first-and-runtime-honesty.md`
- **Prior session reference**: `docs/archive/v2/v2.2/development/history/2026-08-23_v2.2.4-phase-6-hub-update.md`

Phases 1-6 were committed. Remaining work was Phase 7 reconciliation, then push, then a field-review of the original screenshot failures.

---

## 2. Chronological steps

### 2.1 Architecture

**Plan specification**: One installed-catalog module; delete lying fallbacks; propose-then-apply moves; do not drag DF-14 composer merge.

**What happened**: `selectionPolicy.ts` is documented as the only picker feed. Chat and Agents `FALLBACK_LLMS` stay `installed: false` placeholders. `QuickModelSwitcher` no longer copies tool-calling fields onto compact picker options, so Chat/Agents do not show those badges. No file moves: ChatPage and CodingPage already share `installedForTask`. Dead `image.generate` UI hooks were not found.

**Key files changed**: `desktop/src/shared/models/selectionPolicy.ts`, `desktop/src/shared/models/QuickModelSwitcher.tsx`, `desktop/src/modules/chat/ChatPage.tsx`, `desktop/src/modules/coding/CodingPage.tsx`, `desktop/src/pages/settings/ModelsSettings.tsx`

### 2.2 Known gaps

**Plan specification**: Close rows this cycle actually closed. Keep DF-1, DF-2, DF-4, DF-14, DF-16, DF-18-22. Add DFs for unproven packaged Hub update and live GPU.

**What happened**: Screenshot BGs 33-42 stay resolved. DF-17 stays resolved. Added DF-23 (packaged Hub live clone not run) and DF-24 (Unix installer snapshot write not proven; writer is Windows-first `RuntimeProvisioner` only). DF-4 already covers live GPU img2img.

**Key files changed**: `docs/archive/v2/v2.2/known-gaps.md`

### 2.3 CI

**Plan specification**: Desktop vitest, sidecar tests, installer snapshot tests, docs-layout. Path filters, cancel-in-progress. No live Hub clone.

**What happened**: `ci.yml` already runs `npm run test:shell` on develop (Node 22), docs-layout, and TypeScript tests. `installer-tests.yml` already path-filters `scripts/installer/**` with cancel-in-progress. Added a comment that v2.2.4 contracts ride those jobs. Did not add a network Hub clone.

**Key files changed**: `.github/workflows/ci.yml`

### 2.4 Cross-installer snapshot

**Plan specification**: Write the snapshot from the shared complete path on every OS that runs that path; otherwise record a gap.

**What happened**: `write_selection_snapshot` is in `scripts/installer/src/nexus_installer/engine/runtime_provisioner.py`. This repo has no separate Unix complete-path writer. Recorded DF-24 rather than claiming parity.

---

## 3. Verification

- Desktop vitest for Phase 7-touched suites (ModelsSettings, QuickModelSwitcher, ChatPage, CodingPage, selection-policy, catalogTabs, SkillsSettings).
- No package.json bump.

---

## 4. Decisions and deviations

- Did not extract a `useTaskModels` hook. Chat and Coding already share `installedForTask`; a new hook would be a move without a second consumer.
- Did not merge CodingInput into the shared composer (DF-14).

---

## 5. Follow-ups

Push `develop`. Re-check the nine original screenshot failures. Iterate if any still fail. Rebuild the installer for testing. Do not run `/update release` until the operator asks.
