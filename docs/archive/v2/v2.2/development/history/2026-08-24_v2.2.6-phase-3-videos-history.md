# Development Log: v2.2.6 Phase 3 - Videos history and last-clip memory

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Video Lab named sessions persist turns, a follow-up with no new photo continues from the last clip when the protocol already has continueFrom, and remount hydrates the transcript. Do not bump package.json. Context meters stay v2.2.7.
**Outcome**: Video Lab creates/reuses a studio session, appends user then assistant turns (mp4 paths only), attaches continueFrom on in-session follow-ups, and hydrates MessageList on remount. Missing files render error text, not an empty complete.

---

## 1. Starting State

- **Branch**: `develop` (Phase 2 commit `df40e38`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.6-session-memory-and-studio-history.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-24_v2.2.6-phase-2-images-history.md`

---

## 2. Chronological Steps

### 2.1 Persist video turns (3.1)

**Plan specification**: Mirror Phase 2 for VideoLabPage. Persist outputPath. Follow-up without a new photo may set continueFrom or sourceImage only when the existing video protocol already supports it. Missing mp4: error text, not a complete with empty path.

**What happened**: VideoLabPage auto-creates a named session on first send when sidecar is up / an explorer is injected. persistTurn no-ops when sidecar is down. Completes persist mp4Path when it is a real path, never a data: blob. Errors persist text and do not move lastOutputRef. studioTurnsToChatMessages gained mediaKind so hydrated clips render as video.

### 2.2 Implicit last-clip follow-up (3.1)

**Plan specification**: Empty attachments after a successful generate may condition on the last playable clip when continueFrom already exists. Do not invent continueFrom. User photo attachments still win (image2video).

**What happened**: continueFrom already existed for intra-job segment chaining (priorJobId, lastFramePath, segmentIndex, segmentCount). In-session follow-ups with no photo attach that object using the last complete job id and last mp4 path. After remount, priorJobId is not in the session schema, so a follow-up is a new text2video. Unreadable lastOutputRef fail-closes with UNREADABLE_OUTPUT_TEXT.

### 2.3 Tests (3.2)

Implicit continueFrom + persist, remount hydrate, missing-file hydrate. Existing text2video tests still pass.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| desktop VideoLabPage + studioSessionMemory + videoIntent | PASS 31/31 |
| Implicit follow-up sets continueFrom.priorJobId and lastFramePath | PASS |
| Remount hydrate shows user text + video src | PASS |
| Missing file -> error text, no video element | PASS |
| package.json bump | Not done |
| Hub injection scanner | Untouched |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Remount follow-up cannot set continueFrom because priorJobId is not persisted | P2 | Honest: protocol requires priorJobId; schema has lastOutputRef path only. New text2video after remount. |
| lastFramePath is the last mp4 path, not an extracted last frame | P2 | Accepted; no frame extractor in this cycle |
| Packaged disk persist not_observed | P2 | Phase 6 / field |

---

## 5. Plan Discrepancies

- VideoLabPage is the live page name (plan VideoLabPage).
- continueFrom was not invented; session follow-up reuses the existing segment-chain field.
- lastOutputRef is a filesystem path, never an mp4 blob in SQLite.

---

## 6. Assumptions Made

- In-memory explorer is a valid stand-in for two-instance SQLite in UI tests; SQLite round-trip remains the Phase 1 store test.
- outputExists is a test seam; production treats non-data paths as usable until decode fails.

---

## 7. Testing Summary

### Automated Tests

- 31 passed (videoIntent 8, studioSessionMemory 4, VideoLabPage 19)

### Manual Tests

- not_observed (packaged Video Lab quit/reopen)

---

## 8. Next Session

Phase 4 Agents: resume must hydrate visible turns, not only session id. Unknown id -> typed error. Do not load weights on tab peek.
