# Development Log: v2.2.7 Phase 1 - Catalog context on Models tabs

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Installer and Settings Models surfaces show the catalog-reported context window as `<val>k` with no trailing `in`. Null windows omit the chip. Do not invent 128k. Do not bump package.json.
**Outcome**: `ListedModelDto` marshals `contextWindow` / optional in-out split from the catalog. Settings ModelCard and installer `_ModelCard` render `Context: 128k` (or `Context: 32k / 8k`). Diffusion rows with `contextWindow: null` show no chip.

---

## 1. Starting State

- **Branch**: `develop` (v2.2.6 Phase 6 commit `6fab8d9`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.7-context-meter-and-transcript-chrome.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-24_v2.2.6-phase-6-gaps-ci.md`

v2.2.6 session memory is on `develop`. Installer already chipped some text models as `Nk in / Mk out`. Desktop Settings Models had origin/date/guardrails chips but no context window. `InMemoryModelRegistry` still defaults missing windows to 128000; that path is not used for the catalog chip.

---

## 2. Chronological Steps

### 2.1 Marshal contextWindow (T001)

**Plan specification**: Desktop list-models IPC includes `contextWindow` (number or null). Missing is null, not 0. Non-numeric junk skips the chip and logs once. Do not default Gemma's window onto other ids.

**What happened**: Added `core/registry/contextWindow.ts` (`parseContextWindow`, `catalogContextFromSpec`, `formatContextChip`). `NexusModelRegistry.list()` spreads catalog windows onto installed and catalog-only rows. Sidecar `toDto` copies them as null when absent. Optional `contextWindowIn` / `contextWindowOut` are marshaled when the catalog has them; they are not invented from a single window.

**Key files changed**: `core/registry/contextWindow.ts`, `core/registry/catalog.ts`, `core/registry/NexusModelRegistry.ts`, `desktop/sidecar/src/models/modelsService.ts`, `desktop/src/pages/settings/modelsTypes.ts`

### 2.2 Render on Settings and installer (T002)

**Plan specification**: Visible Context chip on both Models tabs as `128k` or `32k / 8k`. Do not append `in`. Independent of the Compatible badge. Every catalog row with numbers gets a chip, not only text models.

**What happened**: Settings `ModelCard` renders `formatContextChip(item)` after the origin chip. Installer `typed_catalog.py` parses with `parse_context_window` (0 means absent), formats with `format_context_chip`, and shows the chip on any row with a positive window (including document OCR 32k). Image fixtures with no window have no Context pill. Over-VRAM SANA still shows Needs VRAM, not Compatible.

**Key files changed**: `desktop/src/pages/settings/ModelsSettings.tsx`, `scripts/installer/src/nexus_installer/pages/typed_catalog.py`

### 2.3 Tests and CI (T002, 1.3)

ModelsSettings: LFM 128000 -> `Context: 128k`; split -> `32k / 8k`; gemma without a field and ltx-video / SANA 4K omit the chip. NexusModelRegistry list + ModelsService toDto round-trip. Installer load + Qt card tests. CI comments name the v2.2.7 chip contracts; installer-tests path filters already cover `scripts/installer/**`.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| `tests/unit/core/registry/contextWindow.test.ts` + `NexusModelRegistry.context-window.test.ts` | PASS 7/7 |
| desktop ModelsSettings + modelsService | PASS 30/30 |
| installer `test_typed_catalog.py` | PASS (70 tests) |
| desktop `tsc --noEmit` | PASS |
| ruff `typed_catalog.py` | PASS |
| Null window invents 128k | No |
| Chip copy contains `in` | No |
| Compatible badge logic changed | No |
| package.json bump | Not done |
| Hub injection scanner | Untouched |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Packaged Settings / installer chips not screenshotted | P2 | DF-28 |
| TypeScript and Python formatters are duplicated | P3 | WN-7; tests lock both |
| `InMemoryModelRegistry` still defaults 128000 | P3 | Out of scope; not the catalog DTO path |

---

## 5. Plan Discrepancies

- Chip copy is `Context: 128k` (label plus value) to match Origin/Released chips. The numeric part is `<val>k` with no `in`.
- Catalog.json has no in/out split rows today; the split path is unit-tested with fixtures.

---

## 6. Assumptions Made

- Floor-divide by 1000 matches the previous installer `// 1000` (128000 -> 128k, 32768 -> 32k).
- Explicit `contextWindowIn: null` falls back to `contextWindow` in the installer loader.

---

## 7. Testing Summary

### Automated Tests

- 7 passed (core contextWindow + registry list)
- 30 passed (desktop ModelsSettings 19, modelsService 11)
- 70 passed (installer typed_catalog)

### Manual Testing Still Needed

Human QA is deferred to the last phase. Packaged Settings and installer Models chips remain DF-28.

---

## 8. TODO Tracker

### Completed This Session

- [x] Phase 1.1 marshal contextWindow
- [x] Phase 1.2 Settings + installer chips
- [x] Phase 1.3 tests, CI comments, session history

### Remaining

- [ ] Phase 2 session token accounting
- [ ] Phase 3 pill meter + picker row + 80% CTA
- [ ] Phase 4 date/time/tokens on all four modes
- [ ] Phase 5 gaps, CI, installer rebuild, push develop

---

## 9. Summary and Next Steps

Phase 1 is done on `develop` pending commit. Next: `/implement` Phase 2 (`sessionContextUsage` + persist per-turn token fields). Do not bump package.json. Do not disable the Hub scanner. Do not run `/update release`.
