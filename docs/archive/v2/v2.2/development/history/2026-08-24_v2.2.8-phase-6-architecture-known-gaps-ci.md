# Development Log: v2.2.8 Phase 6 - Architecture, Known-Gaps, and CI

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Leave the tree clean, close DF-2 with observed Explorer evidence, map T001-T012 onto CI, write Goal-vs-codebase, then commit and push `develop`.
**Outcome**: Unused desktop `SessionListPanel` removed. DF-2 closed. CI comments name the timeout, Gemma probe, FolderTree collapse, Models sort, and Hub quarantine contracts. Last-phase evidence file replaced for v2.2.8. Package.json not bumped.

---

## 1. Starting State

- **Branch**: `develop` (v2.2.8 Phase 5 commit `692d068`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.8-working-local-studio.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-24_v2.2.8-phase-5-hub-latest-apply.md`

Phases 1-5 were committed. FolderTree had replaced Agents `SessionListPanel` in production; the TSX file and its tests were leftover.

---

## 2. Chronological Steps

### 2.1 Architecture refactor (T013 adjacent)

**Plan specification**: Remove obsolete SessionList-only Agents chrome if FolderTree replaced it. Chatbot stays on `chat.explorer.*`. Quote the scan.

**What happened**: `desktop/src/modules/coding/panels/SessionListPanel.tsx` had no production importer (only `desktop/tests/panels.test.tsx`). Deleted the component and its describe block. Agents Sessions tab remains a short pointer to the left FolderTree (`data-testid="sessions-panel"`). VS Code `src/panels/SessionListPanel.ts` untouched.

**Key files changed**: deleted `desktop/src/modules/coding/panels/SessionListPanel.tsx`; `desktop/tests/panels.test.tsx`

### 2.2 Known-gaps (T013)

Closed DF-2 as observed (2026-08-24 packaged Explorer). Kept DF-4, DF-23, DF-24, DF-25, DF-26, DF-27, DF-32, DF-33, DF-34, DF-35. Pointed v2.2.5/v2.2.6 DF-2 rows at the v2.2.8 close.

### 2.3 CI/CD (T014)

Comments on `ci.yml`, `shell-build.yml` (cargo test rpc_timeout_for), and `installer-tests.yml`. No new jobs. No live GPU. No live Hub clone. Dual-installer parity script does not exist here (no-op).

### 2.4 Goal-vs-codebase

Recorded in `docs/archive/v2/v2.2/development/last-phase-evidence.md`. Packaged Hi / GPU generate / Hub latest / Settings identity remain DF, not silent passes.

### 2.5 Testing and Stabilization

Fixed `CodingPage` picker test: mock LLMs now set `agentic: true` so they survive `catalogTab="agentic"` (Phase 4 sort filter). Full desktop and root vitest quoted in last-phase evidence. Unsigned installer not rebuilt (version still 2.2.5).

---

## 3. Verification Gate

| Check | Result |
|---|---|
| desktop vitest (`npm run test:shell`) | PASS 177 files, 1538 tests |
| root vitest | PASS 522 files (3 skipped), 5484 tests (12 skipped) |
| cargo test rpc_timeout | PASS 3 |
| desktop `tsc --noEmit` | PASS |
| SessionListPanel.tsx | Deleted |
| DF-2 | Closed observed |
| package.json bump | Not done |
| Live GPU / live Hub clone in CI | Not added |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Packaged Hi | P2 | DF-32 |
| Agents folder overlay | P3 | DF-33 |
| Packaged Settings vs installer | P2 | DF-34 |
| Packaged Hub Active=latest | P2 | DF-35 |
| Live GPU generate | P2 | DF-4 |
| Unsigned installer not rebuilt this phase | P3 | Operator reuses v2.2.7 bundle or rebuilds after `/update release` |

---

## 5. Plan Discrepancies

- Unsigned Windows installer was not rebuilt in Phase 6. Product version in `tauri.conf.json` is still 2.2.5. Honest skip, not a claimed rebuild.
- `scripts/check_release_preconditions.py` and `scripts/check_installer_parity.py` are Nexus-Hub catalog tools and do not exist in this repo.

---

## 6. Assumptions Made

- Closing DF-2 uses the plan's Review verdict (packaged Explorer screenshots 2026-08-24), not a new soak this phase.
- `/update release` after push owns the version bump; Phase 6 must not bump package.json.

---

## 7. Testing Summary

- Full desktop vitest 1538
- Full root vitest 5484
- rpc_timeout cargo tests 3
- CodingPage agentic picker fixture
- panels.test without SessionListPanel

---

## 8. TODO Tracker

| Item | Status |
|---|---|
| T013 known-gaps + DF-2 close | Done |
| T014 CI map T001-T012 | Done |
| Last-phase evidence | Done |
| Push develop | After this commit |
| `/update release` | After push, confirmation gates |

---

## 9. Summary and Next Steps

Phase 6 removes leftover Agents SessionList chrome, closes DF-2, and maps the cycle's contracts onto existing CI. Next: commit, push `develop`, then `/update release` with confirmation before tag, push of the release, or GitHub Release publish.
