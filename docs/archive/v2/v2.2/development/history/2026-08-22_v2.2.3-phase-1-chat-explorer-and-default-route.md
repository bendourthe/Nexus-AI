# Development Log: v2.2.3 Phase 1 - Chat explorer IPC and default route

**Date**: 2026-08-22
**Operator**: Project owner
**Assisted by**: Codex
**Objective**: Make Local Chatbot render through the production IPC client and become the first-launch module without re-running predecessor cycles.
**Outcome**: The renderer now consumes a promise-safe explorer contract, route failures are contained, and `/` plus invalid restored routes land on `/chatbot`.

---

## 1. Starting State

- **Branch**: `develop`
- **Starting commit**: `4f3001c`
- **Environment**: Windows, Node desktop workspace, Vitest 2.1.9
- **Prior session reference**: `docs/archive/v2/v2.2/development/history/2026-08-22_v2.2.2-phase-6-gaps-ci.md`
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.3-glass-orbs-and-pillar-runtime.md`

The v2.2.3 verification addendum identified a production-only sync/async contract lie that blanked Chatbot before the existing sidecar banner could render. Phase 1 code was already implemented but uncommitted when this continuation began.

---

## 2. Chronological Steps

### 2.1 Promise-safe explorer adapter

**Plan specification**: Reconcile the explorer type families, map every FolderTree call onto a promise-safe client, cache tree-backed lookups, and retain the synchronous in-memory test client.

**What happened**: `AsyncChatExplorerClient` now describes values that may be immediate or promised. The IPC adapter supplies `listTree`, object-form `createFolder`, cached `getFolder`, `getChat`, and `ancestors`, delegated or cached search, and cache invalidation after mutations. FolderTree uses `resolveMaybe` for reads and writes, so a rejected IPC request becomes a one-line tree error rather than an uncaught render failure.

**Key files changed**: `desktop/src/modules/chat/types.ts`, `desktop/src/modules/chat/chatExplorerClient.ts`, `desktop/src/modules/chat/ipcChatExplorerClient.ts`, `desktop/src/modules/chat/FolderTree.tsx`, `desktop/src/modules/chat/ChatPage.tsx`

**Troubleshooting**: No implementation failure occurred during this continuation. The earlier single-file test failure report was a concurrent-Vitest contention artifact; the required solo full suite passed.

### 2.2 Route containment and first-launch route

**Plan specification**: Add a module-level error boundary, redirect `/` to `/chatbot`, and normalize missing, Dashboard, and invalid persisted routes while preserving real module routes.

**What happened**: `ModuleErrorBoundary` wraps the routed module pane and remounts per pathname. ChatPage completes the `minHeight: 0` flex chain. `normalizeActiveRoute` makes Local Chatbot the first-launch fallback while keeping Dashboard explicitly reachable at `/dashboard`.

**Key files changed**: `desktop/src/components/ModuleErrorBoundary.tsx`, `desktop/src/App.tsx`, `desktop/src/lib/persistence.ts`, `desktop/src/main.tsx`

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Solo desktop full suite, `npm run test` | PASS: 154 files, 1325 tests, 0 failures |
| Phase regression files | PASS: 5 files, 71 tests, 0 failures |
| Desktop lint | PASS: 0 errors |
| Desktop typecheck | PASS: 0 errors |
| Desktop production web build | PASS: 1751 modules transformed |
| Git ignore audit | PASS: 0 patterns added; no new cache or generated artifact appeared in the worktree |
| CI/CD readiness | PASS: existing `ci.yml` desktop suite includes the Phase 1 test paths; no workflow change required |
| Docs cleanup audit | PASS: no scratch docs, duplicate plans, or moves proposed |
| Packaged Explorer launch | NOT RUN: tracked as v2.2.3 DF-2 |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Production folder color changes lack an `updateFolder` IPC method | P2 | Deferred as DF-20 |
| Packaged first-paint acceptance was not executed | P1 | Deferred as DF-2; unit behavior is supported, packaged behavior is not proven here |

---

## 5. Plan Discrepancies

None. The implementation follows the verified and corrected Phase 1 instructions.

---

## 6. Assumptions Made

- **Dashboard remains a supported explicit route**: The plan permits `/dashboard` for tests and styleguide access while excluding it from first-launch restoration.
- **Synchronous test clients remain valid**: The async-safe interface intentionally accepts immediate return values so existing in-memory tests do not need artificial promises.

---

## 7. Testing Summary

### Automated Tests

- Desktop Vitest: 1325 passed, 0 failed across 154 files.
- Phase 1 targeted Vitest: 71 passed, 0 failed across 5 files.
- Desktop ESLint and TypeScript checks exited zero.
- Vite production build completed after transforming 1751 modules. The existing chunk-size advisory remains informational and was not introduced by Phase 1.

### Manual Testing Performed

- Git diff inspection confirmed the old `as unknown as ChatExplorerClient` cast is absent from ChatPage.

### Manual Testing Still Needed

- [ ] Launch the packaged shell from Explorer and confirm Local Chatbot renders without a blank pane.

---

## 8. TODO Tracker

### Completed This Session

- [x] Make the production explorer adapter promise-safe.
- [x] Contain routed module crashes.
- [x] Make Local Chatbot the default route and persisted fallback.
- [x] Run the solo full desktop suite.

### Remaining

- [ ] Complete and bank v2.2.3 Phases 2 and 7.
- [ ] Implement v2.2.3 Phases 3, 4, 5, 6, and 8.

### Out of Scope

- [ ] Packaged clean-VM acceptance and native folder-color persistence.

---

## 9. Summary and Next Steps

Phase 1 removes the production sync/async explorer crash, adds an in-pane recovery boundary, and makes Local Chatbot the first-launch module. The next step is the Phase 1 lint, typecheck, targeted regression, staged-diff, and atomic commit gate, followed by the Phase 2 post-phase sequence.
