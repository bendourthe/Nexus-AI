# Development Log: v2.2.3 Phase 3 - Orbs, generation honesty, and playback

**Date**: 2026-08-23
**Operator**: Project owner
**Assisted by**: Codex
**Objective**: Give pending Studio work a clear hero state, make every generation terminal event honest, and make successful MP4 paths playable in the Tauri webview.
**Outcome**: Image and Video generations now end in displayable media or a written error, pending media has a captioned hero orb, and Video Lab resolves output paths through a narrowly scoped asset protocol.

---

## 1. Starting State

- **Branch**: `develop`
- **Starting commit**: `38f5335`
- **Environment**: Windows, React 19 renderer, Node sidecar, Tauri 2, Vitest 2.1.9
- **Prior session reference**: `docs/archive/v2/v2.2/development/history/2026-08-22_v2.2.3-phase-7-installer-document-taskbar-complete.md`
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.3-glass-orbs-and-pillar-runtime.md`

Phases 1, 2, and 7 were already committed as disjoint path-scoped changes. Phase 3 began only after those chrome and installer commits were banked.

---

## 2. Chronological Steps

### 2.1 Captioned hero generation state

**Plan specification**: Use the existing hero orb for pending media, keep active motion cyan, expose useful captions, and avoid widening plain chat bubbles.

**What happened**: AgentStateOrb gained optional inline or hero captions without changing its size contract. Media-bearing pending bubbles use a centered transparent hero surface; text pending states keep the inline orb. Media width is scoped by content type, so ordinary chat bubbles retain their prior fit-content layout.

**Key files changed**: `desktop/src/components/agentState/AgentStateOrb.tsx`, `desktop/src/components/agentState/mapping.ts`, `desktop/src/shared/chat/MessageBubble.tsx`, `desktop/src/shared/chat/MessageList.tsx`

### 2.2 Terminal generation honesty

**Plan specification**: A completion without bytes must become a written error, queue failures must reach the renderer, and actions must exist only for displayable assets.

**What happened**: QueuePump exposes a typed error callback, the sidecar records error completions for queue exceptions and missing outputs, and Video completion is no longer double-gated on optional workflow metadata. Image Studio and Video Lab clear output, workflow, and media state on terminal or decode failure, preventing empty cache entries and false recall actions.

**Key files changed**: `core/generations/queuePump.ts`, `core/generations/index.ts`, `desktop/sidecar/src/handlers.ts`, `desktop/src/modules/image/ImageStudioPage.tsx`, `desktop/src/modules/video/VideoLabPage.tsx`

### 2.3 Playable Tauri video URLs

**Plan specification**: Convert generated filesystem paths for the webview and limit asset access to the outputs directory.

**What happened**: App supplies `convertFileSrc` to Video Lab. The Tauri dependency enables `protocol-asset`, the asset scope admits only the video output subtree, and the CSP admits the local asset schemes. Image and video elements expose visible error fallbacks.

**Key files changed**: `desktop/src/App.tsx`, `desktop/src-tauri/Cargo.toml`, `desktop/src-tauri/tauri.conf.json`

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Focused desktop Phase 3 tests | PASS: 7 files, 85 tests, 0 failures |
| Focused root queue tests | PASS: 20 tests, 0 failures |
| Solo desktop full suite | PASS: 154 files, 1334 tests, 0 failures |
| Root full suite | PASS: 515 files passed, 3 skipped; 5436 tests passed, 12 skipped |
| Desktop lint and typecheck | PASS: 0 errors |
| Root TypeScript build | PASS |
| Desktop web and sidecar builds | PASS: web transformed 1751 modules |
| Tauri debug build without bundle | PASS: `nexus-shell.exe` built with asset protocol enabled |
| Docs cleanup audit | PASS: no scratch docs or move proposed |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Real SANA and LTX generation was not executed on a supported GPU | P1 | Retained as DF-4; automated failure paths are supported, live generation is not proven here |
| A packaged release build did not play a generated MP4 | P1 | Added to DF-2 packaged acceptance; debug compilation proves configuration, not packaged playback |
| Response transport is batched rather than incremental | P2 | Kept for this cycle because no streaming event protocol exists; the pending orb remains visible until completion |

---

## 5. Plan Discrepancies

None. The verification addendum correctly identified both backend error-propagation bugs, the absent asset conversion, and the existing hero orb capability.

---

## 6. Assumptions Made

- **Optional workflow metadata must not block playable video**: MP4 display is the completion contract; metadata embedding remains best-effort.
- **Media element failure is terminal for display state**: A decoded or playable asset is required before recall actions can remain visible.
- **The narrow Tauri asset scope is sufficient**: Generated videos are written beneath `~/.nexus/outputs/videos/`; no broader filesystem grant is needed.

---

## 7. Testing Summary

### Automated Tests

- Desktop full Vitest: 1334 passed, 0 failed across 154 files in one solo process.
- Root full Vitest: 5436 passed, 12 skipped, 0 failed across 515 passing files and 3 skipped files.
- Desktop lint, desktop typecheck, root build, web build, sidecar build, and Tauri debug build all exited zero.

### Manual Testing Performed

- Diff inspection confirmed media-only width scoping, no empty output-cache writes, the video-only asset scope, and no workflow gate around successful MP4 completion.

### Manual Testing Still Needed

- [ ] Run real SANA and LTX generations on a supported GPU.
- [ ] Play the generated MP4 in a packaged Windows build launched from Explorer.

---

## 8. TODO Tracker

### Completed This Session

- [x] Add captioned hero pending states without widening plain chat.
- [x] Propagate queue and empty-output failures as written error events.
- [x] Hide actions when image or video media cannot be displayed.
- [x] Resolve MP4 paths through a narrowly scoped Tauri asset protocol.

### Remaining

- [ ] Implement v2.2.3 Phases 4, 5, 6, and 8.

### Out of Scope

- [ ] Live-GPU and packaged playback acceptance.
- [ ] Incremental token or generation-progress transport.

---

## 9. Summary and Next Steps

Phase 3 closes the Studio's silent terminal states and unplayable filesystem URLs while giving pending media a clear hero activity state. The next step is to bank this phase atomically, then implement Phase 4 durable chat memory while preserving the current batched transport contract.
