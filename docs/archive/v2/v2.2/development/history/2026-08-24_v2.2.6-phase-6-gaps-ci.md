# Development Log: v2.2.6 Phase 6 - Architecture, known-gaps, and CI

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Reconcile architecture, known gaps, and CI after Phases 1-5. Do not bump package.json. Then commit, push develop, rebuild the unsigned Windows installer, and stop before `/update release`.
**Outcome**: No file moves. Chatbot stays on `chat.explorer.*`. Unused Image/Video `activeSessionId` state dropped (ref remains). Broken desktop test import of `StudioTurn` repaired. BG-51 through BG-54 recorded with evidence. DF-2, DF-4, DF-25, DF-26, DF-27 stay open. CI comments name the v2.2.6 persistence contracts. Installer/runtime.json silent no-op.

---

## 1. Starting State

- **Branch**: `develop` (Phase 5 commit `fc0ba55`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.6-session-memory-and-studio-history.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-24_v2.2.6-phase-5-chatbot-history.md`

Phases 1-5 were committed locally. Remaining work was Phase 6 reconciliation, unsigned installer rebuild, push, then `/update release` behind confirmation.

---

## 2. Chronological Steps

### 2.1 Architecture refactor (6.1)

**Plan specification**: Identify obsolete files from the shared explorer extract. Propose-then-apply. Repair every reference. Do not move Chatbot off `chat.explorer.*` without a migration.

**What happened**: `studioAsChatExplorer.ts` is load-bearing (StudioHistoryPane). Chatbot stays on `chat.explorer.*`. No empty dirs or duplicate stores to collapse. Layout debt from Phases 2-3: Image Studio and Video Lab kept React state `activeSessionId` whose setter ran but whose value was unread (pages already use `activeSessionIdRef`). Dropped the unused state; kept the ref. `desktop/tests/studioSessionMemory.test.ts` imported `StudioTurn` via `../../../core/...` (one directory too high). Fixed to `../../core/generations/StudioSessionStore.types` so desktop `tsc --noEmit` can resolve it. Types stay in `core/generations/StudioSessionStore.types.ts` so the Vite bundle does not pull `better-sqlite3`.

**Key files changed**: `desktop/src/modules/image/ImageStudioPage.tsx`, `desktop/src/modules/video/VideoLabPage.tsx`, `desktop/tests/studioSessionMemory.test.ts`

### 2.2 Known-gaps (6.2)

**Plan specification**: Update `docs/archive/v2/v2.2/known-gaps.md`. Close session-memory rows only with evidence. Keep DF-2 / DF-4 unless newly proven. Packaged quit/reopen is DF until a packaged soak.

**What happened**: Added a v2.2.6 section. BG-51 (Image named history / last PNG), BG-52 (Video named history / in-session continueFrom), BG-53 (Agents resume hydrate + rename/delete), BG-54 (Chatbot remount + not-saved proof) closed at unit evidence. DF-2 and DF-4 remain. New DFs: DF-25 packaged persist, DF-26 Video remount cannot restore `continueFrom`, DF-27 Agents tool-call cards not persisted.

**Key files changed**: `docs/archive/v2/v2.2/known-gaps.md`, `README.md` (What's new for v2.2.6 so `/update release` is not a version-only README bump)

### 2.3 CI (6.3)

**Plan specification**: Cover studio session IPC, implicit img2img, resume hydration. No live GPU in PR CI. Path filters and concurrency cancel-in-progress.

**What happened**: `ci.yml` already runs root vitest (StudioSessionStore sqlite) and `npm run test:shell` on develop Node 22 (ImageStudioPage, VideoLabPage, CodingPage, ChatPage.persistence, studioSessionMemory). `concurrency.cancel-in-progress` already present. Comments now name the v2.2.6 contracts. Did not add a GPU job or a Hub clone. `shell-build.yml` already typechecks `desktop/**` + `core/**`; no path-filter change required.

**Key files changed**: `.github/workflows/ci.yml`, `.github/workflows/installer-tests.yml` (comment only)

### 2.4 Cross-installer parity (6.4)

**Plan specification**: Do not change installers unless the session DB path needs documenting in `runtime.json`. If no installer files changed, record a silent no-op.

**What happened**: Silent no-op. Chat DB remains `~/.nexus/chat/explorer.db`. Studio sessions DB is `~/.nexus/generations/sessions.db` next to existing `studio.db`. Coding sessions remain `sessions.json` under nexus home. None of those paths belong in the Node/diffusion `runtime.json` contract.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| desktop typecheck (`npx tsc --noEmit`) | PASS |
| desktop persistence suites (11 files) | PASS 172/172 |
| root Vitest: StudioSessionStore + studio-session + ChatExplorerStore phase5 + chat-explorer-phase5 + ChatExplorerStore | PASS 84/84 |
| Layout moves | none |
| Chatbot moved off `chat.explorer.*` | no |
| Package version bump | Not done (plan lock; `/update release` owns it) |
| Packaged Explorer / GPU generate / packaged persist | not_observed (DF-2, DF-4, DF-25) |
| Installer / runtime.json | silent no-op |

---

## 4. Decisions

- Did not invent a selected-session highlight prop on StudioHistoryPane. The unused React state was layout debt, not a missing highlight contract.
- Did not persist Agents tool cards or Video `priorJobId` in this phase; those are named DFs.
- README What's new for v2.2.6 is written now so `/update release` does not ship a version-only README bump.

---

## 5. Residual risk

- Operator field checklist in the plan (Chatbot quit/reopen, Image last-PNG follow-up, Video when weights exist, Agents resume) is still required before calling the product done.
- `/update release` must not silently bump `package.json` 2.2.5 or `desktop/package.json` 1.5.0 without the operator confirming the tag.
- Unsigned Windows installer rebuild is after this commit; SmartScreen unsigned warning is unchanged.

---

## 6. Testing Summary

### Automated Tests

desktop `tsc --noEmit` clean. desktop vitest 172 passed (studioSessionMemory, ImageStudioPage, VideoLabPage, CodingPage, coding-sessionManager, coding-session-resume, coding-protocol, panels, sidecar-handlers, ChatPage.persistence, ChatPage). Root vitest with `configs/vitest.config.ts`: 84 passed (StudioSessionStore, studio-session IPC, ChatExplorerStore phase5, chat-explorer-phase5, ChatExplorerStore).
