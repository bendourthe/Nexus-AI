# Development Log: v2.2.8 Phase 4 - Models Catalog Parity

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Make Settings Models the same list as the installer Models tab (sort, family collapse, hideBelowVram, compact density, downloaded highlight, over-budget grayed last) and feed QuickModelSwitcher from that order.
**Outcome**: A shared golden fixture dual-asserts installer `collapse_and_sort` and desktop `visibleModelsOnTab`. Settings cards are compact with a highlighted Downloaded chip. Description copy sits behind a closed Details disclosure. Pickers follow the same Chat / Agentic / Image / Video order.

---

## 1. Starting State

- **Branch**: `develop` (v2.2.8 Phase 3 commit `d4c3a42`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.8-working-local-studio.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-24_v2.2.8-phase-3-rail-margin-and-thinking-orbs.md`

Settings listed every catalog sibling, led with a description wall, and muted Downloaded. The installer already collapsed families and hid `hideBelowVramGB`.

---

## 2. Chronological Steps

### 2.1 Shared sort and compact cards (T009, T010)

**Plan specification**: One sort/filter contract for `typed_catalog.py` and `catalogTabs.ts`. Compact Settings rows. Downloaded is obvious. Over-budget grayed last, never Compatible. Pickers use that order. Golden fixture shared or dual-asserted. Context chip stays `<val>k`.

**What happened**: Extracted `nexus_installer.catalog_tab_sort.collapse_and_sort` and ported it to `visibleModelsOnTab` / `collapseAndSortModels`. `hideBelowVramGB` is marshaled on ListedModel. Installed rows are not hidden by the VRAM floor. Family collapse keeps downloaded siblings. Settings cards use `data-compact`, a status-ok Downloaded chip, gray over-budget rows, and a closed Details disclosure for description / Best for / Why. QuickModelSwitcher filters the same ordered tab list; Agents passes `catalogTab="agentic"`.

**Key files changed**: `desktop/src/shared/models/catalogTabs.ts`, `desktop/src/pages/settings/ModelsSettings.tsx`, `desktop/src/shared/models/QuickModelSwitcher.tsx`, `scripts/installer/src/nexus_installer/catalog_tab_sort.py`, `scripts/installer/src/nexus_installer/pages/typed_catalog.py`, `core/registry/NexusModelRegistry.ts`, `tests/fixtures/v2.2.8-catalog-tab-sort.json`

**Verification**: Desktop catalog/settings/switcher tests 36/36. Installer `test_catalog_tab_sort` plus `test_typed_catalog` 71 passed. Golden chat ids omit collapsed `gemma-e2b` and hidden `kimi-hidden`.

### 2.2 Testing and Stabilization

ModelsSettings, catalogTabs, installer typed_catalog, QuickModelSwitcher. CI comments on `test:shell` and installer-tests path filter. Session history this file. No live GPU.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| desktop vitest (catalogTabs, ModelsSettings, QuickModelSwitcher) | PASS 36/36 |
| desktop `tsc --noEmit` | PASS |
| desktop eslint on touched files | PASS |
| installer pytest (catalog_tab_sort + typed_catalog) | PASS 71 |
| installer ruff on touched Python | PASS |
| Golden chat includes kimi-hidden | No |
| Compact `data-compact` | Yes |
| Downloaded `data-downloaded` | Yes |
| Context chip format | Unchanged `<val>k` |
| package.json bump | Not done |
| Hub injection scanner | Untouched |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Packaged Settings vs installer not screenshotted | P2 | DF-34 |
| Agents folders still overlay-only | P3 | DF-33 |
| Packaged Hi still unproven | P2 | DF-32 |

---

## 5. Plan Discrepancies

- Installer required-ness stays `nomic-embed-text` (`is_required`). Settings still treats every embed type as Required for sort rank. The golden fixture uses an explicit `required` tag so both engines agree.
- QuickModelSwitcher still lists only installed-and-ready ids; order is the Settings tab order minus not-installed rows.

---

## 6. Assumptions Made

- `hostVramGB === 0` maps to installer `gpu_vendor == "none"`.
- A closed Details disclosure is compact enough without deleting catalog copy.

---

## 7. Testing Summary

Automated only. Human QA is deferred to the last phase.

- Shared golden fixture (chat + image)
- Family collapse hides the smaller fitting sibling
- hideBelowVram hides catalog-only rows, not downloaded ones
- Compact cards, Downloaded highlight, over-budget gray
- Picker order matches Chat-tab sort

---

## 8. TODO Tracker

| Item | Status |
|---|---|
| T009 Shared sort / compact cards | Done |
| T010 Picker order | Done |
| Phase 5 Hub latest apply | Next |

---

## 9. Summary and Next Steps

Phase 4 makes Settings Models and the installer Models tab share one collapse/sort contract, with compact downloaded-obvious cards and matching pickers. Next: Phase 5 (Hub latest apply with per-skill quarantine, scanner on).
