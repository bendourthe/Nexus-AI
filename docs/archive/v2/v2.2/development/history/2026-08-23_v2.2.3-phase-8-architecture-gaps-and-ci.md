# Development Log: v2.2.3 Phase 8 - Architecture, gaps, and CI

**Date**: 2026-08-23
**Operator**: Project owner
**Objective**: Reconcile end-of-cycle architecture, known gaps, installer parity, and CI coverage without widening product behavior or performing unapproved repository moves.
**Outcome**: Exact structural duplication is consolidated, shell gates cover develop, installer support is declared honestly per platform, the repository layout is fully triaged, and v2.2.3 is ready to hand off to `/update release` when requested.

---

## 1. Starting State

- **Branch**: `develop`
- **Starting commit**: `8af593a`
- **Environment**: Windows, React 19 renderer, Node sidecar, Tauri 2, Vitest 2.1.9, PyQt5 installer
- **Prior session reference**: `docs/archive/v2/v2.2/development/history/2026-08-23_v2.2.3-phase-6-agentic-workspace-and-hub-harness.md`
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.3-glass-orbs-and-pillar-runtime.md`

Phases 1, 2, 3, 4, 5, 6, and 7 were committed before the Phase 8 architecture and release-gate changes began.

---

## 2. Chronological Steps

### 2.1 Approved repository and docs audit

**Plan specification**: Use the project and docs refactor audits, propose changes before applying them, repair references for any moves, and avoid drive-by cleanup.

**What happened**: The audit found no obsolete tracked project file, duplicate document, or safe archive move for this patch. Eight project duplicate groups were intentional fixtures, sentinels, or packaged assets; six empty directories were generated or sentinel paths. The approved proposal made no move or deletion.

**Key files changed**: `docs/archive/v2/v2.2/docs-cleanup-report.md`

### 2.2 Scoped architecture consolidation

**Plan specification**: Evaluate the duplicate composer styles, dual Chat Explorer client types, and Sidebar's duplicate module list without changing behavior.

**What happened**: Chat Explorer now has one mode-parameterized client contract with sync and async aliases. The two composers share only byte-identical structural style objects. Sidebar routes and labels derive from `moduleList`; its local presentation map retains icons and shortcuts only.

**Key files changed**: `desktop/src/modules/chat/chatExplorerClient.ts`, `desktop/src/shared/chat/composerSurfaceStyles.ts`, `desktop/src/components/Sidebar.tsx`

### 2.3 Shell CI coverage on develop

**Plan specification**: Close or document the shell workflow blind spot on the integration branch while preserving CI cost controls.

**What happened**: `shell-build.yml` now triggers for both main and develop pushes. Develop and pull requests run the Ubuntu job, while main pushes and manual dispatch retain the complete operating-system matrix. Workflow tests pin the branch and cost conditions.

**Key files changed**: `.github/workflows/shell-build.yml`, `tests/unit/workflow-discipline.test.ts`

### 2.4 Declarative installer parity

**Plan specification**: Name distributed artifacts, platforms, shared capabilities, target-specific counterparts, and external fallbacks; do not claim Windows-only flags on Unix.

**What happened**: `platform-contracts.json` names all three installers and their build/workflow paths, records shared registry and runtime contracts, marks taskbar transparency and `CREATE_NO_WINDOW` not applicable on Unix, and exposes the unstaged macOS/Linux desktop payload as DF-18. Tests cross-check the declaration against scripts and workflow artifact paths.

**Key files changed**: `scripts/installer/build/platform-contracts.json`, `scripts/installer/tests/test_packaging.py`

### 2.5 Known-gaps reconciliation

**Plan specification**: Close DF-9 only after whole-cycle occupancy acceptance, carry unproven packaged legs, and name any Hub hook behavior that did not land without VS Code.

**What happened**: DF-9 is resolved after the 91-test final occupancy gate. DF-22 now records that arbitrary workspace Hub hook scripts are not executed by the vscode-free sidecar. DF-18 reflects the parity contract's Unix payload finding, WN-2 records existing DOM-harness warnings, and BG-32 records the closed develop-shell CI bug.

**Key files changed**: `docs/archive/v2/v2.2/known-gaps.md`

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Focused architecture refactor tests | PASS: 6 files, 85 tests, 0 failures |
| Final occupancy acceptance | PASS: 7 files, 91 tests, 0 failures |
| Solo desktop full suite | PASS: 157 files, 1369 tests, 0 failures |
| Root full suite | PASS: 516 files passed, 3 skipped; 5441 tests passed, 12 skipped |
| Installer full suite | PASS: 1104 passed, 3 skipped, 0 failures |
| Installer packaging tests | PASS: 32 tests, 0 failures |
| Workflow-discipline tests | PASS: 8 tests, 0 failures |
| Rust shell | PASS: check, Clippy with warnings denied, 15 tests |
| Desktop production builds | PASS: sidecar and web |
| Root lint and TypeScript build | PASS |
| Architecture gate | PASS: 0 errors, 16 pre-existing warnings |
| Docs layout and diff hygiene | PASS: canonical layout, clean diff check, ASCII-only doc additions |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Packaged Windows clean-VM and Explorer-launch acceptance was not run | P1 | Retained as DF-2; automated shell and route evidence does not replace packaged acceptance |
| Live-GPU image and video generation was not run | P1 | Retained as DF-4 |
| macOS and Linux installers do not stage the desktop sidecar payload | P1 | Retained as DF-18 and declared in the parity contract |
| Workspace Hub hook scripts are not executed by the desktop sidecar | P2 | Added as DF-22; the internal HookBus lifecycle remains supported |
| Desktop DOM tests emit existing React and jsdom warnings | P3 | Added as WN-2; all assertions pass |

---

## 5. Plan Discrepancies

The cross-installer prompt asked for real installers to run on their target operating systems. This Windows session could inspect and test all three build contracts but could not execute macOS or Linux artifacts. The parity declaration therefore records their desktop payload as not staged and keeps target acceptance open instead of claiming equivalent behavior.

---

## 6. Assumptions Made

- **Exact duplication defines the safe composer boundary**: shared style objects move; differing interaction markup stays local.
- **Develop needs early shell coverage at bounded cost**: Ubuntu catches TypeScript sidecar and Rust regressions; full OS packaging remains a main/manual responsibility.
- **N/A is not equivalent proof**: Windows taskbar and process-creation features are explicitly not applicable on Unix.
- **No structural churn is a valid audit result**: fully triaged intentional files and directories do not need to move merely to satisfy a cleanup phase.

---

## 7. Testing Summary

### Automated Tests

- Desktop full Vitest: 1369 passed, 0 failed across 157 files in one solo process.
- Root full Vitest: 5441 passed, 12 skipped, 0 failed across 516 passing files and 3 skipped files.
- Installer pytest: 1104 passed, 3 skipped, 0 failed across 1107 collected cases.
- Rust shell: `cargo check --all-targets`, `cargo clippy --all-targets -- -D warnings`, and `cargo test --all-targets` passed; 15 tests ran.
- Focused architecture, occupancy, packaging, and workflow gates passed 85, 91, 32, and 8 tests respectively.
- Desktop sidecar and web builds, root lint, root TypeScript build, architecture, and docs-layout gates exited zero.

### Manual Testing Performed

- Inspected repository and documentation inventories before approval; no move or delete was applied.
- Inspected the installer build scripts and upload workflows against the new parity contract.

### Manual Testing Still Needed

- [ ] Run the packaged Windows clean-VM and Explorer-launch acceptance in DF-2.
- [ ] Run live SANA and LTX generation plus packaged playback in DF-4.
- [ ] Stage and run the macOS and Linux desktop payloads in DF-18.

---

## 8. TODO Tracker

### Completed This Session

- [x] Consolidate the scoped Chat Explorer, composer-style, and Sidebar duplication.
- [x] Extend shell validation to develop with bounded matrix cost.
- [x] Add and test the cross-installer parity contract.
- [x] Reconcile v2.2.3 known gaps and close DF-9.
- [x] Complete the repository and docs audit without unapproved moves.

### Remaining

- [ ] Run `/update release` only when the operator asks to bump the shipping version.

### Out of Scope

- [ ] Package-version changes, tags, pushes, or release publication.
- [ ] Clean-VM, live-GPU, macOS, or Linux runtime acceptance.
- [ ] Native file dialogs and vscode-free workspace hook script execution.

---

## 9. Summary and Next Steps

Phase 8 closes the end-of-cycle structural and CI work without broadening product behavior or overstating platform parity. v2.2.3 implementation is complete on develop; the next authorized step is the `/update release` handoff, with no package bump, tag, or push performed here.
