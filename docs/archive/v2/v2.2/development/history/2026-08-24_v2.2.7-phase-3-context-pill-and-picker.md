# Development Log: v2.2.7 Phase 3 - Context pill, picker row, and 80% CTA

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Shared pill Context meter under all four composers with the model picker to its right. At 80% suggest a new session without wiping the current transcript. Remove the Chatbot header `/` breadcrumb and unicode gear. Hide the bar when neither `contextWindow` nor `visualTokenBudget` exists.
**Outcome**: Chatbot, Agents, Images, and Videos all render `ComposerContextRow` under the typing area. The bar is a progressbar (`Context` / `N%`) or an honest hidden state. 79% has no CTA; 80% creates or selects a new session and leaves the old one in history. Persona is a text control in the Chatbot footer.

---

## 1. Starting State

- **Branch**: `develop` (v2.2.7 Phase 2 commit `a71f531`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.7-context-meter-and-transcript-chrome.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-24_v2.2.7-phase-2-session-token-accounting.md`

Phase 2 usage persist and `sessionContextUsage` are on `develop`. Pickers still lived in page headers. ChatPage still rendered `Breadcrumb` (slash at root) and a gear persona toggle.

Model-routing pre-flight: Cursor has no scriptable switch. Plan recommended frontier/high. Session model is Cursor Grok 4.6 (no downshift).

---

## 2. Chronological Steps

### 2.1 Shared meter + picker row (T005)

**Plan specification**: Pill `ContextUsageBar` under ChatPage, CodingPage, ImageStudioPage, VideoLabPage. `QuickModelSwitcher` immediately to the right. Wire `sessionContextUsage`. At >= 80%, suggest starting a new session. Do not auto-clear history. Peeking another tab must not load weights. Remove the root-only `/` breadcrumb and unicode gear; keep persona reachable.

**What happened**: Added `ContextUsageBar`, `ComposerContextRow`, and `composerSessionUsage`. Display percent uses `Math.floor(percent + 1e-9)` so float 79.9 does not paint as 80 before `atOrAbove80`. Moved Chat/Agents pickers out of the header. Images/Videos now use `QuickModelSwitcher` with the existing `image-model-select` / `video-model-select` testids (installed-and-ready filter, same as Chat/Agents). Chatbot CTA calls `createChat` in the same folder and selects it; Agents CTA reuses `handleNewSession` (clears local turns, does not delete the persisted session); Images/Videos create a new studio session and leave the old one in `StudioHistoryPane`. Persona is a footer "Persona" button (`chat-persona-toggle`), not a header gear.

The meter does not call `residency.request`.

**Key files changed**: `desktop/src/shared/chat/ContextUsageBar.tsx`, `ComposerContextRow.tsx`, `usageTurnsFromMessages.ts`, `ChatPage.tsx`, `CodingPage.tsx`, `ImageStudioPage.tsx`, `VideoLabPage.tsx`.

### 2.2 Tests and CI

79% no CTA; 80% CTA once; Chatbot 80% keeps the old tree row and restores the transcript on reselect. Hidden bar when no denominator. Pickers live under `composer-context-row` on all four pages. No `chat-breadcrumb` / `chat-breadcrumb-root` / gear glyph. CI comment names the meter; no new jobs; no live GPU.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| `desktop/tests/ContextUsageBar.test.tsx` | PASS 3/3 |
| ChatPage, chatExplorerAdapter, ChatPage.persistence | PASS |
| CodingPage, ImageStudioPage, VideoLabPage | PASS |
| `npx tsc --noEmit -p desktop/tsconfig.json` | PASS |
| `npm run lint --workspace @nexus/desktop` | PASS |

---

## 4. Deviations from Plan

- **DEVIATION**: Persona lives as a labeled footer control, not on the active chat row or in Advanced. Same testid; no unicode glyph.
- **DEVIATION**: Images/Videos previously used header `ModelSelector`. They now use `QuickModelSwitcher` so all four tabs share the installed-and-ready picker. Testids are unchanged.

---

## 5. Known Gaps

- **DF-30**: Packaged four-tab meter and 80% CTA not_observed.
- Keep DF-28, DF-29, WN-7, DF-2, DF-4.

---

## 6. Decisions and Rationale

- Hide the bar (not the picker) when `percent` is null / `denominatorKind` is `none`. Image/video tests with `visualTokenBudget` prove the analog path; NO_MODELS hides the bar.
- 80% CTA is a persistent inline notice while `atOrAbove80`, not a toast on every keystroke.
- New session must not wipe `messagesByChat` / studio history rows for the previous id.

---

## 7. Testing Summary

### Automated Tests

- ContextUsageBar 3 passed
- ChatPage 18 passed (including 80% CTA keep-old-chat)
- chatExplorerAdapter, ChatPage.persistence passed
- CodingPage 24, ImageStudioPage 22, VideoLabPage 19 passed
- Desktop tsc and eslint passed

### Manual Testing Still Needed

Human QA is deferred to the last phase (packaged four-tab meter, 80% CTA, peek-does-not-load-weights).

---

## 8. TODO Tracker

- [x] T005 pill + picker under all four composers
- [x] Remove Chat header `/` and gear
- [x] 80% CTA does not delete the old session
- [x] Phase 3 tests and session history
- [ ] Phase 4 date / time / token chrome on all four modes

---

## 9. Summary and Next Steps

Phase 3 is done. Implement Phase 4 next: once-per-day date heading, discrete local time on every bubble, tokens next to time (user input; assistant reasoning+output or em dash). Fake timers. All four modes.
