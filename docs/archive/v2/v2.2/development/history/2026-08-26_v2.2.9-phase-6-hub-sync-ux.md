# Session History - v2.2.9 Phase 6: Hub Sync UX and Quarantine Copy

**Date**: 2026-08-26
**Plan**: [v2.2.9-field-chrome-catalog-and-generate.md](../../plans/v2.2.9-field-chrome-catalog-and-generate.md) (Phase 6)
**Driver**: `/implement in-full` (Phase 6 of 7)

## What was implemented

- **6.1 (T012)**: New `desktop/src/lib/hubTags.ts` (`normalizeHubTag` strips one leading `v`/`V` and compares case-insensitively; `hubTagsEqual` is null-safe; `displayHubTag` renders one canonical form). Both compare sites in `SkillsSettings.tsx` (the header "Upstream latest" line and the `skills-update-available` banner) now use it, so Active `3.21.0` vs upstream `v3.21.0` shows neither a banner nor an implied update; truly different tags still show the banner. The Sync now + auto-update controls row moved to the top of Skills, directly under Active/Upstream. While syncing, a small reused `AgentStateOrb` (inline preset, stable accessible name "Syncing Nexus-Hub") renders next to staged status copy derived only from observables: "New version found" (upstream already known newer at click), "Installing version X.Y.Z now" while the apply RPC runs, "Harness up-to-date with Nexus-Hub version X.Y.Z" only from the RPC result, "Sync blocked" plus the existing summary on failure. `NexusHubSyncer.sync()` is a single RPC with no progress events, so no fabricated progress: with no known-newer upstream the label is "Checking for updates...".
- **6.2**: Quarantined section explains that a quarantined skill is not enabled because the injection scanner found a high-severity pattern and that Review-and-approve is an explicit trust decision; the finding text stays on the row. Narrow waiver GRANTED after reading the actual quarantined v3.21.0 file (`~/.nexus-ai/catalog/quarantine/workflow/cross-project-comparison/SKILL.md`, line 73): the flagged "ignore previous instructions" text is Step 1.5 "Source Security Scan" naming payload styles the agent must scan external sources FOR (defensive teaching, same class as the existing `prompt-injection-defense` waiver), not an operational instruction. One `(workflow/cross-project-comparison/SKILL.md, injection.jailbreak.ignore-previous)` waiver added with that reason; `HUB_ALLOWLIST_REVIEWED_TAG` bumped `v3.20.1` -> `v3.21.0`; a new test proves a different high-severity pattern in the same file still blocks. `PromptInjectionScanner` remains fully on.

## Files changed

`desktop/src/lib/hubTags.ts` (new), `desktop/src/pages/settings/SkillsSettings.tsx`, `core/skills/hubSkillScanAllowlist.ts`; tests `desktop/tests/hubTags.test.ts` (new), `desktop/tests/SkillsSettings.test.tsx`, `tests/unit/core/skills/PromptInjectionScanner.test.ts`.

## Test results

- `npm run lint:shell`: 0 errors.
- `npm run test:shell`: 182 files / 1603 tests passed.
- `npm test` (root, core/skills touched): 522 files / 5488 tests passed, 12 skipped. One earlier concurrent run hit a single load-induced perf-assertion flake (p99 threshold) in an unrelated file; the clean solo run passed everything (ENV class).

## Deviations

None. The orchestrator independently re-verified the allowlist waiver (narrow one-pair entry, documented reason, reviewed-tag bump, flagged line quoted as teaching content).

## Next steps

Phase 7 (final): architecture refactor, known-gaps reconciliation (DF-32/DF-35 close; DF-25/28/29/30/31 review), living docs, git-tree hygiene, CI/CD coverage, Goal-vs-codebase review, last-phase evidence file, installer rebuild.
