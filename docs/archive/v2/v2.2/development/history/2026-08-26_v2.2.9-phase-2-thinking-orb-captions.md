# Session History - v2.2.9 Phase 2: Thinking-Orb Captions

**Date**: 2026-08-26
**Plan**: [v2.2.9-field-chrome-catalog-and-generate.md](../../plans/v2.2.9-field-chrome-catalog-and-generate.md) (Phase 2)
**Driver**: `/implement in-full` (Phase 2 of 7)

## What was implemented

- **2.1 (T006)**: New `desktop/src/components/agentState/captionRotator.ts` with the fixed caption list `Thinking... / Searching... / Working... / Solving...`, a 2400 ms rotate interval, a pure Fisher-Yates shuffle applied once per pending bubble, a caption-to-orb-motion mapping (so word and particle grammar agree), and a `usePendingCaptionRotator` hook.
- `AgentStateOrb` gained `rotateCaptions` and `accessibleName` props. In rotate mode it renders as a dark pill (999px radius, `--bg-0` / `--border-1` tokens, existing accent glow) with the rotating caption; non-rotating orbs (inline status, hero) are behavior-identical to before.
- `MessageBubble` non-studio pending path (48px `bubble` preset: Chatbot and Agents) passes `rotateCaptions` plus `accessibleName="Generating reply"`. Studio pending stays `hero` with the static caption, unchanged size. Completed replies still replace the pending pill (Phase 1 contract).
- Accessibility: the rotating caption is `aria-hidden` with `aria-live="off"`; the pending orb exposes one stable accessible name (`Generating reply`). `prefers-reduced-motion` holds a static frame with the first fixed caption and no interval.
- Dependency guard: a test asserts `thinking-orbs` is absent from `desktop/package.json` and the workspace lockfile (the reference playground was reverse-engineered, not installed).

## Files changed

Source: `captionRotator.ts` (new), `AgentStateOrb.tsx`, `agentState/index.ts`, `MessageBubble.tsx`.
Tests: new `pendingCaptionRotator.test.tsx` (12 tests); updated `mediaMessageBubble.test.tsx`, `ChatPage.test.tsx`, `CodingPage.test.tsx` to the new accessible-name/caption contract.

## Test results

- `npm run lint:shell`: 0 errors.
- `npm run test:shell`: 179 files / 1564 tests, all passed (agent final run; orchestrator re-run confirmed).

## Deviations

None. The Agents (coding) pillar pending orb shares the rotating pill because it mounts through the same non-studio bubble path - this is the plan's intent (Chat/Agents pending) rather than a deviation.

## Next steps

Phase 3 (studio history identity and visual context): remove empty Image/Video headers, Chatbot FolderTree copy, `visualTokenBudget` on studio catalog rows, visual Context bar.
