# Development Log: v2.2.6 Phase 1 - Shared session explorer contract

**Date**: 2026-08-23
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Images and Videos own named sessions in FolderTree without forking Chatbot's SQLite chat schema. Persist createdAt on turns. Do not bump package.json. Do not disable the Hub injection scanner. Context meters, date separators, bubble clocks, and token counts stay v2.2.7.
**Outcome**: StudioSessionStore plus studio.session.* IPC land in ~/.nexus/generations/sessions.db. Image Studio and Video Lab render a left history pane by adapting FolderTree. Chatbot chat.explorer.* is unchanged. Turns persist createdAt only. Phase 1 stability gate passed.

---

## 1. Starting State

- **Branch**: `develop` (up to date with origin/develop; v2.2.5 merged)
- **Starting tag/commit**: `739d22f` (Merge branch main back into develop after v2.2.5)
- **Environment**: Windows 10, Node 20+, Vitest 2.1.9, better-sqlite3 in the root suite
- **Prior session reference**: `docs/archive/v2/v2.2/development/history/2026-08-23_v2.2.5-phase-6-gaps-ci.md`
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.6-session-memory-and-studio-history.md`

Context: Image Studio and Video Lab kept messages in React state. Chatbot already had FolderTree on chat.explorer.*. Phase 1 is the shared store, IPC, and pane only. Implicit last-output follow-up and remount hydrate are Phase 2 / Phase 3.

---

## 2. Chronological Steps

### 2.1 Studio session records (1.1)

**Branch**: `develop` | **PR**: none | **Merged to**: n/a

**Plan specification**: Sidecar-backed folders, named sessions, turns with createdAt, lastOutputRef as a path not a blob. Mirror chat explorer IPC under studio.session.*. Missing pillar rejects create. Corrupt extra JSON skips that row. Delete wins over rename.

**What happened**: Added `core/generations/StudioSessionStore.ts` (better-sqlite3, sessions.db under the generations directory). Pillars are `image` and `video`. `appendTurn` rejects `data:` media refs. `unsafeSetSessionExtraJson` is a test seam for corrupt-row skip. Sidecar `sessionRuntime.ts` exposes ops; handlers register `studio.session.tree|createFolder|renameFolder|moveFolder|deleteFolder|createSession|renameSession|moveSession|deleteSession|appendTurn|listTurns`. Chatbot keeps `chat.explorer.*`. `core/**` does not import `modules/**`.

**Key files changed**: `core/generations/StudioSessionStore.ts`, `core/generations/StudioSessionStore.types.ts`, `core/generations/paths.ts`, `core/generations/index.ts`, `desktop/sidecar/src/studio/sessionRuntime.ts`, `desktop/sidecar/src/handlers.ts`, `desktop/sidecar/src/protocol.ts`

**Troubleshooting**: None in 1.1.

**Verification**: Root `StudioSessionStore.test.ts` and `studio-session.test.ts` (see gate).

### 2.2 Shared FolderTree wiring (1.2)

**Branch**: `develop` | **PR**: none | **Merged to**: n/a

**Plan specification**: Image/Video left aside reuses FolderTree. Sidecar down: empty tree plus existing SidecarDownBanner, no fake sessions. Do not import Chat types into Image/Video pages. Rename empty string still rejects.

**What happened**: FolderTree gained optional `copy` and `storageKey`; Chatbot defaults stay "New chat" / "Start a new chat". `StudioHistoryPane` adapts `StudioExplorerClient` to `ChatExplorerClient` internally. ImageStudioPage and VideoLabPage import only the pane (plus `tauriAvailable` from the existing chat IPC helper). Production uses IPC when Tauri is present and sidecar is up; otherwise an empty in-memory client. Sidecar-down skips FolderTree and shows the empty hint.

**Key files changed**: `desktop/src/modules/chat/FolderTree.tsx`, `desktop/src/shared/explorer/*`, `desktop/src/modules/image/ImageStudioPage.tsx`, `desktop/src/modules/video/VideoLabPage.tsx`

**Troubleshooting**: None in 1.2 (empty-tree bug showed up in 1.3).

**Verification**: Desktop FolderTree default-copy test plus sidecar-down empty hint.

### 2.3 Testing and stabilization (1.3)

**Branch**: `develop` | **PR**: none | **Merged to**: n/a

**Plan specification**: Create an image session, append user + assistant media ref, list after a new store instance. Chat explorer regressions.

**What happened**: Root tests cover two-instance round-trip, pillar isolation, empty rename, delete-then-rename, corrupt JSON skip, blob reject, sidecar ops. Desktop tests cover history pane present, injected session title, sidecar-down empty hint, FolderTree default copy unchanged.

**Troubleshooting**:
- **Problem**: Desktop history pane tests rendered `folder-tree-empty` ("Start a new session") even after `InMemoryStudioExplorerClient.createSession(...)`.
- **Attempted**: Confirmed `mapTree` already maps `sessions` to `chats`. Sidecar-down path was already correct (no FolderTree).
- **Root cause**: `studioClientAsChatExplorer` marked every method `async` and `await Promise.resolve(...)`, so FolderTree `resolveMaybe` took the Promise path. Initial state is EMPTY_TREE; tests asserted on the first paint.
- **Resolution**: Adapter methods stay synchronous when the inner client is synchronous (`wrap` only then-ables). IPC clients still return Promises.
- **Problem**: `StudioSessionStore` two-instance test failed on Windows `rmSync` EPERM because the second store was still open in `finally`.
- **Resolution**: Close `second` before `rmSync`. Swallow leftover EPERM on Windows WAL handles.

**Verification**: Commands and counts in section 3.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Root Vitest: StudioSessionStore + studio-session + ChatExplorerStore phase5 + chat-explorer-phase5 + ChatExplorerStore | PASS 84/84 |
| Desktop Vitest: FolderTree + ImageStudioPage + VideoLabPage + sidecar-handlers + StudioHistoryPane | PASS 88/88 |
| Stability gate: image session, user + assistant media ref, list after new store instance | PASS |
| Chat explorer unchanged (phase5 + ChatExplorerStore + FolderTree default copy) | PASS |
| package.json bump | Not done (plan lock; root stays 2.2.5, desktop 1.5.0) |
| Hub injection scanner | Untouched |
| Context meters / clocks / token counts | Out of scope (v2.2.7); createdAt persisted |
| Lint / full desktop build | NOT RUN (phase tests only, per operator workflow) |

Root command:

`npx vitest run --config configs/vitest.config.ts tests/unit/core/generations/StudioSessionStore.test.ts tests/unit/desktop-sidecar/studio-session.test.ts tests/unit/modules/chat/ChatExplorerStore.phase5.test.ts tests/unit/desktop-sidecar/chat-explorer-phase5.test.ts tests/unit/chat/ChatExplorerStore.test.ts`

Desktop command (cwd desktop/):

`npx vitest run --config vitest.config.ts tests/FolderTree.test.tsx tests/ImageStudioPage.test.tsx tests/VideoLabPage.test.tsx tests/sidecar-handlers.test.ts tests/StudioHistoryPane.test.tsx`

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Image/Video generate turns are not yet appended to StudioSessionStore | P1 | Deferred to Phase 2 / 3 |
| Implicit last-output img2img / continueFrom is not wired | P1 | Deferred to Phase 2 / 3 |
| Remount hydrate of MessageList from listTurns is not wired | P1 | Deferred to Phase 2 / 3 |
| IPC getFolder / getSession are tree walks on the client | P2 | Accepted for Phase 1 |
| Packaged sidecar-down and real sessions.db on disk | P2 | not_observed here |
| ImageStudioPage / VideoLabPage import `tauriAvailable` from chat IPC helper | P2 | Accepted; they do not import Chat types |

---

## 5. Plan Discrepancies

- Plan named ImageStudioPage / VideoLabPage; the codebase uses ImageStudioPage / VideoLabPage. Wired those pages.
- Plan allowed image.explorer.* / video.explorer.*; implementation uses one `studio.session.*` family with a pillar argument.
- Context meters, date separators, bubble clocks, and per-bubble token counts were not added (locked to v2.2.7). createdAt is persisted on turns.

---

## 6. Assumptions Made

- **Separate sessions.db is the right isolation**: Chatbot explorer.db stays the chat schema. Impact if wrong: a later merge would need a migration.
- **FolderTree reuse via adapter is enough**: Image/Video pages must not import Chat types; the adapter lives under `desktop/src/shared/explorer/`.
- **Sidecar down must not invent sessions**: empty hint plus existing banner.
- **createdAt only**: no UI clocks in this phase.

---

## 7. Testing Summary

### Automated Tests

- Root Phase 1 files: 84 passed, 0 failed
- Desktop Phase 1 files: 88 passed, 0 failed
- ImageStudioPage / VideoLabPage still emit pre-existing act(...) and canvas getContext warnings; they did not fail the run

### Manual Testing Performed

- None (unit/jsdom only)

### Manual Testing Still Needed

- [ ] Packaged app: Image/Video history pane with sidecar up and sessions.db on disk
- [ ] Sidecar down: empty hint, no fabricated sessions, existing banner
- [ ] Chatbot FolderTree still reads "Start a new chat" after a live reload

---

## 8. TODO Tracker

### Completed This Session

- [x] 1.1 StudioSessionStore + studio.session.* IPC
- [x] 1.2 FolderTree reuse on Image/Video pages
- [x] 1.3 Phase tests, session history, Phase 1 commit (this file)

### Remaining (Not Started or Partially Done)

- [ ] Phase 2 Images: persist turns, implicit last PNG img2img, remount hydrate
- [ ] Phase 3 Videos: persist turns, last clip / continueFrom if protocol supports it, remount hydrate
- [ ] Phase 4 Agents transcript restore
- [ ] Phase 5 Chatbot remount proof
- [ ] Phase 6 known-gaps, CI persistence without GPU, unsigned Windows installer, push develop

### Out of Scope (Deferred)

- [ ] Context meters, date separators, bubble clocks, per-bubble token counts (v2.2.7)
- [ ] package.json bumps
- [ ] /update release

---

## 9. Summary and Next Steps

Phase 1 stability gate is met: an image session with a user turn and an assistant media path lists back from a new StudioSessionStore instance, and Chatbot explorer tests still pass. Image Studio and Video Lab show named sessions in FolderTree when a client is injected; sidecar-down shows an empty hint and does not fabricate rows.

**Next concrete step**: Implement v2.2.6 Phase 2 (Images history + last-output memory). Do not treat Phase 2 as done until implicit last-output follow-up and remount hydrate are tested.
