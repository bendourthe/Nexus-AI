# Development Log: v2.2.8 Phase 2 - Unified History Chrome

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Chatbot, Agents, Images, and Videos share FolderTree organize/rename/delete. Collapsed history is an icon rail, not an empty 24px gutter. Delete confirm is rounded and quiet.
**Outcome**: Four tabs use the same 280px / 56px history pane. Agents drop `SessionListPanel` as the sole history UI. Confirm-delete keeps `folder-tree-confirm-delete` and is no longer a square flash-red fill.

---

## 1. Starting State

- **Branch**: `develop` (v2.2.8 Phase 1 commit `f672d6d`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.8-working-local-studio.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-24_v2.2.8-phase-1-local-inference-that-finishes.md`

Phase 1 landed minutes-class send/generate timeouts and the Gemma alias probe. Collapsed chats still rendered `null` in a 24px aside. Agents still used a flat session list. Image/Video FolderTree was 240px with no collapse.

---

## 2. Chronological Steps

### 2.1 One FolderTree on four tabs (T004, T005)

**Plan specification**: Agents get the same left pane as Chatbot (folders optional, named sessions, rename/delete). Image/Video panes match width, collapse, and empty chrome. Collapsed state is an icon rail like the module sidebar. Sidecar down: empty hint + banner, no fake rows. Delete while collapsed still confirms.

**What happened**: Extracted `CollapsibleHistoryAside` (280px / 56px) and wired it into ChatPage, StudioHistoryPane, and CodingPage. FolderTree gained a `collapsed` prop that keeps new/folder icons and per-session marks. Agents use `createCodingSessionsAsChatExplorer` over `coding.sessions.list` / rename / delete / start. Sessions without sidecar folders render as a flat list. Overlay folders live in `localStorage` (DF-33). Chatbot stays on `chat.explorer.*`.

**Key files changed**: `desktop/src/modules/chat/FolderTree.tsx`, `desktop/src/modules/chat/ChatPage.tsx`, `desktop/src/modules/coding/CodingPage.tsx`, `desktop/src/shared/explorer/StudioHistoryPane.tsx`, `desktop/src/shared/explorer/CollapsibleHistoryAside.tsx`, `desktop/src/shared/explorer/codingSessionsAsChatExplorer.ts`

**Troubleshooting**: Lucide `Folder` collided with the explorer `Folder` type; aliased to `FolderIcon`. CodingPage JSX duplication from a partial replace was truncated back to one return.

**Verification**: Desktop vitest 123/123 on the Phase 2 file set. Collapse test asserts 56px with new/folder visible. CodingPage rename/delete uses `tree-rename-*` / `confirm-delete-ok`.

### 2.2 Modern delete confirm (T006)

**Plan specification**: Rounded card, quiet Cancel, rounded destructive Delete using status tokens, not a square `#d33` fill. Esc/Cancel: no delete. Missing label still names the kind.

**What happened**: Restyled `folder-tree-confirm-delete`. Escape listener dismisses. `itemNoun` covers chat vs session. Collapsed rail hides inline trash; Delete key and context menu still open the same dialog.

**Key files changed**: `desktop/src/modules/chat/FolderTree.tsx`, `desktop/tests/FolderTree.test.tsx`

### 2.3 Testing and Stabilization

FolderTree, ChatPage, CodingPage, StudioHistoryPane, ImageStudioPage, adapter tests. CI comment on `test:shell`. Session history this file. No live GPU.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| desktop vitest (FolderTree, ChatPage, CodingPage, StudioHistoryPane, ImageStudioPage, panels, codingSessionsAsChatExplorer) | PASS 123/123 |
| desktop `tsc --noEmit` | PASS |
| desktop eslint on touched files | PASS |
| CodingPage history is FolderTree-class | Yes (`coding-history-pane`) |
| Collapsed chats aside is empty 24px | No (56px icon rail) |
| `folder-tree-confirm-delete` testid | Kept |
| Delete button square `#d33` fill | No |
| package.json bump | Not done |
| Hub injection scanner | Untouched |
| thinking-orbs npm package | Not added |
| Chatbot moved off `chat.explorer.*` | No |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Agents folders are localStorage overlay | P3 | DF-33 |
| Packaged four-tab chrome not screenshotted | P2 | Follows DF-32; unit evidence only |
| `SessionListPanel` still exists for isolated tests | P3 | Accepted; CodingPage no longer mounts it as history |

---

## 5. Plan Discrepancies

- Agents folder delete reparents sessions instead of cascading `coding.session.delete`, because folders are chrome-only.
- Collapsed rows hide the pencil/trash pair (56px). Delete still confirms via keyboard or context menu.
- Sessions tab on Agents is a pointer to the left pane rather than a second list.

---

## 6. Assumptions Made

- 56px matches the module sidebar compact rail and is wide enough for new/folder plus a 32px session mark.
- Persist collapse per pillar (`nexus.chat.chatsPaneCollapsed`, `nexus.coding.historyCollapsed`, `nexus.image.historyCollapsed`, `nexus.video.historyCollapsed`).
- Overlay folders are enough to satisfy "folders optional" without a sidecar schema this phase.

---

## 7. Testing Summary

Automated only. Human QA is deferred to the last phase.

- Collapse-not-empty (ChatPage 56px + new/folder + rail marks)
- Agents rename/delete through FolderTree
- Image/Video pane width 280 and collapse
- Confirm-delete cancel, Escape, and rounded quiet destructive
- Adapter: flat list, overlay reparent, workspace required for create

---

## 8. TODO Tracker

| Item | Status |
|---|---|
| T004 One history chrome on four tabs | Done |
| T005 Collapsed icon rail | Done |
| T006 Modern delete confirm | Done |
| Phase 3 rail margin and thinking orbs | Next |

---

## 9. Summary and Next Steps

Phase 2 makes the four tabs share FolderTree chrome: an icon rail when collapsed, a quiet delete dialog, and Agents sessions in the same widget as Chatbot. Next: Phase 3 (rail top inset and centered composing orbs).
