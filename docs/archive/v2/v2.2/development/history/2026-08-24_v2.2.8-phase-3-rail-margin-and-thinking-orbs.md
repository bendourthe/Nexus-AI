# Development Log: v2.2.8 Phase 3 - Rail Margin and Thinking Orbs

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Add a small top inset on the module rail so Chatbot is not flush with the title bar. Center Chat/Agents pending orbs at a size that reads as the jakubantalik grammar without adding the thinking-orbs npm package.
**Outcome**: Compact and expanded rails (including Settings) start with `SIDEBAR_NAV_INSET_TOP`. Chat/Agents pending uses `data-orb-size="bubble"` (48px), centered in the bubble. Image/Video pending stays hero. Reduced-motion remains static. `thinking-orbs` is not in package.json.

---

## 1. Starting State

- **Branch**: `develop` (v2.2.8 Phase 2 commit `f47bc5e`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.8-working-local-studio.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-24_v2.2.8-phase-2-unified-history-chrome.md`

Phase 2 unified FolderTree chrome. The v2.2.4 rail lock still put Chatbot flush under the frameless title bar. Chat pending orbs were `size="inline"` (20px) in a left-aligned row.

---

## 2. Chronological Steps

### 2.1 Rail inset (T007)

**Plan specification**: Small top padding on the module nav. Compact and expanded both keep it. Settings route uses the same rail. Do not restore the brand wordmark block.

**What happened**: Exported `SIDEBAR_NAV_INSET_TOP = "var(--space-2)"` and applied it as the first padding token on the aside in both compact and expanded layouts. Updated the v2.2.4 comment so a later pass does not re-lock flush-to-title-bar.

**Key files changed**: `desktop/src/components/Sidebar.tsx`, `desktop/tests/Sidebar.test.tsx`

**Verification**: Padding no longer starts with `0 `. Compact, expanded, and `/settings` keep the inset.

### 2.2 Centered reference-quality orbs (T008)

**Plan specification**: Chat/Agents pending orbs centered, larger than 20px. Image/Video stay hero. Tighten orbEngine motion toward Working / Searching / Solving / Listening / Composing / Shaping. Missing activity defaults to Composing without throwing. Reduced-motion: static ring, caption remains. Do not add thinking-orbs.

**What happened**: Added `bubble` preset (48px, 28 dots). Chat/Agents MessageBubble pending uses `size="bubble"` with column flex, `justifyContent: center`, and full bubble width. Image/Video still `hero`. Motion: faster working orbit, searching sweep, solving inward spiral, listening pulse, composing ring with chase highlight, shaping morph. `resolveAgentState` falls back to composing for null/unknown activity.

**Key files changed**: `desktop/src/components/agentState/orbEngine.ts`, `desktop/src/components/agentState/AgentStateOrb.tsx`, `desktop/src/components/agentState/mapping.ts`, `desktop/src/shared/chat/MessageBubble.tsx`

**Verification**: Chat composing orb is `bubble` not `inline`. Studio pending remains hero. Reduced-motion tests still static. package.json has no thinking-orbs key.

### 2.3 Testing and Stabilization

Sidebar, AgentStateOrb, orbEngine, mapping, MessageBubble, ChatPage, CodingPage, Image/Video pending tests. CI comment on `test:shell`. Session history this file. No live GPU.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| desktop vitest (Sidebar, AgentStateOrb, orbEngine, mapping, MessageBubble, ChatPage, CodingPage, ImageStudioPage, VideoLabPage, GenerationCanvas, useReducedMotion) | PASS 148/148 |
| desktop `tsc --noEmit` | PASS |
| desktop eslint on touched files | PASS |
| Rail padding starts with `0 ` | No |
| Chat composing `data-orb-size` | `bubble` (48px) |
| Image/Video pending size | `hero` |
| thinking-orbs in package.json | Absent |
| package.json bump | Not done |
| Hub injection scanner | Untouched |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Packaged rail inset and orb motion not screenshotted | P2 | Unit evidence only; field checklist is Phase 6 |
| Live GPU generate still unproven | P2 | DF-4 unchanged |

---

## 5. Plan Discrepancies

- Live IPC chat method remains `chat.session.sendMessage` from Phase 1; this phase did not change timeouts.
- `inline` 20px remains for LocalModelStatus chips. The plan asked to stop using inline for Composing in the bubble, not to delete the preset.

---

## 6. Assumptions Made

- `var(--space-2)` is enough inset without restoring the brand block.
- 48px bubble is large enough to read as the reference playground inside a chat bubble without matching the 64px studio hero.

---

## 7. Testing Summary

Automated only. Human QA is deferred to the last phase.

- Sidebar inset in compact, expanded, and Settings
- bubble > inline; hero still 64
- Chat/Agents pending centered `bubble`; studio pending `hero`
- Missing activity maps to Composing
- thinking-orbs absent from desktop package.json
- Distinct first-dot radii after one second still hold

---

## 8. TODO Tracker

| Item | Status |
|---|---|
| T007 Rail top inset | Done |
| T008 Centered bubble orbs | Done |
| Phase 4 Models catalog parity | Next |

---

## 9. Summary and Next Steps

Phase 3 insets the module rail and centers Chat/Agents composing orbs at 48px without adding thinking-orbs. Next: Phase 4 (installer = Settings Models sort, compact cards, picker order).
