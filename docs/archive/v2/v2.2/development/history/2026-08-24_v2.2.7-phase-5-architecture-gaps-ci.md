# Development Log: v2.2.7 Phase 5 - Architecture, known-gaps, and CI

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Reconcile architecture, known gaps, and CI after Phases 1-4. Do not bump package.json. Rebuild the unsigned Windows installer for operator field QA. Commit Phase 5, then push `develop`. Do not run `/update release` unless asked.
**Outcome**: Deleted unused Chatbot `Breadcrumb.tsx`. Kept DF-28 through DF-31, WN-7, DF-2, and DF-4. CI comments already name the v2.2.7 contracts. Full desktop and root Vitest suites passed. Unsigned `tauri build` ran for packaged Explorer testing.

---

## 1. Starting State

- **Branch**: `develop` (v2.2.7 Phase 4 commit `946658b`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.7-context-meter-and-transcript-chrome.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-24_v2.2.7-phase-4-transcript-chrome.md`

Phases 1-4 were committed locally. Remaining work was Phase 5 reconciliation, unsigned installer rebuild, then push `develop`.

Model-routing pre-flight: Cursor has no scriptable switch. Plan recommended frontier/max. Session model is Cursor Grok 4.6 (no downshift).

---

## 2. Chronological Steps

### 2.1 Architecture refactor (5.1)

**Plan specification**: Identify deprecated/obsolete files from the shared meter and bubble chrome. Propose-then-apply. Repair every reference. Keep `core/**` from importing `modules/**`.

**What happened**: `Breadcrumb.tsx` had no remaining TypeScript imports after Phase 3. Deleted it. ChatPage / chatExplorerAdapter tests already assert `chat-breadcrumb` is null. Updated the v2.2.8 plan grounding line that still described the header slash as present. No other dead meter/chrome files. No empty tracked directories.

**Key files changed**: `desktop/src/modules/chat/Breadcrumb.tsx` (deleted), `docs/archive/v2/v2.2/plans/v2.2.8-working-local-studio.md`.

### 2.2 Known-gaps (5.2)

**Plan specification**: Update `docs/archive/v2/v2.2/known-gaps.md`. Live Ollama thinking stays DF until field-proven. Packaged Settings/installer chips stay DF. Keep DF-2 / DF-4.

**What happened**: Advanced the v2.2.7 last-updated stamp to Phase 5. Did not close DF-28, DF-29, DF-30, DF-31, or WN-7. not_observed != absent.

**Key files changed**: `docs/archive/v2/v2.2/known-gaps.md`, `docs/archive/v2/v2.2/development/last-phase-evidence.md`, README What's new for v2.2.7.

### 2.3 CI (5.3)

**Plan specification**: Cover ModelsSettings context chip, sessionContextUsage, ContextUsageBar 80% CTA, MessageList day separators. No live GPU. Path filters and concurrency cancel-in-progress.

**What happened**: Comments from Phases 1-4 already name those contracts on `ci.yml` Node 22 `npm run test:shell` and `installer-tests.yml`. `concurrency.cancel-in-progress` already true. No new jobs. No GPU job. No Hub clone.

### 2.4 Cross-installer parity (5.4)

**Plan specification**: Installer Models tab and desktop Settings must show the same catalog context numbers.

**What happened**: Dual formatters remain (WN-7). Matching tests lock `128k` / `32k / 8k` / omit-on-null on both sides. No silent mismatch shipped. This repo has no `scripts/check_installer_parity.py` (Nexus-Hub dual-installer gate is a no-op).

### 2.5 Testing and installer rebuild (5.5)

Full desktop vitest and root vitest passed. `npm run build:shell` (`tauri build`) exited 0 and wrote `Nexus AI Studio_2.2.5_x64-setup.exe` (NSIS) and `Nexus AI Studio_2.2.5_x64_en-US.msi`. Artifacts stay untracked. `tauri.conf.json` product version remains 2.2.5.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| `npm run test:shell` | PASS 175 files / 1506 tests |
| `npm run test` (root vitest) | PASS 522 files / 5481 tests (3 files skipped, 12 tests skipped) |
| Layout moves | Breadcrumb.tsx deleted only |
| Package version bump | Not done (plan lock) |
| `/update release` | Not run |
| Packaged Explorer / live Ollama / live GPU | not_observed (DF-28-31, DF-2, DF-4) |

---

## 4. Deviations from Plan

- **DEVIATION**: Did not hand off to `/update release` (operator lock). Last-phase evidence is written; tag/changelog/version bump stay behind that command.
- **DEVIATION**: README What's new for v2.2.7 is written now so a later `/update release` is not a version-only README bump.

---

## 5. Known Gaps

- Keep DF-28, DF-29, DF-30, DF-31, WN-7, DF-2, DF-4.

---

## 6. Decisions and Rationale

- Deleting Breadcrumb is the only layout apply from the meter/chrome work. Do not drive-by-clean adjacent chat files.
- Unsigned installer is for the operator field checklist, not a proof that DF-28-31 are closed.

---

## 7. Testing Summary

### Automated Tests

- Desktop: 1506 passed
- Root: 5481 passed, 12 skipped

### Manual Testing Still Needed

Operator field checklist in the v2.2.7 plan, against the unsigned Windows installer.

---

## 8. TODO Tracker

- [x] Delete unused Breadcrumb
- [x] known-gaps Phase 5 stamp
- [x] Full suites
- [x] Unsigned installer rebuild
- [x] Phase 5 session history
- [ ] Push develop (after this commit)

---

## 9. Summary and Next Steps

v2.2.7 implementation is complete on `develop` at unit evidence. Push `develop` after the Phase 5 commit. Field-prove DF-28 through DF-31 on the unsigned installer. Do not run `/update release` unless asked. v2.2.8 owns rail margin, collapsed-chats strip, delete-dialog, history-chrome parity, orbs, sidecar 15s timeout, Settings-vs-installer identity, and Hub apply.
