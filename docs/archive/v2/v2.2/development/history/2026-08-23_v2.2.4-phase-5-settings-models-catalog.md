# Session history: v2.2.4 Phase 5 - Settings Models as installer catalog

**Date**: 2026-08-23
**Operator**: Project owner
**Objective**: Rebuild Settings > Models to match the installer catalog (tabs, copy, Download vs Downloaded, hardware gating, Favorite per tab).
**Outcome**: The page uses Chat / Agentic / Image / Video / Audio / Document tabs. Cards show installer copy. Download is disabled when the host cannot run the model. One favorite star per tab writes the Phase 2 localStorage key.

---

## 1. Starting state

- **Branch**: `develop`
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.4-chatbot-first-and-runtime-honesty.md`
- **Prior session reference**: `docs/archive/v2/v2.2/development/history/2026-08-23_v2.2.4-phase-4-compact-studio-media.md`

Phase 4 made studio media compact. Settings > Models still used Type / Family / Status filters and Install checkboxes, so it did not match the installer picker.

---

## 2. Chronological steps

### 2.1 Category tabs and installer copy

**Plan specification**: Chat, Agentic, Image, Video, Audio, Document. Reuse description, Best for, license, size, recommended/required/compatible. Unknown task lands in Other.

**What happened**: `catalogTabs.ts` assigns tabs from `task` then `type`, and lists agentic-capable chat models on both Chat and Agentic. `ModelsSettings.tsx` renders those tabs. Catalog copy fields are marshaled through `ListedModel` and the sidecar DTO.

**Key files changed**: `desktop/src/shared/models/catalogTabs.ts`, `desktop/src/pages/settings/ModelsSettings.tsx`, `core/registry/NexusModelRegistry.ts`, `desktop/sidecar/src/models/modelsService.ts`

### 2.2 Download vs Downloaded and hardware disable

**Plan specification**: Downloaded is non-interactive. Not downloaded: Download. Disable when the host cannot run the model. Favorite star per tab.

**What happened**: Download uses existing `client.install`. Over-budget rows show a VRAM reason and no button. Favorites call `writeFavorite` for Chat/Agentic/Image/Video/Audio/Document.

**Key files changed**: `desktop/src/pages/settings/ModelsSettings.tsx`, `desktop/src/shared/models/selectionPolicy.ts`

---

## 3. Verification

- Desktop vitest: ModelsSettings 14, catalogTabs 4, plus SettingsPage, settings-phase7, ipcModelsClient, selection-policy, modelsService (37 in that slice). All passed.

---

## 4. Decisions and deviations

- Search remains as a secondary filter inside a tab. Type/Family/Status dropdowns are gone.
- Other appears only when at least one row has no catalog tab.

---

## 5. Follow-ups

Phase 6: Hub update that finishes; auto-update default ON.
