# Session History - v2.2.2 Phase 1: Hidden sidecar and ready overlay

**Date**: 2026-08-22
**Plan**: `docs/archive/v2/v2.2/plans/v2.2.2-ready-shell-and-studio-chrome.md`
**Phase**: 1 - Hidden sidecar and ready overlay

## What this session set out to do

Stop the post-healthcheck GUI from opening a Node console, and cover startup with an in-app overlay until the sidecar is running and `models.list` has returned. Do not load weights into VRAM as part of ready.

## What landed

- Shared `sidecar_command` in `desktop/src-tauri/src/sidecar.rs` sets Windows `CREATE_NO_WINDOW` (`0x08000000`) for app spawn, restart, and `--healthcheck`. `DETACHED_PROCESS` is not set.
- `ReadyOverlay` sits on the existing shell (not a second window). Copy: "Starting local backend..." then "Reading installed models...".
- Dismiss on running plus catalog list (empty catalog is success). Failure or timeout shows `SidecarDownBanner` plus Restart.
- Outside Tauri (`ipc-unavailable`) the gate becomes ready so Vitest and the Vite dev server do not hang.

## Still open

Packaged Explorer launch on this host was not executed in this coding session (DF-2). macOS/Linux are N/A for the console flag (DF-19).
