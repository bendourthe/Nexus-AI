# Development Log: v2.2.6 Phase 4 - Agents transcript restore

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Opening a previous Agents session restores the visible transcript after remount, unknown ids fail with a typed error, Sessions rename/delete match Chatbot where missing, and peeking the Sessions tab does not load weights. Do not bump package.json. Context meters stay v2.2.7.
**Outcome**: CodingPage resume calls coding.session.resume and hydrates MessageList from persisted user/assistant turns. Sidecar sendMessage now stores assistant token text. Sessions list can rename and delete. Unknown ids empty the transcript and show an error. Peeking Sessions only lists.

---

## 1. Starting State

- **Branch**: `develop` (Phase 3 commit `83f50bc`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.6-session-memory-and-studio-history.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-24_v2.2.6-phase-3-videos-history.md`

---

## 2. Chronological Steps

### 2.1 Hydrate turns on resume (4.1)

**Plan specification**: coding.session.resume must fill turns / MessageList, not only sessionId. Unknown session id: typed error, empty list. Partial trace: show what exists, do not invent assistant text. Add rename/delete if Chatbot has them and Agents do not. Do not load weights on tab peek.

**What happened**: Resume already returned user prompt strings. The UI only called setSessionId, so MessageList stayed empty. sendMessage now appends turns with concatenated token text after the event stream. Resume returns those pairs. CodingPage hydrates user + assistant bubbles, skips empty assistant text, and switches to Chat. Unknown ids clear the transcript and surface Could not resume session. Rename and delete are new IPC methods plus Sessions list chrome.

### 2.2 Tests (4.2)

Remount hydrate (user + assistant, no second start), unknown id error, Sessions peek does not start/send, rename/delete in the list and through IPC/store.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| desktop CodingPage + sessionManager + session resume + protocol + panels + sidecar handlers | PASS 101/101 |
| Remount resume shows user + assistant text | PASS |
| Unknown id -> typed error, empty transcript | PASS |
| Sessions peek does not call start or sendMessage | PASS |
| package.json bump | Not done |
| Hub injection scanner | Untouched |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Tool-call cards are not in the session schema; resume restores token text only | P2 | Honest partial: do not invent cards |
| Pre-v2.2.6 files have user prompts only | P2 | Hydrate those prompts; empty assistant is omitted |
| Packaged disk persist not_observed | P2 | Phase 6 / field |
| Agents Sessions list is flat, not Chatbot folders | P2 | Rename/delete parity only; no folder tree this phase |

---

## 5. Plan Discrepancies

- CodingPage is the live page name.
- Resume IPC already existed; the hole was UI hydration plus missing assistant persistence.
- `messages` stays the user-prompt list for older callers. `turns` is the user/assistant pair list.

---

## 6. Assumptions Made

- Fake IPC is a valid stand-in for UI remount; JSON two-instance round-trip remains the sidecar store test.
- Concatenated `token` event text is the assistant body that exists. No invented prose when tokens are absent.

---

## 7. Testing Summary

### Automated Tests

desktop vitest: CodingPage, coding-sessionManager, coding-session-resume, coding-protocol, panels, sidecar-handlers. 101 passed.
