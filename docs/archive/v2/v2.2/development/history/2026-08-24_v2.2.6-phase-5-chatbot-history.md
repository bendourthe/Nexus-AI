# Development Log: v2.2.6 Phase 5 - Chatbot closeout

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Prove Chatbot remount hydration (user + assistant), within-session follow-up, and attachment restore. Do not claim a durable save when the store cannot append. Keep the Phase 1 model-id alias fold. Do not bump package.json. Context meters stay v2.2.7.
**Outcome**: ChatPage already persisted and hydrated via chat.explorer.appendMessage / listMessages. Tests now prove append-then-remount, image attachment restore, and a typed not-saved error when append fails. Alias fold remains gemma-4-12b-it-gguf -> gemma4:12b.

---

## 1. Starting State

- **Branch**: `develop` (Phase 4 commit `6112f6b`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.6-session-memory-and-studio-history.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-24_v2.2.6-phase-4-agents-history.md`

---

## 2. Chronological Steps

### 2.1 Prove and patch Chatbot (5.1)

**Plan specification**: Quit/reopen continues the conversation. If attachments do not persist, persist them or record a known-gap. Store missing: in-memory fallback must not claim durable save. Keep Phase 1 alias fold.

**What happened**: ChatExplorerStore already round-trips attachments as JSON strings. ChatPage already hydrates listMessages into MessageList, including attachments, and already calls foldModelId on session start. The hole was proof: no append-then-remount gate, no ChatPage attachment hydrate test, no not-saved claim test. Those tests were added. No production patch was required.

### 2.2 Tests (5.2)

ChatPage.persistence: remount both bubbles, attachment src after remount, appendMessage throw -> visible text + "was not saved". ChatPage alias-fold and chat-session alias tests still pass.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| desktop ChatPage.persistence + ChatPage + chat-session | PASS 36/36 |
| Append then remount shows user + assistant | PASS |
| Attachment hydrate after remount | PASS |
| Failed append does not claim saved | PASS |
| Alias fold gemma-4-12b-it-gguf -> gemma4:12b | PASS (existing) |
| package.json bump | Not done |
| Hub injection scanner | Untouched |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Packaged disk persist not_observed | P2 | Phase 6 / field |
| InMemoryChatExplorerClient has no appendMessage | P2 | Honest: persist no-ops; does not claim saved |
| Attachment values are strings (data URL or path), not separate blobs | P2 | Matches existing ChatExplorerStore contract |

---

## 5. Plan Discrepancies

- ChatPage is the live page name.
- No ChatPage production patch; the store and hydrate path were already load-bearing.

---

## 6. Assumptions Made

- PersistentExplorerClient is a valid stand-in for two-instance SQLite in UI tests. SQLite attachment round-trip remains `tests/unit/modules/chat/ChatExplorerStore.phase5.test.ts`.

---

## 7. Testing Summary

### Automated Tests

desktop vitest: ChatPage.persistence (9), ChatPage (17), chat-session (10). 36 passed.
