# Development Log: v2.2.5 Phase 5 - Hub latest at install and Update

**Date**: 2026-08-23
**Operator**: Project owner
**Objective**: Pack-time snapshot and networked install land the latest Nexus-Hub release. 3.12.0 is not a magic product version. Refresh `HUB_SKILL_SCAN_ALLOWLIST` against that tag. Do not disable `PromptInjectionScanner`.
**Outcome**: Snapshot pack fails when the local catalog tag is not GitHub latest (tests inject `v9.9.9`). Installer provisioner tests no longer treat `v3.12.0` as the success pin. Allowlist reviewed against Hub **v3.19.2**. Scanner stays fail-closed. A planted jailbreak still blocks. Live packaged Hub apply was not run (DF-23).

---

## 1. Starting State

- **Branch**: `develop`
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.5-first-successful-generation.md`
- **Prior session reference**: `docs/archive/v2/v2.2/development/history/2026-08-23_v2.2.5-phase-4-chat-explorer-chrome.md`

`NexusHubSyncer` already resolved `/releases/latest`. The installer bundled whatever was in `~/.nexus-ai/catalog` (operator field: Active 3.12.0). Tests asserted `v3.12.0` as the happy-path tag. The allowlist header still said Hub v3.10.0. A 3.12.0 to 3.19.2 diff tripped the scanner, so in-app Update did not apply.

---

## 2. Chronological Steps

### 2.1 Latest Hub at pack and install (T017)

**Plan specification**: Pack-time and networked install pull latest. Stop treating `v3.12.0` as the production pin in tests. Inject a fake latest. Document the snapshot tag in the manifest. Latest tag API fail: fail the pack job rather than silently ship 3.12.0.

**What happened**: `build-hub-snapshot.py` resolves latest via GitHub (or `NEXUS_HUB_LATEST_TAG`). `--require-latest` is the default. A catalog whose `nexus-hub-version.json` does not match latest exits 1 and writes no archive. `--allow-stale` is the explicit escape. Windows/macOS/Linux pack scripts clear `hub-snapshot/` on that failure so a stale 3.12.0 tarball is not embedded; the installer still builds and syncs at install time. Provisioner and desktop Hub tests now use injected `v9.9.9`.

**Key files changed**: `scripts/installer/build/build-hub-snapshot.py`, `build-windows.ps1`, `build-linux.sh`, `build-macos.sh`, `hub_catalog_provisioner.py`, installer and desktop Hub tests

### 2.2 Scanner allowlist for the new tag (T018, T019)

**Plan specification**: Re-review `HUB_SKILL_SCAN_ALLOWLIST` against the latest Hub tag. Do not disable the scanner. Fixture that used to block now applies iff each suppression is justified; a planted jailbreak still blocks.

**What happened**: GitHub `/releases/latest` for `bendourthe/Nexus-Hub` on 2026-08-23 is **v3.19.2**. Existing nine (skill, rule) waivers still name paths that exist in that tag. Exported `HUB_ALLOWLIST_REVIEWED_TAG = "v3.19.2"`. Production scanner still uses the allowlist only for the trusted Hub source. Tests: allowlisted `security/ai-attack-patterns` snippet passes; `security/unreviewed-injection` planted jailbreak blocks. Untrusted `SkillInstaller` still has no suppressions.

**Key files changed**: `core/skills/hubSkillScanAllowlist.ts`, `tests/unit/core/skills/PromptInjectionScanner.test.ts`

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Installer pytest `test_hub_catalog_provisioner.py` | PASS: 15 tests (includes stale 3.12.0 refused) |
| Desktop vitest hub-catalog-phase3 + ipcSkillsClient | PASS: 35 tests |
| Root vitest PromptInjectionScanner + NexusHubSyncer | PASS: 66 tests |
| Live GitHub latest tag | v3.19.2 (2026-08-23) |
| Packaged Hub Update apply | not_observed (DF-23) |
| Package version bump | Not done (plan lock) |

---

## 4. Decisions

- Tests inject `v9.9.9` rather than hardcoding today's v3.19.2 so CI does not freeze another product pin.
- A stale snapshot is deleted from the installer payload; the app install still succeeds and `--sync-hub-catalog` pulls latest when online.
- Allowlist is still nine narrow rows. New Hub skills are not blanket-muted. Additional v3.19.2 findings, if any, still fail closed until reviewed.

---

## 5. Residual risk

- Packaged Explorer Hub Update against a live clone is unproven (DF-23).
- If v3.19.2 added a new educational skill that trips a non-allowlisted rule, Update still blocks until that pair is reviewed. That is the intended fail-closed path.
- Phase 6 owns known-gaps BG-43, CI path filters, and the unsigned installer rebuild.
