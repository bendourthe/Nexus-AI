# Session history: v2.2.4 Phase 6 - Hub update that finishes

**Date**: 2026-08-23
**Operator**: Project owner
**Objective**: Update now can complete a real Nexus-Hub pull. Auto-update to latest release defaults ON.
**Outcome**: `skills.sync` waits up to 10 minutes without raising the global 15s RPC timeout. Hub timeouts say Hub fetch, not sidecar response timeout. Missing auto-update key defaults ON; stored false stays off.

---

## 1. Starting state

- **Branch**: `develop`
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.4-chatbot-first-and-runtime-honesty.md`
- **Prior session reference**: `docs/archive/v2/v2.2/development/history/2026-08-23_v2.2.4-phase-5-settings-models-catalog.md`

Phase 5 rebuilt Settings > Models. Update now still died at the 15s sidecar wait, and auto-update treated a missing key as off.

---

## 2. Chronological steps

### 2.1 Per-method timeout for skills.sync

**Plan specification**: Do not raise Duration::from_secs(15) globally. Prefer a per-method map or a job id.

**What happened**: Chose (a) per-method map. `rpc_timeout_for("skills.sync")` is 600s; ping, models.list, and chat.send stay 15s. A skills.sync timeout returns Hub fetch copy via SidecarError::Request so the Skills page does not treat it as a dead sidecar.

**Key files changed**: `desktop/src-tauri/src/sidecar.rs`, `desktop/src/pages/settings/SkillsSettings.tsx`

### 2.2 Auto-update default ON

**Plan specification**: Label "Auto-update to latest Nexus-Hub release". Missing key: true. Stored false stays false.

**What happened**: `skills.autoSync.get` returns `stored !== false`. Coding bootstrap registers the idle worker when the key is unset. The switch label matches the plan.

**Key files changed**: `desktop/sidecar/src/handlers.ts`, `desktop/sidecar/src/runtime/codingBootstrap.ts`, `desktop/src/pages/settings/SkillsSettings.tsx`

---

## 3. Verification

- Desktop vitest: SkillsSettings 13, codingBootstrap 9, SkillsSettingsBanner 3. Pass.
- Rust: `rpc_timeout_for` tests, 2 passed.

---

## 4. Decisions and deviations

- Job-id polling (option b) was not added. A 10-minute wait covers a typical Hub clone without a second RPC protocol.
- The Skills page still remaps a leftover "sidecar response timeout" string to Hub copy so an older sidecar binary fails honestly.

---

## 5. Follow-ups

Phase 7: refactor, known-gaps, CI. No version bump. Then push and field-review the original screenshot failures.
