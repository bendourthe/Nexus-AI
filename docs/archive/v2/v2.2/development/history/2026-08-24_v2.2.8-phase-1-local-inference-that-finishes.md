# Development Log: v2.2.8 Phase 1 - Local Inference That Finishes

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Packaged Chatbot `Hi` and Agents turns must not die at 15s. Settings must show installed Gemma 4 12B as Downloaded when Ollama has `gemma4:12b`. Sidecar death and slow generation are different errors.
**Outcome**: `rpc_timeout_for("chat.send")` is 600s; `ping` stays 15s. Chat/coding/image/video generate methods use the same minutes-class cap with typed copy, never `sidecar response timeout`. Probe folds `gemma4:12b` onto catalog `gemma-4-12b-it-gguf`.

---

## 1. Starting State

- **Branch**: `develop` (v2.2.7 Phase 5 commit `02616e9`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.8-working-local-studio.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-24_v2.2.7-phase-5-architecture-gaps-ci.md`

v2.2.7 is complete on `develop`. Field session after v2.2.6 showed Chatbot `Hi` becoming `(chat unavailable) sidecar response timeout` and Settings listing installed Gemma as Download.

---

## 2. Chronological Steps

### 2.1 Streaming-safe RPC timeouts (T001, T002)

**Plan specification**: Long-running methods do not share `RPC_TIMEOUT_DEFAULT`. Chat/coding/image/video send or generate stay open for the event stream. Ping stays 15s. Malformed names stay 15s. Sidecar exit mid-stream stays `Exited`. Concurrent ping during generate stays 15s.

**What happened**: Added `RPC_TIMEOUT_INFERENCE` (600s) for exact method names including plan alias `chat.send` and the real `chat.session.sendMessage`, `coding.session.sendMessage`, `coding.startTask`, `image.generate`, `video.generate`, and the diffusion generate methods. Cap hits return typed `SidecarError::Request` copy, not `Timeout`. Chat, Agents, Image, and Video surfaces rewrite a leftover timeout string through `formatChatTurnError` / `formatInferenceError`.

**Key files changed**: `desktop/src-tauri/src/sidecar.rs`, `desktop/src/lib/inferenceRpcError.ts`, `desktop/src/modules/chat/ChatPage.tsx`, `desktop/src/modules/coding/CodingPage.tsx`, `desktop/src/modules/image/ImageStudioPage.tsx`, `desktop/src/modules/video/VideoLabPage.tsx`

**Troubleshooting**: None. Compile and tests passed on the first run.

**Verification**: `cargo test rpc_timeout` 3 passed. ChatPage slow-stream and timeout-rewrite tests passed.

### 2.2 Installed Gemma is Downloaded (T003)

**Plan specification**: Packaged Settings treats alias-folded Ollama tags as installed. Unknown tags do not map onto Gemma. Snapshot-selected missing stays Retry. Ollama down does not crash.

**What happened**: `queryOllamaTags` collects both `name` and `model`. `ollamaTagsIncludeModel` matches catalog id `gemma-4-12b-it-gguf` to tag `gemma4:12b` via aliases/`foldModelId`. `createModelsRuntime` honors `NEXUS_OLLAMA_URL`. ModelsSettings shows Downloaded when `installed && source !== catalog-only`.

**Key files changed**: `core/registry/installedProbe.ts`, `desktop/sidecar/src/models/modelsService.ts`, `desktop/tests/installedProbe.test.ts`, `desktop/tests/modelsService.test.ts`, `desktop/tests/ModelsSettings.test.tsx`

### 2.3 Testing and Stabilization

Cargo timeout table, desktop probe and copy tests, CI comment on `test:shell`. No live GPU. Session history this file.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| `cargo test --quiet rpc_timeout --all-targets` | PASS 3/3 |
| desktop vitest (inferenceRpcError, installedProbe, modelsService, ChatPage, CodingPage, ModelsSettings, sidecar-status) | PASS 109/109 |
| desktop `tsc --noEmit` | PASS |
| desktop eslint on touched files | PASS |
| `rpc_timeout_for("chat.send")` is 15s | No (600s) |
| `rpc_timeout_for("ping")` is 15s | Yes |
| Slow stream surfaces `sidecar response timeout` | No |
| Unknown Ollama tag marks Gemma Downloaded | No |
| package.json bump | Not done |
| Hub injection scanner | Untouched |
| thinking-orbs npm package | Not added |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Packaged Chatbot Hi not screenshotted | P2 | DF-32 |
| Ping-during-generate is timeout-table evidence, not a live two-RPC spawn | P3 | Accepted; sidecar event loop still handles ping while generate waits |

---

## 5. Plan Discrepancies

- The plan names `chat.send`; the live IPC method is `chat.session.sendMessage`. Both are minutes-class so the stability gate and the real path match.
- Concurrent ping during generate is asserted as `ping` 15s and generate 600s, not a spawned sidecar integration.

---

## 6. Assumptions Made

- A 10-minute cap is long enough for a cold Gemma 4 12B first token on a laptop GPU and still bounds a hung generate.
- Drain-event polls stay at 15s because they return buffered events; generate accept/send is the long wait.
- Collecting Ollama `/api/tags` `model` in addition to `name` is safe and does not invent Compatible.

---

## 7. Testing Summary

Automated only. Human QA is deferred to the last phase.

- Rust: `rpc_timeout_keeps_15s_for_ordinary_methods`, `rpc_timeout_chat_send_is_not_15s`, `rpc_timeout_ping_stays_short_while_generate_is_minutes`
- Desktop: slow-stream fixture, timeout rewrite, Gemma alias probe, `model`-only tags, unknown tag, ModelsSettings Downloaded card

---

## 8. TODO Tracker

| Item | Status |
|---|---|
| T001 Per-method RPC timeouts | Done |
| T002 ChatPage timeout copy | Done |
| T003 Gemma alias Downloaded | Done |
| Phase 2 unified history chrome | Next |

---

## 9. Summary and Next Steps

Phase 1 removes the 15s send timeout that turned a cold local `Hi` into a sidecar-death string, and it folds `gemma4:12b` onto the Gemma 4 12B Settings card. Next: Phase 2 (FolderTree on four tabs, collapsed icon rail, modern delete).
