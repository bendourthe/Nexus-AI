# Session History - v2.2.9 Phase 7: Architecture, Known-Gaps Reconciliation, and CI/CD

**Date**: 2026-08-27
**Plan**: [v2.2.9-field-chrome-catalog-and-generate.md](../../plans/v2.2.9-field-chrome-catalog-and-generate.md) (Phase 7, final)
**Driver**: `/implement in-full` (Phase 7 of 7)

## What was done

- **7.1 Architecture refactor**: `npm run cleanup:scan` 0 findings; `npm run check:docs-layout` OK. Deprecated chrome was removed by the phases themselves (empty studio headers, duplicate chip rows, pending em dash). No moves needed.
- **7.2 Known-gaps reconciliation (T013)**: v2.2.9 section added to `known-gaps.md`. Closed DF-32, DF-35 (v2.2.8 Resolved), DF-28, DF-31 (v2.2.9 Resolved), BG-55. Annotated-and-kept DF-30, DF-29, DF-25, DF-23, DF-34, DF-4. New: DF-36 (packaged v2.2.9 chrome unproven), DF-37 (kind prose-only on wire), WN-8 (self-mutating benchmark fixture), WN-9 (load flakes), MT-4 (no coverage percentage), MT-5 (publisher-map parity untested). `docs/todos.md` refreshed.
- **7.3 Living docs**: README/DEVLOG/todos and per-version tree verified; DEVLOG stays a per-release index (v2.2.9 line lands at `/update release`).
- **7.4 Git-tree hygiene**: merged v1.x remote branches reported (report-only, nothing deleted); no `check_release_preconditions.py` in this repo (manual report quoted in the evidence file).
- **7.5 CI/CD (T014)**: all v2.2.9 tests sit inside existing jobs (ci.yml desktop vitest + root vitest + pytest runtimes/; installer-tests.yml). Added the v2.2.9 contract comment block to ci.yml. Concurrency + path filters present on all 17 active workflows. No live GPU, no live Hub clone. `check:tampering` 0 findings.
- **7.6 Independent Goal-vs-codebase review**: fresh read-only agents audited every Goal clause against the tree. Verdicts: all clauses LANDED except one genuine MISS - `allow_stub()` let `NEXUS_DIFFUSION_ALLOW_STUB=1` enable stub PNGs outside pytest, with a test certifying the permissive behavior under a strict name. **Fixed this phase** (BG-55): stub is now pytest-only, the flag alone is refused, and the test asserts the refusal. Residual review notes became DF-37 and MT-5. The contextWindow-null-key observation resolved to the intended reading (never invent a window; test pins `?? null`); stale built catalogs are gitignored artifacts regenerated by the installer rebuild.
- **7.7 Full-suite testing**: lint 0 errors; `python -m pytest tests/python -q` 261 passed (after the stub fix); desktop `Test Files 182 passed / Tests 1603 passed`; root `Test Files 522 passed | 3 skipped / Tests 5488 passed | 12 skipped`; installer pytest exit 0 at its boundary (no installer change since). WN-8 fixture restored after the root run.
- **Evidence file**: `docs/archive/v2/v2.2/development/last-phase-evidence.md` written with all eight required sections, each quoting its proving command.

## Release posture

Per the plan's lock and the operator instruction, the driver stops before `/update release`: no version bump, no tag, no GitHub release. `develop` is pushed and the unsigned Windows installer is rebuilt for the DF-36 field checklist.

## Installer rebuild (post-push)

`npm run build` + `npx vsce package --no-dependencies` + `build-windows.ps1 -SkipSign` produced `dist\NexusSetup.exe` (226.5 MB, SHA256 `7A8FC701344C53128082477DB450F0B19F29D8EF9DA91C16C37F16F1FADE8FAC`). `smoke-windows-exe.ps1` passed all three probes (wizard boot `--version`, bundled registry, embedded desktop payload). Note for local runs: the smoke script needs explicit `-ExePath` / `-DistDir` under Windows PowerShell 5.1 because `$PSScriptRoot` is empty in param defaults there (CI uses pwsh).

## Next steps

Operator field QA on the rebuilt installer (plan checklist items 1-7 / DF-36); a recorded GPU generate would close DF-4. Then `/update release` when asked.
