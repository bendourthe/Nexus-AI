# Development Log: v2.2.7 Phase 2 - Session token accounting

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Persist per-turn usage; `sessionContextUsage` sums used tokens against the current picker window. Missing usage is null, not 0. Estimate only when usage is absent, and flag it. No live GPU. Do not count a stub 1x1 as visual tokens.
**Outcome**: Chat done events carry Ollama/OpenAI counters. Chat explorer columns, studio `extra_json`, and coding persisted turns round-trip `inputTokens` / `reasoningTokens` / `outputTokens`. Image/video persist `visualUnits` (1 for a mediaRef, 0 for failures/stubs). The helper is exact at the 80% boundary and returns null percent when no window or visual budget exists.

---

## 1. Starting State

- **Branch**: `develop` (v2.2.7 Phase 1 commit `8cf5a56`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.7-context-meter-and-transcript-chrome.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-24_v2.2.7-phase-1-catalog-context-chips.md`

Phase 1 catalog chips are on `develop`. Sidecar `collectUsage` already read Ollama/OpenAI counters for the serving gateway; chat and coding done events did not persist them. Studio turns always wrote `extra_json` NULL.

Model-routing pre-flight: Cursor has no scriptable switch. Plan recommended frontier/high. Session model is Cursor Grok 4.6 (no downshift).

---

## 2. Chronological Steps

### 2.1 Capture usage on complete (T003, T004)

**Plan specification**: Reuse existing Ollama `prompt_eval_count` / `eval_count` and OpenAI `usage` readers. Persist on explorer/studio turns. Add `sessionContextUsage`. Image/video analog via visual budget. Estimate fallback labeled Estimate. Stream abort: persist partial if known, else null. Mid-session model switch uses the current window against all turns (do not rewrite history). Do not count a stub 1x1.

**What happened**: `LLMStreamChunkSchema` now keeps `message.thinking` and `usage.reasoning_tokens`. Chat handler folds `collectUsage` onto the done event. Chat explorer adds nullable token columns. Studio stores usage in `extra_json`. Coding turns persist reported usage or an estimate flagged `tokensEstimated`. `sessionContextUsage` uses the latest reported `inputTokens` snapshot so summing `prompt_eval_count` across assistant turns does not double-count the full prompt. Image/video `studioPersistUsage` sets `visualUnits` 1 only when a mediaRef is persisted (1x1 stubs never get a mediaRef).

**Key files changed**: `core/chat/sessionContextUsage.ts`, `modules/chat/storage/ChatExplorerStore.ts`, `core/generations/StudioSessionStore.ts`, `desktop/sidecar/src/chat/chatMessageHandler.ts`, `desktop/sidecar/src/serving/chatCore.ts`, `desktop/sidecar/src/coding/sessionManager.ts`, Chat/Image/Video/Coding persist paths.

### 2.2 Tests and CI

Fixture Ollama final chunk stores 20+8+thinking on done; session sum matches. Helper: 50%, 80% exact, 100%, null window => percent null, reasoning included, null not coerced to 0, visual stub ignored. Store round-trips. CI comment names usage persist; no new jobs; no live GPU.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| `tests/unit/core/chat/sessionContextUsage.test.ts` | PASS 8/8 |
| ChatExplorerStore + StudioSessionStore usage round-trip | PASS |
| desktop chat-session, serving-chatCore, coding-sessionManager/resume, studioTurnUsage, ChatPage.persistence, ImageStudioPage, VideoLabPage | PASS |
| `npx tsc --noEmit -p desktop/tsconfig.json` | PASS |

---

## 4. Deviations from Plan

- **DEVIATION**: `sessionContextUsage` uses the latest non-estimated `inputTokens` snapshot instead of summing every assistant `prompt_eval_count`. Ollama reports the full prompt per request; summing would overcount. Documented in the helper. The single-turn fixture still matches prompt+eval+thinking.

---

## 5. Known Gaps

- **DF-29**: Live Ollama usage/thinking not_observed. Coding estimates when the agent runner does not attach counters.
- Keep DF-28, WN-7, DF-2, DF-4.

---

## 6. Decisions and Rationale

- Estimate user-turn input tokens for later bubble chrome; `tokensEstimated` keeps them out of the latest-reported snapshot so they do not stack on `prompt_eval_count`.
- 80% uses integer compare (`used * 5 >= window * 4`) so 80/100 is exact without float noise.

---

## 7. Testing Summary

### Automated Tests

- 8 passed (sessionContextUsage)
- Chat/studio store round-trips passed
- Desktop chat handler fixture, coding persist, Image/Video page suites passed
- Desktop tsc passed

### Manual Testing Still Needed

Live Ollama Chatbot turn (DF-29). Packaged chips (DF-28). Human QA at last phase.

---

## 8. TODO Tracker

### Completed This Session

- [x] Phase 2.1 persist usage + sessionContextUsage
- [x] Phase 2.2 tests, CI comments, session history

### Remaining

- [ ] Phase 3 pill meter + picker row + 80% CTA
- [ ] Phase 4 date/time/tokens on all four modes
- [ ] Phase 5 gaps, CI, installer rebuild, push develop

---

## 9. Summary and Next Steps

Phase 2 is done on `develop` pending commit. Next: `/implement` Phase 3 (Context pill and model picker under all four composers; header `/` and gear gone). Do not bump package.json. Do not disable the Hub scanner. Do not run `/update release`.
