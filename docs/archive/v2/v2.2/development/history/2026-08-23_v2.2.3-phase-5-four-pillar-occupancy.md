# Development Log: v2.2.3 Phase 5 - Four-pillar submit-time occupancy

**Date**: 2026-08-23
**Operator**: Project owner
**Objective**: Gate Chat, Coding, Image, and Video submits with real free VRAM and the active Studio scheduler job without changing models on navigation.
**Outcome**: All four submit surfaces pause for a conflicting or unknown-capacity switch, App supplies live policy inputs, and Switch resumes the exact request once while Remember lasts only for the renderer session.

---

## 1. Starting State

- **Branch**: `develop`
- **Starting commit**: `6fbcd1c`
- **Environment**: Windows, React 19 renderer, Node sidecar, Tauri 2, Vitest 2.1.9
- **Prior session reference**: `docs/archive/v2/v2.2/development/history/2026-08-23_v2.2.3-phase-4-durable-chat-transcripts-and-memory.md`
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.3-glass-orbs-and-pillar-runtime.md`

Phase 4 was committed before Phase 5 changed shared App, Chat, Coding, and Studio submit surfaces.

---

## 2. Chronological Steps

### 2.1 Read-only scheduler occupancy IPC

**Plan specification**: Expose the Studio `GpuScheduler` active job through a new protocol method; the unrelated cron scheduler is not an occupancy source.

**What happened**: `generation.scheduler.snapshot` now returns the scheduler's typed active, queued, and foreground state. The handler resolves the existing Studio runtime and never mutates the queue or loads a model.

**Key files changed**: `desktop/sidecar/src/protocol.ts`, `desktop/sidecar/src/handlers.ts`

### 2.2 App supplies real policy inputs

**Plan specification**: Use telemetry `vramFreeGB`, not total VRAM, pass the scheduler active job to both Studios, and stop Image from silently defaulting to the low diffusion tier.

**What happened**: App tracks total and free VRAM separately, polls the new scheduler snapshot, passes the active job and free VRAM to all four modules, and passes `classifyDiffusionTier(hostVramGB)` to both Studio pages. A shared renderer-session set preserves Remember across route remounts without persisting consent.

**Key files changed**: `desktop/src/App.tsx`, `desktop/src/shared/models/schedulerResidency.ts`, `desktop/src/shared/models/useModelResidency.ts`

### 2.3 Four submit gates and one-shot approval

**Plan specification**: Chat and Coding must match Image and Video: classify on submit, render the shared dialog, and do not dispatch until the user decides.

**What happened**: Chat and Coding now store the pending prompt before any session send. All four pages derive the incumbent from the active scheduler job, treat missing target sizing as unknown rather than zero, and render a refusal when a model is unavailable. The existing Studio re-entry pattern was corrected: resolving Switch bypasses classification once, so an unchecked Remember box cannot reopen the same dialog.

**Key files changed**: `desktop/src/modules/chat/ChatPage.tsx`, `desktop/src/modules/coding/CodingPage.tsx`, `desktop/src/modules/image/ImageStudioPage.tsx`, `desktop/src/modules/video/VideoLabPage.tsx`

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Focused occupancy and page tests | PASS: 10 files, 126 tests, 0 failures |
| Solo desktop full suite | PASS: 156 files, 1355 tests, 0 failures |
| Root full suite | PASS: 516 files passed, 3 skipped; 5438 tests passed, 12 skipped |
| Desktop lint and typecheck | PASS: 0 errors |
| Root TypeScript build | PASS |
| Desktop web and sidecar builds | PASS: web transformed 1752 modules |
| Architecture gate | PASS: 0 errors, 16 pre-existing warnings |
| Docs cleanup audit | PASS: no scratch docs or move proposed |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Live-GPU cross-pillar switching was not exercised in a packaged build | P1 | DF-9 remains open until the Phase 8 final reconciliation; automated four-page and policy evidence is supported, packaged behavior is not proven here |
| The scheduler snapshot is poll-based at a 2 second cadence | P2 | Accepted for this cycle; occupancy is read-only and generation jobs are long-running, while a push transport would broaden the protocol |

---

## 5. Plan Discrepancies

The existing Image and Video code did not actually preserve Remember across route remounts because each page owned a separate hook, and Switch without Remember could reopen the dialog on re-entry. Phase 5 added a shared App-session memory set and a one-shot approved resume to satisfy the stated session and dispatch behavior.

---

## 6. Assumptions Made

- **The Studio scheduler is the canonical occupancy snapshot for this phase**: this follows the verification addendum and avoids confusing the cron-style AgentRunScheduler with GPU work.
- **An active job without a model id still represents an incumbent**: the adapter uses an internal sentinel for policy comparison while the dialog says "The current model".
- **Missing model VRAM is not zero**: an unknown catalog estimate cannot justify silent co-residency.

---

## 7. Testing Summary

### Automated Tests

- Four runtime page tests prove Chat, Coding, Image, and Video do not dispatch while confirmation is pending and resume once after Switch.
- Policy tests prove unknown VRAM confirms and remembered pairs survive a page-hook remount when the App-session set is shared.
- Scheduler IPC tests prove the active job includes module, model, and estimated VRAM.
- Desktop full Vitest: 1355 passed, 0 failed across 156 files in one solo process.
- Root full Vitest: 5438 passed, 12 skipped, 0 failed across 516 passing files and 3 skipped files.
- Desktop lint, typecheck, root build, web build, sidecar build, and architecture validation all exited zero.

### Manual Testing Performed

- Diff inspection confirmed that every policy call remains below a submit callback and no route effect invokes `residency.request`.

### Manual Testing Still Needed

- [ ] Exercise all four collision directions with real resident models on a supported GPU.
- [ ] Confirm the dialogs and resumed prompts in a packaged Windows build.

---

## 8. TODO Tracker

### Completed This Session

- [x] Expose the existing Studio scheduler snapshot over typed IPC.
- [x] Feed free VRAM, active work, and diffusion tier from App.
- [x] Gate Chat and Coding submits with the shared policy and dialog.
- [x] Prove all four surfaces block dispatch until resolution.
- [x] Preserve Remember for one renderer session and fix one-shot Switch resume.

### Remaining

- [ ] Implement v2.2.3 Phases 6 and 8.

### Out of Scope

- [ ] Push-based scheduler events.
- [ ] Native model-runtime load and unload orchestration changes.
- [ ] Packaged live-GPU collision acceptance.

---

## 9. Summary and Next Steps

Phase 5 turns the existing switch policy into a field-fed gate on every user submit surface without making tab navigation consequential. The next step is to bank this phase atomically, then implement Phase 6 workspace selection and sidecar-side Hub harness enrichment.
