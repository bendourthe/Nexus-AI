# Session history: v2.2.4 Phase 3 - Chatbot and Agents honesty

**Date**: 2026-08-23
**Operator**: Project owner
**Objective**: Sending Hi always shows a user bubble. Agents is a cowork surface without a pink coding banner or jargon badges. Tools stay on; ConfirmationGate and sandbox still gate execution.
**Outcome**: Chat and Agents append the human turn before a not-installed return. Agents header is the model switcher plus workspace field. The Enable tools checkbox is gone.

---

## 1. Starting state

- **Branch**: `develop`
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.4-chatbot-first-and-runtime-honesty.md`
- **Prior session reference**: `docs/archive/v2/v2.2/development/history/2026-08-23_v2.2.4-phase-2-installed-catalog.md`

Phase 2 bound pickers to this-install models. Screenshot 6 could still happen if residency returned before the user row was appended. Agents still showed "Agentic AI Coding" and harness badges. Chat still had an Enable tools checkbox defaulted off.

---

## 2. Chronological steps

### 2.1 User bubble before not-installed

**Plan specification**: Append and persist the user message before residency or not-installed returns. Empty prompts stay empty. After Phase 2 this branch is rare; it must still show Hi.

**What happened**: `ChatPage.handleSubmit` writes the trimmed user text first when residency is `not-installed` or `defer`, then writes the assistant explanation and returns. Confirm-to-switch still does not append (cancel must not leave a stray bubble). `CodingPage` mirrors this by recording a local turn with the user prompt and the same notice.

**Key files changed**: `desktop/src/modules/chat/ChatPage.tsx`, `desktop/src/modules/coding/CodingPage.tsx`

### 2.2 Agents chrome

**Plan specification**: Remove the pink h1. Stop passing harness / tool-calling badges from Chatbot and Agents.

**What happened**: Coding header is the compact switcher (right-aligned) plus the workspace path field. Session tabs Chat / Memory / Trace / Sessions remain. `ModelSelector` still accepts badge props so unit tests of that component stay valid; Coding and Chat no longer pass them.

**Key files changed**: `desktop/src/modules/coding/CodingPage.tsx`

### 2.3 Tools always on

**Plan specification**: Delete Enable tools. Keep ConfirmationGate and sandbox.

**What happened**: Chat no longer has `enableTools` state or the checkbox. `MessageList` is passed `enableTools={true}`. Shared tests still prove cards render when tools are on and hide when a host opts out.

**Key files changed**: `desktop/src/modules/chat/ChatPage.tsx`, `desktop/tests/ChatPage.test.tsx`

---

## 3. Verification

- Desktop vitest Phase 3 slice: 65 tests, pass (`ChatPage` 15, `CodingPage` 20, `sharedChat` 24, `QuickModelSwitcher` 6).
- Assertions added: Hi bubble + send id is the visible installed model (LFM 2.5, not `gemma4:e4b`); Hi bubble remains when the model is missing; Enable tools checkbox absent; Agents heading and harness badges absent.

---

## 4. Decisions and deviations

- Residency confirm-dialog still waits to append the user row so a cancelled switch does not dirty the transcript.
- Attachment-only turns still append after grouping, because the displayed user content is not just `text.trim()`.
- ModelSelector badge rendering is unchanged for a future Settings/developer surface.

---

## 5. Follow-ups

Phase 4: compact in-chat media, lightbox, empty completes as error text.
