# Development Log: v2.2.6 Phase 2 - Images history and last-output memory

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Image Studio named sessions persist turns, a follow-up with no attachment img2imgs the last PNG path, and remount hydrates the transcript. Do not bump package.json. Context meters stay v2.2.7.
**Outcome**: Image Studio creates/reuses a studio session, appends user then assistant turns (paths only), infers img2img from lastOutputRef, and hydrates MessageList on remount. Missing files render error text, not an empty complete.

---

## 1. Starting State

- **Branch**: `develop` (Phase 1 commit `d0f19e3`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.6-session-memory-and-studio-history.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-23_v2.2.6-phase-1-shared-session-explorer.md`

---

## 2. Chronological Steps

### 2.1 Persist image turns (2.1)

**Plan specification**: On send, append user turn first. On usable complete, persist media ref and lastOutputRef. On hydrate, rebuild MessageList. Sidecar down: do not claim saved.

**What happened**: ImageStudioPage auto-creates a named session on first send when sidecar is up / an explorer is injected. persistTurn no-ops when sidecar is down. Completes persist outputPath when it is a real path, never a data: blob. Errors persist text and do not move lastOutputRef.

### 2.2 Implicit last-image edit (2.2)

**Plan specification**: Empty attachments + usable lastOutputRef -> img2img. User attachments win. Unreadable lastOutputRef -> typed error, not silent txt2img.

**What happened**: inferImageIntent gained lastOutputRef. handleSubmit fail-closes with UNREADABLE_OUTPUT_TEXT when the probe rejects the path.

### 2.3 Tests (2.3)

Implicit img2img + persist, remount hydrate, missing-file hydrate. Existing txt2img test now allows the session title to repeat the prompt in the tree.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| desktop imageIntent + studioSessionMemory + ImageStudioPage | PASS 33/33 |
| Implicit follow-up uses last PNG path | PASS |
| Remount hydrate | PASS |
| Missing file -> error text | PASS |
| package.json bump | Not done |
| Hub injection scanner | Untouched |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Completes without outputPath still display inline PNG but do not set lastOutputRef | P2 | Accepted; implicit edit needs a path |
| Packaged disk persist not_observed | P2 | Phase 6 / field |

---

## 5. Plan Discrepancies

- ImageStudioPage is the live page name (plan ImageStudioPage).
- lastOutputRef is a filesystem path (or generations path), never a PNG blob in SQLite.

---

## 6. Assumptions Made

- In-memory explorer is a valid stand-in for two-instance SQLite in UI tests; SQLite round-trip remains the Phase 1 store test.
- outputExists is a test seam; production treats non-data paths as usable until img decode fails.

---

## 7. Testing Summary

### Automated Tests

- 33 passed (imageIntent 8, studioSessionMemory 3, ImageStudioPage 22)

### Manual Testing Still Needed

- [ ] Packaged Image Studio: generate, quit, reopen, follow-up without re-attach

---

## 8. TODO Tracker

### Completed This Session

- [x] Phase 2 persist + implicit img2img + remount hydrate

### Remaining

- [ ] Phase 3 Videos
- [ ] Phase 4 Agents
- [ ] Phase 5 Chatbot
- [ ] Phase 6 gaps, CI, installer, push, /update release

---

## 9. Summary and Next Steps

Image Studio now remembers the last PNG path inside a named session. A follow-up with empty attachments calls img2img with that path. Remount rebuilds the transcript. Next: Phase 3 Videos with the same persist / continueFrom / remount bar.
