# Development Log: v2.2.3 Phase 6 - Agentic workspace and Hub harness

**Date**: 2026-08-23
**Operator**: Project owner
**Objective**: Run desktop coding turns inside the user's project and make invoked Hub commands, workspace rules, lifecycle hooks, and permissions policy affect both interactive and scheduled headless execution.
**Outcome**: Coding sessions require an explicit persisted workspace, interactive and scheduled runs share one invoked-command enrichment path, lifecycle reflection fires with a turn transcript and written files, and sidecar tools honor per-workspace deny policy.

---

## 1. Starting State

- **Branch**: `develop`
- **Starting commit**: `25a94f7`
- **Environment**: Windows, React 19 renderer, Node sidecar, Tauri 2, Vitest 2.1.9
- **Prior session reference**: `docs/archive/v2/v2.2/development/history/2026-08-23_v2.2.3-phase-5-four-pillar-occupancy.md`
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.3-glass-orbs-and-pillar-runtime.md`

Phase 5 was committed before Phase 6 changed the Coding header, sidecar agent runner, shared headless tools, and scheduler composition.

---

## 2. Chronological Steps

### 2.1 Explicit workspace selection and persistence

**Plan specification**: Send the already-supported `workspacePath` on `coding.session.start`; refuse an omitted path rather than using the install directory; use either a fully installed native dialog plugin or a persisted text field.

**What happened**: CodingPage now displays a workspace field in its header, persists it through `desktop/src/lib/persistence.ts`, and refuses to start an agent session while the field is empty. The runner no longer falls through to `process.cwd()` and rejects relative or parent-traversing roots.

**Key files changed**: `desktop/src/lib/persistence.ts`, `desktop/src/modules/coding/CodingPage.tsx`, `desktop/sidecar/src/coding/headlessAgentRunner.ts`

### 2.2 Invoked-command and workspace prompt enrichment

**Plan specification**: Resolve command or skill bodies sidecar-side, include workspace `AGENTS.md` and `.nexus` rules, skip scanner-blocked bodies without aborting the base turn, and never inject the entire Hub catalog.

**What happened**: `headlessRunEnrichment.ts` detects a known non-built-in slash command, reads only its Hub Markdown file, strips frontmatter, scans the body, and passes an allowed body through `skillBody`. Workspace instructions and sorted Markdown rules become scoped system instructions. Unknown commands and built-in collisions continue without Hub injection.

**Key files changed**: `desktop/sidecar/src/coding/headlessRunEnrichment.ts`, `desktop/sidecar/src/coding/headlessAgentRunner.ts`

### 2.3 Shared lifecycle and scheduled composition

**Plan specification**: Use the same enrichment for interactive Coding and `AgentRunScheduler`, and wire a vscode-free HookBus lifecycle position or record a residual.

**What happened**: Interactive and scheduled runs call the same enrichment and lifecycle wrapper. The main sidecar creates one telemetry-backed HookBus. Start, user-prompt, skill-entry, end, stop, and reflection events fire; reflection includes the user/tool/assistant transcript and explicit paths written by file mutation tools. Hook failures log and do not abort the turn.

**Key files changed**: `desktop/sidecar/src/coding/scheduledHeadlessRunner.ts`, `desktop/sidecar/src/coding/headlessRunEnrichment.ts`, `desktop/sidecar/src/main.ts`

### 2.4 Per-workspace deny enforcement

**Plan specification**: Honor `.nexus/permissions.deny` on the sidecar headless tool path and fail closed when policy parsing fails.

**What happened**: The shared sidecar tool factory reads the current tool context's workdir policy before execution, evaluates the tool against every relevant argument subject, and returns a written refusal before mutation. Missing policy is inert; malformed or unreadable policy fails closed for the attempted tool.

**Key files changed**: `desktop/sidecar/src/coding/sidecarHeadlessTools.ts`

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Focused Phase 6 tests | PASS: 8 files, 107 tests, 0 failures |
| Solo desktop full suite | PASS: 157 files, 1368 tests, 0 failures |
| Root full suite | PASS: 516 files passed, 3 skipped; 5438 tests passed, 12 skipped |
| Desktop lint and typecheck | PASS: 0 errors |
| Root lint and TypeScript build | PASS |
| Desktop web and sidecar builds | PASS: web transformed 1752 modules |
| Architecture gate | PASS: 0 errors, 16 pre-existing warnings |
| Docs cleanup audit | PASS: no scratch docs or move proposed |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Native workspace folder picking is unavailable | P2 | Retained as DF-16; the persisted path field is supported and avoids adding a partially configured Tauri plugin |
| A packaged turn against a real synced Hub and local model was not run | P1 | Automated prompt, policy, scheduler, and lifecycle behavior is supported; packaged local-model acceptance is not proven here and belongs with the existing clean-VM acceptance boundary |

---

## 5. Plan Discrepancies

The older prompt described loading a matching SkillCatalog body, but the verified tree exposes Hub command Markdown bodies as the established extension-host directive and returns only descriptors over IPC. Phase 6 resolves that command body sidecar-side and passes it through the existing `skillBody` prompt section. No new body IPC was needed.

---

## 6. Assumptions Made

- **The persisted text field is the cycle's workspace picker**: this is the plan-approved alternative to adding Cargo, JavaScript, capability, and Rust registration for a native dialog plugin.
- **Renderer built-ins keep precedence**: a same-named Hub file cannot reinterpret a built-in command such as `/plan`.
- **Workspace rules are local Markdown**: root `AGENTS.md`, `.nexus/rules.md`, and sorted `.nexus/rules/*.md` are included when present; missing files are omitted.
- **Explicit file tool calls define reflection writes**: write, create, edit, and delete paths are recorded; arbitrary filesystem mutations hidden inside terminal commands are not inferred.

---

## 7. Testing Summary

### Automated Tests

- Renderer tests prove workspace display, persistence, empty-path refusal, and `coding.session.start` transport.
- Runner tests prove per-session workspace scoping, no cwd fallback, relative-root rejection, and error-safe completion.
- Enrichment tests prove invoked-command-only loading, built-in precedence, scanner-blocked fallback, AGENTS/rule composition, scheduled parity, hook failure isolation, and reflection file tracking.
- Sidecar tool tests prove matching deny rules block mutation and malformed policy fails closed.
- Desktop full Vitest: 1368 passed, 0 failed across 157 files in one solo process.
- Root full Vitest: 5438 passed, 12 skipped, 0 failed across 516 passing files and 3 skipped files.
- Desktop lint, typecheck, root lint/build, web build, sidecar build, and architecture validation all exited zero.

### Manual Testing Performed

- Diff inspection confirmed the renderer does not start an IPC session before workspace validation and both live sidecar compositions import the shared enrichment wrapper.

### Manual Testing Still Needed

- [ ] Select a real project in a packaged Windows build and run a multi-tool coding task.
- [ ] Invoke a synced Hub command against a local model and inspect lifecycle telemetry end to end.

---

## 8. TODO Tracker

### Completed This Session

- [x] Persist and display the Coding workspace path.
- [x] Refuse missing or unsafe workspace roots.
- [x] Inject invoked Hub command bodies and workspace rules without catalog dumping.
- [x] Share enrichment and lifecycle hooks with scheduled runs.
- [x] Enforce `.nexus/permissions.deny` on sidecar headless tools.

### Remaining

- [ ] Implement v2.2.3 Phase 8.

### Out of Scope

- [ ] Native cross-platform folder dialog integration.
- [ ] Automatic execution of arbitrary Hub hook scripts.
- [ ] Inferring filesystem writes performed inside opaque terminal commands.

---

## 9. Summary and Next Steps

Phase 6 makes the desktop coding surface project-scoped and turns Hub discovery into real, bounded headless prompt behavior with shared policy and lifecycle enforcement. The next step is to bank this phase atomically, then execute Phase 8 architecture, known-gaps, and CI reconciliation.
