# Development Log: v2.2.8 Phase 5 - Hub Latest Apply

**Date**: 2026-08-24
**Operator**: Project owner
**Assisted by**: Cursor Grok 4.6
**Objective**: Apply GitHub-latest Nexus-Hub without turning the injection scanner off: quarantine high-severity skills, enable the rest, move Active to the fetched tag.
**Outcome**: `NexusHubSyncer.sync({ apply: true })` no longer fails the whole bundle on a high finding. High-severity skills move to `catalog/quarantine/` with `index.json`. Clean skills stay under `skills/`. A scanner crash still fails closed and keeps the previous Active tag. Concurrent Sync now is single-flight. Settings copy uses "Sync blocked" only when apply did not happen.

---

## 1. Starting State

- **Branch**: `develop` (v2.2.8 Phase 4 commit `3b557c0`)
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.8-working-local-studio.md`
- **Prior session**: `docs/archive/v2/v2.2/development/history/2026-08-24_v2.2.8-phase-4-models-catalog-parity.md`

Field Active stayed at 3.12.0 because one high scanner finding refused `--apply` for the entire 3.12.0-to-latest delta. The scanner is a product lock and stayed on.

---

## 2. Chronological Steps

### 2.1 Apply with quarantine (T011, T012)

**Plan specification**: Quarantine high-severity skills, apply the rest, set Active to latest. Keep `PromptInjectionScanner`. "Sync blocked" only for hard fail-closed cases. Pack-time snapshot still refuses stale 3.12.0. Do not allowlist a planted jailbreak. Re-review the allowlist stamp against GitHub latest.

**What happened**: After scan, `quarantineHighSeveritySkills` moves each high-severity skill dir from staged `skills/` to `catalog/quarantine/<relPath>/` and writes `quarantine/index.json`. `--apply` then swaps the staged catalog including quarantine. `scan.decision === "block"` no longer skips the swap. Scanner throw or a quarantine-move throw deletes staging and does not apply. Module-level `syncFlight` coalesces overlapping `sync()` calls across `new NexusHubSyncer({})` instances. Allowlist re-stamped `HUB_ALLOWLIST_REVIEWED_TAG = "v3.20.1"` (GitHub latest on 2026-08-24); existing nine (skill, rule) waivers unchanged; planted jailbreak not added. `ipcSkillsClient.syncNow` throws only when `blocked && !applied`. `skills.sync` summary includes `; quarantined N`. `readHubCatalog` walks nested Hub folders and lists quarantine index rows as `active: false` with `quarantine.decision === "block"`. Installer CLI treats applied-with-quarantine as success. `nexus skills sync` no longer exits 1 on scanner findings when apply still ran.

**Key files changed**: `core/skills/NexusHubSyncer.ts`, `core/skills/hubSkillScanAllowlist.ts`, `desktop/sidecar/src/handlers.ts`, `desktop/sidecar/src/protocol.ts`, `desktop/sidecar/src/skills/hubSkillReader.ts`, `desktop/sidecar/src/cli/hubCatalogCli.ts`, `desktop/src/pages/settings/ipcSkillsClient.ts`, `bin/nexus.mjs`

**Verification**: Root vitest 69/69 (NexusHubSyncer 44, PromptInjectionScanner 25). Desktop vitest 52/52 (ipcSkillsClient, hub-catalog-phase3, SkillsSettings). Installer `test_hub_catalog_provisioner` 15 passed (stale 3.12.0 still refused). Desktop `tsc --noEmit` pass. Desktop eslint on touched files pass.

### 2.2 Testing and Stabilization

Planted-jailbreak fixture among many clean skills: apply true, Active is fixture tag, evil under quarantine not skills. Scanner crash keeps previous Active. Overlapping sync clones once. SkillsSettings shows `Synced v3.21.0 (... quarantined 1)` without blocked copy.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| root vitest (NexusHubSyncer + PromptInjectionScanner) | PASS 69/69 |
| desktop vitest (ipcSkillsClient, hub-catalog, SkillsSettings) | PASS 52/52 |
| desktop `tsc --noEmit` | PASS |
| desktop eslint on touched files | PASS |
| installer pytest hub catalog provisioner | PASS 15 |
| Scanner still on | Yes (`PromptInjectionScanner` + allowlist) |
| Planted jailbreak allowlisted | No |
| Stale 3.12.0 pack refused | Yes |
| package.json bump | Not done |
| Live Hub clone in CI | Not added |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Packaged Update now / Active = GitHub latest unproven | P2 | DF-35 |
| Packaged live Hub clone still unproven | P2 | DF-23 (carried) |
| Agents folders still overlay-only | P3 | DF-33 |
| Packaged Settings vs installer not screenshotted | P2 | DF-34 |
| Packaged Hi still unproven | P2 | DF-32 |

---

## 5. Plan Discrepancies

- Live IPC method remains `skills.sync` (already minutes-class from v2.2.4), not a new alias.
- Nested Hub `skills/<category>/<slug>/` listing was one-level before this phase; the reader now walks category folders so the stability-gate "clean skills are listed" holds for the real Hub layout.

---

## 6. Assumptions Made

- GitHub `/releases/latest` for `bendourthe/Nexus-Hub` on 2026-08-24 is v3.20.1. The allowlist stamp tracks that review; it is not a pin and does not expand waivers.
- Medium/low findings stay enabled. Only `severity === "high"` moves to quarantine.
- Concurrent Sync now sharing one in-flight promise is correct because production always targets the same catalog root.

---

## 7. Testing Summary

Automated only. Human QA is deferred to the last phase.

- One planted jailbreak among many clean skills: apply, Active tag, quarantine path, clean skills remain
- Non-allowlisted twin of an allowlisted security snippet still quarantines
- Allowlisted Hub security skill still applies
- Scanner crash: previous Active kept
- Single-flight overlapping sync
- ipcSkillsClient: fail-closed still throws; applied+quarantine does not
- Hub CLI: applied+quarantine is success; fetched-but-not-applied still fails
- Listing: nested clean skill enabled; quarantine index row not enabled
- Pack-time 3.12.0 refusal unchanged

---

## 8. TODO Tracker

| Item | Status |
|---|---|
| T011 Latest apply with per-skill quarantine | Done |
| T012 Scanner on; planted jailbreak not enabled | Done |
| Phase 6 architecture, known-gaps, CI | Next |

---

## 9. Summary and Next Steps

Phase 5 makes Hub latest apply with the scanner on: high-severity skills quarantine, clean skills land, Active moves. Next: Phase 6 (architecture, known-gaps including DF-2 observed, CI coverage of T001-T012, Goal-vs-codebase, then `/update release` with confirmation gates).
