# Session History - v2.2.9 Phase 5: Catalog Identity (Installer = Settings)

**Date**: 2026-08-26
**Plan**: [v2.2.9-field-chrome-catalog-and-generate.md](../../plans/v2.2.9-field-chrome-catalog-and-generate.md) (Phase 5)
**Driver**: `/implement in-full` (Phase 5 of 7)

## What was implemented

- **5.1 (T010)**: An Embeddings tab now precedes Chat in both installer `TYPE_TABS` and desktop `CATALOG_TAB_DEFS`; `embed` task/type rows (Nomic, EmbeddingGemma, Qwen3-Embedding) land there instead of Chat. Both surfaces render one header row - display name, then pills in the locked order `Company / Country / Agentic / Context window / Multimodal / Guardrails / License / Released`. Derivation is shared and dual-asserted from `tests/fixtures/v2.2.9-model-pills.json` (7 golden cases; WN-7 discipline): Company from `FAMILY_TO_PUBLISHER` (unmapped or Community-mapped families omit the pill; "Community" is never rendered), Country from origin, Multimodal from modalities/vision, Guardrails from the `uncensored` tri-state, Released as ASCII en-US "Month YYYY". Installer keeps checkbox / Why this one / Recommended; Settings keeps Download / Downloaded / Remove, the downloaded highlight, and a Details disclosure. New TS mirror module `desktop/src/shared/models/modelPills.ts`; new Qt `derive_fact_pills` + flow layout in `typed_catalog.py`.
- **5.2 (T011)**: The installer no longer hides `patient-tier` rows - Inkling-Small shows in the wizard (still absent from every `recommended.json` tier, asserted). Settings `visibleModelsOnTab` partitions downloaded-and-ready first, each partition in installer `collapse_and_sort` rank (Gemma, LFM, gpt-oss becomes Gemma, gpt-oss, LFM once gpt-oss downloads); the installer keeps pure installer order. New `downloaded_first` helper in `catalog_tab_sort.py`; new fixture `tests/fixtures/v2.2.9-catalog-tab-sort.json` dual-asserted Python + TS.

Golden pill example (identical string asserted in Qt and React) - Gemma 4 12B: `Company: Google | Country: USA | Agentic: Yes | Context window: 262k tokens | Multimodal: Yes | Guardrails: Censored | License: Gemma Terms of Use | Released: May 2026`.

## Files changed

Installer: `typed_catalog.py`, `catalog_tab_sort.py`; tests `test_model_pills.py` (new), `test_typed_catalog.py`, `test_catalog_tab_sort.py`, `test_patient_tier_gate.py`, `test_phase6_shell.py`.
Desktop: `catalogTabs.ts`, `modelPills.ts` (new), `ModelsSettings.tsx`; tests `modelPills.test.ts` (new), `catalogTabs.test.ts`, `ModelsSettings.test.tsx`.
Fixtures: `v2.2.9-model-pills.json` (new), `v2.2.9-catalog-tab-sort.json` (new), `v2.2.8-catalog-tab-sort.json` (nomic moved to the embeddings key).

## Test results

- Installer: `uv run pytest -q` in `scripts/installer` - 1120 passed, 3 pre-existing opt-in skips, exit 0 (orchestrator re-run confirmed exit 0).
- `npm run lint:shell`: 0 errors.
- `npm run test:shell`: 181 files / 1587 tests passed (boundary re-run).
- Root suite not run: `core/` untouched this phase (the installer-side hide removal alone satisfies same-rows; Settings never gated patient-tier).

## Deviations

None marked. Judgment calls within the locked rules: Community-mapped families (kokoro, piper) omit the Company pill per "never invent Community"; split in/out context windows render the in-window value in the single-value locked format.

## Next steps

Phase 6 (Hub sync UX): tag normalize at both compare sites, compact Sync now with orb + staged status, quarantine explanation copy.
