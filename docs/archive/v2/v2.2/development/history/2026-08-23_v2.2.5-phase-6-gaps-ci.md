# Development Log: v2.2.5 Phase 6 - Architecture, known-gaps, and CI

**Date**: 2026-08-23
**Operator**: Project owner
**Objective**: Reconcile architecture, known gaps, and CI after Phases 1-5. Do not bump package.json. Then commit, push develop, and hand `/update release` its own confirmation gates.
**Outcome**: No file moves. `modelAliases.ts` stays the single id table. BG-43 through BG-50 are recorded with evidence. DF-2 and DF-4 stay open. CI comments name the v2.2.5 contracts. Windows, macOS, and Linux pack scripts share the Python latest-tag snapshot gate. Unsigned installer rebuild and tagged release are not part of this commit.

---

## 1. Starting State

- **Branch**: `develop`
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.5-first-successful-generation.md`
- **Prior session reference**: `docs/archive/v2/v2.2/development/history/2026-08-23_v2.2.5-phase-5-hub-latest.md`

Phases 1-5 were committed locally. Remaining work was Phase 6 reconciliation, push, then `/update release` behind confirmation.

---

## 2. Chronological Steps

### 2.1 Architecture refactor (6.1)

**Plan specification**: Identify empty dirs, duplicates, and leftover model-id shims. Propose-then-apply. Do not bump package.json.

**What happened**: Phase 1 shipped `core/registry/modelAliases.ts` as the canonical table, not a temporary shim. Chat/Agents still fold through that module. No duplicate catalog files to collapse. Docs already live under `docs/archive/v2/v2.2/`. No tracked empty dirs or obsolete files were moved this phase.

**Key files changed**: none for layout (README ledger plus What's new only)

### 2.2 Known-gaps (T020)

**Plan specification**: Add BG-43 through BG-50. Close only with evidence. Keep DF-2 and DF-4 unless packaged soak / GPU generate ran. not_observed != absent.

**What happened**: BG-43 (dual ids) added as resolved at unit plus live Ollama Hi. BG-44 through BG-50 were already recorded from earlier phases. DF-2 and DF-4 remain open. DF-23 still covers packaged Hub Update. DF-24 still covers Unix selection-snapshot complete-path (unchanged).

**Key files changed**: `docs/archive/v2/v2.2/known-gaps.md`

### 2.3 CI (T021)

**Plan specification**: Cover alias, Models overflow/badge, Hub allowlist, snapshot-latest stubs. Path filters, cancel-in-progress, cache. No live GPU or live Hub clone in PR CI.

**What happened**: `ci.yml` already runs root vitest (aliases, scanner) and `npm run test:shell` on develop Node 22 (Models, FolderTree, ChatPage, hub-catalog stubs). `installer-tests.yml` already path-filters `scripts/installer/**` (snapshot-latest pytest). Comments now name the v2.2.5 contracts. Did not add a GPU job or a Hub clone.

**Key files changed**: `.github/workflows/ci.yml`, `.github/workflows/installer-tests.yml`

### 2.4 Cross-installer Hub snapshot (6.4)

**Plan specification**: If multiple installers, Hub snapshot latest-resolution must match. Unix skip is a known-gap, not claimed parity.

**What happened**: `resolve_latest_tag` lives in shared `build-hub-snapshot.py`. Windows, macOS, and Linux pack scripts all call it and clear `hub-snapshot/` on stale-tag failure. Selection-snapshot Unix complete-path remains DF-24.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Prior phase tests (1-5) | PASS in those phase commits |
| Layout moves | none |
| Package version bump | Not done (plan lock; `/update release` owns it) |
| Packaged Explorer / GPU generate | not_observed (DF-2, DF-4) |
| Unsigned Windows installer rebuild | deferred to after push (operator verification) |

---

## 4. Decisions

- Did not invent a second alias module or merge FALLBACK_LLMS into the catalog. Placeholders stay `installed: false`.
- README What's new for v2.2.5 is written now so `/update release` does not ship a version-only README bump.

---

## 5. Residual risk

- Operator field checklist in the plan (packaged Hi, Settings scroll, image generate, Hub Active latest) is still required before calling the product done.
- `/update release` must not silently bump `package.json` 2.1.0 or `desktop/package.json` 1.5.0 without the operator confirming the tag.
