# Development Log: v2.2.3 Phase 7 - Installer Document, taskbar, Complete

**Date**: 2026-08-22
**Operator**: Project owner
**Assisted by**: Codex
**Objective**: Finish the field-day installer requests by pre-selecting OCR, matching the transparent app taskbar mark, and advancing automatically to a compact Complete page.
**Outcome**: The recommendation matrix covers Document on every tier, the taskbar canvas is transparent, and successful installs reach Complete without an extra Next.

---

## 1. Starting State

- **Branch**: `develop`
- **Starting commit**: `a443f07`
- **Environment**: Windows, Python installer managed by uv, PyQt5
- **Prior session reference**: `docs/archive/v2/v2.2/development/history/2026-08-22_v2.2.3-phase-2-glass-beam-and-icon-actions.md`
- **Plan reference**: `docs/archive/v2/v2.2/plans/v2.2.3-glass-orbs-and-pillar-runtime.md`

Phase 7 code was implemented but uncommitted at continuation start and occupied a path set disjoint from the desktop renderer phases.

---

## 2. Chronological Steps

### 2.1 Document OCR defaults

**Plan specification**: Add Document as multi-pick, use RapidOCR on CPU and 8 GB, and use Unlimited-OCR 3B on 12, 16, and 24 GB.

**What happened**: `SECTION_ORDER` and all five `recommended.json` tiers now include Document. Tests assert the exact low/high tier split and verify Document never joins `SINGLE_PICK_SECTIONS`.

**Key files changed**: `core/registry/recommended.json`, `scripts/installer/src/nexus_installer/tier_defaults.py`, `scripts/installer/tests/test_tier_defaults.py`

### 2.2 Transparent Windows taskbar tile

**Plan specification**: Prefer the no-background asset and stop filling the taskbar canvas navy.

**What happened**: `_render_taskbar_tile` paints on `canvas.fill(0)`, prefers `nexus-ai-primary_no-background.png`, and preserves `icon.png` as fallback. Pixel-level tests cover preference and alpha behavior.

**Key files changed**: `scripts/installer/src/nexus_installer/widgets/win_titlebar.py`, `scripts/installer/tests/test_win_titlebar.py`

### 2.3 Successful auto-advance and compact Complete

**Plan specification**: Auto-advance after success only, unpin the stepper, remove wasted margins, prevent label clipping, and shrink Copy locally.

**What happened**: `_on_install_finished(True)` schedules `_advance_to_complete`; false remains on Installing. Complete cards and rows are compacted, the service name width is 200px, and `_CommandRow` applies a local 24px Copy rule without touching the global button height.

**Key files changed**: `scripts/installer/src/nexus_installer/window.py`, `scripts/installer/src/nexus_installer/pages/complete.py`, `scripts/installer/tests/test_phase6_shell.py`, `scripts/installer/tests/test_complete.py`

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Installer full pytest | PASS: 1101 passed, 3 skipped, 0 failed |
| Installer ruff format | PASS: all touched files format-clean |
| Installer ruff lint | PASS for Phase 7 source and new test paths; inherited SIM117 remains in `tests/test_complete.py:13` as WN-1 |
| Git ignore audit | PASS: 0 patterns added; no executable or cache introduced |
| CI/CD readiness | PASS: `installer-tests.yml` includes `scripts/installer/**`; shared registry changes are also covered by repository tests |
| Docs cleanup audit | PASS: no scratch docs, duplicate pages, or moves proposed |
| Frozen `NexusSetup.exe` rebuild | NOT RUN: explicitly out of scope unless requested |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Frozen installer visual and taskbar acceptance | P2 | Not proven here; automated PyQt and pixel tests are the supported evidence |
| Pre-existing `SIM117` nested patch contexts in `test_complete.py` | Cosmetic | Retained unchanged and recorded as WN-1 |

---

## 5. Plan Discrepancies

None. The implementation follows the corrected global-button-height finding and keeps the Complete override local.

---

## 6. Assumptions Made

- **Failure includes user cancellation**: `finish(False)` remains on Installing and is never presented as successful completion.
- **Transparent PNG is the primary taskbar source**: `icon.png` remains a fallback only when the no-background asset is absent.

---

## 7. Testing Summary

### Automated Tests

- Fresh installer pytest: 1101 passed, 3 skipped, 0 failed.
- Ruff format: all touched Phase 7 files passed.
- Ruff lint: all Phase 7 source files and new test paths passed; one pre-existing `SIM117` outside the Phase 7 hunks remains in `test_complete.py` and is tracked as WN-1.

### Manual Testing Performed

- Diff inspection confirmed the global `BUTTON_HEIGHT` is untouched and taskbar rendering no longer imports or fills `_TASKBAR_TILE_BG`.

### Manual Testing Still Needed

- [ ] Rebuild and launch `NexusSetup.exe` on Windows only if the operator requests a frozen installer rehearsal.

---

## 8. TODO Tracker

### Completed This Session

- [x] Pre-select a Document OCR model on every hardware tier.
- [x] Render the taskbar tile on a transparent canvas with the correct asset preference.
- [x] Auto-advance successful installs and compact the Complete page locally.

### Remaining

- [ ] Implement v2.2.3 Phases 3, 4, 5, 6, and 8.

### Out of Scope

- [ ] Rebuild `NexusSetup.exe` or run a clean-VM installer rehearsal.

---

## 9. Summary and Next Steps

Phase 7 closes the remaining installer field requests without broad theme or packaging changes. The immediate next step is the fresh full pytest, ruff, staged-diff, and atomic commit gate, then Phase 3 generation honesty.
