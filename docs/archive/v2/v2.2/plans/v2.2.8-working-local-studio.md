# Plan -- Working Local Studio, Unified History, and Catalog Parity

**Project**: Nexus AI Studio
**Version**: v2.2.8
**Slug**: working-local-studio
**Filename**: v2.2.8-working-local-studio.md
**Plan Type**: Feature / Enhancement (patch follow-on)
**Created**: 2026-08-24
**Predecessor**: [v2.2.7-context-meter-and-transcript-chrome.md](v2.2.7-context-meter-and-transcript-chrome.md)
**Seeded from**: operator packaged-Explorer screenshots after v2.2.6 (2026-08-24). Not a comparison report. Discovery answers were taken from that field session (goal, in/out scope, affected areas, constraints, definition of done, testing).
**Goal**: By the end of this cycle a packaged Windows build can complete a local Chatbot turn, an Agents turn, an Image Studio generate, and a Video Lab generate without `sidecar response timeout`, with Nexus-Hub Active matching GitHub latest, Settings Models identical in sort/compactness/downloaded state to the installer, and the four tabs sharing Chatbot history chrome plus the thinking-orb quality the operator asked for.

## Overview

v2.2.6 landed named Image/Video sessions and Agents resume at unit evidence. The 2026-08-24 packaged session proved Explorer launches, then failed the product bar: Chatbot `Hi` showed Composing and died with `(chat unavailable) sidecar response timeout`; Settings listed installed Gemma 4 as Download; Hub Active stayed 3.12.0 and Sync now was blocked by the injection scanner; Image/Video/Agents history chrome did not match Chatbot; collapsed chats became an empty 24px strip; delete used a square flash-red button; the rail sat flush under the title bar; the in-bubble orb was small, left-aligned, and not the [thinking-orbs grammar](https://orbs.jakubantalik.com/).

v2.2.7 (amended the same day) owns the Context pill, moving the model picker under the composer, `<val>k` chips, transcript clocks, and removing the header `/` and gear glyph. This plan does not re-do that chrome.

This cycle is the field-repair that v2.2.5 and v2.2.0 promised and did not finish: generation RPCs that outlive a cold local model, an installed-probe that sees Ollama tags, Hub latest that applies without turning the scanner off, one history pane on all four tabs, installer-identical Models, and orbs that read as the reference. Delivery is the existing Tauri app, Node sidecar, and Python installer. Success is the operator field checklist at the bottom, not another unit-only close of DF-4.

## Constitution Check

No constitution file found at `docs/archive/v2/v2.2/constitution.md` (or `docs/constitution.md`). Skipping the MUST/SHOULD gate. Recommend `/constitution` if this project wants a durable principle file. This is informational, not blocking.

Grounding that was read instead:

- `AGENTS.md` (local-first, honest capability claims, `not_observed != absent`)
- `docs/archive/v2/v2.2/known-gaps.md` (DF-2 packaged Explorer, DF-4 live GPU, DF-23 Hub apply, DF-25 packaged persist, DF-26 video remount continueFrom, DF-27 Agents tool cards)
- `desktop/src-tauri/src/sidecar.rs` `rpc_timeout_for`: default 15s, including `chat.send` (unit test asserts 15s). Hub `skills.sync` is the only 10-minute exception. A local Gemma 4 12B cold prompt routinely exceeds 15s, which matches screenshot 6.
- `desktop/src/modules/chat/ChatPage.tsx`: collapsed chats render `null` inside a 24px aside (`CHATS_PANE_COLLAPSED_WIDTH = 24`). v2.2.7 removed the header `Breadcrumb` slash and unicode gear.
- `desktop/src/components/Sidebar.tsx`: v2.2.4 lock "Chatbot is the first row with no spacer gap above it". Operator now wants a small top margin.
- `desktop/src/modules/chat/FolderTree.tsx` confirm-delete: `backgroundColor: var(--status-err)` square button.
- `desktop/src/shared/explorer/StudioHistoryPane.tsx`: reuses FolderTree with Sessions copy, 240px, no collapse-to-icon rail. Agents still use `SessionListPanel` (flat), not FolderTree. v2.2.6 Goal said four tabs match Chatbot; Phase 4 narrowed Agents to rename/delete on the flat list.
- `desktop/src/shared/chat/MessageBubble.tsx`: chat pending orb is `size="inline"` (20px) in a row, not centered in the bubble. `orbEngine.ts` reverse-engineers a dotted ring; v2.2.3 forbade `npm install thinking-orbs`.
- `desktop/src/pages/settings/ModelsSettings.tsx`: large description cards; `downloaded` requires `item.installed && source !== "catalog-only"`. Installer `typed_catalog.py` hides `hideBelowVramGB`, collapses families, sorts required / recommended / compatible then over-budget grayed.
- `desktop/src/pages/settings/ipcSkillsClient.ts`: `Sync blocked by the injection scanner: +N new, ~M modified, -K removed`. Scanner stays on (product lock). Apply currently fails the whole bundle.

No `STRATEGY.md` / `docs/STRATEGY.md` and no `docs/solutions/` store.

Persona: the same local-first laptop user as v2.2.5-v2.2.7, now needing the packaged app to actually answer, generate, and show Hub latest.

## Current model map

**Model map status**: fresh as of 2026-08-24; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.7-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.7-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.7-flash` | `composer-2.5-fast` |

A distinct Gemini 3.7 Pro API slug was not confirmed from official docs in this session, so Flash fills every Google cell rather than inventing a Pro id.

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/about-claude/models/overview, https://platform.claude.com/docs/en/about-claude/models/choosing-a-model
- OpenAI: https://developers.openai.com/api/docs/models, https://developers.openai.com/api/docs/guides/latest-model
- Google: https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/, https://docs.cloud.google.com/gemini-enterprise-agent-platform/models/gemini/3-7-flash
- Cursor: https://cursor.com/docs/models-and-pricing, https://cursor.com/docs/models/grok-4-6

## Review verdict (this session)

### v2.2.6 miss-plan vs miss-implement

| Field item | Verdict | Evidence |
|---|---|---|
| Image/Video named history + last-output follow-up | Implemented at unit evidence. Packaged persist still DF-25. | BG-51, BG-52, `StudioHistoryPane` |
| Four tabs share Chatbot history chrome | Miss-planned in Phase 4. Goal said matching Chatbot folders/organize; Phase 4 made Agents chrome optional and shipped `SessionListPanel`. Image/Video reuse FolderTree but 240px, different empty copy, no collapse-to-icons. | v2.2.6 locked decisions vs Phase 4 prompt; screenshots 2 and 4 |
| Context meter + picker under composer | Out of scope on purpose. | v2.2.6 residual; now v2.2.7 amendment |
| Sidecar timeout on `Hi` | Not in v2.2.6. Pre-existing 15s RPC default. | `sidecar.rs` `RPC_TIMEOUT_DEFAULT` |
| Gemma 4 showing Download | Not in v2.2.6. v2.2.5 BG-45 covered Qwen snapshot Retry, not a live Ollama `gemma4:12b` miss in packaged Settings. | screenshot 7 |
| Hub 3.12.0 + scanner block | Not in v2.2.6. v2.2.5 refused stale pack snapshots and kept the scanner; apply still fail-closes the whole 3.21.0 delta (DF-23). | screenshots 9-10 |
| Orbs, rail margin, delete dialog, collapsed chats | Not in v2.2.6. Orbs were v2.2.3 (reverse-engineered, inline 20px). Collapsed chats were v2.2.5 BG-48 (24px pill only). | screenshots 1-3, 5 |

Packaged Explorer launch (DF-2) is now **observed** in this field session. Do not keep writing it as unproven. The remaining failure is generation RPC timeout and Hub apply, not a blank window.

### What v2.2.7 will resolve (do not duplicate here)

Context `<val>k` chips; pill Context meter + model picker under all four composers; date/time/token transcript chrome; removal of header `/` and gear glyph. Finish v2.2.7 on `develop` before this cycle's Phase 1.

### What this plan must resolve

Sidecar/long-running local inference; installed-model honesty; installer=Settings Models identity and picker sort; Hub latest apply with scanner on; unified history + collapsed icon rail + modern delete; rail top margin; orb centering and quality.

## Project health

| Surface | Status | Detail |
|---|---|---|
| Git version control | OK | Repo present with history on `develop` |
| Version number | OK | Plan version v2.2.8; do not bump root `package.json` (2.2.5) or `desktop/package.json` (1.5.0) until `/update release` |
| Branch model | OK | `develop` + `main` in use |
| Baseline docs | OK | README, CHANGELOG, known-gaps, per-version plans |
| Per-version docs tree | OK | `docs/archive/v2/v2.2/plans/` present |

## Locked product decisions

- **Do not start** until v2.2.7 is complete on `develop`. Do not re-run v2.2.0-v2.2.7 as greenfield.
- **Long-running RPCs**: `chat.send`, coding send, image generate, and video generate must not share the 15s default timeout. Streaming methods stay open for the stream (or a documented minutes-class timeout). `sidecar response timeout` is only for a dead/hung sidecar, never for a slow first token. Do not raise every RPC to 10 minutes.
- **Installed means installed**: if Ollama `/api/tags` has `gemma4:12b` (or an alias of `gemma-4-12b-it-gguf`), Settings shows Downloaded/Ready, not Download. Snapshot-selected-but-missing stays Retry (v2.2.5 lock).
- **One Models contract**: installer Models tab and desktop Settings Models tab share sort, family collapse, `hideBelowVramGB`, compact row density, over-budget grayed-at-end, and downloaded highlight. If Settings shows a row, the installer shows that row (or both hide it for the same VRAM reason). Tab pickers (`QuickModelSwitcher`) use that same order.
- **Hub latest**: Active tag matches GitHub `/releases/latest`. `PromptInjectionScanner` stays on. Apply must not fail the whole bundle: clean skills apply, flagged skills quarantine, operator sees counts, Active still moves to latest.
- **Four history panes**: Chatbot, Agents, Images, Videos use the same FolderTree chrome (folders, rename, delete, new session, collapse). Collapsed state is an icon rail (new/folder plus per-session marks), not an empty 24px gutter. Delete confirm is a rounded quiet dialog, not a square flash-red OS button.
- **Rail**: small top margin under the title bar so Chatbot is not flush. Do not bring back the old brand block.
- **Orbs**: keep `AgentStateOrb` / `orbEngine`. Do not `npm install thinking-orbs`. Chat/Agents pending orbs are centered in the bubble at a size that reads as the reference playground, not a 20px left-aligned spinner. Image/Video pending stays hero. Reduced-motion: static ring.
- **Cold start Chatbot**. Peeking a tab never loads weights. Tab labels: Chatbot, Agents, Images, Videos. Tools always on.
- **Do not bump** package.json. **Do not disable** the scanner. **No live GPU in PR CI**.

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|-------|-------|---------|------------------------|--------------------------|
| 1 | Local inference that finishes | Packaged Hi/Agents complete; no 15s send timeout; Gemma shows Downloaded | frontier | max |
| 2 | Unified history chrome | Four tabs share FolderTree; collapsed icon rail; modern delete | frontier | high |
| 3 | Rail margin and thinking orbs | Small rail inset; centered orbs matching the reference grammar | frontier | high |
| 4 | Models catalog parity | Settings = installer compact sort; pickers match; incompatible grayed last | frontier | max |
| 5 | Hub latest apply | Active = GitHub latest; scanner on; quarantine not whole-bundle fail | frontier | max |
| 6 | Architecture, known-gaps, CI | Layout clean; gaps honest; field checklist | frontier | max |

---

## Task inventory (T###)

- T001 [P] [desktop/src-tauri sidecar.rs] Per-method RPC timeouts; streaming send/generate never use 15s. Tests for `chat.send` != 15s and `ping` still 15s.
- T002 [P] [ChatPage / sidecar chat] First-token slowness shows Composing until tokens or a typed Ollama/runtime error, not `(chat unavailable) sidecar response timeout`.
- T003 [P] [installedProbe / ModelsService] Packaged Ollama `gemma4:12b` lists as Downloaded for catalog id `gemma-4-12b-it-gguf`.
- T004 [P] [FolderTree + CodingPage + StudioHistoryPane] One history chrome; Agents drop flat SessionList as the only pane.
- T005 [P] [ChatPage chats aside] Collapsed width shows new/folder plus session icons, not `null`.
- T006 [P] [FolderTree confirm dialog] Rounded, quiet destructive, not square flash-red.
- T007 [P] [Sidebar.tsx] Small top padding under the title bar.
- T008 [P] [AgentStateOrb / MessageBubble] Centered composing orb; raise motion toward the reference without the npm package.
- T009 [P] [catalogTabs + typed_catalog] Shared sort/filter; compact Settings cards; downloaded highlight; over-budget gray last; installer shows the same set.
- T010 [P] [QuickModelSwitcher] Same order as installer/Settings for that task tab.
- T011 [P] [NexusHubSyncer + SkillsSettings] Latest apply with per-skill quarantine; Active tag updates; scanner stays on.
- T012 [P] [installer hub snapshot] Pack uses latest; stale 3.12.0 still refused; install-time sync uses the same apply path as Settings.
- T013 [P] [docs/archive/v2/v2.2/known-gaps.md] Close DF-2 as observed; keep DF-4 until a GPU generate is recorded; add Hub/timeout rows only with evidence.
- T014 [P] [CI] Timeout, probe, FolderTree collapse, Models sort, Hub quarantine tests. No live GPU.

---

## Phase 1: Local Inference That Finishes

**Goal**: A packaged Chatbot `Hi` and an Agents turn complete against local Ollama. Settings shows installed Gemma as Downloaded. Sidecar death and slow generation are different errors.
**Prerequisites**: v2.2.7 complete on `develop`.
**Stability Gate**: Rust/unit: `rpc_timeout_for("chat.send")` is not 15s; `rpc_timeout_for("ping")` stays 15s. Desktop: fixture slow stream does not surface `sidecar response timeout`. Probe test: Ollama tag `gemma4:12b` marks catalog `gemma-4-12b-it-gguf` installed. Operator field: one packaged Hi completes or a typed `ollama` / weights error, never the 15s sidecar string.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Production RPC + Ollama residency + packaged probe. High blast radius if every IPC inherits a long timeout.

### Sub-tasks

#### 1.1 -- Streaming-safe RPC timeouts (T001, T002)

**Objective**: Long-running methods do not share `RPC_TIMEOUT_DEFAULT`. Chat/coding/image/video send or generate stay open for the event stream.

**Failure modes**: Malformed method name: default 15s. Sidecar process exit mid-stream: `Exited` with stderr tail, not Timeout. Concurrent ping during a long generate: ping still 15s and succeeds while generate continues.

**Build class**: load-bearing.

**Prompt**:
> Implement Nexus AI Studio v2.2.8 Phase 1.1 from docs/archive/v2/v2.2/plans/v2.2.8-working-local-studio.md. Follow AGENTS.md. Do not bump package.json. Do not re-run v2.2.7. Do not disable the Hub scanner.
>
> In `desktop/src-tauri/src/sidecar.rs`, stop using a 15s timeout for `chat.send` and the coding/image/video generate/send methods. Keep 15s for ping/status/list. Streaming RPCs wait for the stream to finish or a minutes-class cap documented in the test. ChatPage must not rewrite a slow first token into `(chat unavailable) sidecar response timeout`. Tests: timeout table, slow-stream fixture, ping during generate. Session history after 1.3.

#### 1.2 -- Installed Gemma is Downloaded (T003)

**Objective**: Packaged Settings and tab pickers treat alias-folded Ollama tags as installed.

**Failure modes**: Ollama down: catalog rows stay Download/Retry, not a crash. Unknown tag: not mapped onto Gemma. Snapshot-selected missing tag: Retry (v2.2.5).

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.8 Phase 1.2. Trace `installedProbe` + `ModelsService.list` for `gemma-4-12b-it-gguf` / `gemma4:12b`. A host whose `/api/tags` includes `gemma4:12b` must not show Download on that card. Tests without live GPU. Do not invent Compatible for over-budget rows.

#### 1.3 -- Testing and Stabilization

**Prompt**:
> Cargo tests for `rpc_timeout_for`, desktop/sidecar tests for probe aliases and the timeout copy. CI path filters. `/generate-session-history` for Phase 1. Do not ask the operator to GPU-generate yet.

### Phase 1 Exit Checklist

- [x] `chat.send` is not a 15s RPC
- [x] Ping stays short
- [x] Gemma alias shows Downloaded when the tag exists
- [x] Tests passing
- [x] Session history written

---

## Phase 2: Unified History Chrome

**Goal**: Chatbot, Agents, Images, and Videos share FolderTree organize/rename/delete. Collapsed history is an icon rail. Delete confirm is modern.
**Prerequisites**: Phase 1 (app must stay usable while chrome changes).
**Stability Gate**: CodingPage uses FolderTree (or the shared history pane) with folders + rename/delete. Collapsed chats aside still shows new/folder controls and is not an empty 24px strip. Confirm-delete testid keeps working; button is not square flash-red.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Four-tab chrome plus Agents session store mapping onto FolderTree. Easy to regress Chatbot explorer.

### Sub-tasks

#### 2.1 -- One FolderTree on four tabs (T004, T005)

**Objective**: Agents get the same left pane contract as Chatbot (folders optional, named sessions, rename/delete). Image/Video panes match width, collapse, and empty chrome. Collapsed state is an icon rail like the module sidebar (new session, new folder, per-session marks), not `chatsCollapsed ? null`.

**Failure modes**: Sidecar down: empty hint + banner, no fake rows. Delete while collapsed: still confirm. Agents sessions.json without folders: show a flat list inside FolderTree, not a second widget.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.8 Phase 2.1. Replace Agents `SessionListPanel` as the sole history UI with FolderTree (adapter over `coding.session.*` is fine). Align `StudioHistoryPane` width/collapse/copy with ChatPage chats aside. Collapsed chats/sessions must keep new-session and new-folder actions visible as icons. Persist collapse keys. Tests for collapse-not-empty, Agents rename/delete through FolderTree, Image/Video pane parity. Do not move Chatbot off `chat.explorer.*`.

#### 2.2 -- Modern delete confirm (T006)

**Objective**: Shared dialog: rounded card, quiet Cancel, rounded destructive Delete using status tokens, not a square `#d33` fill.

**Failure modes**: Esc/Cancel: no delete. Confirm: existing delete IPC. Missing label: still names the kind (chat/session/folder).

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.8 Phase 2.2. Restyle `folder-tree-confirm-delete` (and any Agents leftover confirm) to match the glass dialog language. Keep testids. Tests for cancel vs confirm. Session history after 2.3.

#### 2.3 -- Testing and Stabilization

**Prompt**:
> FolderTree, ChatPage, CodingPage, ImageStudioPage, VideoLabPage collapse/delete tests. CI. Session history for Phase 2.

### Phase 2 Exit Checklist

- [x] Agents history is FolderTree-class
- [x] Collapsed pane is not empty
- [x] Delete dialog is rounded and quiet
- [x] Tests passing
- [x] Session history written

---

## Phase 3: Rail Margin and Thinking Orbs

**Goal**: Module rail has a small top inset. Pending orbs are centered and read as the jakubantalik grammar without adding the npm package.
**Prerequisites**: Phase 2 (bubble layout stable).
**Stability Gate**: Sidebar module nav is not flush with the title bar (measured padding > 0). Chat pending orb is centered in the assistant bubble; `data-orb-size` is not `inline` 20px for Composing. Reduced-motion still static. No `thinking-orbs` in package.json.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Visual identity the operator already paid for in v2.2.3 and rejected in the field. Motion plus layout.

### Sub-tasks

#### 3.1 -- Rail inset (T007)

**Objective**: A few pixels of margin above Chatbot so the selected row is not glued to the frameless title bar. Do not restore the brand wordmark block.

**Failure modes**: Compact vs expanded both keep the inset. Settings route keeps the same rail.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.8 Phase 3.1. In `desktop/src/components/Sidebar.tsx`, add a small top padding on the module nav (or the sidebar column) so Chatbot is not flush. Update the v2.2.4 comment. Test the padding/style assertion.

#### 3.2 -- Centered reference-quality orbs (T008)

**Objective**: Match https://orbs.jakubantalik.com/ states (Working, Searching, Solving, Listening, Composing, Shaping) in motion grammar and presence. Chat/Agents: center in the bubble, larger than 20px. Image/Video: keep hero. Still no `thinking-orbs` package; improve `orbEngine` / `AgentStateOrb`.

**Failure modes**: Reduced-motion: static ring, caption remains. Missing activity: default Composing, do not throw. Streaming tokens: orb hides when content arrives.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.8 Phase 3.2. Center the pending orb in MessageBubble for chat/coding (`justifyContent: center`, full bubble width). Increase inline composing size so it is not a 20px mystery. Tighten `orbEngine` motion toward the reference (dot count, orbit, captions). Do not add the npm package. Tests: center styles, size, reduced-motion, no thinking-orbs dependency. Session history after 3.3.

#### 3.3 -- Testing and Stabilization

**Prompt**:
> Sidebar, MessageBubble, AgentStateOrb, Image/Video pending tests. CI. Session history for Phase 3.

### Phase 3 Exit Checklist

- [x] Rail has top margin
- [x] Composing orb is centered and larger than 20px
- [x] No thinking-orbs package
- [x] Tests passing
- [x] Session history written

---

## Phase 4: Models Catalog Parity

**Goal**: Settings Models is the installer Models tab: compact rows, same sort, same visibility, downloaded obvious, compatible-not-downloaded obvious, incompatible grayed at the end. Tab pickers use that order.
**Prerequisites**: Phase 1.2 (installed probe).
**Stability Gate**: Shared sort fixture: family collapse + hideBelowVram + required/recommended/compatible then over-budget produces the same id order in Python installer tests and `sortModelsOnTab` (or a shared JSON fixture both consume). Compact Settings card does not render the long description wall as the primary row. Over-budget rows are grayed and last. Picker order test matches that list for `taskType="llm"` / image / video.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Two UIs (PyQt installer + React Settings) plus four pickers. Drift is the bug the operator keeps seeing.

### Sub-tasks

#### 4.1 -- Shared sort and compact cards (T009, T010)

**Objective**: Extract one sort/filter contract used by `typed_catalog.py` and `catalogTabs.ts`. Settings cards become compact like the installer (name, badges, chips including `<val>k` from v2.2.7, size, VRAM). Downloaded/ready uses a quiet highlight (not a tiny muted "Downloaded" that looks like Download). Compatible-not-downloaded stays a clear Download. Over-budget is grayed, last, not Compatible. Installer must not hide a model Settings shows unless both apply `hideBelowVramGB`.

**Failure modes**: Missing VRAM: do not invent Compatible (v2.2.5). Missing contextWindow: omit chip (v2.2.7). Unknown family: still listed.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.8 Phase 4.1. Make installer Models and desktop Settings Models the same list: sort, family collapse, hideBelowVram, compact density, downloaded highlight, gray over-budget at the end. Feed `QuickModelSwitcher` on Chat/Agents/Images/Videos from that ordered list. Golden fixture shared or dual-asserted. Do not show Compatible on over-budget cards. Context chip stays `<val>k` from v2.2.7.

#### 4.2 -- Testing and Stabilization

**Prompt**:
> ModelsSettings, catalogTabs, installer typed_catalog, QuickModelSwitcher tests. Session history for Phase 4.

### Phase 4 Exit Checklist

- [x] Same id order installer vs Settings vs pickers
- [x] Compact Settings rows
- [x] Downloaded is obvious
- [x] Tests passing
- [x] Session history written

---

## Phase 5: Hub Latest Apply

**Goal**: Active Nexus-Hub is GitHub latest. Update now / Sync now / installer sync apply without disabling the scanner. Blocked skills quarantine; the rest land.
**Prerequisites**: Phase 1 (sidecar alive for a 10-minute sync).
**Stability Gate**: Fixture catalog with one planted jailbreak and many clean skills: apply reports quarantine count, Active tag is the latest fixture tag, clean skills are listed. Planted skill is not enabled. `PromptInjectionScanner` still on. Pack-time snapshot still refuses a stale 3.12.0 when latest is injected as 3.21.0-class.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Security gate plus catalog apply. Whole-bundle fail is why the field is stuck on 3.12.0.

### Sub-tasks

#### 5.1 -- Apply with quarantine (T011, T012)

**Objective**: `NexusHubSyncer` / `skills.sync` apply latest even when some skills scan high: those skills go to Quarantined, others enable, Active version updates. SkillsSettings shows the same. Installer provisioner uses this path. Re-review `HUB_SKILL_SCAN_ALLOWLIST` against the current latest tag; do not turn the scanner off. Do not allowlist a planted jailbreak.

**Failure modes**: Network fail: keep previous Active, typed error. Scanner process crash: fail closed, do not apply. Concurrent Sync now: single-flight.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.8 Phase 5.1. Stop failing the entire Hub apply because the 3.12.0-to-latest diff is large. Quarantine high-severity skills, apply the rest, set Active to latest. Keep `PromptInjectionScanner`. Update SkillsSettings copy so "Sync blocked" is only for hard fail-closed cases, not for a partial quarantine that still advanced the tag. Installer snapshot still refuses stale 3.12.0 at pack time; install-time sync uses the new apply path. Tests: planted jailbreak quarantined, clean skills applied, latest tag, allowlist does not disable the scanner.

#### 5.2 -- Testing and Stabilization

**Prompt**:
> NexusHubSyncer, ipcSkillsClient, SkillsSettings, hub catalog provisioner tests. Session history for Phase 5.

### Phase 5 Exit Checklist

- [x] Active can become latest with scanner on
- [x] Quarantine is per-skill
- [x] Stale 3.12.0 pack still refused
- [x] Tests passing
- [x] Session history written

---

## Phase 6: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, and its CI/CD complete and optimized, with independent evidence the plan Goal landed.
**Prerequisites**: All prior phases.
**Stability Gate**: Layout clean; known-gaps updated (DF-2 observed; DF-4 only if a GPU generate was recorded); CI covers T001-T012; field checklist items that need a GPU are listed honestly.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, Goal-vs-codebase review, and release gating.

### Sub-tasks

#### 6.1 -- Architecture refactor

**Objective**: Remove obsolete SessionList-only Agents chrome if FolderTree replaced it. Repair references.

**Prompt**:
> Identify obsolete files from the shared history extract and Hub apply split. Propose-then-apply. Chatbot stays on `chat.explorer.*`. Quote the scan in session history.

#### 6.2 -- Known-gaps reconciliation (T013)

**Prompt**:
> Update docs/archive/v2/v2.2/known-gaps.md. Close DF-2 (packaged Explorer was observed 2026-08-24). Close timeout/Downloaded/Hub-apply rows only with test or operator evidence. Keep DF-4 unless a GPU image/video generate was recorded. Keep DF-25/26/27 unless this cycle proved them.

#### 6.3 -- CI/CD (T014)

**Prompt**:
> Cover rpc_timeout_for, Gemma alias Downloaded, FolderTree collapse-not-empty, Models sort fixture, Hub quarantine apply. No live GPU. Path filters and concurrency cancel-in-progress.

#### 6.4 -- Cross-installer parity

**Prompt**:
> Settings vs installer Models order and Hub apply path must match. If Unix snapshot write is still DF-24, leave it. Silent no-op only if no installer files changed.

#### 6.5 -- Independent Goal-vs-codebase review

**Prompt**:
> Re-read this plan Goal. Inspect the tree as if this agent had not implemented the phases. Record whether packaged Chatbot/Agents/Image/Video can complete locally, whether Hub Active is latest, and whether the chrome locks landed. A miss is a known-gap, not a pass.

#### 6.6 -- Testing and Stabilization

**Prompt**:
> Full test suite. Session history for Phase 6. Rebuild the unsigned Windows installer. Stop before `/update release` unless asked. Push `develop` after the Phase 6 commit when the operator asked to push. Human field checklist is below; do not ask for GPU generate on earlier phases.

### Phase 6 Exit Checklist

- [x] Refactor done
- [x] known-gaps.md updated
- [x] CI covers new contracts
- [x] Goal-vs-codebase written
- [x] Session history written

---

## Complexity Tracking

> Fill ONLY if Constitution Check has violations that must be justified

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| | | |

---

## Out of scope

- Re-running v2.2.0-v2.2.7 as greenfield (v2.2.7 Context pill, picker under composer, `<val>k`, date/time/tokens, header `/` and gear).
- Context auto-summarize at 80%.
- `npm install thinking-orbs`.
- Bumping package.json.
- Disabling the Hub injection scanner.
- Live GPU in PR CI.
- Persisting Agents tool-call cards (DF-27) unless it falls out of FolderTree work; do not expand into a trace player.
- Video remount `priorJobId` (DF-26) unless a generate path already has the id.

## Residual risks

- First Ollama load can still take minutes; the UI must keep Composing and show a typed wait, not a 15s sidecar death.
- Hub latest may quarantine many skills; Active latest with a non-empty Quarantined list is success, not failure.
- Live GPU image/video remains hardware-gated. Unit tests cannot close DF-4.

## Operator field checklist (required before calling v2.2.8 done)

1. Packaged Chatbot: `Hi` completes with a local Gemma/Qwen reply. No `sidecar response timeout`.
2. Settings Models: installed Gemma shows Ready/Downloaded, compact, same order as the installer. Over-budget gray last.
3. Chat/Agents/Images/Videos pickers: same order as that list. Footer still has the v2.2.7 Context pill plus picker.
4. Images: one generate produces a visible PNG or a typed runtime-not-ready error (record skip if no GPU; do not claim generate).
5. Videos: same class as Images.
6. Agents: one turn completes in a named session; history pane matches Chatbot; collapsed pane still shows icons.
7. Skills: Active matches GitHub latest (or quarantined-partial with latest tag). Scanner still on. Update now does not whole-bundle fail.
8. Chrome: rail not flush; delete dialog rounded; composing orb centered.

## Copyable implementation prompt

```
Implement Nexus AI Studio v2.2.8 from docs/archive/v2/v2.2/plans/v2.2.8-working-local-studio.md. Follow AGENTS.md. Do not start until v2.2.7 is complete on develop. Do not re-run v2.2.0-v2.2.7 as greenfield. Do not bump package.json (root 2.2.5, desktop 1.5.0). Do not disable the Hub injection scanner. Do not npm install thinking-orbs. Do not run /update release unless asked.

PRODUCT LOCKS
- Cold start Chatbot. Peeking a tab never loads weights.
- Tab labels: Chatbot, Agents, Images, Videos.
- Tools always on.
- chat.send / generate must not use the 15s RPC default.
- Settings Models = installer Models (sort, compact, downloaded, gray over-budget).
- Hub apply: quarantine blocked skills, still move Active to latest.
- Four tabs share FolderTree; collapsed history is an icon rail.
- Orbs: centered, reference grammar, no thinking-orbs package.

HOW TO WORK
- /implement phase 1, then commit, then 2, commit, then 3, commit, then 4, commit, then 5, commit, then 6, commit, then push develop.
- After each phase: run that phase's tests, fix failures, write session history under docs/archive/v2/v2.2/development/history/.
- After Phase 6: remaining known-gaps, unsigned Windows installer, stop before /update release unless asked.
- ASCII commit messages. No Co-Authored-By.

Start at Phase 1.1. Stop at the phase stability gate. Phase 1 is not done while Hi still becomes sidecar response timeout. Phase 5 is not done if Update now still whole-bundle fails.
```
