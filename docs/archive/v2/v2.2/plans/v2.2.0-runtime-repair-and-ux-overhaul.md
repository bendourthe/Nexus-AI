# Plan - Runtime Repair and UX Overhaul

**Project**: Nexus AI Studio
**Version**: v2.2.0
**Slug**: runtime-repair-and-ux-overhaul
**Plan Type**: Feature / Enhancement
**Created**: 2026-08-21
**Goal**: A fresh install of Nexus AI Studio is fully functional out of the box (sidecar up, all downloaded models visible and usable, Nexus-Hub harness present), models switch intelligently on a single GPU, and the app presents a modern chat-first UI with a compact sidebar, integrated GPU monitor, redesigned chatbot, modernized settings, and local data export/import.

## Overview

The v2.1.0 installer reports success, but the installed app is functionally inert: the Tauri shell never packages the Node sidecar (`tauri.conf.json` has no `bundle.resources`), spawns bare `node` from PATH instead of the installer-provisioned Node, never ships `core/registry/catalog.json` into the sidecar bundle, and never connects the provisioned diffusion Python venv. Every IPC call returns `sidecar-not-running`, so Image Studio / Video Lab show no models despite 9 of 10 models verified on disk, the Skills page misreports "not yet synced", and the Nexus-Hub harness never arrives (it is a best-effort runtime fetch that also depends on the sidecar). This plan repairs that plumbing first, so the app becomes usable in the earliest phases.

On the repaired foundation, the plan then delivers the smart single-GPU model-switching policy (no switch on tab click; switch on submit; confirm only when another model is busy; scheduler-mediated cross-model use during agentic tasks), rebuilds the Local Chatbot as a modern chat-first module with persistent storage, optional projects/folders organization, and auto-generated chat titles, and modernizes the shell: brand header removed from the sidebar, compact icon-only sidebar mode, GPU monitor integrated into the sidebar, a unified composer with embedded +/mic/send controls, an Ask-Inbox notification bell, modernized settings controls, User Profile retired into Settings, and a data export/import feature for machine-to-machine transfer.

Success looks like: install on a clean machine, open the app, and every tab works immediately - chat responds and titles itself, Image Studio and Video Lab list and run the downloaded models with the smart switch policy, the Agentic tab exposes the full Nexus-Hub command/skill surface, and the whole UI reads as one modern design system.

## Field evidence (2026-08-22)

A rebuilt installer now runs `--healthcheck` (Phase 1.4 landed enough to fail loudly). The Complete page reports:

`Installed (health check FAILED -- fail: sidecar request error: The pipe is being closed. (os error 232) | stderr: [ / / Nodejs v22.11.0] -- the app backend cannot start`

That changes the Phase 1 diagnosis. Provisioned Node 22.11.0 **does** start. The child then dies, so the next JSON-RPC write hits a closed stdin (Windows `ERROR_NO_DATA` 232). Restart backend uses the same spawn path and cannot recover. The likely crash is still boot-time: `better-sqlite3` is external and must sit next to `sidecar/dist/main.js` in the NSIS resource tree; esbuild already documents that missing native addon as "the sidecar died at startup before answering a single request." Healthcheck currently joins only the last three stderr fragments, which is why the UI shows garbage instead of the real stack.

In the running app (same session):

- Settings Models shows the honest banner "The Nexus backend could not start" plus Restart / Show details. Installed/Available/External remain 0.
- GPU footer reads "GPU: unavailable" (expected while the sidecar is dead).
- Sidebar still shows the logo plus "Nexus AI Studio" wordmark (duplicate of the title bar). Compact toggle exists but the rail is still icon-plus-label, not the default icon-only bar.
- `ADMIN_ENTRIES` still contains **User Profile** pointing at `/profile`, which only `<Navigate>`s to Settings. User instruction: **delete the nav item**. Do not keep a tab whose only job is to open Settings.
- Approvals is a sidebar bell. Opening it shows a "Pending approvals" dialog with the same 232 error. The popover has **no X, no Esc, no click-outside**. It is `position: absolute; bottom: calc(100% + 4px)` over the bell, so the toggle target is covered and the box cannot be dismissed from any tab.
- Settings chrome is only partly restyled: Models Type/Family/Status use a Select, but Models Search is still a native `input type="search"`. Skill Optimizer (and the rest of the Settings tabs) still use flat unstyled text fields and gray buttons. User instruction: modernize **every** leftover native control on every Settings tab, not only the Models filters.

## Locked product decisions

Confirmed with the user on 2026-08-21, amended 2026-08-22 from the post-install session:

- **GPU switching**: smart policy. Tab clicks never load or unload. Submits classify: resident -> proceed; both models fit with >= 2 GB VRAM headroom -> co-reside via the existing `GpuScheduler.enqueuePanel`, no dialog; GPU idle -> auto-switch with a status chip; another module's job busy -> confirm dialog with a "remember for this session" option. Unknown/null VRAM telemetry -> confirm, never a silent swap. Agentic cross-model tool calls use the same gate, automated once consented.
- **Voice**: folded into the composer mic button (click = push-to-talk; menu holds Voice loop / VAD / Hold to talk). Persona moves to a per-chat settings popover, persisted.
- **Ask Inbox**: leaves the nav. A quiet bell (badge only when pending > 0) is allowed; **the popover must be dismissable** (X, Escape, click-outside) and must never cover its own toggle. Sidecar-down: do not auto-open, do not pin a red 232 dialog on every tab; the existing Settings backend banner is enough. Full list lives under Settings > Approvals. No auto-approve. Schedules stay in Settings.
- **User Profile**: **remove the sidebar item entirely**. No tab that redirects to Settings. Display name lives under Settings > General (or a Profile *settings section*, not a second nav entry). Silent `/profile` bookmark redirect is fine.
- **Sidebar**: brand block removed (title bar keeps the single brand, without the accent-reactive outline); compact icon-rail is the **default**, persisted, with expand toggle.
- **Settings chrome**: every Settings tab (Models, Skills, Skill Optimizer, Credentials, Local API, Fine-tuning, MCP, Security, Data, Approvals, General) uses the shared Select / SearchInput / Button / Switch primitives. No leftover native search boxes, flat text fields, or unstyled Preview buttons.
- **Hub harness**: available offline via a checksummed bundled snapshot, refreshed by installer-time fetch when online and by Sync thereafter; user-authored skills are never wiped by a re-sync.
- **Release**: one plan, v2.2.0, plumbing phases first.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at docs/v2.2.0/constitution.md - skipping check. Recommend running /constitution to establish project principles.

## Current model map

**Model map status**: fresh as of 2026-08-21; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.5-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/about-claude/models/overview
- OpenAI: https://developers.openai.com/api/docs/models
- Google: https://ai.google.dev/gemini-api/docs/changelog
- Cursor: https://cursor.com/help/models-and-usage/available-models

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|-------|-------|---------|------------------------|--------------------------|
| 1 | Sidecar Packaging and Runtime Wiring Repair | Installed app spawns the sidecar with the provisioned Node, catalog, and diffusion env; installer health check proves it | frontier | high |
| 2 | Model Availability End to End | All installed models (HF weights + Ollama) appear and run in Image Studio / Video Lab; truthful empty/error states | strong | high |
| 3 | Nexus-Hub Harness Provisioning and Skills Surface | Harness present after install; Skills page lists real skills; agentic tab discovers hub commands | strong | high |
| 4 | Smart Single-GPU Model Orchestration | Tab clicks never trigger loads; submits switch with confirm-only-when-busy; agentic cross-model requests queue through the scheduler | frontier | high |
| 5 | Local Chatbot Rebuild | Chat-first modern UI, persistent SQLite storage, optional projects/folders, auto-titled chats, voice folded into mic menu | frontier | high |
| 6 | Shell UI Modernization | Brand header removed, compact icon sidebar, GPU monitor integrated, Ask-Inbox bell, unified modern composer | strong | medium |
| 7 | Settings Modernization, Profile Retirement, and Data Transfer | Modern controls across settings/studios, URL-addressable settings tabs, profile folded into Settings, export/import of local data | strong | high |
| 8 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Clean layout, reconciled gaps, optimized CI, installer parity | frontier | max |

Parallelism: Phases 2, 3, and the policy core of 4 can start once Phase 1 lands; Phase 5 waits on Phase 1 (IPC) and benefits from 4; Phase 6 waits on 5.4 (composer base) and 4.3 (residency context); Phase 7 waits on 6 (ui primitives).

---

## Phase 1: Sidecar Packaging and Runtime Wiring Repair

**Goal**: The installed (NSIS-packaged) app reliably spawns the Node sidecar with its catalog and connects it to the installer-provisioned Node and diffusion Python runtimes.
**Prerequisites**: None.
**Stability Gate**: On this Windows host after rebuild, Complete shows sidecar ok (not os error 232); Restart backend is unnecessary because the child stays alive; `ipc_call("models.list")` returns catalog rows. A sabotaged native-addon tree still fails the health check, but the verdict includes the child exit code and a readable stderr tail (not three slash-joined fragments).
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Cross-layer packaging work (Tauri config, Rust spawner, esbuild, NSIS payload, installer engine) with high blast radius and no existing test coverage.

### Sub-tasks

#### 1.1 - Bundle the sidecar and catalog into the Tauri app

**Objective**: `sidecar/dist/main.js`, its `wasm/` grammars, and `catalog.json` ship inside the installed app and resolve via `BaseDirectory::Resource`.

**Failure modes**: If the resource is missing at runtime (corrupt install), `Sidecar::spawn` must surface a user-visible fatal state (Phase 2.2 renders it), not a silent stderr line; if the catalog file is unreadable or fails schema parse, `resolveCatalog()` must log and report a distinct `catalog-load-failed` status through `models.list` rather than silently returning `{ models: [] }`; concurrent spawn attempts (retry button + startup) must be idempotent via a spawn mutex.

**Build class**: load-bearing.

**Prompt**:
> In the Nexus-AI repo, make the packaged Tauri app self-contained for the sidecar. (1) `bundle.resources` must ship the **entire** `desktop/sidecar/dist/` tree: `main.js`, `package.json` (`type: commonjs`), `catalog.json`, `wasm/`, **and** `dist/node_modules/better-sqlite3` (plus `bindings` and `file-uri-to-path`) that esbuild already copies. The 2026-08-22 field failure is Node 22.11.0 starting then dying; the esbuild file already states that bundling the sqlite wrapper without the `.node` binary kills the sidecar before the first RPC. A packaging test must unzip/list the NSIS/Tauri resource dir and assert `better_sqlite3.node` sits next to the bundle Node can resolve from `main.js`. (2) `Sidecar::spawn_with` must set the child cwd to the directory that contains `main.js` so native `require` resolution cannot miss the copied addon. (3) Keep `NEXUS_CATALOG_PATH` and the `catalog-load-failed` status. Acceptance: healthcheck on this host is sidecar ok; deleting only the native addon from a fixture bundle reproduces 232 (or a spawn crash) with a stderr line that names better-sqlite3, not `[ / / Nodejs v22.11.0]`.

---

#### 1.2 - Spawn the provisioned Node runtime

**Objective**: The Rust spawner uses the installer-provisioned Node (`%LOCALAPPDATA%\Nexus\runtime\node\node.exe` on Windows, per-OS equivalents), falling back to PATH `node` only in dev.

**Failure modes**: Provisioned Node missing or non-executable -> try PATH `node`, and if both fail record a structured `sidecar-spawn-failed:<reason>` state queryable over a new `ipc_status` command instead of the bare `sidecar-not-running` string; Node present but sidecar crashes on boot (exit within 3 s) -> capture last stderr lines into the status; two spawns racing (auto-restart + manual retry) -> single-flight guard.

**Build class**: load-bearing.

**Prompt**:
> In `desktop/src-tauri/src/sidecar.rs` and `lib.rs`, replace `Command::new("node")` with a resolution chain: (1) explicit `NEXUS_NODE_PATH` env var, (2) a Node runtime bundled with the app itself (prefer a Node SEA binary or Tauri `externalBin` sidecar next to the resources - this makes the app independent of the installer's PATH handling, since Windows GUI processes often do not inherit a freshly-written user PATH), (3) the installer-provisioned runtime path (Windows `%LOCALAPPDATA%\Nexus\runtime\node\node.exe`; mirror the per-OS paths written by `scripts/installer/src/nexus_installer/engine/node_provisioner.py`, read via `~/.nexus/runtime.json`), (4) PATH `node` (dev fallback). If a stale runtime.json points at an uninstalled Node, fall through the chain and report which candidates failed. Store spawn outcome (which node, which script path, failure reason, last stderr tail on early exit) in `AppState`, expose it via a new `sidecar.status` Tauri command, and add a `sidecar.restart` command with a single-flight guard. Keep `sidecar-not-running` as the IPC error string for compatibility but make the richer status available to the frontend. Add Rust unit tests for the resolution chain (temp dirs standing in for the provisioned path). Acceptance: on a machine without system Node, the packaged app spawns the provisioned Node; `sidecar.status` reports the resolved path; deliberate misconfiguration produces a reasoned failure status.

---

#### 1.3 - Connect the diffusion Python runtime

**Objective**: The sidecar's diffusion runtime spawns the installer-provisioned venv Python (`%LOCALAPPDATA%\Nexus\python\venv`) instead of bare `python`, so image/video generation can actually run in an installed app.

**Failure modes**: Venv missing (user skipped diffusion provisioning) -> `image.generate` / `video.generate` return a typed `runtime-unavailable` error the studios can render with a "provision in installer/settings" hint; venv present but import fails or process exits early -> capture stderr tail into the job failure; two generate jobs racing for the runtime -> existing GpuScheduler single-active-job invariant applies, verify it holds through the new spawn path.

**Build class**: load-bearing.

**Prompt**:
> Wire the diffusion runtime to the provisioned environment. (1) Define a small runtime-config contract: the installer (`scripts/installer/src/nexus_installer/`) writes `~/.nexus/runtime.json` recording `nodePath`, `diffusionPython`, `diffusionCwd`, `modelsRoot`, and Ollama binary info after provisioning. (2) The sidecar reads it at boot (`desktop/sidecar/src/main.ts`) and passes `NEXUS_DIFFUSION_PYTHON` / `NEXUS_DIFFUSION_CWD` into the existing `runtimeFactory.ts` resolution (which already honors those vars, defaulting to bare `python`). (3) Ship the `runtimes/diffusion/` Python sources with the installer payload (they are not in the NSIS bundle today) and record their location in runtime.json. (4) Typed `runtime-unavailable` error when config or venv is absent. Add tests: sidecar unit tests with a fake runtime.json; installer pytest asserting runtime.json contents. Acceptance: packaged app on a provisioned machine runs an image generation end to end; with the venv renamed away, the studio shows the typed error, not a crash or hang.

---

#### 1.4 - Installer health check that proves the app works

**Objective**: The installer's first-run health check fails when the sidecar cannot spawn or answer, instead of passing whenever the process survives 5 seconds.

**Failure modes**: App starts but sidecar dead -> health check reports the `sidecar.status` failure reason and marks install failed with remediation text; health-check IPC times out (slow disk cold start) -> retry with backoff up to 30 s before failing; app cannot start at all -> existing failure path retained.

**Build class**: load-bearing.

**Prompt**:
> Keep `--healthcheck` (already invoked by `first_run_health_check`). Fix the verdict so a dead child is diagnosable: include `exitCode`, `nodePath`, `scriptPath`, and at least the last 20 stderr lines as a JSON array (the Complete page must not `" / ".join` three fragments into `[ / / Nodejs v22.11.0]`). Before the first `models.list` write, `try_wait` the child; if it has already exited, fail immediately with that stderr instead of writing to a closed pipe (the 232). Retry/backoff stays for slow starts only while the process is still alive. `sidecar.restart` must use the same spawn_with cwd/resources; it cannot succeed until 1.1 ships the native addon. Acceptance: this host's next installer either prints sidecar ok, or prints a single readable reason that names the missing file or thrown exception.

---

#### 1.5 - Sidecar must stay alive after spawn (Windows 232)

**Objective**: Node 22.11.0 starting is not success. The child must still be running when the first JSON-RPC line is written, and Restart backend must not be a no-op of the same crash.

**Failure modes**: Native `.node` ABI mismatch vs bundled Node 22 -> rebuild better-sqlite3 against that Node before packaging, fail the sidecar build if `npm rebuild` was skipped; unhandled exception at `main.ts` import time (scheduler, serving bind, sqlite open) -> process.exit after writing the stack to stderr, never a silent 232; `rl.on("close")` firing because stdin was not piped correctly in `--healthcheck` -> do not treat healthcheck as a TTY; CREATE_NO_WINDOW / GUI PATH are already out of the picture on this host (Node was found).

**Build class**: load-bearing.

**Prompt**:
> Treat the 2026-08-22 Complete-page 232 as the Phase 1 acceptance bug. (1) After `Command::spawn`, wait until stderr shows `[nexus-sidecar]` or 500 ms of liveness (`try_wait` is None) before `writeln` of `models.list`. (2) If the child exits, surface `fail: sidecar-exited:<code>` plus full stderrTail; never map that to "no models installed". (3) Confirm `tauri.conf.json` resources include `sidecar/dist/node_modules/**` (not only main.js). (4) Set cwd to the script directory. (5) Add a desktop/installer test: spawn fixture `main.js` that `require('better-sqlite3')` against a missing addon and assert the healthcheck verdict contains the module name. Acceptance: Restart backend on this machine either brings models.list back or the banner Show details names the real crash; it must not remain "The pipe is being closed."

---

#### 1.6 - Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until the phase is stable before advancing to Phase 2.

**Prompt**:
> Generate comprehensive tests for Phase 1: Rust unit tests for the Node resolution chain, spawn cwd, and spawn-status states; packaging assertion that the NSIS/Tauri payload contains `sidecar/dist/main.js`, `catalog.json`, and `better_sqlite3.node`; sidecar unit tests for catalog resolution; installer pytest for runtime.json, healthcheck verdict shape (exitCode + stderrTail array, no three-line join), and the 232-style dead-child path. Run root vitest, desktop vitest, installer pytest, and cargo test. Update CI path filters. Do not proceed to Phase 2 until this host's healthcheck is sidecar ok (or the remaining failure is a named, tested gap). After all tests pass, run /generate-session-history to document Phase 1.

---

### Phase 1 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, packaging)
- [ ] No known regressions from prior phases
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 2

---

## Phase 2: Model Availability End to End

**Goal**: Every model the installer downloaded (HF weights and Ollama pulls) is detected, listed, and runnable in the app, and every empty or error state tells the truth.
**Prerequisites**: Phase 1.
**Stability Gate**: On the reference machine (RTX 3080, models from `dist/nexus-install-models-log.txt`), Image Studio lists realvisxl-v5 + sana-1.6b-2k, Video Lab lists ltx-video + wan2.1-t2v-1.3b + sana-video-2b-720p, Settings > Models shows correct installed counts, and a killed sidecar produces a "sidecar unreachable - restart" state, never "no models installed".
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Focused reconciliation and UI-truthfulness work on a now-working substrate; moderate complexity, well-localized files.

### Sub-tasks

#### 2.1 - Fix installed-model reconciliation

**Objective**: The weights-dir probe matches what the installer actually wrote, and a non-default models root is honored.

**Failure modes**: Weights dir contains an id-sanitized name (`safe_dir_name` output with `.`/`:` replaced) -> probe matches by applying the same sanitization to catalog ids, plus a marker-file fallback (`model-id.txt` written by the installer); models root missing/unreadable -> probe returns empty with a surfaced `weights-root-missing` warning, not a crash; probe racing a mid-download model -> only directories whose files pass the existing verification manifest count as installed.

**Build class**: load-bearing.

**Prompt**:
> Fix installed-model detection. (1) In `core/registry/installedProbe.ts`, the weights probe compares raw catalog ids against directory names, but `scripts/installer/src/nexus_installer/engine/hf_weights_puller.py` writes `safe_dir_name(id)` directories (dots/colons sanitized) - reproduce the exact sanitization in TypeScript and compare sanitized-to-sanitized; also have the installer write a `.nexus-model-id` marker file in each weights dir and prefer marker-based matching. (2) Honor `modelsRoot` from `~/.nexus/runtime.json` (Phase 1.3) in `createModelsRuntime()` (`desktop/sidecar/src/handlers.ts` currently calls it with no args) and add a `NEXUS_MODELS_ROOT` env override; use one shared resolver for the installer's `models_root` and the sidecar's default. (3) Resilience: when catalog load has failed (Phase 1.1 status), synthesize installed rows directly from the `weights/` tree and Ollama `/api/tags` instead of returning nothing - degraded metadata beats an empty list, and the catalog-load-failed banner still shows. (4) Cover ids `wan2.1-t2v-1.3b`, `sana-1.6b-2k`, `qwen2.5-coder:14b` in unit tests against fixture dirs. Acceptance: with fixture weights dirs named per the installer, all five diffusion models flip to installed; a custom models_root install is detected; an empty catalog plus a weights tree still lists those ids as installed (degraded).

---

#### 2.2 - Truthful sidecar/empty/error states across the app

**Objective**: The UI distinguishes "sidecar unreachable" from "no models installed" from "catalog failed to load", with a restart affordance.

**Failure modes**: IPC error while listing -> studios render a sidecar-down banner with a Restart button (calls `sidecar.restart` from Phase 1.2) instead of the hardcoded `FALLBACK_MODEL` + "No models installed"; restart fails -> show the structured reason from `sidecar.status` with a copy-diagnostics action; status flapping (sidecar restarting) -> debounce state transitions so banners do not flicker.

**Build class**: load-bearing.

**Prompt**:
> Make error states truthful. Add a shared `useSidecarStatus` hook (polls the Phase 1.2 `sidecar.status` command, exposes restart) in `desktop/src/lib/`. In `ImageStudioPage.tsx`, `VideoLabPage.tsx`, `ModelsSettings.tsx`, `SkillsSettings.tsx`, and the chat/coding model selectors, branch the empty state three ways: sidecar-down (banner + Restart + diagnostics), catalog-load-failed (from Phase 1.1 status), and genuinely-no-models ("get more models" as today). Remove the silent catch-to-fallback in the studios. Debounce transitions (>= 500 ms). Add vitest coverage for all three branches per page using mock clients. Acceptance: killing the sidecar process while the app runs flips all surfaces to the sidecar-down banner; restart recovers them without reload.

---

#### 2.3 - Ollama version compatibility and failed-pull recovery

**Objective**: The installer detects when the pinned Ollama is too old for a catalog model (the gemma-4-12b `412` failure) and upgrades or surfaces retry, and the app can retry failed pulls.

**Failure modes**: Ollama returns 412 (needs newer version) -> installer upgrades the managed Ollama install (or records a per-model actionable failure if the upgrade fails), never a silent log-only continue; Ollama daemon unreachable mid-pull -> retry with backoff then mark failed-retryable; two pulls of the same model (installer retry + in-app install) -> Ollama's own store dedupes, but guard the app-side job queue against duplicate concurrent pulls of one id.

**Build class**: load-bearing.

**Prompt**:
> Handle Ollama version drift. Note: `ollama_installer.py` already carries a `MIN_OLLAMA_VERSION` floor (0.32.15 per the v2.1 changelog pin) and catalog entries may already declare `minOllamaVersion` - verify what exists before adding anything; the field evidence (this host's gemma4:12b HTTP 412) proves the enforcement path does not fire for a pre-existing older Ollama install. In `scripts/installer/src/nexus_installer/engine/ollama_installer.py` and `model_puller.py`: (1) before pulls, compare the installed Ollama version against the floor and any per-model `minOllamaVersion` (add the field to `core/registry/catalog.json` only if genuinely missing), upgrading the managed Ollama when too old (respect the existing pin mechanism - bump the pin, do not float); (2) classify pull failures (412 -> version, network, disk) into the summary UI with per-model retry; (3) surface the same classification in the app's in-app install flow (`ModelsSettings`). Update `catalog_invariants.py` to validate the field if added. Add pytest coverage with a fake ollama binary emitting 412. Acceptance: a simulated 412 against an old pre-existing Ollama triggers the upgrade path and the retry succeeds in tests; the install summary names the failed model with a retry button rather than a log line.

---

#### 2.4 - Live GPU telemetry

**Objective**: The GPU status chip reflects the real adapter, not `createMockTelemetryStream` - today "Gemma 4 7B Active / GPU 41%" renders on machines where nothing is loaded, which undermines every truthful-state fix in this phase.

**Failure modes**: No NVIDIA adapter / telemetry source unavailable -> "GPU: unavailable" state, never a fabricated utilization bar or model name; poll stall -> hold the last sample with a visible stale marker; multiple stream consumers -> a single stream owner in `App.tsx`, subscribers share it.

**Build class**: load-bearing.

**Prompt**:
> Replace `createMockTelemetryStream` in `desktop/src/App.tsx:86` with the real sidecar-backed telemetry via `desktop/src/lib/telemetryStream.ts` (the implementation exists but is unwired). Keep the mock behind a dev/test flag and as the injection default in tests. Handle unavailable-adapter and stale-poll states in `LocalModelStatus` (its dock placement moves in Phase 6; the data must be real now). Vitest for unavailable and stale paths. Acceptance: on the reference RTX 3080 host the chip reports the real adapter and free VRAM; on a machine without NVIDIA it shows unavailable instead of inventing "Gemma 4 7B".

---

#### 2.5 - Generation smoke path

**Objective**: Prove image and video generation run end to end on the packaged stack (catalog -> scheduler -> diffusion venv -> file output), with live-GPU tests gated.

**Build class**: load-bearing.

**Prompt**:
> Add an end-to-end generation smoke: a sidecar-level integration test (mock runtime) proving `image.generate` and `video.generate` route through GpuScheduler to the runtime with the resolved venv Python and write outputs to the expected media dirs; plus a `NEXUS_LIVE_GPU=1`-gated live smoke script (not CI-default) that generates one 512px image with sana-1.6b-2k and a short ltx-video clip, asserting nonzero output files. Document the live smoke in the phase's session history and known-gaps if it cannot be run on CI. Acceptance: mocked integration test green in CI; live smoke documented and runnable locally.

---

#### 2.6 - Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Generate comprehensive tests for Phase 2: probe reconciliation fixtures (sanitized dirs, marker files, custom roots, synthesize-when-catalog-failed), three-way empty-state branching per surface, telemetry unavailable/stale states, Ollama version-gate pytest, and the mocked generation integration test. Run root + desktop vitest and installer pytest, fix failures, iterate until green. Update CI path filters and caching for the touched areas; keep live-GPU smokes out of default CI (schedule/manual gate). After all tests pass, run /generate-session-history to document Phase 2.

---

### Phase 2 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing
- [ ] No known regressions from prior phases
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 3

---

## Phase 3: Nexus-Hub Harness Provisioning and Skills Surface

**Goal**: The Nexus-Hub harness (skills, commands, rules, hooks) is present immediately after install, visible in Settings > Skills, and discoverable from the Agentic tab.
**Prerequisites**: Phase 1.
**Stability Gate**: Fresh install on a networked machine shows a populated Skills page (real counts, active tag) without pressing Sync; the Agentic composer lists hub commands beyond the 16 built-ins; sync failures render actionable reasons.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Closes known gaps NHC.P6.B/C/D and NHC.P3.A with clear prior art in the VS Code extension path; moderate risk concentrated in provisioning order.

### Sub-tasks

#### 3.1 - Provision the hub catalog at install time

**Objective**: The installer populates `~/.nexus-ai/catalog/` during installation (when network is guaranteed present), with the sidecar's first-launch sync kept as a repair path.

**Failure modes**: Network/repo unreachable during install -> the bundled snapshot is extracted instead, so the harness is present regardless; snapshot checksum mismatch (corrupt payload) -> refuse extract, mark the step failed-retryable, Skills page shows "catalog absent - sync now" truthfully; git missing on later syncs -> tarball fallback (already in NexusHubSyncer) exercised; partial/corrupt catalog from an interrupted fetch -> atomic swap semantics preserved (fetch to temp, verify, swap), never a half-written catalog dir; re-sync vs user-authored content -> the user overlay (user skills/commands) is never wiped by a sync.

**Build class**: load-bearing.

**Prompt**:
> Provision the harness offline-first. (1) Bundle a checksummed Nexus-Hub catalog snapshot (tar/zip + sha256, produced at installer build time from the pinned release tag) in the installer payload - note v1.10 deliberately removed a broken bundled baseline with placeholder pins, so the snapshot must be real, checksummed, and covered by a packaging test that fails on placeholder content. (2) The installer extracts it to `~/.nexus-ai/catalog/` when that tree is absent (atomic temp-then-swap). (3) When online, a subsequent installer step refreshes via the existing sync logic: add a small CLI entry to the sidecar bundle (`node main.js --sync-hub-catalog`, wiring `NexusHubSyncer.sync({apply:true})` with JSON progress lines on stdout) invoked from a new `scripts/installer/src/nexus_installer/engine/hub_catalog_provisioner.py` step using the provisioned Node. Show progress in the installer UI, classify failures (network, git/tarball, scan-quarantine), mark the step retryable, record the outcome in runtime.json, and guarantee user-authored skills/commands survive any re-sync. Keep the sidecar first-launch auto-sync as repair. Add pytest with a stub sidecar CLI and fixture snapshot. Acceptance: fresh OFFLINE install yields a populated ~/.nexus-ai/catalog with nexus-hub-version.json from the snapshot; a networked install refreshes to the latest tag; simulated network failure still leaves the snapshot catalog in place.

---

#### 3.2 - Wire the skills management IPC (close NHC.P6.B / P6.C)

**Objective**: Settings > Skills lists real skills from the synced catalog with working counts, active tag, auto-sync toggle, and truthful error states.

**Failure modes**: Sidecar down -> the page shows the Phase 2.2 sidecar-down banner (not "not yet synced" - stop swallowing IPC errors in `activeTag()`); catalog dir present but unreadable/corrupt -> `skills.list` returns a typed parse error surfaced inline; sync while a sync is running -> single-flight with progress, second request attaches to the running one.

**Build class**: load-bearing.

**Prompt**:
> Replace the stubbed skills client. Implement `skills.list` in `desktop/sidecar/src/handlers.ts` reading the synced catalog (skills/commands/agents/rules with names, categories, descriptions, source section Nexus-Hub/User/Built-in), wire `ipcSkillsClient.list()` to it (currently hardcoded `[]`), implement `setActive`/`approveQuarantined` or remove those affordances from the UI this cycle (decide by what the sidecar can genuinely do; do not ship dead buttons). Stop swallowing IPC errors in `activeTag()` so sidecar-down is distinguishable from not-synced. Wire the auto-sync weekly toggle to a persisted setting honored by a sidecar idle worker (NHC.P6.C). Add hermetic tests with a `NEXUS_AI_HOME` override pointing at fixture catalogs (closes NHC.P6.D). Acceptance: after Phase 3.1 provisioning, the page shows real nonzero counts and the active tag; killing the sidecar shows the sidecar-down banner.

---

#### 3.3 - Hub command and skill discovery in the Agentic tab

**Objective**: The desktop coding module discovers hub commands/skills like the VS Code extension does, instead of a static 16-item array sliced to 8.

**Failure modes**: Catalog absent -> composer falls back to built-ins only (current behavior) with a one-line hint linking to Skills settings; a hub command file is malformed -> skip it with a logged warning, never break the dropdown; command list large -> virtualize/scroll the dropdown and lift the `.slice(0, 8)` cap to a scrollable list with type-ahead filtering.

**Build class**: load-bearing.

**Prompt**:
> Bring harness discovery to the desktop Agentic tab. Construct `HubCommandCatalogLoader` (modules/coding/commands/) in the sidecar and expose a `commands.list` IPC; in `desktop/src/modules/coding/slashCommands.ts` + `CodingInput.tsx`, merge built-ins with hub commands via the existing (currently unused) `filterSlashCommandsWithSkills` path, remove the 8-item cap in favor of a scrollable, filtered dropdown grouped by source (Built-in / Nexus-Hub), and show command descriptions. Mirror the VS Code extension's wiring in `src/panels/ChatPanelBootstrap.ts:513` as the reference. Prompt-budget constraint: discovery must NOT inject the full hub catalog into every system prompt - reuse the existing skill render ladder (~2% context envelope) for skill content, loading full skill bodies only on invocation. Keep the copy distinction between the coding header's harness scaffold profile (e.g. `balanced-scaffold`, a model scaffold) and the Nexus-Hub harness pack. Add vitest for merge/filter/malformed-entry skip. Acceptance: with a synced catalog, typing "/" in the Agentic composer lists hub commands (e.g. /plan variants, hub-specific commands) beyond the 16 built-ins; with no catalog, built-ins render with the hint.

---

#### 3.4 - Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Generate comprehensive tests for Phase 3: installer provisioner pytest (success, network-fail-retryable, tarball fallback), hermetic sidecar skills.* tests under a fixture NEXUS_AI_HOME, command-discovery vitest (merge, cap removal, malformed skip), and an integration test that a fixture catalog flows installer -> disk -> skills.list -> UI counts. Run all suites, fix failures, iterate until green. Update CI (path filters for installer + sidecar + coding module; cache node/pytest deps). After all tests pass, run /generate-session-history to document Phase 3.

---

### Phase 3 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing
- [ ] No known regressions from prior phases
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 4

---

## Phase 4: Smart Single-GPU Model Orchestration

**Goal**: Model switching on a single GPU follows the agreed smart policy: never on tab click, on submit with confirm-only-when-busy, automatic with a toast when idle, and scheduler-mediated cross-model use during agentic tasks.
**Prerequisites**: Phases 1-2 (models must load and list correctly).
**Stability Gate**: Clicking between tabs never changes GPU residency; submitting an image prompt while the GPU is idle swaps models with a status chip (or co-resides silently when both models fit with 2 GB headroom); submitting while an agentic task is generating raises a confirm dialog whose "remember for this session" suppresses repeats; an agentic task can invoke image generation and the scheduler swaps/co-resides and restores models automatically.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Policy layer over concurrent scheduler/VRAM machinery where wrong sequencing corrupts user sessions; high-risk concurrency semantics.

### Sub-tasks

#### 4.1 - Switch-policy engine over GpuScheduler

**Objective**: A `ModelSwitchPolicy` layer that classifies every model-requiring action as no-op (resident), co-reside (both fit in VRAM), auto-switch (GPU idle), or confirm-required (another model busy), reusing `core/scheduler/GpuScheduler.ts`, `enqueuePanel` co-residency, and `modelSwap.ts`.

**Failure modes**: Telemetry stream absent/null free-VRAM -> classify as `confirm` (never a silent swap and never an indefinite defer - the user decides when the machine cannot); the busy job finishes between classification and user confirm -> re-classify on confirm so the dialog resolves as auto-switch (no stale confirm); two surfaces submitting simultaneously -> classification and enqueue are atomic within the scheduler's queue lock so exactly one confirm dialog can win; requested model not installed -> typed not-installed error, no queue entry.

**Build class**: load-bearing.

**Prompt**:
> Implement `core/scheduler/ModelSwitchPolicy.ts` on top of the existing GpuScheduler + modelSwap: input (targetModelId, requestingModule, currentResidency, freeVram, sessionRememberSet, activeJobs), output one of `resident | coreside | auto-switch | confirm { busyWith } | defer { reason }`. Rules: tab navigation never calls it (audit desktop routes to confirm no load fires on mount - the studios currently only list, keep it that way; `setForegroundModule` stays a scheduling hint, never a load trigger); generation/chat submits and agentic cross-model tools call it; if the requested and resident models both fit with >= 2 GB VRAM headroom, return `coreside` and admit via the existing `enqueuePanel` co-residency path (no dialog); `confirm` only when an active job from another module is running or when free VRAM is unknown; a "remember for this session" flag on confirm suppresses future dialogs for that modality pair this session; re-classify at confirm time; after an honored swap with `keepWorkerResident: false`, reuse the existing `unloadOllamaModel` path. Emit scheduler telemetry events for every decision. Unit-test the matrix (idle/busy x resident/non-resident x fit/no-fit x remember x same/other module x telemetry-null, confirm-time re-classify race, not-installed). Acceptance: full decision-matrix tests green; no code path loads a model from a route mount.

---

#### 4.2 - Cross-model requests from agentic sessions

**Objective**: An agentic task can request another installed model (e.g. image generation for a UI mockup) as a scheduler-queued step that swaps models automatically and restores the agentic model afterward.

**Failure modes**: Requested model not installed -> tool returns a typed not-installed error with the catalog id (agent can tell the user), no queue entry left behind; swap-in fails (VRAM, runtime error) -> job fails, original model restored via ModelPinRegistry hold, agentic session resumes with the error in-context; user cancels the agentic task mid-swap -> AbortSignal propagates, scheduler releases holds and restores residency.

**Build class**: load-bearing.

**Prompt**:
> Wire agentic cross-model tool calls through the scheduler. Give the coding/agent loop's image- and video-generation tools (and any model-switching tool surface) a path that: takes a `holdForPanel` pin on the current agentic model, enqueues the cross-model job via GpuScheduler with the ModelSwitchPolicy (auto, no dialog - the user consented by running the task; policy still defers on VRAM), awaits completion, releases the hold triggering restore. Surface progress in the coding Trace panel ("switching to sana-1.6b-2k... generating... restoring qwen2.5-coder"). Handle the three failure modes (not-installed typed error, swap-fail restore, abort restore) with integration tests using a mock runtime. Acceptance: mocked agentic session generates an image mid-task and ends with the original model resident, all transitions in telemetry.

---

#### 4.3 - Surfacing residency and switches in the UI

**Objective**: The GPU panel (relocated in Phase 6) and per-surface toasts show which model is resident, switch progress, and queue state; the confirm dialog is clear and non-blocking to other work.

**Failure modes**: Switch takes long (big weights) -> progress state with elapsed time, cancel returns to prior residency; toast spam from rapid successive switches -> coalesce into one persistent status chip while switching is in flight; dialog dismissed/ignored -> request expires after 60 s and the queue entry is dropped cleanly.

**Build class**: load-bearing.

**Prompt**:
> Build the switch UX: a `ModelResidencyContext` fed by scheduler telemetry exposing {residentModels, switching: {from,to,progress}, queue}; a confirm dialog component ("Image generation needs sana-1.6b-2k. qwen2.5-coder:14b is currently busy with an agentic task. Switch now / Queue after current job / Keep current model", plus a "Remember for this session" checkbox, 60 s expiry); auto-switch and co-reside events show a coalesced status chip, never a modal. Integrate into the studios' and chat/coding submit paths from Phase 4.1. Vitest for dialog outcomes (switch, queue, keep, remember-suppression, expiry) and chip coalescing. Acceptance: manual walkthrough matches the stability gate scenarios (including: Remember checked, second Generate shows no dialog); tests green.

---

#### 4.4 - Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Generate comprehensive tests for Phase 4: the full policy decision matrix, scheduler integration under concurrent submits, agentic cross-model happy/failure/abort paths, dialog/toast behavior, and a regression test asserting zero model loads on route navigation. Run all suites, iterate until green. Update CI path filters for core/scheduler and touched desktop modules. After all tests pass, run /generate-session-history to document Phase 4.

---

### Phase 4 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing
- [ ] No known regressions from prior phases
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 5

---

## Phase 5: Local Chatbot Rebuild

**Goal**: The Local Chatbot becomes a modern chat-first module: type immediately, persistent storage, optional projects/folders organization, auto-generated chat titles, and voice folded into the mic control.
**Prerequisites**: Phase 1 (persistence needs the sidecar); Phase 4 recommended (chat submits use the switch policy).
**Stability Gate**: Fresh app: open Local Chatbot, type a message, get a response, and the chat self-titles and persists across app restarts; chats can live at root or be organized into projects/folders created on demand; everything renames; the old voice button row and persona textarea are gone.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Large rebuild spanning storage IPC, hierarchy model, LLM-driven titling, and a full UI redesign of the app's most-used surface.

### Sub-tasks

#### 5.1 - Persistent chat storage over IPC (close 3.P1.N)

**Objective**: The desktop chat runs on the existing SQLite `ChatExplorerStore` (better-sqlite3 + FTS5) via sidecar IPC instead of the in-memory maps.

**Failure modes**: DB locked/corrupt at open -> sidecar returns a typed storage error, UI offers read-only session + a "backup and reset" path, never data loss without consent; IPC write fails mid-conversation -> optimistic UI with a retry queue and a dirty indicator, messages never silently dropped; concurrent writes (rename while a message streams in) -> store serializes by chat id.

**Build class**: load-bearing.

**Prompt**:
> Wire chat persistence. Extend the schema in `core/memory/migrations/` for the hierarchy: projects (optional) -> folders (optional, nestable within a project) -> chats, plus chats at root; migrate existing tables per `modules/chat/storage/ChatExplorerStore.ts`. Expose `chat.explorer.*` IPC (list/create/rename/move/delete for projects/folders/chats, message append/list, FTS search) in the sidecar, implement an `IpcChatExplorerClient` replacing `InMemoryChatExplorerClient` in `desktop/src/modules/chat/` (keep the in-memory client for tests/mocks). Persist message turns (today they live only in a React Map). Handle the three failure modes with typed errors and a write-retry queue. Migration test from an empty and a fixture DB. Acceptance: chats, hierarchy, and messages survive app restart; kill -9 during streaming loses at most the in-flight turn.

---

#### 5.2 - Chat-first UI with optional organization

**Objective**: The module opens ready to type (no folder prerequisite); the left rail shows a collapsible session history with optional projects/folders the user creates as needed; everything is renameable.

**Failure modes**: Empty state (no chats) -> composer-centered welcome, first send creates the chat at root; deep hierarchies -> tree virtualizes and collapse state persists (existing `nexus.chat.expanded` pattern); a move/drag that would orphan children or cycle a folder into itself -> rejected with a shake/tooltip, tree never enters an invalid state.

**Build class**: load-bearing.

**Prompt**:
> Rebuild the chat shell UI in `desktop/src/modules/chat/`: (1) default view is the conversation pane with the composer ready - sending in a fresh view creates a root-level chat; (2) rework FolderTree into a session-history rail: "New chat" primary action, recent chats at root, optional Projects section (create project -> create folders inside as desired), drag chats between root/projects/folders, expand/collapse persisted, rename via double-click/F2/context menu for chats, folders, and projects; (3) rail is collapsible to maximize conversation width; (4) match the Agentic tab's visual quality - reuse its patterns, drop the per-chat Enable-tools checkbox row into a compact header control. Keep keyboard/tree-nav accessibility from the current FolderTree. Vitest for create-on-first-send, hierarchy ops, invalid-move rejection. Acceptance: stability-gate walkthrough passes; no folder is required to start chatting.

---

#### 5.3 - Auto-titling from the first prompt

**Objective**: A new chat titles itself from the user's first message using the best available local model, while the main response streams; titles are always renameable.

**Failure modes**: No local model available/loaded -> fallback to a truncated first-prompt title (first ~6 words), retitle attempt on next model availability, never a blocking load just for a title; titling call fails or times out (5 s) -> keep fallback title silently; user renames before the auto-title lands -> user title wins, auto-title result discarded (renamed flag on the chat).

**Build class**: load-bearing.

**Prompt**:
> Implement chat auto-titling. Add a sidecar `chat.generateTitle` op: given the first user message, ask the currently-resident local chat model (piggyback on the same session/model answering the message - a short second prompt after the response completes, or a parallel low-token call if the runtime supports it; never trigger a model switch for titling) for a 3-6 word title; sanitize (no quotes/punctuation wrap, length cap). Client-side: on first send set the truncated-prompt fallback immediately, request the generated title in the background, apply unless the user has renamed (persist a `userRenamed` flag). Unit-test fallback, timeout, rename-wins, and sanitization. Acceptance: first message in a new chat yields a sensible generated title within a few seconds of the response, with the fallback visible meanwhile; manual rename is never overwritten.

---

#### 5.4 - Modern composer with integrated voice

**Objective**: One composer surface: a single input box with the + (attach) and send controls embedded inside it, mic button with a small menu holding the voice modes, persona moved to a per-chat settings popover and persisted.

**Failure modes**: Long/multiline text -> input grows to a max height then scrolls internally; embedded controls never overlap text (padding reserves + controls pinned); mic permission denied -> mic button shows a disabled state with an explanatory tooltip, voice modes unavailable but chat unaffected; voice mode active while user types -> typing pauses VAD capture (existing voiceLoop state machine hooks).

**Build class**: load-bearing.

**Prompt**:
> Redesign `desktop/src/shared/chat/MediaComposer.tsx` (used by chat and studios): one rounded input surface containing a textarea with reserved left/right padding, + attach button inside-left, mic and send inside-right; auto-grow to ~6 lines then internal scroll; drag-drop/paste attachments unchanged, chips render above the text within the surface. Mic button: click = push-to-talk; a chevron/long-press menu exposes Voice loop, VAD (start/stop), Hold to talk - wiring to the existing `voiceLoop.ts` state machine and `micRecorder.ts`; remove the five-button voice bar from `ChatPage.tsx`. Move Persona into a per-chat settings popover (gear in the chat header) and persist it in the chat record (Phase 5.1 schema) instead of the unpersisted React state. Update CodingInput to reuse the same base composer (keep slash-command dropdown). Vitest for layout constraints (no overlap at max content), menu wiring, persona persistence. Acceptance: composer matches the modern single-surface design in all four modules; all previous voice capabilities remain reachable via the mic menu.

---

#### 5.5 - Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Generate comprehensive tests for Phase 5: storage IPC round-trips and migration, hierarchy operations incl. invalid moves, create-on-first-send, auto-title fallback/timeout/rename-wins, composer layout and voice-menu wiring, and persona persistence. Run root + desktop suites, iterate until green. Update CI path filters for modules/chat, core/memory migrations, and desktop chat/shared dirs. After all tests pass, run /generate-session-history to document Phase 5.

---

### Phase 5 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing
- [ ] No known regressions from prior phases
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 6

---

## Phase 6: Shell UI Modernization

**Goal**: The app shell reads as one modern system: no duplicate branding, a compact icon-capable sidebar with the GPU monitor integrated, an Ask-Inbox notification bell, and the unified composer everywhere.
**Prerequisites**: Phase 5 (composer base); Phase 4.3 (residency context for the GPU panel).
**Stability Gate**: Sidebar shows no logo/wordmark (title bar keeps the brand); default view is the icon-only rail; GPU status is a real chip in the footer, not a floating overlay; the Approvals popover can always be closed (X, Escape, click-outside) and never covers the bell; User Profile is absent from the sidebar.
**Recommended model tier**: strong
**Recommended effort level**: medium
**Rationale**: Well-scoped component work with clear specs; the risky substrate (composer, residency data) lands in earlier phases.

### Sub-tasks

#### 6.1 - Sidebar: remove brand header, add compact mode

**Objective**: Brand appears only in the title bar; the sidebar collapses to a thin icon-only rail (tooltips + accent states preserved), preference persisted.

**Failure modes**: Window very narrow -> sidebar auto-compacts below a breakpoint but the user's explicit choice wins otherwise; keyboard users -> Ctrl+1..4 and focus order unchanged in both modes; tooltip layer in compact mode must not clip at screen edges.

**Build class**: load-bearing.

**Prompt**:
> Rework `desktop/src/components/Sidebar.tsx`: delete the brand block (logo img + "Nexus AI Studio" wordmark; TitleBar stays the only brand). **Default compact** (`nexus.sidebar.compact` default true; about 56 px icon rail, labels as tooltips). Expand toggle remains. Remove `User Profile` from `ADMIN_ENTRIES` (2026-08-22: that row only redirects to Settings). Keep Settings. Auto-compact below a breakpoint without clobbering an explicit expand. Vitest: no "Nexus AI Studio" in the nav, compact default, no `/profile` link. Acceptance: field screenshots of this cycle would not show a sidebar wordmark or a Profile item.

---

#### 6.2 - Integrate the GPU monitor into the sidebar

**Objective**: The floating bottom-right dock is removed; a compact GPU/model status lives at the sidebar foot, expanding to the full panel (residency, queue, VRAM detail) on click, with a minimal indicator in compact mode.

**Failure modes**: No telemetry stream -> muted state exactly as today's dock handles it; compact sidebar -> a slim utilization bar/dot with tooltip, popover opens the full panel; telemetry updates at 2 s cadence must not re-render the whole sidebar (memoized subtree).

**Build class**: load-bearing.

**Prompt**:
> Move GPU status into the sidebar. Remove `LocalModelStatusDock` (fixed bottom-right) and its `DockMount` in App.tsx; refactor `LocalModelStatus` into (a) `GpuStatusFooter` - compact card at the sidebar foot: model name, GPU% bar, free VRAM; (b) icon-rail variant - vertical bar/dot with tooltip; (c) an expand popover carrying the existing detail (queue modal content from Phase 4.3's residency context, VRAM breakdown, power). Telemetry is already real as of Phase 2.4 - this sub-task only relocates the surface. Memoize so 2 s ticks re-render only the footer. Dashboard's inline widget keeps working. Vitest for the three variants and stream fallback. Acceptance: no floating overlay anywhere; GPU status visible in both sidebar modes; buttons previously blocked by the dock are reachable.

---

#### 6.3 - Approvals bell that can actually be dismissed

**Objective**: Ask Inbox stays out of the main nav. The bell popover is optional UX, not a modal trap. Sidecar-down must not pin a red 232 box on every Settings tab.

**Failure modes**: Sidecar down -> bell stays quiet (no auto-open, no badge pretending 0 pending); popover open and child dies -> show one error line **plus Close**; popover geometrically covers the bell -> still closable via X, Escape, and mousedown outside (the 2026-08-22 trap); click on the error text must not be a dead end.

**Build class**: load-bearing.

**Prompt**:
> Fix `desktop/src/components/ApprovalsBell.tsx`. Today `open` only toggles from the bell button, the dialog has no Close control, and `bottom: calc(100% + 4px)` covers that button, so the box cannot be dismissed from any tab. Required: (1) Close button with aria-label; (2) Escape; (3) pointerdown outside the dialog **and** outside the bell; (4) never start `open === true`; (5) if `list("pending")` throws, do not keep the dialog stuck -- show the error inside a closable panel, or skip opening and rely on the Settings backend banner; (6) route `/inbox` to Settings > Approvals. Schedules stay in Settings. No auto-approve. Vitest: outside click closes, Escape closes, X closes, sidecar-down does not leave a persistent overlay. Acceptance: Skill Optimizer (and every other tab) can be used without a stuck Pending approvals card.

---

#### 6.4 - Design tokens and shared primitives repair

**Objective**: The token layer is made truthful (define or migrate the widely-used but undefined `--border-1`, `--accent-primary`, `--accent-danger`, `--accent-warning`), and shared UI primitives (Button, IconButton, Card, Tooltip, Popover) exist for the phases that follow.

**Failure modes**: Token rename sweep breaking a surface silently -> visual regression pass per route (Styleguide route renders all primitives/tokens for eyeballing + snapshot tests); a primitive misused with the old inline-style pattern -> lint rule or review checklist noting new-code-uses-primitives; dark-only app today -> primitives read tokens, no hardcoded colors.

**Build class**: load-bearing.

**Prompt**:
> Repair the design substrate. In `desktop/src/styles/tokens.css`, define the missing tokens as aliases of the real ones (`--border-1: var(--border-subtle)` etc.) then migrate the ~55 usages to the canonical names and delete the aliases; fix the stale Tailwind comment. Create `desktop/src/components/ui/` with Button, IconButton, Card, Tooltip, Popover, Toast primitives (tokens-driven, no inline color literals), and render them all on the existing `/_styleguide` route. Adopt them in the components this phase touches (Sidebar, GpuStatusFooter, bell popover). Snapshot tests on the styleguide route. Acceptance: no undefined-token usages remain (grep-verifiable); styleguide renders the primitive set; touched components use primitives.

---

#### 6.5 - Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Generate comprehensive tests for Phase 6: sidebar modes and persistence (compact default, no wordmark, no User Profile row), GPU footer variants, bell badge, and the 2026-08-22 dismiss trap: X / Escape / pointerdown-outside all close the popover, sidecar-down error is closable and never a sticky overlay. Token-migration grep as a CI guard. Styleguide snapshots. Run desktop + root suites. After all tests pass, run /generate-session-history to document Phase 6.

---

### Phase 6 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing
- [ ] No known regressions from prior phases
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 7

---

## Phase 7: Settings Modernization, Profile Retirement, and Data Transfer

**Goal**: Settings look and behave like a modern app (styled controls, URL-addressable tabs), the User Profile page is retired into Settings, and users can export/import their local Nexus data as a single file.
**Prerequisites**: Phase 6 (ui primitives); Phase 5 (chat store is a major export payload).
**Stability Gate**: No raw native `<select>`, unstyled `<input type="search">`, or flat gray action buttons remain on any Settings tab (including Skill Optimizer Preview and Models Search); User Profile is gone from the sidebar; a round-trip export -> wipe -> import on a second profile dir restores selected data categories.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Broad mechanical UI sweep plus a new export/import subsystem touching every data store; the archive format needs care but has no concurrency depth.

### Sub-tasks

#### 7.1 - Modern form controls across settings and studios

**Objective**: Shared Select, SearchInput, TextField, Button, Switch, and SegmentedControl replace every leftover native control on every Settings tab (Models Search, Skill Optimizer field + Preview, Credentials, Local API, Fine-tuning, MCP, Security, Data) plus Image/Video advanced forms. `SearchInput` already exists in `desktop/src/components/ui/Select.tsx` but Models Search still uses a bare `input type="search"`; Button and TextField do not exist yet.

**Failure modes**: Keyboard/screen-reader regressions from a custom Select -> build on native `<select>` visually restyled where possible, full listbox pattern (roving focus, type-ahead, Esc) where custom rendering is needed; long option lists (model pickers) -> scrollable with search; form value drift during the sweep -> each replacement keeps the exact value/onChange contract, verified by existing page tests.

**Build class**: load-bearing.

**Prompt**:
> Add Select, Switch/Checkbox, SegmentedControl, SearchInput, TextField, and Button to `desktop/src/components/ui/` (token-driven, dark-theme, a11y). Sweep **every Settings tab**, not only Models filters: `ModelsSettings.tsx` (the search box at `type="search"` is still native as of 2026-08-22), `SkillsSettings.tsx`, `SkillOptimizerSettings.tsx` (skill-id field + Preview button), `FineTuningSettings.tsx`, `SecuritySettings.tsx`, `ServingSettings.tsx`, `McpRegistrySettings.tsx`, `CredentialsSettings.tsx`, Image/Video advanced forms, `ModelSelector.tsx`, `QuickModelSwitcher.tsx`. Add each primitive to `/_styleguide`. Keep value contracts. Acceptance: a screenshot of Skill Optimizer and Models Search matches the Select styling (radius, border, height, focus ring); `grep` for unstyled native search/text inputs in `desktop/src/pages/settings` is empty.

---

#### 7.2 - Settings shell refactor

**Objective**: Settings tabs are data-driven, URL-addressable (`/settings/models` etc.), restyled with the ui primitives, and include the new sections (Approvals/Schedules from Phase 6.3, Profile and Data from 7.3/7.4).

**Failure modes**: Old `/settings` links/bookmarks -> redirect to the first tab; unknown tab slug -> fallback to first tab, no blank page; per-page mount cost -> lazy-load heavy pages so switching tabs stays instant.

**Build class**: load-bearing.

**Prompt**:
> Refactor `desktop/src/pages/settings/SettingsPage.tsx`: replace the hand-written button blocks and ternary chain with a declarative tab registry (id, label, icon, component, lazy) rendered as a modern side-tab or top-tab bar using ui primitives; route as `/settings/:tab` with redirect from `/settings` and unknown slugs; add sections: Approvals & Schedules (from Phase 6.3), Profile, Data. Update the sidebar Settings link and Ctrl+, target. Vitest for routing, redirects, lazy mounting. Acceptance: every tab deep-links; visual style matches the modernized shell.

---

#### 7.3 - Retire the User Profile page

**Objective**: `/profile` and its nav entry are removed; profile fields (name, and the `~/.nexus/profile.json` contract once relevant) live in Settings > Profile.

**Failure modes**: Existing localStorage `nexus.profile` data -> migrated into the settings-backed store on first open; profile read failing -> defaults ("User") without blocking settings; stale `/profile` route hits -> redirect to `/settings/profile`.

**Build class**: load-bearing.

**Prompt**:
> Remove User Profile from the product chrome. Delete the `ADMIN_ENTRIES` item in `Sidebar.tsx` (2026-08-22: it only `<Navigate>`s to `/settings`). Do **not** replace it with another sidebar row. Put display name under Settings > General (or Settings > Profile as a *tab inside Settings*, never a sibling of Settings in the rail). Keep a silent `/profile` -> `/settings/general` redirect for old bookmarks. Migrate `localStorage` `nexus.profile` into `~/.nexus/profile.json`. Acceptance: sidebar items are the four pillars plus Settings (plus optional quiet Approvals bell); zero "User Profile" labels.

---

#### 7.4 - Local data export and import

**Objective**: Settings > Data lets the user export selected categories (preferences, hub harness catalog, chats/projects, agentic sessions, generated images/videos, model pins/configs, credentials excluded by default) into one archive, and import that archive on another machine.

**Failure modes**: Archive partially written (disk full, cancel) -> write to temp + atomic rename, never a half archive presented as complete; import of a corrupt/version-mismatched archive -> manifest with schema version + per-category checksums validated before any write, typed errors listed per category, nothing applied on manifest failure; malicious archive (zip-slip / path traversal entries) -> rejected outright before extraction; import colliding with existing data -> per-category strategy surfaced up front (merge for chats/projects with id remap, replace-with-backup for preferences/configs), and a pre-import backup archive is written automatically; import requested while generation/agentic jobs are running -> refuse or queue until idle, never mutate stores under an active writer.

**Build class**: load-bearing.

**Prompt**:
> Build export/import. Sidecar side: `data.export` (input: category selection; output: zip archive with manifest.json {schemaVersion, appVersion, categories, checksums} + per-category payloads from ~/.nexus, ~/.nexus-ai/catalog, the chat SQLite DB, media output dirs, localStorage-equivalent settings passed from the renderer) and `data.import` (validate manifest + checksums, per-category merge/replace strategies, automatic pre-import backup export, dry-run mode returning a summary). Renderer: Settings > Data with category checkboxes + size estimates, export-to-file via save dialog, import flow showing the dry-run summary before applying. Exclude credentials/secrets by default behind an explicit opt-in with a warning, and run a secret-redaction scan over opted-in payloads anyway. Validate every archive entry path against zip-slip/traversal before extraction. Large media -> stream into the zip, progress events. Tests: round-trip fixture export/import, corrupt-archive rejection, zip-slip rejection, merge id-remap, dry-run accuracy, credentials-off default. Acceptance: export on machine A, import on a clean profile restores selected categories; corrupt or malicious archive changes nothing.

---

#### 7.5 - Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Generate comprehensive tests for Phase 7: control-sweep regression per settings/studio page, settings routing, profile migration/redirect, and the export/import suite (round-trip, corruption, merge, dry-run, atomicity). Run all suites, iterate until green. Update CI path filters. After all tests pass, run /generate-session-history to document Phase 7.

---

### Phase 7 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing
- [ ] No known regressions from prior phases
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 8

---

## Phase 8: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, and its CI/CD complete and optimized.
**Prerequisites**: All prior phases.
**Stability Gate**: The layout is clean (no deprecated/obsolete files, empty dirs, redundant files/dirs, or overcomplicated structure left un-triaged); the version's known gaps are reconciled; CI/CD covers every change and is optimized; project validation/tests pass.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, reference repair, known-gap reconciliation, and release gating carry high context volume and blast radius.

### Sub-tasks

#### 8.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure, then refactor toward a clean, intuitive layout via [[project-refactor]] and [[docs-layout-refactor]] (propose-then-apply, with confirmation; repair every reference for anything that moves). Candidates known now: the retired LocalModelStatusDock, InMemoryChatExplorerClient (if fully replaced), the removed /profile and /inbox routes, and duplicated composer style objects.

#### 8.2 - Known-gaps reconciliation
**Objective**: Reconcile the version's open gaps.
**Prompt**:
> Reconcile this version's known gaps via [[known-gaps-tracker]]: resolve, defer, or transfer each open item, and finalize docs/archive/v2/v2.2/known-gaps.md. This plan deliberately closes NHC.P6.B, NHC.P6.C, NHC.P6.D, NHC.P3.A (desktop surface), and 3.P1.N - mark them accordingly in their source files; carry forward anything discovered during the cycle (e.g. live-GPU smokes that could not run on CI).

#### 8.3 - CI/CD create/update/optimize
**Objective**: CI/CD covers all changes and is optimized.
**Prompt**:
> Create or update the CI/CD pipeline so it covers every change in this plan (Rust shell, sidecar bundle + packaging assertions, installer engine, desktop UI, core scheduler/registry), then optimize it to reduce action minutes (path filters, concurrency cancel-in-progress, dependency caching, gating expensive-OS or matrix jobs to merges/schedule) while keeping comprehensive testing. Keep it platform-agnostic; GitHub Actions is the primary example.

#### 8.4 - Cross-installer parity
**Objective**: Prevent operating-system installer drift when the repository ships multiple installers.
**Prompt**:
> If this repository ships more than one installer, run or add a declarative cross-installer parity gate covering distributed artifacts, supported platforms, named capability counterparts, and external-tool fallbacks, then execute the real installers on their target operating systems with identical postconditions. Cross-link [[platform-contract-verification]] so discovery and delivery are checked in the same pass. If the repository ships zero or one installer, record a silent no-op and continue. Note: the sidecar-bundling and runtime.json contracts from Phase 1 must hold on macOS (.app/ditto) and Linux (AppImage) paths in desktop_provisioner.py, not just Windows NSIS.

#### 8.5 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and CI/CD is green.
**Prompt**:
> Run the full validation/test suite, confirm the refactor changed no behavior, confirm CI/CD passes and the action-minute reduction is real, and iterate until clean. Generate a session-history entry for this phase.

---

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| | | |

---

### Phase 8 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] Session history generated for this phase
- [ ] Release readiness confirmed (hand off to /update release)

---

## Out of scope

- Multi-GPU scheduling (single-GPU policy only this cycle).
- Closing v2.1's live-GPU/hardware-gated known gaps (DF rows requiring on-device soaks) unless a GPU host run is explicitly added mid-cycle; do not claim live-GPU passes that were not run.
- Injecting the full Hub catalog into every system prompt (the render ladder budget stands).
- Deleting the Ask Inbox capability (it is relocated, not removed).
- Light-theme support and the deferred Claude Code-style VS Code extension UX (IRSC.P7.A).
