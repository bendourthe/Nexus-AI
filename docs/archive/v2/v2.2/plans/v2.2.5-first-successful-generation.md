# Plan -- First Successful Generation and Catalog Parity

**Project**: Nexus AI Studio
**Version**: v2.2.5
**Slug**: first-successful-generation
**Filename**: v2.2.5-first-successful-generation.md
**Plan Type**: Feature / Bugfix (patch follow-on)
**Created**: 2026-08-23
**Predecessor**: [v2.2.4-chatbot-first-and-runtime-honesty.md](v2.2.4-chatbot-first-and-runtime-honesty.md)
**Seeded from**: operator field session 2026-08-23 (five screenshots after the v2.2.4 installer) plus a read-only review of the desktop app, sidecar, catalog, installer Hub snapshot, and injection-scanner allowlist. Not a comparison report.
**Goal**: On a machine that already installed models, Chatbot returns a real model reply, Image Studio returns image bytes, Video Lab returns a clip when weights exist, Settings > Models matches the installer catalog (scroll, badges, metadata, order, Downloaded), the chat list has visible rename/delete plus a collapse pill, and install plus in-app Hub update land on the latest Nexus-Hub release rather than a frozen 3.12.0 snapshot.

## Constitution Check

No constitution file found at `docs/archive/v2/v2.2/constitution.md` (or `docs/constitution.md`). Skipping the MUST/SHOULD gate. Recommend `/constitution` if this project wants a durable principle file. This is informational, not blocking.

Grounding that was read instead:

- `AGENTS.md` (local-first, four pillars, no invented capability, evidence tiers)
- `docs/archive/v2/v2.2/known-gaps.md` (v2.2.4 closed BG-33 through BG-42 at unit/integration evidence; DF-2, DF-4, DF-23, DF-24 still open)
- `docs/archive/v2/v2.2/plans/v2.2.4-chatbot-first-and-runtime-honesty.md` (honesty and shell locks; live GPU and packaged Hub left unproven)
- No `STRATEGY.md` / `docs/STRATEGY.md` and no `docs/solutions/` store

This plan serves the same local-first persona as v2.2.4: a laptop user who already ran the installer and expects the desktop app to talk to the models they just downloaded. The new bar is field-true generation, not another honesty-only cycle.

## Current model map

**Model map status**: fresh as of 2026-08-23; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|------|-----------|--------|--------|--------|
| frontier | claude-fable-5 | gpt-5.6-sol | gemini-3.7-flash | grok-4.6 |
| strong | claude-opus-5 | gpt-5.6-terra | gemini-3.7-flash | grok-4.5 |
| standard | claude-sonnet-5 | gpt-5.6-terra | gemini-3.7-flash | composer-2.5 |
| fast | claude-haiku-4-5 | gpt-5.6-luna | gemini-3.7-flash | composer-2.5-fast |

Google's public 2026-08-13 changelog lists Gemini 3.7 Flash (`gemini-3.7-flash`) as the GA workhorse. A distinct Gemini 3.7 Pro API slug was not confirmed from official docs in this session, so Flash fills every Google cell rather than inventing a Pro id.

Host-platform enumeration may confirm the Cursor picker at implement time. It must not collapse this table to one provider.

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/about-claude/models/overview
- OpenAI: https://developers.openai.com/api/docs/models and https://developers.openai.com/api/docs/guides/latest-model
- Google: https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/ and https://ai.google.dev/gemini-api/docs/changelog
- Cursor: https://cursor.com/docs/models-and-pricing

## Review verdict (this session)

**NO-GO** as a working product. v2.2.4 made failures honest (user bubble for "Hi", empty image completes become error text, Settings uses installer tabs). The operator still cannot run a model once. Honesty without generation is not success.

This review is a **targeted field-failure review**, not a ten-lens full-repo audit. Scope is the five screenshots plus the code paths they exercise.

### Project health

| Surface | Status | Detail |
|---|---|---|
| Git version control | OK | Repo present with history on `develop` |
| Version number | OK | Product cycle v2.2.4 on develop; root `package.json` stays 2.1.0 until release handoff; `desktop/package.json` 1.5.0 drift is pre-existing. Do not bump either. |
| Branch model | OK | `develop` + `main` in use |
| Baseline docs | OK | README, CHANGELOG, known-gaps, per-version plans |
| Per-version docs tree | OK | `docs/archive/v2/v2.2/plans/` and `known-gaps.md` present |

### Why v2.2.4 still fails in the field

v2.2.4 closed BG-36 through BG-42 at Vitest / Rust evidence. Packaged GPU, live Ollama, and live Hub network paths were labeled DF-2, DF-4, and DF-23. The new screenshots are those unproven paths, plus four defects that unit tests never could see: a dual catalog id namespace, a Models list with no overflow, Compatible badges that ignore VRAM, and a Hub snapshot frozen at 3.12.0.

### Root causes (supported)

#### R1 -- Chatbot: two model-id namespaces (screenshot 3) -- P0

**Supported.** Catalog id for Gemma 4 12B is `gemma-4-12b-it-gguf` (`core/registry/catalog.json`, Ollama URL `ollama://gemma4:12b`). Chat picker lists that catalog id. Send calls `chat.session.start` with `chat?.modelId ?? modelId` (`desktop/src/modules/chat/ChatPage.tsx` `sendChatTurn`). Sidecar `ChatSessionManager.start` calls `requireModel` (`desktop/sidecar/src/coding/models.ts`), which looks up `ModelCatalog.listLlm()` (`core/registry/ModelCatalog.ts`). That LLM table still starts at `gemma4:e4b` and does not contain `gemma-4-12b-it-gguf`. Result: `(chat unavailable) sidecar request error: Unknown model id: gemma-4-12b-it-gguf`.

v2.2.4 Phase 3 appended the user bubble before residency returns. That is why "Hi" is visible. The session still dies on `requireModel`. Same class as v2.2.4 R4/R6 (dropdown vs send id), not closed for GGUF catalog ids vs Ollama tags vs the coding LLM table.

#### R2 -- Settings > Models cannot scroll (screenshot 1)

**Supported.** `desktop/src/pages/settings/ModelsSettings.tsx` `listStyle` is a flex column `ul` with no `overflowY`, no `flex: 1`, no `minHeight: 0`. `pageStyle` is also a flex column. `SettingsPage` shell is `flex: 1; flex-direction: column` with no overflow. Serving / Fine-tuning settings already use `overflowY: "auto"`. The Models list grows until the window clips it. Qwen 3.5 4B is cut off with no scrollbar.

#### R3 -- Qwen 3.5 4B shows Download after a successful-looking install (screenshot 1)

**Supported as a class of bugs; live Ollama tags not re-probed in this session.** Catalog id is `qwen3.5:4b`. Downloaded in Settings is `item.installed && item.source !== "catalog-only"`. Installed-ness is Ollama `/api/tags` intersect plus `~/.nexus/selected-models.json`. Likely stacked causes:

1. Installer family collapse can hide or skip the 4B pull once 9B fits (`catalog.json` differentiators text; `tier_defaults.py`).
2. Snapshot `orderedIds` may record a different sibling than the card the operator ticked.
3. Probe matches Ollama tags (`qwen3.5:4b`) while the card id or snapshot uses another spelling.
4. Pull appeared to finish in the wizard UI but Ollama never registered the tag.

v2.2.5 must treat snapshot + Ollama tags + catalog id as one residency contract, and show Downloaded only when the tag is actually present.

#### R4 -- Compatible plus Needs 20 GB VRAM (screenshot 2)

**Supported.** `recommendationKind()` in `desktop/src/shared/models/catalogTabs.ts` returns `required` / `recommended` from tags, else always `"compatible"`. Hardware fit is a separate `modelFitsHost` check. `ModelCard` still paints the Compatible badge while `RowActions` shows "Needs N GB VRAM". The installer `_card_status` in `typed_catalog.py` never returns Compatible when `fits` is false. Settings must use that same badge priority.

#### R5 -- Settings cards are not installer cards (screenshot 2)

**Supported.** `ListedModelDto` has description / strengths / license / size / VRAM. It does **not** marshal `releaseDate`, `origin`, or `uncensored` even though `catalog.json` has them. `modelsOnTab` filters only; it does not sort by recommended, then release date, then name. There is no shared card component with the Qt wizard. v2.2.4 Phase 5 shipped tabs + Download/Downloaded, not visual or metadata parity.

#### R6 -- Chat list: rename/delete only via context menu; no chats-pane pill (screenshot 3)

**Supported.** `FolderTree.tsx` documents right-click Rename/Delete, F2, and double-click rename. There are no per-row icon buttons. Left-click on an already-selected chat opens the chat; it does not enter rename. The main rail has an edge collapse pill from v2.2.4; the chats `aside` is a fixed 280px column with no twin pill.

#### R7 -- Image generation completes without bytes (screenshot 4); video same class

**Supported.** Sidecar `handlers.ts` throws `Image generation completed without image bytes.` when `result.pngBase64` is empty. The UI maps that to the screenshot 4 string. v2.2.4 Phase 4 made empty completes honest. It did **not** make diffusion return PNG bytes. Video analog: complete without `mp4Path`. Residual live-GPU remains DF-4 until a real generate on this host succeeds. This cycle's DoD requires that field proof, not another error-copy tweak.

#### R8 -- Hub Active 3.12.0; sync blocked by injection scanner (screenshot 5)

**Supported.** Installer `hub_catalog_provisioner.py` is offline-first: extract bundled snapshot, then optional `--sync-hub-catalog`. Tests and the frozen snapshot pin `v3.12.0`. That is why Settings shows Active 3.12.0. In-app Update hits `NexusHubSyncer` fail-closed `PromptInjectionScanner`. `HUB_SKILL_SCAN_ALLOWLIST` was reviewed against Hub **v3.10.0**. A 3.12.0 -> 3.19.2 diff (`+9 new, ~101 modified, -295 removed`) trips the scanner, so apply never happens. Do not disable the scanner. Refresh the allowlist against the new tag and make install + pack-time snapshot resolve **latest** when the network is available.

### User-reported issues (must each appear in a sub-task)

U1. Settings > Models list does not scroll; later cards are clipped.
U2. Qwen 3.5 4B shows Download after the operator selected it in the installer and saw a successful-looking pull.
U3. Compatible badge on a model that Needs 20 GB VRAM.
U4. Settings cards missing installer metadata (release date, country of origin, censored/uncensored) and installer sort (Recommended first, then performance / release date).
U5. Settings card visuals must match the installer cards (not a thinner Settings-only layout).
U6. Chatbot "Hi" still fails (`Unknown model id: gemma-4-12b-it-gguf`).
U7. Chat list: visible rename and delete icons; keep right-click; left-click on an already-selected chat enters rename.
U8. Chats pane gets the same edge collapse/expand pill as the main rail.
U9. Image generate must produce image bytes (not only an honest error). Treat video as the same class.
U10. Hub install must pull the latest available release. In-app Update must apply that latest, not stay on 3.12.0 behind a scanner block.

### Locked product decisions

- **Success bar**: at least one real Chatbot reply, one real Image Studio PNG, and one Video Lab clip (when video weights are installed) on the operator machine. Vitest-only honesty is not enough.
- **Single id space**: every picker, snapshot row, Ollama tag, `requireModel`, and Settings card resolve through one alias table. Catalog id, Ollama tag, and coding LLM id are aliases of the same model, never competing sources of truth.
- **Downloaded**: true only when the runtime (Ollama `/api/tags` or weights dir) actually has the artifact. Snapshot membership alone is not enough.
- **Badge priority** (installer `_card_status`): Required > does-not-fit (show the VRAM/RAM reason, never Compatible) > Recommended > Compatible.
- **Sort**: Recommended / Required first, then newer `releaseDate`, then display name. Match the installer.
- **Cards**: shared field set with the Qt catalog card (description, best-for, origin, release date, censored/uncensored, size, VRAM, license, badge, Download vs Downloaded). React will not iframe Qt; it must look the same.
- **Chat explorer**: right-click stays. Icon buttons are required. Second left-click on the selected chat enters inline rename. Chats pane collapse pill matches the rail pill pattern (`nexus-sidebar-collapse-pill`).
- **Hub**: fail-closed scanner stays. Allowlist is reviewed per Hub tag, not a blanket mute. Installer with network pulls latest; pack-time snapshot is rebuilt from that latest, not from a hardcoded 3.12.0. Offline install may extract the bundled latest-at-build snapshot, then refresh on first app Update.
- **Shipping version**: do not bump root `package.json` (2.1.0) or `desktop/package.json` (1.5.0). Plan version is v2.2.5.
- **Do not re-run** v2.2.0-v2.2.4 as greenfield. Keep v2.2.4 product locks (cold start Chatbot, tab labels, peeking never loads weights, tools always on).
- **Do not disable** the injection scanner to make Hub Update green.

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|-------|-------|---------|------------------------|--------------------------|
| 1 | Unify model ids and make Chatbot talk | One alias table; "Hi" gets a model reply | frontier | max |
| 2 | Diffusion actually returns bytes | Image PNG and video path on installed weights | frontier | max |
| 3 | Settings Models installer parity | Scroll, badges, metadata, sort, Qwen residency | strong | high |
| 4 | Chat explorer chrome | Icons, reclick rename, chats-pane pill | standard | medium |
| 5 | Hub latest at install and Update | Latest tag installed; scanner allowlist refreshed | strong | high |
| 6 | Architecture, known-gaps, CI | Layout clean, gaps honest, CI covers the contract | frontier | max |

---

## Task inventory (T###)

Use these ids in commits and tests. Phases below own the prompts.

- T001 [P] [core/registry] Canonical alias table: catalog id, Ollama tag, coding LLM id, HF/weights id for every shipped LLM and diffusion entry.
- T002 [P] [desktop/sidecar/src/coding/models.ts] `requireModel` / `lookupModel` resolve aliases; never throw on a catalog id that maps to a known runtime tag.
- T003 [P] [desktop/src/modules/chat/ChatPage.tsx] Send uses the resolved runtime id; picker display name stays; select value === start payload after alias fold.
- T004 [P] [core/registry + sidecar modelsService] Installed probe: snapshot id OR any alias present in Ollama tags / weights dir counts as installed.
- T005 [P] [desktop tests] Regression: start+send with `gemma-4-12b-it-gguf` when `gemma4:12b` is the Ollama tag; coding session with `gemma4:e4b` still works.
- T006 [P] [desktop/sidecar diffusion] Trace empty `pngBase64`; fix dispatcher/runtime so a successful job returns bytes or a typed pre-complete error (runtime not loaded, wrong model id, img2img missing source).
- T007 [P] [desktop/sidecar video] Same contract for `mp4Path`.
- T008 [P] [desktop Image/Video pages] Keep honest empty-complete errors; add a sidecar-down / runtime-not-ready path that does not claim "completed".
- T009 [P] [desktop/src/pages/settings/ModelsSettings.tsx] List region `flex: 1; minHeight: 0; overflowY: auto` through Settings shell so every card is reachable.
- T010 [P] [catalogTabs.ts + ModelCard] Badge = installer `_card_status` (never Compatible when over budget).
- T011 [P] [ListedModelDto + sidecar marshal] Surface `releaseDate`, `origin`, `uncensored`; render pills on the card.
- T012 [P] [ModelsSettings] Sort Recommended/Required first, then `releaseDate` desc, then name. Card chrome matches installer (title row, description, best-for, chips, actions).
- T013 [P] [selection snapshot + probe] Qwen 3.5 4B Downloaded iff the 4B tag is on disk; if installer skipped the pull, say so; do not show Download as "never selected" when snapshot says selected and pull failed.
- T014 [P] [FolderTree.tsx] Per-row rename and delete icon buttons; keep context menu.
- T015 [P] [FolderTree.tsx] Left-click on already-selected chat enters rename; first click still opens.
- T016 [P] [ChatPage.tsx] Edge collapse pill for the chats pane, same pattern as the main rail.
- T017 [P] [hub_catalog_provisioner.py + build-hub-snapshot.py] Pack-time snapshot from latest Hub release; install with network syncs latest; tests stop treating 3.12.0 as the production pin.
- T018 [P] [hubSkillScanAllowlist.ts] Re-review suppressions against the new Hub tag; keep fail-closed; untrusted install stays strict.
- T019 [P] [ipcSkillsClient + NexusHubSyncer] Update applies when scan passes; surface remaining findings without a fake success.
- T020 [P] [docs/archive/v2/v2.2/known-gaps.md] Reopen field-false v2.2.4 rows as BG-43+; keep DF-2 until packaged soak; mark live generate proven only with operator evidence.
- T021 [P] [CI] Unit/integration for alias, scroll, badges, Hub allowlist fixtures; no live GPU in PR CI; document the operator field checklist.

---

## Phase 1: Unify Model Ids and Make Chatbot Talk

**Goal**: "Hi" with the visible picker model produces a real local-model reply (or a typed Ollama-down / not-pulled error that names the runtime tag). `Unknown model id: gemma-4-12b-it-gguf` is impossible.
**Prerequisites**: None.
**Stability Gate**: A test starts a chat session with catalog id `gemma-4-12b-it-gguf` and `requireModel` succeeds via alias to `gemma4:12b` / the coding entry. ChatPage send payload id after fold equals an id the sidecar runner can load. Manual: operator sends Hi to Gemma 4 12B (or the first actually-pulled LLM) and gets tokens.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Dual catalogs, three id spellings, picker vs session vs Ollama. High-risk, load-bearing, prior cycle missed it.

### Sub-tasks

#### 1.1 -- Canonical alias table (T001)

**Objective**: One module maps every shipped model to catalog id, Ollama tag, coding LLM id, and weights id.

**Failure modes**: Unknown catalog id: throw a structured error that lists known aliases, never a silent fallback to `gemma4:e4b`. Duplicate aliases: fail tests at catalog-load. Missing coding entry for a chat-only model: still allow chat via Ollama tag; coding tools stay unavailable.

**Build class**: load-bearing.

**Prompt**:
> Implement Nexus AI Studio v2.2.5 Phase 1.1 from docs/archive/v2/v2.2/plans/v2.2.5-first-successful-generation.md. Follow AGENTS.md. Do not bump package.json.
>
> Add a single alias table consumed by sidecar `requireModel`, installed probe, and Chat/Agents pickers. Source of truth is `core/registry/catalog.json` `id` plus `source.url` (e.g. `ollama://gemma4:12b`). Project `ModelCatalog.ts` LLM ids (`gemma4:e4b`, ...) as aliases of those rows, not a second catalog. Cover Gemma 4 12B (`gemma-4-12b-it-gguf` <-> `gemma4:12b`), Qwen 3.5 4B/9B (`qwen3.5:4b` / `qwen3.5:9b`), LFM2.5, and every diffusion id used by Image/Video pickers.
>
> Acceptance: `lookupModel("gemma-4-12b-it-gguf")` and `lookupModel("gemma4:12b")` return the same runtime record. Tests in `desktop/tests/coding-models.test.ts` and a new alias test. No hardcoded `gemma4:e4b` as the send default unless that id is actually installed.

#### 1.2 -- Sidecar requireModel and installed probe (T002, T004)

**Objective**: Session start and residency use aliases.

**Failure modes**: Ollama down: typed "runtime unavailable", not Unknown model id. Tag present under an alias: installed true. Snapshot lists 4B but Ollama does not: installed false, UI may say "selected at install but not on disk".

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.5 Phase 1.2. Change `desktop/sidecar/src/coding/models.ts` `requireModel` / `lookupModel` to resolve the alias table. Change the installed probe (`core/registry` / `modelsService`) so a model is installed if any alias appears in Ollama `/api/tags` or the weights directory. Keep snapshot as "operator selected / in-app downloaded" not as proof of bytes. Tests: catalog id installed when tags contain `gemma4:12b`; unknown id still throws `Unknown model id`.

#### 1.3 -- Chat send id fold (T003, T005)

**Objective**: Picker value and `chat.session.start` modelId are the same after alias fold.

**Failure modes**: Stale chat.modelId from an old session: fold on send. Empty installed list: do not start; keep v2.2.4 user bubble plus install CTA.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.5 Phase 1.3. In `ChatPage.tsx` `sendChatTurn`, pass the alias-folded runtime id into `chat.session.start`. Ensure `activeChat.modelId` cannot stay on a catalog id the sidecar does not resolve. Mirror on Agents/coding session start. Tests: Hi with listed model `gemma-4-12b-it-gguf` starts a session whose sidecar id is the Ollama/coding alias. Screenshot 3 unknown-id error must be impossible when the alias exists.

#### 1.4 -- Testing and Stabilization

**Prompt**:
> Run desktop vitest for chat, coding models, installed probe. Add CI coverage for the alias module. Then `/generate-session-history` for Phase 1. Operator field check: send Hi to an actually pulled LLM and confirm tokens (or a typed Ollama error). Do not mark Phase 1 done on Unknown model id.

### Phase 1 Exit Checklist

- [x] Alias table covers catalog + Ollama + coding ids
- [x] requireModel accepts Gemma 4 12B catalog id
- [x] Installed probe uses aliases
- [x] Hi produces a model reply or typed runtime error
- [x] Tests passing
- [x] Session history written

---

## Phase 2: Diffusion Actually Returns Bytes

**Goal**: Image Studio returns PNG bytes for a txt2img (and img2img when a source image is attached). Video Lab returns an output path when video weights are installed. Empty complete remains an error, but a healthy runtime no longer completes empty.
**Prerequisites**: Phase 1 alias table (image/video picker ids must resolve to the same ids the dispatcher loads).
**Stability Gate**: Unit tests: dispatcher returns non-empty `pngBase64` on a fake runtime; empty still errors. Field: operator prompt on an installed image model yields a visible image. If GPU cannot run, the error must be "runtime/GPU not ready" before complete, not "completed without image bytes".
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Live generate path, GPU, img2img. v2.2.4 stopped at honesty. DF-4 cannot remain a silent pass for this cycle's DoD.

### Sub-tasks

#### 2.1 -- Trace and fix empty pngBase64 (T006, T008)

**Objective**: A successful diffusion job always has image bytes; failures happen before `complete`.

**Failure modes**: Runtime process missing: error "image runtime is not running". Unknown model id: same alias fold as Phase 1. Img2img without bytes: reject at enqueue. Job complete with empty payload: keep today's throw, plus a log of which backend returned empty.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.5 Phase 2.1 from docs/archive/v2/v2.2/plans/v2.2.5-first-successful-generation.md. Follow AGENTS.md. Do not bump package.json.
>
> Trace `desktop/sidecar/src/handlers.ts` studio pump, `diffusion/dispatcher.ts`, and the Python/Comfy/diffusers runtime. Find why `pngBase64` is empty on a job that still completes. Fix the generate path (load weights by aliased id, img2img source, backend stdout). Do not paper over emptiness in the UI. ImageStudioPage keeps `isUsableImageBase64`. Add tests with a stub runtime that returns bytes and one that returns empty (still errors). Log backend stderr on empty complete.

#### 2.2 -- Video analog (T007)

**Objective**: Video complete without `mp4Path` is the same class as empty PNG.

**Failure modes**: No video weights: disable generate with install CTA, do not enqueue. FFmpeg missing: typed error.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.5 Phase 2.2. Mirror Phase 2.1 for video jobs (`mp4Path`). Alias-fold the picker id. Tests for empty path vs stub file path. Field: if video weights are installed, one short generate; if not, record DF and do not claim video works.

#### 2.3 -- Testing and Stabilization

**Prompt**:
> Sidecar + ImageStudioPage + VideoLab tests. Document the operator GPU checklist. CI stays fixture-based (DF-4 is not a PR GPU job). `/generate-session-history` for Phase 2.

### Phase 2 Exit Checklist

- [x] Successful stub generate returns bytes
- [x] Empty complete still errors
- [x] Field image generate works on operator GPU or typed pre-complete error
- [x] Video path fixed or explicitly deferred with evidence
- [x] Tests passing
- [x] Session history written

---

## Phase 3: Settings Models Installer Parity

**Goal**: Settings > Models scrolls, sorts, badges, and cards like the installer. Qwen 3.5 4B Downloaded matches disk. Origin, release date, and censored/uncensored are visible.
**Prerequisites**: Phase 1 (residency aliases).
**Stability Gate**: A tall window clips the list with a working scrollbar. SANA 1.6B 4K on a 16 GB host is not Compatible. Recommended image model is above Compatible rows. Qwen 3.5 4B is Downloaded iff `qwen3.5:4b` (or alias) is in Ollama tags.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Layout plus catalog DTO plus sort/badge policy. Crosses installer semantics into React.

### Sub-tasks

#### 3.1 -- Scroll the Models list (T009, U1)

**Objective**: Every catalog row is reachable.

**Failure modes**: Short catalog: no stray scrollbar covering cards. Nested flex: `minHeight: 0` on every parent from settings shell to `ul`.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.5 Phase 3.1. Fix `SettingsPage` + `ModelsSettings` so the model list is a flex child with `flex: 1`, `minHeight: 0`, `overflowY: auto`. Match ServingSettings overflow. Test: render more rows than the viewport (jsdom layout or a style assertion on the list testid). Screenshot 1 clipped Qwen 3.5 4B must be scrollable.

#### 3.2 -- Badge and sort parity (T010, T012, U3, U4)

**Objective**: Same status language and order as `typed_catalog.py`.

**Failure modes**: Unknown VRAM: do not invent Compatible. Missing releaseDate: sort those last within the same badge group.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.5 Phase 3.2. Change `recommendationKind` (or replace it) so over-budget models never display Compatible; they display the installer incompatibility string (Needs N GB VRAM). Sort `modelsOnTab`: Required, Recommended, then Compatible-that-fits, then over-budget, each group by `releaseDate` desc then name. Tests from catalog fixtures including SANA 1.6B 4K vs a 16 GB host.

#### 3.3 -- Installer card fields and chrome (T011, T012, U4, U5)

**Objective**: Cards show origin, release date, censored/uncensored, and installer-like layout.

**Failure modes**: Missing origin: omit the chip, do not show "Unknown". Uncensored false: show Censored or the installer wording, not a blank.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.5 Phase 3.3. Extend `ListedModelDto` and sidecar marshal from `catalog.json` (`origin`, `releaseDate`, `uncensored`). Restyle `ModelCard` to match `_ModelCard` in `typed_catalog.py` (title + badge + size, description, best-for, chip row, Download/Downloaded). Do not iframe Qt. Visual snapshot or DOM tests for chips. Share copy helpers if the installer Python and TS would otherwise drift.

#### 3.4 -- Qwen 3.5 4B residency (T013, U2)

**Objective**: Download vs Downloaded matches disk and installer selection truthfully.

**Failure modes**: Selected but pull failed: message "Selected during setup but not found in Ollama" plus Retry, not a silent Download as if never chosen. Family collapse skipped 4B: do not claim the installer downloaded it.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.5 Phase 3.4. After Phase 1 aliases, assert Settings Downloaded for `qwen3.5:4b` iff the probe sees the tag. If snapshot `orderedIds` contains 4B and Ollama does not, show selected-but-missing. Fix installer snapshot writer / family collapse if the wizard ticks 4B but never pulls it (`runtime_provisioner.py` / `model_puller.py` / `tier_defaults.py`). Tests for snapshot+tags matrix.

#### 3.5 -- Testing and Stabilization

**Prompt**:
> ModelsSettings + catalogTabs + installer snapshot tests. CI. `/generate-session-history` for Phase 3.

### Phase 3 Exit Checklist

- [x] List scrolls
- [x] No Compatible+Needs-VRAM
- [x] Origin / date / censored chips
- [x] Recommended first
- [x] Qwen 4B residency truthful
- [x] Tests passing
- [x] Session history written

---

## Phase 4: Chat Explorer Chrome

**Goal**: Each chat row has rename and delete icons; right-click remains; reclick selected chat renames; chats pane has an edge collapse pill.
**Prerequisites**: None (can parallelize after Phase 1 starts). Must not block Phases 1-2.
**Stability Gate**: Tests click the delete icon, rename icon, and second left-click-to-rename. Collapse pill hides the 280px aside and shows an expand pill on the inner edge.
**Recommended model tier**: standard
**Recommended effort level**: medium
**Rationale**: Localized FolderTree + ChatPage layout. No sidecar protocol change.

### Sub-tasks

#### 4.1 -- Row icons (T014, U7)

**Objective**: Visible rename and delete without relying on right-click.

**Failure modes**: Delete still confirms. Icons do not steal the open-chat click (stopPropagation). Keyboard Delete/F2 stay.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.5 Phase 4.1. In `FolderTree.tsx`, add lucide (or existing) pencil and trash buttons on each chat row. Keep the context menu. Tests for icon click handlers.

#### 4.2 -- Recklick rename (T015, U7)

**Objective**: Left-click on the already-selected chat enters inline rename.

**Failure modes**: First click on an unselected chat still opens/selects only. Rename commit on Enter/blur; Escape cancels.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.5 Phase 4.2. If selected kind is chat and the user left-clicks the same chat id, call the existing start-rename path. Tests.

#### 4.3 -- Chats pane pill (T016, U8)

**Objective**: Same collapse control language as the main rail.

**Failure modes**: Collapsed: transcript still usable; expand target is at least 24px. Persist collapsed state in localStorage with a namespaced key. Do not collapse the main rail when toggling chats.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.5 Phase 4.3. Reuse the rail collapse pill pattern from v2.2.4 Sidebar on the ChatPage chats `aside` edge. Tests for collapsed width and expand.

#### 4.4 -- Testing and Stabilization

**Prompt**:
> FolderTree + ChatPage tests. CI. `/generate-session-history` for Phase 4.

### Phase 4 Exit Checklist

- [x] Rename/delete icons
- [x] Recklick rename
- [x] Chats pane pill
- [x] Right-click still works
- [x] Tests passing
- [x] Session history written

---

## Phase 5: Hub Latest at Install and Update

**Goal**: A networked install leaves Active = latest Hub release. In-app Update applies that release. 3.12.0 is not a magic product version.
**Prerequisites**: None strictly; do not block Phases 1-2.
**Stability Gate**: Provisioner tests use a stub latest tag (not hardcoded 3.12.0 as the only success). Allowlist review notes the new tag. Fixture sync with scanner findings in educational skills applies after suppressions; a real injection still blocks.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Trust-boundary scanner plus installer snapshot. Easy to "fix" by disabling the scanner (forbidden).

### Sub-tasks

#### 5.1 -- Latest Hub at pack and install (T017, U10)

**Objective**: Snapshot and install resolve latest, not v3.12.0.

**Failure modes**: Offline install: extract bundled snapshot (latest at pack time), warn, succeed. Network sync fail: keep snapshot, surface the error, do not abort the whole app install unless catalog is missing entirely. Latest tag API fail: fail the pack job rather than silently ship 3.12.0.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.5 Phase 5.1. Change `scripts/installer/build/build-hub-snapshot.py` and `hub_catalog_provisioner.py` so pack-time and networked install pull the latest Nexus-Hub release. Stop treating `v3.12.0` as the production pin in tests; inject a fake latest. Document the snapshot tag in the manifest. Follow AGENTS.md. Do not bump package.json.

#### 5.2 -- Scanner allowlist for the new tag (T018, T019, U10)

**Objective**: Update now can apply a legitimate Hub catalog.

**Failure modes**: New finding in a non-allowlisted skill: still block. Third-party `skills install`: no Hub allowlist. Diff summary still visible when blocked.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.5 Phase 5.2. Re-review `HUB_SKILL_SCAN_ALLOWLIST` against the latest Hub tag (same discipline as the v3.10.0 comments in `core/skills/hubSkillScanAllowlist.ts`). Do not disable `PromptInjectionScanner`. Keep `ipcSkillsClient` scanner copy. Tests: fixture bundle that used to block now applies iff each suppression is justified; a planted jailbreak still blocks.

#### 5.3 -- Testing and Stabilization

**Prompt**:
> Installer pytest + sidecar/skills tests. CI. `/generate-session-history` for Phase 5.

### Phase 5 Exit Checklist

- [x] Latest tag at pack/install
- [x] Allowlist refreshed, scanner on
- [x] Update can apply
- [x] Tests passing
- [x] Session history written

---

## Phase 6: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, and its CI/CD complete and optimized.
**Prerequisites**: All prior phases.
**Stability Gate**: The layout is clean (no deprecated/obsolete files, empty dirs, redundant files/dirs, or overcomplicated structure left un-triaged); the version's known gaps are reconciled; CI/CD covers every change and is optimized; project validation/tests pass.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, reference repair, known-gap reconciliation, and release gating carry high context volume and blast radius.

### Sub-tasks

#### 6.1 -- Architecture refactor

**Objective**: Refactor toward a well-organized, intuitive layout.

**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure, then refactor toward a clean, intuitive layout via project-refactor and docs-layout-refactor (propose-then-apply, with confirmation; repair every reference for anything that moves). Collapse duplicate model-id lists if Phase 1 left a temporary shim. Follow AGENTS.md. Do not bump package.json.

#### 6.2 -- Known-gaps reconciliation (T020)

**Objective**: Reconcile the version's open gaps.

**Prompt**:
> Update `docs/archive/v2/v2.2/known-gaps.md` for v2.2.5. Add BG-43 (dual model ids), BG-44 (Models overflow), BG-45 (Qwen 4B residency), BG-46 (Compatible vs VRAM), BG-47 (installer card parity), BG-48 (chat row icons/pill), BG-49 (empty diffusion bytes), BG-50 (Hub 3.12 pin + scanner). Close them only with evidence. Keep DF-2 (packaged Explorer) open unless a packaged soak ran. Keep DF-4 open unless the operator GPU generate is recorded. Do not claim screenshot failures closed at unit evidence only. Follow evidence-and-support-tiers.md: not_observed != absent.

#### 6.3 -- CI/CD create/update/optimize (T021)

**Objective**: CI/CD covers all changes and is optimized.

**Prompt**:
> Create or update the CI/CD pipeline so it covers alias tests, Models overflow/badge tests, Hub allowlist fixtures, and installer snapshot-latest stubs. Optimize action minutes (path filters, concurrency cancel-in-progress, dependency caching, gating expensive-OS or matrix jobs to merges/schedule) while keeping comprehensive testing. Do not add a live GPU or live Hub clone to PR CI.

#### 6.4 -- Cross-installer parity

**Objective**: Prevent operating-system installer drift when the repository ships multiple installers.

**Prompt**:
> If this repository ships more than one installer, run or add a declarative cross-installer parity gate covering Hub snapshot latest-resolution and selection-snapshot writes. If Unix installers skip Hub latest-sync, record a known-gap rather than claiming parity. Silent no-op only if no installer files changed.

#### 6.5 -- Testing and Stabilization

**Objective**: Prove the refactor preserved behavior and CI/CD is green.

**Prompt**:
> Run the full validation/test suite, confirm the refactor changed no behavior, confirm CI/CD passes, and iterate until clean. Generate a session-history entry for this phase. Then rebuild the Windows installer for operator verification (unsigned is fine). Stop before `/update release` unless asked.

### Phase 6 Exit Checklist

- [x] Refactor done
- [x] known-gaps.md updated
- [x] CI covers new contracts
- [x] Cross-installer Hub/snapshot noted
- [x] Session history written

---

## Complexity Tracking

> Fill ONLY if Constitution Check has violations that must be justified

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| | | |

---

## Out of scope

- Re-running v2.2.0-v2.2.4 as greenfield.
- Loading models on tab peek.
- Streaming chat tokens (DF-21).
- Native file dialogs (DF-16).
- Node SEA (DF-1).
- Live-GPU as a PR CI gate (operator field checklist is required instead).
- Light theme.
- Bumping package.json.
- Disabling the Hub injection scanner.
- Changing Hub's upstream repo.

## Residual risks (label, do not hide)

- A host without a working GPU still cannot paint a SANA image. Phase 2 must fail before complete with a runtime error, not claim success.
- Packaged Explorer launch (DF-2) stays unproven until a packaged soak.
- Hub latest requires network at install or at first Update; offline-only hosts keep the pack-time snapshot.
- Family-collapsed installer siblings can still confuse operators if copy is weak; Phase 3.4 must tell the truth.

## Operator field checklist (required before calling v2.2.5 done)

1. Settings > Models, Agentic: scroll to the last card. Qwen 3.5 4B matches Ollama (Downloaded or honest missing).
2. Settings > Models, Image: no Compatible+Needs-VRAM. Recommended near the top. Origin / date / censored visible.
3. Chatbot: send Hi to the visible installed model. User bubble plus a real reply (not Unknown model id).
4. Chat list: rename icon, delete icon, reclick rename, chats-pane pill.
5. Images: one generate with an installed image model produces a picture.
6. Videos: one generate if weights exist; otherwise record skip.
7. Settings > Skills: Active matches latest Hub (or pack-time latest) and Update applies without a scanner false block.

## Copyable implementation prompt

Paste this into a **new session** to run the whole plan: implement, test, iterate, troubleshoot, and finish. Do not stop at unit tests if Chatbot or Image still fail on the operator machine.

```
Implement Nexus AI Studio v2.2.5 from docs/archive/v2/v2.2/plans/v2.2.5-first-successful-generation.md. Follow AGENTS.md. Do not re-run v2.2.0-v2.2.4 as greenfield. Do not bump package.json (root 2.1.0, desktop 1.5.0) unless asked. Do not disable the Hub injection scanner. Do not run /update release unless asked.

PRODUCT LOCKS (keep from v2.2.4)
- Cold start always Chatbot. Peeking a tab never loads weights.
- Tab labels: Chatbot, Agents, Images, Videos.
- Tools always on. User bubble before residency errors.

SUCCESS BAR (this cycle)
- Chatbot "Hi" to the visible installed model returns model tokens. Unknown model id: gemma-4-12b-it-gguf is a P0 blocker.
- Image generate returns image bytes on installed weights (or a typed runtime-not-ready error BEFORE complete). Empty "completed without image bytes" on a healthy GPU is a P0 blocker.
- Settings > Models scrolls; badges match installer hardware fit; cards show origin, release date, censored; Recommended first; Qwen 3.5 4B Downloaded iff the tag is on disk.
- Chat list: rename + delete icons, right-click stays, reclick selected chat renames, chats pane has an edge collapse pill.
- Installer + in-app Hub Update land on latest Nexus-Hub, not a frozen 3.12.0, with a reviewed scanner allowlist.

HOW TO WORK
- Implement Phase 1 through Phase 6 in order. Commit once per phase (ASCII commit messages, no Co-Authored-By).
- After each phase: run the phase tests, fix failures, do not skip the stability gate.
- After Phase 6: commit, push develop, rebuild the Windows installer (unsigned OK).
- Then /review the original five screenshot failures. Iterate until the operator field checklist in the plan is true. Commit those fixes. Push. Rebuild installer again if needed.
- Stop before /update release.

Start at Phase 1.1. Stop at the phase stability gate. Do not skip to Phase 6 until 1-5 are done. Phase 1 and Phase 2 are not done until a real model reply and a real image (or a typed pre-complete GPU error) are proven -- Vitest honesty is not enough.
```
