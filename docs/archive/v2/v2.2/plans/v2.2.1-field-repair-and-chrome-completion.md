# Plan - Field Repair and Chrome Completion

**Project**: Nexus AI Studio
**Version**: v2.2.1
**Slug**: field-repair-and-chrome-completion
**Filename**: v2.2.1-field-repair-and-chrome-completion.md
**Plan Type**: Feature / Enhancement (patch follow-on)
**Created**: 2026-08-22
**Predecessor**: [v2.2.0-runtime-repair-and-ux-overhaul.md](v2.2.0-runtime-repair-and-ux-overhaul.md)
**Goal**: After a v2.2.0 install the sidecar stays alive, the Pending approvals popover is always dismissable, every Settings tab uses the shared control primitives, and the sidebar is a compact icon rail with no User Profile row.

## Overview

v2.2.0 landed the plumbing and a first pass at shell chrome, then reported its eight phases complete in session history. A rebuilt installer on 2026-08-22 proved the cycle is not done in the field. Node 22.11.0 starts and then dies, so the next JSON-RPC write hits a closed stdin (Windows `ERROR_NO_DATA` 232). The Complete page prints `sidecar request error: The pipe is being closed. (os error 232) | stderr: [ / / Nodejs v22.11.0]`. Restart backend uses the same spawn path and cannot recover. Models stay at 0, Hub stays empty, and Approvals opens a red error card that cannot be closed.

The same session showed the chrome pass as incomplete. Settings Models restyled Type / Family / Status selects, then left Models Search as a native `input type="search"`. Skill Optimizer (and the rest of Settings) still uses flat unstyled text fields and gray buttons. The Approvals bell popover has no Close control, no Escape handler, and no click-outside listener, so once opened it follows the user across every tab. User Profile remains a sidebar row whose only job is to redirect to Settings. Compact mode exists but is not the default on a wide window.

This patch plan does not re-run the v2.2.0 eight-phase cycle. It closes the field gaps: keep the sidecar alive, make Approvals dismissable, finish the control sweep, and finish the sidebar contract the user already locked.

Success looks like: the next installer Complete page reports sidecar ok; opening Approvals and then clicking X, Escape, or outside closes it even when the sidecar is down; Skill Optimizer, Models Search, and every other Settings control match the already-styled selects; the sidebar is an icon rail with no wordmark and no User Profile item.

## Field evidence (2026-08-22)

Installer Complete page:

`Installed (health check FAILED -- fail: sidecar request error: The pipe is being closed. (os error 232) | stderr: [ / / Nodejs v22.11.0] -- the app backend cannot start`

That means provisioned Node was found. The child then exited. `desktop_provisioner.py` joins only the last three stderr fragments with `" / "`, which is why the UI shows garbage instead of the real stack.

In the running app (same session):

- Settings Models shows "The Nexus backend could not start" plus Restart / Show details. Installed / Available / External remain 0.
- GPU footer reads "GPU: unavailable" (expected while the sidecar is dead).
- Sidebar still showed a logo plus "Nexus AI Studio" in the installed build the user photographed. Current source comments claim the brand block is gone; v2.2.1 must pin that with a test and make compact the default.
- `ADMIN_ENTRIES` still contains User Profile pointing at `/profile`, which only `<Navigate>`s to Settings.
- Approvals is a sidebar bell. Opening it shows "Pending approvals" with the 232 error. No X, no Esc, no click-outside. `position: absolute; bottom: calc(100% + 4px)` with `width: 20rem` covers the main pane. Sidebar does not unmount on tab change, so the overlay follows the user.
- Skill Optimizer skill-id field and Preview button are unstyled native controls.

## Locked product decisions

Confirmed 2026-08-21, amended 2026-08-22 from the post-install session:

- **Ask Inbox**: stays as a capability. Quiet bell (badge only when pending > 0). The popover must be dismissable (X, Escape, click-outside) and must never cover its own toggle. Sidecar-down: do not auto-open, do not pin a red 232 dialog on every tab; the existing Settings backend banner is enough. Full list lives under Settings > Approvals. No auto-approve.
- **User Profile**: remove the sidebar item entirely. No tab that redirects to Settings. Display name may live under Settings > General. Silent `/profile` bookmark redirect is fine.
- **Sidebar**: no wordmark in the rail (title bar keeps the single brand); compact icon-rail is the default (~56 px), persisted, with expand toggle.
- **Settings chrome**: every Settings tab uses Select / SearchInput / TextField / Button / Switch. No leftover native search boxes, flat text fields, or unstyled Preview buttons.
- **Sidecar**: Node starting is not success. The child must still be running when the first JSON-RPC line is written. Healthcheck must name the real crash.

## Copyable implementation prompt

Paste the block below into a new agent session to implement this cycle. Canonical plan: this file. Predecessor (do not re-run as greenfield): [v2.2.0-runtime-repair-and-ux-overhaul.md](v2.2.0-runtime-repair-and-ux-overhaul.md).

```
Implement Nexus AI Studio v2.2.1 from docs/archive/v2/v2.2/plans/v2.2.1-field-repair-and-chrome-completion.md. Follow AGENTS.md. Do not re-run the whole v2.2.0 cycle; close what the field install still proves broken, and finish the user-reported chrome that v2.2.0 claimed done.

PRODUCT LOCKS
- Peeking a tab never loads or unloads a model. Generate / Send / agent tool: co-reside if summed VRAM plus 2 GB headroom, else Switch / Keep / Remember-for-session. Unknown VRAM: confirm, never silent swap.
- Compact icon-only sidebar is the default (~56 px). Title bar keeps the brand. No sidebar wordmark. Logo must not recolor with the active tab.
- GPU status lives in the sidebar footer, never as a floating overlay on Send/Generate.
- + attach and send live inside the composer field, not as a separate square.
- Ask Inbox stays as a capability. Quiet bell (badge only when pending > 0). Popover must be dismissable (X, Escape, click-outside) and must never cover its toggle. Sidecar-down: do not pin a red error card on every tab. Full list under Settings > Approvals. No auto-approve.
- Delete the User Profile sidebar item. It must not exist as a row that only opens Settings. Silent /profile redirect is fine. Display name may live under Settings > General.
- Every Settings tab uses shared Select / SearchInput / TextField / Button / Switch. Visual parity with the already-styled Models filters, not "selects only."
- Hub is available offline via bundled snapshot + Sync. Do not dump every Hub skill into every prompt (2% render ladder).
- Export is a .nexus-pack zip with checkboxes; credentials default off; import merges.

USER-REPORTED ISSUES (screenshots and follow-ups)

UI chrome
1. Sidebar shows a logo plus "Nexus AI Studio" under a title bar that already says the same thing. Remove the sidebar brand.
2. The sidebar logo outline changes with the active tab. Stop that.
3. Compact the left nav into an icon-only vertical rail (default), with an expand toggle.
4. The GPU live monitor sat bottom-right and covered Send / Generate. It belongs in the sidebar footer.
5. The + and Send controls are a separate square next to the field. Put them inside the composer.
6. Settings filters and later the Models Search bar still look like Windows 95 (native OS chrome on a dark app).
7. After a supposed chrome pass, leftover controls on EVERY Settings tab are still unstyled: Skill Optimizer skill-id field and Preview button, Models Search, Credentials, Local API, Fine-tuning, MCP, Security, Data, and similar. Modernize all of them, not only <select>.
8. User Profile tab only leads to Settings. Remove the nav item entirely.
9. Pending approvals box cannot be closed no matter where you click. No X, no Esc, no click-outside. It follows you across tabs (Skill Optimizer screenshot).

Backend / empty product
10. Image Studio and Video Lab say no models are installed after a successful-looking install that downloaded SANA, RealVisXL, LTX, Wan, etc.
11. Local Chatbot requires creating a folder first; the composer is hidden until a chat exists. It should work like Coding: type immediately, optional folders/projects, auto-title from the first prompt.
12. Nexus-Hub skills and slash commands are missing; Skills Sync failed.
13. Ask Inbox is confusing as a primary nav item (empty most of the time, then broken when the backend is down).
14. Need Settings-based export/import of local Nexus data to move to another machine.
15. On one consumer GPU, switching tabs must not yank the coding model for an accidental Image Studio peek. Confirm before a swap that would evict a busy model; automate when both fit or the GPU is idle.
16. Approvals is not working (sidecar error in the popover).
17. Backend cannot start even after Restart backend. Installer Complete page: sidecar request error: The pipe is being closed. (os error 232) | stderr: [ / / Nodejs v22.11.0].

ISSUES DETECTED IN CODE (not named by the user, same session)

P0 sidecar (why the app is inert)
- Original packaged spawn: Tauri had no sidecar resources and used Command::new("node"), which Windows GUI often cannot find. Installer-provisioned Node was unused.
- Sidecar esbuild did not copy catalog.json. Empty catalog plus installedProbe cannot invent rows from ~/.nexus/models/weights/.
- Image/Video catch IPC failure and set noneInstalled, then show a hardcoded FALLBACK_MODEL (SANA) marked installed. That lies twice: "no models" and a fake SANA chip.
- 2026-08-22 field: Node 22.11.0 DOES start, then the child dies. The next JSON-RPC write hits a closed pipe (Windows ERROR_NO_DATA 232). Restart uses the same spawn path, so it cannot recover.
- Likely crash: better-sqlite3 is external; GenerationIndex opens sqlite at import time; the .node binary must sit next to sidecar/dist/main.js. esbuild already copies it into dist/node_modules, but spawn_with does not set cwd to the script dir, and a packaging test must prove the NSIS resource tree includes better_sqlite3.node.
- Healthcheck joins only the last three stderr fragments with " / ", which produced [ / / Nodejs v22.11.0] instead of the real stack. Node starting is not success; try_wait before the first models.list write.
- gemma-4-12b-it-gguf failed HTTP 412 because host Ollama was below 0.32.15.

Honesty / telemetry
- GPU widget was createMockTelemetryStream. "Gemma 4 7B Active" was not a loaded model.
- Skills list returned [] when the sidecar was down and the UI said "not yet synced" instead of backend-down.
- Coding slash palette is a hardcoded filterSlashCommands slice of 8, not the Hub catalog.

Chat / Hub / occupancy (still required if field still shows them)
- Chat explorer defaulted to InMemoryChatExplorerClient (Phase 4 stub); empty CTA was "Create your first folder."
- Hub harness is a best-effort runtime fetch; offline install has no snapshot unless the installer build actually ships catalog.tar.gz.
- Tab navigation does not load models; generation does. GpuScheduler.enqueuePanel already co-resides when VRAM fits. Missing piece is occupancy UX plus live VRAM, not a new scheduler.

Approvals trap (code)
- desktop/src/components/ApprovalsBell.tsx: open toggles only from the bell. No Close, no Escape, no outside click. position absolute bottom calc(100% + 4px), zIndex 40, width 20rem covers the main pane. Sidebar does not unmount on route change. Tests assert the 232 error is shown and never that it can be dismissed.

Chrome leftovers (code)
- SearchInput already exists in desktop/src/components/ui/Select.tsx but Models Search still uses a bare input type="search". There is no shared Button or TextField.
- ADMIN_ENTRIES still has User Profile -> /profile.
- compact = storedCompact ?? narrow, so a wide window starts expanded. User lock is compact default true.
- Coding panels still have raw <select>s: TimelineScrubber, TraceDashboardPanel, SessionCompareView.

ACCEPTANCE
- Next installer Complete page: sidecar ok, or a single readable reason that names the missing file or thrown exception (not os error 232 garbage).
- models.list returns the downloaded HF ids (realvisxl-v5, sana-1.6b-2k, ltx-video, sana-video-2b-720p, wan2.1-t2v-1.3b). Image/Video never map sidecar-down to "no models installed."
- Approvals: X / Esc / click-outside close it on every tab, including when list("pending") throws.
- Skill Optimizer and Models Search match the styled Select chrome. grep of desktop/src/pages/settings has no leftover unstyled native search/text/action controls.
- Sidebar: icon rail default, no wordmark, no User Profile row, GPU chip in the footer.
- Composer: in-field + and send. Chat: composer visible with no folder prerequisite.
```

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at docs/v2.2.0/constitution.md - skipping check. Recommend running /constitution to establish project principles.

## Current model map

**Model map status**: fresh as of 2026-08-21; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.5-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/about-claude/models/overview
- OpenAI: https://developers.openai.com/api/docs/models
- Google: https://ai.google.dev/gemini-api/docs/changelog
- Cursor: https://cursor.com/help/models-and-usage/available-models

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|-------|-------|---------|------------------------|--------------------------|
| 1 | Sidecar stays alive | Packaged sidecar survives spawn; healthcheck names the real crash, not os error 232 | frontier | high |
| 2 | Sidebar chrome contract | Default icon rail, no wordmark, no User Profile row | strong | medium |
| 3 | Dismissable Approvals | X / Escape / click-outside always close the popover; sidecar-down never pins it | strong | medium |
| 4 | Settings and studio control sweep | Every Settings tab and leftover studio/coding control uses shared primitives | strong | high |
| 5 | Architecture refactor, known-gaps, CI/CD | Layout clean, gaps reconciled, CI covers the patch | frontier | max |

Parallelism: Phase 2, 3, and 4 can proceed in parallel once Phase 1 is in flight (they do not need a live sidecar to code or unit-test). Do not call the cycle done until Phase 1's host healthcheck is sidecar ok.

---

## Phase 1: Sidecar stays alive

**Goal**: The installed app's Node child is still running when the first JSON-RPC line is written, and a crash produces a readable reason instead of Windows 232.
**Prerequisites**: None.
**Stability Gate**: On this Windows host after rebuild, Complete shows sidecar ok (not os error 232); Restart backend is unnecessary because the child stays alive; `ipc_call("models.list")` returns catalog rows. A sabotaged native-addon tree still fails the health check, but the verdict includes the child exit code and a readable stderr tail (not three slash-joined fragments).
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Cross-layer packaging (Tauri resources, Rust spawn cwd, esbuild native copy, installer healthcheck formatting) with a field-proven crash and no existing test that the NSIS payload contains `better_sqlite3.node`.

### Sub-tasks

#### 1.1 - Ship the native addon with the Tauri resource tree

**Objective**: `sidecar/dist/node_modules/better-sqlite3` (plus `bindings` and `file-uri-to-path`) lands next to `main.js` in the packaged app, and spawn cwd is that directory.

**Failure modes**: Resource tree copies `main.js` but omits `node_modules` -> child throws at `GenerationIndex` import and exits before the first RPC; ABI mismatch vs bundled Node 22 -> rebuild against that Node and fail `build:sidecar` if the `.node` is missing; two concurrent spawns (startup + Restart) -> existing single-flight guard must still hold.

**Build class**: load-bearing.

**Prompt**:
> In the Nexus-AI repo, make the packaged sidecar able to `require('better-sqlite3')`. `desktop/sidecar/esbuild.config.mjs` already marks the addon external and copies it into `desktop/sidecar/dist/node_modules`. `desktop/src-tauri/tauri.conf.json` maps `../sidecar/dist` to `sidecar/dist`; confirm that glob includes `node_modules/**` (not only `main.js` / `wasm`). Add a packaging assertion that lists the NSIS/Tauri resource dir and requires `better_sqlite3.node` to be resolvable from `sidecar/dist/main.js`. In `desktop/src-tauri/src/sidecar.rs`, `Sidecar::spawn_with` currently does not set cwd; set it to `script.parent()` so Node's module lookup cannot miss the copied addon. Keep `NEXUS_CATALOG_PATH` and `catalog-load-failed`. Acceptance: healthcheck on this host is sidecar ok; a fixture that deletes only the native addon reproduces a crash whose stderr names better-sqlite3, not `[ / / Nodejs v22.11.0]`.

---

#### 1.2 - Do not write JSON-RPC to a dead child

**Objective**: Healthcheck and app spawn wait for liveness (or a recorded exit) before the first `models.list` write.

**Failure modes**: Child exits during the 500 ms wait -> surface `sidecar-exited:<code>` plus stderrTail, never write to a closed pipe; slow-but-alive start on a cold disk -> retry/backoff only while `try_wait` is None; `--healthcheck` stdin not piped -> do not treat it as a TTY; Restart backend must use the same `spawn_with` cwd/resources.

**Build class**: load-bearing.

**Prompt**:
> Treat the 2026-08-22 Complete-page 232 as the Phase 1 acceptance bug. In `desktop/src-tauri/src/sidecar.rs` and `lib.rs` `run_healthcheck`: after `Command::spawn`, wait until stderr shows a sidecar ready line or 500 ms of liveness (`try_wait` is None) before writing `models.list`. If the child has exited, fail immediately with `fail: sidecar-exited:<code>` plus the stderr tail; never map that to "no models installed". `sidecar.restart` must use the same spawn path. Acceptance: Restart backend on this machine either brings `models.list` back or Show details names the real crash; it must not remain "The pipe is being closed."

---

#### 1.3 - Healthcheck verdict the Complete page can print

**Objective**: The installer Complete page shows exitCode, nodePath, scriptPath, and a readable stderr tail instead of three slash-joined fragments.

**Failure modes**: Verdict JSON missing fields on an older app -> installer still fails closed with the raw stdout, never a silent pass; stderrTail longer than the UI can show -> keep at least 20 lines in the JSON array, print a bounded preview on the Complete page; `" / ".join(stderr_tail[-3:])` in `desktop_provisioner.py` is the 2026-08-22 garbage path and must go.

**Build class**: load-bearing.

**Prompt**:
> Keep `--healthcheck` (already invoked by `first_run_health_check`). Fix the verdict so a dead child is diagnosable: include `exitCode`, `nodePath`, `scriptPath`, and at least the last 20 stderr lines as a JSON array. In `scripts/installer/src/nexus_installer/engine/desktop_provisioner.py`, stop joining three fragments with `" / "` (that produced `[ / / Nodejs v22.11.0]`). Print a single readable reason. Add installer pytest for verdict shape and the 232-style dead-child path. Acceptance: this host's next installer either prints sidecar ok, or prints a reason that names the missing file or thrown exception.

---

#### 1.4 - Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until the phase is stable before advancing to Phase 2.

**Prompt**:
> Generate comprehensive tests for Phase 1: Rust unit tests for spawn cwd and the dead-child path; packaging assertion that the NSIS/Tauri payload contains `sidecar/dist/main.js`, `catalog.json`, and `better_sqlite3.node`; installer pytest for healthcheck verdict shape (exitCode + stderrTail array, no three-line join). Run root vitest, desktop vitest, installer pytest, and cargo test. Update CI path filters for `desktop/src-tauri`, `desktop/sidecar`, and the installer healthcheck. Do not proceed to Phase 2 as "done" until this host's healthcheck is sidecar ok (or the remaining failure is a named, tested gap). After all tests pass, run /generate-session-history to document Phase 1.

---

### Phase 1 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, packaging)
- [ ] No known regressions from prior phases
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 2

---

## Phase 2: Sidebar chrome contract

**Goal**: The rail is a compact icon bar by default, with no duplicate wordmark and no User Profile item.
**Prerequisites**: None (can overlap Phase 1).
**Stability Gate**: Sidebar shows no logo/wordmark (title bar keeps the brand); default view is the ~56 px icon-only rail; User Profile is absent from the sidebar; Settings remains.
**Recommended model tier**: strong
**Recommended effort level**: medium
**Rationale**: Localized `Sidebar.tsx` contract with persistence and existing vitest; the field screenshot disagrees with a source comment that already claims the brand is gone, so tests must pin the user-visible contract.

### Sub-tasks

#### 2.1 - Default compact rail, no wordmark, no Profile row

**Objective**: Compact is the default; brand lives only in the title bar; User Profile is deleted from `ADMIN_ENTRIES`.

**Failure modes**: No stored preference -> icon rail, not "follow window width into expanded"; explicit expand still persists; auto-compact below the existing breakpoint must not clobber an explicit expand; `/profile` bookmarks still redirect into Settings, they just have no nav item.

**Build class**: load-bearing.

**Prompt**:
> Rework `desktop/src/components/Sidebar.tsx`. (1) Default compact: `nexus.sidebar.compact` default true (~56 px, labels as tooltips). Today `compact = storedCompact ?? narrow`, so a wide window starts expanded; change the null-preference path to compact. (2) Assert there is no sidebar logo or "Nexus AI Studio" wordmark in the rail (title bar stays the only brand). If any installed-build leftover remains, delete it. (3) Remove `{ label: "User Profile", to: "/profile", ... }` from `ADMIN_ENTRIES`. Keep Settings. Do not replace Profile with another sidebar row. Keep a silent `/profile` -> Settings redirect in `App.tsx`. Vitest: no "Nexus AI Studio" in the nav, compact default, no `/profile` link, Settings still present. Acceptance: a screenshot of this cycle would not show a sidebar wordmark or a Profile item.

---

#### 2.2 - Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Generate comprehensive tests for Phase 2: compact default with no localStorage key, persisted expand, auto-compact vs explicit expand precedence, no wordmark in the sidebar, no User Profile / `/profile` nav item. Run desktop vitest. After all tests pass, run /generate-session-history to document Phase 2.

---

### Phase 2 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing
- [ ] No known regressions from prior phases
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 3

---

## Phase 3: Dismissable Approvals

**Goal**: The Pending approvals card can always be closed, and sidecar-down never pins it across tabs.
**Prerequisites**: None (can overlap Phase 1 and 2).
**Stability Gate**: X, Escape, and pointerdown outside close the popover; the dialog never covers its toggle; sidecar-down does not leave a persistent overlay on Skill Optimizer or any other tab.
**Recommended model tier**: strong
**Recommended effort level**: medium
**Rationale**: Single-component trap with existing tests that currently assert the error and never assert dismiss, which is why it shipped.

### Sub-tasks

#### 3.1 - Close paths for the Approvals popover

**Objective**: The bell popover is optional UX, not a modal trap.

**Failure modes**: Sidecar down -> bell stays quiet (no auto-open, no badge pretending 0 pending); popover already open when `list("pending")` throws -> one error line plus Close, never a stuck card; popover geometrically covers the bell (`bottom: calc(100% + 4px)`) -> still closable via X, Escape, and mousedown outside both the dialog and the bell; Sidebar staying mounted across routes must not keep `open === true` after dismiss.

**Build class**: load-bearing.

**Prompt**:
> Fix `desktop/src/components/ApprovalsBell.tsx`. Today `open` only toggles from the bell button, the dialog has no Close control, and `desktop/tests/shell-phase6.test.tsx` asserts the sidecar error is shown but never that it can be dismissed. Required: (1) Close button with aria-label; (2) Escape; (3) pointerdown outside the dialog and outside the bell; (4) never start `open === true`; (5) if `list("pending")` throws, do not keep the dialog stuck -- show the error inside a closable panel, or skip opening and rely on the Settings backend banner; (6) keep `/inbox` redirected to Settings > Approvals. No auto-approve. Reposition so the popover cannot cover the toggle (anchor above without eating the hit target, or render into a portal). Vitest: outside click closes, Escape closes, X closes, sidecar-down does not leave a persistent overlay. Acceptance: Skill Optimizer (and every other tab) can be used without a stuck Pending approvals card.

---

#### 3.2 - Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Extend `desktop/tests/shell-phase6.test.tsx` (and Sidebar tests if needed) for the 2026-08-22 dismiss trap: X / Escape / pointerdown-outside all close the popover; sidecar-down error is closable; the overlay is gone after close even if the Sidebar stays mounted. Run desktop vitest. After all tests pass, run /generate-session-history to document Phase 3.

---

### Phase 3 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing
- [ ] No known regressions from prior phases
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 4

---

## Phase 4: Settings and studio control sweep

**Goal**: Leftover native search boxes, text fields, textareas, and action buttons on every Settings tab (and remaining studio/coding selects) match the already-styled Models filters.
**Prerequisites**: Phase 6.4 primitives from v2.2.0 (`Select`, `SearchInput`, `Switch` in `desktop/src/components/ui/Select.tsx`). Button and TextField still need to be added.
**Stability Gate**: No unstyled native search/text/action controls remain in `desktop/src/pages/settings`; Skill Optimizer Preview and Models Search match the Select styling (radius, border, height, focus ring); coding-panel leftover `<select>`s are wrapped.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Mechanical sweep across every Settings tab; v2.2.0 Phase 7.1 only restyled selects, which is the gap the user called out on Skill Optimizer.

### Sub-tasks

#### 4.1 - Button and TextField primitives

**Objective**: Shared Button and TextField sit next to Select / SearchInput / Switch, token-driven, dark-theme, keyboard-accessible.

**Failure modes**: Disabled / busy states must stay perceivable (opacity plus `aria-disabled` / native `disabled`); focus ring must be visible on keyboard focus, not only hover; do not reimplement native inputs as divs (same contract as Select: wrap and restyle).

**Build class**: load-bearing.

**Prompt**:
> Add `Button` and `TextField` (and a textarea variant if Fine-tuning still needs it) to `desktop/src/components/ui/`. Reuse the `controlBase` tokens already in `Select.tsx` (background `--bg-elevated`, border `--border-subtle`, radius `--radius-md`, padding, font). Render them on `/_styleguide`. Do not introduce a div-based listbox. Acceptance: styleguide shows Select, SearchInput, TextField, Button, and Switch at matching height and radius.

---

#### 4.2 - Sweep every Settings tab and leftover selects

**Objective**: Apply the primitives everywhere the 2026-08-22 screenshots still look like Windows 95.

**Failure modes**: Value/onChange contracts must not drift (existing page tests are the regression net); `SearchInput` already exists but `ModelsSettings.tsx` still uses a bare `input type="search"` at the Models Search field -- that is the first replacement; Skill Optimizer skill-id + Preview + Approve are the second.

**Build class**: load-bearing.

**Prompt**:
> Sweep **every Settings tab**, not only Models filters. Replace native controls with Select / SearchInput / TextField / Button / Switch in: `ModelsSettings.tsx` (search box), `SkillsSettings.tsx`, `SkillOptimizerSettings.tsx` (skill-id field, Preview, Approve & write), `FineTuningSettings.tsx`, `SecuritySettings.tsx`, `ServingSettings.tsx`, `McpRegistrySettings.tsx`, `CredentialsSettings.tsx`, `DataSettings.tsx`, plus Image/Video advanced forms if any native leftovers remain. Also wrap leftover coding-panel `<select>`s in `TimelineScrubber.tsx`, `TraceDashboardPanel.tsx`, and `SessionCompareView.tsx`. Keep value contracts. Acceptance: a screenshot of Skill Optimizer and Models Search matches the Select styling; `grep` for unstyled native search/text inputs in `desktop/src/pages/settings` is empty of app-code hits that skip the primitives.

---

#### 4.3 - Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Generate comprehensive tests for Phase 4: primitive snapshots or styleguide coverage; per-page regression that Models Search, Skill Optimizer Preview, and one control per other Settings tab still fire the same handlers; grep/CI guard that settings pages do not introduce new unstyled search inputs. Run desktop vitest. After all tests pass, run /generate-session-history to document Phase 4.

---

### Phase 4 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing
- [ ] No known regressions from prior phases
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 5

---

## Phase 5: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, and its CI/CD complete and optimized.
**Prerequisites**: All prior phases.
**Stability Gate**: The layout is clean (no deprecated/obsolete files, empty dirs, redundant files/dirs, or overcomplicated structure left un-triaged); the version's known gaps are reconciled; CI/CD covers every change and is optimized; project validation/tests pass.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, reference repair, known-gap reconciliation, and release gating carry high context volume and blast radius.

### Sub-tasks

#### 5.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure, then refactor toward a clean, intuitive layout via [[project-refactor]] and [[docs-layout-refactor]] (propose-then-apply, with confirmation; repair every reference for anything that moves). Candidates known now: any Approvals popover positioning hacks replaced by a portal, duplicate composer style objects still in CodingInput (v2.2.0 DF-14, only if in scope), and token aliases that 6.4 left behind (v2.2.0 DF-15) if the rename is still a one-line mechanical sweep.

#### 5.2 - Known-gaps reconciliation
**Objective**: Reconcile the version's open gaps.
**Prompt**:
> Reconcile this version's known gaps via [[known-gaps-tracker]]: resolve, defer, or transfer each open item, and finalize docs/archive/v2/v2.2/known-gaps.md with a v2.2.1 section. Carry forward v2.2.0 DFs that this patch did not close (live-GPU soaks, Node SEA, URL-addressable settings tabs, native file dialog for data transfer). Close the field bugs this plan named: 232 spawn, undismissable Approvals, unstyled Settings controls, User Profile nav row, compact-not-default.

#### 5.3 - CI/CD create/update/optimize
**Objective**: CI/CD covers all changes and is optimized.
**Prompt**:
> Create or update the CI/CD pipeline so it covers every change in this plan (packaging assertion for better_sqlite3.node, Approvals dismiss tests, settings control-sweep guard), then optimize it to reduce action minutes (path filters, concurrency cancel-in-progress, dependency caching, gating expensive-OS or matrix jobs to merges/schedule) while keeping comprehensive testing. Keep it platform-agnostic; GitHub Actions is the primary example.

#### 5.4 - Cross-installer parity
**Objective**: Prevent operating-system installer drift when the repository ships multiple installers.
**Prompt**:
> If this repository ships more than one installer, run or add a declarative cross-installer parity gate covering distributed artifacts, supported platforms, named capability counterparts, and external-tool fallbacks, then execute the real installers on their target operating systems with identical postconditions. Cross-link [[platform-contract-verification]] so discovery and delivery are checked in the same pass. If the repository ships zero or one installer, record a silent no-op and continue. Note: the sidecar native-addon and spawn-cwd contracts from Phase 1 must hold on macOS (.app/ditto) and Linux (AppImage) paths, not just Windows NSIS.

#### 5.5 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and CI/CD is green.
**Prompt**:
> Run the full validation/test suite, confirm the refactor changed no behavior, confirm CI/CD passes and the action-minute reduction is real, and iterate until clean. Generate a session-history entry for this phase.

---

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| | | |

---

### Phase 5 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit, integration, and any phase-specific tests)
- [ ] No known regressions from prior phases
- [ ] Session history generated for this phase
- [ ] Release readiness confirmed (hand off to /update release)

---

## Out of scope

- Re-running the full v2.2.0 eight-phase cycle (chat persistence, Hub snapshot builder, occupancy policy core, data-transfer runtime). Those stay as predecessor work; this patch only closes what the 2026-08-22 install proved still broken.
- Live-GPU generation soaks (v2.2.0 DF-4).
- Node SEA / Tauri `externalBin` (v2.2.0 DF-1).
- URL-addressable settings tabs (v2.2.0 DF-17) except as a known-gap carry-forward.
- Native file dialog for export/import (v2.2.0 DF-16 remaining).
- Dumping the Hub catalog into every system prompt.
- Deleting the Ask Inbox capability.
- Multi-GPU scheduling.
- Light-theme support.
