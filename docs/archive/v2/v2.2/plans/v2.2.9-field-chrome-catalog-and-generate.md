# Plan -- Field Chrome, Catalog Identity, and Honest Generate

**Project**: Nexus AI Studio
**Version**: v2.2.9
**Slug**: field-chrome-catalog-and-generate
**Filename**: v2.2.9-field-chrome-catalog-and-generate.md
**Plan Type**: Feature / Enhancement (patch follow-on)
**Created**: 2026-08-25
**Predecessor**: [v2.2.8-working-local-studio.md](v2.2.8-working-local-studio.md)
**Seeded from**: operator packaged-Explorer screenshots after the v2.2.8 cut (2026-08-25). Not a comparison report. Discovery answers were taken from that field session (goal, in/out scope, affected areas, constraints, definition of done, testing).
**Goal**: By the end of this cycle a packaged Windows build shows Chatbot chrome the operator asked for (no leftover Mic/Persona labels, Context 70-75% / picker 25-30%, time and tokens above the bubble in full words, rotating thinking-orbs captions with no pending meta, highlighted active session, persisted auto-title), Image/Video history matches Chatbot with no empty top bar and a visual-session meter, Image/Video generate either paints media or a typed CUDA-vs-weights error, installer Models and Settings Models share one card grammar including an Embeddings tab and the same rows (Inkling-Small included), and Skills no longer claims an update from 3.21.0 to v3.21.0.

## Overview

v2.2.8 closed the 15s send timeout class and Hub apply-with-quarantine at unit evidence. The 2026-08-25 packaged session proved Chatbot `Hi` now returns a local reply (DF-32 observed). The remaining failure is not a blank Explorer: leftover Chat chrome, transcript meta under the text, composing orbs that still read as a small dotted spinner plus `8:34 p.m. --`, history rows with no selected state and no persisted title, Image/Video empty header plus Sessions copy, a missing Context meter on studio tabs, generate failing closed as `GPU or diffusion weights unavailable` on a host that already shows 15 GB free NVIDIA VRAM, Settings Models still a different card than the installer, Inkling-Small visible in-app and hidden in the wizard, and Skills showing a false update banner because Active is `3.21.0` while upstream is `v3.21.0`.

This cycle is field-chrome and catalog identity on the v2.2.8 engine. Delivery is the existing Tauri app, Node sidecar, Python installer, and `runtimes/diffusion`. Success is the operator field checklist at the bottom. Do not call Image/Video generate done from a unit stub.

## Constitution Check

No constitution file found at `docs/archive/v2/v2.2/constitution.md` (or `docs/constitution.md`). Skipping the MUST/SHOULD gate. Recommend `/constitution` if this project wants a durable principle file. This is informational, not blocking.

Grounding that was read instead:

- `AGENTS.md` (local-first, honest capability claims, `not_observed != absent`)
- `docs/archive/v2/v2.2/known-gaps.md` (DF-4 live GPU generate, DF-32 packaged Hi, DF-34 Settings vs installer, DF-35 packaged Hub latest)
- [thinking-orbs reference](https://github.com/Jakubantalik/Libraries) and [orbs.jakubantalik.com](https://orbs.jakubantalik.com/) (MIT; reverse-engineer appearance; do not `npm install thinking-orbs`)
- Unicode hygiene: this repo has no `scripts/validate_unicode_safety.py`. This file was scanned for non-ASCII (0 hits) before presentation.

No `STRATEGY.md` / `docs/STRATEGY.md` and no `docs/solutions/` store.

Persona: the same local-first laptop user as v2.2.8, now needing the packaged reply to look finished and Image/Video to either generate or say exactly why not.

## Current model map

**Model map status**: fresh as of 2026-08-25; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.7-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.7-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.7-flash` | `composer-2.5-fast` |

A distinct Gemini 3.7 Pro API slug was not confirmed from official docs in this session, so Flash fills every Google cell rather than inventing a Pro id. Cursor has no scriptable `/model` switch; `/implement` prints the picker instruction. Map JSON validated 2026-08-25 via `model-routing/scripts/model-map.py` (`valid: true`).

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/about-claude/models/overview, https://platform.claude.com/docs/en/about-claude/models/choosing-a-model
- OpenAI: https://developers.openai.com/api/docs/models, https://developers.openai.com/api/docs/models/gpt-5.6-sol
- Google: https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/, https://docs.cloud.google.com/gemini-enterprise-agent-platform/models/gemini/3-7-flash
- Cursor: https://cursor.com/docs/models-and-pricing, https://cursor.com/docs/models/grok-4-6

## Review verdict (this session)

### Project health

| Surface | Status | Detail |
|---|---|---|
| Git version control | OK | Repo present; tag `v2.2.8` on `main` |
| Version number | OK | Plan version v2.2.9; do not bump root `package.json` (2.2.8) or `desktop/package.json` (1.5.0) until `/update release` |
| Branch model | OK | `develop` + `main` in use |
| Baseline docs | OK | README, CHANGELOG, known-gaps, per-version plans |
| Per-version docs tree | OK | `docs/archive/v2/v2.2/plans/` present |

### Field issues (screenshots 1-10)

This section is the operator-facing inventory. Each row names the screenshot, the requested change, the code cause, and the phase that owns the fix.

#### Screenshot 1 -- Chat leftover chrome and Context/picker split

| Request | Cause | Phase |
|---|---|---|
| Remove idle `Mic closed` and `Persona` text under the composer | `ChatPage.tsx` always renders `chat-voice-capture-indicator` (`Mic closed` when not recording) and a `Persona` toggle button under the composer. v2.2.0 called that real feedback; the field session does not. | 1 |
| Context bar ~70-75% width; model selector ~25-30% | `ContextUsageBar` is `flex: 0 0 auto` with `minWidth: 9rem`. `ComposerContextRow` gives the picker `flex: 1 1 auto`, so the selector eats the row. | 1 |

Persona capability stays: move it into the composer overflow (attach/`+` menu or a single icon), not leftover footer copy. Show a recording indicator only while the mic is open.

#### Screenshot 2 -- First packaged Chatbot reply (DF-32 observed)

| Request | Cause | Phase |
|---|---|---|
| Time and token counts above the bubble text | `MessageBubble` renders body, then `BubbleMeta` with `marginTop`. | 1 |
| `1 in` -> `1 input token` (pluralize) | `formatBubbleTokens` in `transcriptChrome.ts` | 1 |
| `75 think + 96 out` -> `171 tokens (75 reasoning + 96 output)` | Same helper. Sum = reasoning + output when both known. | 1 |
| Highlight the session on screen | `FolderTree` `rowStyle` uses `backgroundColor: selected ? var(--bg-1) : transparent` on a pane that is already `--bg-1`. No visible selected state. | 1 |
| Auto-name after the first prompt so the rail is not `New chat` | `ChatPage` calls `client.generateTitle` on first send, then only `setActiveChat` + `setTreeVersion`. It never calls `chat.explorer.renameChat` with the generated title. `FolderTree` reads the explorer store, so the rail stays `New chat`. Sidecar `generateChatTitle` also does not persist. `titleGenerator.ts` comments say the caller should show `fallbackTitle` immediately; ChatPage does not. `TITLE_TIMEOUT_MS` is 5s and contends with the live `Hi`. | 1 |

Packaged Chatbot `Hi` completing is **observed**. Close DF-32 in Phase 7 with this session as evidence. Do not keep writing it as unproven.

#### Screenshot 3 -- Composing chrome

| Request | Cause | Phase |
|---|---|---|
| Pending bubble is only the orb + rotating caption; no clock; no token dash | `BubbleMeta` still renders on pending rows (`8:34 p.m.` plus unknown-token mark). `AgentStateOrb` uses a single `chat-streaming` mapping, not a cycling Thinking/Searching/Working/Solving pill. | 2 |

#### Screenshots 4-5 -- thinking-orbs reference

| Request | Cause | Phase |
|---|---|---|
| Randomly iterate Thinking / Searching / Working / Solving while the model works | `orbEngine.ts` is a reverse-engineered dotted ring (v2.2.3 / v2.2.8). Captions are a single activity label. The reference is a dark pill with a particle sphere plus `....` label ([Jakubantalik/Libraries](https://github.com/Jakubantalik/Libraries), `thinking-orbs`, MIT). | 2 |

Do not `npm install thinking-orbs`. Raise motion toward that playground (pill, particle variants, cycling labels, Nexus accents). Reduced-motion: static ring plus one caption. When the reply lands, replace the pending bubble with text plus time and tokens (Phase 1 meta).

#### Screenshots 6-7 -- Image (and Video) history, Context, generate fail

| Request | Cause | Phase |
|---|---|---|
| Remove the empty horizontal bar above history + transcript | `ImageStudioPage.tsx` and `VideoLabPage.tsx` always render a `<header>` with padding and `borderBottom`. When models are installed the header is empty. Chatbot has no such header. | 3 |
| History chrome identical to Chatbot (not `Start a new session`) | `StudioHistoryPane` `IMAGE_COPY` / `VIDEO_COPY` use `paneTitle: Sessions`, `emptyCta: Start a new session`, `newItem: New session`. Chatbot FolderTree uses Chats / New chat. | 3 |
| Highlight the active studio session | Same `--bg-1` selected row, plus `StudioHistoryPane` keeps its own `selected` state instead of binding the open session id. | 1 + 3 |
| Context meter on Image/Video | `composerSessionUsage` needs `contextWindow` or `visualTokenBudget`. Image/video catalog rows generally have neither. LLM `contextWindow` on SDXL would be a lie. The analog is `visualTokenBudget` (v2.2.7). Iteration on the same image is last-output img2img (v2.2.6), which screenshot 7 already attempted (prompt plus prior PNG). | 3 |
| Generation failed: `image runtime is not ready: GPU or diffusion weights unavailable` | `runtimes/diffusion/pipelines/base.py` `can_run_real()` requires `torch.cuda.is_available()` in the **Python** diffusion env. OS telemetry (15.4 GB free NVIDIA) does not prove that. If CUDA torch is missing, `select_executor` uses `fail_closed_execute`, which always raises that **combined** string and never says which of GPU vs weights failed. RealVisXL also needs HF weights under `~/.nexus/models`. | 4 |

Answer to "do these models have context windows?": chat/agentic LLMs yes (`contextWindow` in `catalog.json`). Diffusion checkpoints no, not as an LLM window. Session iteration is: keep the named session, attach last PNG, img2img. The Context pill on studio tabs should fill against `visualTokenBudget` (usable images/frames this session), not a fake 128k.

#### Screenshot 8 -- Installer Models

| Request | Cause | Phase |
|---|---|---|
| Company left of country; pills on the name row | `typed_catalog.py` puts Origin / Agentic / Context / Multimodal / license / Released **below** the description. Publisher already exists as `FAMILY_TO_PUBLISHER` in `constants.py` (Gemma -> Google, Inkling -> Thinking Machines) but is not on the card as `Company:`. | 5 |
| Ordered facts: name, Company, Country, Agentic Yes/No, Context window ...k tokens, Multimodal Yes/No, Guardrails Censored/Uncensored/other, License, Released Month YYYY | Current pills are a shorter, different set (`Origin: USA`, `Agentic: Yes`, `Context: 256k`, `Released 2026-05`). | 5 |
| Embeddings tab before Chat | `TYPE_TABS` starts at Chat. `task: embed` / `type: embed` map to the Chat tab (`TASK_TO_TAB["embed"] = "chat"`). Catalog already has Nomic, EmbeddingGemma, Qwen3 Embedding. | 5 |

#### Screenshot 9 -- Settings Models vs installer

| Request | Cause | Phase |
|---|---|---|
| Same card grammar, same category sort as the installer | `ModelsSettings.tsx` is still a compact list (letter tile, Recommended / Also agentic, id-size-vram line, Origin/Context/Released **under** the name). v2.2.8 shared **sort**, not **card chrome**. | 5 |
| Downloaded/Remove/Download plus a downloaded highlight are the only extra controls | Keep those actions. | 5 |
| Sort: downloaded-and-ready first, then installer recommendation order among those, then the rest in installer order | `visibleModelsOnTab` / `catalog_tab_sort.py` do not boost downloaded. Example: Gemma 4 12B and LFM2.5 2.6B selected and downloaded; gpt-oss not downloaded. App Agentic list = Gemma, LFM, then gpt-oss. After gpt-oss downloads, gpt-oss sits above LFM because it ranks higher in the installer list. | 5 |
| Same catalog rows (Inkling-Small in both) | Installer hides `patient-tier` unless `NEXUS_PATIENT_TIER=1` (`typed_catalog.py`). Settings lists sidecar catalog rows, so Inkling-Small appears. | 5 |

#### Screenshot 10 -- Skills Hub

| Request | Cause | Phase |
|---|---|---|
| Remove `Update available: 3.21.0 to v3.21.0` | `SkillsSettings.tsx` compares `activeTag !== upstreamTag` as raw strings (banner and the header `Upstream latest` line). Active is stored without `v`; GitHub latest is `v3.21.0`. Packaged Active matching GitHub latest is **observed** (DF-35 close in Phase 7). The remaining defect is the false banner. | 6 |
| Sync now row at the top; compact | The false banner sits above the real Sync now row. After tag-normalize, hide the banner when tags are equal. | 6 |
| Syncing animation (thinking-orbs class) plus status copy | Sync now becomes static `Syncing...` / `Updating...`. No orb. No `New version found` / `Installing version X.Y.Z` / `Harness up-to-date with Nexus-Hub version X.Y.Z`. | 6 |
| Why was a skill quarantined? | v2.2.8 Phase 5: `PromptInjectionScanner` stays on. High-severity hits move to `catalog/quarantine/` and stay disabled. Screenshot 10: `cross-project-comparison` flagged `[high] injection.jailbreak.ignore-previous`. That is success of the scanner, not a random fail. Do not disable the scanner. Improve copy so Quarantined means "scanner blocked this skill; it is not enabled." Optionally re-review that Hub file for a **narrow** allowlist waiver if the match is a quoted example (same pattern as `hubSkillScanAllowlist.ts`). A planted jailbreak is not allowlisted. | 6 |

### What v2.2.8 already resolved (do not duplicate)

Minutes-class `chat.send` / generate RPCs; Ollama `gemma4:12b` as Downloaded; four-tab FolderTree 280px / 56px rail; rail inset; compact Models **sort** fixture; Hub apply with per-skill quarantine; scanner on. Packaged `Hi` is now observed.

### Additional review findings (not in the screenshots as copy, but in the tree)

- Pending bubbles show `UNKNOWN_TOKEN_MARK` (`\u2014` em dash) because `BubbleMeta` always calls `formatBubbleTokens`. Skipping meta while pending also removes that character from the composing pill.
- `ComposerContextRow` uses `flexWrap: wrap` plus an unbounded picker (`flex: 1 1 auto`). On a narrow pane the picker can wrap under a 9rem Context pill and look even more inverted. Phase 1.2 should keep one row (`nowrap`) or wrap only below a documented min width.
- Settings Models does not apply the installer `NEXUS_PATIENT_TIER` hide (`isPatientTierModelVisible` is unused in `desktop/src`). That is why Inkling-Small appears in Settings and not in the wizard. Do not hide it in Settings to match the old installer; show it in both.
- Image/Video empty header is not a spacer component. It is a real `<header>` with padding and `borderBottom` that still renders when `noneInstalled` is false (only a `display:none` settings link inside). Deleting the empty chrome is a render-when-children test, not a CSS height hack.
- `StudioHistoryPane` still uses `paneTitle: Sessions`, `newItem: New session`, `emptyCta: Start a new session`. v2.2.8 unified width/collapse, not copy.
- `fail_closed_execute` in both `runtimes/diffusion/pipelines/base.py` and `video_base.py`, plus sidecar `resultGuard.ts` constants, all use the same combined GPU-or-weights string. Typed errors must update all three.
- Hub header `upstreamTag !== activeTag` will keep showing `Upstream latest: v3.21.0` next to `Active: 3.21.0` even after the banner is removed, unless tag normalize is shared.

### What this plan must resolve

Chat chrome and transcript meta; orb captions; studio history identity and visual meter; typed diffusion failure; installer = Settings **cards** plus Embeddings tab plus same row set; Hub tag normalize and sync feedback.

## Locked product decisions

- **Do not start** until v2.2.8 is tagged on `main` (already true at plan time). Do not re-run v2.2.0-v2.2.8 as greenfield.
- **Composer footer**: Context pill `flex` ~3, picker `flex` ~1, picker `max-width` ~30% of the row, `min-width` large enough for names like `Nemotron 3.5 Lightning 30B-A3B`. Same row on Chatbot, Agents, Images, Videos (Images/Videos show the bar when a visual budget exists).
- **Bubble meta**: above the text. User: `1 input token` / `N input tokens`. Assistant: `171 tokens (75 reasoning + 96 output)` when both parts exist; omit unknown parts rather than guessing 0. Pending: no meta (no clock, no em dash).
- **Models card row**: one header row. Display name first, then pills in this order: `Company:`, `Country:`, `Agentic: Yes/No`, `Context window: {n}k tokens`, `Multimodal: Yes/No`, `Guardrails: Censored/Uncensored/other`, `License:`, `Released: Month YYYY`. Pills sit on that name row, not under the description. Do not invent a company; use `FAMILY_TO_PUBLISHER` with a documented fallback.
- **Pill derivation and unknowns**: `Multimodal: Yes` when the catalog row lists modalities beyond text (or `vision: true`); otherwise `No`. `Guardrails` from the existing `uncensored` field (`false` -> Censored, `true` -> Uncensored, per BG-47). Any pill whose source value is missing is **omitted**, never rendered as `Unknown` / `Community` / an invented default. Same derivation table in Qt and React (dual-asserted golden strings, WN-7 discipline).
- **Auto-title**: persist through `chat.explorer.renameChat` (not `byUser`). Immediate `fallbackTitle` so the rail never stays `New chat` after a first send. Optional model refine after the first assistant turn completes. Never overwrite a user rename.
- **Selected session**: visible contrast (left accent bar or `--bg-2` / mix), not `--bg-1` on `--bg-1`. Bind studio pane selection to the open session id.
- **Orbs**: keep `AgentStateOrb` / `orbEngine`. Do not `npm install thinking-orbs`. Cycle captions Thinking / Searching / Working / Solving on chat/agents pending. Image/Video pending stays hero generate, may share the caption rotator. Reduced-motion: static.
- **Studio history copy**: Chatbot FolderTree strings (Chats / New chat / empty hint), not Sessions / Start a new session. Remove the empty Image/Video header when it has no children.
- **Studio context**: do not invent LLM windows for diffusion models. Thread `visualTokenBudget` onto image/video catalog rows and the picker DTO. Empty-attachment follow-up remains last-output img2img / `continueFrom`.
- **Generate honesty**: split CUDA-unavailable vs weights-missing vs true GPU-not-ready. Telemetry VRAM is not `torch.cuda.is_available()`. No stub PNG on a real host (`NEXUS_DIFFUSION_ALLOW_STUB` stays test-only).
- **One Models card**: installer Qt and Settings React share the name-row pill order. Embeddings tab first. Patient-tier rows visible in both (not installer-hidden). Downloaded-first sort only in Settings.
- **Hub**: compare tags after stripping a leading `v`. No update banner when equal. Scanner stays on. Quarantine copy explains the finding.
- **Do not bump** package.json. **No live GPU in PR CI**.

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|-------|-------|---------|------------------------|--------------------------|
| 1 | Chat chrome, meta, selection, titles | No leftover Mic/Persona; Context/picker split; meta above; selected row; persisted title | frontier | high |
| 2 | Thinking-orb captions | Cycling Thinking/Searching/Working/Solving; pending has no clock/tokens | frontier | high |
| 3 | Studio history and visual context | No empty header; Chatbot copy; visual Context bar; last-output iteration stays | frontier | high |
| 4 | Honest image/video generate | Typed CUDA vs weights errors; probe path documented | frontier | max |
| 5 | Catalog identity | Embeddings tab; shared cards; Inkling in both; downloaded-first in Settings | frontier | max |
| 6 | Hub sync UX | Tag normalize; compact Sync now; orb + status; quarantine copy | frontier | high |
| 7 | Architecture, known-gaps, CI | Layout clean; gaps honest; field checklist | frontier | max |

---

## Task inventory (T###)

- T001 [P] [ChatPage] Hide idle Mic closed; recording-only indicator; Persona in composer overflow.
- T002 [P] [ComposerContextRow / ContextUsageBar / QuickModelSwitcher] Context ~70-75%, picker ~25-30% on all four tabs.
- T003 [P] [transcriptChrome / MessageBubble] Meta above text; full-word token copy; skip meta while pending.
- T004 [P] [FolderTree] Visible selected row; studio pane binds open session.
- T005 [P] [titleGenerator / ChatPage / explorer rename] Persist auto-title; fallback immediately; optional refine after first reply.
- T006 [P] [AgentStateOrb / mapping / orbEngine] Caption rotator; pill layout toward the reference; no thinking-orbs npm package.
- T007 [P] [ImageStudioPage / VideoLabPage / StudioHistoryPane] Remove empty header; Chatbot FolderTree copy.
- T008 [P] [catalog.json / ListedModelDto / composerSessionUsage] visualTokenBudget on image/video rows; Context bar uses it.
- T009 [P] [runtimes/diffusion] Distinct CUDA vs weights errors; unit tests; no stub on real hosts.
- T010 [P] [typed_catalog.py / ModelsSettings / catalogTabs] Embeddings tab; shared pill row; company + country; month-year released.
- T011 [P] [patient-tier + visibleModelsOnTab] Same rows in installer and Settings; Settings downloaded-first then installer order.
- T012 [P] [SkillsSettings] Normalize Hub tags; drop equal-tag banner; sync orb + status strings; quarantine explanation.
- T013 [P] [docs/archive/v2/v2.2/known-gaps.md] Close DF-32 and DF-35 as observed; reconcile DF-25 / DF-28 / DF-29 / DF-30 / DF-31 against the 2026-08-25 screenshots; keep DF-4 until a real generate is recorded; add the v2.2.9 section.
- T014 [P] [CI] Chrome, title persist, orb captions, visual budget, diffusion error kinds, catalog pills, tag normalize. No live GPU.

---

## Phase 1: Chat Chrome, Transcript Meta, Selection, and Titles

**Goal**: Chatbot (and Agents, which share the composer row and FolderTree) match screenshots 1-2: no leftover Mic/Persona labels, Context-dominant footer, meta above the text in full words, a visible active session, and a rail title that is not stuck on New chat.
**Prerequisites**: None (v2.2.8 is on `main`).
**Stability Gate**: Desktop tests: Context row flex; picker not `flex: 1 1 auto` unbounded; `formatBubbleTokens` golden strings; pending bubbles have no `message-meta`; FolderTree selected uses a contrast token; `generateTitle` path calls `renameChat` and FolderTree shows the new title after `refreshToken`.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Cross-cutting chat UI plus explorer persist. High blast radius if titles overwrite user renames.

### Sub-tasks

#### 1.1 -- Composer leftover chrome (T001)

**Objective**: Idle Chatbot no longer shows `Mic closed` or a leftover `Persona` label.

**Failure modes**: Mic open with no indicator; persona lost with no way to set it; Agents accidentally lose a control they never had.

**Prompt**:
> Implement v2.2.9 Phase 1.1 from docs/archive/v2/v2.2/plans/v2.2.9-field-chrome-catalog-and-generate.md. In `desktop/src/modules/chat/ChatPage.tsx`, render the voice capture indicator only when `voiceLoop.captureVisible` is true (copy: Recording -- microphone is open). Remove the always-visible Persona button from the composer footer. Keep persona editing behind the composer `+` / overflow control (or equivalent) so `chat.explorer.setPersona` still works. Tests: ChatPage must not show `Mic closed` on the empty composer; recording still exposes the indicator test id. Do not change Agents unless they share this footer.

#### 1.2 -- Context vs picker width (T002)

**Objective**: On all four tabs the Context pill is the wide control and the model selector is a short trailing control.

**Failure modes**: Long model names clip without a title/`title` attribute; wrapping dumps the picker to a second row that looks like screenshot 1 inverted; Images/Videos hide the bar when budget exists.

**Prompt**:
> Implement v2.2.9 Phase 1.2. Change `ContextUsageBar` from `flex: 0 0 auto` to grow (~3). Change `ComposerContextRow` so the picker wrapper (`QuickModelSwitcher`) is `flex: 0 1 30%` (or `max-width: 30%`) with `min-width` enough for catalog display names. Prefer `flexWrap: nowrap` so the inverted screenshot-1 layout cannot return via wrap. Long names ellipsize with a `title` tooltip. Apply the same row on Chatbot, Agents, Images, Videos. Tests: layout contract (flex grow on the bar, bounded picker). Do not invent a Context bar when `denominatorKind` is `none` (Phase 3 adds visual budgets).

#### 1.3 -- Transcript meta above the text (T003)

**Objective**: Completed bubbles show clock and full-word tokens above the body. Pending bubbles show neither.

**Failure modes**: Pending still shows unknown-token punctuation; 0 invented for missing counts; user pluralization wrong at 1.

**Prompt**:
> Implement v2.2.9 Phase 1.3. Move `BubbleMeta` above `message.content` in `MessageBubble.tsx`. Skip `BubbleMeta` when `message.pending`. Update `formatBubbleTokens`: user `1 input token` / `N input tokens` / omit if unknown; assistant `TOTAL tokens (R reasoning + O output)` when both known; if only output `N output tokens`; if only reasoning `N reasoning tokens`; omit token span if unknown. Keep `formatBubbleTime`. Update `transcriptChrome.test.ts` and MessageList/ChatPage tests. ASCII hyphens only in new copy (no em dash for pending).

#### 1.4 -- Visible selected session (T004)

**Objective**: The open chat/session row is obvious.

**Failure modes**: Selected and hover look the same; collapsed rail still invisible; studio pane selection drifts from the open id.

**Prompt**:
> Implement v2.2.9 Phase 1.4. In `FolderTree.tsx` `rowStyle`, do not use `--bg-1` as the selected fill on a `--bg-1` pane. Use a mix (`--bg-2` or `color-mix` on `--fg-0`) plus a 4px accent left bar for chat rows. Bind `StudioHistoryPane` `selected` to the session the page has open. Tests: selected row style / `aria-selected` plus contrast assertion (not equal to pane background).

#### 1.5 -- Persist auto-title (T005)

**Objective**: After the first prompt, the rail is not left as New chat.

**Failure modes**: User rename overwritten; title lost if sidecar down; 5s title RPC blocks or races the live Hi.

**Prompt**:
> Implement v2.2.9 Phase 1.5. After first send on a still-default title, immediately `renameChat` with `fallbackTitle(prompt)` (`byUser` false). Still call `chat.generateTitle` **after** the first assistant turn completes (or on a short timeout if the model path is free) and rename again only if `userRenamed` is false. `generateChatTitle` must persist through explorer rename, not only `setActiveChat`. Bump FolderTree `refreshToken`. Tests: store title changes; user rename wins; empty prompt still New chat. Agents/Image/Video: reuse the same helper if they still default to New chat / New session copy (Phase 3 copy change).

#### 1.6 -- Testing and Stabilization

**Prompt**:
> Generate and run tests for Phase 1 (ChatPage, transcriptChrome, FolderTree, title persist). Fix failures. Do not ask for a GPU generate. Session history under docs/archive/v2/v2.2/development/history/.

### Phase 1 Exit Checklist

- [x] T001-T005 landed
- [x] Tests passing
- [x] Session history written
- [x] Ready for Phase 2

---

## Phase 2: Thinking-Orb Captions

**Goal**: While the model works, Chatbot/Agents show a reference-class pill orb that cycles Thinking / Searching / Working / Solving with no clock or tokens. The completed reply replaces it (Phase 1 meta).
**Prerequisites**: Phase 1 (pending meta already omitted).
**Stability Gate**: Desktop tests: pending orb cycles captions from a fixed list; reduced-motion holds one caption; `package.json` has no `thinking-orbs` dependency.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Motion + a11y + shared AgentStateOrb. Easy to ship a noisy spinner that fights reduced-motion.

### Sub-tasks

#### 2.1 -- Caption rotator and pill chrome (T006)

**Objective**: Pending chat/agents orbs match the reference grammar closely enough that screenshots 3 vs 4/5 no longer diverge on caption and pill shape.

**Failure modes**: Captions change every frame; screen readers hear a flood; image/video hero orbs accidentally shrink to bubble.

**Prompt**:
> Implement v2.2.9 Phase 2.1 from the plan. Reverse-engineer pill + particle variants from https://github.com/Jakubantalik/Libraries (`thinking-orbs`) and https://orbs.jakubantalik.com/. Do not npm install thinking-orbs. Add a caption rotator (interval ~2-3s, shuffle once per pending bubble) over Thinking..., Searching..., Working..., Solving.... Wire Chat/Agents pending `AgentStateOrb` to that rotator. Keep Image/Video pending `hero`. Nexus accent tokens only. `prefers-reduced-motion`: static dots + first caption. Screen readers: the rotating caption must not announce every rotation -- keep the visible caption out of the live region (`aria-live="off"` or `aria-hidden`) and expose one stable accessible name (e.g. `Generating reply`) on the pending bubble. Tests for rotator order/interval (fake timers), reduced-motion, and the stable accessible name. Skills Sync now orb is Phase 6.

#### 2.2 -- Testing and Stabilization

**Prompt**:
> Run AgentStateOrb / orbEngine / MessageBubble pending tests. Confirm package-lock has no thinking-orbs. Session history for Phase 2.

### Phase 2 Exit Checklist

- [x] Caption rotator landed
- [x] No thinking-orbs dependency
- [x] Tests passing
- [x] Session history written

---

## Phase 3: Studio History Identity and Visual Context

**Goal**: Images and Videos use Chatbot history chrome, no empty top bar, and a Context pill backed by visual budget so a session can show fill while last-output img2img remains the iteration mechanism.
**Prerequisites**: Phase 1 Context row flex (shared component).
**Stability Gate**: Image/Video tests: no empty header when models installed; FolderTree copy matches Chatbot strings; Context bar appears when `visualTokenBudget` is set; empty-attachment follow-up still img2imgs last PNG (existing tests).
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Shared FolderTree copy plus catalog DTO. Easy to fake an LLM window onto RealVisXL.

### Sub-tasks

#### 3.1 -- Empty header and Chatbot copy (T007)

**Objective**: Screenshots 6-7 no longer show an empty bar or Start a new session.

**Failure modes**: Get-more-models CTA disappears when none installed; Video diverges from Image.

**Prompt**:
> Implement v2.2.9 Phase 3.1. In ImageStudioPage and VideoLabPage, do not render the top `<header>` when it has no visible children; if none-installed, keep the get-more-models control in the composer area or a non-empty header. Change `StudioHistoryPane` copy to Chatbot FolderTree strings (Chats, New chat, empty hint without Start a new session). Selected session from Phase 1.4. Tests for header absence and copy.

#### 3.2 -- Visual session meter (T008)

**Objective**: Image/Video composers show Context against a visual budget, not a fake LLM window.

**Failure modes**: 128k invented; stub 1x1 counted; bar hidden when budget exists.

**Prompt**:
> Implement v2.2.9 Phase 3.2. `visualTokenBudget` already exists in the codebase on three vision LLM rows (`qwen3.5:9b`, `qwen3.5:4b`, `gemma-4-12b-it-gguf`) with a defined shape, a schema in `core/registry/catalog.ts` / `NexusModelRegistry.ts`, and a DTO path through `desktop/sidecar/src/protocol.ts` and `desktop/src/pages/settings/modelsTypes.ts`. Reuse that exact shape; do not invent a second field. Add `visualTokenBudget` to image and video rows in `core/registry/catalog.json` (honest maxImages / maxVideoFrames; reuse `core/chat/vision.ts` defaults if a row has none) and widen the catalog schema/types if they currently restrict the field to chat rows. Thread through sidecar `ListedModelDto` to `composerSessionUsage`. Context bar uses `denominatorKind: visual`. Do not set `contextWindow` on diffusion rows. Keep v2.2.6 last-output follow-up. Tests: catalog parse (schema accepts the field on image/video rows), composerSessionUsage visual path, ImageStudioPage bar present.

#### 3.3 -- Testing and Stabilization

**Prompt**:
> Run ImageStudioPage, VideoLabPage, StudioHistoryPane, catalog tests. Session history for Phase 3. Do not claim a live PNG.

### Phase 3 Exit Checklist

- [x] Header/copy/visual bar landed
- [x] Tests passing
- [x] Session history written

---

## Phase 4: Honest Image and Video Generate

**Goal**: A packaged generate either writes a PNG/MP4 or a typed reason (Python CUDA missing vs weights missing vs GPU not ready). Screenshot 7's combined string is not the only message.
**Prerequisites**: None strictly; can overlap Phase 3.
**Stability Gate**: Python tests: `can_run_real` false yields a CUDA-or-runtime message distinct from missing weights; missing weights path names the model id; stub still test-only. Desktop tests: UI shows the typed message. No CI job with a live GPU.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Production diffusion + packaged Python env. High risk of claiming generate from a stub.

### Sub-tasks

#### 4.1 -- Typed runtime errors (T009)

**Objective**: Operators can tell CUDA torch from missing RealVisXL weights.

**Failure modes**: Combined string remains; stub PNG on a laptop; Ollama GPU telemetry treated as torch CUDA.

**Prompt**:
> Implement v2.2.9 Phase 4.1. In `runtimes/diffusion/pipelines/base.py`, `runtimes/diffusion/pipelines/video_base.py`, and `real_execute.py`, split `fail_closed_execute` / `RuntimeNotReady` into explicit messages (image and video paths both; the review findings above show all three surfaces share the combined string today): (1) diffusion Python has no CUDA (`torch.cuda.is_available()` false) even if the app telemetry footer shows NVIDIA VRAM; (2) weights path missing for `model_id`; (3) GPU not available as today. Sidecar `resultGuard` must not collapse those into one string. Document that Ollama can use the GPU while the diffusion venv is CPU-only. Tests in `tests/python/diffusion`. Desktop diffusion-resultGuard tests for the new strings. Do not enable `NEXUS_DIFFUSION_ALLOW_STUB` outside pytest. Do not close DF-4 without a recorded PNG/MP4.

#### 4.2 -- Testing and Stabilization

**Prompt**:
> Run python diffusion tests and desktop resultGuard tests. Session history for Phase 4. Human GPU generate waits for Phase 7.

### Phase 4 Exit Checklist

- [x] Typed errors landed
- [x] Tests passing
- [x] Session history written

---

## Phase 5: Catalog Identity (Installer = Settings)

**Goal**: Screenshot 8 and screenshot 9 are the same catalog UI except install actions and downloaded-first sort. Embeddings is the first tab. Inkling-Small appears in the installer without `NEXUS_PATIENT_TIER`.
**Prerequisites**: None strictly; golden sort fixture from v2.2.8 still applies.
**Stability Gate**: Installer pytest and desktop ModelsSettings tests: Embeddings first; pill order; company from `FAMILY_TO_PUBLISHER`; patient-tier visible; Settings downloaded-first then `collapse_and_sort`. Dual-assert fixture updated.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Qt + React + shared catalog. Easy to drift pills again.

### Sub-tasks

#### 5.1 -- Shared card grammar and Embeddings tab (T010)

**Objective**: Name row: display name, then pills Company, Country, Agentic Yes/No, Context window {n}k tokens, Multimodal Yes/No, Guardrails Censored/Uncensored/other, License, Released Month YYYY.

**Failure modes**: Unknown company rendered as Community without a fallback policy; embeddings dropped from Chat and lost; month locale non-ASCII.

**Prompt**:
> Implement v2.2.9 Phase 5.1. Add an Embeddings tab **before** Chat in installer `TYPE_TABS` and desktop `CATALOG_TAB_DEFS`. Map `task: embed` / `type: embed` to that tab (stop parking them on Chat). On installer `typed_catalog.py` and Settings `ModelsSettings.tsx`, put the fact pills on the **name row** (not under the description) in the order in this plan. Company from `FAMILY_TO_PUBLISHER` (`Company: Google`). Country from `origin` (`Country: USA`). Multimodal from modalities beyond text / `vision: true`. Guardrails from `uncensored` (`false` -> Censored, `true` -> Uncensored). Released as `January 2025` (en-US month, ASCII). Omit any pill whose source value is missing; never render Unknown or invent Community. Keep installer checkbox / Why this one / Recommended. Settings keeps Download / Downloaded / Remove and a downloaded highlight. Tests for tab order and pill strings (golden). Description stays behind Details if needed for density.

#### 5.2 -- Same rows and downloaded-first (T011)

**Objective**: Installer shows Inkling-Small. Settings sorts downloaded-ready first, then installer rank.

**Failure modes**: Patient-tier auto-selected as recommended; gpt-oss downloaded still sorts after LFM when installer rank says otherwise among downloaded.

**Prompt**:
> Implement v2.2.9 Phase 5.2. Stop hiding `patient-tier` in the installer picker (or gate both surfaces identically; the field request is **show** Inkling in the wizard). Do not add it to recommended.json defaults. Settings `visibleModelsOnTab`: partition installed-and-ready first, each partition ordered by existing `collapse_and_sort` / recommendation rank (Gemma before LFM before gpt-oss when those three are downloaded; gpt-oss stays after downloaded Gemma+LFM until it is downloaded). Update `tests/fixtures/v2.2.8-catalog-tab-sort.json` or add a v2.2.9 fixture. Dual-assert Python + TS.

#### 5.3 -- Testing and Stabilization

**Prompt**:
> Run installer catalog tests and desktop ModelsSettings / catalogTabs tests. Session history for Phase 5.

### Phase 5 Exit Checklist

- [x] Embeddings tab and pills landed
- [x] Inkling visible in installer tests
- [x] Downloaded-first tests passing
- [x] Session history written

---

## Phase 6: Hub Sync UX and Quarantine Copy

**Goal**: Skills does not claim an update from 3.21.0 to v3.21.0. Sync now is the top control, shows an orb plus staged status, and Quarantined explains the scanner.
**Prerequisites**: Phase 2 orb rotator (reuse, do not fork a third engine).
**Stability Gate**: SkillsSettings tests: `3.21.0` equals `v3.21.0`; update banner absent when equal; syncing shows orb + status strings; quarantine row includes scanner-reason copy. Scanner still on.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Tag compare is mechanical; allowlist review is security-sensitive. Default to keep quarantine.

### Sub-tasks

#### 6.1 -- Tag normalize, compact Sync now, orb status (T012)

**Objective**: Screenshot 10 banner gone when tags match; Sync now is the first skills control; sync is not a frozen label.

**Failure modes**: Banner hidden when tags truly differ; orb blocks the button; status lies that install finished.

**Prompt**:
> Implement v2.2.9 Phase 6.1. Normalize Hub tags (strip leading `v`, case-insensitive) before compare and before display if you keep one canonical form. Apply the same normalize to **both** compare sites in `SkillsSettings.tsx` (the banner at the `skills-update-available` block and the header `Upstream latest` line around line 191) so the header cannot keep implying an update after the banner is fixed. Remove `skills-update-available` when normalized tags are equal. Move the Sync now + auto-update row to the top of Skills (under Active / Upstream). While syncing, show a Phase 2-class orb and status steps: New version found; Installing version X.Y.Z now; Harness up-to-date with Nexus-Hub version X.Y.Z (or Sync blocked + existing summary). Tests for 3.21.0 vs v3.21.0. Do not disable PromptInjectionScanner.

#### 6.2 -- Quarantine explanation (and optional narrow waiver)

**Objective**: Operators understand why `cross-project-comparison` is listed.

**Failure modes**: Blanket mute of jailbreak rule; planted jailbreak allowlisted.

**Prompt**:
> Implement v2.2.9 Phase 6.2. Quarantine section copy: this skill is not enabled because the injection scanner found a high-severity pattern; Review and approve is explicit trust. Keep the finding text. Optionally open the Hub skill file: if the match is a quoted teaching example, add one (skill, rule) waiver to `hubSkillScanAllowlist.ts` with a reason, and bump `HUB_ALLOWLIST_REVIEWED_TAG` only after reading the line. If the skill body instructs ignoring prior instructions as an operational step, leave it quarantined. Tests for copy; allowlist tests if a waiver is added.

#### 6.3 -- Testing and Stabilization

**Prompt**:
> Run SkillsSettings and NexusHubSyncer tests. Session history for Phase 6.

### Phase 6 Exit Checklist

- [x] Tag normalize landed
- [x] Scanner still on
- [x] Tests passing
- [x] Session history written

---

## Phase 7: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, its living docs architecture current, and its CI/CD complete and optimized, with independent evidence that the plan Goal landed.
**Prerequisites**: All prior phases.
**Stability Gate**: `docs/archive/v2/v2.2/development/last-phase-evidence.md` exists with one quoted section per duty below. A missing Goal, a Goal-review miss without a recorded known-gap, or a missing evidence section blocks `/update release`.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, Goal-vs-codebase review, known-gap reconciliation, and release gating carry high context volume and blast radius.

### Sub-tasks

#### 7.1 -- Architecture refactor

**Objective**: Refactor toward a well-organized layout and quote the scan.

**Prompt**:
> Identify deprecated chrome (empty studio headers, duplicate pill formatters). Propose-then-apply via project-refactor / docs-layout-refactor. Chatbot stays on `chat.explorer.*`. Write detector output into docs/archive/v2/v2.2/development/last-phase-evidence.md under Architecture refactor.

#### 7.2 -- Known-gaps reconciliation (T013)

**Prompt**:
> Update docs/archive/v2/v2.2/known-gaps.md. Close DF-32 (packaged Hi observed 2026-08-25, screenshot 2). Close DF-35 (packaged Active 3.21.0 matching GitHub latest v3.21.0, screenshot 10; residual is tag-normalize, owned by this cycle). Also reconcile the v2.2.7 packaged-observation rows against the same 2026-08-25 field session: DF-31 (screenshot 2 shows packaged date/time/token chrome rendering -- close if the screenshots cover what the row asked for, else annotate what remains), DF-28 (screenshot 9 shows the packaged Settings Models tab; screenshot 8 shows the installer -- same test), DF-30 (Context pill observed on packaged Chatbot; the 80% CTA part stays open unless a screenshot shows it), DF-29 and DF-25 only if the session actually evidences them. Close each row only on what the screenshots actually show; partial observation is an annotation, not a close. Close chrome/catalog/Hub-banner rows only with tests or operator evidence. Keep DF-4 unless a GPU PNG/MP4 was recorded. Keep DF-33 overlay folders. Keep DF-34 until packaged Settings vs installer screenshots match the shared card grammar. Keep DF-23 if a live git-clone Hub Update path is still unproven after this soak. Add the v2.2.9 section (summary table, open, resolved) and bump the file's Last updated line.

#### 7.3 -- Living docs architecture

**Prompt**:
> Check docs/handbooks, docs/decisions, README / DEVLOG / todos. Self-gated: never invent docs/testing or docs/validation. Quote the check under Living docs architecture.

#### 7.4 -- Git-tree hygiene

**Prompt**:
> Report merged remote branches and repo settings without deleting anything. Quote under Git-tree hygiene.

#### 7.5 -- CI/CD (T014)

**Prompt**:
> Cover T001-T012 tests on existing jobs (test:shell, test-ts, installer-tests). No live GPU. No live Hub clone. Path filters and concurrency cancel-in-progress.

#### 7.6 -- Independent Goal-vs-codebase review

**Prompt**:
> Re-read this plan Goal. Inspect the tree as if this agent had not implemented the phases. Record chrome, catalog identity, Hub banner, and generate honesty. A miss is a known-gap, not a pass.

#### 7.7 -- Testing and Stabilization

**Prompt**:
> Full test suite. Session history for Phase 7. Rebuild the unsigned Windows installer for field QA. Stop before /update release unless asked. Human checklist below.

### Phase 7 Exit Checklist

- [x] Evidence file complete
- [x] known-gaps.md updated
- [x] CI comments cover new contracts
- [x] Session history written

---

## Complexity Tracking

> Fill ONLY if Constitution Check has violations that must be justified

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| | | |

---

## Out of scope

- Re-running v2.2.0-v2.2.8 as greenfield (timeouts, Hub apply-with-quarantine, FolderTree 280/56, Models **sort** fixture).
- `npm install thinking-orbs` or `border-beam` / `liquid-gooey`.
- Disabling `PromptInjectionScanner`.
- Inventing LLM `contextWindow` on SDXL / video checkpoints.
- Closing DF-4 from unit tests.
- Bumping package.json / tagging / GitHub Release.
- Agents sidecar folder schema (DF-33) unless it falls out of title persist.
- Video remount `priorJobId` (DF-26).
- Persisting Agents tool-call cards (DF-27).

## Residual risks

- Diffusion Python may stay CPU-only on a laptop where Ollama already uses NVIDIA. Typed errors explain that; they do not install CUDA torch.
- Hub latest may still quarantine skills; Active latest plus a non-empty Quarantined list remains success.
- Long Gemma names in a 30% picker may ellipsize; `title` tooltip is required.

## Operator field checklist (deferred to DF-36)

Release note (2026-08-28): the operator field checklist was explicitly deferred for this release and remains open as DF-36. The unsigned Windows installer was rebuilt and smoke-probed, but items 1-7 were not observed; not_observed != absent.

1. Chatbot: no Mic closed / Persona leftover. Context bar is the wide control; Gemma 4 12B fits in the short picker.
2. After `Hi`: rail title is not New chat; active row is highlighted; time and `1 input token` sit above the user text; assistant meta is `N tokens (R reasoning + O output)` above the reply.
3. While composing: cycling Thinking/Searching/Working/Solving pill; no clock; no token dash. Reply replaces the orb.
4. Images/Videos: no empty top bar; history matches Chatbot copy; Context pill visible when a visual budget exists; follow-up without an attachment still targets last PNG.
5. Images generate: visible PNG **or** a message that names CUDA-missing vs weights-missing. Videos generate follows the same rule (visible MP4 or the typed reason). Combined-only string is a miss on either tab.
6. Installer Models and Settings Models: Embeddings first, same pill row, Inkling-Small present, downloaded highlight + actions only in Settings, downloaded-first sort in Settings.
7. Skills: no 3.21.0-to-v3.21.0 banner; Sync now at top with orb + status; Quarantined explains the scanner; scanner still on.

## Copyable implementation prompt

```
Implement Nexus AI Studio v2.2.9 from docs/archive/v2/v2.2/plans/v2.2.9-field-chrome-catalog-and-generate.md. Follow AGENTS.md. Do not re-run v2.2.0-v2.2.8 as greenfield. Do not bump package.json (root 2.2.8, desktop 1.5.0). Do not disable the Hub injection scanner. Do not npm install thinking-orbs. Do not run /update release unless asked.

PRODUCT LOCKS
- Cold start Chatbot. Peeking a tab never loads weights.
- Tab labels: Chatbot, Agents, Images, Videos.
- Tools always on.
- Context ~70-75% / picker ~25-30% on all four composers.
- Bubble meta above text; pending has no meta.
- Auto-title persists via explorer rename.
- Selected session has contrast, not --bg-1 on --bg-1.
- Orbs: rotator captions, no thinking-orbs package.
- Image/Video history copy = Chatbot; no empty header.
- No fake LLM window on diffusion; visualTokenBudget only.
- Typed CUDA vs weights generate errors.
- Settings Models cards = installer pills; Embeddings tab first; same rows.
- Hub tags compare without a leading v. Scanner on.

HOW TO WORK
- /implement phase 1, then commit, then 2, commit, then 3, commit, then 4, commit, then 5, commit, then 6, commit, then 7, commit, then push develop.
- After each phase: run that phase's tests, fix failures, write session history under docs/archive/v2/v2.2/development/history/.
- After Phase 7: remaining known-gaps, unsigned Windows installer, stop before /update release unless asked.
- ASCII commit messages. No Co-Authored-By.

Start at Phase 1.1. Stop at the phase stability gate.
```
