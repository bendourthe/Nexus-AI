# Plan -- Chatbot-First Shell and Runtime Honesty

**Project**: Nexus AI Studio
**Version**: v2.2.4
**Slug**: chatbot-first-and-runtime-honesty
**Filename**: v2.2.4-chatbot-first-and-runtime-honesty.md
**Plan Type**: Feature / Bugfix (patch follow-on)
**Created**: 2026-08-23
**Predecessor**: [v2.2.3-glass-orbs-and-pillar-runtime.md](v2.2.3-glass-orbs-and-pillar-runtime.md)
**Seeded from**: operator field session 2026-08-23 (nine screenshots) plus a read-only front/back review of the desktop app, sidecar, and installer snapshot. Not a comparison report.
**Goal**: Cold launch always opens Chatbot. The four tabs are Chatbot, Agents, Images, Videos. Every picker lists only this-install (or later in-app downloaded) models in installer order, with the recommended or favorite pre-selected. Sending "Hi" shows a user bubble and talks to the visible model. Image/video results are compact, click-to-preview, and never a giant empty rectangle. Settings > Models looks like the installer catalog. Hub Update now actually updates.

## Constitution Check

No constitution file found at `docs/archive/v2/v2.2/constitution.md` (or `docs/constitution.md`). Skipping the MUST/SHOULD gate. Recommend `/constitution` if this project wants a durable principle file. This is informational, not blocking.

Grounding that was read instead:

- `AGENTS.md` (local-first, four pillars, no invented capability)
- `docs/archive/v2/v2.2/known-gaps.md` (DF-1, DF-2, DF-4, DF-14, DF-16 through DF-22 still open)
- `docs/archive/v2/v2.2/plans/v2.2.3-glass-orbs-and-pillar-runtime.md` (several v2.2.3 "fixes" are still field-false)
- `docs/todos.md` (active branch is develop at v2.2.3; package remains 2.1.0 until `/update release`)
- No `STRATEGY.md` at repo root

This plan serves the local-first persona: a laptop user who already ran the installer and expects the desktop app to talk to the models they just downloaded, not a hardcoded Gemma 4 id.

## Current model map

**Model map status**: fresh as of 2026-08-23; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|------|-----------|--------|--------|--------|
| frontier | claude-fable-5 | gpt-5.6-sol | gemini-3.7-flash | grok-4.6 |
| strong | claude-opus-5 | gpt-5.6-terra | gemini-3.7-flash | grok-4.5 |
| standard | claude-sonnet-5 | gpt-5.6-terra | gemini-3.7-flash | composer-2.5 |
| fast | claude-haiku-4-5 | gpt-5.6-luna | gemini-3.7-flash | composer-2.5-fast |

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/about-claude/models/overview
- OpenAI: https://developers.openai.com/api/docs/guides/latest-model
- Google: https://ai.google.dev/gemini-api/docs/changelog
- Cursor: https://cursor.com/docs/models-and-pricing

Google's public 2026-08-13 changelog lists Gemini 3.7 Flash as the GA workhorse. A distinct Gemini 3.7 Pro id was not confirmed from that page in this session, so Flash fills strong/standard/fast rather than inventing a Pro slug.

Host-platform enumeration may confirm the Cursor picker at implement time. It must not collapse this table to one provider.

## Review verdict (this session)

**NO-GO** as a working product. The installer is now looking and working as expected. The desktop app is not. v2.2.3 closed several of these rows in tests (BG-16 default route, BG-24 empty completes, installed-only pickers) while the running UI still violates the operator's contract.

### Project health

| Surface | Status | Detail |
|---|---|---|
| Git version control | OK | Repo present with history on `develop` / `main` |
| Version number | OK | Product cycle v2.2.3 on develop; package.json remains 2.1.0 until release handoff; desktop package.json is 1.5.0 (pre-existing drift, do not bump unless asked) |
| Branch model | OK | `develop` + `main` in use |
| Baseline docs | OK | README.md, CHANGELOG.md, docs/DEVLOG.md have real content |
| Per-version docs tree | OK with gap | `docs/archive/v2/v2.2/plans/` and `known-gaps.md` exist. `docs/archive/v2/v2.2/comparisons/` has no files. |

Setup needed: comparisons folder is empty. Run `/setup project` only if you want that surface bootstrapped. This plan does not mutate setup.

### Why the app is not working as intended (root causes)

Mapped from the nine screenshots onto the tree. Support tier: **supported** where the file and call site were read in this session; **not proven here** where a live GPU or packaged Explorer launch was not repeated.

#### R1 -- Cold launch does not open Chatbot (screenshot 1, 5)

**Supported.** `desktop/src/main.tsx` restores `normalizeStoredRoute(readStoredRoute())`. `desktop/src/lib/persistence.ts` treats `/coding`, `/images`, `/videos`, and `/settings` as restorable. v2.2.3 BG-16 only normalized `/`, `/dashboard`, and garbage onto `/chatbot`. If the last session ended on Agents, the next launch opens Agents. The operator's rule is stronger: **every process start lands on Chatbot** because it is the top tab. Last-module restore is the opposite product.

`App.tsx` still `<Navigate to="/chatbot" />` for `/`, so a truly empty store is fine. The field failure is last-route restore, not the `/` redirect.

#### R2 -- Tab labels are still the old product names

**Supported.** `desktop/src/types/modules.ts` still ships `Local Chatbot`, `Agentic AI Coding`, `Image Studio`, `Video Lab`. Tests in `desktop/tests/shell-phase6.test.tsx` lock those strings. The operator wants **Chatbot**, **Agents**, **Images**, **Videos**.

#### R3 -- Gap above the first tab; collapse control is a top-right square (screenshot 1)

**Supported.** `desktop/src/components/Sidebar.tsx` renders the collapse `<button>` as the first flex child (`alignSelf: compact ? "center" : "flex-end"`), then `<nav>`. That button is the empty band above Chatbot. Brand was already removed in v2.2.0. The operator wants no gap, and a discrete pill on the **center of the right edge** of the pane (triangle left = collapse, triangle right = expand).

#### R4 -- Pickers list models that were not in this installation (screenshot 2)

**Supported, two stacked bugs.**

1. **Hardcoded fallback ids are not the installed set.** `desktop/src/modules/coding/models.ts` exports `DEFAULT_MODEL_ID = "gemma4:e4b"`. Chat and Agents initialize `useState(DEFAULT_MODEL_ID)`. Image Studio seeds `FALLBACK_MODEL` as SANA 1.5 1.6B (`ImageStudioPage.tsx`). Video has the same pattern. The visible `<select>` in `CompactModelSwitcher` then **displays** `ready[0]` when `value` is not in the installed list, **without calling `onChange`**. Screenshot 6 is the smoking gun: the dropdown shows LFM2.5 1.2B while send uses `gemma4:e4b`.

2. **`models.list` is a catalog-plus-probe, not "what this installer run selected".** `desktop/sidecar/src/models/modelsService.ts` reconciles the catalog against Ollama `/api/tags` and `~/.nexus/models/weights/*`. Leftover weights from a previous install, or a probe that marks extra catalog rows installed, appear in Image Studio. `installedModelsForType` only filters `installed && source !== "catalog-only"`. It does not intersect with `InstallerState.selected_model_ids`. The installer already records that ordered list (`scripts/installer/src/nexus_installer/installer_state.py`) and does **not** publish a runtime snapshot the sidecar reads.

There is no per-tab favorite. Recommended installer defaults are not the app's pre-selected ids.

`+ Get more models` already navigates to `SETTINGS_MODELS_PATH` (`/settings?tab=models`), but `SettingsPage` ignores the query string (`useState(initialTab)` with default `"models"`). Deep-link is DF-17. If Settings is already mounted on Skills, the query change may not switch tabs.

#### R5 -- Agents chrome is coding-specific and user-hostile (screenshots 4, 5)

**Supported.** `CodingPage.tsx` still paints a pink `h1` "Agentic AI Coding" (`color: "var(--accent-coding)"`). `ModelSelector` still renders `balanced scaffold (off)` and `tool-calling verified` badges. Chat still has an **Enable tools** checkbox default **false** (`ChatPage.tsx` `useState(false)`). The operator wants Agents to read as ChatGPT Work / Claude Cowork / Claude Code (coding and not-only-coding), no pink title, no jargon badges, and tools always on.

#### R6 -- "Hi" produces `gemma4:e4b is not installed` and no user bubble (screenshot 6)

**Supported. This is the P0 chat break.** `ChatPage.handleSubmit`:

1. Creates/opens a chat with `modelId` state, which is still `gemma4:e4b`.
2. Runs residency. `not-installed` appends an **assistant** banner and **returns**.
3. The user message is appended **after** that return (`appendMessage` of the user row is later in the same function).

So the transcript shows only the error, never "Hi". The dropdown lied about which model would be used.

Coding has the same `DEFAULT_MODEL_ID` seed and will fail the same way on a host that installed LFM2.5 and not Gemma 4.

#### R7 -- Image attach+generate yields a giant empty rectangle plus unexplained icons (screenshots 7, 8)

**Supported.**

- `MessageBubble` sizes media at `width: "48rem"`, `maxWidth: "100%"`, `minHeight: "8rem"`, `background: "var(--bg-2)"`. An empty or transparent PNG still occupies most of the pane. Pending studio messages use `minHeight: "12rem"`.
- Action row (Download, Copy workflow, Use prompt, Use seed, Use all, Remix, Use as source) renders whenever `m.media` is set. Screenshot 8 is that row under an empty shape, so `media` was set with a non-displayable src.
- v2.2.3 BG-24 claimed empty completes become written errors. Field evidence contradicts: the empty rectangle is still the dominant object. Either `complete.png` is a truthy empty/invalid payload, decode `onError` does not clear layout, or the pending placeholder never yields to an error bubble.
- `handlers.ts` still throws `NotImplementedError("image.generate")`. The UI path is `diffusion.txt2img` / `img2img`. A Python runtime that returns quickly with empty bytes still produces the screenshot 7 shape.
- There is no click-to-lightbox. The operator wants compact inline media, click opens a preview dialog with fullscreen plus copy/download.

Button semantics today (for the plan to document, not invent):

| Icon | Code name | What it actually does |
|---|---|---|
| Download | `image-download-*` | Writes the PNG bytes to a file |
| Two squares / JSON | Copy workflow | Copies Comfy-style workflow JSON, not the image |
| T | Use prompt | Puts the workflow prompt back into the composer |
| # | Use seed | Puts the workflow seed back |
| Layers | Use all | Restores prompt, seed, size, steps, cfg, sampler, model |
| Shuffle | Remix | Same as use-all plus a new random seed |
| Frame | Use as source | Seeds the next img2img from this PNG |

The operator asked for copy **image** (clipboard PNG) in the lightbox, not copy workflow as a primary action.

#### R8 -- Settings > Models is a filter form, not the installer catalog (operator request)

**Supported.** `ModelsSettings.tsx` is Type / Family / Status / Tier-fit dropdowns plus Installed / Available / External sections. The installer page (`scripts/installer/src/nexus_installer/pages/typed_catalog.py`) is Chat / Agentic / Image / Video / Audio / Document **tabs**, with the same descriptions, Best-for line, fact pills, Required/Recommended/Compatible badges, and hardware-disabled cards. The app page does not reuse that information architecture. Checkboxes in the installer should become Downloaded vs Download (disabled when the host cannot run the model).

#### R9 -- Skills Update now dies with `sidecar response timeout` (screenshot 9)

**Supported.** `desktop/src-tauri/src/sidecar.rs` waits **15 seconds** for every IPC reply. `skills.sync` runs `NexusHubSyncer.sync({ apply: true })`, which sparse-clones or tarball-fetches `bendourthe/Nexus-Hub` and scans the tree. That cannot finish in 15s on a cold network. The renderer maps `SidecarError::Timeout` to "sidecar response timeout". Auto-sync weekly is stored as `enabled: stored === true`, so the default is **OFF**. The operator wants the label **Auto-update to latest Nexus-Hub release**, default **ON**.

The red "The Nexus backend could not start" banner can also appear when `skills.list` / `skills.status` fail for the same timeout, even if the sidecar is alive.

### Structure / quality notes (review lenses, scoped to this failure surface)

- **Structure**: Pillar pages each own a copy of "load models, pick default, send". There is no shared `useInstalledCatalog(task)` that takes installer order + favorite. Settings Models does not share the installer's typed-catalog widgets. This duplication is why Chat, Agents, Images, and Videos can each lie differently.
- **Quality**: `CompactModelSwitcher` displaying `ready[0]` while parent state stays on `gemma4:e4b` is a classic view/state split. `FALLBACK_LLMS` marks every catalog LLM `installed: true` (`CodingPage.tsx`), so tests and ipc-unavailable mode teach the UI that Gemma 4 is installed.
- **Coverage**: Desktop vitest (v2.2.3 Phase 8: 1369 tests) never asserted "the selected option id equals the id passed to `chat.session.send`". It never asserted IPC methods that clone a git repo get a longer timeout. Image tests can pass with a fake `complete.png` of one byte.
- **Security**: No new outbound except Hub sync (already the skills path). Long-running sync must stay scoped to `~/.nexus-ai/catalog/` (existing `assertScopedCatalogRoot`). Do not raise the global 15s timeout for `models.list` / `chat.send`. Add a per-method timeout map instead. Do not log Hub tokens.
- **Changes vs v2.2.3**: Last-route restore, hardcoded `DEFAULT_MODEL_ID`, 15s global IPC, and 48rem media are leftover design, not unfixed typos. Treat them as product reversals, not drive-by edits.

### Deferred items this review does not reopen as in-scope

DF-1 SEA bundling, DF-2 packaged Explorer soak, DF-4 live-GPU soak (still label residual), DF-14 shared composer markup, DF-16 native file dialogs, DF-18/19 Unix packaging, DF-21 streaming chat tokens, DF-22 workspace Hub hook scripts. DF-17 (Settings URL tabs) **is in scope** because `+ Get more models` depends on it.

## User-reported issues (must each appear in a sub-task)

U1. Startup always opens Chatbot (top tab), not last-used Agents/Images/Videos.
U2. Rename tabs to Chatbot, Agents, Images, Videos.
U3. Remove the gap above the first tab. Collapse control is a discrete edge pill (triangle left/right).
U4. Per-tab pickers: only installed models, installer order, recommended as default, one favorite per tab stored in Settings, `+` opens Settings > Models.
U5. Agents: drop pink "Agentic AI Coding", drop jargon badges, drop Enable tools (tools always on).
U6. Chat "Hi" shows a user bubble and talks to the visible installed model.
U7. Image/video: no giant empty shape; compact inline media; click opens preview with fullscreen, copy, download; document what the remaining icons mean and keep only the ones that belong in-chat vs in-dialog.
U8. Settings > Models matches the installer catalog IA (category sub-tabs, same copy, Download vs Downloaded, hardware-disabled).
U9. Skills Update now succeeds (or fails with a real Hub error). Auto-update to latest Nexus-Hub release, default ON.

## Locked product decisions

- **Cold start**: every new renderer process opens `/chatbot`. Do not restore `/coding` / `/images` / `/videos`. Restoring a Chatbot *thread* inside `/chatbot` is allowed. Settings is not a cold-start landing.
- **Tab copy**: Chatbot, Agents, Images, Videos. Routes stay `/chatbot`, `/coding`, `/images`, `/videos` unless a rename is cheaper than churn; labels change either way.
- **Agents positioning**: general computer-use / cowork agent that can code. Not a coding-only IDE. Do not retitle the module "Coding".
- **Tools**: always enabled for Chatbot and Agents. Remove the checkbox. Web search and other tools stay behind existing confirmation/sandbox gates, not behind a user toggle.
- **Picker source of truth**: intersection of (a) catalog entries for that tab's task, (b) actually on disk / in Ollama, (c) in the installer `selected_model_ids` snapshot **or** later downloaded from Settings. Leftover weights from a previous install that were not in this run and not later downloaded do not appear.
- **Order**: installer `selected_model_ids` order, then in-app downloads appended.
- **Default selection**: favorite for that tab if set and still installed; else the installer-recommended id for that tab if installed; else first in the ordered list. Never `gemma4:e4b` unless that id is actually installed.
- **Favorite**: at most one per tab, chosen in Settings > Models. Changing favorite updates the next cold start and the next empty-session picker, not an in-flight session mid-send.
- **`+`**: navigates to Settings > Models (URL-addressable tab). That page shows catalog + download, not a fake model id.
- **Inline media**: fit in one viewport (suggested max height ~40vh, max width of the bubble column). Click opens a modal preview (dialog, Esc closes, fullscreen control, Download, Copy image).
- **In-chat icon row**: Download and Copy image only. Prompt/seed/remix/use-as-source live in the preview dialog (or Advanced settings), with visible tooltips. Do not leave a row of unexplained glyphs under an empty box.
- **Empty generate**: assistant bubble is an error string, never a grey rectangle, never a 48rem empty `<img>`.
- **Hub auto-update**: default ON. Label: "Auto-update to latest Nexus-Hub release". Idle-time cadence may stay weekly; the copy must not say "sync" as if it were optional housekeeping.
- **IPC**: do not globally raise the 15s timeout. `skills.sync` (and only methods that clone/fetch Hub) get a long timeout or a job+poll protocol.
- **Shipping version**: do not bump root `package.json` (2.1.0) or `desktop/package.json` (1.5.0) unless asked. Plan version is v2.2.4.
- **Do not re-run** v2.2.0 / v2.2.1 / v2.2.2 / v2.2.3 as greenfield. Do not rebuild `NexusSetup.exe` unless the runtime snapshot write needs an installer hook; prefer writing the snapshot from the existing install summary onto disk at end of install **and** a one-time migration that reconstructs it from weights+Ollama if the file is missing (so current users are not stuck until they reinstall).
- **Peeking a tab still never loads weights** (v2.2.3 occupancy lock).

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|-------|-------|---------|------------------------|--------------------------|
| 1 | Chatbot-first shell and rail | Cold start Chatbot; labels; edge pill; no gap | standard | medium |
| 2 | Installed catalog contract | Installer snapshot + ordered installed-only pickers + favorites | frontier | high |
| 3 | Chatbot and Agents honesty | Hi works; tools always on; Agents chrome gone | frontier | high |
| 4 | Compact studio media | No empty rectangle; lightbox; honest failures | frontier | high |
| 5 | Settings Models as installer | Category tabs, same copy, Download vs Downloaded | strong | high |
| 6 | Hub update that finishes | Long-running skills.sync; auto-update default ON | strong | high |
| 7 | Architecture refactor, known-gaps, CI | Layout clean, gaps reconciled, CI covers the contract | frontier | max |

---

## Phase 1: Chatbot-First Shell and Rail

**Goal**: A fresh process always shows Chatbot. Tabs read Chatbot / Agents / Images / Videos. The rail has no dead gap. Collapse is an edge pill.
**Prerequisites**: None.
**Stability Gate**: Tests prove `/` and every restorable last-route except in-chatbot thread restore land on `/chatbot`. Labels match. Collapse pill sits on the pane edge and does not consume a row above the first tab.
**Recommended model tier**: standard
**Recommended effort level**: medium
**Rationale**: Isolated shell/routing/CSS. No sidecar protocol change. One high-risk product reversal (stop restoring last module) but the implementation is localized.

### Sub-tasks

#### 1.1 -- Cold start always Chatbot

**Objective**: Last-module restore no longer overrides the top tab.

**Failure modes**: Malformed stored route still becomes `/chatbot`. A slow sidecar must not block first paint of Chatbot. Do not fight the user if they click Agents during the ready overlay; navigation after first paint is allowed.

**Build class**: load-bearing.

**Prompt**:
> Implement Nexus AI Studio v2.2.4 Phase 1.1 from docs/archive/v2/v2.2/plans/v2.2.4-chatbot-first-and-runtime-honesty.md. Follow AGENTS.md. Do not bump package.json.
>
> Product lock: every new renderer process opens Chatbot. `desktop/src/main.tsx` plus `normalizeStoredRoute` in `desktop/src/lib/persistence.ts` currently restore `/coding`, `/images`, `/videos`, and `/settings`. Change the restorable set so cold start is always `/chatbot`. You may still restore a Chatbot thread id inside `/chatbot` if that is already persisted. Update `desktop/tests/App.test.tsx` and persistence tests: a stored `/coding` launches `/chatbot`. Do not remove in-session navigation to Agents.
>
> Acceptance: a test that writes `nexus.shell.activeRoute=/coding` then mounts the app sees Chatbot selected. `/` still redirects to `/chatbot`.

#### 1.2 -- Rename tabs to Chatbot, Agents, Images, Videos

**Objective**: Rail copy matches the operator.

**Failure modes**: Missing module id still renders a fallback label, never a blank row. Shortcut Ctrl+1..4 still hit the same routes.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.4 Phase 1.2. Change `desktop/src/types/modules.ts` labels to Chatbot, Agents, Images, Videos. Keep routes unless a rename is cheaper than updating every test id. Update `desktop/tests/shell-phase6.test.tsx`, ModuleCard tests, and any aria-label assertions. Coding page must not reintroduce "Agentic AI Coding" in the rail (Phase 3 removes the in-page h1).

#### 1.3 -- Edge-centered collapse pill; delete the top gap

**Objective**: No empty band above Chatbot. Collapse lives on the rail's right edge.

**Failure modes**: Compact rail (56px) still shows a reachable expand pill. Keyboard and aria-expanded stay accurate. Clicking the pill must not navigate modules.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.4 Phase 1.3. In `desktop/src/components/Sidebar.tsx`, stop rendering the collapse control as the first flex child (that is the gap). Position a discrete pill on the vertical center of the aside's right border: triangle left when expanded, triangle right when compact. Keep `data-testid="sidebar-collapse-toggle"`. Add a regression that the first module link's bounding box is near the top of the aside (no spacer equal to two nav rows). Follow existing glass tokens; no per-tab accent.

#### 1.4 -- Testing and Stabilization

**Objective**: Shell tests and CI path filters cover Phase 1.

**Prompt**:
> Add/adjust desktop vitest for cold-start Chatbot, new labels, and collapse pill layout. Run the desktop suite for Sidebar, App, persistence. Update CI path filters if new files appear. Then `/generate-session-history` for Phase 1.

### Phase 1 Exit Checklist

- [x] Cold start Chatbot
- [x] Labels Chatbot / Agents / Images / Videos
- [x] Edge pill, no top gap
- [x] Tests passing
- [x] Session history written

---

## Phase 2: Installed Catalog Contract

**Goal**: One sidecar-backed contract: ordered installed models per tab, recommended default, one favorite per tab, `+` deep-links to Settings > Models.
**Prerequisites**: Phase 1.
**Stability Gate**: Chatbot/Agents/Images/Videos pickers never contain a catalog-only or leftover-previous-install id. Default id is favorite or recommended or first installed, and the `<select>` value equals the id the send path uses.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Crosses installer snapshot, sidecar list, four pickers, and Settings. The screenshot 2/6 failures are here. Uncertainty about leftover weights defaults to frontier.

### Sub-tasks

#### 2.1 -- Persist the installer selection snapshot for the app

**Objective**: Sidecar can read what *this* install (plus later in-app downloads) owns.

**Failure modes**: Missing snapshot file: reconstruct from successful installer log / weights markers + Ollama tags and label the reconstruction in logs, never silently show the full catalog. Corrupt JSON: ignore and reconstruct. Concurrent Settings download vs list: list after job complete, not a torn read.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.4 Phase 2.1. The installer already has ordered `selected_model_ids` on `InstallerState`. Write a runtime snapshot (for example `~/.nexus/runtime/selected-models.json`) at the end of install: ordered ids, recommended id per task (chat, agentic, image, video), timestamp. Teach the sidecar `models.list` (and a small dedicated getter if cleaner) to expose `installedForTask(task)` = catalog row AND on-disk AND (id in snapshot OR id downloaded in-app since snapshot). In-app downloads append to the snapshot. Add a one-time migration when the file is missing so current users are not forced to reinstall: seed from probe intersection with catalog, ordered by catalog/installer order, not by hashmap iteration. Tests in sidecar + installer if you add a writer hook. Do not rebuild NexusSetup.exe unless the writer cannot live in the existing complete/summary step; prefer writing from the same Python that already knows `selected_model_ids`.

#### 2.2 -- Shared picker hook: order, default, favorite, Get more models

**Objective**: Delete the view/state split in `CompactModelSwitcher`.

**Failure modes**: Empty installed list: picker shows only "+ Get more models..." and send is disabled with a Settings CTA, never a fake SANA/Gemma row. Favorite id uninstalled: fall back to recommended then first; do not error the whole page. `+` while Settings is already mounted on Skills: URL `?tab=models` must switch the tab (close DF-17 for this query).

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.4 Phase 2.2. Add a shared hook (for example `desktop/src/shared/models/useTaskModels.ts`) used by ChatPage, CodingPage, ImageStudioPage, VideoLabPage. It must: (1) list only `installedForTask`, (2) preserve installer order, (3) resolve selected id as favorite || recommended || first, (4) `setSelected` writes React state AND is the id send uses, (5) intercept GET_MORE_MODELS_ID and `navigate('/settings?tab=models')`. Fix `CompactModelSwitcher` so it never displays `ready[0]` while `value` is a different id; if value is stale, call `onChange(ready[0])` once in an effect or refuse to render a lying option. Persist favorites in sidecar settings (`nexus.ui.favoriteModel.chatbot` etc.), one id per tab. Surface the favorite control on Settings > Models in Phase 5; Phase 2 may write the setting API and a small per-tab star in the picker. Wire `SettingsPage` to `useSearchParams` so `tab=models` is honored (DF-17, at least for models). Tests: lying select is impossible; Chat send payload id equals selected option id.

#### 2.3 -- Kill hardcoded `DEFAULT_MODEL_ID` / `FALLBACK_MODEL` as installed

**Objective**: Gemma 4 and SANA are not installed just because the catalog mentions them.

**Failure modes**: ipc-unavailable (vitest): inject a fake installed list in tests; production must not mark catalog rows installed. Sidecar down: empty picker + existing SidecarDownBanner, never FALLBACK_LLMS with `installed: true`.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.4 Phase 2.3. Remove production use of `DEFAULT_MODEL_ID = "gemma4:e4b"` as a selected id. Remove Image `FALLBACK_MODEL` as an installed row (sidecar-down already has a banner; ipc-unavailable tests should pass an explicit fixture). `CodingPage` `FALLBACK_LLMS` currently sets `installed: true` for every `FRONTEND_MODELS` entry -- that is how Gemma 4 leaks. Replace with empty list or test-only fixtures. Grep for gemma4:e4b as a default selection and delete those call sites.

#### 2.4 -- Testing and Stabilization

**Prompt**:
> Cover snapshot parse, reconstruction, picker order, favorite fallback, Get more models navigation, and the select-value === send-id invariant for all four pillars. Run sidecar + desktop tests. Update CI. `/generate-session-history` for Phase 2.

### Phase 2 Exit Checklist

- [x] Snapshot exists or migrates
- [x] Pickers installed-only, ordered, non-lying
- [x] Favorites API in place
- [x] Tests passing
- [x] Session history written

---

## Phase 3: Chatbot and Agents Honesty

**Goal**: "Hi" shows as a user bubble and is answered by the visible installed model. Agents is a cowork surface, not a pink coding banner. Tools are always on.
**Prerequisites**: Phase 2 (send id must be an installed id).
**Stability Gate**: A host with only LFM2.5 installed can send Hi from Chatbot and get a user bubble plus a real request to LFM2.5 (or a honest model-error that still shows Hi). Agents has no pink h1, no harness/tool-calling badges, no Enable tools checkbox.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: The screenshot 6 failure is a control-flow bug plus the Phase 2 lie. Tool/default policy is product-sensitive.

### Sub-tasks

#### 3.1 -- User bubble first; never send an uninstalled id

**Objective**: Fix `handleSubmit` so the human message is visible even when the model is missing, and so the model id matches the picker.

**Failure modes**: Empty prompt: do not append an empty bubble. Sidecar timeout after the user bubble: keep the bubble, show an assistant error. Double-submit: disable send while pending.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.4 Phase 3.1. In `desktop/src/modules/chat/ChatPage.tsx` `handleSubmit`, append the user message (and persist it) BEFORE residency/not-installed returns. If the selected model is not installed, the assistant row may explain that, but "Hi" must already be in the transcript. After Phase 2 this branch should be rare; still keep it honest. Use the hook's selected id, not `DEFAULT_MODEL_ID`. Mirror the user-bubble-first rule on `CodingPage` submit. Add `ChatPage` tests: send "Hi" with listedModels that do not include gemma4:e4b results in a user bubble with "Hi" and a request whose modelId is the first installed LLM (e.g. LFM2.5). Screenshot 6 must become impossible.

#### 3.2 -- Agents chrome: no pink title, no jargon badges

**Objective**: Agents reads as a general agentic workspace.

**Failure modes**: Missing workspace path still shows the path field, not a crash. Session tabs Chat/Memory/Trace/Sessions remain.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.4 Phase 3.2. Remove the pink `h1` "Agentic AI Coding" from `CodingPage.tsx` (and any ModuleCard that still says that in the shell). Keep workspace path. In `desktop/src/shared/chat/ModelSelector.tsx`, stop rendering `harnessLabel` / `toolCallingVerified` badges on Chatbot and Agents (delete the props from call sites). Tests that require those badges should move to a Settings/developer surface or be deleted. Update `sharedChat.test.tsx` and CodingPage tests.

#### 3.3 -- Tools always on; delete Enable tools

**Objective**: Web search and other tools are not opt-in.

**Failure modes**: Tool execution still goes through ConfirmationGate / sandbox. Always-on is not auto-approve. Missing tool host: assistant explains, user bubble stays.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.4 Phase 3.3. Remove the Enable tools checkbox from `ChatPage.tsx`. Pass `enableTools={true}` (or delete the prop and default true). Keep tool cards visible when the model calls tools. ConfirmationGate and path guards stay. Tests: checkbox absent; a tool call still renders cards.

#### 3.4 -- Testing and Stabilization

**Prompt**:
> Chat + Agents tests for Hi, badges gone, tools always on. Run desktop suite. CI. `/generate-session-history` for Phase 3.

### Phase 3 Exit Checklist

- [x] Hi user bubble + correct model id
- [x] Agents chrome cleaned
- [x] Tools always on
- [x] Tests passing
- [x] Session history written

---

## Phase 4: Compact Studio Media and Honest Completes

**Goal**: Image/video results fit on one screen, click opens a preview dialog, empty completes are errors, unexplained icon rows are gone from the empty state.
**Prerequisites**: Phase 2 (correct image/video model ids).
**Stability Gate**: A complete without bytes never mounts a 48rem empty `<img>`. A successful PNG/MP4 renders compactly. Click opens a dialog with fullscreen, download, copy image. Vitest covers empty/invalid bytes; live GPU remains DF-4.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Field-false vs v2.2.3 BG-24. Media layout plus sidecar empty-complete paths. Residual live GPU.

### Sub-tasks

#### 4.1 -- Compact inline media + preview dialog

**Objective**: Chat-style media, not a full-pane canvas.

**Failure modes**: Missing src: do not mount `<img>`. Decode error: error text, dialog does not open. Esc / backdrop click closes dialog without losing the transcript. Video uses the existing asset-protocol URL (v2.2.3 BG-25); dialog plays the same URL.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.4 Phase 4.1. Change `MessageBubble` media styles: max-height ~40vh, width auto, no minHeight that creates a grey slab, no 48rem default width. Click (and Enter/Space) opens a modal preview with fullscreen toggle, Download, and Copy image (PNG clipboard / video file copy as documented). Put Use prompt / Use seed / Use all / Remix / Use as source in the dialog (or behind Advanced settings), each with a visible text label or tooltip that matches the table in this plan. In-chat action row: Download + Copy image only. Tests for compact layout, dialog open/close, and actions not rendering without displayable bytes.

#### 4.2 -- Empty or invalid completes cannot paint a rectangle

**Objective**: Make BG-24 true in the field.

**Failure modes**: `png` of `""`, whitespace, invalid base64, or a 1x1 transparent pixel: treat as failure. Python runtime crash: typed error event. img2img with attachment: do not complete success with empty bytes.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.4 Phase 4.2. Trace `ImageStudioPage` `advanceFromEvents` and the sidecar `diffusion.*` complete path (Python runtime + `enqueueInteractive` + drain). Reject empty/invalid payloads before `patchMessage(..., { media })`. `onError` on `<img>`/`<video>` must clear media and set an error string (already partially present; the layout must collapse). Re-read `handlers.ts` `image.generate` NotImplemented -- do not wire the UI to it; keep `diffusion.txt2img` / `img2img`. Add tests with empty png, invalid base64, and img2img+attachment that completes with no bytes. Screenshot 7 must become an error bubble, not a grey slab.

#### 4.3 -- Testing and Stabilization

**Prompt**:
> Image + video page tests, MessageBubble tests, sidecar empty-complete tests. Note DF-4 live GPU as residual. CI. `/generate-session-history` for Phase 4.

### Phase 4 Exit Checklist

- [x] Compact media + dialog
- [x] Empty complete is an error
- [x] Icon semantics documented in UI
- [x] Tests passing
- [x] Session history written

---

## Phase 5: Settings Models as Installer Catalog

**Goal**: Settings > Models uses the same category sub-tabs, descriptions, and hardware gating as the installer, with Download vs Downloaded instead of checkboxes.
**Prerequisites**: Phase 2 snapshot (so Downloaded matches the app pickers).
**Stability Gate**: A reviewer can place installer screenshot and Settings > Models side by side and see the same groups (Chat / Agentic / Image / Video / Audio / Document), same names, same recommended badges, Download disabled when the host cannot run the model, Favorite star per tab.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Large UI plus reuse of installer catalog fields. Download already exists on `models.install`. Risk is IA drift, not new protocols.

### Sub-tasks

#### 5.1 -- Category sub-tabs and installer copy

**Objective**: Replace the Type/Family/Status filter bar as the primary IA.

**Failure modes**: Unknown catalog task: land in an Other tab, do not drop the row. Sidecar down: existing banner, no fake Downloaded. Search may remain as a secondary filter inside a tab.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.4 Phase 5.1. Rebuild `ModelsSettings.tsx` to match `typed_catalog.py` tabs: Chat, Agentic, Image, Video, Audio, Document. Reuse catalog description, Best-for / strengths, license, size, recommended/required/compatible. Do not iframe the Qt installer. Share catalog JSON with the installer (already in `core/registry`). Favorite control: one star per tab, writes the Phase 2 setting. Deep link `?tab=models` already required in Phase 2.2.

#### 5.2 -- Download vs Downloaded and hardware disable

**Objective**: Checkbox-free, installer-parity gating.

**Failure modes**: Download failure: row error, not a silent Installed flip. Over-VRAM: button disabled with the same reason the installer uses. In-progress: progress from existing `models.install.drainEvents`.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.4 Phase 5.2. Downloaded rows show a non-interactive Downloaded state (and Favorite). Not downloaded: Download button calling existing install IPC. Disable when `modelFitsHost` is false. After success, snapshot append (Phase 2.1) so pickers update. Tests for disable, download, favorite-one-per-tab.

#### 5.3 -- Testing and Stabilization

**Prompt**:
> ModelsSettings tests against catalog fixtures. CI. `/generate-session-history` for Phase 5.

### Phase 5 Exit Checklist

- [x] Category tabs + installer copy
- [x] Download / Downloaded / hardware disable
- [x] Favorite per tab
- [x] Tests passing
- [x] Session history written

---

## Phase 6: Hub Update That Finishes

**Goal**: Update now can complete a real Nexus-Hub pull. Auto-update to latest release defaults ON.
**Prerequisites**: None besides Phase 1 shell (Settings remains reachable).
**Stability Gate**: A test double that sleeps >15s still succeeds for `skills.sync`. Default auto-update is true for new profiles. Timeout failures mention Hub fetch, not "sidecar response timeout", unless the sidecar is actually dead.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Rust IPC timeout plus git clone. Easy to "fix" by raising all timeouts (forbidden). Need a per-method map or job protocol.

### Sub-tasks

#### 6.1 -- Long-running `skills.sync` without a 15s global raise

**Objective**: Screenshot 9 cannot happen on a healthy sidecar.

**Failure modes**: Sidecar really dead: keep the backend-down banner. Network failure: typed error with retry. Partial clone: do not swap catalog (existing atomic swap). Concurrent Update now: ignore or disable the button while a job is active.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.4 Phase 6.1. `desktop/src-tauri/src/sidecar.rs` uses `Duration::from_secs(15)` for every RPC. Do not raise that globally. Options (pick one and test it): (a) per-method timeout map with `skills.sync` on the order of minutes, (b) return a job id immediately and poll `skills.sync.status` until apply/fail. Prefer (b) if a clone can exceed any UI timeout. Surface progress on the Skills page. Map failures to Hub-specific copy, not "sidecar response timeout", when the sidecar is still answering pings. Tests: fake sync >15s succeeds; killed sidecar still shows backend-down.

#### 6.2 -- Auto-update default ON and copy change

**Objective**: Match the operator's label and default.

**Failure modes**: Existing users who stored `false` stay false (do not override an explicit off). Missing key: default true. Auto-update still idle-gated so it cannot fight a user-started generate.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.4 Phase 6.2. In `SkillsSettings.tsx` change the switch label to "Auto-update to latest Nexus-Hub release". Sidecar `skills.autoSync.get` currently treats only `stored === true` as on, so missing key is off -- invert the default for missing keys. Keep weekly idle cadence unless a cheaper "on app start if idle" is already there. Tests for default-on when unset, and label.

#### 6.3 -- Testing and Stabilization

**Prompt**:
> Sidecar + desktop Skills tests, Rust timeout tests if you add a map. CI. `/generate-session-history` for Phase 6.

### Phase 6 Exit Checklist

- [x] Long Hub update works
- [x] Auto-update default ON, new copy
- [x] Tests passing
- [x] Session history written

---

## Phase 7: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, known gaps reconciled, CI covering the new contracts.
**Prerequisites**: Phases 1-6.
**Stability Gate**: No new duplicate picker stacks; known-gaps updated; CI green; unicode-safe plan/docs.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide reconciliation and release gating.

### Sub-tasks

#### 7.1 -- Architecture refactor

**Objective**: One installed-catalog module; delete lying fallbacks.

**Prompt**:
> Refactor toward `useTaskModels` + snapshot as the only picker feed. Delete dead `image.generate` UI hooks if any remain. Remove unused badge props. Propose-then-apply file moves; repair imports. Do not drag DF-14 composer merge into this patch unless it is already identical.

#### 7.2 -- Known-gaps reconciliation

**Objective**: Update `docs/archive/v2/v2.2/known-gaps.md` for v2.2.4.

**Prompt**:
> Close rows this cycle actually closed (default Chatbot-always, lying pickers, Enable tools, empty media rectangle, Hub 15s, DF-17 at least for models tab). Keep DF-1, DF-2, DF-4, DF-14, DF-16, DF-18-22 as open unless proven. Add new DFs for anything still unproven (packaged Hub update, live GPU img2img). Follow evidence-and-support-tiers.md: not_observed != absent.

#### 7.3 -- CI/CD create/update/optimize

**Prompt**:
> Ensure desktop vitest, sidecar tests, installer tests (if snapshot writer landed there), and docs-layout run on the touched paths. Path filters, concurrency cancel-in-progress, caching. Do not add a live Hub clone to PR CI; use fixtures.

#### 7.4 -- Cross-installer parity

**Prompt**:
> This repo ships a Windows-first installer plus macOS/Linux scripts. If Phase 2.1 writes a snapshot, write it from the shared installer complete path on every OS that runs that path. If Unix installers skip that step, record a known-gap rather than claiming parity. Silent no-op only if no installer files changed.

#### 7.5 -- Testing and Stabilization

**Prompt**:
> Full desktop vitest, sidecar tests, lint, `tsc -b`, docs-layout. Iterate until clean. `/generate-session-history` for Phase 7. Do not bump package.json.

### Phase 7 Exit Checklist

- [x] Refactor done
- [x] known-gaps.md updated
- [x] CI covers new contracts
- [x] Cross-installer snapshot noted
- [x] Session history written

---

## Complexity Tracking

> Fill ONLY if Constitution Check has violations that must be justified

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| | | |

---

## Out of scope

- Re-running v2.2.0-v2.2.3 as greenfield.
- Loading models on tab peek.
- Streaming chat tokens (DF-21).
- Native file dialogs (DF-16).
- Node SEA (DF-1).
- Live-GPU soaks (DF-4) as a CI gate.
- Light theme.
- Bumping package.json.
- Rebuilding NexusSetup.exe unless Phase 2.1 truly cannot write the snapshot without it.
- Changing Hub's upstream repo or turning off the injection scanner.

## Residual risks (label, do not hide)

- Live SANA/LTX generate on a real GPU is still DF-4. Phase 4 makes empty completes honest; it does not prove a pretty picture.
- Packaged Explorer launch (DF-2) is still unproven; Hub update in a packaged app depends on git/tar and network.
- Reconstructing a missing snapshot from leftover weights may still show a model the operator does not remember installing from an *older* install until they remove it in Settings. Document that in Settings copy.
- Chat replies remain batched (DF-21).

## Copyable implementation prompt

```
Implement Nexus AI Studio v2.2.4 from docs/archive/v2/v2.2/plans/v2.2.4-chatbot-first-and-runtime-honesty.md. Follow AGENTS.md. Do not re-run v2.2.0-v2.2.3. Do not bump package.json (root 2.1.0, desktop 1.5.0) unless asked.

PRODUCT LOCKS
- Cold start always /chatbot. Do not restore last module.
- Tab labels: Chatbot, Agents, Images, Videos.
- Collapse: edge pill, no gap above first tab.
- Pickers: this-install + in-app downloads only, installer order, favorite || recommended || first. Select value === send id.
- Agents: no pink "Agentic AI Coding", no harness/tool-calling badges. Tools always on. No Enable tools checkbox.
- User message appends before not-installed returns. Hi must be visible.
- Media compact + lightbox. Empty complete = error text, never a grey rectangle.
- Settings > Models = installer category tabs. Download vs Downloaded. Hardware disable. One favorite per tab.
- skills.sync must outlive 15s. Auto-update to latest Nexus-Hub release default ON.
- Peeking still never loads weights.

Start at Phase 1.1. Stop at the phase stability gate. Do not skip to Phase 7 until 1-6 are done.
```
