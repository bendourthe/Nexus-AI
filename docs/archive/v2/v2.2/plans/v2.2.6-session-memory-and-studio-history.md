# Plan -- Durable Session Memory and Studio History

**Project**: Nexus AI Studio
**Version**: v2.2.6
**Slug**: session-memory-and-studio-history
**Filename**: v2.2.6-session-memory-and-studio-history.md
**Plan Type**: Feature / Enhancement (patch follow-on)
**Created**: 2026-08-23
**Predecessor**: [v2.2.5-first-successful-generation.md](v2.2.5-first-successful-generation.md)
**Seeded from**: operator product bar after v2.2.5 (within-session memory, history panes on Images/Videos matching Chatbot, quit/reopen restore). Not a comparison report.
**Goal**: Each Chatbot, Agents, Images, and Videos session keeps its own conversation (and last generated media) so a follow-up prompt uses that context, and reopening the same session after quitting the app continues as if it had not been interrupted. Images and Videos get a history pane with the same organize/rename/delete contract as Chatbot.

## Overview

Chatbot already has a folder/chat explorer and durable transcripts when the sidecar store is up. Image Studio and Video Lab are chat-shaped composers that keep messages in React state, so a quit, a remount, or even a follow-up without re-attaching the last PNG starts from empty context. Agents can list and resume session ids, but the visible turn list is not proven to reload after process restart.

This patch adds a shared studio-session contract (named sessions, folders, turns with createdAt, lastOutputRef on disk), wires FolderTree onto Images and Videos, makes an empty-attachment follow-up use the last usable output, and hydrates Agents and Chatbot after reopen. Delivery is the existing Tauri desktop app plus Node sidecar; no new installer surface. Success is the operator field checklist at the bottom: four tabs remember a session, Images/Videos organize like Chatbot, and a follow-up without re-attach edits the last picture (or fails honestly).

Context meters, date separators, bubble clocks, and per-bubble token counts are out of scope here (v2.2.7). Persist createdAt so that cycle can render clocks.

## Constitution Check

No constitution file found at `docs/archive/v2/v2.2/constitution.md` (or `docs/constitution.md`). Skipping the MUST/SHOULD gate. Recommend `/constitution` if this project wants a durable principle file. This is informational, not blocking.

Grounding that was read instead:

- `AGENTS.md` (local-first, four pillars, no invented capability)
- `docs/archive/v2/v2.2/known-gaps.md` (v2.2.4 closed BG-33 through BG-42 at unit evidence; DF-2 / DF-4 still open; v2.2.5 field generate still in flight)
- Chatbot already hydrates `chat.explorer.listMessages` / `appendMessage` (`desktop/src/modules/chat/ChatPage.tsx`)
- Image Studio and Video Lab keep `messages` in React state only (`ImageStudioPage.tsx`, `VideoLabPage.tsx`); next txt2img/img2img uses composer attachments, not the last assistant PNG unless the user re-attaches
- Agents have `coding.session.list` / `resume` and a Sessions panel, but `CodingPage` turns live in `useState` and `turnsToMessages` does not stamp timestamps

No `STRATEGY.md` / `docs/STRATEGY.md` and no `docs/solutions/` store.

This plan serves the same local-first persona as v2.2.5: a laptop user who expects each tab to behave like a sessioned studio, not a stateless form.

## Current model map

**Model map status**: fresh as of 2026-08-23; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|------|-----------|--------|--------|--------|
| frontier | claude-fable-5 | gpt-5.6-sol | gemini-3.7-flash | cursor-grok-4.6 |
| strong | claude-opus-5 | gpt-5.6-terra | gemini-3.7-flash | cursor-grok-4.5 |
| standard | claude-sonnet-5 | gpt-5.6-terra | gemini-3.7-flash | composer-2.5 |
| fast | claude-haiku-4-5 | gpt-5.6-luna | gemini-3.7-flash | composer-2.5-fast |

A distinct Gemini 3.7 Pro API slug was not confirmed from official docs in this session, so Flash fills every Google cell rather than inventing a Pro id.

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/about-claude/models/overview
- OpenAI: https://developers.openai.com/api/docs/models and https://developers.openai.com/api/docs/guides/latest-model
- Google: https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/ and https://docs.cloud.google.com/gemini-enterprise-agent-platform/models/gemini/3-7-flash
- Cursor: https://cursor.com/docs/models-and-pricing and https://cursor.com/docs/models/grok-4-6

## Review verdict (this session)

**v2.2.5 does not include this product bar.** Chatbot already has a folder/chat explorer and durable transcripts when the sidecar store is up. Images and Videos are chat-shaped composers with no history pane and no quit/reopen restore. Agents can list/resume session ids, but the visible turn list is not proven to reload after app restart. Last-generated-image-as-implicit-img2img is not wired: a new prompt with empty attachments is txt2img.

Do not fold this work into v2.2.5. Finish v2.2.5 first (generate + catalog + explorer chrome + Hub). This plan is v2.2.6.

### Project health

| Surface | Status | Detail |
|---|---|---|
| Git version control | OK | Repo present with history on `develop` |
| Version number | OK | Plan version v2.2.6; root `package.json` stays 2.1.0 until release handoff; `desktop/package.json` 1.5.0 drift is pre-existing. Do not bump either. |
| Branch model | OK | `develop` + `main` in use |
| Baseline docs | OK | README, CHANGELOG, known-gaps, per-version plans |
| Per-version docs tree | OK | `docs/archive/v2/v2.2/plans/` present |

### Locked product decisions

- **Four sessioned tabs**: Chatbot, Agents, Images, Videos. Each has a left history pane (folders + named sessions) matching Chatbot organize/rename/delete.
- **Within-session memory**: the next prompt in a session sees prior turns. For Images, an empty-attachment follow-up after a successful generate uses the last usable assistant PNG as `sourceImage` (img2img / inpaint as intent requires). For Videos, a follow-up can condition on the last playable clip / last frame when the protocol already has `continueFrom` / `sourceImage`.
- **Quit/reopen**: selecting the same session after process restart reloads transcript, attachments (or persisted media paths), and the last-output pointer. In-memory React state alone is not enough.
- **Media on disk**: do not keep multi-megabyte PNG/MP4 only in SQLite blobs if a generations index path already exists; store job/output paths plus a thumbnail, and fail closed if the file is gone (honest "output missing", not a grey rectangle).
- **Do not re-run** v2.2.0-v2.2.5 as greenfield. Keep v2.2.4/v2.2.5 locks (cold start Chatbot, tab labels, peeking never loads weights, tools always on).
- **Shipping version**: do not bump root `package.json` (2.1.0) or `desktop/package.json` (1.5.0).
- **Do not disable** the Hub injection scanner.
- Context-window progress bars, date separators, bubble clocks, and per-bubble token counts are **v2.2.7**, not this plan. Persist `createdAt` here so v2.2.7 can render clocks.

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|-------|-------|---------|------------------------|--------------------------|
| 1 | Shared session explorer contract | One store + FolderTree reuse for Images/Videos | frontier | max |
| 2 | Images: history + last-output memory | Named image sessions, implicit img2img, quit/reopen | frontier | max |
| 3 | Videos: history + last-clip memory | Named video sessions, continuation, quit/reopen | frontier | max |
| 4 | Agents: transcript restore | Resume reloads visible turns after restart | frontier | high |
| 5 | Chatbot closeout | Prove Chatbot already meets the bar; fill remaining holes | strong | high |
| 6 | Architecture, known-gaps, CI | Layout clean, gaps honest, CI covers persistence | frontier | max |

---

## Task inventory (T###)

- T001 [P] [core/generations or sidecar chat] Shared `StudioSession` record: pillar, title, folderId, modelId, createdAt, updatedAt, lastOutputRef.
- T002 [P] [desktop sidecar] IPC: list/create/rename/delete/move sessions + append/list turns for `image` and `video` pillars (mirror `chat.explorer.*`, do not import Chat types into Image UI).
- T003 [P] [desktop/src/shared] Reuse FolderTree (or a thin wrapper) with pillar-scoped client; keep Chatbot's existing chat.explorer path working.
- T004 [P] [ImageStudioPage] History pane; persist user/assistant turns; last usable PNG becomes default img2img source when attachments are empty.
- T005 [P] [ImageStudioPage] Quit/reopen: listMessages hydrates; missing files are error text, not empty complete.
- T006 [P] [VideoLabPage] Same history + persist + last clip / last frame for follow-up; honor existing `continueFrom` when present.
- T007 [P] [VideoLabPage] Quit/reopen hydrate.
- T008 [P] [CodingPage] Resume + list hydrates `turns` into MessageList after restart; Sessions tab rename/delete parity with Chatbot where missing.
- T009 [P] [ChatPage] Field-proof quit/reopen; if hydration already works, add regression tests only.
- T010 [P] [tests] Fixture IPC: create session, generate-or-stub turn, restart client, transcript + lastOutputRef present.
- T011 [P] [docs/archive/v2/v2.2/known-gaps.md] Open BG rows for any unproven packaged persist; close only with evidence.
- T012 [P] [CI] Persistence tests without live GPU.

---

## Phase 1: Shared Session Explorer Contract

**Goal**: Images and Videos can own named sessions in a FolderTree without forking Chatbot's SQLite chat schema into a second incompatible tree.
**Prerequisites**: v2.2.5 complete on `develop` (alias table, generate honesty).
**Stability Gate**: Unit tests create an `image` session, append a user turn + assistant media ref, list it back after a new store instance. Chatbot `chat.explorer.tree` tests still pass.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Cross-pillar persistence, schema, and FolderTree reuse. High blast radius if Chatbot explorer regresses.

### Sub-tasks

#### 1.1 -- Studio session records (T001, T002)

**Objective**: Durable session + turn storage for image and video pillars.

**Failure modes**: Missing pillar: reject create. Corrupt JSON/SQLite row: skip that session, log, do not crash the tree. Concurrent rename+delete: delete wins, rename returns not-found.

**Build class**: load-bearing.

**Prompt**:
> Implement Nexus AI Studio v2.2.6 Phase 1.1 from docs/archive/v2/v2.2/plans/v2.2.6-session-memory-and-studio-history.md. Follow AGENTS.md. Do not bump package.json. Do not re-run v2.2.5.
>
> Add a sidecar-backed store for Images/Videos sessions (folders, named sessions, turns with createdAt, lastOutputRef as a generations path or PNG/MP4 path, not a giant blob). Mirror the Chat explorer IPC shape (`tree`, `createChat`, `renameChat`, `deleteChat`, `appendMessage`, `listMessages`) under `studio.session.*` or `image.explorer.*` / `video.explorer.*`. Do not make `core/**` import `modules/**`. Chatbot keeps `chat.explorer.*`.

#### 1.2 -- Shared FolderTree wiring (T003)

**Objective**: Images and Videos render the same organize/rename/delete chrome Chatbot uses (including v2.2.5 icons/pill if present).

**Failure modes**: Sidecar down: empty tree + SidecarDownBanner, no fake sessions. Rename empty string: reject.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.6 Phase 1.2. Extract only what FolderTree needs into a pillar-agnostic client interface if Chat-specific types block reuse. ImageStudioPage and VideoLabPage get a left aside. Do not change Chatbot behavior except through the shared component.

#### 1.3 -- Testing and Stabilization

**Prompt**:
> Tests for create/rename/delete/list/append across two store instances (simulates restart). Chat explorer regressions. CI path filters. `/generate-session-history` for Phase 1.

### Phase 1 Exit Checklist

- [ ] Image/video session IPC round-trips
- [ ] Chat explorer unchanged
- [ ] Tests passing
- [ ] Session history written

---

## Phase 2: Images History and Last-Output Memory

**Goal**: Image Studio matches Chatbot: named sessions, within-session follow-up on the last picture, quit/reopen restore.
**Prerequisites**: Phase 1.
**Stability Gate**: Test: session A generates (stub usable PNG path), new prompt with no attachment calls img2img with that PNG; new store instance lists both turns.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Implicit img2img plus media persistence. Easy to fake in the UI and fail on disk.

### Sub-tasks

#### 2.1 -- Persist image turns and last output (T004, T005)

**Objective**: Every user prompt and assistant PNG (or typed error) is stored on the active session.

**Failure modes**: Generate error: persist the error text, do not update lastOutputRef. File deleted: hydrate shows "output missing on disk". Sidecar down: do not claim saved.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.6 Phase 2.1. ImageStudioPage: bind FolderTree to the Phase 1 client. On send, append user turn first (v2.2.4 honesty). On usable complete, persist media ref and set lastOutputRef. On hydrate, rebuild MessageList including media src from path/data. Keep `isUsableImageBase64` for inline display.

#### 2.2 -- Implicit last-image edit (T004)

**Objective**: Attach image, modify, then a new prompt without re-attaching edits the last generated image.

**Failure modes**: No lastOutputRef: txt2img as today. lastOutputRef unreadable: typed error, do not silent txt2img. User attached a new image: attachments win.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.6 Phase 2.2. In `inferImageIntent` / `handleSubmit`, if attachments are empty and lastOutputRef is a usable image, treat as img2img (or inpaint if a mask is present). Tests for attach-modify-follow-up and for no-last-output txt2img.

#### 2.3 -- Testing and Stabilization

**Prompt**:
> ImageStudioPage tests: history pane, rename, implicit img2img, hydrate after remount. No live GPU in CI. Session history for Phase 2.

### Phase 2 Exit Checklist

- [ ] History pane works
- [ ] Follow-up without re-attach uses last PNG
- [ ] Hydrate after remount
- [ ] Tests passing
- [ ] Session history written

---

## Phase 3: Videos History and Last-Clip Memory

**Goal**: Video Lab matches Images: named sessions, follow-up on last clip, quit/reopen.
**Prerequisites**: Phase 2 (shared patterns).
**Stability Gate**: Same class of tests as Phase 2 with `mp4Path` / `continueFrom`.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Same persistence class as Images plus video continuation protocol.

### Sub-tasks

#### 3.1 -- Persist video turns (T006, T007)

**Objective**: History pane + hydrate + last playable path.

**Failure modes**: Missing mp4: error text, not a complete with empty path. Continuation fields absent: new text2video, do not invent continueFrom.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.6 Phase 3.1. Mirror Phase 2 for VideoLabPage. Persist `outputPath`. Follow-up without a new photo may set `continueFrom` or `sourceImage` only when the existing video protocol already supports it. Tests + session history.

#### 3.2 -- Testing and Stabilization

**Prompt**:
> VideoLabPage history, implicit continuation, hydrate. CI fixtures only. Session history for Phase 3.

### Phase 3 Exit Checklist

- [ ] Video history pane
- [ ] Follow-up contract documented and tested
- [ ] Hydrate after remount
- [ ] Tests passing
- [ ] Session history written

---

## Phase 4: Agents Transcript Restore

**Goal**: Opening a previous Agents session shows the same turns after quit/reopen.
**Prerequisites**: Phase 1 (shared chrome optional; Agents already has Sessions panel).
**Stability Gate**: Test: start session, send a turn (stub runner), new CodingPage instance + resume, MessageList contains user + assistant text.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: Existing resume IPC vs empty `useState` turns. Production session data.

### Sub-tasks

#### 4.1 -- Hydrate turns on resume (T008)

**Objective**: `coding.session.resume` (or list-messages equivalent) fills `turns` / MessageList.

**Failure modes**: Unknown session id: typed error, empty list. Partial trace: show what exists, do not invent assistant text.

**Build class**: load-bearing.

**Prompt**:
> Implement v2.2.6 Phase 4.1. CodingPage resume must restore visible transcript, not only sessionId. Add rename/delete in the Sessions list if Chatbot has them and Agents do not. Do not load weights on tab peek. Tests for resume hydration. Session history.

#### 4.2 -- Testing and Stabilization

**Prompt**:
> CodingPage + sidecar session tests. CI. Session history for Phase 4.

### Phase 4 Exit Checklist

- [ ] Resume restores transcript
- [ ] Tests passing
- [ ] Session history written

---

## Phase 5: Chatbot Closeout

**Goal**: Chatbot already meets the session bar; prove it and fill holes (attachments on hydrate, model id fold).
**Prerequisites**: Phase 4.
**Stability Gate**: Test: append user+assistant, new ChatPage client, same chat id, both messages visible.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Mostly regression on an existing store; still cross-file ChatPage + explorer.

### Sub-tasks

#### 5.1 -- Prove and patch Chatbot (T009)

**Objective**: Quit/reopen Chatbot continues the conversation. Document any remaining DF.

**Failure modes**: Store missing: in-memory fallback must not claim durable save.

**Build class**: load-bearing where patched.

**Prompt**:
> Implement v2.2.6 Phase 5.1. Add/extend ChatPage tests for remount hydration and within-session follow-up. If attachments do not persist, persist them or record a known-gap. Keep Phase 1 alias fold. Session history.

#### 5.2 -- Testing and Stabilization

**Prompt**:
> Chat explorer + ChatPage tests. Session history for Phase 5.

### Phase 5 Exit Checklist

- [ ] Chatbot remount test green
- [ ] Gaps recorded if any
- [ ] Session history written

---

## Phase 6: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, and its CI/CD complete and optimized.
**Prerequisites**: All prior phases.
**Stability Gate**: Layout clean; known-gaps updated; CI covers persistence; tests pass.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, known-gap reconciliation, and release gating.

### Sub-tasks

#### 6.1 -- Architecture refactor

**Objective**: Refactor toward a well-organized layout.

**Prompt**:
> Identify deprecated/obsolete files from the shared explorer extract, then refactor toward a clean layout (propose-then-apply). Repair every reference. Do not move Chatbot off `chat.explorer.*` without a migration.

#### 6.2 -- Known-gaps reconciliation (T011)

**Objective**: Reconcile v2.2.6 gaps.

**Prompt**:
> Update docs/archive/v2/v2.2/known-gaps.md. Close session-memory rows only with test or operator evidence. Keep DF-2 / DF-4 unless newly proven. Packaged quit/reopen is DF until a packaged soak.

#### 6.3 -- CI/CD create/update/optimize (T012)

**Objective**: CI covers persistence tests.

**Prompt**:
> Cover studio session IPC, implicit img2img, resume hydration. No live GPU in PR CI. Path filters and concurrency cancel-in-progress.

#### 6.4 -- Cross-installer parity

**Objective**: Prevent installer drift.

**Prompt**:
> This plan does not change installers unless the session DB path needs documenting in runtime.json. If no installer files changed, record a silent no-op.

#### 6.5 -- Testing and Stabilization

**Prompt**:
> Full test suite. Session history for Phase 6. Rebuild the Windows installer for operator verification (unsigned is fine). Stop before `/update release` unless asked. Push `develop` after the Phase 6 commit when the operator asked to push.

### Phase 6 Exit Checklist

- [ ] Refactor done
- [ ] known-gaps.md updated
- [ ] CI covers new contracts
- [ ] Session history written

---

## Complexity Tracking

> Fill ONLY if Constitution Check has violations that must be justified

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| | | |

---

## Out of scope

- Re-running v2.2.0-v2.2.5 as greenfield.
- Context usage progress bars, date separators, bubble clocks, per-bubble token counts (v2.2.7).
- Loading models on tab peek.
- Bumping package.json.
- Disabling the Hub injection scanner.
- Live GPU in PR CI.

## Residual risks

- Large PNG/MP4 in SQLite will blow disk; path + generations index is required.
- Packaged Explorer persist (DF-2 class) stays unproven until a packaged soak.
- Video continuation quality is protocol-limited; do not claim infinite extend if `continueFrom` is prototype.

## Operator field checklist (required before calling v2.2.6 done)

1. Chatbot: two turns, quit app, reopen, same chat continues.
2. Images: history pane rename; generate; follow-up prompt without re-attach edits the last image (or honest error).
3. Images: quit/reopen same session shows transcript + last picture (or missing-file copy).
4. Videos: same class as Images when weights exist; otherwise record skip, do not claim video memory works.
5. Agents: send a turn, quit/reopen, resume session, transcript visible.

## Copyable implementation prompt

```
Implement Nexus AI Studio v2.2.6 from docs/archive/v2/v2.2/plans/v2.2.6-session-memory-and-studio-history.md. Follow AGENTS.md. Do not start until v2.2.5 is complete on develop. Do not re-run v2.2.0-v2.2.5 as greenfield. Do not bump package.json (root 2.1.0, desktop 1.5.0). Do not disable the Hub injection scanner. Do not run /update release unless asked. Context meters, date separators, bubble clocks, and token counts are v2.2.7; persist createdAt only.

PRODUCT LOCKS
- Cold start Chatbot. Peeking a tab never loads weights.
- Tab labels: Chatbot, Agents, Images, Videos.
- Tools always on. User bubble before residency errors.

HOW TO WORK
- /implement phase 1, then commit, then phase 2, commit, then 3, commit, then 4, commit, then 5, commit, then 6, commit, then push develop.
- After each phase: run that phase's tests, fix failures, write session history under docs/archive/v2/v2.2/development/history/.
- After Phase 6: address remaining known-gaps, rebuild the Windows installer (unsigned OK) for testing before release.
- ASCII commit messages. No Co-Authored-By.

Start at Phase 1.1. Stop at the phase stability gate. Phase 2 and Phase 3 are not done until implicit last-output follow-up and remount hydrate are tested.
```
