# Plan - Governed Autonomy and the Multimodal Studio (v2.0 Convergence Release)

**Project**: Nexus AI Studio
**Version**: v2.0.0
**Slug**: governed-autonomy-multimodal
**Filename**: v2.0.0-adoption-governed-autonomy-multimodal.md
**Plan Type**: Feature / Enhancement (adoption, multi-source; convergence release)
**Created**: 2026-08-05
**Generated**: 2026-08-05
**Seeded from**:
- [../comparisons/v2.0.0-comparison-qm.md](../comparisons/v2.0.0-comparison-qm.md) (QM -- scopes, posture dial, hard denials, provenance screening)
- [../comparisons/v2.0.1-comparison-hermes-agent.md](../comparisons/v2.0.1-comparison-hermes-agent.md) (Hermes Agent -- harness reliability, catalog models, DNS pinning)
- [../comparisons/v2.0.2-comparison-atomic-agent.md](../comparisons/v2.0.2-comparison-atomic-agent.md) (Atomic Agent -- browser tools, loop veto, fs tools, router)
- [../comparisons/v2.0.3-comparison-airi.md](../comparisons/v2.0.3-comparison-airi.md) (Airi -- local voice loop, autonomy guards, code-as-action, avatar)
- [../comparisons/v2.0.4-comparison-longcat-video.md](../comparisons/v2.0.4-comparison-longcat-video.md) (LongCat-Video -- avatar video tier, continuation, quantized weights)
- [../comparisons/v2.0.5-comparison-inkling-small.md](../comparisons/v2.0.5-comparison-inkling-small.md) (Inkling-Small -- modality metadata, vision/audio chat, patient-tier entry)
- [../comparisons/v2.0.6-comparison-kimi-k3-in-c.md](../comparisons/v2.0.6-comparison-kimi-k3-in-c.md) (kimi-k3-in-c -- patient-tier calibration, presets, determinism)
**Goal**: Ship v2.0.0 as the CONVERGENCE release of the v1.18-v2.0 plan family: this plan keeps the deep product wave -- make the Chat pillar multimodal (vision routing, audio input, a fully local voice loop), give the coding agent a gated local browser tool surface, and add long-form continuation plus an audio-driven avatar tier to the Video Lab, with a bounded stretch phase and a closing refactor/known-gaps/CI phase -- while the near-term, well-bounded themes it originally carried (skill-native wins + agent-loop/guardrail hardening; catalog/model expansion + patient-tier calibration) now ship earlier as the v1.19.1 and v1.19.2 subplans. The v2.0.0 release cut is gated on completion of the v1.18.0 plan, the v1.19.0/.1/.2 subplans, and this plan's own phases -- all local-first, reverse-engineer-first, with every hosted or as-a-service surface from all seven sources rejected wholesale.

## Plan Family

v2.0.0 is the convergence release: it ships only when every plan below is complete (gated in Phase 6).

- [../../../v1/v1.18/plans/v1.18.0-adoption-agent-harness-and-governance.md](../../../v1/v1.18/plans/v1.18.0-adoption-agent-harness-and-governance.md) -- harness activation, catalog/registry governance, ask inbox + scheduler, ACP, OS exec sandbox.
- [../../../v1/v1.19/plans/v1.19.0-adoption-liquid-lfm-agentic.md](../../../v1/v1.19/plans/v1.19.0-adoption-liquid-lfm-agentic.md) -- LFM2.5-2.6B low-VRAM agentic catalog entry, license label, harness profile, 8B-A1B bake-off.
- [../../../v1/v1.19/plans/v1.19.1-adoption-agent-loop-and-guardrail-hardening.md](../../../v1/v1.19/plans/v1.19.1-adoption-agent-loop-and-guardrail-hardening.md) -- skill-native wave + agent-loop and guardrail hardening (this plan's original Phases 1-2).
- [../../../v1/v1.19/plans/v1.19.2-adoption-catalog-and-model-expansion.md](../../../v1/v1.19/plans/v1.19.2-adoption-catalog-and-model-expansion.md) -- modality/audio catalog schema, quantized-weights puller, Hermes family, patient-tier calibration (this plan's original Phase 3).
- v2.0.0-adoption-governed-autonomy-multimodal.md (this plan) -- multimodal chat + voice loop, browser tool surface, Video Lab continuation + avatar tier, stretch, refactor, and the v2.0.0 convergence/release gate.

## Overview

This milestone operationalizes the adoption plans (Section 7) and implementation sequences (Section 8) of the seven v2.0 comparison reports. The seven sources converge on four themes that define the v2.0 wave:

1. **Agent governance and autonomy hardening.** QM contributes the named security-posture dial, command-level hard denials that survive every posture, provenance-labelled content screening, and per-project scoping concepts. Airi's Minecraft agent, Atomic Agent's no-progress veto, and Hermes Herald's tool self-recovery all describe the same class of loop circuit breakers from three independent angles. **This theme now ships as the v1.19.1 subplan** (together with the skill-native wave); this plan's browser and stretch phases consume its outputs.
2. **Catalog, model, and tier additions.** Hermes-family local models, the merged modality/audio-conditioning schema, the quantized-weights puller, and the calibrated patient tier. **This theme now ships as the v1.19.2 subplan**; this plan's multimodal chat and Video Lab phases consume its schema and puller.
3. **Multimodal chat and the local voice loop.** Inkling-Small motivates vision-chat routing and an audio-input bridge; Airi proves the fully local STT + TTS + VAD voice loop is buildable on consumer hardware. The audio work is sequenced as one pipeline: attachment plus transcribe-then-chat first, then the real-time voice loop on top. Kept here (Phase 1).
4. **New tool and generation surfaces.** Atomic Agent's Playwright-driven `browser_*` tool family is the headline coding-agent build, gated behind the v1.19.1 hardening. LongCat-Video contributes video continuation for minutes-long output and the `audio2video` avatar tier behind `diffusion-pro`. Kept here (Phases 2-3).

Per the MCP Registry Policy's reverse-engineer-first ordering, the skill-native items landed first (v1.19.1 Phase 1), then the hardening and catalog builds (v1.19.1 Phase 2, v1.19.2 Phase 1), then this plan's product builds grouped by theme and dependency (Phases 1-3), then a clearly deferrable stretch phase (Phase 4) so core phases stay bounded, a closing architecture/known-gaps/CI phase (Phase 5), and the convergence/release gate (Phase 6). Every hosted, cloud, gateway, connector, or as-a-service surface named by any of the seven sources is rejected in the consolidated "Out of Scope / Rejected" section, which binds the whole plan family. No external code is vendored anywhere in this plan except the explicitly gated LongCat Avatar pipeline integration (Phase 3), which carries its own byte-level scan precondition per the comparison's Section 9.

Success looks like: the agent loop cannot run away and the user has one named posture dial (delivered by v1.19.1); the catalog gains the Hermes family, modality metadata, quantized-weight delivery, and an honestly-labelled patient tier (delivered by v1.19.2); Chat accepts images and audio against capable local models and can hold a fully local voice conversation; the coding agent can drive a local browser under DANGEROUS-tier gating with sanitized ARIA snapshots; the Video Lab chains clips into minutes-long output and produces lip-synced avatar video from a photo and an audio track, entirely on-device; and v2.0.0 cuts only when the whole v1.18-v2.0 plan family is verifiably complete.

## Cross-Source Dedup Decisions

Every merge/dedup decision made while consolidating the seven Section 7/8 plans, with the winning shape. This section remains the single source of truth for the plan family; each decision now names the plan file where the merged item lives.

- **D1 -- Loop hardening merged three-ways.** *Lives in: v1.19.1, Phase 2.2.* Airi A2 (bounded control-action queue, no-action budget, error-burst guard), Atomic C4 (no-progress veto after N identical calls), and the iteration-ceiling revisit inside Hermes A2 all target the same seam (`src/tools/AgentLoop.ts` auto mode) and describe overlapping circuit breakers. Merged into one work package delivering a single `LoopGuards` layer with four named guards: identical-call veto, no-action budget, error-burst guard, bounded action queue, plus a revisited iteration ceiling. The existing `modules/coding/guardrails/LoopDetector.ts` is the integration point, not a parallel implementation.
- **D2 -- Tool self-recovery kept separate from loop hardening.** *Lives in: v1.19.1, Phase 2.3.* Hermes A2's remaining behaviors (truncation spill-to-file, already-applied-patch detection, near-miss search probes) recover individual tool calls rather than govern the loop; they stay a distinct sub-task so D1 stays strictly tightening while D2 is strictly resilience.
- **D3 -- Posture dial vs existing tiers.** *Lives in: v1.19.1, Phases 2.1 and 2.5.* QM A2's Strict/Auto/Dangerous dial is adopted as named presets composed from the existing `PermissionTiers` + `ConfirmationGate` + auto-mode verification gate -- not a new permission system. QM A3's command-level hard denials land first because the dial's invariant ("denials survive every posture") requires them to exist. No no-floor mode ships; the `PermissionTiers.ts` clamp stays authoritative in every posture.
- **D4 -- Provenance screening feeds the browser surface.** *Built in: v1.19.1, Phase 2.6; consumed by: this plan, Phase 2.* QM A5 (provenance-labelled tool results routed through `PromptInjectionScanner.ts`) and Atomic C1's requirement that ARIA snapshots be sanitized before entering the prompt are the same mechanism at two call sites. Built once in v1.19.1; this plan's browser tools consume it as a hard dependency rather than building their own sanitizer.
- **D5 -- Catalog schema extensions merged.** *Lives in: v1.19.2, task 1.2.* Inkling A1 (`modalities` input-modality field) and LongCat A4 (`audioConditioning` fields in `DiffusionTierVideoDefaults` + catalog video entries) are one schema change wave on `core/registry/catalog.json` / `core/registry/catalog.ts` / `core/config/DiffusionTier.ts`, so the schema is touched once and every downstream consumer (this plan's chat attachment gating, Phase 1; avatar tier, Phase 3) reads the same fields.
- **D6 -- Quantized-weights tier built once, consumed twice.** *Built in: v1.19.2, task 1.3; consumed by: v1.19.2 task 1.6 and this plan, Phase 3.* LongCat A3 (precision-variant file sets in the installer puller) is the same plumbing Inkling A4's 74.8 GB GGUF entry needs. Both the Avatar-1.5 INT8 weights (this plan, Phase 3.2) and the Inkling-Small patient-tier entry ride it.
- **D7 -- Patient-tier items clustered.** *Lives in: v1.19.2, tasks 1.1, 1.6, and 1.7.* Kimi K2 (throughput calibration copy), K1 (RAM-budget presets), K3 (determinism-across-budget test), and Inkling A4 (patient-tier catalog entry) form one patient-tier calibration cluster: K2's copy lands first, and K1 + K3 + the Inkling entry land together with K2's numbers feeding K1's presets, per the kimi comparison's own sequencing.
- **D8 -- Audio input unified into one pipeline.** *Lives in: this plan, Phase 1 (1.2 then 1.3).* Inkling A3 (audio attachment + transcribe-then-chat via curated faster-whisper) is the STT half of Airi A1 (full local voice loop). Sequenced as one pipeline: attachment + STT bridge first (1.2), then TTS + VAD/push-to-talk real-time loop on top (1.3). No duplicate STT integration.
- **D9 -- Two avatars, deliberately NOT merged.** *Lives in: this plan, Phase 3 (LongCat) and Phase 4.7 (Airi VRM).* Airi A5 (VRM presence layer in Chat) and LongCat A1 (audio-driven talking-head video in Video Lab) are different features that merely share the word "avatar". LongCat's avatar video mode is a core Phase 3 deliverable; Airi's VRM presence is cosmetic P3 and goes to the stretch phase (4.7), keeping its dependency on the voice loop's lip-sync timing.
- **D10 -- Sandbox family related but distinct, both stretch.** *Lives in: this plan, Phase 4 (4.2, 4.3, with 4.1 first).* QM A4 (durable per-project exec sandbox) and Airi A3 (code-as-action sandboxed worker + Query DSL) both extend the v1.12.0 exec-sandbox audit but solve different problems (persistence vs execution model). Kept as separate stretch items with QM A1's `ProjectScope` (4.1) sequenced before the durable sandbox, matching QM's own A1 -> A4 ordering.
- **D11 -- Router complements, never bypasses.** *Lives in: v1.19.2, task 1.5 (Hermes A5 profiles) and this plan, Phase 4.4 (router).* Atomic C3 (fast small-model command router) overlaps in spirit with the existing v1.12.0 `HarnessSelector` per-model tuning and with Hermes A5's harness profiles. Decision: Hermes A5 is a profile-addition to `HarnessSelector.ts` (v1.19.2); the router is a distinct dispatch layer and goes to stretch with the explicit invariant that routed calls never drop a permission tier.
- **D12 -- Skill-native and verify-only dedups bundled.** *Lives in: v1.19.1, task 1.4 (verify-only bundle) and this plan, Phase 4.5 (Atomic C5 code half).* QM's scope-owned-skills and crons items are verify-only dedups (Nexus-Hub `nexus skills sync` and the v1.18 OpenWorker A2 scheduler already cover them); Atomic's lessons-capture loop and playbooks map to the existing `continuous-learning` skill and the Nexus-Hub catalog. All are bundled into one verification sub-task instead of five no-op tasks. Atomic C5's thin memory-kind storage half remains code and goes to stretch (4.5).
- **D13 -- Compression patterns absorb no rivals.** *Lives in: v1.19.1, Phase 2.4.* Hermes A1 (per-model thresholds, micro-compaction, user-tail retention) is the only compression candidate across the seven sources; Atomic's per-turn result compression is already covered by existing `compress_range` / `compress_message` tools and is folded into that task's validation scope rather than tracked separately.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found under `docs/` (checked `docs/archive/v2/v2.0/` and all prior version trees) - skipping check. Recommend running `/constitution` to establish project principles. This is informational, not blocking.

Project-level guardrails that nonetheless apply to this plan (from `AGENTS.md` / `CLAUDE.md` / `README.md`): local-first / zero-outbound (every adoption is a local build; the only network activity is install-time HF weights download over the already-sanctioned, sha256-pinned channel); the MCP Registry Policy reverse-engineer-first decision tree (skill-native > re-full > re-partial > vendor-intrinsic > drop; all rejections recorded below); zero as-a-service (all hosted inference, hosted TTS, gateways, connectors, webhooks, and federation surfaces rejected); the `PermissionTiers.ts` floor clamp stays authoritative in every posture (no "dangerous" no-floor mode ever ships); untrusted-input discipline (browser page content, model weights, spill files, and transcripts are data, never instructions; `redactSecrets` applies before indexing); single-GPU consumer-hardware ceiling (patient tier stays opt-in and honestly labelled); every changed line traces to a numbered adoption item; per-phase testing and CI updates; ASCII-only commit messages.

## Phases at a Glance

The original Phases 1-3 of this plan moved to the v1.19 subplans and are replaced by pointers:

- **Moved**: Skill-native wins + agent-loop/guardrail hardening (original Phases 1-2) -> [../../../v1/v1.19/plans/v1.19.1-adoption-agent-loop-and-guardrail-hardening.md](../../../v1/v1.19/plans/v1.19.1-adoption-agent-loop-and-guardrail-hardening.md)
- **Moved**: Catalog, model, and tier additions + patient-tier calibration (original Phase 3) -> [../../../v1/v1.19/plans/v1.19.2-adoption-catalog-and-model-expansion.md](../../../v1/v1.19/plans/v1.19.2-adoption-catalog-and-model-expansion.md)

| Phase | Title | Outcome | Rec. model / effort |
|-------|-------|---------|---------------------|
| 1 | Multimodal chat + local voice loop | Vision-chat routing, audio attachment + transcribe-then-chat, full local STT/TTS/VAD voice loop in the Chat pillar | Mid-strong tier, high - claude-sonnet-5 (high): new media pipelines, installer weights, real-time UX |
| 2 | Browser tool surface | Gated `browser_*` tool family (navigate/click/type/aria_snapshot) at DANGEROUS tier over local Playwright, isolated profile, sanitized snapshots | Strong tier, high - claude-opus-4-8 (high): largest new threat surface of the cycle |
| 3 | Video Lab: continuation + avatar tier | Minutes-long clip chaining on the existing pipeline; `audio2video` avatar mode gated to `diffusion-pro` with provenance metadata | Mid-strong tier, high - claude-sonnet-5 (high): sidecar pipeline work + gated upstream integration |
| 4 | Stretch: scoping, sandboxes, router, memory kinds, VRM | Deferrable items that must not inflate core phases; each independently shippable or cleanly slippable to a patch slot | Mid tier, medium - claude-sonnet-5 (medium): bounded, independent builds |
| 5 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Clean layout, v1.15+ gaps reconciled into v2.0, CI green and optimized | Strong tier, high - claude-opus-4-8 (high): repo-wide refactor + reference repair |
| 6 | v2.0.0 Convergence and Release | Release cut gated on completion of the v1.18.0 plan, the v1.19.0/.1/.2 subplans, and this plan's phases; version bump, changelog, cross-version known-gaps sweep, milestone ledger | Mid tier, medium - claude-sonnet-5 (medium): verification + release mechanics over completed work |

---

## Phase 1: Multimodal Chat + Local Voice Loop

**Goal**: The Chat pillar accepts images and audio against capable local models and holds a fully local real-time voice conversation -- STT, TTS, and VAD all on-device, weights via the installer.
**Prerequisites**: v1.19.2 Phase 1 (task 1.2 `modalities` gating; task 1.3 installer catalog path for STT/TTS weights). Internal ordering: 1.1 and 1.2 independent; 1.3 builds on 1.2's STT plumbing (per D8).
**Stability Gate**: Image attachments route to vision-capable local models and are blocked (with a clear affordance) for text-only models; audio attaches, transcribes locally via curated faster-whisper, and enters chat as labelled transcript content through the `redactSecrets` path; the voice loop runs push-to-talk and VAD modes fully offline with a visible capture indicator; no audio or image bytes leave the machine.
**Recommended model**: Mid-strong tier, high effort - claude-sonnet-5 (high). Rationale: new media pipelines and real-time UX over existing chat plumbing; no new outbound surface, moderate security surface (mic capture, transcript indexing). Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 1.1 - Vision-chat routing (Inkling A2)

**Objective**: Existing image attachments reach vision-capable local models.

**Prompt**:
> Extend the Chat pillar so `ChatMessage.attachments` (typed since v1.15.0 in `desktop/src/shared/chat/types.ts`) flow through to vision-capable local models served by Ollama, instead of being confined to the Image Studio / Video Lab surfaces. Gate the image-attachment affordance in the shared chat shell (`desktop/src/shared/chat/MediaComposer.tsx`) on the selected model's `modalities` field from v1.19.2 task 1.2: enabled for image-capable models, disabled with an explanatory tooltip otherwise. Image bytes go to the local loopback endpoint only. Acceptance: an image question answered by a local vision model end-to-end; affordance correctly gated per model; no regression to existing Image Studio attachment flows (`desktop/tests/MediaComposer.test.tsx` and related suites still pass).

---

#### 1.2 - Audio attachment + transcribe-then-chat bridge (Inkling A3 + STT half of Airi A1, per D8)

**Objective**: Audio in, local transcript out, into chat.

**Prompt**:
> Add audio attachment support to `desktop/src/shared/chat/MediaComposer.tsx` (file attach + microphone capture behind explicit user activation with a clear recording indicator), gated on the selected model path via the v1.19.2 `modalities` field (transcribe-then-chat works for any text model, so the gate governs UI copy, not availability). Route captured/attached audio through the already-curated local faster-whisper STT (catalog `task: "audio"`); insert the transcript into the conversation as provenance-labelled content (v1.19.1 Phase 2.6 origin class) and pass it through the `redactSecrets` discipline before memory indexing. Native audio-token reasoning stays out of scope until a fitting local model exists (documented gap). Acceptance: file and mic audio produce a local transcript that the model answers; capture indicator visible whenever the mic is open; transcripts scrubbed before indexing; zero outbound.

---

#### 1.3 - Local real-time voice loop (Airi A1)

**Objective**: A full offline voice conversation in Chat.

**Prompt**:
> Build the real-time voice loop on top of 1.2's STT plumbing in the Chat pillar (`desktop/src/modules/chat/ChatPage.tsx`): local TTS (Kokoro-class open weights) for spoken responses, push-to-talk and VAD-driven capture modes, and interruption handling (stop TTS when the user starts speaking). STT and TTS weights ship as catalog entries pulled through the v1.19.2 task 1.3 installer path with pinned digests -- no ad-hoc downloads (Airi comparison Section 9). Reuse the v1.19.1 Phase 2.6 provenance labelling for transcripts. The whole loop must function with networking disabled. Acceptance: a spoken question receives a spoken answer fully offline; both capture modes work; barge-in stops playback; TTS/STT weights install via the standard catalog/installer flow.

---

#### 1.4 - Testing and Stabilization

**Objective**: Cover routing, gating, capture, and the loop; wire CI.

**Prompt**:
> Add tests: modality-gating unit tests (affordances per model `modalities`); vision routing tests (attachment reaches the local endpoint payload for capable models, blocked otherwise); audio bridge tests (transcription invoked, transcript labelled and scrubbed, indicator state); voice-loop state-machine tests (push-to-talk, VAD, barge-in) with audio hardware mocked in jsdom. Run the desktop suite (`npm run test:shell` or the current equivalent) + lint + typecheck; iterate to green. Update CI for `desktop/src/shared/chat` and `desktop/src/modules/chat`. Do not proceed to Phase 2 until stable. After tests pass, run `/generate-session-history` for Phase 1.

---

### Phase 1 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (gating, routing, bridge, voice loop)
- [x] Fully offline operation verified for the whole voice loop
- [x] Mic capture always behind explicit activation with a visible indicator
- [x] Transcripts provenance-labelled and secret-scrubbed before indexing
- [x] STT/TTS weights delivered via the standard installer/catalog path
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 2

---

## Phase 2: Browser Tool Surface

**Goal**: The coding agent can drive a local browser -- navigate, click, type, read ARIA snapshots -- as a gated DANGEROUS-tier tool family over a locally-installed Playwright dependency and an isolated browser profile, with page content treated as hostile input.
**Prerequisites**: v1.19.1 Phase 2 (loop guards protect long browser runs; provenance screening sanitizes snapshots -- Atomic's own sequencing gates C1 on C4).
**Stability Gate**: All `browser_*` tools sit at DANGEROUS tier and pass the `ConfirmationGate` per consequential action; the browser runs an isolated profile (never the user's logged-in default); ARIA snapshots enter the prompt only as provenance-labelled, scanner-screened data; a seeded injection payload in page content is flagged; hard denials and loop guards apply to browser-driven runs.
**Recommended model**: Strong tier, high effort - claude-opus-4-8 (high). Rationale: the largest new threat surface of the cycle (agent-steered outbound + indirect prompt injection); security design quality dominates. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 2.1 - Security design pass

**Objective**: Decide the boundaries before writing the tools.

**Prompt**:
> Produce the browser-surface security design (a short doc in `docs/archive/v2/v2.0/`): isolated browser profile strategy (dedicated profile directory; the user's default profile is never touched -- session-cookie exfiltration is the named risk); permission mapping (every `browser_*` tool at DANGEROUS tier in `modules/coding/guardrails/PermissionTiers.ts`, per-action confirmation via `src/tools/ConfirmationGate.ts`, no tier lower than the existing `fetch_page`); snapshot handling (ARIA snapshots, not screenshots, for token economy; labelled with the v1.19.1 Phase 2.6 browser-snapshot origin class and screened by `PromptInjectionScanner` before entering context); interaction budget (how the v1.19.1 Phase 2.2 loop guards bound a browsing session); and the Playwright dependency posture (local OSS library, pinned version, no service). Acceptance: the doc exists and each decision above is explicit; reviewed against the Atomic comparison Sections 5.1/5.2/9.

---

#### 2.2 - `browser_*` tool family (Atomic C1)

**Objective**: The tools themselves.

**Prompt**:
> Implement the browser tool family in `src/tools/ToolRegistry.ts` + `src/tools/handlers/`: `browser_navigate`, `browser_click`, `browser_type`, `browser_aria_snapshot` (and a `browser_close`/session-end tool), reverse-engineering Atomic Agent's tool shape (schemas, ARIA snapshot representation) over a locally-installed pinned Playwright dependency driving the user's local Chrome/Edge/Chromium with the 2.1 isolated profile. Register DANGEROUS-tier entries; wire per-action confirmation; route every snapshot through the 2.1 sanitization path before it reaches the model. Sessions are bounded by the loop guards and closed cleanly on run end or guard trip. Acceptance: a scripted flow (navigate, snapshot, click, type, snapshot) works end-to-end under confirmation gating; the isolated profile is verifiably separate from the default; snapshots arrive labelled and screened.

---

#### 2.3 - Adversarial validation

**Objective**: Prove the injection posture holds.

**Prompt**:
> Build an adversarial test page set (local fixtures, no network): pages embedding instruction-shaped payloads ("ignore previous instructions", hidden text, ARIA-label injection), a page that tries to redirect the agent to a denied command, and a long multi-page flow designed to trip the interaction budget. Assert: payloads are flagged/neutralized by the screening path and never executed as instructions; a browser-suggested denied command is still hard-denied (v1.19.1 Phase 2.1); the loop guards bound the flow. Apply `[[ai-attack-patterns]]` and `[[prompt-injection-defense]]` framing for coverage. Acceptance: all adversarial cases pass; findings that required fixes are documented in the session history.

---

#### 2.4 - Testing and Stabilization

**Objective**: Full coverage and CI for the new surface.

**Prompt**:
> Add the remaining tests: tier/gating unit tests per tool (DANGEROUS tier, confirmation required, no override below floor); profile-isolation test; snapshot labelling/screening tests; session lifecycle tests (clean close on completion and on guard trip). Run the coding-module suite + lint + typecheck; iterate to green. Update CI to cover the browser handlers (Playwright available in CI or the browser-dependent tests marked/skipped with a documented local-run requirement). Do not proceed to Phase 3 until stable. After tests pass, run `/generate-session-history` for Phase 2.

---

### Phase 2 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (tools, gating, isolation, screening, lifecycle, adversarial set)
- [x] Every browser tool at DANGEROUS tier; per-action confirmation enforced
- [x] Isolated profile verified; user default profile untouched
- [x] Seeded injections flagged; denied commands stay denied under browser influence
- [x] Security design doc committed
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 3

---

## Phase 3: Video Lab -- Continuation + Avatar Tier

**Goal**: The Video Lab escapes the fixed 4-8 s clip ceiling via autoregressive continuation, and gains an `audio2video` avatar mode (photo + audio -> lip-synced video) gated to `diffusion-pro`, fully local.
**Prerequisites**: v1.19.2 Phase 1 (task 1.2 `audioConditioning` schema; task 1.3 quantized-weight delivery for Avatar-1.5 INT8 -- the puller build lives in v1.19.2 per D6, this plan only consumes it). The v1.19.1 task 1.3 avatar-prep skill ships alongside 3.2 per the LongCat sequencing.
**Stability Gate**: Continuation chains segments in `TimelinePreviewer.tsx` beyond a single tier clip on the existing Wan 2.2 pipeline; the avatar mode runs the official Avatar-1.5 INT8 pipeline behind `diffusion-pro` with an explicit VRAM preflight; a byte-level security scan of the imported LongCat inference tree is recorded BEFORE integration; all weight files sha256-pinned via `model_preflight.py`; generated avatar output embeds provenance metadata.
**Recommended model**: Mid-strong tier, high effort - claude-sonnet-5 (high). Rationale: substantial sidecar pipeline work plus a gated upstream-code integration with mandatory scan preconditions. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 3.1 - Video continuation for long-form output (LongCat A2)

**Objective**: Minutes-long output by chaining, prototyped on the existing pipeline first.

**Prompt**:
> Implement autoregressive clip continuation in the diffusion sidecar video pipeline (`desktop/sidecar/src/diffusion/`): condition each new segment on the prior segment's ending frames, and chain segments in the Video Lab timeline (`desktop/src/modules/video/TimelinePreviewer.tsx`) beyond the tier-fixed clip lengths in `core/config/DiffusionTier.ts`. Prototype on the existing Wan 2.2 pipeline per the comparison's guidance (the technique is model-portable; LongCat's continuation-pretrained weights are not required for the mechanism), and validate the chaining UX cheaply before any larger model enters. Respect the GPU scheduler and per-tier limits for each segment. Acceptance: a multi-segment continuation renders and plays as one timeline sequence; per-segment tier limits and VRAM budgets respected; seam quality documented honestly in the session history.

---

#### 3.2 - `audio2video` avatar mode (LongCat A1)

**Objective**: Photo + audio -> lip-synced talking-head video, local and gated.

**Prompt**:
> Add an `audio2video` dispatch mode to `desktop/sidecar/src/diffusion/videoDispatcher.ts` alongside `text2video`/`image2video`, a new avatar form surface in `desktop/src/modules/video/VideoPromptForm.tsx` (reference photo + audio track inputs), and the LongCat-Video-Avatar-1.5 INT8 pipeline in the Python diffusion sidecar. Mandatory preconditions, in order: (1) byte-level security scan of the LongCat inference tree being integrated, recorded in the session history (comparison Section 9); (2) official `meituan-longcat` weights only, INT8 variant via the v1.19.2 task 1.3 mechanism, every file sha256-pinned through `scripts/installer/src/nexus_installer/engine/model_preflight.py` -- community re-quantizations remain rejected (N2); (3) integrated code stripped per MCP Registry Policy bucket-3 conventions. Gate the mode to the `diffusion-pro` tier with an explicit VRAM preflight; use the v1.19.2 task 1.2 `audioConditioning` fields; keep the capability behind an explicit user action; embed generation-provenance metadata in saved output (deepfake-adjacent output demands it); user photos and audio never leave the device. Acceptance: photo + audio produce lip-synced video locally on a `diffusion-pro` host; below-tier hosts get a clear gate message; provenance metadata present in saved files; scan + pinning evidence recorded.

---

#### 3.3 - Testing and Stabilization

**Objective**: Cover dispatch, gating, chaining, and metadata; wire CI.

**Prompt**:
> Add tests: dispatcher tests for the `audio2video` method (validated IPC payloads, progress correlation like the existing modes); tier-gating tests (mode hidden/blocked below `diffusion-pro`, VRAM preflight path); continuation tests (segment chaining logic, per-segment limits); provenance-metadata assertions on saved output; installer tests for the Avatar weights manifest. Heavy GPU paths get marked integration tests with documented local-run requirements; CI covers all logic that runs without a GPU. Run the sidecar + desktop suites + lint + typecheck; iterate to green. Do not proceed to Phase 4 until stable. After tests pass, run `/generate-session-history` for Phase 3.

---

### Phase 3 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (dispatch, gating, chaining, metadata, weights manifest)
- [x] Byte-level scan of integrated upstream code recorded; only official pinned weights
- [x] Avatar mode gated to `diffusion-pro` with VRAM preflight; explicit user action required
- [x] Provenance metadata embedded in avatar output
- [x] Zero runtime outbound; photos/audio never leave the device
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 4

---

## Phase 4: Stretch -- Scoping, Sandboxes, Router, Memory Kinds, VRM

**Goal**: The clearly deferrable items, isolated here so core phases stay bounded. Each sub-task is independently shippable; any of them may slide to a later patch slot without re-analysis if the cycle runs long. Implement in the listed order if time allows; otherwise transfer the remainder to known-gaps in Phase 5.2.
**Prerequisites**: v1.19.1 Phase 2 (guards, posture, provenance) for 4.1-4.4; Phase 1 (voice loop timing) for 4.7.
**Stability Gate**: Whatever ships is fully tested to the same bar as core phases; whatever does not ship is explicitly transferred to `docs/archive/v2/v2.0/known-gaps.md` with its dependency notes intact.
**Recommended model**: Mid tier, medium effort - claude-sonnet-5 (medium). Rationale: bounded, well-specified builds; the risk-heavy design work happened in the v1.19.1 hardening phase. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 4.1 - Per-project `ProjectScope` object (QM A1)

**Objective**: Unify Nexus's scattered scoping into one owned object.

**Prompt**:
> Build a `ProjectScope` abstraction composing the existing partial scoping -- per-chat memory (`modules/chat/memory/ChatScopedMemory.ts`), the per-project MCP registry (`modules/coding/mcp/HubRegistryPolicyFilter.ts`), per-scope permission overrides, and per-scope skill enablement -- reverse-engineered from QM's per-person/per-room scope onto per-project boundaries. Permission deltas are tightening-only (the clamp still floors everything); QM's multi-user grant/promotion machinery is not built. Acceptance: one object owns a project's memory scope, MCP registry, permission overrides, and skill set; a scope-level tightening is enforced in tests; no override loosens below the global floor.

---

#### 4.2 - Durable per-project exec sandbox (QM A4)

**Objective**: A project-keyed "durable computer" for the agent.

**Prompt**:
> Build a persistent, project-keyed exec sandbox root where installed tooling survives across sessions, per QM's durable-sandbox concept and the recommendations of the v1.12.0 exec-sandbox audit (`docs/archive/v1/v1.12/exec-sandbox-audit.md`), keyed off the 4.1 `ProjectScope`. Requirements from the QM comparison Section 5: sandbox state isolated per project, excluded from memory indexing, treated as untrusted, and resettable in one click. Acceptance: a tool installed in one session is available in the next for the same project only; reset wipes it verifiably; sandbox contents never enter the memory index.

---

#### 4.3 - Code-as-action sandbox + Query DSL (Airi A3)

**Objective**: Script execution for bounded multi-step sequences, without bypassing anything.

**Prompt**:
> Implement sandboxed code-as-action for bounded multi-step tool sequences, reverse-engineered from Airi's Minecraft agent: the model may emit a script executed in a locked-down worker (no network, no fs by default, hard timeout) with a side-effect-free Query DSL for read-only introspection, while every mutating operation routes through `modules/coding/guardrails/PermissionTiers.ts` / `src/tools/ConfirmationGate.ts` and is checkpointed by `modules/coding/guardrails/GitSafetyNet.ts`. JSON tool calls remain the default; code-as-action is opt-in per run. Build inside the exec-sandbox boundary (4.2 if shipped, else the audited v1.12.0 sandbox). Acceptance: a script performing reads via the DSL and mutations via gated tools works; a script attempting direct fs/network access fails closed; timeouts enforced; mutations checkpointed.

---

#### 4.4 - Fast small-model command router (Atomic C3)

**Objective**: Short imperative requests dispatch fast, safely.

**Prompt**:
> Build a local intent router in front of `src/tools/AgentLoop.ts`: short imperative requests are classified by a small fast local model (Qwen-class from `core/registry/catalog.json`) that emits a single proposed tool call, with abstain-and-escalate to the full loop when uncertain. Invariant (Atomic comparison Section 9): the router is a dispatcher, never a bypass -- every routed call passes the full `PermissionTiers`/`ConfirmationGate` path at its normal tier. Complements, and reuses model selection from, `modules/coding/orchestration/HarnessSelector.ts`. Acceptance: routed simple commands execute faster than the full loop (measured); ambiguous requests escalate; a routed DANGEROUS-tier call still requires confirmation.

---

#### 4.5 - Lessons/procedures memory kinds (Atomic C5, code half per D12)

**Objective**: Thin storage for distilled learning.

**Prompt**:
> Add `lesson` and `procedure` memory kinds with a usefulness-vote counter to `core/memory/MemoryHub.ts`, written through the existing `core/observability/redactSecrets.ts` pre-index path. Capture/minting stays with the Nexus-Hub `continuous-learning` skill (verified in v1.19.1 task 1.4); this is storage plumbing only. Stored lessons are advisory retrieval context, never executable instructions (poisoned-lesson risk from the Atomic comparison). Acceptance: kinds store/retrieve through hybrid retrieval; votes update; scrubbing applies; retrieval presents them as context, not directives.

---

#### 4.6 - Substrate-behind-interface seams (QM A6)

**Objective**: Hygiene refactor formalizing the remaining substrate seams.

**Prompt**:
> Formalize interface seams for session store, sandbox, and memory, mirroring the existing per-model harness-selector seam (v1.12.0), per QM's substrate-behind-interface pattern. Pure internal refactor: no behavior change, no new capability. Acceptance: the three seams are typed interfaces with the current implementations behind them; full suite green with zero behavior diffs.

---

#### 4.7 - VRM avatar presence layer (Airi A5, per D9)

**Objective**: Optional animated presence in Chat -- only if pillar demand justifies it.

**Prompt**:
> If, and only if, Chat pillar demand justifies it at implementation time: add an optional VRM avatar presence pane to Chat using an open `three-vrm`-class renderer (auto-blink, lip-sync driven by the Phase 1.3 TTS timing). VRM only -- Live2D is excluded (proprietary Cubism Core SDK; vendor-intrinsic sub-part rejected in the Airi comparison). Local rendering only; strictly cosmetic; off by default. Acceptance: the pane renders and lip-syncs against TTS output, can be disabled entirely, and adds no cost when disabled.

---

#### 4.8 - Testing and Stabilization

**Objective**: Full coverage for whatever shipped; explicit transfer for whatever did not.

**Prompt**:
> For each shipped stretch item, add the tests named in its acceptance criteria and run the full relevant suites + lint + typecheck to green. For each unshipped item, write a transfer entry (item, source comparison, dependencies, why deferred) staged for `docs/archive/v2/v2.0/known-gaps.md` in Phase 5.2. Update CI for shipped surfaces. After tests pass, run `/generate-session-history` for Phase 4.

---

### Phase 4 Exit Checklist

- [x] Shipped items fully tested to core-phase standard
- [x] Unshipped items have complete transfer entries staged for known-gaps
- [x] No stretch item weakened any v1.19.1 hardening invariant (clamp, denials, gates)
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 5

---

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| (none - no constitution file; no violations) | | |

---

## Phase 5: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, and its CI/CD complete and optimized ahead of the v2.0.0 release cut.
**Prerequisites**: All prior phases of this plan.
**Stability Gate**: The layout is clean (no deprecated/obsolete files, empty dirs, redundant files/dirs, or overcomplicated structure left un-triaged); the version's known gaps are reconciled including all Phase 4 transfers; CI/CD covers every change and is optimized; project validation/tests pass.
**Recommended model**: Strong tier, high effort - claude-opus-4-8 (high). Repo-wide refactor + reference repair is high-risk - default to the strong reasoning tier.

### Sub-tasks

#### 5.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure introduced or exposed across this plan's Phases 1-4 and the residue of the v1.19.1/v1.19.2 subplans (e.g. superseded loader/guard logic replaced by the unified loop guards, transitional scaffolding around the posture dial or provenance labels, duplicated media plumbing between the audio bridge and voice loop), then refactor toward a clean, intuitive layout via `[[project-refactor]]` and `[[docs-layout-refactor]]` (propose-then-apply, with confirmation; repair every reference for anything that moves). Confirm the `docs/archive/v2/v2.0/` tree is complete (this plan, the seven seeding comparisons, the Phase 2 security design doc, phase session-histories, known-gaps).

#### 5.2 - Known-gaps reconciliation
**Objective**: Reconcile the version's open gaps.
**Prompt**:
> Reconcile this version's known gaps via `[[known-gaps-tracker]]`: create `docs/archive/v2/v2.0/known-gaps.md`, resolve/defer/transfer each still-open item carried from `docs/archive/v1/v1.15/known-gaps.md` (and any v1.16/v1.17/v1.18/v1.19 carry-forward that exists by then, including the v1.19.0-v1.19.2 subplan gaps), and record the new v2.0 gaps: every unshipped Phase 4 stretch item; the native audio-token reasoning gap (Phase 1.2, waits on a fitting local model); the Inkling GGUF multimodal-support gap if it shipped text-only (v1.19.2 task 1.6); Kimi K4 (Kimi K3 catalog entry deferred until a GGUF lands in the Ollama-wrapped lineage); the OpenWorker A2 scheduler dependency if still unshipped (v1.19.1 task 1.4, item b); continuation seam-quality follow-ups (Phase 3.1); and any browser-surface hardening follow-ups from the Phase 2 adversarial pass. Finalize the per-minor known-gaps file for v2.0.

#### 5.3 - CI/CD create/update/optimize
**Objective**: CI/CD covers all changes and is optimized.
**Prompt**:
> Create or update the CI/CD pipeline so it covers every change in this plan (chat multimodal surfaces, browser handlers, video sidecar + Video Lab, and any shipped stretch items) and confirms coverage of the v1.19.1/v1.19.2 paths (guardrails/loop/tools, registry + installer engine), then optimize it to reduce action minutes (path filters per touched tree, concurrency cancel-in-progress, dependency caching for npm and pip) while keeping comprehensive testing. Document which suites require local hardware (GPU integration tests, patient-tier determinism, live browser) and how they are marked/skipped in CI. Keep it platform-agnostic; GitHub Actions is the primary example.

#### 5.4 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and CI/CD is green.
**Prompt**:
> Run the full validation/test suite across all touched workspaces (desktop shell, coding module, core, sidecar, installer: unit + lint + typecheck + golden-task gate), confirm the refactor changed no behavior, confirm CI/CD passes and the action-minute reduction is real, and iterate until clean. Update `docs/todos.md` (dashboard metrics + checked-off items) via `[[dev-progress-tracker]]`. Generate a session-history entry for this phase.

---

### Phase 5 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing across all touched workspaces
- [x] No known regressions from prior phases
- [x] Known gaps reconciled (v1.15+ carry-forward + all new v2.0 items + Phase 4 transfers)
- [x] CI/CD covers all changes and is optimized; hardware-gated suites documented
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 6

---

## Phase 6: v2.0.0 Convergence and Release

**Goal**: Cut v2.0.0 only when the whole plan family has converged: the v1.18.0 plan, the v1.19.0, v1.19.1, and v1.19.2 subplans, and this plan's Phases 1-5 are all verifiably complete, and the release mechanics (version bump, changelog, cross-version known-gaps sweep, milestone ledger) are done.
**Prerequisites**: Phases 1-5 of this plan, plus completion of every sibling plan in the Plan Family section.
**Stability Gate**: Every plan-family exit checklist is satisfied with evidence pointers (session histories, release tags, or known-gaps transfer records); the version is bumped to 2.0.0 everywhere it is declared; the changelog covers v1.18.0 through v2.0.0; the known-gaps sweep across v1.18-v2.0 leaves no untracked open item; the README/docs milestone ledger reflects the converged release.
**Recommended model**: Mid tier, medium effort - claude-sonnet-5 (medium). Rationale: verification and release mechanics over completed work; the value is completeness of evidence, not invention. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 6.1 - Convergence gate verification

**Objective**: Prove every plan in the family is complete before any release mechanics run.

**Prompt**:
> Verify, with one evidence pointer each (final-phase session history, release tag, or equivalent), that every plan in the Plan Family section is complete: (a) `docs/archive/v1/v1.18/plans/v1.18.0-adoption-agent-harness-and-governance.md` (all phases, including its gated/deferred items recorded); (b) `docs/archive/v1/v1.19/plans/v1.19.0-adoption-liquid-lfm-agentic.md` (all four phases, including the bake-off verdict record); (c) `docs/archive/v1/v1.19/plans/v1.19.1-adoption-agent-loop-and-guardrail-hardening.md` (both phases); (d) `docs/archive/v1/v1.19/plans/v1.19.2-adoption-catalog-and-model-expansion.md` (its phase); (e) this plan's Phases 1-5, including Phase 4 stretch transfers staged into known-gaps. Any plan not complete is a NO-GO: stop here, do not proceed to 6.2, and record what blocks convergence. Acceptance: a convergence checklist with per-plan evidence pointers exists in this phase's session history, and the GO/NO-GO verdict is explicit.

---

#### 6.2 - Release readiness: version bump, changelog, known-gaps sweep, milestone ledger

**Objective**: The release mechanics for the converged v2.0.0 cut.

**Prompt**:
> Execute the release-readiness work for v2.0.0: (a) bump the version to 2.0.0 in every place it is declared (package manifests, installer metadata, docs headers, and any version constants), keeping the bump atomic; (b) write the v2.0.0 changelog entry covering the converged wave -- summarize v1.18.0, v1.19.0, v1.19.1, v1.19.2, and this plan's phases, crediting each theme to its plan file; (c) run a known-gaps sweep across `docs/archive/v1/v1.18/`, `docs/archive/v1/v1.19/`, and `docs/archive/v2/v2.0/` so every open item from the family is either resolved or tracked in exactly one known-gaps file with no duplicates or orphans; (d) update the README and docs milestone ledger so the version history reflects the multi-subplan v1.19 milestone and the v2.0.0 convergence release. ASCII-only in commit messages and changelog. Acceptance: version strings agree everywhere; the changelog names all five plans; the sweep leaves no untracked or double-tracked gap; the milestone ledger is current.

---

#### 6.3 - Final validation and release cut

**Objective**: Green everything, then hand off to the release flow.

**Prompt**:
> Run the full validation/test suite across all workspaces one final time on the release candidate (installer pytest suite + `npm test` + `npm run test:shell` + `npm run lint` + typecheck + golden-task gate), confirm CI is green on the release branch state, and iterate until clean. Update `docs/todos.md` (dashboard metrics + checked-off items) via `[[dev-progress-tracker]]`. Generate a session-history entry for this phase, then hand off to `/update release` to cut v2.0.0.

---

### Phase 6 Exit Checklist

- [x] All sub-tasks completed
- [x] Convergence gate GO: v1.18.0, v1.19.0, v1.19.1, v1.19.2, and this plan's Phases 1-5 all complete with evidence
- [x] Version bumped to 2.0.0 everywhere; changelog covers the converged wave
- [x] Known-gaps sweep across v1.18-v2.0 complete; every open item tracked exactly once
- [x] README/docs milestone ledger updated
- [x] Full suite + CI green
- [x] Session history generated for this phase
- [x] v2.0.0 released (handed off to `/update release`)

---

## Out of Scope / Rejected

Consolidated from the Section 9 rejections of all seven comparisons. Every item below is a `drop-outright` (or evaluated-and-rejected vendor-intrinsic) under the MCP Registry Policy decision tree (`AGENTS.md`; hard no on search-as-service, embeddings-as-service, scraping-as-service, generation-as-service) and the local-first / zero-outbound design principles (`README.md`). This section binds the whole plan family (v1.19.1 and v1.19.2 included); none of these may be resurrected by any phase of any family plan without a new comparison-level policy review.

### Hosted inference, generation, and fine-tuning (MCP Registry Policy hard no: generation-as-a-service)

- **Nous Portal hosted inference** incl. the Herald 20% discount (Hermes N1).
- **Cloud LLM provider breadth** -- OpenRouter, OpenAI, Anthropic, Gemini, Groq, Together, Mistral, regional APIs, and every other non-loopback endpoint (Hermes N2; Atomic N2; Airi N1; consistent with the v1.6.0 D1 drop; `modules/coding/llm/LocalAdapterRegistry.ts` rejects non-loopback by construction).
- **Hosted TTS/voice services** -- ElevenLabs, Azure Speech, OpenAI-compatible TTS (Airi N2; the Phase 1 local Kokoro-class slice delivers the capability without the service).
- **Hosted LongCat/Meituan video-generation APIs** (LongCat N1; biometric-grade inputs -- a user's face and voice -- must never reach a third-party processor).
- **Tinker platform / Tinker Playground hosted access and hosted fine-tuning of Inkling-Small** (Inkling N1, N2; local Apache-2.0 weights are the only sanctioned path).
- **Honcho hosted user modeling** (Hermes N3; Nexus's local 4-layer memory is the already-built internal equivalent).
- **Exa search-as-service** (Atomic N1; MCP Registry Policy hard no on search-as-service).

### Off-machine communication, federation, and inbound control surfaces

- **Multi-tenant server core** -- Fastify headless core + Postgres persistence (QM N1; the opposite of the zero-infrastructure desktop posture).
- **Slack integration and hosted web/admin/public portals** (QM N2).
- **Messaging-gateway surface** -- Telegram, Discord, Slack, WhatsApp, Signal, Buzz/Nostr (Hermes N4; Airi N3; credential sprawl + persistent outbound relays).
- **Telegram Bot API remote control** (Atomic N3; evaluated for the trusted-vendor bucket and rejected on all three criteria).
- **Signed outbound webhooks** (Hermes N5; streams session/tool telemetry off-machine by design).
- **A2A v1.0 agent-to-agent protocol plugin** (Hermes N6; a new inbound agent-driven control channel plus outbound federation; default-deny posture of `modules/coding/mcp/HubRegistryPolicyFilter.ts`).
- **Atomic's inbound webhook trigger surface** (Atomic Section 9; new inbound surface, not taken).
- **Cloud deployment CLI** -- Fly/AWS targets, operator cloud accounts (QM N3).

### External engines, hubs, and multi-user machinery

- **External vendor harness backends** -- Pi, OpenCode, Codex, Claude Code as interchangeable engines (QM N4; only the interface pattern is adopted, as stretch item 4.6).
- **Org identity, skill grants, admin-gated promotion** (QM N5; no single-user analogue beyond 4.1's per-project scoping).
- **ClawHub external skill hub** (Atomic N4; Nexus-Hub is the sole, author-owned upstream skill feed).
- **Bespoke C99 engine import/port** from kimi-k3-in-c (Kimi R1; identical to the v1.12.0 D1 Colibri drop -- the technique already ships via the llama.cpp/Ollama lineage).
- **A CPU-only AVX2 inference path inside Nexus** (Kimi R3; belongs in the user-registered adapter, not in Nexus).

### Weights, artifacts, and product-surface drops

- **Shipping/bundling the Kimi K3 checkpoint (1.56 TB) or listing it as a default entry** (Kimi R2; revisit only as a gated patient-tier entry if a GGUF lands in the Ollama-wrapped lineage -- tracked as known-gap K4).
- **Unvetted community quantizations** -- third-party FP8/INT8 re-uploads not published by the model's official org (LongCat N2; the v1.19.2 task 1.3 puller admits official variants only).
- **Live2D rendering** (Airi, vendor-intrinsic sub-part of A5: proprietary Cubism Core SDK; the stretch VRM path uses open renderers only).
- **The game-playing product surface** -- Minecraft/Factorio/KSP bots (Airi drop-outright; only the loop machinery transfers, adopted in v1.19.1 Phase 2.2 and stretch 4.3).
- **The 80 GB full-precision LongCat-Video path** (LongCat Section 9; consumer story is quantized-or-nothing).
- **A literal QM "Dangerous" no-floor posture** (QM Section 9; the most permissive Nexus posture is "fewer confirmations above the floor", never "no floor").
- **Raw 1M-token context as a feature** (Inkling I4/Section 9; KV-cache at that length is data-center-scale; hybrid retrieval + compaction remains the local-first answer).
