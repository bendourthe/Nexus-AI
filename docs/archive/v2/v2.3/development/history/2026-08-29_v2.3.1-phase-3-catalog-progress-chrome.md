# Development Log: Catalog and progress chrome

**Date**: 2026-08-29
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: Name rows and installing model lists sit on card surfaces, not `BG_WINDOW` bars.
**Outcome**: Global QSS no longer paints every QWidget with `#0a0d14`. `#cardHeaderRow` and PhaseGroup details are transparent so they show `BG_CARD`.

---

## 1. Starting State

- **Branch**: `fix/v2.3.1-installer-field-repair`
- **Starting tag/commit**: `ce85fe3` (`fix(installer): keep wizard alive when the install thread throws`)
- **Environment**: Windows 10, Python 3.12.3, uv, PyQt5 installer suite
- **Prior session reference**: [2026-08-29_v2.3.1-phase-2-install-thread-crash.md](2026-08-29_v2.3.1-phase-2-install-thread-crash.md)
- **Plan reference**: [v2.3.1-installer-field-repair.md](../../plans/v2.3.1-installer-field-repair.md)

Context: in-full implement of v2.3.1. Phases 1-2 are committed locally. Field review showed black name-row and progress-list fills from `QMainWindow, QWidget { background-color: BG_WINDOW }`.

---

## 2. Chronological Steps

### 2.1 Surface tokens instead of global QWidget fill

**Branch**: `fix/v2.3.1-installer-field-repair` | **PR**: none | **Merged to**: not merged

**Plan specification**: Stop applying `BG_WINDOW` to the bare `QWidget` selector. Paint QMainWindow / the background host only. Add `#cardHeaderRow` and PhaseGroup details rules: `background: transparent`. Give `QWidget#modelCard QWidget` transparent fill.

**What happened**: Split the global rule into `QMainWindow` (window fill) and `QWidget` (transparent fill, same font/color). Added dedicated transparent rules for `#cardHeaderRow`, `#phaseGroupDetails`, and nested model-card widgets. Title bar, sidebar, cards, footer tokens, QTextEdit, and disabled checkbox indicators are unchanged.

**Key files changed**: `scripts/installer/src/nexus_installer/theme.py`, `scripts/installer/src/nexus_installer/widgets/header.py` (comment only)

**Troubleshooting**: None identified.

**Verification**: `test_theme.py` asserts the global `QWidget` block does not contain `BG_WINDOW` and `#cardHeaderRow` is transparent.

---

### 2.2 Card and PhaseGroup widgets

**Plan specification**: `cardHeaderRow` stylesheet `background: transparent` in typed_catalog. PhaseGroup `_details` transparent with objectName. Fail if `#0a0d14` is the header or details fill.

**What happened**: Header sets `WA_StyledBackground`, `autoFillBackground` false, and transparent QSS. Details use objectName `phaseGroupDetails` plus the same fill policy. Model list is not wrapped in a second `BG_WINDOW` frame. Over-budget cards still use `BG_CARD` with a dashed border.

**Key files changed**: `scripts/installer/src/nexus_installer/pages/typed_catalog.py`, `scripts/installer/src/nexus_installer/widgets/phase_group.py`

**Troubleshooting**: None identified.

**Verification**: Widget tests read `styleSheet()` and objectName; `#0a0d14` is absent from header and details.

---

### 2.3 Testing and stabilization

**Plan specification**: QSS/widget tests for catalog header and installing details. One local commit. Do not push. Human screenshot checks wait for Phase 6.

**What happened**: Extended `test_theme.py`, `test_model_pills.py`, and `test_phase_group.py`. Full installer pytest is green at 85% line coverage.

**Key files changed**: `scripts/installer/tests/test_theme.py`, `scripts/installer/tests/test_model_pills.py`, `scripts/installer/tests/test_phase_group.py`

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Installer pytest (`uv run pytest --cov=src` in `scripts/installer`) | PASS (3 skipped, 0 failed, 85% line coverage) |
| Ruff on Phase 3 files | PASS |
| `theme.py` line coverage | 100% |
| Build / import | PASS |
| Remote CI | NOT RUN (non-final phase) |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Packaged Windows screenshots of catalog/installing chrome | Cosmetic | Deferred to Phase 6 human QA |
| `host_detect.py` remains 40% line coverage | Cosmetic | Still v2.3.1 MT-1 |

---

## 5. Plan Discrepancies

None. T010 named typed_catalog; PhaseGroup details live in `phase_group.py` as specified in the subtask body.

---

## 6. Assumptions Made

- **Transparent QWidget plus tokenized chrome is enough**: the constellation BackgroundWidget still shows through `#contentWrapper`. Title bar, sidebar, and cards keep their own fills.
- **QSS assertions plus styleSheet() lock the fill**: they cannot prove a screenshot. Phase 6 does that.

---

## 7. Testing Summary

### Automated Tests

- Installer pytest: full suite green, 3 skipped, 85% line coverage.
- Focused Phase 3 files: theme, model_pills, phase_group, typed_catalog, pages_qt.

### Manual Testing Performed

None; human QA is deferred to the last phase.

### Manual Testing Still Needed

Human QA is deferred to the last phase. Packaged Windows screenshots of catalog name rows and installing model lists.

---

## 8. TODO Tracker

### Completed This Session

- [x] T009 Stop painting every QWidget with `BG_WINDOW`
- [x] T010 Make `cardHeaderRow` and PhaseGroup details inherit the card fill
- [x] T011 Test stylesheet tokens for header and details backgrounds

### Remaining (Not Started or Partially Done)

- [ ] T012-T027 (Phases 4-6)

### Out of Scope (Deferred)

- [ ] Packaged Windows screenshot verification (Phase 6)

---

## 9. Summary and Next Steps

Phase 3 is committed locally on `fix/v2.3.1-installer-field-repair`. Next: Phase 4 VS Code step and Unsloth placement (decision file first).

**CI impact**: new assertions under `scripts/installer/tests/`. `.github/workflows/installer-tests.yml` already path-filters `scripts/installer/**`. No new command, dependency, env var, artifact, or workflow file.
