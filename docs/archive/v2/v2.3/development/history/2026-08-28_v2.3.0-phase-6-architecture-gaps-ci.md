# v2.3.0 Phase 6 Architecture, Known Gaps, and CI History

**Plan**: [Local Video Enhancement and Security Audit Intake](../../plans/v2.3.0-adoption-qwen-video2x-openworker.md)
**Phase**: 6 - Architecture Refactor, Known-Gaps Reconciliation, and CI/CD
**Executed**: 2026-08-28
**Branch**: `feat/v2.3.0-qwen-video2x-openworker`
**Starting commit**: `5f48246`
**Remote Phase 6 CI**: Not run (publication pending approval)
**Human testing**: Suggestions recorded in last-phase evidence. Execution remains DF-3.

## 1. Starting State

- Branch at `5f48246` (Phase 5 packaging). Package **2.2.9**.
- Phases 1-5 complete. Last-phase evidence file did not exist.
- Local `develop` later gained `3bea05e` (v2.4.0 comparison) and `39c5007` (v2.3.1 installer field-repair). Those commits are not on this branch.

## 2. Chronological Steps

### 2.1 Architecture refactor (T017)

**Branch**: `feat/v2.3.0-qwen-video2x-openworker` | **PR**: none | **Merged to**: n/a

**Plan specification**: Scan empty dirs, duplicates, orphans; propose-then-apply via project-refactor and docs-layout-refactor.

**What happened**: `npm run cleanup:scan` reported 0 findings. `npm run check:docs-layout` passed. Empty dirs were caches and skill-catalog placeholders. No moves.

**Key files changed**: `docs/archive/v2/v2.3/development/last-phase-evidence.md`

**Troubleshooting**: Mid-phase the working tree was on `develop` (`39c5007`). Feature-branch work was restored by checking out `feat/v2.3.0-qwen-video2x-openworker` and recovering the handler allowlist plus known-gaps from a wip stash. The WN-8 `results.json` fixture mutation in that stash was not restored.

**Verification**: Quoted in last-phase evidence.

### 2.2 Known-gaps, living docs, git hygiene, CI, Goal review, human suggestions (T018-T023)

**What happened**: Reconciled 29 `docs/**/known-gaps.md` files. Added DF-4, QG-1, QG-2. Compared `ci.yml` field-by-field and did not apply pipeline changes. Goal review listed artifacts per clause; live hardware is DF-3, hydrate is DF-1, Hub is DF-2. Human suggestions written, not executed.

**Key files changed**: `docs/archive/v2/v2.3/known-gaps.md`, `docs/archive/v2/v2.3/development/last-phase-evidence.md`

### 2.3 Full local stabilization (T024)

**What happened**: Ran lint, `tsc -b`, root coverage, desktop coverage, installer pytest, python pytest, tampering, naming. First desktop aggregate failed the unimplemented-method allowlist (TEST) and three adapter 5000ms timeouts (ENV, CPU contention). Allowlist updated. Isolated filesystem-deadline test passed in 117ms. Isolated desktop coverage then passed.

**Key files changed**: `desktop/tests/sidecar-handlers.test.ts`

**Troubleshooting**:
- **Problem**: `declared-but-unimplemented methods throw NotImplementedError` resolved `{ capability: { ... } }` instead of rejecting.
- **Root cause**: `video.enhancement.capability` is implemented and returns an unavailable capability when Video2X is unset.
- **Resolution**: Add `video.enhancement.*` and `video.video2xPath.*` to the implemented allowlist.

### 2.4 Linux CI reopen (T025)

**What happened**: After the first push, GitHub `Test TypeScript (Node 22.x)` desktop vitest failed 3 tests. Node 24.x root tests, installer tests, lint, and the rest of CI passed. Classified TEST. Reproduced by reading `path.isAbsolute` on POSIX: `C:\\...` is not absolute, so Linux CI joined it to the workspace or rejected it.

**Key files changed**: `desktop/tests/generation-queue-handlers.test.ts`, `desktop/tests/video-enhancement-process-factory.test.ts`

**Troubleshooting**:
- **Problem**: `output.outputPath must be absolute`; process-factory `cwd` became `/home/runner/.../desktop/C:\\workspace`; probe diagnostic was the absolute-path message instead of the unconfirmed-termination message.
- **Root cause**: Fixtures used Windows drive-letter paths. Sidecar and GenerationDatabase use host `path.isAbsolute` / `path.resolve`.
- **Resolution**: Use `os.tmpdir()` host-absolute fixtures. Do not treat the Windows local pass as Linux evidence.

## 3. Verification Gate

| Check | Result |
| ----- | ------ |
| `npm run lint` | PASS |
| `npm run lint:shell` | PASS |
| `npx tsc -b` | PASS |
| `npm test -- --coverage` | PASS (5693 passed / 12 skipped, 87.66% lines) |
| `npm run test:shell:coverage` (isolated) | PASS (1820 passed / 1 skipped, 87.39% lines) |
| `python -m pytest tests/python -q` | PASS (261) |
| Installer pytest | PASS (1162 passed / 3 skipped) |
| `npm run check:tampering` | PASS (0 findings) |
| `npm run check:naming` | PASS |
| Remote CI | First push: Test TypeScript (Node 22.x) desktop vitest FAIL (3 tests, Windows-path fixtures). Other CI jobs PASS. Stabilization commit pending second push. |
| Human testing execution | NOT RUN (suggestions only) |

## 4. Known Issues

| Issue | Severity | Decision |
| ----- | -------- | -------- |
| DF-1 hydrate Enhance | P2 | Deferred |
| DF-2 Hub unreleased | P2 | Deferred |
| DF-3 real Video2X / GPU / packaged | P1 | Deferred |
| DF-4 missing check_release_preconditions.py | P2 | Deferred |
| WN-1 jsdom canvas notices | Cosmetic | Accepted |
| MT-1 VideoLabPage 72.41% functions | P2 | Accepted (86.94% lines; global desktop 87.39%) |
| QG-1 named CI profiles | P2 | Recorded, not applied |
| QG-2 permissions / aggregate required check | P2 | Recorded, not applied |

## 5. Plan Discrepancies

- T023 is suggestions plus DF-3, not a completed operator walkthrough.
- T025 is not done. Push requires explicit approval.

## 6. Assumptions Made

- Integration target is `develop` (develop+main). `ci.yml` `pull_request.branches` is `main` only (QG-2).
- Cursor cannot auto-switch models. Phase 6 recommended frontier / max; session model is Cursor Grok 4.6 (frontier). Visible no-degradation note.
- First desktop failure under parallel load was ENV, not a product timeout bug, because the same test passed isolated in 117ms.

## 7. Testing Summary

### Automated Tests
- Root: 5693 passed, 12 skipped
- Desktop isolated coverage: 1820 passed, 1 skipped
- Installer: 1162 passed, 3 skipped
- Python: 261 passed

### Manual Testing Performed
- None; suggestions are in last-phase evidence.

### Manual Testing Still Needed
- See last-phase evidence section Human/manual testing suggestions. DF-3.

## 8. TODO Tracker

### Completed This Session
- [x] T017-T024 last-phase duties and local gate

### Remaining (Not Started or Partially Done)
- [ ] T025 publish: push once after approval, open develop PR, wait for required checks, merge on green, then `/update release`

### Out of Scope (Deferred)
- DF-1, DF-2, DF-3, DF-4, QG-1, QG-2

## 9. Summary and Next Steps

Phase 6 local work is ready to commit. The plan's first remote event still needs explicit approval.

**Next session should**:
1. After approval, push `feat/v2.3.0-qwen-video2x-openworker` and open a PR into `develop`.
2. Wait for required checks against the merge result. Reproduce any red check locally before a second push.
3. After green merge, run `/update release` (not from this driver).
