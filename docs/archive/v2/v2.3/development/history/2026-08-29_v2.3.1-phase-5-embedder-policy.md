# Development Log: Required embedder policy

**Date**: 2026-08-29
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: The required memory embedder is an explicit product choice, not a screenshot guess. Default: keep Nomic Embed Text.
**Outcome**: KEEP. `nomic-embed-text` remains required. EmbeddingGemma stays 300M opt-in. No silent swap.

---

## 1. Starting State

- **Branch**: `fix/v2.3.1-installer-field-repair`
- **Starting tag/commit**: `3e59fa5` (`fix(installer): offer VS Code 1.135 replace and move Unsloth`)
- **Environment**: Windows 10, Python 3.12.3, uv, PyQt5 installer suite, Node/vitest
- **Prior session reference**: [2026-08-29_v2.3.1-phase-4-vscode-unsloth.md](2026-08-29_v2.3.1-phase-4-vscode-unsloth.md)
- **Plan reference**: [v2.3.1-installer-field-repair.md](../../plans/v2.3.1-installer-field-repair.md)

Context: in-full implement of v2.3.1. Phases 1-4 are committed locally. Field review asked about promoting GemmaEmbedding 300B. Catalog id is `embeddinggemma` / EmbeddingGemma 300M.

---

## 2. Chronological Steps

### 2.1 T016 decision: required embedder

**Branch**: `fix/v2.3.1-installer-field-repair` | **PR**: none | **Merged to**: not merged

**Plan specification**: Write `embedder-default-decision.md` before changing `is_required`. Recommend KEEP unless a reindex tool is named.

**What happened**: KEEP. Existing memory is Nomic-shaped. SWITCH would need a reindex tool and a wizard warning. This file names neither, so T017 cannot change defaults.

**Key files changed**: `docs/archive/v2/v2.3/development/embedder-default-decision.md`

**Troubleshooting**: None identified.

**Verification**: Installer test reads the decision file for `**KEEP**` and `nomic-embed-text`.

---

### 2.2 Apply KEEP

**Plan specification**: Leave `nomic-embed-text` required. Clarify EmbeddingGemma is 300M and opt-in. Do not invent 300B. Do not change recommended.json embed lists or `package.json` `embeddingModel`.

**What happened**: `is_required` now compares against `REQUIRED_EMBEDDER_ID`. EmbeddingGemma differentiators say 300 million parameters. `validate_catalog` rejects 300B copy and a Nomic entry whose task is not embed.

**Key files changed**: `core/registry/catalog.json`, `scripts/installer/src/nexus_installer/pages/typed_catalog.py`, `scripts/installer/src/nexus_installer/catalog_invariants.py`

**Troubleshooting**: Catalog copy must not contain the substring 300B even as a negation, or the invariant would false-positive. Used "300 million parameters" instead of "not 300B".

**Verification**: Repo catalog displayName is EmbeddingGemma 300M. recommended.json embed lists stay `[nomic-embed-text]`.

---

### 2.3 Testing and stabilization

**Plan specification**: Tests for `is_required`, recommended embed defaults, and copy that does not say 300B. Installer pytest plus root catalog tests. One local commit. Do not push.

**What happened**: Added `TestRequiredEmbedderPolicy` and extended typed-catalog plus `catalog.test.ts`. Full installer pytest is green at 85%. catalog.test.ts 53 passed.

**Key files changed**: `scripts/installer/tests/test_catalog_invariants.py`, `scripts/installer/tests/test_typed_catalog.py`, `tests/unit/core/registry/catalog.test.ts`

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Installer pytest (`uv run pytest --cov=src` in `scripts/installer`) | PASS (3 skipped, 0 failed, 85% line coverage) |
| Ruff on Phase 5 files | PASS |
| `catalog_invariants.py` line coverage | 88% |
| `npx vitest run tests/unit/core/registry/catalog.test.ts` | PASS (53 tests) |
| Build / import | PASS |
| Remote CI | NOT RUN (non-final phase) |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Packaged Windows screenshot of Embed tab copy | Cosmetic | Deferred to Phase 6 human QA |
| `host_detect.py` remains 40% line coverage | Cosmetic | Still v2.3.1 MT-1 |

---

## 5. Plan Discrepancies

None. T016 wrote the decision file before T017. No "Newer opt-in" chip: the plan marked that optional, and the status ladder has no spare slot without demoting Compatible.

---

## 6. Assumptions Made

- **KEEP is the product choice**: existing indexes are Nomic-shaped. not_observed != absent for a later reindex tool.
- **Substring 300B is forbidden in user-facing EmbeddingGemma fields**: the operator's 300B guess must not appear as a model size.

---

## 7. Testing Summary

### Automated Tests

- Installer pytest: full suite green, 3 skipped, 85% line coverage.
- Focused Phase 5 files: catalog_invariants, typed_catalog.
- Root catalog.test.ts: 53 passed.

### Manual Testing Performed

None; human QA is deferred to the last phase.

### Manual Testing Still Needed

Human QA is deferred to the last phase. Packaged Windows catalog: Nomic Required, EmbeddingGemma 300M opt-in.

---

## 8. TODO Tracker

### Completed This Session

- [x] T016 decision: keep Nomic as required embedder
- [x] T017 Apply KEEP and 300M copy
- [x] T018 Test `is_required` and recommended embed defaults

### Remaining (Not Started or Partially Done)

- [ ] T019-T027 (Phase 6)

### Out of Scope (Deferred)

- [ ] Embedder SWITCH / reindex migrator
- [ ] Packaged Windows screenshot verification (Phase 6)

---

## 9. Summary and Next Steps

Phase 5 is committed locally on `fix/v2.3.1-installer-field-repair`. Next: Phase 6 last-phase evidence, gates, and publication after explicit approval.

**CI impact**: installer pytest already path-filters `scripts/installer/**` and `core/registry/catalog.json` plus `recommended.json`. `catalog.test.ts` is already in the root vitest job. No new command, dependency, env var, artifact, or workflow file.
