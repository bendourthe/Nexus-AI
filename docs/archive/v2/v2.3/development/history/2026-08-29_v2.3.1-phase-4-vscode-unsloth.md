# Development Log: VS Code step and Unsloth placement

**Date**: 2026-08-29
**Operator**: Benjamin Dourthe
**Assisted by**: Cursor Grok 4.6
**Objective**: The VS Code page is about the extension: visible checkbox, replace if installed, honest ABI warning. Unsloth Core moves to Configuration with an explanation.
**Outcome**: 1.135.0 hosts get an enabled, default-checked extension checkbox. Replace uses `--force`. Unsloth is only on Configuration.

---

## 1. Starting State

- **Branch**: `fix/v2.3.1-installer-field-repair`
- **Starting tag/commit**: `a0413bb` (`fix(installer): paint catalog rows on card surfaces`)
- **Environment**: Windows 10, Python 3.12.3, uv, PyQt5 installer suite
- **Prior session reference**: [2026-08-29_v2.3.1-phase-3-catalog-progress-chrome.md](2026-08-29_v2.3.1-phase-3-catalog-progress-chrome.md)
- **Plan reference**: [v2.3.1-installer-field-repair.md](../../plans/v2.3.1-installer-field-repair.md)

Context: in-full implement of v2.3.1. Phases 1-3 are committed locally. Field review showed the extension checkbox hidden on VS Code 1.135.0 and Unsloth parked on that page.

---

## 2. Chronological Steps

### 2.1 T012 VS Code host policy

**Branch**: `fix/v2.3.1-installer-field-repair` | **PR**: none | **Merged to**: not merged

**Plan specification**: Write `docs/archive/v2/v2.3/development/vscode-host-policy.md` before changing the checkbox. Options A/B/C. Recommend A if `scripts/build-vsix.ps1` exists.

**What happened**: Decision A. Operator host is 1.135.0. vscode-versions shows 1.134.0 and 1.135.0 both ship Electron 42.8.1. `engines.vscode` is `>=1.134.0 <1.136.0`. The rebuild pin stays 42.8.1. 1.136+ is treated as version-mismatch (visible, disabled), not option C.

**Key files changed**: `docs/archive/v2/v2.3/development/vscode-host-policy.md`, `package.json`, `package-lock.json`

**Troubleshooting**: None identified.

**Verification**: `test_packaging.py` locks `engines.vscode` to `SUPPORTED_VSCODE_VERSION` / `SUPPORTED_VSCODE_MAX_EXCLUSIVE`. `build-vsix.ps1` still asserts `$SupportedElectronVersion = '42.8.1'`.

---

### 2.2 Tickable extension, replace if present

**Plan specification**: Checkbox always visible. Enabled on supported `code`. `--force` only when listing finds Nexus or legacy id. List failure fails open.

**What happened**: `vscode_version_is_supported` matches major.minor 1.134 or 1.135. `installed_nexus_extension_id` lists before install. Disabled indicator QSS uses `BG_INPUT` so the box stays visible on a card. Copy no longer says "exactly 1.134.0".

**Key files changed**: `scripts/installer/src/nexus_installer/engine/extension_installer.py`, `scripts/installer/src/nexus_installer/pages/vscode_extension.py`, `scripts/installer/src/nexus_installer/theme.py`

**Troubleshooting**: None identified.

**Verification**: Page tests for 1.135 enablement, 1.136 visible-disabled, replace label, list-exception install label. Engine tests for `--force` replace and no `--force` on list failure.

---

### 2.3 Move Unsloth Core to Configuration

**Plan specification**: Remove Unsloth from `VsCodeExtensionPage`. Add it under Configuration Features with NVIDIA 16 GB+ copy and license names. Off by default.

**What happened**: Configuration owns `_unsloth`, help, and a warning when the host is not NVIDIA 16 GB+. The engine comment now points at Configuration. VS Code page tests assert no `_unsloth` widget.

**Key files changed**: `scripts/installer/src/nexus_installer/pages/configuration.py`, `scripts/installer/src/nexus_installer/engine/installer.py`

**Troubleshooting**: None identified.

**Verification**: `test_pages_qt.py` toggles `install_unsloth` and checks the NVIDIA warning path.

---

### 2.4 Testing and stabilization

**Plan specification**: Checkbox visibility, 1.135 enablement, `--force` replace, Unsloth only on Configuration. Record CI impact. One local commit. Do not push.

**What happened**: Extended installer tests listed in section 7. Living install docs now say 1.134 or 1.135. Historical CHANGELOG 2.3.0 notes were left as shipped.

**Key files changed**: `scripts/installer/tests/test_vscode_extension_page.py`, `scripts/installer/tests/test_extension_installer.py`, `scripts/installer/tests/test_packaging.py`, `scripts/installer/tests/test_navigation.py`, `scripts/installer/tests/test_pages_qt.py`, `scripts/installer/tests/test_theme.py`, `docs/install.md`, `README.md`, `scripts/installer/build/windows-pipeline.md`

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Installer pytest (`uv run pytest --cov=src` in `scripts/installer`) | PASS (3 skipped, 0 failed, 85% line coverage) |
| Ruff on Phase 4 files | PASS |
| `vscode_extension.py` line coverage | 100% |
| `configuration.py` line coverage | 98% |
| `theme.py` line coverage | 100% |
| `extension_installer.py` line coverage | 89% |
| Build / import | PASS |
| Remote CI | NOT RUN (non-final phase) |

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| Packaged Windows screenshot of VS Code checkbox / Unsloth | Cosmetic | Deferred to Phase 6 human QA |
| `host_detect.py` remains 40% line coverage | Cosmetic | Still v2.3.1 MT-1 |
| 1.136+ Electron ABI | Cosmetic | Unproven here; treated as mismatch |

---

## 5. Plan Discrepancies

None. T012 wrote the decision file before T013. Rebuild pin was not changed because 1.134 and 1.135 share Electron 42.8.1.

---

## 6. Assumptions Made

- **Same Electron ABI is enough**: vscode-versions lists Electron 42.8.1 for both 1.134.0 and 1.135.0. Native rebuild is not required to admit 1.135.0.
- **Fail-open on list failure**: a failed `--list-extensions` must not invent a replace (`--force` without proof).

---

## 7. Testing Summary

### Automated Tests

- Installer pytest: full suite green, 3 skipped, 85% line coverage.
- Focused Phase 4 files: vscode_extension_page, extension_installer, packaging, navigation, pages_qt, theme.

### Manual Testing Performed

None; human QA is deferred to the last phase.

### Manual Testing Still Needed

Human QA is deferred to the last phase. Packaged Windows wizard: checkbox visible and tickable on the operator `code` version, replace copy if the extension is already installed, Unsloth on Configuration.

---

## 8. TODO Tracker

### Completed This Session

- [x] T012 decision: VS Code host policy
- [x] T013 Always-visible extension checkbox and `--force` replace
- [x] T014 Move Unsloth Core to Configuration
- [x] T015 Test VS Code checkbox visibility, replace argv, Unsloth page move

### Remaining (Not Started or Partially Done)

- [ ] T016-T027 (Phases 5-6)

### Out of Scope (Deferred)

- [ ] Packaged Windows screenshot verification (Phase 6)

---

## 9. Summary and Next Steps

Phase 4 is committed locally on `fix/v2.3.1-installer-field-repair`. Next: Phase 5 required embedder policy (KEEP Nomic unless a written decision plus reindex lands).

**CI impact**: installer pytest already path-filters `scripts/installer/**`, `package.json`, and `package-lock.json`. `test_packaging.py` now asserts the engines range. Workflows that call `scripts/build-vsix.ps1` (`ci.yml`, `release.yml`, `installer-build.yml`, `installer-macos.yml`, `installer-linux.yml`) already cover the unchanged Electron 42.8.1 rebuild. No new command, dependency, env var, artifact, or workflow file.
