# v2.3.0 Phase 5 Quality, Performance, and Packaging History

**Plan**: [Local Video Enhancement and Security Audit Intake](../../plans/v2.3.0-adoption-qwen-video2x-openworker.md)
**Phase**: 5 - Quality, Performance, and Packaging Evidence
**Executed**: 2026-08-28
**Branch**: `feat/v2.3.0-qwen-video2x-openworker`
**Starting commit**: `6989f16`
**Remote Phase 5 CI**: Not run
**Human testing**: Not requested; human QA is deferred to the last phase

## Outcome

Phase 5 proved the enhancement envelope with a fake deterministic harness, a shared support-copy contract, installer and Settings configuration that detect a user-installed executable without downloading or bundling it, and packaging tests that keep those claims honest. Real Video2X 6.4.0, GPU, VRAM, and packaged-app detection remain not proven here (DF-3). The Nexus-Hub security-audit workflow remains an unreleased upstream item (DF-2).

## Work completed

| Task | Evidence |
| ---- | -------- |
| T014 | `scripts/bench-video-enhancement.mjs`, `tests/fixtures/video-enhancement/v1/` (8 fixtures plus manifest), isolated `--output-dir` plus `bench.lock`, `--backend real` typed failures |
| T015 | `core/video/video-enhancement-support.json`, `core/video/videoEnhancementSupport.ts`, Settings > Video, sidecar `video.video2xPath.get` / `set`, installer notes, `docs/archive/v2/v2.3/benchmarks/video-enhancement-baseline.md`, README Video Lab paragraph |
| T016 | `tests/unit/video/video-enhancement-packaging.test.ts`, desktop Video Settings and path-IPC tests, installer `test_video_enhancement_support.py` |

## 1. Starting State

- Branch `feat/v2.3.0-qwen-video2x-openworker` at `6989f16` (Phase 4 Enhance UI). Package **2.2.9**.
- Phases 1-4 complete. Enhance UI existed; no benchmark harness, no Settings path, no installer note, no baseline report.
- Working tree already held draft Phase 5 files at the start of this session continuation. Unrelated untracked `docs/archive/v2/v2.3/plans/v2.3.1-installer-field-repair.md` and installer-field-review notes were left out of this commit.

## 2. Chronological Steps

### 2.1 Deterministic quality and resource benchmark harness (T014)

**Branch**: `feat/v2.3.0-qwen-video2x-openworker` | **PR**: none | **Merged to**: n/a

**Plan specification**: Tiny versioned 480p and 720p fixtures (photoreal, animation, text/edges, motion/occlusion). Fake backend for CI. Explicit real-backend mode. Record geometry, duration, resources when observable, typed failure. No invented perceptual score.

**What happened**: The harness defaults to `--backend fake`, writes isolated tmp output unless `--output-dir` is set, holds `bench.lock` with `wx`, and refuses to overwrite `results.json` without `--force`. Fake runs copy fixture bytes to the expected 2x or interpolated geometry. `--backend real` records `missing_configuration` or `backend_unavailable` when Video2X is absent. Peak CPU, GPU, and VRAM are recorded as not observed rather than zero.

**Key files changed**: `scripts/bench-video-enhancement.mjs`, `tests/fixtures/video-enhancement/v1/`

**Troubleshooting**: Concurrent packaging tests print `{"ok":false,"code":"output_conflict",...}` on stderr by design. That is the lock invariant, not a failed assertion.

**Verification**: Fake run 8/8 pass on the planning host. Packaging tests cover lock conflict and fixture geometry.

### 2.2 Support matrix, setup, and packaging gates (T015)

**Branch**: `feat/v2.3.0-qwen-video2x-openworker` | **PR**: none | **Merged to**: n/a

**Plan specification**: Baseline from observed data. Label untested combinations not proven here. Installer and desktop detect a separately installed executable. Declarative parity. Do not download or bundle Video2X. Hub handoff is a known gap unless a released Hub version supplies evidence.

**What happened**: `VIDEO_ENHANCEMENT_SUPPORT` is the SSOT. Adapter, panel, Settings, installer, and baseline all consume those keys and strings. `NEXUS_VIDEO2X_PATH` wins over `video.video2xPath`. Empty setting clears the path; a non-empty path must be absolute for the sidecar platform. Installer Configuration and Complete pages show a note, not a toggle. Hub remains DF-2.

**Key files changed**: `core/video/video-enhancement-support.json`, `core/video/videoEnhancementSupport.ts`, `desktop/src/pages/settings/VideoSettings.tsx`, `desktop/sidecar/src/protocol.ts`, `desktop/sidecar/src/handlers.ts`, `scripts/installer/src/nexus_installer/video_enhancement_support.py`, `docs/archive/v2/v2.3/benchmarks/video-enhancement-baseline.md`, `README.md`

**Troubleshooting**:
- **Problem**: Packaging tests first asserted the full JSON `setupCopy` / `installerNote` as a contiguous substring of the Python source. Ruff E501 forced implicit string concatenation, so the source is not one substring even though runtime equality holds. Exact error shape: assertion failed because the concatenated Python source is not a single contiguous copy of the JSON sentence.
- **Attempted**: Require the full JSON sentence in `.py` source.
- **Root cause**: Python adjacent-string concatenation is runtime-equal, not source-equal.
- **Resolution**: Tests assert runtime imports plus distinctive fragments (`never installed by this wizard`, `does not download, bundle, or search`) and the baseline tokens `` `video.video2xPath` `` and `fake-deterministic`.
- **Problem**: `userEvent.type` of a Windows `C:\...` path interpreted `\v` as a vertical tab.
- **Resolution**: Tests type a POSIX absolute path `/opt/video2x/video2x`.

**Verification**: Packaging 6 passed. Desktop Video Settings and path IPC tests pass. Installer pytest for support copy and ConfigurationPage pass. `core/registry/catalog.json` has no Qwen3.8 entry.

### 2.3 Testing and stabilization (T016)

**Branch**: `feat/v2.3.0-qwen-video2x-openworker` | **PR**: none | **Merged to**: n/a

**Plan specification**: Fake-backend tests, packaging parity, installer scoped tests, desktop runtime tests, lint, typecheck. No Video2X binary or automatic download. Record CI impact without editing pipeline files. Local commit only.

**What happened**: Coverage on `videoEnhancementSupport.ts` started at 78.57% lines / 50% functions. Added unknown-reason capability-copy coverage and `frameCount` fallback via combined geometry without `frameCount`. Added `ipcVideoSettingsClient` IPC mock tests and VideoSettings load/save error paths.

**Key files changed**: `tests/unit/video/video-enhancement-packaging.test.ts`, `desktop/tests/VideoSettings.test.tsx`, `desktop/tests/ipcVideoSettingsClient.test.ts`, `desktop/tests/video-video2x-path-settings.test.ts`

**Troubleshooting**:
- **Problem**: Ruff E501 on `test_optional_video2x_note_is_not_an_install_toggle` (90 > 88).
- **Resolution**: Renamed to `test_video2x_note_is_not_an_install_toggle`.

**Verification**: See section 3.

## 3. Verification Gate

| Check | Result |
| ----- | ------ |
| Root packaging Vitest (`tests/unit/video/video-enhancement-packaging.test.ts`) | PASS (6 tests) |
| File-level coverage `core/video/videoEnhancementSupport.ts` | PASS (100% lines, branches, functions) |
| Desktop focused Vitest (protocol, panel, VideoSettings, ipc client, path settings) | PASS (33 tests) |
| Desktop file-level coverage (new Phase 5 files) | PASS (VideoSettings 98.87% lines, ipc client 100%, mock client 92.59%, Panel 95.72%; all functions 100%) |
| Installer pytest (`test_video_enhancement_support.py`, ConfigurationPage note) | PASS |
| Installer ruff on Phase 5 installer files | PASS |
| Desktop ESLint `src sidecar/src tests --max-warnings=0` | PASS |
| Desktop `tsc --noEmit` | PASS |
| Root `tsc -b` | PASS |
| Catalog Qwen3.8 absence | PASS (no match in `core/registry/catalog.json`) |
| Video2X binary / automatic download URL | PASS (packaging walk plus installer grep; no Video2X download URL) |
| Remote CI | NOT RUN (non-final phase) |
| Human testing | NOT RUN (deferred to last phase) |

## 4. Known Issues

| Issue | Severity | Decision |
| ----- | -------- | -------- |
| DF-1 hydrated sessions omit Enhance | P2 | Deferred (Phase 4; unchanged) |
| DF-2 Hub security-audit not released | P2 | Deferred (T015) |
| DF-3 real Video2X / GPU / packaged detection not measured | P1 | Deferred (T014, T015; Phase 6 human testing) |
| WN-1 jsdom canvas and React act notices | Cosmetic | Accepted (Phase 4) |
| MT-1 VideoLabPage function coverage 72.41% on Phase 4 focused run | P2 | Accepted; line coverage above 80%; Phase 6 aggregate is the release gate |

## 5. Plan Discrepancies

- `# DEVIATION:` packaging tests live at `tests/unit/video/video-enhancement-packaging.test.ts` instead of the plan path `tests/video-enhancement-packaging.test.ts`. Root Vitest `include` is `tests/unit/**` and `tests/integration/**`; a repo-root `tests/*.test.ts` file would not be discovered.
- Python installer copy is implicitly concatenated to satisfy ruff E501. Runtime equality with JSON is tested; contiguous source-substring equality is not.

## 6. Assumptions Made

- **Cursor model routing**: Phase 5 recommended frontier / max. Session model is Cursor Grok 4.6 (frontier). Cursor cannot auto-switch. Proceeded with a visible no-degradation note rather than blocking.
- **File-level coverage is the Phase 5 gate**: Whole-desktop coverage against a subset still instruments the rest of `desktop/src` and `sidecar/src`. Aggregate desktop coverage remains a Phase 6 gate.
- **No new gitignore patterns**: The harness writes to `os.tmpdir()` or an explicit `--output-dir`. Existing `tests/benchmarks/results/` and `bench-results.json` ignores already cover other benches.

## 7. Testing Summary

### Automated Tests
- Root packaging: 6 passed, 0 failed, 0 skipped
- Desktop focused: 33 passed, 0 failed, 0 skipped
- Installer scoped pytest: support module plus ConfigurationPage note, all passed
- Fake-backend harness: 8 cases, 0 failed (informational wall/RSS; GPU/VRAM not observed)

### Manual Testing Performed
- None; human QA is deferred to the last phase.

### Manual Testing Still Needed
- human QA is deferred to the last phase

## 8. TODO Tracker

### Completed This Session
- [x] T014 deterministic enhancement quality and resource benchmarks
- [x] T015 support matrix, setup, packaging, and fallback behavior
- [x] T016 test and stabilize benchmark and packaging gates
- [x] Phase 5 post-phase sequence (gitignore, test review, CI impact, known-gaps, docs cleanup, DEVLOG, docs, session history, local commit)

### Remaining (Not Started or Partially Done)
- [ ] T017-T025 Phase 6 last-phase duties, evidence file, local aggregate gate, publish after explicit approval

### Out of Scope (Deferred)
- [ ] Real Video2X / GPU / packaged measurement (DF-3)
- [ ] Hub security-audit consumption (DF-2)
- [ ] Session-hydrate Enhance eligibility (DF-1)

## 9. Summary and Next Steps

Phase 5 landed the evidence and packaging honesty the Enhance UI needed. Fake-backend contract checks are automated. Real hardware, packaged detection, and perceptual review wait for Phase 6. No pipeline file changed. Nothing was pushed.

**Next session should**:
1. Run Phase 6 last-phase duties and write `docs/archive/v2/v2.3/development/last-phase-evidence.md`.
2. Reconcile known gaps across in-progress `docs/**/known-gaps.md` and run the local aggregate gate.
3. Create the final commit, obtain explicit approval, then push once and open the integration pull request.

## CI impact

| Surface | Phase 5 impact | Existing coverage |
| ------- | ------------- | ----------------- |
| Command | `node scripts/bench-video-enhancement.mjs --backend fake` (invoked by packaging tests; not a new npm script) | Root `npm run test` discovers `tests/unit/**` |
| Dependency | None (no package, lockfile, binary, or model asset) | Unchanged |
| Environment | Optional `NEXUS_VIDEO2X_PATH` (user path, not a CI secret) | CI fake backend does not require Video2X, GPU, or ffmpeg |
| Test path | `tests/unit/video/video-enhancement-packaging.test.ts`; desktop `VideoSettings.test.tsx`, `ipcVideoSettingsClient.test.ts`, `video-video2x-path-settings.test.ts`; installer `test_video_enhancement_support.py` | Root test-ts covers unit tests. Desktop `test:shell` covers desktop tests. Installer-tests path filter includes `scripts/installer/**` |
| Artifacts | Isolated tmp `results.json` / `bench.lock`; committed tiny `.fixture` files | No release artifact. Benchmark output is not uploaded |
| Pipeline | None | No workflow file changed. Terminal reconciliation belongs to Phase 6 |

## Gitignore

0 patterns added. Bench output uses `os.tmpdir()` or `--output-dir`. Existing ignores already cover `tests/benchmarks/results/` and `bench-results.json`.
