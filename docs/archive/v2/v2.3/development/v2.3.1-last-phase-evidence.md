# Last-Phase Evidence - v2.3.1 Phase 6

**Project**: Nexus AI Studio
**Plan**: [Installer Field Repair](../plans/v2.3.1-installer-field-repair.md)
**Date**: 2026-08-29
**Driver**: `/implement full` final phase (Phase 6 of 6)
**Branch**: `fix/v2.3.1-installer-field-repair`
**Package**: **2.3.0** during Phase 6; version bump waits for `/update release`

Each section quotes the proving command or scan per the fail-closed last-phase gate. This file is `v2.3.1-last-phase-evidence.md` so it does not overwrite v2.3.0's evidence file.

## Architecture refactor

`npm run cleanup:scan` (2026-08-29):

```
[cleanup-scanner] scanned C:\Users\bdour\Documents\Projects\Development\Nexus-AI at 2026-08-29T15:29:57.734Z
  note: no MemoryStore database found at .nexus/memory.db; skipping DB checks

  stale-cache-files:         0
  deleted-path-references:   0
  orphan-memory-rows:        0
  orphan-fts-rows:           0
  dangling-embeddings:       0

Total findings: 0
```

`npm run check:docs-layout`:

```
check-docs-layout: canonical layout OK (no docs/versions|docs/archive/versions wrappers)
```

Empty-directory scan (PowerShell, excluding `node_modules`, `out`, `dist`, `.git`, `target`, `__pycache__`, `coverage`, `.venv`, `.ruff_cache`):

```
.vscode-test\inspect-user\User
modules\coding\skills\catalog\__nonexistent_user__
modules\coding\skills\catalog\__none__
```

Those are local VS Code test state and skill-catalog placeholders. Propose-then-apply: no file moves. Confirmation was not requested because the scan did not propose a move.

Phase 6 code change: none beyond restoring the Phase 4 heading in `docs/archive/v2/v2.3/docs-cleanup-report.md` (the Phase 5 prepend had dropped it).

## Known-gaps reconciliation

Glob `docs/**/known-gaps.md` found 29 files (canonical `docs/v*/v*/` plus archive). No `docs/releases/**/known-gaps.md` tree exists.

| File | Status line | Disposition |
| ---- | ----------- | ----------- |
| `docs/archive/v2/v2.3/known-gaps.md` `## v2.3.1` | in-progress | This cycle. Open: MT-1 (`host_detect.py` 40%). No new NI/DF/BG/WN/QG this phase. |
| `docs/archive/v2/v2.3/known-gaps.md` `## v2.3.0` | finalized (2026-08-29) | Closed cycle. Open rows (DF-1..4, BG-1, WN-1, MT-1, QG-1, QG-2) stay for later `/plan` ingest. CI diffs below point at QG-1/QG-2 rather than duplicating them into v2.3.1. |
| `docs/archive/v2/v2.2/known-gaps.md` | finalized | Closed cycle. No v2.3.1 edit. |
| `docs/archive/v2/v2.1/known-gaps.md` | finalized | No v2.3.1 action. |
| `docs/archive/v2/v2.0/known-gaps.md` | in-progress | Hardware/audio/avatar DF rows remain that cycle's. Indexed, not closed here. |
| `docs/archive/v1/v1.20/known-gaps.md` | in-progress | DF-5 / DF-6 Docling/OCR. Indexed, not closed here. |
| `docs/archive/v1/v1.19/known-gaps.md` | in-progress | LFM/catalog DF rows. Indexed, not closed here. |
| `docs/archive/v1/v1.5`, `v1.3`, `v1.8`, `v1.9` | historical in-progress / COMPLETE | Cycle leftovers. Not rewritten. |
| Remaining `docs/archive/v1/*` and `docs/archive/v0/*` | finalized / closed / complete | No v2.3.1 action. |

v2.3.1 open items after this phase: MT-1 host_detect OS-specific probes. Packaged Windows screenshot proof is last-phase human QA, not a new gap id.

## Living docs architecture

- `docs/handbooks/` is absent. This repository's living tree is `docs/v*/`, `docs/adr/`, `docs/DEVLOG.md`, `docs/todos.md`, `docs/index.md`, and root `README.md`. Self-gate: `docs/testing/` and `docs/validation/` were not invented. `docs/decisions/` is absent; ADRs live in `docs/adr/`.
- `docs/adr/README.md` exists as the MADR index.
- `npm run check:docs-layout` passed (quoted above). Markdown is the source of truth; no generated `html/` handbook atlas exists to disagree with.
- Installer living copy for this plan: `docs/install.md`, README Quick Start, `docs/archive/v2/v2.3/development/vscode-host-policy.md`, `docs/archive/v2/v2.3/development/embedder-default-decision.md`. Historical CHANGELOG 2.3.0 notes were not rewritten.
- `docs/todos.md` still headlines v2.3.0. That is expected until `/update release`.

## Git-tree hygiene

`scripts/check_release_preconditions.py` does not exist (v2.3.0 DF-4). Report produced from git (report-only, nothing deleted):

```
HEAD: fix/v2.3.1-installer-field-repair
remote: https://github.com/bendourthe/Nexus-AI.git
merge-base with origin/develop: 67f3640ec8a1327fc2717ca53fa67b80b6724aa2
this branch commits not in origin/develop:
  3d35228 fix(installer): thread host RAM onto catalog badges
  ce85fe3 fix(installer): keep wizard alive when the install thread throws
  a0413bb fix(installer): paint catalog rows on card surfaces
  3e59fa5 fix(installer): offer VS Code 1.135 replace and move Unsloth
  7137827 fix(catalog): keep Nomic required and lock EmbeddingGemma 300M copy
  (plus this phase's final commit)
origin/develop not in this branch: (empty)
origin/main: a8377b3 chore(release): merge v2.3.0 into main
```

Merged historical `origin/feat/v1.*` branches remain on the remote. `branch-cleanup.yml` exists. Report only; no branch was deleted.

## CI/CD coverage

**Detect**: GitHub Actions (`.github/workflows/*.yml`, 19 workflow files). Not none.

Per-field comparison against cicd-architect (existing-pipeline mode). No workflow file was changed this plan. Silence is not approval. Proposed diffs below are the same topology recorded as v2.3.0 QG-1 and QG-2; they are not duplicated into v2.3.1.

| Field | Observable evidence | Result |
| ----- | ------------------- | ------ |
| Provider | `.github/workflows/ci.yml` and siblings | PASS |
| Profiles | `npm test`, `npm run test:shell`, `npm run lint`, `python -m pytest tests/python`, installer `uv run pytest` exist and are what CI invokes. Named `fast` / `full` / `platform` / `report` / `release` npm scripts are absent. | DIFF (v2.3.0 QG-1) |
| Events | `ci.yml` `push` (all branches except dependabot) plus `pull_request` to `main`. Feature-branch PRs into `develop` rely on the push trigger. | DIFF (v2.3.0 QG-2) |
| Runners | Hosted `ubuntu-latest`; Windows `init.ps1` on push only. No self-hosted untrusted runners. | PASS |
| Aggregate required check | No always-resolving aggregate job in `ci.yml`. | DIFF (v2.3.0 QG-2) |
| Permissions | `ci.yml` has no workflow-level `permissions`. | DIFF (v2.3.0 QG-2) |
| Immutable actions | SHA-pinned `actions/checkout@93cb6efe... # v5.0.1` and peers | PASS |
| Caching | `actions/setup-node` `cache: npm` | PASS |
| Concurrency | `ci.yml` `cancel-in-progress: true` | PASS |
| Path scoping | `installer-tests.yml` path-filters `scripts/installer/**`, `package.json`, `package-lock.json`, `core/registry/catalog.json`, `recommended.json`. `ci.yml` is not path-filtered. | PASS for installer tests |
| Artifact retention | `retention-days: 7` (coverage/build), 14, 30 on selected jobs | PASS |
| Structured reports | Coverage lcov uploaded from test-ts. No repository `reports/summary.md` profile directory. | DIFF folded into v2.3.0 QG-1 |
| Deployment boundaries | `release.yml` and `semantic-release.yml` are separate from pull-request CI | PASS |
| Failure recovery | `fail-fast: false` on node matrices | PASS |
| Installer parity | One Windows Python provisioning installer. macOS/Linux receive VSIX; raw Tauri bundles remain withheld. Cross-installer Video2X parity is a silent no-op. This plan did not add a second installer. | PASS (no-op) |

Proposed and not applied: named npm profile aliases; `permissions: contents: read` on `ci.yml`; an aggregate required job; add `develop` to `pull_request.branches`.

`npm run check:tampering`: `nexus-check: 0 findings`.
`npm run check:naming`: `check-no-devai-hub: clean`.

This plan's CI impact is already covered: installer pytest path filter, packaging engines-range assertion, catalog.json / catalog.test.ts in existing jobs. `scripts/build-vsix.ps1` Electron 42.8.1 pin was not changed.

## Goal-vs-codebase review

**Plan Goal restated**: A first-run Windows install keeps the window alive through model downloads, shows real RAM so patient-tier cards are gated honestly, paints catalog and progress rows on card surfaces instead of black window-color bars, and treats the VS Code step as an optional, tickable extension install (replace if already present) with Unsloth Core explained on Configuration.

Independent inspection of the tree (not the session notes):

| Goal clause | Artifacts | Verdict |
| ---------- | --------- | ------- |
| Window stays alive through model downloads, or fails in-window with a log path | `scripts/installer/src/nexus_installer/engine/crash.py` (`call_step`, `format_engine_exception`); `_InstallThread.run` emits `install_finished` and does not `sys.exit`; Installing page "Installation Stopped" | LANDED at automated evidence. Packaged windowed PyInstaller is last-phase human QA, not proven here. |
| Real RAM so patient-tier cards gate honestly | `InstallerState.total_ram_gb`; `detect_total_ram_gb` / ctypes `GlobalMemoryStatusEx`; catalog badges use `host_ram_gb=state.total_ram_gb`; RAM 0 copy is "RAM not detected" | LANDED at automated evidence. Operator 16 GB VRAM screenshot is last-phase human QA. |
| Catalog and progress rows on card surfaces, not `BG_WINDOW` bars | `theme.py`: `QMainWindow` uses `BG_WINDOW`; bare `QWidget` is `background: transparent`; `#cardHeaderRow` / `#phaseGroupDetails` transparent | LANDED at QSS assertions. Screenshot proof is last-phase human QA. |
| VS Code checkbox always visible; tickable on supported `code`; replace if present | `vscode-host-policy.md` decision A; `vscode_version_is_supported` 1.134/1.135; checkbox `setVisible(True)`; `--force` after list; `engines.vscode` `^1.134.0` (vsce-legal); wizard still disables 1.136+ | LANDED at automated evidence. Operator 1.135.0 host is last-phase human QA. |
| Unsloth Core explained on Configuration | `configuration.py` `_unsloth`; VS Code page has no `_unsloth` | LANDED |
| EmbeddingGemma 300M stays opt-in unless a recorded decision plus reindex | `embedder-default-decision.md` KEEP; `REQUIRED_EMBEDDER_ID`; catalog displayName "EmbeddingGemma 300M"; no 300B copy | LANDED |

No unresolved Goal miss without a recorded gap. Packaged visual proof is human QA below, not a silent pass and not a new known-gap id.

## Human/manual testing suggestions

These are suggestions only. They were not executed in this phase.

1. Packaged unsigned Windows wizard on the operator GPU host (16 GB VRAM): catalog name rows and installing lists sit on card surfaces, not black bars.
2. Same host: Inkling-Small badges Compatible (patient-tier) with real system RAM, not 0 GB next to a correct VRAM badge. Gemma 4 26B still reports the VRAM shortfall if over budget.
3. VS Code step: Microsoft stable `code` 1.135.0 shows a visible, tickable extension checkbox, default checked. Untick and retick. If Nexus is already installed, the label offers replace and install uses `--force`.
4. A host without `code`, or with Cursor / Insiders / 1.136.0: the checkbox stays visible and disabled. Copy names 1.134 or 1.135.
5. Configuration: Unsloth is off by default, explains NVIDIA 16 GB+ and Apache-2.0 / LGPL-3.0-or-later, and is absent from the VS Code page.
6. Start a multi-model install. The window must stay up. If a step fails, Complete or a modal shows the reason and a log path. `hf_token` must not appear in the crash log.
7. Embed tab: Nomic Embed Text is Required and locked. EmbeddingGemma is labeled 300M and is not required.

## Full-suite testing and stabilization

Local gate (2026-08-29 local clock), before publication. Numbers filled after the commands below finished:

- `npm run check:docs-layout`: pass (quoted above)
- `npm run cleanup:scan`: 0 findings
- `npm run check:tampering`: 0 findings
- `npm run check:naming`: `check-no-devai-hub: clean`
- `npm run lint`: pass (`eslint src modules`)
- `npm run lint:shell`: pass (`eslint src sidecar/src tests --max-warnings=0`)
- `npx tsc -b`: pass
- `python -m pytest tests/python -q`: **261 passed**
- Installer `uv run pytest -q`: **0 failed, 3 skipped** (Phase 5 coverage run: 85% lines)
- `npm test -- --coverage`: **Test Files 527 passed | 3 skipped (530)** / **Tests 5693 passed | 12 skipped (5705)** / All files **87.66%** statements and lines, **83.64%** branches, **90.45%** functions

Advisory model-prompting freshness: `scripts/check_model_prompting_freshness.py` does not exist. Recorded as a logged no-op (skill: never blocks, degrades offline / missing helper).

## Publication and integration

Pushed `fix/v2.3.1-installer-field-repair` to `origin` after explicit approval. Integration PR: https://github.com/bendourthe/Nexus-AI/pull/53 (base `develop`).

Resolved branching model: develop+main. Protected release branch is `main`. Integration target is `develop`. Remote is `origin` (`https://github.com/bendourthe/Nexus-AI.git`).

First remote CI (push SHA `9260c5d`, run https://github.com/bendourthe/Nexus-AI/actions/runs/33261033361):

- Pass: Lint TypeScript Node 22.x and 24.x; Test TypeScript Node 22.x and 24.x; Coverage gate (80%); Build TypeScript 22.x and 24.x; Installer tests + lint + smoke; Lint commit messages; docs/index.md sync; nexus-check; fast-bench; check-prompts; check-architecture; init.sh; init.ps1; pytest (runtimes/); npm audit (prod); npm audit (dev, non-blocking); pip-audit.
- Fail: Package VSIX (smoke), job https://github.com/bendourthe/Nexus-AI/actions/runs/33261033361/job/99123019426. Annotation: `Invalid vscode engine compatibility version '>=1.134.0 <1.136.0'`.

Local reproduction (vsce 2.24.0 `validateEngineCompatibility` regex `^\*$|^(\^|>=)?((\d+)|x)\.((\d+)|x)\.((\d+)|x)(\-.*)?$`):

```
bad >=1.134.0 <1.136.0 false
caret ^1.134.0 true
```

Stabilization: `engines.vscode` is `^1.134.0` (`VSCE_ENGINES_VSCODE`). Wizard still disables 1.136+ via `SUPPORTED_VSCODE_MINORS`. Packaging test locks the vsce regex so a compound range cannot return.

Follow-up CI (push SHA `f53a5f7`, run https://github.com/bendourthe/Nexus-AI/actions/runs/33261543213): every listed check succeeded, including Package VSIX (smoke) in 1m33s.

Merged to `develop` at `838268ff1697009ac47d35dee7e55c61da7fd6c4` on 2026-08-29 after explicit merge approval. Post-merge push to `develop`: CI success (`33263859447`), Installer tests success (`33263859408`), Shell Build ubuntu success (`33263859440`).

`/update release` prepares package **2.3.1**. Tag, GitHub Release, and merge to `main` remain confirmation-gated.
