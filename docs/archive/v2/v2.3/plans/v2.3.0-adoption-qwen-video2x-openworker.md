# Plan -- Local Video Enhancement and Security Audit Intake

**Project**: Nexus AI Studio
**Version**: v2.3.0
**Slug**: adoption-qwen-video2x-openworker
**Filename**: v2.3.0-adoption-qwen-video2x-openworker.md
**Plan Type**: Feature / Enhancement (from comparison)
**Status**: Complete - integrated to develop (PR 52); package 2.3.0 release preparation
**Created**: 2026-08-27
**Confirmed**: 2026-08-27
**Seeded from**: `docs/archive/v2/v2.3/comparisons/v2.3.0-comparison-qwen-video2x-openworker.md`
**Reverse-engineer-first**: true
**Goal**: By the end of v2.3.0, Video Lab offers an optional, local, cancellable enhancement workflow that can upscale and/or interpolate a completed generation without replacing the original, records complete provenance, fails honestly on unsupported hardware or missing backends, and has measured quality and resource evidence; Qwen3.8-Flash-Next remains excluded behind explicit catalog-admission gates; and OpenWorker's useful security-workflow refinements have a versioned Nexus-Hub handoff without importing OpenWorker, cloud services, or unsafe auto-approval.

## Overview

This plan turns the comparison's highest-value local capability into a Nexus feature. Video enhancement is added as a post-generation child job, not as a new generation engine. The internal service stays backend-neutral, the first adapter may call a separately installed Video2X executable on supported Windows/Linux hardware, and no Video2X binary or AGPL code is bundled until a distribution review explicitly permits it. The workflow preserves the original output, validates the enhanced media, and tells users that super-resolution and interpolation synthesize detail and frames rather than recover ground truth.

The plan also closes the other two comparison decisions without expanding product scope. Qwen3.8-Flash-Next is documented as a rejected v2.3.0 catalog candidate with measurable future admission gates. OpenWorker's new security coworkers are not integrated; instead, a release-scoped handoff specifies the small scanner-coverage and independent-rescan improvements that belong upstream in Nexus-Hub. Nexus-AI consumes those changes later through the existing versioned Hub sync rather than modifying a sibling repository under the wrong commit history.

Success is a packaged local workflow with automated contract, queue, UI, metadata, and benchmark evidence. Human perceptual review and platform-specific manual checks wait until the mandatory final phase, after the implementation is complete.

## Goals First

**Target problem**: Users can generate a local video but cannot improve low-resolution or low-frame-rate output inside Nexus, while experimental model and external-agent announcements create pressure to add capabilities before their licensing, hardware, and security evidence fit the product.

**Target persona**: A privacy-focused developer or creator using Nexus on a consumer Windows, Linux, or macOS computer who wants honest local enhancement without uploading media or losing the original generation.

**Observable definition of done**: A supported machine can enhance a completed Video Lab output locally with upscale, interpolation, or both; progress and cancellation work; the result is separately downloadable and provenance-linked; unsupported cases fail closed with actionable copy; benchmark evidence records quality, duration, and resource use; Qwen3.8 is absent from user-facing catalogs; and the Nexus-Hub security refinement has an upstream-ready handoff with no cross-repository write hidden in this plan.

**Strategy grounding**: No `STRATEGY.md` or version-scoped strategy anchor exists. The scope is grounded in the repository's AGENTS.md local-first product description and the comparison report. No `docs/solutions/` store exists, so no solved-problem entry changed the phase design.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at docs/<version>/constitution.md - skipping check. Recommend running /constitution to establish project principles.

## Current model map

**Model map status**: fresh as of 2026-08-27; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.5-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

Map JSON validated on 2026-08-27 with `model-routing/scripts/model-map.ps1` (`valid: true`, four tiers, four providers). Cursor remains a manual picker surface for implementation-time switching.

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/about-claude/models/overview
- OpenAI: https://developers.openai.com/api/docs/models/all
- Google: https://ai.google.dev/gemini-api/docs/models, https://ai.google.dev/gemini-api/docs/models/gemini-3.7-flash
- Cursor: https://cursor.com/docs/models-and-pricing, https://cursor.com/help/models-and-usage/grok-4-6

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|---|---|---|---|---|
| 1 | Admission, Licensing, and Upstream Handoff | Locks the Qwen non-admission gates, Video2X boundary, service contract, and Nexus-Hub security handoff before code | frontier | max |
| 2 | Safe Video Enhancement Core | Adds backend-neutral types, capability detection, and a guarded optional local adapter | frontier | max |
| 3 | Queue, Output Lifecycle, and Provenance | Runs enhancement as a cancellable child job and validates separately stored, provenance-linked output | frontier | max |
| 4 | Video Lab Enhance Experience | Adds accessible Enhance actions, presets, progress, errors, and preserved-original UX | strong | high |
| 5 | Quality, Performance, and Packaging Evidence | Proves the supported envelope, documents setup and fallback behavior, and prevents unsupported packaging claims | frontier | max |
| 6 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Reconciles architecture, evidence, pipeline coverage, human testing, publication, and integration | frontier | max |

## Task Inventory

- [x] T001 [US3] Write the Nexus-Hub Security Audit upstream handoff docs/archive/v2/v2.3/development/nexus-hub-security-audit-handoff.md
- [x] T002 [US2] Record Qwen3.8-Flash-Next catalog admission gates docs/archive/v2/v2.3/development/model-admission-qwen38.md
- [x] T003 [US1] Resolve the video enhancement backend, license, platform, and service contract docs/archive/v2/v2.3/development/video-enhancement-contract.md
- [x] T004 [US1] Verify Phase 1 decisions and record CI impact docs/archive/v2/v2.3/development/history/2026-08-27_v2.3.0-phase-1-decisions.md
- [x] T005 [US1] Implement backend-neutral video enhancement types and service core/video/VideoEnhancement.ts
- [x] T006 [US1] Implement capability probing and the guarded Video2X process adapter desktop/sidecar/src/video/Video2xAdapter.ts
- [x] T007 [US1] Test and stabilize the enhancement core tests/unit/core/video/VideoEnhancement.test.ts
- [x] T008 [US1] Add enhancement child-job queue and cancellation semantics desktop/sidecar/src/video/VideoEnhancementRuntime.ts
- [x] T009 [US1] Extend video provenance and output validation core/video/WorkflowMetadata.ts
- [x] T010 [US1] Test and stabilize queue, lifecycle, and provenance desktop/tests/video-enhancement-runtime.test.ts
- [x] T011 [US1] Add Video Lab Enhance actions and presets desktop/src/modules/video/VideoLabPage.tsx
- [x] T012 [US1] Add enhancement status, typed errors, and accessible interaction copy desktop/src/modules/video/VideoEnhancementPanel.tsx
- [x] T013 [US1] Test and stabilize the Video Lab enhancement experience desktop/tests/VideoLabPage.test.tsx
- [x] T014 [US1] Add deterministic enhancement quality and resource benchmarks scripts/bench-video-enhancement.mjs
- [x] T015 [US1] Document supported platforms, setup, packaging, and fallback behavior docs/archive/v2/v2.3/benchmarks/video-enhancement-baseline.md
- [x] T016 [US1] Test and stabilize benchmark and packaging gates tests/unit/video/video-enhancement-packaging.test.ts
- [x] T017 Refactor architecture and record evidence docs/archive/v2/v2.3/development/last-phase-evidence.md
- [x] T018 Reconcile known gaps across active releases docs/archive/v2/v2.3/development/last-phase-evidence.md
- [x] T019 Verify living documentation architecture docs/archive/v2/v2.3/development/last-phase-evidence.md
- [x] T020 Report git-tree hygiene docs/archive/v2/v2.3/development/last-phase-evidence.md
- [x] T021 Reconcile terminal CI/CD coverage docs/archive/v2/v2.3/development/last-phase-evidence.md
- [x] T022 Perform an independent Goal-vs-codebase review docs/archive/v2/v2.3/development/last-phase-evidence.md
- [x] T023 Run last-phase-only human testing docs/archive/v2/v2.3/development/last-phase-evidence.md
- [x] T024 Run full local stabilization docs/archive/v2/v2.3/development/last-phase-evidence.md
- [x] T025 Publish and integrate the completed branch docs/archive/v2/v2.3/development/last-phase-evidence.md

---

## Phase 1: Admission, Licensing, and Upstream Handoff

**Goal**: Resolve every consequential boundary before implementation: OpenWorker security refinements go upstream to Nexus-Hub, Qwen3.8 stays out until explicit gates pass, and local video enhancement has a backend-neutral contract with a lawful first-release delivery path.
**Prerequisites**: None.
**Stability Gate**: The three decision documents exist, cite the comparison, contain no unresolved implementation-blocking question, and the video contract names supported/unsupported platforms, failure modes, process boundaries, original-file preservation, and provenance fields.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Signal scores were scope medium, structural complexity high, context volume high, risk high, and reasoning high; licensing, security, and cross-repository ownership make uncertainty unacceptable.

### Sub-tasks

#### 1.1 - Nexus-Hub Security Audit upstream handoff (T001)

**Objective**: Specify the skill-native OpenWorker refinements in the repository that owns them without modifying Nexus-Hub from a Nexus-AI phase.

**Prompt**:
> Read `docs/archive/v2/v2.3/comparisons/v2.3.0-comparison-qwen-video2x-openworker.md`, the sibling Nexus-Hub `catalog/agents/security-reviewer.md`, `data/workflows.json`, and the existing security-review, dependency-security-audit, cve-reachability-analyzer, cloud-security-posture-detection, security-patch-advisor, and pre-commit-checklist skills. Write `docs/archive/v2/v2.3/development/nexus-hub-security-audit-handoff.md` with an upstream-ready scope: a Security Audit preset, scanner receipts using `RAN`, `NOT_APPLICABLE`, `UNAVAILABLE`, `FAILED`, or `DECLINED` plus required reasons, aggregate degraded coverage, optional local recipes for Semgrep, gitleaks, OSV-Scanner, Trivy config, and Checkov, and an independent post-fix rescan/diff review. State that this Nexus-AI phase does not edit or commit the sibling repository. Name the Nexus-Hub files an upstream plan would own, acceptance criteria, security constraints, and the Nexus-Hub version evidence Nexus-AI would require before sync. Treat `data/bundles.json` as install-selector parity only. Reject connectors, cloud models, and reviewer auto-approval.

#### 1.2 - Qwen3.8 catalog admission decision (T002)

**Objective**: Record a fail-closed, evidence-based decision that prevents accidental installer or desktop admission.

**Prompt**:
> Write `docs/archive/v2/v2.3/development/model-admission-qwen38.md` from the comparison's Qwen section. Record the official 125B main model/6B activated-per-token shape plus 51B n-gram embeddings and 4B multi-token-prediction parameters, the 105-360 GB artifact envelope, current Ollama MLX labeling and newly released but unproven llama.cpp path, Qwen Community License review requirement, inconsistent artifact-license provenance, current runtime/tool-parser evidence, and the six future admission gates. Assert that v2.3.0 makes no `core/registry/catalog.json`, installer, Settings, or recommended-tier entry. Define a future re-evaluation checklist that requires official sources, a stable cross-platform artifact, hardware checks, and a passing Nexus tool-calling benchmark. Do not state a legal conclusion.

#### 1.3 - decision: video enhancement backend, license, platform, and service contract (T003)

**Objective**: Select the smallest lawful first-release backend boundary and define the load-bearing internal contract that later phases implement.

**Prompt**:
> Write `docs/archive/v2/v2.3/development/video-enhancement-contract.md` as a decision record. Compare three options: separately installed Video2X executable adapter, bundled Video2X, and a permissively licensed native Real-ESRGAN/RIFE subset. Record evidence that would settle licensing and packaging, then choose the separately installed adapter for the first release unless a documented distribution review has already approved another option. Define `VideoEnhancementRequest`, preset/mode, capability result, progress event, cancellation, typed result/error, source/output identity, provenance fields, platform support, configuration path (`NEXUS_VIDEO2X_PATH` or equivalent typed setting), subprocess argument handling, timeouts, and output validation. Preserve originals. Reject shell-string execution and automatic download. Name the later tasks this decision unblocks.

**Failure modes**: Malformed or absent requests fail schema validation before a job is queued; a missing, incompatible, or slow backend returns a typed unavailable/timeout result without deleting output; conflicting enhancement requests for the same source receive distinct job/output identities and never overwrite each other.

**Build class**: load-bearing, because every runtime and UI phase depends on this contract.

#### 1.4 - Testing and Stabilization (T004)

**Objective**: Verify the decision artifacts are complete and internally consistent before code begins.

**Prompt**:
> Validate links and ASCII safety for the three Phase 1 documents. Add a focused contract checklist or documentation test that proves every comparison acceptance criterion is assigned to a later phase and that `core/registry/catalog.json` contains no `qwen3.8-flash-next` entry. Re-read the Constitution Check after the design is fixed. Run the relevant scoped checks, fix failures, and do not proceed until the decisions are stable. Record Phase 1 CI IMPACT against `[[cicd-architect]]` without running remote CI: name any new command, dependency, environment variable, test path, or artifact and whether existing CI covers it. Do not edit pipeline files unless CI/CD is the explicit deliverable. Do not ask for human testing. Generate the Phase 1 session history at `docs/archive/v2/v2.3/development/history/2026-08-27_v2.3.0-phase-1-decisions.md`, then create one local ASCII-only commit. Do not push, open a pull request, or start remote CI.

### Phase 1 Exit Checklist

- [x] Phase task range T001-T004 complete
- [x] Qwen3.8 non-admission and future gates are explicit
- [x] Video enhancement contract and license boundary are resolved
- [x] Nexus-Hub work has an upstream-only handoff
- [x] Automated document/contract checks pass
- [x] CI impact is recorded with no remote CI run
- [x] Session history is generated
- [x] One local commit exists and nothing was pushed

---

## Phase 2: Safe Video Enhancement Core

**Goal**: Add a backend-neutral enhancement service, capability detection, and a guarded adapter for a separately installed local Video2X executable.
**Prerequisites**: Phase 1.
**Stability Gate**: Unit and process-adapter tests prove typed requests/results, deterministic presets, safe argument arrays, executable/version/capability detection, timeout and cancellation, canonical paths, no network use during enhancement, and no source overwrite.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Signal scores were scope high, structural complexity high, context volume medium, risk high, and reasoning high because native-process execution and untrusted media create a broad safety boundary.

### Sub-tasks

#### 2.1 - Backend-neutral enhancement types and service (T005)

**Objective**: Implement the contract without binding core code to Video2X or a platform-specific executable.

**Prompt**:
> Implement `core/video/VideoEnhancement.ts` from the Phase 1 contract. Define typed requests for upscale, interpolation, and combined modes; model-compatible animation 2x/4x, general 4x, and integer 2x interpolation presets; capability and backend descriptors; progress/cancellation; staged output identity; and typed success/failure results. Keep the new `core/video/VideoEnhancement.ts` and its narrow index export free of desktop, module, child-process, and Video2X imports; the existing `WorkflowMetadata.ts` process adapter is outside this new-file boundary. Validate mutually exclusive fields and resource bounds. Do not expose arbitrary target-resolution or 48/60 fps fields that the pinned backend cannot map honestly. Export the contract through a narrow core video index that does not re-export process-bound metadata helpers. Add unit tests under `tests/unit/core/video/VideoEnhancement.test.ts`.

**Failure modes**: Malformed dimensions, fps, paths, or incompatible mode fields return typed validation errors; an unreachable backend is represented as unavailable without retrying in core; concurrent requests use immutable request/job identities and cannot share mutable output state.

**Build class**: load-bearing, because it is the vendor-neutral boundary future backends must implement.

#### 2.2 - Capability probe and guarded Video2X adapter (T006)

**Objective**: Detect and invoke a user-installed Video2X executable without shell interpolation, hidden downloads, or unsupported platform claims.

**Prompt**:
> Create `desktop/sidecar/src/video/Video2xAdapter.ts` and the smallest supporting files. Resolve an absolute executable from `NEXUS_VIDEO2X_PATH` or a typed setting without searching `PATH`, canonicalize it, and pin Video2X 6.4.0 through exact `--version`, `--help`, and `--list-devices` probes plus separate model readiness. Parse tagged device types and prefer the lowest discrete-GPU ID, otherwise the lowest integrated-GPU ID; fail closed on CPU, virtual, unknown, or malformed devices. Expose a capability result that distinguishes missing executable, unsupported OS/architecture/AVX2/Vulkan, incompatible version/grammar, unavailable or unverified model, and ready. Spawn with an argument array and shell disabled through a process group or the Phase 1 argv-safe Windows host. Map only the Phase 1 presets, return a private staged result rather than public completion, redact sensitive paths from user-visible errors, enforce timeout and authoritative cancellation even when the child exits 0, isolate working/temporary/output paths under the generation job, scrub loader/Vulkan overrides, and validate that the process cannot overwrite the source. Never install or download Video2X. Mock process execution in tests; do not require Video2X or a GPU in CI.

**Failure modes**: Missing or malformed executable paths return typed unavailable results; a hung or crashing process is terminated and leaves the source intact with partial output quarantined or removed by exact path; simultaneous adapter runs receive isolated job directories and cancellation affects only its own process tree.

**Build class**: load-bearing adapter behind the core contract; it can be replaced by a permissive native backend without UI changes.

#### 2.3 - Testing and Stabilization (T007)

**Objective**: Prove the enhancement core and adapter are safe before queue or UI integration.

**Prompt**:
> Add and run unit tests for request validation, every preset mapping, capability classification, canonical path enforcement, shell-disabled argument passing, timeout, cancellation, process exit codes, partial output cleanup, and concurrent job isolation. Include adversarial paths with spaces and metacharacters. Use deterministic fake executables/process adapters rather than invoking a real binary. Run the relevant lint, typecheck, and tests until green. Record Phase 2 CI IMPACT against `[[cicd-architect]]` without remote CI, including the optional environment variable and new test paths. Do not edit pipeline files or ask for human testing. Generate Phase 2 session history, then create one local ASCII-only commit. Do not push, open a pull request, or start remote CI.

### Phase 2 Exit Checklist

- [x] Phase task range T005-T007 complete
- [x] `core/video/VideoEnhancement.ts` and its narrow index export have no Video2X, desktop, module, or child-process dependency
- [x] Adapter uses shell-disabled argument arrays and exact paths
- [x] Missing/unsupported backends fail closed
- [x] Automated tests, lint, and typecheck pass
- [x] CI impact and session history are recorded
- [x] One local commit exists and nothing was pushed

---

## Phase 3: Queue, Output Lifecycle, and Provenance

**Goal**: Run enhancement as a first-class child job linked to a completed video generation, with progress, cancellation, validated output, durable provenance, and preserved originals.
**Prerequisites**: Phase 2.
**Stability Gate**: Integration tests prove a completed video can enqueue one or more isolated enhancement children, observe progress, cancel safely, resume application state without false success, validate the media with ffprobe, and index a new output linked to the source hash and workflow.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Signal scores were scope high, structural complexity medium, context volume high, risk high, and reasoning high because queue persistence, subprocess lifecycle, media validation, and provenance cross several load-bearing modules.

### Sub-tasks

#### 3.1 - Enhancement child-job runtime (T008)

**Objective**: Extend the existing generation job infrastructure instead of creating a second queue.

**Prompt**:
> Create `desktop/sidecar/src/video/VideoEnhancementRuntime.ts` and integrate it through the existing generation queue/studio runtime. Represent enhancement as a child of a completed video generation with source job/output ids, immutable source path/hash, preset, backend, progress, timestamps, cancellation, and typed terminal state. Reject enhancement of incomplete, missing, or non-video outputs. Permit multiple distinct enhancement children without output collision. On restart, do not report an interrupted process as successful; mark it interrupted/retryable according to the existing queue contract. Keep generation and enhancement scheduling resource-aware so two GPU-heavy jobs do not bypass the GPU coordinator.

**Failure modes**: Missing or malformed parent/output data rejects enqueue; an unreachable/slow adapter produces a typed failed/timeout state with retry metadata; duplicate requests either deduplicate by an explicit idempotency key or create separately named outputs, never overwrite or cross-cancel.

**Build class**: load-bearing extension of the existing generation lifecycle.

#### 3.2 - Provenance and validated output lifecycle (T009)

**Objective**: Create a separately indexed enhanced file whose lineage and transformation are recoverable.

**Prompt**:
> Extend `core/video/WorkflowMetadata.ts` and the generation provenance/index path with an enhancement record containing source generation id, source hash, backend/version/executable hash and trust status, model/algorithm, scale or frame multiplier, source/output fps, normalized argument fields, start/end time, and outcome. Prove and use a compatible ffmpeg/ffprobe path to validate container, duration, resolution, fps, and readable streams before declaring success; the existing provisioner alone is not runtime-wiring evidence. Preserve the original byte-for-byte. Store enhanced output under a collision-safe derived name and expose both files to the session only after atomic promotion and durable indexing. If metadata embedding fails after valid media is produced, return a typed provenance failure and keep the artifact quarantined from normal success presentation until repaired.

**Failure modes**: Malformed or absent source metadata blocks lineage creation; unavailable/slow ffprobe returns a typed validation failure and never promotes an unverified file; concurrent metadata writes use atomic output identity and cannot attach one child's provenance to another file.

**Build class**: load-bearing evidence boundary for generated and transformed media.

#### 3.3 - Testing and Stabilization (T010)

**Objective**: Verify job lifecycle, persistence, cancellation, output validation, and lineage end to end without a real GPU backend.

**Prompt**:
> Add integration tests using a fake enhancement backend and tiny deterministic video fixtures. Cover completed-parent enforcement, child identity, progress, cancellation, timeout, restart/interruption, resource coordination, multiple children, source preservation, ffprobe validation outcomes, atomic provenance, and generation-index visibility. Run scoped lint, typecheck, and tests until stable. Record Phase 3 CI IMPACT against `[[cicd-architect]]` without remote CI, including fixtures and artifacts. Do not change pipeline files or ask for human testing. Generate Phase 3 session history, then create one local ASCII-only commit. Do not push, open a pull request, or start remote CI.

### Phase 3 Exit Checklist

- [x] Phase task range T008-T010 complete
- [x] Enhancement uses the existing generation queue and GPU coordination
- [x] Original outputs remain byte-identical
- [x] Enhanced media and provenance are validated before success
- [x] Restart, timeout, cancellation, and concurrency tests pass
- [x] CI impact and session history are recorded
- [x] One local commit exists and nothing was pushed

---

## Phase 4: Video Lab Enhance Experience

**Goal**: Let users discover, configure, run, monitor, cancel, and download local enhancement from a completed Video Lab output with honest capability and quality copy.
**Prerequisites**: Phase 3.
**Stability Gate**: Component tests prove Enhance appears only for eligible completed videos, unsupported states explain why, presets create the expected typed request, progress/cancel map to the correct child job, original and enhanced downloads remain distinct, and all controls have keyboard access and accessible names.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Signal scores were medium across scope, structure, context, risk, and reasoning; the UI composes known patterns but spans state, accessibility, and asynchronous job feedback.

### Sub-tasks

#### 4.1 - Enhance actions and preset panel (T011)

**Objective**: Add the user-facing entry point and preset selection without obscuring the original generation workflow.

**Prompt**:
> Extend `desktop/src/modules/video/VideoLabPage.tsx` with an Enhance action beside completed video workflow/download actions. Add `desktop/src/modules/video/VideoEnhancementPanel.tsx` using existing field, button, dialog/sheet, and queue patterns. Offer the capability-backed animation 2x/4x, general 4x, Smooth 2x, and combined modes from the Phase 1 contract. Display derived output resolution and rational fps; do not offer arbitrary target-resolution or 48/60 fields. Route presets through the typed sidecar client; do not construct backend flags in React. State plainly that enhancement synthesizes pixels/frames, may create artifacts, takes additional local compute, and never replaces the original. Hide or disable unsupported combinations based on capability data, with a visible reason.

**Failure modes**: Missing/malformed capability or output data disables submission with typed copy; a slow/unreachable sidecar retains the draft and reports retryable failure; simultaneous panels remain bound to their own source/output and cannot submit against a newly selected video by accident.

**Build class**: load-bearing user surface over the backend-neutral contract.

#### 4.2 - Progress, cancellation, results, and accessibility (T012)

**Objective**: Make long-running local work understandable and controllable, including failure and multiple-output states.

**Prompt**:
> Connect enhancement child jobs to the existing GenerationQueueBar or the smallest shared extension. Show stage, percent when known, elapsed time, backend, and derived output resolution/fps, plus Cancel. Present typed missing-backend, unsupported-hardware, timeout, validation, and provenance failures with actionable setup or retry copy. When successful, render the enhanced video as a separate message/output with its own Download and workflow/provenance action while keeping the original visible. Add keyboard focus management, accessible names, live-region status updates that do not spam, reduced-motion handling, and no color-only status. Do not claim real-time processing or recovered true detail.

**Failure modes**: Malformed/absent progress falls back to indeterminate status rather than false percentages; disconnected/slow job polling preserves the last truthful state and exposes retry; cancel/result races resolve from the authoritative terminal job state and never show both cancelled and successful.

#### 4.3 - Testing and Stabilization (T013)

**Objective**: Prove the complete Video Lab interaction with automated component and protocol tests.

**Prompt**:
> Extend `desktop/tests/VideoLabPage.test.tsx` and supporting protocol/client tests for eligible/ineligible outputs, every preset, capability changes, pending/progress/cancel/success/failure states, multiple enhancements, distinct downloads, preserved original, workflow/provenance actions, keyboard navigation, accessible names, live-region behavior, and reduced motion. Run desktop lint, typecheck, and scoped tests until green. Record Phase 4 CI IMPACT against `[[cicd-architect]]` without remote CI. Do not edit pipeline files or ask for human testing. Generate Phase 4 session history, then create one local ASCII-only commit. Do not push, open a pull request, or start remote CI.

### Phase 4 Exit Checklist

- [x] Phase task range T011-T013 complete
- [x] Enhance is available only for eligible completed video outputs
- [x] Honest limitation, setup, and failure copy is present
- [x] Progress, cancellation, separate outputs, and accessibility work in tests
- [x] Automated desktop checks pass
- [x] CI impact and session history are recorded
- [x] One local commit exists and nothing was pushed

---

## Phase 5: Quality, Performance, and Packaging Evidence

**Goal**: Define and prove the supported enhancement envelope with deterministic automated fixtures, resource benchmarks, installer/runtime detection parity, and no unreviewed binary bundling.
**Prerequisites**: Phase 4.
**Stability Gate**: A reproducible benchmark report records source/output properties, duration, peak resources, backend/version, and typed failures for the declared support matrix; packaging tests prove Nexus detects but does not bundle/download Video2X and unsupported platforms remain honest.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Signal scores were scope high, structural complexity medium, context volume high, risk high, and reasoning high because hardware-dependent evidence and packaging/license claims have release-wide consequences.

### Sub-tasks

#### 5.1 - Deterministic quality and resource benchmark harness (T014)

**Objective**: Measure what enhancement actually costs and produces without turning subjective quality claims into unsupported marketing.

**Prompt**:
> Add `scripts/bench-video-enhancement.mjs` with tiny versioned 480p and 720p fixtures representing photoreal, animation, text/edges, and motion/occlusion. Support a fake deterministic backend for CI contract checks and an explicitly invoked real-backend mode for local hardware measurement. Record source/output resolution and fps, frame count/duration, wall time, peak CPU/RAM/GPU/VRAM when observable, backend/model/version, output size, validation result, and typed failure. Add automated invariants for readable output, expected dimensions/fps, duration tolerance, source preservation, and no NaN/zero-frame results. Do not invent a perceptual-quality score; human artifact review belongs only in Phase 6.

**Failure modes**: Malformed/missing fixtures fail before measurement; unavailable/slow telemetry or backend records `not observed` or a typed timeout rather than fabricated zeros; concurrent benchmark runs use isolated output dirs and cannot overwrite a baseline.

**Build class**: load-bearing evidence harness for support and future backend decisions.

#### 5.2 - Support matrix, setup, and packaging gates (T015)

**Objective**: Ensure the installer, packaged app, settings, and documentation make the same truthful claim about the optional backend.

**Prompt**:
> Write `docs/archive/v2/v2.3/benchmarks/video-enhancement-baseline.md` from observed benchmark data and explicitly label untested combinations `not proven here`. Document the supported Windows/Linux CPU/Vulkan/backend envelope, macOS status, setup path, expected disk/resource costs, limitations, and troubleshooting. Update the installer/desktop configuration surfaces only as needed to detect or select a separately installed executable; do not download or bundle Video2X. Add a declarative parity test proving installer copy, desktop capability copy, environment/setting names, and runtime detection agree. Preserve the existing FFmpeg provisioner as the media probe/mux dependency. Record the Nexus-Hub security handoff status and consume an upstream Hub version only if a separate Nexus-Hub release supplies the required scanner-coverage and rescan evidence; otherwise record a known gap without editing the sibling repository.

**Failure modes**: Missing/malformed configuration is presented as optional-backend unavailable; slow/unreachable capability detection times out without blocking app startup; concurrent installer/settings changes resolve through one canonical setting contract and cannot advertise different support states.

#### 5.3 - Testing and Stabilization (T016)

**Objective**: Verify benchmarks, support claims, optional dependency behavior, and packaged configuration before the final release gate.

**Prompt**:
> Run fake-backend benchmark tests, packaging/configuration parity tests, installer scoped tests, desktop runtime tests, lint, and typecheck. Verify repository artifacts contain no Video2X binary, copied AGPL source, automatic download URL, or unreviewed model bundle. Verify `core/registry/catalog.json` still has no Qwen3.8 entry and the Hub handoff remains cross-repository honest. Iterate until stable. Record Phase 5 CI IMPACT against `[[cicd-architect]]` without remote CI, including benchmark commands, fixtures, optional settings, and artifact expectations. Do not edit pipeline files unless Phase 5 explicitly owns a packaging gate; do not ask for human testing. Generate Phase 5 session history, then create one local ASCII-only commit. Do not push, open a pull request, or start remote CI.

### Phase 5 Exit Checklist

- [x] Phase task range T014-T016 complete
- [x] Benchmark evidence distinguishes observed, failed, and not-proven cases
- [x] Installer, desktop, docs, and runtime support claims match
- [x] No Video2X binary, AGPL source, automatic download, or Qwen3.8 catalog entry exists
- [x] Hub refinement is either version-evidenced or an explicit known gap
- [x] Automated benchmark/packaging checks pass
- [x] CI impact and session history are recorded
- [x] One local commit exists and nothing was pushed

---

## Phase 6: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, its living docs architecture current, and its pipeline reconciled against the canonical contract, with independent evidence that the plan Goal landed, then publish and integrate the completed branch.
**Prerequisites**: All prior phases.
**Stability Gate**: `docs/archive/v2/v2.3/development/last-phase-evidence.md` exists with one quoted section per duty below. A missing Goal, a Goal-review miss without a recorded known-gap, or a missing evidence section blocks `/update release`.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, Goal-vs-codebase review, known-gap reconciliation, and release gating carry high context volume and blast radius.

### Sub-tasks

#### 6.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout and quote the scan.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure, then refactor toward a clean, intuitive layout via [[project-refactor]] and [[docs-layout-refactor]] (propose-then-apply, with confirmation; repair every reference for anything that moves). Write the detector output into `docs/archive/v2/v2.3/development/last-phase-evidence.md` under `## Architecture refactor`. An empty finding is allowed only when the scan output is quoted.

#### 6.2 - Known-gaps reconciliation
**Objective**: Reconcile this version and every other still-open known-gaps file.
**Prompt**:
> Via [[known-gaps-tracker]], reconcile this version's known gaps AND every other `docs/**/known-gaps.md` whose Status is in-progress or whose Open Items remain. Glob both canonical (`docs/releases/v<MAJOR>/v<MAJOR>.<MINOR>/known-gaps.md`) and legacy layouts. If a file is unreachable, record the glob result and continue with what was found. Write the disposition into the evidence file under `## Known-gaps reconciliation`.

#### 6.3 - Living docs architecture
**Objective**: Prove handbooks, decisions, and generated HTML match the required living tree.
**Prompt**:
> Check `docs/handbooks/` (markdown source of truth, generated `html/`, one atlas walkthrough, technical companions for key components), `docs/decisions/`, and living `docs/README.md` / `docs/DEVLOG.md` / `docs/todos.md`. Self-gated: never invent `docs/testing/` or `docs/validation/`. Markdown wins if HTML disagrees; regenerate or fail the check. Quote the check in the evidence file under `## Living docs architecture`. Details live in [[docs-layout-refactor]].

#### 6.4 - Git-tree hygiene
**Objective**: Report branch and repository-settings hygiene without deleting anything.
**Prompt**:
> Run `python scripts/check_release_preconditions.py --branches --repo-settings`. Quote the output in the evidence file under `## Git-tree hygiene`. Report only; never delete branches.

#### 6.5 - Terminal CI/CD reconciliation
**Objective**: The repository's pipeline is compared field by field against the canonical contract, and every difference is either applied with approval or recorded as a known gap.
**Prompt**:
> Invoke [[cicd-architect]] in existing-pipeline comparison mode and run its six steps in order: DETECT the active CI provider (recording "none detected" rather than assuming one); COMPARE the existing pipeline against every field of the canonical contract - repository-native profiles, event separation, runner selection, the always-resolving aggregate required check, permissions, immutable action or plugin references, caching, concurrency, path scoping, artifact retention, structured reports, deployment boundaries, and failure recovery; PROPOSE each difference with its cost, its risk, and the smallest change that closes it; obtain explicit APPROVAL per change (silence is not approval); APPLY the approved changes and re-run the local gate; and RECORD every declined or environment-only difference as a known gap with an owner and a next step via [[known-gaps-tracker]]. The comparison concludes PASS only when every required field has observable evidence; a green pipeline run is not evidence, because a pipeline that checks nothing is green for the wrong reason. If this repository ships more than one installer, run or add a declarative cross-installer parity gate covering distributed artifacts, supported platforms, named capability counterparts, and external-tool fallbacks, then execute the real installers on their target operating systems with identical postconditions. Cross-link [[platform-contract-verification]] so discovery and delivery are checked in the same pass. If this repository ships zero or one installer, record a silent no-op and continue. Quote the per-field comparison result in the evidence file under `## CI/CD coverage`.

#### 6.6 - Independent Goal-vs-codebase review
**Objective**: Prove the plan Goal landed in the codebase, not that checkboxes were ticked.
**Prompt**:
> Re-read the plan header Goal and the Goals First / Definition of Done. Inspect the codebase as if this agent had not implemented the phases. Write `## Goal-vs-codebase review` in `docs/archive/v2/v2.3/development/last-phase-evidence.md` listing: the plan Goal restated; the code/docs artifacts that satisfy it; and any gap. A missing or malformed Goal is a fail-closed finding that names the missing Goal. Completing every prior-phase sub-task is not evidence the Goal landed. A miss is a blocking finding or a new known-gap, not a pass.

#### 6.7 - Human/manual testing suggestions (last phase only)
**Objective**: Ask a human to exercise only what automated tests cannot cover, now that the feature is complete.
**Prompt**:
> Emit human/manual testing suggestions for user-facing workflows, external integrations, and environment-specific cases that automated tests cannot cover. Include perceptual review of faces, text, animation edges, repeated texture, fast motion, and occlusion; Video2X setup on supported Windows/Linux hardware; truthful unsupported behavior on macOS or non-Vulkan hardware; cancellation under load; and comparison of original versus enhanced downloads. Do not invent a fake walkthrough. This duty exists only on the last phase.

#### 6.8 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and the complete LOCAL gate is green before anything is published.
**Prompt**:
> Run the full local validation/test suite, confirm the refactor changed no behavior, and iterate until clean. This gate is local by construction: it must pass before the branch is published, so it cannot depend on a remote run. Quote the suite in the evidence file under `## Full-suite testing and stabilization`. Generate a session-history entry for this phase.

#### 6.9 - Publication and integration
**Objective**: Publish the completed branch once, validate the merge result remotely, and hand a green integration to the release flow.
**Prompt**:
> Create the final local commit for this phase. Then present the resolved branching model, remote, branch name, and pull-request target, and obtain EXPLICIT approval before the plan's first branch push. Push once, open the integration pull request against the integration branch, and report the exact required checks expected. Wait until every required check reaches a terminal state. A red check REOPENS this phase: classify the failure, reproduce it locally, apply the narrow fix, re-run the local gate, update known gaps and session history, then amend the final commit or add one narrowly scoped stabilization commit and push again with approval. Never re-run a red check without a local reproduction; a re-run without one is a guess. Merge only after every required check is green and the user approves. Confirm the post-merge workflow performed only its intended smoke, publication, or provenance work and did not rerun the complete suite. Only then hand off to `/update release`, which owns the version bump, changelog, tag, push, and GitHub Release behind its own confirmation gates. Quote the required-check results in the evidence file under `## Publication and integration`.

### Phase 6 Exit Checklist

- [x] Phase task range T017-T025 complete
- [x] Every last-phase duty has quoted evidence or a structured known gap
- [x] Goal-vs-codebase review passes or records a blocking gap
- [x] Last-phase-only human testing suggestions are recorded (execution remains DF-3)
- [x] Full local suite passes before publication
- [x] One final local phase commit exists
- [x] User approved the first push and integration pull request
- [x] Required remote checks are green and merge is approved
- [x] Release flow begins only after green integration

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|---|---|---|

## Out of Scope

- Adding Qwen3.8-Flash-Next to any catalog, recommendation, or automatic installer path.
- Bundling Video2X, copying AGPL source, or downloading Video2X automatically without a completed distribution review.
- Promising real-time enhancement, recovered true detail, or universal 4K/60 performance.
- Integrating OpenWorker, its SaaS connectors, OAuth broker, cloud model providers, or reviewer auto-approval.
- Editing or committing the sibling Nexus-Hub repository from a Nexus-AI plan phase.
- Replacing the existing generation queue, FFmpeg provisioner, video workflow metadata, permission tiers, scheduler, ask inbox, or MCP governance.

## Residual Risks

- Video2X licensing may keep the adapter user-installed indefinitely; the backend-neutral contract protects the product from that outcome.
- macOS may remain unsupported until a lawful native backend and measured Vulkan/Metal path are proven.
- Super-resolution and interpolation can create visually plausible but incorrect detail, flicker, ghosting, warping, or damaged text.
- 4K/60 processing can exceed consumer VRAM/RAM or take substantially longer than generation; benchmarks, not marketing copy, define the support envelope.
- The Nexus-Hub security refinement depends on a separate upstream plan and release. Nexus-AI records the handoff but does not claim the upstream work landed without version evidence.
- Qwen artifacts, licenses, and runtimes can change after this plan. Re-evaluation must use fresh official evidence and Nexus benchmarks.
