# Plan -- Installer Field Repair

**Project**: Nexus AI Studio
**Version**: v2.3.1
**Slug**: installer-field-repair
**Filename**: v2.3.1-installer-field-repair.md
**Plan Type**: Feature / Enhancement (field-repair of the Windows wizard)
**Created**: 2026-08-28
**Seeded from**: `docs/archive/v2/v2.3/development/installer-field-review-2026-08-28.md` (operator screenshots 2026-08-28)
**Goal**: A first-run Windows install keeps the window alive through model downloads, shows real RAM so patient-tier cards are gated honestly, paints catalog and progress rows on card surfaces instead of black window-color bars, and treats the VS Code step as an optional, tickable extension install (replace if already present) with Unsloth Core explained on Configuration.

## Overview

This plan repairs the v2.2.9 Windows wizard using defects an operator hit in one session: black name-row and progress-list fills, Inkling-Small marked as having 0 GB RAM next to a card that correctly reported 16 GB VRAM, a VS Code page that hid its own checkbox on 1.135.0 and parked Unsloth Core there, and a process close after Dependencies finished and Models showed "Waiting to start".

The work stays in `scripts/installer/` plus the shared catalog and the extension engine pin. It does not add Video Lab features and must not land inside the in-flight v2.3.0 video plan. Run it on a dedicated branch. Last-phase evidence for this plan is `docs/archive/v2/v2.3/development/v2.3.1-last-phase-evidence.md` so it does not overwrite v2.3.0's evidence file.

Success is a packaged unsigned Windows wizard that: (1) survives the model step or fails in-window with a log path, (2) reports system RAM on RAM-gated cards, (3) has no window-color bars on catalog headers or install rows, (4) always shows a VS Code extension checkbox when Microsoft `code` is on PATH, with replace/upgrade when the extension is already installed, and (5) explains Unsloth Core on Configuration. EmbeddingGemma 300M (not 300B) stays opt-in unless a recorded decision plus a reindex story says otherwise.

## Goals First

**Target problem**: A privacy-first local studio is unusable on first run if the installer paints broken chrome, lies about RAM, disables the VS Code option the page exists for, and then closes while models are queued.

**Target persona**: A developer or creator installing Nexus on a Windows laptop with one consumer NVIDIA GPU who wants honest hardware gating and a wizard that either finishes or fails with a visible log.

**Observable definition of done**: On a 16 GB VRAM host with normal system RAM, Inkling-Small is Compatible (patient-tier), catalog and installing lists use card surfaces rather than black bars, the VS Code checkbox is visible and tickable when `code` is present, Unsloth is explained on Configuration, and starting a multi-model install cannot silently destroy the process.

**Strategy grounding**: No `STRATEGY.md` or version-scoped strategy anchor exists. Scope is grounded in AGENTS.md (local-first studio, installer in `scripts/installer/`) and the 2026-08-28 field review. No `docs/solutions/` store exists.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at docs/<version>/constitution.md - skipping check. Recommend running /constitution to establish project principles.

## Current model map

**Model map status**: fresh as of 2026-08-28; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|------|-----------|--------|--------|--------|
| frontier | `claude-fable-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.5-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

Map JSON validated on 2026-08-28 with `model-routing/scripts/model-map.ps1` (`valid: true`, four tiers, four providers). Cursor remains a manual picker surface for implementation-time switching.

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/models/overview
- OpenAI: https://developers.openai.com/api/docs/models
- Google: https://ai.google.dev/gemini-api/docs/changelog, https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/
- Cursor: https://cursor.com/docs/models-and-pricing

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|-------|-------|---------|------------------------|--------------------------|
| 1 | Honest host RAM and disk fields | Catalog badges use real RAM; disk GB is one field | strong | high |
| 2 | Install thread crash containment | Model step cannot silently kill the wizard | frontier | max |
| 3 | Catalog and progress chrome | No window-color bars on name rows or model lists | strong | high |
| 4 | VS Code step and Unsloth placement | Tickable extension install, replace if present, Unsloth on Configuration | frontier | max |
| 5 | Required embedder policy | Keep Nomic unless a written decision plus reindex lands | standard | medium |
| 6 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Evidence file, local gates, single publication | frontier | max |

Phase 2 carries the most risk: an uncaught GUI-thread exception and Qt signals from pool workers can close the process the operator already lost.

## Task Inventory

- [x] T001 [US2] Add `total_ram_gb` to installer state and copy it from host detection scripts/installer/src/nexus_installer/installer_state.py
- [x] T002 [US2] Write `free_disk_gb` from the install-path probe (stop using it as RAM) scripts/installer/src/nexus_installer/pages/install_path.py
- [x] T003 [US2] Gate catalog cards on `state.total_ram_gb` so Inkling-Small is Compatible on a normal RAM host scripts/installer/src/nexus_installer/pages/typed_catalog.py
- [x] T004 [US2] Test RAM vs disk vs VRAM badges and rebuild-on-show scripts/installer/tests/test_typed_catalog.py
- [x] T005 [US4] Catch `_InstallThread.run` exceptions and always emit `install_finished` scripts/installer/src/nexus_installer/engine/installer.py
- [x] T006 [US4] Marshal model telemetry onto the GUI thread before touching widgets scripts/installer/src/nexus_installer/pages/installing.py
- [x] T007 [US4] Persist a crash log the Complete/resume path can show in a windowed build scripts/installer/src/nexus_installer/background/recorder.py
- [x] T008 [US4] Test thread exception containment and signal affinity scripts/installer/tests/test_install_engine.py
- [x] T009 [US1] Stop painting every QWidget with `BG_WINDOW`; token child surfaces scripts/installer/src/nexus_installer/theme.py
- [x] T010 [US1] Make `cardHeaderRow` and PhaseGroup details inherit the card fill scripts/installer/src/nexus_installer/pages/typed_catalog.py
- [x] T011 [US1] Test stylesheet tokens for header and details backgrounds scripts/installer/tests/test_theme.py
- [x] T012 [US3] decision: VS Code host policy (current stable vs exact 1.134.0) scripts/installer/src/nexus_installer/engine/extension_installer.py
- [x] T013 [US3] Always-visible extension checkbox, enable when `code` exists, `--force` replace scripts/installer/src/nexus_installer/pages/vscode_extension.py
- [x] T014 [US3] Move Unsloth Core to Configuration with plain-language copy scripts/installer/src/nexus_installer/pages/configuration.py
- [x] T015 [US3] Test VS Code checkbox visibility, replace argv, Unsloth page move scripts/installer/tests/test_vscode_extension_page.py
- [x] T016 [US5] decision: keep Nomic as required embedder or ship a reindex migrator core/registry/catalog.json
- [x] T017 [US5] Apply the embedder decision (default: keep Nomic, clarify 300M copy) core/registry/catalog.json
- [x] T018 [US5] Test `is_required` and recommended.json embed defaults scripts/installer/tests/test_typed_catalog.py
- [x] T019 Architecture refactor quote docs/archive/v2/v2.3/development/v2.3.1-last-phase-evidence.md
- [x] T020 Known-gaps reconciliation docs/archive/v2/v2.3/known-gaps.md
- [x] T021 Living docs architecture check docs/archive/v2/v2.3/development/v2.3.1-last-phase-evidence.md
- [x] T022 Git-tree hygiene quote docs/archive/v2/v2.3/development/v2.3.1-last-phase-evidence.md
- [x] T023 Terminal CI/CD reconciliation docs/archive/v2/v2.3/development/v2.3.1-last-phase-evidence.md
- [x] T024 Independent Goal-vs-codebase review docs/archive/v2/v2.3/development/v2.3.1-last-phase-evidence.md
- [x] T025 Last-phase human testing suggestions docs/archive/v2/v2.3/development/v2.3.1-last-phase-evidence.md
- [x] T026 Full-suite local stabilization docs/archive/v2/v2.3/development/v2.3.1-last-phase-evidence.md
- [x] T027 Publication and integration (explicit approval) docs/archive/v2/v2.3/development/v2.3.1-last-phase-evidence.md

---

## Phase 1: Honest host RAM and disk fields

**Goal**: RAM-gated catalog cards show real system RAM; disk remaining is not reused as RAM.
**Prerequisites**: None.
**Stability Gate**: On a fixture host with 32 GB RAM, 16 GB VRAM, and 200 GB free disk, Inkling-Small badges Compatible and Gemma 4 26B still reports the VRAM shortfall; `free_disk_gb` equals the install-path probe.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Several related files and a user-visible hardware gate, but the fix is wiring an already-computed HostProfile field.

### Sub-tasks

#### 1.1 -- Thread RAM onto installer state

**Objective**: `InstallerState` carries `total_ram_gb` from `detect_host()`, never from disk.

**Failure modes**: malformed CIM/sysctl output stays 0 and the badge says RAM is unknown rather than inventing a number; unreachable PowerShell/WMI falls back to a ctypes/GlobalMemoryStatusEx (Windows) or `/proc/meminfo` (Linux) probe, then 0 with unknown copy; disk probe and RAM probe must not write the same field if one fails and the other succeeds.

**Build class**: load-bearing.

**Prompt**:
> Add `total_ram_gb: int = 0` to `scripts/installer/src/nexus_installer/installer_state.py`. In the wizard shell (GPU page completion and/or a single `detect_host()` call already used by the Review guard), copy `HostProfile.total_ram_gb` onto that field. Strengthen `_detect_windows_total_ram_gb` so a windowed frozen process does not depend only on PowerShell CIM: prefer a ctypes `GlobalMemoryStatusEx` (or equivalent) and keep PowerShell as fallback. Do not log raw command output that might include machine inventory beyond the integer GB. Acceptance: a unit test feeds a fake 32 GB RAM host and the catalog page reads 32, not `free_disk_gb`.

---

#### 1.2 -- One disk field

**Objective**: Install Path writes the same GB the catalog footer and install guard read.

**Failure modes**: missing path or `disk_usage` error keeps `free_disk_gb` at 0 and the footer already treats 0 as "do not block selection"; a later Review `detect_host()` must not overwrite a successful path-local probe with 0; `disk_space_gb` and `free_disk_gb` must not diverge after this phase (either alias or one field).

**Build class**: load-bearing.

**Prompt**:
> `InstallPathPage._update_disk_display` currently sets `state.disk_space_gb` only. Also set `state.free_disk_gb` (integer GB, same rounding policy as host_detect). Grep the installer for `disk_space_gb` and `free_disk_gb` and make the catalog, disk-aware footer, and install guard read one source. Remove the comment `placeholder until HostProfile threaded` in `typed_catalog.py`. Acceptance: after visiting Install Path in tests, `free_disk_gb > 0` when `disk_usage` returns a positive free byte count.

---

#### 1.3 -- Catalog badges use RAM

**Objective**: Inkling-Small is Compatible when RAM >= 8 GB even if `free_disk_gb` is still 0.

**Failure modes**: absent `requiredRamGB` stays ungated; RAM 0 with `requiredRamGB > 0` shows "RAM not detected" rather than "you have 0" if the probe failed; VRAM badges must still use `vram_mb`.

**Prompt**:
> In `typed_catalog.py` `_build_tab`, pass `host_ram_gb=state.total_ram_gb`. Rebuild tabs in `showEvent` after GPU and path pages have run so a 0-at-init does not stick. Change the RAM-mismatch copy so a failed probe (0 after detect) is distinguishable from a true 0 GB machine. Keep VRAM copy unchanged. Acceptance: a page test with vram_mb=16384, total_ram_gb=32, free_disk_gb=0 shows Inkling-Small Compatible and Gemma 4 26B still over-budget on VRAM.

---

#### 1.4 -- Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until the phase is stable before advancing to Phase 2.

**Prompt**:
> Generate automated tests for honest RAM vs disk vs VRAM gating: unit tests for Windows RAM fallback, Install Path writing `free_disk_gb`, and TypedCatalogPage Inkling Compatible / Gemma 26B VRAM warning. Run installer pytest for the touched files, fix failures, and iterate until green. Record this phase's CI IMPACT: new test paths under `scripts/installer/tests/`; the existing installer pytest job already covers them if it still globs that tree. Do not ask for manual wizard clicks. After tests pass, run `/generate-session-history` for Phase 1, then create ONE local commit scoped to this phase. Do NOT push, do NOT open a pull request, and do NOT start remote CI.

---

### Phase 1 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 2

---

## Phase 2: Install thread crash containment

**Goal**: Starting the model step cannot close the wizard with no error surface.
**Prerequisites**: Phase 1.
**Stability Gate**: A injected exception in `InstallEngine.run` during the model step leaves the window up, emits `install_finished(False, ...)`, and writes a log path; model row updates only occur on the GUI thread.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Cross-cutting thread affinity, windowed PyInstaller, and a P0 process-death failure.

### Sub-tasks

#### 2.1 -- Fail closed on the install QThread

**Objective**: `_InstallThread.run` never lets an exception kill the process silently.

**Failure modes**: malformed state still finishes with `install_finished(False)` and a one-line reason; a hung puller is cancellation (`cancel()`), not this handler; two overlapping `start_installation` calls remain idempotent via `_has_started`.

**Build class**: load-bearing.

**Prompt**:
> Wrap `InstallEngine.run` from `_InstallThread.run` in try/except BaseException (log, then re-raise KeyboardInterrupt/SystemExit after signaling). Always emit `install_finished` with success=False and the exception type plus message (no token, no argv). Mirror the headless `run_step` guard so GUI and headless share one helper if that is smaller than duplicating. Acceptance: a test thread whose `run` raises RuntimeError still emits `install_finished` and does not call `sys.exit`.

---

#### 2.2 -- GUI-thread telemetry

**Objective**: `model_started` / progress / completed / failed never touch widgets from a `ThreadPoolExecutor` worker.

**Failure modes**: a worker that cannot queue to the GUI thread records the event in state and retries once, then marks the row failed rather than emitting from the worker; a slow GUI does not block pulls beyond the existing progress throttle; cancel during queue drain does not double-complete a row.

**Build class**: load-bearing.

**Prompt**:
> `ModelStepRouter` calls `events.started` from pool threads. Keep compute on the workers. Marshal Qt signal emits onto the `InstallEngine` QObject thread (QueuedConnection from the engine QThread, or a single slot that the engine invokes with `QMetaObject.invokeMethod`). Do not call `PhaseGroup.ensure_model_row` from a non-GUI thread. Acceptance: a test that stubs the router to call `started` from a dummy thread fails if the installing page slot would run on that thread; after the fix it runs on the QApplication thread.

---

#### 2.3 -- Visible crash log

**Objective**: A windowed build writes the last exception where Resume/Complete can show it.

**Failure modes**: log write failure still shows an in-memory message box; secrets (HF token) must not appear in the crash file (reuse recorder field allowlist); a second crash overwrites atomically, no partial JSON.

**Prompt**:
> When `install_finished` fires with an engine exception, write the reason into the existing install state file / log path (`background/state_store.py`) and show it on Complete or a modal before navigating away. Windowed `sys.stderr` is None; do not rely on it. Acceptance: tests assert the crash reason is in the state log and `hf_token` is not.

---

#### 2.4 -- Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until the phase is stable before advancing to Phase 3.

**Prompt**:
> Generate automated tests for QThread exception containment, queued model signals, and crash-log redaction. Run installer pytest for engine, installing page, and background recorder. Record CI IMPACT: same installer pytest path; no new pipeline job. After tests pass, `/generate-session-history` for Phase 2, then ONE local commit. Do NOT push, do NOT open a pull request, and do NOT start remote CI.

---

### Phase 2 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 3

---

## Phase 3: Catalog and progress chrome

**Goal**: Name rows and installing model lists sit on card surfaces, not `BG_WINDOW` bars.
**Prerequisites**: Phase 1.
**Stability Gate**: Stylesheets for `#modelCard`, `#cardHeaderRow`, and PhaseGroup details do not paint `BG_WINDOW` on nested widgets; visual tests or QSS assertions lock that.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: QSS inheritance across theme, cards, and PhaseGroup; visual regression risk is medium.

Phase 3 may run in parallel with Phase 2 after Phase 1 (no code overlap required if theme and engine stay split). If both are in flight, merge engine first.

### Sub-tasks

#### 3.1 -- Surface tokens instead of global QWidget fill

**Objective**: Only the window/root uses `BG_WINDOW`; cards and details use `BG_CARD` or transparent.

**Failure modes**: a new unnamed QWidget without a token must inherit transparent-on-parent, not window black; disabled controls must keep a visible border; QTextEdit/log remains `BG_INPUT`.

**Build class**: load-bearing.

**Prompt**:
> In `scripts/installer/src/nexus_installer/theme.py`, stop applying `background-color: BG_WINDOW` to the bare `QWidget` selector. Paint `QMainWindow` / the background host only. Add `#cardHeaderRow` and PhaseGroup details rules: `background: transparent`. Give `QWidget#modelCard QWidget` transparent fill. Keep title bar, sidebar, and footer on their tokens. Acceptance: `test_theme.py` asserts the global `QWidget {` rule does not set `BG_WINDOW`, and `#cardHeaderRow` is transparent.

---

#### 3.2 -- Card and PhaseGroup widgets

**Objective**: `cardHeaderRow` and `_details` cannot pick up the window palette even if QSS order changes.

**Failure modes**: `WA_StyledBackground` on the header with transparent stylesheet; details widget `autoFillBackground` false; over-budget dashed cards still readable.

**Prompt**:
> Set `cardHeaderRow` stylesheet `background: transparent` in `typed_catalog.py`. On `PhaseGroup._details`, set transparent background and objectName for QSS. Do not wrap the model list in a second solid `BG_WINDOW` frame. Acceptance: widget tests read `styleSheet()` / palette and fail if `#0a0d14` is the header or details fill.

---

#### 3.3 -- Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until the phase is stable before advancing to Phase 4.

**Prompt**:
> Generate automated QSS/widget tests for catalog header and installing details backgrounds. Run installer pytest for theme, typed_catalog, phase_group. Record CI IMPACT: existing installer pytest. After tests pass, `/generate-session-history` for Phase 3, then ONE local commit. Do NOT push, do NOT open a pull request, and do NOT start remote CI. Human screenshot checks wait for Phase 6.

---

### Phase 3 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 4

---

## Phase 4: VS Code step and Unsloth placement

**Goal**: The VS Code page is about the extension: visible checkbox, replace if installed, honest ABI warning. Unsloth Core moves to Configuration with an explanation.
**Prerequisites**: Phase 3 (chrome) so the VS Code card is not a second black box.
**Stability Gate**: With `code` 1.135.0 on PATH, the extension checkbox is visible and enabled (or enabled-with-warning per T012); Unsloth is only on Configuration; install argv includes `--force` when replacing.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Native-module ABI and VSIX rebuild can break VS Code if the version policy is wrong.

### Sub-tasks

#### 4.1 -- decision: VS Code host policy

**Objective**: Record whether this plan supports current Microsoft stable `code` or keeps exact 1.134.0.

**Prompt**:
> Write a short decision in `docs/archive/v2/v2.3/development/vscode-host-policy.md`. Options: (A) rebuild `better-sqlite3` for the current stable VS Code Electron ABI and set `engines.vscode` to a range that matches that ABI; (B) keep exact 1.134.0 but always show a disabled checkbox plus install-VS-Code-this-version copy; (C) allow tick on mismatch with a written ABI-risk warning and fail-closed at install if `--install-extension` errors. Recommend A if a rebuild is in-repo (`scripts/build-vsix.ps1`); otherwise C plus B copy. Do not start T013 until this file exists. Evidence that settles it: `code --version` on the operator host (1.135.0) and the Electron ABI `build-vsix.ps1` currently pins.

---

#### 4.2 -- Tickable extension, replace if present

**Objective**: Operators can tick or untick the Nexus VS Code extension. If it is already installed, the control offers replace with the bundled VSIX.

**Failure modes**: missing `code` CLI leaves the box visible, unchecked, disabled, with "Install VS Code to enable"; version-mismatch follows T012 (never an invisible checkbox); `code --install-extension --force` failure records a step failure, does not crash the wizard; listing extensions fails open with a warning, not a false "not installed".

**Build class**: load-bearing.

**Prompt**:
> In `vscode_extension.py`, keep the QCheckBox enabled whenever Microsoft stable `code` is found, unless T012 chose strict disable (then the indicator must remain visible: do not paint disabled indicator with `BG_CARD` on `BG_CARD`). Default checked when compatible; default unchecked when ABI-unproven if T012 is C. Detect `EXTENSION_ID` / `LEGACY_EXTENSION_ID` via `--list-extensions` and change the label to "Replace the installed Nexus VS Code extension with this installer's copy". In `extension_installer.py`, pass `--force` when replacing. Update `test_extension_installer.py` that currently asserts `--force` is absent. Acceptance: 1.135.0 host still shows a square checkbox; already-installed ID uses `--force`.

---

#### 4.3 -- Move Unsloth Core to Configuration

**Objective**: Fine-tuning is not a VS Code option.

**Failure modes**: missing NVIDIA / VRAM < 16 still allows the box but the provisioner records skip-success as today; Unsloth checked with no GPU logs a plain warning on the page; `install_unsloth` remains off by default.

**Prompt**:
> Remove the Unsloth checkbox from `VsCodeExtensionPage`. Add it to `ConfigurationPage` under Features with copy: what it is (optional local QLoRA fine-tuning runtime for Nexus, not the VS Code extension), who it is for (NVIDIA, 16 GB+ VRAM), license (unsloth Apache-2.0, unsloth-zoo LGPL-3.0-or-later, dynamically linked, no AGPL Studio). Link `docs/archive/v2/v2.1/development/unsloth-license-boundary.md` only in developer docs, not as a raw filename in the wizard. Wire `state.install_unsloth` as today. Acceptance: VS Code page tests have no Unsloth widget; configuration tests toggle `install_unsloth`.

---

#### 4.4 -- Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until the phase is stable before advancing to Phase 5.

**Prompt**:
> Generate automated tests for checkbox visibility when disabled, 1.135.0 enablement per T012, `--force` replace, and Unsloth only on Configuration. Run installer pytest and any packaging test that pins `engines.vscode` if T012 changed it. Record CI IMPACT: installer pytest; if `engines.vscode` or VSIX rebuild arguments change, name the workflow files that already call `scripts/build-vsix.ps1` (already covered) vs a new pin that CI must learn. After tests pass, `/generate-session-history` for Phase 4, then ONE local commit. Do NOT push, do NOT open a pull request, and do NOT start remote CI.

---

### Phase 4 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 5

---

## Phase 5: Required embedder policy

**Goal**: The required memory embedder is an explicit product choice, not a screenshot guess. Default: keep Nomic Embed Text.
**Prerequisites**: Phase 1 (catalog page still the surface).
**Stability Gate**: `is_required` matches the decision file; catalog copy does not call EmbeddingGemma 300B; new installs still get a working default embedder.
**Recommended model tier**: standard
**Recommended effort level**: medium
**Rationale**: Default path is copy plus a locked id; switching embedders would be a high-risk fork and is out of scope unless T016 chooses it.

### Sub-tasks

#### 5.1 -- decision: required embedder

**Objective**: Keep Nomic or accept a reindex migrator before promoting EmbeddingGemma.

**Prompt**:
> Write `docs/archive/v2/v2.3/development/embedder-default-decision.md`. Facts: operator asked about "GemmaEmbedding 300B"; catalog id is `embeddinggemma` / EmbeddingGemma **300M**, 2K context, Gemma Terms of Use, 0.62 GB; Nomic Embed Text is Apache-2.0, 8K, 0.27 GB, hardcoded `is_required`, and `recommended.json` embed lists. Existing semantic memory is Nomic-shaped. Recommend KEEP Nomic as required for v2.3.1; optionally badge EmbeddingGemma as "Newer opt-in" without locking it on. Switching required embedder is only allowed if this decision names a reindex tool and a one-time wizard warning. Do not start T017 until the file exists.

---

#### 5.2 -- Apply the decision

**Objective**: Catalog and installer `is_required` match the decision. No silent swap.

**Failure modes**: missing embedder id in recommended.json must not tick a random embed model; two required embedders are forbidden; a KEEP decision must not change `is_required`.

**Build class**: load-bearing for the KEEP path (id lock). Scaffolding if a migrator is chosen: name the follow-up version that ships reindex.

**Prompt**:
> If T016 is KEEP: leave `nomic-embed-text` required; clarify catalog copy that EmbeddingGemma is 300M and opt-in because a switch needs a memory reindex. If T016 is SWITCH: implement the named reindex path and change `is_required`, `recommended.json` embed lists, and `package.json` `embeddingModel` default together. Do not invent 300B. Acceptance: tests still (or now) assert the required id from the decision file.

---

#### 5.3 -- Testing and Stabilization

**Objective**: Generate and run all tests for this phase. Iterate until the phase is stable before advancing to Phase 6.

**Prompt**:
> Generate automated tests for `is_required`, recommended embed defaults, and copy that does not say 300B. Run installer pytest plus any root catalog invariant tests. Record CI IMPACT: existing catalog/installer tests. After tests pass, `/generate-session-history` for Phase 5, then ONE local commit. Do NOT push, do NOT open a pull request, and do NOT start remote CI.

---

### Phase 5 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] No branch push, pull request, or remote CI run occurred
- [x] Ready to advance to Phase 6

---

## Phase 6: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, its living docs architecture current, and its pipeline reconciled against the canonical contract, with independent evidence that the plan Goal landed, then publish and integrate the completed branch.
**Prerequisites**: All prior phases.
**Stability Gate**: `docs/archive/v2/v2.3/development/v2.3.1-last-phase-evidence.md` exists with one quoted section per duty below. A missing Goal, a Goal-review miss without a recorded known-gap, or a missing evidence section blocks `/update release`.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, Goal-vs-codebase review, known-gap reconciliation, and release gating carry high context volume and blast radius.

Use `docs/archive/v2/v2.3/development/v2.3.1-last-phase-evidence.md` (not the unprefixed v2.3.0 evidence file).

### Sub-tasks

#### 6.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout and quote the scan.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure, then refactor toward a clean, intuitive layout via [[project-refactor]] and [[docs-layout-refactor]] (propose-then-apply, with confirmation; repair every reference for anything that moves). Write the detector output into `docs/archive/v2/v2.3/development/v2.3.1-last-phase-evidence.md` under `## Architecture refactor`. An empty finding is allowed only when the scan output is quoted.

#### 6.2 - Known-gaps reconciliation
**Objective**: Reconcile this version and every other still-open known-gaps file.
**Prompt**:
> Via [[known-gaps-tracker]], reconcile this version's known gaps AND every other `docs/**/known-gaps.md` whose Status is in-progress or whose Open Items remain. Glob both canonical (`docs/releases/v<MAJOR>/v<MAJOR>.<MINOR>/known-gaps.md`) and legacy layouts. If a file is unreachable, record the glob result and continue with what was found. Write the disposition into the evidence file under `## Known-gaps reconciliation`.

#### 6.3 - Living docs architecture
**Objective**: Prove handbooks, decisions, and generated HTML match the required living tree.
**Prompt**:
> Check `docs/handbooks/` (markdown source of truth, generated `html/`, one atlas walkthrough, technical companions for key components), `docs/decisions/`, and living `docs/README.md` / `docs/DEVLOG.md` / `docs/todos.md`. Self-gated: never invent `docs/testing/` or `docs/validation/`. Markdown wins if HTML disagrees; regenerate or fail the check. Quote the check in the evidence file under `## Living docs architecture`. Details live in [[docs-layout-refactor]].

#### 6.4 - Git-tree hygiene
**Objective**: Report branch and repository-settings hygiene without deleting anything.
**Prompt**:
> Run `python scripts/check_release_preconditions.py --branches --repo-settings`. Quote the output in the evidence file under `## Git-tree hygiene`. Report only; never delete branches.

#### 6.5 - Terminal CI/CD reconciliation
**Objective**: The repository's pipeline is compared field by field against the canonical contract, and every difference is either applied with approval or recorded as a known gap.
**Prompt**:
> Invoke [[cicd-architect]] in existing-pipeline comparison mode and run its six steps in order: DETECT the active CI provider (recording "none detected" rather than assuming one); COMPARE the existing pipeline against every field of the canonical contract - repository-native profiles, event separation, runner selection, the always-resolving aggregate required check, permissions, immutable action or plugin references, caching, concurrency, path scoping, artifact retention, structured reports, deployment boundaries, and failure recovery; PROPOSE each difference with its cost, its risk, and the smallest change that closes it; obtain explicit APPROVAL per change (silence is not approval); APPLY the approved changes and re-run the local gate; and RECORD every declined or environment-only difference as a known gap with an owner and a next step via [[known-gaps-tracker]]. The comparison concludes PASS only when every required field has observable evidence; a green pipeline run is not evidence, because a pipeline that checks nothing is green for the wrong reason. If this repository ships more than one installer, run or add a declarative cross-installer parity gate covering distributed artifacts, supported platforms, named capability counterparts, and external-tool fallbacks, then execute the real installers on their target operating systems with identical postconditions. Cross-link [[platform-contract-verification]] so discovery and delivery are checked in the same pass. If the repository ships zero or one installer, record a silent no-op and continue. Quote the per-field comparison result in the evidence file under `## CI/CD coverage`.

#### 6.6 - Independent Goal-vs-codebase review
**Objective**: Prove the plan Goal landed in the codebase, not that checkboxes were ticked.
**Prompt**:
> Re-read the plan header Goal and the Goals First / Definition of Done. Inspect the codebase as if this agent had not implemented the phases. Write `## Goal-vs-codebase review` in `docs/archive/v2/v2.3/development/v2.3.1-last-phase-evidence.md` listing: the plan Goal restated; the code/docs artifacts that satisfy it; and any gap. A missing or malformed Goal is a fail-closed finding that names the missing Goal. Completing every prior-phase sub-task is not evidence the Goal landed. A miss is a blocking finding or a new known-gap, not a pass.

#### 6.7 - Human/manual testing suggestions (last phase only)
**Objective**: Ask a human to exercise only what automated tests cannot cover, now that the feature is complete.
**Prompt**:
> Emit human/manual testing suggestions for user-facing workflows, external integrations, and environment-specific cases that automated tests cannot cover. Do not invent a fake walkthrough. This duty exists only on the last phase. Minimum: packaged Windows wizard on the operator GPU host (16 GB VRAM): catalog chrome, Inkling RAM badge, VS Code checkbox on the installed `code` version, Unsloth on Configuration, and a model-step run that either completes or fails in-window.

#### 6.8 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and the complete LOCAL gate is green before anything is published.
**Prompt**:
> Run the full local validation/test suite, confirm the refactor changed no behavior, and iterate until clean. This gate is local by construction: it must pass before the branch is published, so it cannot depend on a remote run. Quote the suite in the evidence file under `## Full-suite testing and stabilization`. Generate a session-history entry for this phase.

#### 6.9 - Publication and integration
**Objective**: Publish the completed branch once, validate the merge result remotely, and hand a green integration to the release flow.
**Prompt**:
> Create the final local commit for this phase. Then present the resolved branching model, remote, branch name, and pull-request target, and obtain EXPLICIT approval before the plan's first branch push. Push once, open the integration pull request against the integration branch, and report the exact required checks expected. Wait until every required check reaches a terminal state. A red check REOPENS this phase: classify the failure, reproduce it locally, apply the narrow fix, re-run the local gate, update known gaps and session history, then amend the final commit or add one narrowly scoped stabilization commit and push again with approval. Never re-run a red check without a local reproduction; a re-run without one is a guess. Merge only after every required check is green and the user approves. Confirm the post-merge workflow performed only its intended smoke, publication, or provenance work and did not rerun the complete suite. Only then hand off to `/update release`, which owns the version bump, changelog, tag, push, and GitHub Release behind its own confirmation gates. Quote the required-check results in the evidence file under `## Publication and integration`.

---

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| | | |

---

### Phase 6 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] CI impact recorded against the canonical contract, with no remote CI run
- [x] Session history generated for this phase
- [x] One local commit created for this phase
- [x] Branch push, pull request, and remote CI occurred only after explicit approval in 6.9
- [x] Ready for `/update release` after green integration
