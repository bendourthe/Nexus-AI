# v1.5.0 -- Known Gaps, Deferrals, and Carryovers

**Status**: in-progress (Phase 6 closed 2026-06-14). v1.5.0 is the "Local Agent Maturity" cycle ([plans/adoption-ecosystem-2026-06.md](plans/adoption-ecosystem-2026-06.md), derived from [comparison-ecosystem-2026-06.md](comparison-ecosystem-2026-06.md)). Phase 1 (2026-06-10) ships the three Bucket 1 `local-only` foundations: the Gemma 4 12B-IT GGUF quant ladder (item 32), the OS-keychain credential vault (item 2), and the intelligence-per-watt energy estimator (item 18). Phase 2 (2026-06-10) ships the two `skill-native` adoptions (items 11, 21). Phase 3 (2026-06-10) ships the inbound prompt-injection classifier (item 3, warn-then-allow). Phase 4 (2026-06-12) ships the planner/critic/worker swarm orchestration (item 36, opt-in, default off) and closes three v1.4.0 deferrals it subsumes: live worktree wiring (`T018.P3.A`), the orchestration layer + read-tool worktree rooting (`T018.P3.B`), and the live PreCompact WIP hook (`T016.P3.A`). Phase 5 (2026-06-14) ships the Bucket 4 desktop / model-layer re-partials: multimodal (image) input via the Gemma 4 vision gate (item 33), the side-by-side preview pane (item 24), the vault-only credential-management surface (item 25), and cross-surface session resume over a persistent session store (item 26); the local cron scheduler (item 38) is deferred (no confirmed demand). Phase 6 (2026-06-14) lands the Tree-sitter packaging closure (v1.4.0 `T022.P3.A`): a bundled-wasm-dir override on the codegraph scanner, an esbuild sidecar copy of the 4 grammar `.wasm` + the web-tree-sitter runtime `.wasm` with a startup warm-up, and a VSIX grammar trim. The final Nexus-Hub sync + acceptance gate is Phase 7. This file is appended phase-by-phase; items move to `## 2. Resolved` when closed; the `## 3. Summary` is recomputed each pass.

**Audience**: v1.5.0 phase authors, code reviewer, future-cycle planners
**Last updated**: 2026-06-15 (Phase 7 -- Nexus-Hub sync + whole-plan acceptance gate; cycle complete)
**Sibling reviews**: [../v1.4.0/known-gaps.md](../v1.4/known-gaps.md) (the upstream gap log; its open P3 deferrals `T016.P3.A`, `T018.P3.A`, `T018.P3.B`, `T022.P3.A` are the carryforward this cycle resolves in Phases 4 and 6), and [plans/adoption-ecosystem-2026-06.md](plans/adoption-ecosystem-2026-06.md) (the active plan).

**Cycle context**: This file is created in Phase 1 (rather than at a later phase) because the implement-phase post-phase sequence appends gaps every phase. Phase 1 introduces no bug, test failure, coverage shortfall, suppressed lint, or bypassed quality gate; the two seeded entries below are forward-tier follow-ups (`candidate` / `future`), not defects.

**Wording convention**: every prose claim follows [../v1.4.0/development/evidence-and-support-tiers.md](../v1.4/development/evidence-and-support-tiers.md) (A7): a gap describes its unbuilt capability at tier `future` or `candidate` (never `supported`); the "Suggested next step" states what cited evidence would raise the tier. "not_observed != absent" applies throughout.

Severity tags: **P0** release-blocker; **P1** should-fix; **P2** nice-to-have; **P3** out-of-scope for v1.5.0 / recorded for future planning.
Category tags: **NI** not implemented; **DF** deferred; **BG** bug; **MT** missing tests; **WN** warning; **QG** quality gate.

---

## 0. Adoption Ledger

Per-sub-task closure ledger for the 2026-06 ecosystem adoption plan. Rows land as each phase closes.

### Local Agent Maturity (adoption-ecosystem-2026-06)

| Plan sub-task | Item | Status | Closing reference |
|---|---|---|---|
| T001 | Item 32 -- Gemma 4 12B-IT Unsloth GGUF quant ladder (local-only) | Resolved | Phase 1 (2026-06-10); catalog entry `gemma-4-12b-it-gguf` in [../../../../core/registry/catalog.json](../../../core/registry/catalog.json) (256K context, `multimodal: true`, `ollama://hf.co/unsloth/gemma-4-12b-it-GGUF`); quant ladder + hardware-aware picker in [../../../../modules/coding/config/Gemma4GgufQuants.ts](../../../modules/coding/config/Gemma4GgufQuants.ts) (`selectGemma4GgufQuant`, tier mapping via `classifyTier`); installer rows in [../../../../scripts/installer/pyqt/src/nexus_installer/pages/recommended_models.py](../../../scripts/installer/pyqt/src/nexus_installer/pages/recommended_models.py) (`GEMMA4_GGUF_QUANTS` + opt-in preset rows). Tests: [../../../../tests/unit/config/Gemma4GgufQuants.test.ts](../../../tests/unit/config/Gemma4GgufQuants.test.ts), catalog assertion in [../../../../tests/unit/core/registry/catalog.test.ts](../../../tests/unit/core/registry/catalog.test.ts), `test_recommended_models.py` |
| T002 | Item 2 -- local OS-keychain credential vault (local-only) | Resolved | Phase 1 (2026-06-10); [../../../../core/security/CredentialVault.ts](../../../core/security/CredentialVault.ts) (`KeychainCredentialVault`, per-integration get/set/delete/list, redacts secret-shaped backend errors via `redactSecrets`, throws `KeychainUnavailableError` with no plaintext fallback) over [../../../../core/security/KeychainBackend.ts](../../../core/security/KeychainBackend.ts) (macOS `security` / Linux `secret-tool` / Windows `PasswordVault` via injected exec; no new npm dependency). Wired as the `${vault}` env source in [../../../../modules/coding/mcp/McpManager.ts](../../../modules/coding/mcp/McpManager.ts) `_resolveEnv`, constructed at [../../../../src/panels/ChatPanelBootstrap.ts](../../../src/panels/ChatPanelBootstrap.ts). Tests: KeychainBackend + CredentialVault unit suites, [../../../../tests/integration/mcp/credential-vault-mcp.test.ts](../../../tests/integration/mcp/credential-vault-mcp.test.ts) |
| T003 | Item 18 -- intelligence-per-watt energy telemetry (local-only) | Resolved | Phase 1 (2026-06-10); [../../../../core/telemetry/EnergyEstimator.ts](../../../core/telemetry/EnergyEstimator.ts) (`estimateEnergy` -> watts / tokens-per-watt / joules-per-request; `estimateEnergyForText` integrates `TokenCost.tokenize`; `samplePowerDraw` via nvidia-smi `power.draw`; reports `unavailable` on a missing sensor). Optional `powerQuery` + `powerDrawWatts` / `energyStatus` on [../../../../core/telemetry/GpuTelemetrySource.ts](../../../core/telemetry/GpuTelemetrySource.ts); surfaced through the desktop Local Model Status panel ([../../../../desktop/src/components/LocalModelStatus.tsx](../../../desktop/src/components/LocalModelStatus.tsx), `telemetryStream.ts`). Tests: EnergyEstimator unit suite, extended GpuTelemetrySource + desktop telemetryStream / LocalModelStatus suites |
| T004 | Phase 1 testing + stabilization | Resolved | Phase 1 (2026-06-10); `npm test` 3962 passed / 5 skipped / 0 failed; `npm run test:shell` 422 passed; installer `test_recommended_models.py` 29 passed; `tsc -b`, `npm run lint`, `npm run check-architecture` (0 errors), `npm run security:check` all clean; no outbound call introduced (vault uses local OS keychain CLI, energy uses local nvidia-smi) |
| T005-T007 | Phase 2 -- skill-native adoptions (items 11, 21) | Resolved | Phase 2 (2026-06-10); authored in Nexus-Hub on branch `feat/dci-discipline-and-agent-presets` (commit `786651f`): `catalog/skills/developer-experience/direct-corpus-interaction/SKILL.md` (item 11 -- DCI discipline over `HybridRetriever` RRF + code-graph MCP) and `catalog/skills/workflow/agent-presets/SKILL.md` (item 21 -- morning-briefing / research / coding-assistant bundles). Both PASS `scripts/validate_skills.py` (0 errors) + `--quality`; `data/skills.json` + `data/SKILL_INDEX.md` regenerated. Nexus-AI: `nexus-check --rule skill-duplicate-name` 0 findings, `check:prompts` exit 0, no `core/`/`modules/` change. Merge/publish/`nexus skills sync` -> Phase 7 (T023), tracked as `T005.P3.A`. |
| T008-T009 | Phase 3 -- inbound prompt-injection classifier (item 3) | Resolved | Phase 3 (2026-06-10); [../../../../modules/coding/security/InboundClassifier.ts](../../../modules/coding/security/InboundClassifier.ts) (`InboundClassifier.screen` -> deterministic heuristic reusing `guardrails/PromptInjectionScanner.scan` + supplementary inbound markers (new-instructions / role-spoof / imperative-override / exfiltration / HTML-comment), optional local-model second opinion via injected `InboundModelScreener` + `createLlmInboundScreener` over the `LLMClient` port). Warn-then-allow `annotate` wraps flagged content in an untrusted-content banner, never drops it. Routed at the agent-context boundary in [../../../../src/tools/AgentLoop.ts](../../../src/tools/AgentLoop.ts) (`_screenInboundResult` over `fetch_page` / `web_search`, classifier-failure degrades to raw output); constructed in [../../../../src/panels/ChatController.ts](../../../src/panels/ChatController.ts) `buildAgentLoop`. Toggles `nexus.coding.inboundClassifier.enabled` (default on) + `.deepScan` (default off). Tests: [../../../../tests/unit/modules/coding/security/InboundClassifier.test.ts](../../../tests/unit/modules/coding/security/InboundClassifier.test.ts) (21) + [../../../../tests/integration/security/inbound-classifier-routing.test.ts](../../../tests/integration/security/inbound-classifier-routing.test.ts) (5) |
| T010-T014 | Phase 4 -- swarm/DAG orchestration (item 36) + v1.4.0 `T018.P3.A/B`, `T016.P3.A` | Resolved | Phase 4 (2026-06-12); T010 live-wires `setWorktreeManager(new WorktreeManager(workspacePath))` at session bootstrap ([../../../../src/panels/ChatPanelBootstrap.ts](../../../src/panels/ChatPanelBootstrap.ts)) and the `DAGExecutor` sets `isolate: true` for write-capable nodes (verification) when `isolateWrites` is on ([../../../../modules/coding/orchestration/DAGExecutor.ts](../../../modules/coding/orchestration/DAGExecutor.ts), `WRITE_CAPABLE_AGENT_TYPES`). T011 adds the critic layer ([../../../../modules/coding/orchestration/CriticAgent.ts](../../../modules/coding/orchestration/CriticAgent.ts) `CriticReviewer` + fail-open `parseCriticVerdict`) gating each worker's output before merge in `DAGExecutor`, opt-in via the `Orchestrator` `swarmEnabled` flag (default off) wired from `nexus.coding.swarmOrchestration.enabled`; concurrency stays bounded by the existing GPU-tier semaphore. T012 threads the `pathGuard` `root` override through `read_file` / `list_directory` / `grep_codebase` ([../../../../src/tools/handlers/filesystem.ts](../../../src/tools/handlers/filesystem.ts)) and roots them at the worktree in `SubAgentManager._buildScopedRegistry`. T013 attaches `attachPreCompactWipHook(hookBus)` at bootstrap and emits `lifecycle.context.preCompact` from `ContextCompactor.compact()` (`setHookBus`) at the real compaction boundary. Tests: CriticAgent (10) + DAGExecutor.swarm (8, incl. scheduler-bound proof) + Orchestrator.swarm (3) + ContextCompactor.preCompact (3) unit; swarm-orchestration (2) + worktree-read-rooting (4) + precompact-wiring (2) integration. Full suite 4020 passed / 5 skipped / 0 failed; `tsc -b`, lint, check-architecture (0 errors), security:check, check:tampering all clean |
| T015-T020 | Phase 5 -- model-layer & desktop re-partials (items 33, 24, 25, 26, 38) | Resolved | Phase 5 (2026-06-14); **T015** (item 33) multimodal image input: optional `images` on `LLMMessage` ([../../../../modules/coding/llm/types.ts](../../../modules/coding/llm/types.ts)) + `Message` ([../../../../modules/coding/chat/types.ts](../../../modules/coding/chat/types.ts)); gated by `isVisionCapableModel` ([../../../../modules/coding/config/ModelCapabilities.ts](../../../modules/coding/config/ModelCapabilities.ts)) via the shared `toLlmMessages` mapper ([../../../../modules/coding/chat/llmMessages.ts](../../../modules/coding/chat/llmMessages.ts)) at both assembly sites (`StreamingPipeline`, `AgentLoop._streamOneTurn`); catalog `multimodal` flag surfaced on `ListedModel` ([../../../../core/registry/NexusModelRegistry.ts](../../../core/registry/NexusModelRegistry.ts)). **T016** (item 24) preview pane: [../../../../desktop/src/components/PreviewPane.tsx](../../../desktop/src/components/PreviewPane.tsx) (reuses `InteractiveArtifact` for HTML, `<pre>` for text) wired beside chat in [../../../../desktop/src/modules/chat/ChatPage.tsx](../../../desktop/src/modules/chat/ChatPage.tsx) via an additive `onSelectMessage` on the shared `MessageList`/`MessageBubble`. **T017** (item 25) credential UI: vault-only `credentials.*` IPC ([../../../../desktop/sidecar/src/protocol.ts](../../../desktop/sidecar/src/protocol.ts), [../../../../desktop/sidecar/src/handlers.ts](../../../desktop/sidecar/src/handlers.ts) over the Phase 1 `CredentialVault`) + [../../../../desktop/src/pages/settings/CredentialsSettings.tsx](../../../desktop/src/pages/settings/CredentialsSettings.tsx) (new Settings tab; writes only through the keychain, never a config file). **T018** (item 26) cross-surface resume: persistent [../../../../desktop/sidecar/src/coding/sessionStore.ts](../../../desktop/sidecar/src/coding/sessionStore.ts) (`JsonFileSessionStore` at `<nexusHome>/sessions.json`) wired into `CodingSessionManager`; `resume` extended with full message history. **T019** (item 38) cron: DEFERRED -- see `T019.P3.A`. Tests: ModelCapabilities (5) + llmMessages (4) + StreamingPipeline.multimodal (4) + NexusModelRegistry multimodal (1) unit; desktop PreviewPane (6) + ChatPage.preview (3) + CredentialsSettings (4) + credentials-handlers (5) + coding-session-resume (5). Full suites: root 4035 passed / 5 skipped / 0 failed; desktop 445 passed / 0 failed; `tsc -b`, desktop `tsc --noEmit`, lint, lint:shell, check-architecture (0 errors), security:check, check:prompts (0 errors), check:tampering all clean. No outbound call introduced. |
| T021-T022 | Phase 6 -- Tree-sitter `.wasm` packaging (v1.4.0 `T022.P3.A`) | Resolved | Phase 6 (2026-06-14); **T021** packaging: the codegraph scanner gained a bundled-wasm-dir override (`setTreeSitterWasmDir` + a `Parser.init({ locateFile })` hook for the runtime wasm) in [../../../../core/codegraph/scanner/TreeSitterScanner.ts](../../../core/codegraph/scanner/TreeSitterScanner.ts) so a node_modules-free packaged host can locate the grammar + runtime `.wasm`. Desktop sidecar: a `build:sidecar` esbuild config ([../../../../desktop/sidecar/esbuild.config.mjs](../../../desktop/sidecar/esbuild.config.mjs)) bundles `main.ts`, writes a `{ "type": "commonjs" }` dist marker (the bundle is CJS but `desktop/package.json` is `type: module`), and copies the 4 grammar `.wasm` + the `web-tree-sitter` runtime `.wasm` into `sidecar/dist/wasm`; [../../../../desktop/sidecar/src/treeSitterWarmup.ts](../../../desktop/sidecar/src/treeSitterWarmup.ts) points the loader at that dir and is warmed up at sidecar startup ([../../../../desktop/sidecar/src/main.ts](../../../desktop/sidecar/src/main.ts), fire-and-forget, stderr log). VSIX: [../../../../.vscodeignore](../../../.vscodeignore) trimmed `tree-sitter-wasms/out` to the 4 used grammars (keeps the runtime wasm), verified via `npx vsce ls`. **T022** stabilization: packaged-app readiness proven by [../../../../tests/integration/codegraph/treeSitterPackaged.test.ts](../../../tests/integration/codegraph/treeSitterPackaged.test.ts) (loads runtime + 4 grammars from an explicit node_modules-free dir, `isTreeSitterReady()` true, parses a regex-fallback miss) and by spawning the built `sidecar/dist/main.js` (stderr: "tree-sitter codegraph scanner: ready"). Root suite 4037 passed / 5 skipped / 0 failed; `npm run build:sidecar` + `build:web` clean; `tsc -b`, desktop `tsc --noEmit`, lint, lint:shell, check-architecture (0 errors), check:tampering all clean. |
| T023-T024 | Phase 7 -- Nexus-Hub sync + whole-plan acceptance gate | Resolved | Phase 7 (2026-06-15); **T023** published the 2 Phase 2 skills onto Hub `develop` (commit `fe8eb68`, no tag per operator decision) and integrated all four net-new non-skill Hub surfaces selected this cycle: `HUB.P3.DATA` (data/skills.json index enrichment in [../../../../core/skills/DevAIHubSyncer.ts](../../../core/skills/DevAIHubSyncer.ts) `buildManifestWithIndex`), `HUB.P3.RULES` ([../../../../modules/coding/chat/LanguageRuleBuilder.ts](../../../modules/coding/chat/LanguageRuleBuilder.ts) -> PromptBuilder section), `HUB.P3.AGENT` ([../../../../modules/coding/agents/HubAgentPersonaLoader.ts](../../../modules/coding/agents/HubAgentPersonaLoader.ts) -> SubAgentManager `personaName`), `HUB.P3.CMD` ([../../../../modules/coding/commands/HubCommandCatalogLoader.ts](../../../modules/coding/commands/HubCommandCatalogLoader.ts) -> CommandRouter `hub-command`), `HUB.P3.HOOK` (catalog/hooks added to the sparse-checkout + [../../../../core/skills/HubHookInstaller.ts](../../../core/skills/HubHookInstaller.ts)), `HUB.P3.MCPCFG` ([../../../../modules/coding/mcp/HubRegistryPolicyFilter.ts](../../../modules/coding/mcp/HubRegistryPolicyFilter.ts) -> `McpManager.policyFilterHubRegistry`, policy default-deny, filter-only). See [development/nexus-hub-integration-delta.md](development/nexus-hub-integration-delta.md). **T024** whole-plan acceptance gate green: lint 0 errors, root suite 4080 passed / 5 skipped / 0 failed, desktop suite 445 passed / 0 failed, `tsc -b` clean, check-architecture 0 errors, check:tampering 0 findings, check:prompts 0 errors, security:check in sync, check:audit-prod 0 blocking. Tests: +43 (DATA/RULES/AGENT/CMD/HOOK/MCPCFG unit suites). |

---

## 1. Open Items

### Phase 1 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `T001.P3.A` | P3 | DF | Partially closed by Phase 5. The T001 `multimodal` flag now HAS a production consumer (the item 33 vision gate `isVisionCapableModel` + `ListedModel.multimodal` surfaced from the catalog). The remaining open half is the `selectGemma4GgufQuant` VRAM-driven quant auto-selection helper, still exported + unit-tested but with no runtime UI call site (the desktop/installer picker surfaces the quants statically but does not call the VRAM picker). Tier: multimodal-flag consumption `supported`; quant auto-select `candidate`. | Wire `selectGemma4GgufQuant` into the desktop/installer model picker (auto-pick the largest quant that fits detected VRAM) to raise the quant-picker half to `supported`. |
| `T003.P3.B` | P3 | DF | `samplePowerDraw` implements the NVIDIA path (nvidia-smi `power.draw`); macOS `powermetrics` and Linux RAPL exist only as pure parse helpers (`raplWattsFromEnergyDelta`), not wired into the sampler, and no production `GpuTelemetrySource` is yet constructed with a `powerQuery` (the sidecar telemetry feed has no live construction site). Tier: NVIDIA `supported` (estimator + sampler unit-tested); macOS/Linux RAPL `future`. | When the sidecar constructs `GpuTelemetrySource`, pass `createPowerSampler()`; add the `powermetrics` (macOS) and RAPL-sysfs (Linux) branches to `samplePowerDraw` with cited readings to raise those platforms' tier. |

### Phase 2 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `T005.P3.A` | P3 | DF | The two Phase 2 skills (`developer-experience/direct-corpus-interaction`, `workflow/agent-presets`) are authored, validated, and committed on the Nexus-Hub branch `feat/dci-discipline-and-agent-presets` (commit `786651f`), but not yet merged to the Hub `develop` / `main` line and not yet reflected by `nexus skills sync` on the Nexus-AI consumer side. Tier: `candidate` (authored + validated; not yet published). | Phase 7 (T023) merges / publishes the Hub branch with attribution (`python scripts/validate_skills.py`) and runs `nexus skills sync` so the consumed catalog reflects the two skills, raising the tier to `supported`. |

### Phase 3 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `T008.P3.A` | P3 | DF | The inbound classifier's local-model second opinion (`nexus.coding.inboundClassifier.deepScan`, off by default) is built and unit-tested via a fake `LLMClient` exercising `createLlmInboundScreener` + `parseModelVerdict`, but has no integration test against a live local model (the repo norm: model-dependent paths are not exercised against a running Ollama in CI). The always-on deterministic heuristic IS `supported` (unit + production-path integration tested). Tier: heuristic `supported`; deep-scan model path `candidate`. | Add an opt-in live-model smoke test (gated on `OLLAMA_URL`, mirroring `skipIfNoOllama`) that feeds a known injection page through the deep-scan screener and asserts a flag, to raise the deep-scan path to `supported`. |

### Phase 4 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `T011.P3.A` | P3 | DF | The planner/critic/worker swarm (`swarmEnabled`, default off) is wired end-to-end and proven on the production path with a real `WorktreeManager` + real `run_terminal`, real `git worktree` isolation, a fail-open critic gate, and a scheduler-capacity-bound proof -- but every test drives the critic with a deterministic fake or single-response client, not a live multi-worker run against a running Ollama (repo norm: model-dependent paths are not exercised against a live backend in CI). Tier: mechanism + isolation + critic-gating + concurrency bound `supported`; live multi-model swarm behavior `candidate`. | Add an opt-in live-model smoke test (gated on `OLLAMA_URL`, mirroring `skipIfNoOllama`) that runs a small multi-node plan with `swarmEnabled` and asserts isolated dispatch + a real critic verdict, to raise the live-swarm path to `supported`. |
| `T012.P3.A` | P3 | DF | Read-tool worktree rooting covers `read_file`, `list_directory`, and the primary ripgrep path of `grep_codebase`. The `grep_codebase` `vscode.workspace.findFiles` FALLBACK (used only when ripgrep is absent on PATH) still searches the VS Code workspace, which the VS Code API cannot re-root to an arbitrary worktree directory. Tier: ripgrep path + read_file + list_directory `supported`; findFiles-fallback rooting `future`. | If a ripgrep-less environment needs worktree-rooted grep, replace the `findFiles` fallback with a worktree-rooted directory walk; otherwise document the fallback's workspace scope. |

### Phase 5 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `T019.P3.A` | P3 | DF | Item 38 (local cron scheduler) was demand-gated and is DEFERRED: no concrete recurring-task need was confirmed for this cycle, and a scheduler adds an autonomy surface the README design principles ("the autonomy unit is the session") are deliberately cautious about. Nothing was built. Tier: `future` (not built; not needed). | If a concrete recurring-task need arises, build a LOCAL-ONLY cron over the agent loop (no network triggers, explicit per-job consent, jobs persisted locally, scheduled runs treated as explicit consented sessions) with local-only + per-job-consent tests. |
| `T015.P3.A` | P3 | DF | Multimodal input wires image attachments through `LLMMessage.images` (Ollama `/api/chat` `images`) and is unit/integration-tested with fakes, but is not exercised against a live vision model (repo norm: model-dependent paths are not run against a running Ollama in CI). Audio input (the "image/audio" aspiration in the plan title) is NOT wired: Ollama's `/api/chat` accepts an `images` array but no audio field, so there is no local-serving wire path for audio today. Image attachments are also held in-memory only (not persisted to `ChatHistoryStore`). Tier: image wiring + vision gate `supported`; live vision-model behavior `candidate`; audio input `future`. | Add an opt-in live-model smoke test (gated on `OLLAMA_URL`, mirroring `skipIfNoOllama`) that feeds an image to a running vision-capable model and asserts it is accepted. Revisit audio if/when a local runtime exposes an audio-input wire path. |
| `T017.P3.A` | P3 | DF | The credential surface manages secrets per a user-entered integration name; it does not yet auto-populate the integration list from the MCP registry (`mcp.list` is still `NotImplemented` in the sidecar), and "provider/tool" management beyond the existing `ModelsSettings` + this credential surface was not built (out of the item-25 acceptance scope, which is credential-focused). The vault-only credential routing is `supported` (handler + UI tested; OS-keychain write itself covered by the Phase 1 vault suite). Tier: credential management `supported`; integration auto-discovery + provider/tool surfaces `future`. | When `mcp.list` is wired, feed the MCP server names as integration suggestions; add provider/tool management panels if a concrete need emerges. |
| `T018.P3.C` (v1.5.0 Phase 5 task T018; distinct from the v1.4.0 carryforward `T018.P3.A/B` in `## 2. Resolved`) | P3 | DF | Cross-surface resume is proven at the store/manager contract level: two `CodingSessionManager` instances over the same `<nexusHome>/sessions.json` share sessions + full history (the handshake). The sidecar is still Tauri-spawned, so a live CLI process does not yet connect to a shared daemon, and the active-session pointer (`resolveActiveSessionId` + `SettingsStore` `nexus.coding.activeSessionId`, already in `core/coding`) is not yet wired into sidecar bootstrap to auto-resume on connect. Tier: persistent store + resume-with-history contract `supported`; live two-process CLI<->desktop handshake + auto-resume `candidate`. | Wire a CLI client to the shared sidecar daemon and auto-resume the stored active session id on connect; add a two-process integration proof. |

### Phase 7 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `T023.P3.A` | P3 | DF | The 2 Phase 2 skills are published on Hub `develop` (`fe8eb68`) but **no release tag was cut** (the Hub is mid-cycle on an unfinished v3.4.0). `nexus skills sync` pins the latest *release* tag (`v3.3.4`), which predates the skills, so a live sync does not yet surface them. Tier: published-on-develop `supported`; sync-consumable `candidate`. | When the Hub cuts a release containing `develop`, run `nexus skills sync --apply`; the skills then flow through the existing loader. Also run the Hub's `make build-catalog` to regenerate its drifted `data/skills.json`. |
| `HUB.P3.EXT.*` | P3 | DF | The 6 Hub `extensions/` MCP servers are not registered with the Nexus-AI MCP bridge. Nexus-AI ships in-process equivalents (codegraph, SSRF-guarded fetch, prompt-injection scanner, context compaction), so this is opt-in optionality, not a capability gap. Tier: in-process equivalents `supported`; external Hub-server registration `future`. | Register a Hub extension as an external MCP server only if cross-product reuse is wanted; it would pass through the `HubRegistryPolicyFilter` like any registry entry. |
| `HUB.P3.NS` | P3 | DF | The local on-disk `devai-hub` namespace (active-tag pointer, `source: "devai-hub"` provenance, IPC enum) is unchanged after the upstream `DevAI-Hub -> Nexus-Hub` rename. Tier: `future` (cosmetic disk-layout + IPC migration; deferred for churn-vs-gain). | Migrate the on-disk namespace + provenance label + IPC enum in a dedicated cycle if the naming drift becomes confusing. |
| `T017.P3.E` / `T002.P2.A` (Hub-owned, carryforward) | P2/P3 | DF | The two Hub-validator gaps (`validate_skills.allowlist.json` over-long descriptions; 7 secret-scan false positives) remain Hub-repo-owned and are not closeable from Nexus-AI. Nexus-AI's own sync path screens skills with `PromptInjectionScanner` and is unaffected. | Track against the Nexus-Hub repo's validation overhaul. |

### Phase 6 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `T021.P3.A` | P3 | DF | Packaged-app Tree-sitter readiness was verified at two levels that do not require the Rust/Tauri toolchain (absent in this environment, so `npm run build:shell` / `tauri build` was not executed here): the node_modules-free wasm-dir load path (`treeSitterPackaged` integration test) and the built CJS bundle itself (spawning `sidecar/dist/main.js` logs "tree-sitter codegraph scanner: ready"). The full `tauri build` packaged binary + in-app activation readiness was not run. Tier: bundled-sidecar readiness `supported` (spawn-verified); full Tauri-shell packaged build + in-app readiness `candidate`. | On a host with `cargo` + the Tauri CLI, run `npm run build:shell` and confirm the packaged app's sidecar logs Tree-sitter ready on launch, to raise the full-shell path to `supported`. |

### Carryforward from v1.4.0 (resolved this cycle)

`T022.P3.A` (bundle the Tree-sitter grammar `.wasm` into the packaged app) was closed in Phase 6 (see `## 2. Resolved`). `T018.P3.A`, `T018.P3.B`, and `T016.P3.A` were closed in Phase 4 (see `## 2. Resolved`).

---

## 2. Resolved

| ID | Resolution |
|---|---|
| T001 | Phase 1 -- Gemma 4 12B-IT GGUF quant ladder in the catalog + tier-mapped picker + installer rows (see Adoption Ledger). |
| T002 | Phase 1 -- local OS-keychain `CredentialVault` wired as the MCP `${vault}` credential source (see Adoption Ledger). |
| T003 | Phase 1 -- intelligence-per-watt `EnergyEstimator` + optional GPU power sampling surfaced on the Local Model Status panel (see Adoption Ledger). |
| T004 | Phase 1 -- full + desktop + installer suites green; lint / arch / security / type-check clean; no outbound call introduced. |
| T005 | Phase 2 -- `direct-corpus-interaction` DCI search-discipline skill authored in Nexus-Hub (item 11); passes `validate_skills.py` + `--quality`. |
| T006 | Phase 2 -- `agent-presets` skill (morning-briefing / research / coding-assistant bundles) authored in Nexus-Hub (item 21); passes `validate_skills.py` + `--quality`. |
| T007 | Phase 2 -- Hub `validate_skills.py` PASS on both; Nexus-AI `nexus-check --rule skill-duplicate-name` 0 findings + `check:prompts` exit 0; no `core/` / `modules/` source changed. |
| T008 | Phase 3 -- inbound prompt-injection `InboundClassifier` (heuristic core + optional local-model second opinion) wired at the agent-context boundary over `fetch_page` / `web_search`; warn-then-allow annotation, never drops content; toggleable (default on). |
| T009 | Phase 3 -- 21 unit + 5 production-path integration tests green; full suite 3988 passed / 5 skipped / 0 failed; `tsc -b`, `lint`, `check-architecture` (0 errors), `check:prompts`, `security:check` all clean; no outbound call introduced. |
| T010-T014 | Phase 4 -- swarm/DAG orchestration (item 36): worktree live-wiring + write-capable isolation, planner/critic/worker layer (opt-in `swarmEnabled`), read-tool worktree rooting, live PreCompact hook (see Adoption Ledger). |
| T015-T020 | Phase 5 -- model-layer & desktop re-partials: multimodal image input via the Gemma 4 vision gate (item 33), side-by-side preview pane (item 24), vault-only credential-management surface (item 25), cross-surface session resume over a persistent session store (item 26); local cron (item 38) deferred (`T019.P3.A`). Root suite 4035 / 5 skipped / 0 failed; desktop suite 445 / 0 failed; all gates clean (see Adoption Ledger). |
| `T018.P3.A` (carryforward, v1.4.0) | Phase 4 (T010) -- `setWorktreeManager(new WorktreeManager(workspacePath))` live-wired at session bootstrap; the `DAGExecutor` sets `isolate: true` for write-capable nodes under `isolateWrites`. Production-path integration test proves a dispatched write-capable node mutates an isolated worktree, never the shared workspace, and the worktree is removed when left unchanged. Raises `T018.P3.A` candidate -> supported. |
| `T018.P3.B` (carryforward, v1.4.0) | Phase 4 (T011, T012) -- team-orchestration half: the `CriticAgent` gates each worker's output before merge in the `DAGExecutor`, opt-in via the `Orchestrator` `swarmEnabled` flag, concurrency bounded by the GPU-tier semaphore (scheduler-bound proof). Read-tool half: the `pathGuard` `root` override is threaded through `read_file` / `list_directory` / `grep_codebase` and rooted at the worktree, proven by a write-then-read parity integration test (a worker reads back its own in-worktree `run_terminal` write). Raises `T018.P3.B` candidate -> supported. |
| `T016.P3.A` (carryforward, v1.4.0) | Phase 4 (T013) -- `attachPreCompactWipHook(hookBus)` attached at session bootstrap alongside the reflection hook; `ContextCompactor.compact()` emits `lifecycle.context.preCompact` at the real compaction boundary (`setHookBus`). Production-path integration test proves the warning fires and a restorable checkpoint persists on a real (forced) compaction without blocking it. Raises `T016.P3.A` candidate -> supported. |
| T021-T022 | Phase 6 -- Tree-sitter `.wasm` packaging: scanner bundled-wasm-dir override + sidecar warm-up + esbuild wasm copy + VSIX grammar trim; packaged-app readiness proven by `treeSitterPackaged` integration test + a live spawn of the built sidecar bundle (see Adoption Ledger). |
| T023-T024 | Phase 7 -- Nexus-Hub sync (2 skills published to Hub `develop`; all four net-new non-skill Hub surfaces integrated: `HUB.P3.DATA`/`RULES`/`AGENT`/`CMD`/`HOOK`/`MCPCFG`) + whole-plan acceptance gate green (see Adoption Ledger + [development/nexus-hub-integration-delta.md](development/nexus-hub-integration-delta.md)). |
| `T022.P3.A` (carryforward, v1.4.0) | Phase 6 (T021) -- the codegraph scanner gained a bundled-wasm-dir override (`setTreeSitterWasmDir` + `Parser.init({ locateFile })`), the desktop sidecar build copies the 4 grammar `.wasm` + the `web-tree-sitter` runtime `.wasm` into `sidecar/dist/wasm` and warms them up at startup, and the VSIX ships the 4 grammars + runtime via `.vscodeignore`. Proven by the `treeSitterPackaged` integration test (node_modules-free load, `isTreeSitterReady()` true) and by spawning the built `sidecar/dist/main.js` (stderr "ready"). Raises `T022.P3.A` candidate -> supported. |

---

## 3. Summary

- **Phase 1 (2026-06-10)**: 3 Bucket-1 `local-only` adoptions resolved (items 32, 2, 18) + stabilization (T001-T004). 2 forward-tier follow-ups recorded (`T001.P3.A` candidate, `T003.P3.B` NVIDIA-supported / RAPL-future). 0 defects, 0 coverage shortfalls, 0 suppressed warnings, 0 bypassed gates.
- **Phase 2 (2026-06-10)**: 2 Bucket-2 `skill-native` adoptions resolved (items 11, 21) + stabilization (T005-T007), authored in the Nexus-Hub catalog with no Nexus-AI `core/` / `modules/` source change. 1 forward-tier follow-up recorded (`T005.P3.A` candidate -- merge / publish / `nexus skills sync` to Phase 7). 0 defects, 0 coverage shortfalls, 0 suppressed warnings, 0 bypassed gates.
- **Phase 3 (2026-06-10)**: 1 Bucket-3 `re-full` adoption resolved (item 3 -- inbound prompt-injection classifier) + stabilization (T008-T009). Warn-then-allow `InboundClassifier` routed over `fetch_page` / `web_search` at the agent-context boundary; deterministic heuristic always on, optional local-model second opinion behind `deepScan` (default off). 1 forward-tier follow-up recorded (`T008.P3.A` candidate -- deep-scan live-model smoke test). 0 defects, 0 coverage shortfalls, 0 suppressed warnings, 0 bypassed gates.
- **Phase 4 (2026-06-12)**: 1 Bucket-3 `re-full` adoption resolved (item 36 -- planner/critic/worker swarm orchestration, opt-in default off) + stabilization (T010-T014), and 3 v1.4.0 carryforward deferrals closed (`T018.P3.A` worktree live-wiring, `T018.P3.B` orchestration layer + read-tool rooting, `T016.P3.A` live PreCompact hook -- all candidate -> supported). 2 forward-tier follow-ups recorded (`T011.P3.A` live-swarm smoke test candidate; `T012.P3.A` grep findFiles-fallback rooting future). +32 tests; full suite 4020 passed / 5 skipped / 0 failed. 0 defects, 0 coverage shortfalls, 0 suppressed warnings, 0 bypassed gates.
- **Phase 5 (2026-06-14)**: 4 Bucket-4 `re-partial` adoptions resolved (item 33 multimodal image input + vision gate, item 24 side-by-side preview pane, item 25 vault-only credential UI, item 26 cross-surface session resume) + stabilization (T015-T020); item 38 (local cron) deferred per the demand gate (no confirmed need). `T001.P3.A` partially closed (multimodal-flag consumer now exists). 4 forward-tier follow-ups recorded (`T019.P3.A` cron deferral future; `T015.P3.A` live-vision smoke test candidate + audio input future; `T017.P3.A` integration auto-discovery future; `T018.P3.C` live two-process CLI<->desktop handshake candidate). +37 tests (root +14, desktop +23); root suite 4035 passed / 5 skipped / 0 failed, desktop suite 445 passed / 0 failed. 0 defects, 0 coverage shortfalls, 0 suppressed warnings, 0 bypassed gates.
- **Phase 6 (2026-06-14)**: v1.4.0 carryforward `T022.P3.A` (Tree-sitter `.wasm` packaging) closed (candidate -> supported) + stabilization (T021-T022). The codegraph scanner gained a bundled-wasm-dir override (`setTreeSitterWasmDir` + `Parser.init({ locateFile })`); the desktop sidecar build (esbuild config) copies the 4 grammar `.wasm` + the web-tree-sitter runtime `.wasm` into `sidecar/dist/wasm` (with a CJS dist marker) and warms them up at startup; the VSIX `.vscodeignore` ships the 4 grammars + runtime. 1 forward-tier follow-up recorded (`T021.P3.A` -- full `tauri build` not run here, `cargo` absent; bundled-sidecar readiness spawn-verified). +2 tests; root suite 4037 passed / 5 skipped / 0 failed. 0 defects, 0 coverage shortfalls, 0 suppressed warnings, 0 bypassed gates.
- **Phase 7 (2026-06-15, FINAL)**: Nexus-Hub sync + whole-plan acceptance gate (T023-T024). The 2 Phase 2 skills published to Hub `develop` (no tag per operator decision). All four net-new non-skill Hub surfaces selected this cycle integrated into the Nexus-AI runtime: `HUB.P3.DATA` (sync index enrichment), `HUB.P3.RULES` (language rules -> prompt), `HUB.P3.AGENT` (persona dispatch), `HUB.P3.CMD` (hub-command routing), `HUB.P3.HOOK` (sparse-checkout + installer), `HUB.P3.MCPCFG` (policy-gated registry filter). Whole-plan Definition of pass verified: all Bucket 1-3 items (32, 2, 18, 11, 21, 3, 36), the planned Bucket 4 re-partials (33, 24, 25, 26; item 38 deferred), and the v1.4.0 deferrals (`T018.P3.A/B`, `T016.P3.A`, `T022.P3.A`) closed. Acceptance gate green: lint 0, root suite 4080 passed / 5 skipped / 0 failed, desktop 445 / 0, `tsc -b` clean, check-architecture 0 errors, check:tampering 0, check:prompts 0 errors, security:check in sync, check:audit-prod 0 blocking. +43 tests. 4 forward-tier follow-ups recorded (`T023.P3.A` sync-on-next-release candidate; `HUB.P3.EXT.*` / `HUB.P3.NS` future; the 2 Hub-validator gaps remain Hub-owned). 0 defects, 0 coverage shortfalls, 0 suppressed warnings, 0 bypassed gates.
- **Carryforward tracked**: all four v1.4.0 P3 deferrals this cycle targeted are closed (`T022.P3.A` in Phase 6; `T018.P3.A`, `T018.P3.B`, `T016.P3.A` in Phase 4). The two Hub-validator gaps (`T017.P3.E`, `T002.P2.A`) remain Hub-repo-owned. None blocking.
- **Remaining phases**: none -- all 7 phases complete; v1.5.0 ready for release.
