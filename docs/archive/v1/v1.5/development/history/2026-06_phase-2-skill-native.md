# Session history: v1.5.0 Phase 2 -- Skill-native Adoptions

**Date**: 2026-06-10
**Cycle**: v1.5.0 (Local Agent Maturity)
**Phase**: 2 (Skill-native adoptions -- Bucket 2: items 11, 21)
**Plan reference**: [docs/versions/v1/v1.5.0/plans/adoption-ecosystem-2026-06.md](../../plans/adoption-ecosystem-2026-06.md)
**Source comparison**: [docs/versions/v1/v1.5.0/comparison-ecosystem-2026-06.md](../../comparison-ecosystem-2026-06.md)
**Branch (Nexus-AI, docs only)**: `feat/v1.5.0-phase-2-skill-native` (off the Phase 1 branch; v1.5.0 not yet merged to `main`)
**Branch (Nexus-Hub, skill deliverables)**: `feat/dci-discipline-and-agent-presets` (off `develop`; commit `786651f`)
**Acceptance scope**: ship the two `skill-native` items as Nexus-Hub catalog skills with no Nexus-AI runtime code change. Stability gate: `nexus-check --rule skill-duplicate-name` passes; `npm run check:prompts` clean for the new skills; no `core/` or `modules/` source changed.

---

## 1. Sub-tasks completed

| Sub-task | Output | Status |
|---|---|---|
| T005 (item 11) | DCI / hybrid-retrieval search-discipline skill. Authored `catalog/skills/developer-experience/direct-corpus-interaction/SKILL.md` in Nexus-Hub. Codifies the Direct Corpus Interaction discipline (hypothesize -> anchor with semantic/hybrid recall -> expand laterally with grep / call-graph trace / read -> verify exact strings, versions, error codes -> refine on dead ends), references Nexus's `HybridRetriever` (BM25 + dense + graph via RRF `k=60`) and the code-graph MCP (`codegraph_trace` / `callers` / `callees`) as substrate, and forbids pre-filtering evidence out of the reasoning loop. Source: DCI / GrepSeek (S2). | Closed |
| T006 (item 21) | Agent presets. Authored `catalog/skills/workflow/agent-presets/SKILL.md` in Nexus-Hub as a single skill (chosen shape, mirroring `workflow/context-modes`) documenting three preset bundles -- `morning-briefing`, `research`, `coding-assistant` -- each composing existing catalog skills + slash commands into a one-invocation workflow. Zero new tools, zero outbound surface. Source: OpenJarvis presets (S4). | Closed |
| T007 | Validation + stabilization. Hub `scripts/validate_skills.py` PASS on both (0 errors; `--quality` 0 warnings). Nexus-AI regression: `nexus-check --rule skill-duplicate-name` 0 findings; `check:prompts` exit 0; working tree clean (no `core/` / `modules/` change). Regenerated `data/skills.json` + `data/SKILL_INDEX.md`; committed on the Hub branch. | Closed |

## 2. Design decisions & deviations from the plan text

| # | Decision / deviation | Resolution |
|---|---|---|
| D1 | The plan describes items 11 / 21 as "Nexus-Hub skills"; the Nexus-AI repo also has an in-repo bundled catalog at `modules/coding/skills/catalog`. | Authored both skills in the separate **Nexus-Hub** repo (`catalog/skills/...`), the canonical catalog the plan and comparison name (item 11 path `developer-experience/direct-corpus-interaction` is given verbatim). The in-repo `nexus-check` / `check:prompts` runs are regression checks confirming the bundled catalog stayed untouched; they do not see the new Hub skills. This satisfies the "no `core/` or `modules/` source changed" gate. |
| D2 | The plan leaves item 21's shape open ("a small set of agent-preset bundles"). | Chose a **single** `workflow/agent-presets` skill documenting all three bundles as sections (user decision), mirroring the existing `workflow/context-modes` precedent, rather than three separate catalog entries. One discoverable entry, less index surface. |
| D3 | The Nexus-Hub working tree is shared across concurrent sessions; the user asked for isolation so this work does not interfere. | Created a dedicated Hub branch `feat/dci-discipline-and-agent-presets` off `develop`, staged only the two `SKILL.md` files + the regenerated `data/skills.json` / `data/SKILL_INDEX.md` (never `git add -A`), and left a pre-existing antigravity-template WIP untouched. The `data/templates.json` rebuild side-effect (derived from that WIP, not mine) was restored to HEAD so it did not enter my commit. |
| D4 | The Hub category `README.md` / `CATALOG.md` indexes are human-facing and already stale (the `workflow` README claims 14 skills against an actual 38) and are not regenerated by `build_skills_catalog.py` nor gated by CI. | Left them untouched (out of scope; editing two entries into a 24-stale manual index would not make it correct). The authoritative machine catalog (`data/skills.json`) and generated `data/SKILL_INDEX.md` -- the surfaces `nexus skills sync` and the MCP `search_skills` consume -- were regenerated and do include both skills. |
| D5 | The Hub validator enforces a hard 250-character single-line `description` limit. | Both descriptions were trimmed to <= 250 chars (243 and 249) while preserving the trigger nouns; all four required frontmatter fields (`name`, `description`, `summary_l0`, `overview_l1`) present, `summary_l0` <= 15 words, `overview_l1` <= 150 words. |

## 3. Open items added to known-gaps

One forward-tier follow-up recorded in [docs/versions/v1/v1.5.0/known-gaps.md](../../known-gaps.md) (`candidate`, not a defect):

- `T005.P3.A` (P3/DF) -- the two skills are authored, validated, and committed on the Hub branch `feat/dci-discipline-and-agent-presets`, but not yet merged to the Hub `develop` / `main` line and not yet reflected by `nexus skills sync`. Phase 7 (T023) publishes + syncs them (closes `T005.P3.A`).

## 4. Verification evidence

- Hub `python scripts/validate_skills.py --path <skill>` on both: **PASS** (0 errors, 5 optional-field warnings each -- `version` / `tags` / `author` / `license` / `category` -- matching the catalog norm, e.g. `tool-design` / `context-modes`).
- Hub `validate_skills.py --quality` on both: **PASS** (0 warnings -- `## Common Rationalizations`, binary `## Verification` checklist, `## Related Skills` with `[[ ]]` cross-links, and Tier-1 word budgets all satisfied).
- Nexus-AI `node bin/nexus-check.mjs --rule skill-duplicate-name modules/coding/skills/catalog` -> **0 findings**.
- Nexus-AI `npm run check:prompts` -> **exit 0** (one pre-existing `review-pr/SKILL.md` ~811-token oversize **warning** in the bundled catalog, unrelated to this phase; no errors).
- Nexus-AI `git status` -> clean: **no `core/` or `modules/` source changed** (the Phase 2 stability gate).
- Hub commit `786651f` on `feat/dci-discipline-and-agent-presets` (4 files: the two `SKILL.md` + `data/skills.json` + `data/SKILL_INDEX.md`). **Not pushed** (push gated on user confirmation).
- No outbound call introduced: pure documentation skills; no code, no dependency.

## 5. Next steps

- Advance to Phase 3 (Inbound security): the inbound prompt-injection classifier (item 3, T008-T009), warn-then-allow default.
- Phase 7 (T023): merge / publish the Hub branch `feat/dci-discipline-and-agent-presets` with attribution, run `nexus skills sync` so the consumed catalog reflects the two skills (closes `T005.P3.A`).
- Push of both the Nexus-Hub skill commit and the Nexus-AI docs commit is pending user confirmation.
