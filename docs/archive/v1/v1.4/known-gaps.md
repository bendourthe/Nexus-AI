# v1.4.0 -- Known Gaps, Deferrals, and Carryovers

**Status**: finalized (2026-06-09). v1.4.0 is the claude-code-harness adoption + known-gaps-closure + Nexus-Hub-sync cycle ([plans/adoption-claude-code-harness.md](plans/adoption-claude-code-harness.md), derived from [../v1.3.0/comparison-claude-code-harness.md](../v1.3/comparison-claude-code-harness.md)). Phase 1 (2026-05-30) ships the four skill-native adoption items (A3, A7, A12, A11) as documentation conventions under [development/](development); no `core/` or `modules/` surface is touched. Phases 2-6 land the code-shaped adoptions (network/subprocess hardening, static-analysis + CI gates, the safety-config SSOT, operator tooling, and worktree-isolated parallel execution). Phases 7-8 close the 36 carryforward gaps from v1.1.0 / v1.2.0 / v1.3.0. Phase 9 (final) syncs Nexus-Hub and runs the whole-plan acceptance gate. This file is appended phase-by-phase; items move to `## 2. Resolved` when closed; the `## 3. Summary` is recomputed each pass.

**Audience**: v1.4.0 phase authors, code reviewer, future-cycle planners
**Last updated**: 2026-06-09 (Phase 9 -- Nexus-Hub sync + whole-plan acceptance gate, FINAL)
**Sibling reviews**: [../v1.3.0/known-gaps.md](../v1.3/known-gaps.md), [../v1.2.0/known-gaps.md](../v1.2/known-gaps.md), [../v1.1.0/known-gaps.md](../v1.1/known-gaps.md) (the upstream gap logs; their open items are the 36-item carryforward set this cycle resolves in Phases 7-9), and [plans/adoption-claude-code-harness.md](plans/adoption-claude-code-harness.md) (the active plan).

**Cycle context**: This file is created in Phase 1 (rather than at the first code-shaped phase) because the implement-phase post-phase sequence appends gaps every phase. Phase 1 is documentation-only and introduces no new gap; the seeded sections below are forward-compatible with the per-sub-task ledger that Phases 2-9 will append.

**Wording convention**: every prose claim in this file follows [development/evidence-and-support-tiers.md](development/evidence-and-support-tiers.md) (A7): a gap describes its unbuilt capability at tier `future` or `candidate` (never `supported`), and the "Suggested next step" states what cited evidence would raise the tier. "not_observed != absent" applies throughout.

Each entry has a severity tag:

- **P0** -- release-blocker for v1.4.0 (must close)
- **P1** -- should-fix in v1.4.0
- **P2** -- nice-to-have; documented for completeness
- **P3** -- out-of-scope for v1.4.0; explicitly recorded for future planning

Each entry has a category tag:

- **NI** (not implemented) -- a plan sub-task that was skipped
- **DF** (deferred) -- a plan sub-task explicitly deferred to a later phase / cycle
- **BG** (bug) -- a deviation that revealed a real defect
- **MT** (missing tests) -- a coverage shortfall
- **WN** (warning) -- a suppressed lint or runtime warning
- **QG** (quality gate) -- a Phase 7 gate the cycle author bypassed with "Proceed anyway"

---

## 0. Adoption Ledger

Per-sub-task closure ledger for the claude-code-harness adoption plan. Rows land as each phase closes.

### claude-code-harness adoption (adoption-claude-code-harness)

| Plan sub-task | Item | Status | Closing reference |
|---|---|---|---|
| T001 | A3 -- pre-commit/pre-PR self-review checklist (skill-native) | Resolved | Phase 1 (2026-05-30); [development/self-review-checklist.md](development/self-review-checklist.md) encodes G1-G5 (DRY / all-symbols-called / DoD-verified-with-evidence / no-test-regression / TDD-red-evidence); referenced from [.github/PULL_REQUEST_TEMPLATE.md](../../../.github/PULL_REQUEST_TEMPLATE.md) Submission Checklist |
| T002 | A7 -- "not_observed != absent" evidence + support-tier convention (skill-native) | Resolved | Phase 1 (2026-05-30); [development/evidence-and-support-tiers.md](development/evidence-and-support-tiers.md) (four tiers: supported / internal-compatible / candidate / future); anchored from [AGENTS.md](../../../AGENTS.md) Critical Rules; governs this file's wording |
| T003 | A12 -- evidence-pack discipline for PR/release (skill-native) | Resolved | Phase 1 (2026-05-30); [development/evidence-pack.md](development/evidence-pack.md) ("verified-only"; "PR ready is not release ready"); referenced from [.github/PULL_REQUEST_TEMPLATE.md](../../../.github/PULL_REQUEST_TEMPLATE.md); upstream gate is T001 |
| T004 | A11 -- stakeholder HTML surfaces (skill-native) | Resolved | Phase 1 (2026-05-30); [development/stakeholder-surfaces.md](development/stakeholder-surfaces.md) with three self-contained zero-outbound HTML templates (plan brief / progress / acceptance) |
| T005 | Phase 1 testing + stabilization | Resolved | Phase 1 (2026-05-30); `nexus-check --rule skill-duplicate-name` -> 0 findings (exit 0); `npm run check:prompts` -> 0 errors (1 pre-existing `review-pr/SKILL.md` oversized warning, unrelated); all four docs ASCII-clean; `git diff --stat` confirms no `core/` or `modules/` change |
| T006-T008 | A4 -- network-egress denylist (re-full); A5 -- run_terminal env scrubbing (re-full) | Resolved | Phase 2 (2026-05-30); A4 in [../../../../modules/coding/utils/ssrf.ts](../../../modules/coding/utils/ssrf.ts) (default cloud-metadata + paste-host denylist, exact-or-sub-domain match, enforced pre- and post-redirect across fetch_page / web_search / OTLP, extensible per-call and via `nexus.coding.egressDenyExtra` wired at [../../../../src/runtime/NexusCodingRuntime.ts](../../../src/runtime/NexusCodingRuntime.ts)); A5 in new [../../../../core/observability/scrubEnv.ts](../../../core/observability/scrubEnv.ts) consumed by [../../../../src/tools/handlers/terminal.ts](../../../src/tools/handlers/terminal.ts) (name- and value-based scrub reusing `redactSecrets`, allowlist opt-in, `nexus.coding.terminalEnvScrub` toggle default on); 144 new/extended unit assertions; full suite 3782 passed, coverage 87.05% |
| T009-T011 | A2 -- test-tampering detection rules (re-full); A9 -- OpenSSF Scorecard CI workflow (re-full) | Resolved | Phase 3 (2026-05-30); A2 = five deterministic, LLM-free rules under [../../../../lib/checks/](../../../lib/checks) (`no-focused-tests`, `no-skipped-tests-without-reason`, `no-tautological-assertion`, `no-commented-out-assertion`, `no-disabled-ci-check`) registered in [index.mjs](../../../lib/checks/index.mjs); the walker ([bin/nexus-check.mjs](../../../bin/nexus-check.mjs)) gained a `scannedExtensions` opt-in so the CI rule reaches workflow YAML; wired via the new `check:tampering` script into [.husky/pre-push](../../../.husky/pre-push) and the CI `nexus-check` job. A9 = [.github/workflows/scorecard.yml](../../../.github/workflows/scorecard.yml) (SHA-pinned `ossf/scorecard-action` v2.4.3, weekly + push + branch-protection triggers, SARIF upload), mirroring `codeql.yml`. 35 new tests; full suite 332 files / 0 failed, coverage 87.05% lines |
| T012-T014 | A1 -- safety-config SSOT + generator + CI drift gate (re-full) | Resolved | Phase 4 (2026-05-30); SSOT at [nexus.security.toml](../../../nexus.security.toml) authors the egress denylist (A4) + secret-path denylist and carries a generated `[permissions]` mirror of [src/guardrails/PermissionTiers.ts](../../../src/guardrails/PermissionTiers.ts) (which stays canonical per Section 13 -- no competing permissions source). The extended generator [scripts/generate-tool-permission-table.mjs](../../../scripts/generate-tool-permission-table.mjs) regenerates the runtime artifact [modules/coding/utils/generated/safetyConfig.generated.ts](../../../modules/coding/utils/generated/safetyConfig.generated.ts) (imported by `ssrf.ts` + `secretPaths.ts`), the harness-hook copy [scripts/hooks/lib/secret-paths.mjs](../../../scripts/hooks/lib/secret-paths.mjs), the TOML perms mirror, and the architecture-doc table. CI drift gate `npm run security:check` (alias of `perm-tier:check`) wired in [.github/workflows/ci.yml](../../../.github/workflows/ci.yml); 11 new tests; full suite 333 files passed, coverage 87.05% lines |
| T015-T017 | A6 -- `nexus doctor --migration-report` (re-full); A8 -- PreCompact WIP-detection + checkpoint hook (re-full) | Resolved | Phase 5 (2026-05-30); A6 = read-only stale-state inventory in [../../../../core/diagnostics/DoctorReport.ts](../../../core/diagnostics/DoctorReport.ts) (legacy `~/.gemma-code/`, stale caches, duplicate skills, dangling symlinks, memory state) wired live into [../../../../bin/nexus.mjs](../../../bin/nexus.mjs) as `nexus doctor [--migration-report] [--json]`; never mutates disk (integration test snapshots the tree before/after). A8 = [../../../../core/lifecycle/PreCompactHook.ts](../../../core/lifecycle/PreCompactHook.ts) `attachPreCompactWipHook` subscribing to the existing `lifecycle.context.preCompact` event (no new event kind): detects uncommitted git edits + caller-supplied in-flight tasks, persists a restorable checkpoint to `~/.nexus/checkpoints/<sessionId>.json`, and emits a non-blocking `lifecycle.notification` warning. 40 new tests; full suite 3863 passed, coverage 87.09% lines. Live daemon wiring of the A8 hook is deferred (`T016.P3.A`, parallels `5.4.P3.T`). |
| T018-T019 | A10 -- worktree-isolated parallel sub-agent execution (re-partial) | Resolved | Phase 6 (2026-05-31); new [../../../../src/agents/WorktreeManager.ts](../../../src/agents/WorktreeManager.ts) (detached `git worktree add`/`remove` lifecycle, fault-tolerant like `GitSafetyNet`, removes a worktree only when left unchanged), opt-in `isolate` flag on [../../../../src/agents/types.ts](../../../src/agents/types.ts) `SubAgentConfig` (default off), additive `root` override on [../../../../src/tools/handlers/pathGuard.ts](../../../src/tools/handlers/pathGuard.ts) `resolveInsideWorkspace` + `_rootOverride` on [../../../../src/tools/handlers/terminal.ts](../../../src/tools/handlers/terminal.ts) `RunTerminalTool`, wired through [../../../../src/agents/SubAgentManager.ts](../../../src/agents/SubAgentManager.ts) (`setWorktreeManager`, worktree created/cleaned around the run, `run_terminal` rooted at it). 13 new tests (8 unit + 5 integration incl. real-git parallel no-collision proof); full suite 3876 passed, 0 failed; lint + check-architecture clean. Live runtime wiring and the read-tool rooting / team-orchestration extensions are deferred (`T018.P3.A`, `T018.P3.B`). |
| T020-T024 | architectural carryforward (src->modules/coding move; TS project refs; Tree-sitter scanner; HNSW index) | Resolved | Phase 7 (2026-05-31); see the Phase 7 section below. Closes carryforward `1.4.P1.B`, `1.1.P1.A`, `3.3.P2.G`, `4.1.P2.J`, `6.1.P3.V`, `4.2.P3.K`. Full suite 3888 passed, 0 failed; `tsc -b` builds core then root in dependency order with no double-emit; check-architecture 0 errors; lint clean; security:check in sync. One new follow-up: `T022.P3.A` (bundle grammar .wasm into the packaged extension/sidecar). |
| T025-T031 | wiring / deferrals / the P1 CVE carryforward | Resolved | Phase 8 (2026-06-02); see the Phase 8 section below. Closes 22 carryforward gaps: the lone P1 `7.x.P1.D` (embedder migrated to `@huggingface/transformers`), the wiring set (`5.3.P2.R`, `5.3.P3.S`, `6.1.P3.W`, `5.4.P3.T`, `5.2.P3.Q`, `5.1.P2.P`, `5.1.P2.O`, `6.2.P2.X`, `6.2.P3.Y`, `6.3.P2.Z`), the hygiene set (`2.4.P2.E`, `2.4.P3.F`, `4.3.P3.M`, `4.x.P3.N`, `3.4.P3.H`, `3.5.P3.I`, `6.1.P3.U`), and the benchmarks/audit set (`4.4.P2.L`, `7.1.P2.A`, `T012.P2.C`, `T013.P3.D`). Full suite 3901 passed (+ desktop 418), 0 failed; check:audit-prod 0 high/critical; tsc -b clean; check-architecture 0 errors; lint + security:check clean. No new open gap introduced. |
| T032-T035 | Nexus-Hub sync + whole-plan acceptance gate | Resolved | Phase 9 (2026-06-09); see the Phase 9 section below. T032 = [development/nexus-hub-integration-delta.md](development/nexus-hub-integration-delta.md); T033 = upstream coordinate fixed (`DEFAULT_UPSTREAM` `bendourthe/DevAI-Hub` -> `bendourthe/Nexus-Hub`) in [../../../../core/skills/DevAIHubSyncer.ts](../../../core/skills/DevAIHubSyncer.ts), closing `1.1.P3.B` (+ confirming `1.1.P2.A`); `T017.P3.E` / `T002.P2.A` re-justified as Hub-owned. T034 = whole-plan acceptance gate green (build/lint/check-architecture/`check src`/check:tampering/security:check/check:audit-prod/test+coverage all pass; the newly-surfaced `hono` prod advisory was fixed in-phase via an `overrides` pin to `^4.12.21` resolving 4.12.25). T035 = this finalization + [RELEASE_NOTES.md](RELEASE_NOTES.md). |

---

## 1. Open Items

### Phase 1 (skill-native adoptions)

No new gap. Phase 1 is documentation-only: it created four convention docs under [development/](development) plus the two reference edits required by acceptance ([.github/PULL_REQUEST_TEMPLATE.md](../../../.github/PULL_REQUEST_TEMPLATE.md) +2 lines, [AGENTS.md](../../../AGENTS.md) +1 line). No deviation, test failure, coverage shortfall, suppressed lint, or bypassed quality gate occurred.

### Phase 2 (network & subprocess hardening)

No new gap. A4 and A5 both landed with full test coverage and no bypassed gate. One informational deviation was recorded and resolved in-phase: the plan prompts cite `src/utils/ssrf.ts`, but that sub-tree was migrated to `modules/coding/utils/ssrf.ts` in v1.1.0 Phase 3 (this is exactly the partial-move state that gap `1.4.P1.B` tracks, due to close in Phase 7); the denylist was implemented at the live path. The A4 requirement that the denylist "apply to FetchPageTool, WebSearchTool, and the OTLP exporter" was satisfied structurally by adding the check inside the shared guard functions (`isSsrfBlockedSync` / `isSsrfBlocked`, the latter re-run on every redirect hop by `fetchWithSsrfGuard`), since all three consumers already route through them, rather than by editing each consumer. No test failure, coverage shortfall (overall 87.05% lines; `scrubEnv.ts` 100%; `terminal.ts` 87.79%), suppressed lint, or bypassed quality gate occurred. `npm run check-architecture` reports 0 errors (11 pre-existing warnings, none in the files this phase touched).

### Phase 3 (static-analysis & CI gates)

No new gap. A2 (test-tampering rules) and A9 (OpenSSF Scorecard workflow) both landed with full test coverage and no bypassed gate. Three informational deviations were recorded and resolved in-phase:

- **D1 (no new gap):** the A2 rule tests live at [../../../../tests/unit/lib/checks-tampering-rules.test.ts](../../../tests/unit/lib/checks-tampering-rules.test.ts) rather than the plan's suggested `tests/unit/checks/`, matching the established `tests/unit/lib/checks-prompt-rules.test.ts` location that side-steps the historical vitest + Windows + node:vm cli-dir parse bug (10.O.D). Trigger substrings in that file are assembled at runtime so the file does not trip its own `check:tampering` scan.
- **D2 (no new gap):** the plan cites `lib/checks/index.mjs` and the husky pre-push `npm run check` as the wiring points; both were honoured. The CI wiring was added as a second step on the existing `nexus-check` job (dependency-free, no `npm ci`) rather than a new job, to keep the runner footprint small.
- **D3 (no new gap):** the four pre-existing legitimate `continue-on-error: true` usages (ci.yml x2, codeql.yml, coverage-diff.yml) were annotated with explicit `nexus-check-allow: no-disabled-ci-check` justification markers so the new CI rule scans clean; no behavioural change to those workflows. The `isAllowed` helper now also accepts the canonical `nexus-check-allow*` spelling alongside the legacy `gemma-check-allow*` (backward compatible).

No test failure, coverage shortfall (overall 87.05% lines; the new `.mjs` rules are out of the coverage `include` set but are exercised by 35 new assertions), suppressed lint, or bypassed quality gate occurred. `npm run check:tampering` reports 0 findings over `tests/` + `.github/workflows/`; `--list-rules` shows all 15 rules.

### Phase 4 (safety config SSOT)

No new gap. A1 (the safety-config SSOT, generator, and CI drift gate) landed as a behavior-preserving refactor with full test coverage and no bypassed gate. Three informational deviations were recorded and resolved in-phase:

- **D1 (no new gap):** the plan prompt cites `src/utils/ssrf.ts`, but that sub-tree lives at `modules/coding/utils/ssrf.ts` (the partial-move state tracked by `1.4.P1.B`, due to close in Phase 7). Same note as Phase 2; the SSOT-generated artifact and the rewired guards were implemented at the live paths.
- **D2 (design decision, no new gap):** the plan offered "keep `PermissionTiers.ts` canonical OR generate it from the SSOT -- pick one and document it", and Section 13 of the source comparison decides it ("avoid two sources of truth for permissions"). Decision: `PermissionTiers.ts` remains the canonical permission-tier source; `nexus.security.toml` captures the tiers as a GENERATED, drift-gated mirror (not an authored second source). The egress denylist (A4) and secret-path denylist are AUTHORED in the SSOT and generated into the committed runtime artifacts. Documented in the SSOT header, the generator header, and the rewired guards.
- **D3 (deliberate scope boundary, no new gap):** the secret *value* redaction regexes in [../../../../core/observability/redactSecrets.ts](../../../core/observability/redactSecrets.ts) are intentionally left outside the SSOT (regexes do not round-trip cleanly through TOML and are bound to the scrubber's own logic). The plan's three named safety surfaces (permission tiers, egress denylist, secret-path denylist) are all covered; value-pattern SSOT integration is `future`-tier and not required by A1.

No test failure, coverage shortfall (overall 87.05% lines; the generated data module and the generator `.mjs` are exercised by the 11 new assertions in [../../../../tests/unit/scripts/security-ssot-generator.test.ts](../../../tests/unit/scripts/security-ssot-generator.test.ts)), suppressed lint, or bypassed quality gate occurred. `npm run security:check` confirms all four safety surfaces are in sync with the SSOT; `npm run check-architecture` reports 0 errors (11 pre-existing warnings, none in the files this phase touched).

### Phase 5 (operator tooling & lifecycle)

A6 (`nexus doctor`) landed fully live-wired: the subcommand is reachable from [../../../../bin/nexus.mjs](../../../bin/nexus.mjs) `main()` and exercised end-to-end by [../../../../tests/integration/doctor-cli.test.ts](../../../tests/integration/doctor-cli.test.ts), which also asserts the inventory leaves the scanned tree byte-for-byte unchanged (the read-only contract). No deviation, test failure, coverage shortfall, suppressed lint, or bypassed quality gate occurred.

A8 (PreCompact WIP hook) landed as an attachable hook (`attachPreCompactWipHook`) with full unit coverage, deliberately mirroring the shape of the existing `attachSessionReflectionHook` reference implementation. The hook subscribes to the existing `lifecycle.context.preCompact` event rather than introducing a 14th event kind, honoring the plan's "reuse the existing 13-event lifecycle bus" instruction; the "PostCompact can restore" half is satisfied by the persisted checkpoint plus the `readCompactionCheckpoint(sessionId)` restore primitive. One deferral was recorded:

| ID | Sev / Cat | Source phase | Plan reference | Reason | Suggested next step |
|---|---|---|---|---|---|
| `T016.P3.A` | P3 / DF | Phase 5 (T016, A8) | [plans/adoption-claude-code-harness.md](plans/adoption-claude-code-harness.md) sub-task 5.2 | The A8 hook ships attachable and unit-tested but is not yet called at daemon session construction, exactly parallel to the open reflection-hook wiring gap `5.4.P3.T` (the daemon does not live-wire either hook yet). A8's acceptance (the hook fires on the PreCompact event and warns without blocking compaction) is met without daemon wiring. | Live-wire `attachPreCompactWipHook(hookBus)` once at session construction alongside `attachSessionReflectionHook` when `5.4.P3.T` is resolved in Phase 8 (T027); add an integration test proving the warning fires on a real compaction. Cited evidence (a passing production-path integration test) raises this from `candidate` to `supported`. |

### Phase 6 (parallel agent execution)

A10 (worktree-isolated parallel sub-agent execution) landed as the re-partial adoption the source comparison specified: worktree isolation shipped; the full Breezing-style team-orchestration layer was deliberately not. Isolation is opt-in (`SubAgentConfig.isolate`, default off) and degrades gracefully to the shared workspace when no `WorktreeManager` is wired or the workspace is not a git repo. The acceptance criterion is met by [../../../../tests/integration/agents/worktree-isolation.test.ts](../../../tests/integration/agents/worktree-isolation.test.ts): two parallel write-capable `run_terminal` executions in separate worktrees do not collide, and `cleanupIfUnchanged` removes an unchanged worktree while retaining a modified one. One scope decision and two deferrals were recorded:

- **Scope decision (no new gap):** isolation roots only `run_terminal` at the worktree, because per [ADR-0004](../../adr/0004-sub-agent-isolation-contract.md) `run_terminal` is the sole file-mutation surface in every sub-agent tool scope (verification / research / planning all exclude write_file / edit_file / create_file / delete_file). Rooting the mutation surface fully satisfies the no-collision acceptance without threading a root override through the safety-critical module-level path-guard helpers used by the read tools.

| ID | Sev / Cat | Source phase | Plan reference | Reason | Suggested next step |
|---|---|---|---|---|---|
| `T018.P3.A` | P3 / DF | Phase 6 (T018, A10) | [plans/adoption-claude-code-harness.md](plans/adoption-claude-code-harness.md) sub-task 6.1 | Worktree isolation ships attachable (`SubAgentManager.setWorktreeManager`) and opt-in (`config.isolate`), fully unit + integration tested, but the runtime bootstrap does not yet call `setWorktreeManager(new WorktreeManager(workspaceRoot))` and the `DAGExecutor` dispatch path does not yet set `isolate: true` for concurrently-dispatched write-capable nodes -- so production dispatch still runs on the shared workspace. Exactly parallel to the A8-hook wiring gap `T016.P3.A`. A10's acceptance (no-collision when isolation is enabled; worktree removed when unchanged) is met without bootstrap wiring. | Wire `setWorktreeManager` at session/bootstrap construction and enable `isolate` for parallel write-capable DAG nodes; add a production-path integration test. Cited evidence (a passing production-path test showing real dispatched sub-agents isolated) raises this from `candidate` to `supported`. |
| `T018.P3.B` | P3 / DF | Phase 6 (T018, A10) | [plans/adoption-claude-code-harness.md](plans/adoption-claude-code-harness.md) sub-task 6.1 | Two re-partial extensions deferred: (1) the read tools (`read_file` / `list_directory` / `grep_codebase`) are not rooted at the worktree, so a write-then-read of the same file within one isolated run does not observe the in-worktree `run_terminal` write (reads of unmodified tracked files are equivalent since the worktree is a HEAD checkout); (2) the full Breezing-style Planner/Critic/Worker team-orchestration layer was deferred per the plan. | (1) Thread the `pathGuard` `root` override through the read tools' module-level resolution helpers if a consumer needs write-then-read parity inside a worktree. (2) Plan the team-orchestration layer as its own cycle item if multi-agent planner/critic/worker composition is wanted. |

### Phase 7 (architectural carryforward)

All four sub-tasks landed and the six carryforward gaps they target are resolved (moved to `## 2. Resolved`). Full suite 3888 passed, 0 failed; `tsc -b` clean; check-architecture 0 errors / 11 pre-existing warnings; lint clean; security:check in sync. Four scope decisions / deviations were recorded and resolved in-phase:

- **D1 (no new gap):** T020's plan prompt says `src/llm` "merges into `core/llm`", but `core/llm` does not exist and `modules/coding/llm` already holds `PromptFormat.ts` / `ToolCallFormat.ts`. Because the architecture boundary forbids `core/**` importing `modules/**` and the LLM client drivers are coding-module code, `src/llm` was merged into `modules/coding/llm` (the live location), not `core/llm`. The `no-llm-outside-llm-folder` dependency-cruiser rule was repointed accordingly.
- **D2 (no new gap):** T020 landed as one atomic, fully-verified commit rather than 12 per-sub-tree commits. The import codemod can only re-point external importers; moving sub-trees one at a time would leave each moved tree's own cross-tree imports dangling until its siblings also moved (or force double-churn rewriting the same imports twice). Moving all 12 together preserves sibling offsets and yields a single green state.
- **D3 (no new gap):** T021 deliberately leaves the `desktop/` workspace outside the root `tsc -b` reference graph. desktop bundles `core` via esbuild (sidecar) / Vite (UI) and type-checks with its own `tsc --noEmit`; it consumes `core` as bundled source, not tsc project-reference output, so a reference there would add risk with no build-ordering benefit. The acceptance (core + modules/coding build in dependency order, no double-emit) holds for every project that emits via tsc.
- **D4 (minor simplification, no new gap):** the Tree-sitter (T022) extractor tags Python `def` and Rust `fn` uniformly as `function` (it does not reclassify class-nested defs / impl methods to `method` via ancestry). The four language fixtures and the codegraph integration tests do not depend on that distinction, and caller attribution uses line-range containment regardless of kind. TS `method_definition` and Go `method_declaration` are still tagged `method` (the grammar distinguishes them directly).

| ID | Sev / Cat | Source phase | Plan reference | Reason | Suggested next step |
|---|---|---|---|---|---|
| `T022.P3.A` | P3 / DF | Phase 7 (T022, gap 3.3.P2.G) | [plans/adoption-claude-code-harness.md](plans/adoption-claude-code-harness.md) sub-task 7.3 | The Tree-sitter scanner is implemented, tested, and warmed up at extension activation, and works from source (node/tests/dev). The grammar `.wasm` files (`tree-sitter-wasms/out/*.wasm`) and the `web-tree-sitter` runtime `.wasm` are not yet bundled into the packaged VSIX or the esbuild-bundled sidecar, so a packaged install falls back to the regex extractor until they are. `extractSymbols` degrades gracefully (regex fallback), so this is a packaging/coverage gap, not a correctness gap. | Add the grammar + runtime `.wasm` to the VSIX `files` / esbuild copy step and add a sidecar `initTreeSitter()` warm-up; add a packaged-app integration test asserting `isTreeSitterReady()` is true after activation. Cited evidence (a passing packaged-app test) raises this from `candidate` to `supported`. |

### Phase 8 (wiring, deferrals & the P1 CVE)

All seven sub-tasks landed and the 22 carryforward gaps they target are resolved (moved to `## 2. Resolved`). **No new open gap was introduced.** Full suite 3901 passed, 0 failed; desktop suite 418 passed; `check:audit-prod` 0 high/critical (only the documented `brace-expansion` moderate, allowlisted); `tsc -b` clean; `check-architecture` 0 errors / 10 warnings; lint (root + shell) clean; `check:tampering` 0 findings; `security:check` in sync. Two scope decisions and five explicit closures-with-rationale were recorded and resolved in-phase:

- **D1 (gap 7.x.P1.D, no new gap):** plan option (a) -- "upgrade `@xenova/transformers`" -- was infeasible because 2.17.2 is its final release (the package is abandoned). Resolved via option (b): migrated `core/memory/LocalEmbedder.ts` to the maintained successor `@huggingface/transformers@4.x`, whose `onnxruntime-web@1.26` drops `onnx-proto` and pulls patched `protobufjs@^7.2.4`. The critical + three high advisories are gone; the allowlist now carries only the unrelated `brace-expansion` moderate. The real ONNX weights remain a runtime/installer concern (unchanged from the prior package; the dynamic-import + hash-fallback contract is intact).
- **D2 (gap 5.2.P3.Q, supported-mechanism):** the path-scope reevaluation wiring is live and unit-tested end-to-end (SkillLoader parses `pathScope` frontmatter; `AgentLoop.reevaluateSkillsForPath` fires on focus change via `activeEditPathProvider` and emits `lifecycle.skill.entry`). Its visible effect scales with how many skills declare `pathScope` frontmatter -- a content authoring choice, not a code gap. The mechanism is `supported`; broad path-scoped skill authoring is left to users.
- **Explicit closures (documented at each code site):** `2.4.P3.F` (keep tee footer on the result -- co-located with its teePath), `4.3.P3.M` (keep the `.mjs` migration wrapper -- no scripts tsconfig; typed logic already in the `.ts`), `4.x.P3.N` (`ingestFile` is the single code-aware ingest API; nothing bypasses it), `3.4.P3.H` (codegraph stays in-process -- no external consumer), `6.1.P3.U` (keep `fs.watch` -- covers every platform; chokidar unjustified), `6.2.P3.Y` (LSP definition/references subset is intentional), `7.1.P2.A` (token-usage benchmark stays a deterministic synthesis), `T013.P3.D` (MinHash/LSH not a cost driver at the ~230-skill scale).

No deviation introduced a defect, test failure, coverage shortfall, suppressed lint, or bypassed quality gate. T016.P3.A (A8 PreCompact daemon wiring) is *not* closed by this phase: Phase 8 creates a session `HookBus` at bootstrap and wires the reflection hook to it (closing `5.4.P3.T`), but `attachPreCompactWipHook` is not attached because no production code emits `lifecycle.context.preCompact` on that bus yet -- attaching it would be inert. T016.P3.A stays open with the bootstrap HookBus now in place to make that wiring trivial.

### Phase 9 (Nexus-Hub sync + whole-plan acceptance gate -- FINAL)

The four Hub-dependent carryforward items are dispositioned (see [development/nexus-hub-integration-delta.md](development/nexus-hub-integration-delta.md) Section 6). Two are **resolved** (moved to `## 2. Resolved`); two are **explicitly re-justified as Hub-owned** and remain open against the Nexus-Hub repo (not closeable from Nexus-AI without mutating a separately-versioned, actively-developed repo -- the gaps themselves say "Track as a Nexus-Hub-side issue"):

- `1.1.P2.A` -> **Resolved**: the Hub's own v3.x release cycle ran `make build-catalog`; both target skills (`hallmark-design`, `html-output-conventions`) are present in the current Hub catalog (verified read-only against the local clone).
- `1.1.P3.B` -> **Resolved**: the documented blocker (stale upstream coordinate `bendourthe/DevAI-Hub`) is fixed to `bendourthe/Nexus-Hub`; offline `buildManifest` over the local Hub catalog enumerates 251 skills including both targets; live `sync --apply` against the published release is a now-unblocked operator step (requires network).
- `T017.P3.E` (Hub-owned, P3/DF) -> **open, re-justified**: draining `scripts/validate_skills.allowlist.json` requires editing Hub content; the read-only-on-Hub posture (the Hub is mid-cycle on v3.x with active concurrent commits) forbids it this phase. The Hub's v3.x `nexus-skill-scanner` + producer-catalog allowlist is the live owner. No Nexus-AI surface is affected.
- `T002.P2.A` (Hub-owned, P2/WN) -> **open, re-justified**: the 7 secret-scan false positives are in Nexus-Hub `scripts/validate_skills.py`; the fix is Hub-side and likely superseded by the Hub's v3.x fence-aware scanner. Nexus-AI's own sync path screens skills with `PromptInjectionScanner` and is unaffected.

Two new items were introduced/observed during the Phase 9 acceptance gate:

| ID | Sev / Cat | Source phase | Plan reference | Reason | Suggested next step |
|---|---|---|---|---|---|
| `T034.P2.A` | P2 / WN | Phase 9 (T034) | [plans/adoption-claude-code-harness.md](plans/adoption-claude-code-harness.md) sub-task 9.3 | `npm audit` (full tree) reports ~16 **dev-only** advisories (`ws`, `tmp`, `@inquirer/*`, `external-editor` chains). These are not gated by `check:audit-prod` (which is production-scope only) and do not ship to users. They were left unfixed to avoid pulling a broad, untraceable dev-dependency churn into a final-phase commit (a plain `npm audit fix` would touch 17 advisories across the dev tree). | Run `npm audit fix` (non-`--force`) as a standalone dev-hygiene commit, or upgrade `semantic-release` / `@inquirer` to drop the `tmp` / `external-editor` chains. Cited evidence (a clean full-tree `npm audit`) closes this. |

Net-new Nexus-Hub v3.x integration opportunities (the Hub's parallel v3.x expansion created surfaces Nexus-AI does not yet consume) are recorded as forward items for the in-flight v1.5.0 ecosystem cycle ([../v1.5.0/comparison-ecosystem-2026-06.md](../v1.5/comparison-ecosystem-2026-06.md)). They are **not** part of the original 36-item carryforward set and do not block v1.4.0:

| ID | Sev / Cat | Reason | Suggested next step |
|---|---|---|---|
| `HUB.P3.CMD` | P3 / DF | Hub `catalog/commands` (14 verb-first + aliases + 40 shims) are sparse-cloned but unconsumed by Nexus-AI. | Wire a Nexus-AI command router over `catalog/commands/*.md`. |
| `HUB.P3.AGENT` | P3 / DF | Hub `catalog/agents` (23 reviewer/specialist personas) sparse-cloned but unconsumed. | Wire into `src/agents/SubAgentManager.ts` as selectable personas. |
| `HUB.P3.HOOK` | P3 / DF | Hub `catalog/hooks` (30) not even in the sparse-checkout set. | Add to sparse-checkout + install consumer. |
| `HUB.P3.RULES` | P3 / DF | Hub language rules sparse-cloned but unconsumed. | Feed into the prompt/policy layer. |
| `HUB.P3.MCPCFG` | P3 / DF | Hub `catalog/mcp-configs/mcp-servers.json` not pulled. | Consume into the MCP bridge under the MCP Registry Policy. |
| `HUB.P3.EXT.*` | P3 / DF | 6 Hub `extensions/` MCP servers pulled but unregistered (most have in-process Nexus-AI equivalents already). | Evaluate registering / adopting per extension. |
| `HUB.P3.NS` | P3 / DF | The local `devai-hub` on-disk namespace was deliberately not renamed alongside the upstream `DevAI-Hub` -> `Nexus-Hub` coordinate fix (on-disk contract migration risk). | Migrate `~/.nexus/skills/devai-hub/` + the `source: "devai-hub"` provenance label + the IPC namespace enum to `nexus-hub` as a contained change with its tests. |

### Carryforward (resolved in Phases 7-9)

Of the 36 carryforward open items, **Phase 7 resolved 6**, **Phase 8 resolved 22**, and **Phase 9 resolved 2 + re-justified 2** (sets in `## 2. Resolved` / the Phase 9 section above). The 4 Nexus-Hub-dependent items split: `1.1.P2.A` + `1.1.P3.B` are **resolved** (skills-catalog flow unblocked); `T017.P3.E` + `T002.P2.A` remain **open but explicitly re-justified as Hub-owned** (they live in the Nexus-Hub repo and cannot be closed from Nexus-AI without mutating a separately-versioned, actively-developed upstream). They are not re-listed individually here to avoid divergence from the source of truth (see the linked files):

- **v1.1.0** ([../v1.1.0/known-gaps.md](../v1.1/known-gaps.md)): `1.4.P1.B`, `1.1.P1.A` -> Phase 7 -- **both resolved**.
- **v1.2.0** ([../v1.2.0/known-gaps.md](../v1.2/known-gaps.md)): `3.3.P2.G`, `4.2.P3.K`, `4.1.P2.J`, `6.1.P3.V` -> Phase 7 -- **resolved**. `7.x.P1.D`, `5.3.P2.R`, `5.3.P3.S`, `6.1.P3.W`, `5.4.P3.T`, `5.2.P3.Q`, `5.1.P2.P`, `5.1.P2.O`, `6.2.P2.X`, `6.2.P3.Y`, `6.3.P2.Z`, `2.4.P2.E`, `2.4.P3.F`, `4.3.P3.M`, `4.x.P3.N`, `3.4.P3.H`, `3.5.P3.I`, `6.1.P3.U`, `4.4.P2.L`, `7.1.P2.A` -> Phase 8 -- **all 20 resolved**. `1.1.P2.A`, `1.1.P3.B` (Hub catalog rebuild / release-flow) -> Phase 9 (T033) -- **both resolved** (the Hub's own v3.x cycle ran build-catalog + cut release tags containing the skills; the Nexus-AI-side upstream-coordinate blocker is fixed). Not scoped by the v1.4.0 plan's Phase 8 sub-tasks: `7.x.P3.B` and `7.x.P3.C` are self-documented no-action items (the plan paths were typos; the lived conventions are correct), and `1.3.P2.C` (hooks-over-prompts migrations) is satisfied by the `.claude/` harness hooks already in place (git-guardrails, format-/require-description) -- **Phase 9 confirm-and-closed**.
- **v1.3.0** ([../v1.3.0/known-gaps.md](../v1.3/known-gaps.md)): `T012.P2.C`, `T013.P3.D` -> Phase 8 -- **both resolved**. `T017.P3.E`, `T002.P2.A` -> Phase 9 (T033) -- **open, re-justified as Hub-owned** (cannot be closed from Nexus-AI; tracked against the Nexus-Hub repo).

---

## 2. Resolved

| Item | Severity / Category | Resolved in | Evidence |
|---|---|---|---|
| A3 self-review checklist (T001) | n/a (adoption) | Phase 1 (2026-05-30) | [development/self-review-checklist.md](development/self-review-checklist.md) + PR template reference |
| A7 evidence + support tiers (T002) | n/a (adoption) | Phase 1 (2026-05-30) | [development/evidence-and-support-tiers.md](development/evidence-and-support-tiers.md) + AGENTS.md anchor |
| A12 evidence-pack discipline (T003) | n/a (adoption) | Phase 1 (2026-05-30) | [development/evidence-pack.md](development/evidence-pack.md) + PR template reference |
| A11 stakeholder HTML surfaces (T004) | n/a (adoption) | Phase 1 (2026-05-30) | [development/stakeholder-surfaces.md](development/stakeholder-surfaces.md) (3 templates) |
| A2 test-tampering rules (T009) | n/a (adoption) | Phase 3 (2026-05-30) | five rules under [../../../../lib/checks/](../../../lib/checks) + `check:tampering` wired into pre-push and CI |
| A9 OpenSSF Scorecard workflow (T010) | n/a (adoption) | Phase 3 (2026-05-30) | [../../../../.github/workflows/scorecard.yml](../../../.github/workflows/scorecard.yml) (SHA-pinned, weekly + push + branch-protection) |
| A1 safety-config SSOT (T012-T014) | n/a (adoption) | Phase 4 (2026-05-30) | [nexus.security.toml](../../../nexus.security.toml) SSOT + extended generator + `npm run security:check` CI drift gate; runtime guards read [modules/coding/utils/generated/safetyConfig.generated.ts](../../../modules/coding/utils/generated/safetyConfig.generated.ts) |
| A6 `nexus doctor` migration-report (T015) | n/a (adoption) | Phase 5 (2026-05-30) | [../../../../core/diagnostics/DoctorReport.ts](../../../core/diagnostics/DoctorReport.ts) read-only inventory + `nexus doctor` CLI surface in [../../../../bin/nexus.mjs](../../../bin/nexus.mjs); read-only contract proven by [../../../../tests/integration/doctor-cli.test.ts](../../../tests/integration/doctor-cli.test.ts) |
| A8 PreCompact WIP hook (T016) | n/a (adoption) | Phase 5 (2026-05-30) | [../../../../core/lifecycle/PreCompactHook.ts](../../../core/lifecycle/PreCompactHook.ts) `attachPreCompactWipHook` on the existing `lifecycle.context.preCompact` event (checkpoint + non-blocking warning); live daemon wiring tracked as `T016.P3.A` |
| A10 worktree-isolated parallel sub-agent execution (T018-T019) | n/a (adoption) | Phase 6 (2026-05-31) | [../../../../src/agents/WorktreeManager.ts](../../../src/agents/WorktreeManager.ts) + opt-in `isolate` flag + `run_terminal` worktree rooting through [../../../../src/agents/SubAgentManager.ts](../../../src/agents/SubAgentManager.ts); real-git parallel no-collision + cleanup-when-unchanged proven by [../../../../tests/integration/agents/worktree-isolation.test.ts](../../../tests/integration/agents/worktree-isolation.test.ts); live runtime wiring + read-tool/team-orchestration extensions deferred (`T018.P3.A`, `T018.P3.B`) |
| `1.4.P1.B` src -> modules/coding wholesale move (carryforward, v1.1.0) | P1 / DF | Phase 7 (2026-05-31; T020) | 12 sub-trees (config, llm->modules/coding/llm, observability, orchestration, guardrails, mcp, commands, agents, chat, evaluation, skills, runtime) moved via `git mv` + a path-aware codemod ([scripts/dev/move-coding-subtrees.mjs](../../../scripts/dev/move-coding-subtrees.mjs), 178 importers rewritten); config/scripts/rules repointed; architecture boundary intact (0 errors); full suite 3888 passed |
| `1.1.P1.A` TypeScript project-references wiring (carryforward, v1.1.0) | P1 / DF | Phase 7 (2026-05-31; T021) | [tsconfig.base.json](../../../tsconfig.base.json) + composite [core/tsconfig.json](../../../core/tsconfig.json) + root [tsconfig.json](../../../tsconfig.json) referencing core; `npm run build` = `tsc -b` builds core then root in dependency order with no double-emit (incremental rebuild up-to-date). desktop excluded by design (esbuild/Vite bundles core) |
| `3.3.P2.G` Tree-sitter scanner swap (carryforward, v1.2.0) | P2 / DF | Phase 7 (2026-05-31; T022) | web-tree-sitter (WASM) extractor in [core/codegraph/scanner/TreeSitterScanner.ts](../../../core/codegraph/scanner/TreeSitterScanner.ts) behind the unchanged `extractSymbols` boundary (regex fallback retained as `extractSymbolsRegex`); 4 grammars + 3 edge cases (multi-line decls, property-method assignments, computed names) proven by [tests/unit/core/codegraph/TreeSitterScanner.test.ts](../../../tests/unit/core/codegraph/TreeSitterScanner.test.ts); packaged-app .wasm bundling tracked as `T022.P3.A` |
| `4.1.P2.J` AstChunker AST-awareness (carryforward, v1.2.0; cascaded) | P2 / DF | Phase 7 (2026-05-31; T022) | `AstChunker` consumes `extractSymbols`, so it inherits the Tree-sitter parse with no chunker-layer change (the abstraction boundary the v1.2.0 gap anticipated) |
| `6.1.P3.V` WatchedRepoScanner symbol extraction (carryforward, v1.2.0; cascaded) | P3 / DF | Phase 7 (2026-05-31; T022) | `WatchedRepoScanner` consumes `extractSymbols`, so it inherits the Tree-sitter parse with no watcher-layer change |
| `4.2.P3.K` PrunedDenseIndex multi-layer HNSW (carryforward, v1.2.0) | P3 / DF | Phase 7 (2026-05-31; T023) | `compact()` builds the kNN graph via hnswlib-node HNSW (O(N log N)) above `HNSW_MIN_NODES`, all-pairs fallback below / when the native module is absent; topology-only on-disk format unchanged (VERSION 1); HNSW path + recall + save/load proven by [tests/unit/core/memory/PrunedDenseIndex.hnsw.test.ts](../../../tests/unit/core/memory/PrunedDenseIndex.hnsw.test.ts) |
| `7.x.P1.D` protobufjs CVE chain (carryforward, v1.2.0; the lone P1) | P1 / BG | Phase 8 (2026-06-02; T025) | Migrated [core/memory/LocalEmbedder.ts](../../../core/memory/LocalEmbedder.ts) off the abandoned `@xenova/transformers@2.17.2` to `@huggingface/transformers@4.x` (`onnxruntime-web@1.26` drops `onnx-proto`, pulls patched `protobufjs@^7.2.4`); `quantized: true`->`dtype: "q8"`. `npm run check:audit-prod` now 0 critical / 0 high / 1 moderate (`brace-expansion`, allowlisted); was 1 critical + 3 high. Allowlist in [scripts/check-prod-audit.mjs](../../../scripts/check-prod-audit.mjs) reduced to `brace-expansion`. LocalEmbedder 17 tests green |
| `5.3.P2.R` / `5.3.P3.S` / `6.1.P3.W` permissions.deny wiring + codegraph ignore-parser unification (carryforward, v1.2.0) | P2/P3 / WN+DF | Phase 8 (2026-06-02; T026) | `evaluateDeny` wired as a central pre-dispatch gate in [src/tools/ToolRegistry.ts](../../../src/tools/ToolRegistry.ts) (`setPermissionsDeny` + `DENY_SUBJECT_PARAM`), loaded from `.nexus/permissions.deny` at [src/panels/ChatPanelBootstrap.ts](../../../src/panels/ChatPanelBootstrap.ts); [core/codegraph/scanner/RepoScanner.ts](../../../core/codegraph/scanner/RepoScanner.ts) ignore parsing collapsed onto `core/storage/NexusIgnore` (shared with FileWatcher). 4 new ToolRegistry tests; `no-orphans` warning on `PermissionsDeny.ts` cleared (11->10) |
| `5.4.P3.T` / `5.2.P3.Q` / `5.1.P2.P` / `5.1.P2.O` lifecycle + explore-policy wiring (carryforward, v1.2.0) | P2/P3 / DF | Phase 8 (2026-06-02; T027) | Session `HookBus` + `attachSessionReflectionHook` wired at bootstrap; [src/tools/AgentLoop.ts](../../../src/tools/AgentLoop.ts) emits `lifecycle.session.reflection` at session end and `reevaluateSkillsForPath` on focus change (SkillLoader parses `pathScope`); `mcpToolLooksReadOnly` in [core/coding/SubAgentPolicy.ts](../../../core/coding/SubAgentPolicy.ts) admits read-only MCP tools under explore intent (dispatcher already in `modules/coding/agents` post-T020). New AgentLoop + SubAgentPolicy + SkillLoader tests |
| `6.2.P2.X` / `6.2.P3.Y` / `6.3.P2.Z` LSP install prompts + desktop DOMPurify (carryforward, v1.2.0) | P2/P3 / DF | Phase 8 (2026-06-02; T028) | `lspInstallInstructions` surfaced in the missing-server error in [core/coding/lsp/LspClient.ts](../../../core/coding/lsp/LspClient.ts); definition/references subset documented closed (won't-expand); [desktop/src/components/InteractiveArtifact.tsx](../../../desktop/src/components/InteractiveArtifact.tsx) switched to `isomorphic-dompurify`. LSP (6) + desktop InteractiveArtifact (7) tests green |
| `2.4.P2.E` / `2.4.P3.F` / `4.3.P3.M` / `4.x.P3.N` / `3.4.P3.H` / `3.5.P3.I` / `6.1.P3.U` hygiene deferrals (carryforward, v1.2.0) | P2/P3 / DF+WN | Phase 8 (2026-06-02; T029) | Deleted dead `preToolHook` (2.4.P2.E); implemented the codegraph-trim prompt notice (3.5.P3.I: `trimmedCodegraph` -> `PromptContext.toolCapNotice`); five explicit closures-with-rationale documented at each code site (2.4.P3.F keep-per-result, 4.3.P3.M keep-.mjs, 4.x.P3.N canonical-entry, 3.4.P3.H in-process, 6.1.P3.U keep-fs.watch). New ToolActivationRules trim tests; -6 dead tests |
| `4.4.P2.L` / `7.1.P2.A` / `T012.P2.C` / `T013.P3.D` benchmarks + audit deferrals (carryforward, v1.2.0 + v1.3.0) | P2/P3 / MT+DF | Phase 8 (2026-06-02; T030) | 100k memory-tier sweep published at [benchmarks/memory-storage-size-2026-06-02.md](benchmarks/memory-storage-size-2026-06-02.md) (`lastBuildMethod: hnsw`, recall 96.5%, unblocked by T023); `scanUsage` widened to multi-root in [core/skills/SkillUsageScanner.ts](../../../core/skills/SkillUsageScanner.ts) (+tests); 7.1.P2.A (deterministic synthesis canonical) + T013.P3.D (MinHash not a cost driver at ~230 skills) closed with rationale |
| `1.1.P2.A` Nexus-Hub catalog index rebuild (carryforward, v1.2.0) | P2 / WN | Phase 9 (2026-06-09; T033) | The action is Hub-side; Nexus-Hub's own v3.x release cycle ran `make build-catalog` and regenerated `data/skills.json` + `SKILL_INDEX.md`. Both target skills (`hallmark-design`, `html-output-conventions`) are present in the current Hub catalog -- verified read-only against the local clone via `buildManifest` (251 skills enumerated). No Nexus-AI change required. See [development/nexus-hub-integration-delta.md](development/nexus-hub-integration-delta.md) Section 6 |
| `1.1.P3.B` New Hub skills require an upstream release to flow through `nexus skills sync` (carryforward, v1.2.0) | P3 / DF | Phase 9 (2026-06-09; T033) | The documented blocker was the stale upstream coordinate. `DEFAULT_UPSTREAM` fixed `bendourthe/DevAI-Hub` -> `bendourthe/Nexus-Hub` in [../../../../core/skills/DevAIHubSyncer.ts](../../../core/skills/DevAIHubSyncer.ts) (+ 2 regression tests in [../../../../tests/unit/core/skills/DevAIHubSyncer.test.ts](../../../tests/unit/core/skills/DevAIHubSyncer.test.ts); the local `devai-hub` on-disk namespace intentionally preserved). Hub release tags (v3.0.0+) now contain the skills; offline `buildManifest` over the local Hub catalog -- the gap's own accepted faithful verification -- enumerates both targets. Live `sync --apply` against the published release is a now-unblocked operator step (requires network) |

---

## 3. Summary

| Bucket | Count |
|---|---|
| Open items introduced by v1.4.0 phases (P3/DF deferrals) | 5 (`T016.P3.A` A8 hook daemon wiring; `T018.P3.A` A10 runtime wiring; `T018.P3.B` A10 read-tool rooting / team orchestration; `T022.P3.A` Tree-sitter grammar .wasm bundling; `T034.P2.A` dev-only npm advisories, P2/WN). Phase 9 introduced 1 (`T034.P2.A`). |
| Net-new Nexus-Hub v3.x integration items (routed to v1.5.0) | 7 (`HUB.P3.CMD`, `HUB.P3.AGENT`, `HUB.P3.HOOK`, `HUB.P3.RULES`, `HUB.P3.MCPCFG`, `HUB.P3.EXT.*`, `HUB.P3.NS` -- all P3/DF). Not part of the original 36-item carryforward; created by the Hub's parallel v3.x expansion; non-blocking. |
| Carryforward open items remaining (Hub-owned, re-justified) | 2 (`T017.P3.E`, `T002.P2.A` -- live in the Nexus-Hub repo; not closeable from Nexus-AI). `1.1.P2.A` + `1.1.P3.B` resolved in Phase 9. |
| Resolved this cycle | 42 -- 12 adoptions (A1-A12) + 6 carryforward in Phase 7 + 22 carryforward in Phase 8 + 2 carryforward in Phase 9 (`1.1.P2.A`, `1.1.P3.B`) |
| Adoption items landed (of 12) | 12 (A1, A2, A3, A4, A5, A6, A7, A8, A9, A10, A11, A12) |

**Phase 7 status**: complete. The architectural carryforward landed: the `src/` -> `modules/coding/` move (`1.4.P1.B`), the `tsc -b` project-references build (`1.1.P1.A`), the Tree-sitter (WASM) scanner (`3.3.P2.G`, cascading to `4.1.P2.J` + `6.1.P3.V`), and the multi-layer HNSW PrunedDenseIndex build (`4.2.P3.K`) -- 6 carryforward gaps closed.

**Phase 8 status**: complete. The wiring / deferral / P1-CVE carryforward landed across T025-T031: the lone P1 protobufjs chain resolved by the `@huggingface/transformers` embedder migration (`7.x.P1.D`); `permissions.deny` + codegraph ignore-parser unification (`5.3.P2.R`, `5.3.P3.S`, `6.1.P3.W`); session-reflection + path-scope + explore-MCP wiring (`5.4.P3.T`, `5.2.P3.Q`, `5.1.P2.P`, `5.1.P2.O`); LSP install prompts + desktop DOMPurify (`6.2.P2.X`, `6.2.P3.Y`, `6.3.P2.Z`); seven hygiene deferrals (`2.4.P2.E`, `2.4.P3.F`, `4.3.P3.M`, `4.x.P3.N`, `3.4.P3.H`, `3.5.P3.I`, `6.1.P3.U`); and the 100k benchmark + multi-root usage scan + audit closures (`4.4.P2.L`, `7.1.P2.A`, `T012.P2.C`, `T013.P3.D`) -- 22 carryforward gaps closed, 0 new gaps. Full suite 3901 passed, 0 failed; desktop 418 passed; `check:audit-prod` 0 high/critical; `tsc -b` clean; check-architecture 0 errors / 10 warnings; lint + check:tampering + security:check clean. Only the 4 Nexus-Hub-dependent carryforward items remained going into Phase 9.

**Phase 9 status**: complete (FINAL). Nexus-Hub sync + whole-plan acceptance gate landed across T032-T035: the integration delta enumerates every Hub functionality with an integrated/not-integrated verdict ([development/nexus-hub-integration-delta.md](development/nexus-hub-integration-delta.md)); the `DEFAULT_UPSTREAM` coordinate fix closes `1.1.P3.B` and confirms `1.1.P2.A` (both skills-catalog gaps resolved); `T017.P3.E` + `T002.P2.A` are re-justified as Hub-owned (open against the Nexus-Hub repo). The whole-plan definition of pass holds with fresh evidence: (1) all 12 adoptions A1-A12 landed; (2) all 36 ingested carryforward gaps resolved (34) or explicitly re-justified (2 Hub-owned); (3) Nexus-Hub latest updates accounted for in the delta; (4) full test matrix green -- `tsc -b` clean, lint clean, check-architecture 0 errors / 10 warnings, `check src/` 0 findings, check:tampering 0 findings, security:check in sync, check:audit-prod 0 blocking (the newly-surfaced `hono` prod advisory fixed in-phase via an `overrides` pin to 4.12.25), full suite 339 files passed / 2 skipped (live-backend) / 0 failed at 87.19% lines. One new P2/WN (`T034.P2.A`, dev-only advisories) and seven P3/DF Hub-v3.x integration items (`HUB.P3.*`) recorded for v1.5.0.
