# Plan - Low-VRAM Agentic Catalog Adoption (Liquid LFM2.5)

**Project**: Nexus AI Studio
**Version**: v1.19.0 (cut 2026-08-19)
**Slug**: adoption-liquid-lfm-agentic
**Filename**: v1.19.0-adoption-liquid-lfm-agentic.md
**Plan Type**: Feature / Enhancement (adoption - catalog curation cycle)
**Created**: 2026-08-05
**Generated**: 2026-08-05
**Sources**: [../comparisons/v1.19.0-comparison-liquid-lfm-agentic.md](../comparisons/v1.19.0-comparison-liquid-lfm-agentic.md)
**Seeded from**: [../comparisons/v1.19.0-comparison-liquid-lfm-agentic.md](../comparisons/v1.19.0-comparison-liquid-lfm-agentic.md)
**Goal**: Give every hardware tier, including CPU-only machines, a working agentic (tool-calling) model by curating LFM2.5-2.6B into the model catalog as the low-VRAM Agentic-tier entry, labeling its LFM Open License v1.0 use restriction accurately and visibly, tuning a HarnessSelector profile to its tool-call output format with A/B golden-task validation, and gating the optional 8B-A1B sibling behind a benchmark bake-off - all as local catalog data and local logic, with zero new runtime dependencies and zero hosted-inference surface.

## Overview

This milestone operationalizes the adoption plan from the Liquid AI LFM model library comparison (Section 7) in its recommended sequence (Section 8). The comparison found that Nexus's Agentic tier bottoms out at ~7 GB VRAM (the qwen2.5-coder 7B class entries in `core/registry/catalog.json`), so machines below that get a chat model but no dedicated tool-calling-capable agentic option. LFM2.5-2.6B - a 2.6B open-weight agentic model that plans, calls tools, and runs multi-step tasks in under 2.5 GB of memory (Q4_K_M GGUF is 1.67 GB, CPU-capable) - fills that gap exactly.

Because the source is a model library rather than a codebase, adoption is catalog curation, not program-logic reverse-engineering; per Section 6 every candidate classifies as re-full or re-partial (none is skill-native, none is vendor-intrinsic). Four adoption items land, ordered reverse-engineer-first by P-tier and dependency: **A1 (P0)** the LFM2.5-2.6B catalog entry plus low-tier `recommended.json` placement; **A4 (P0, depends on A1)** the LFM Open License v1.0 use-restriction label, reusing the v1.14 `licenseUrl` surface but presented as a use restriction (free commercial use capped at USD 10M annual revenue), not a download gate (`requiresLicense: false` - the weights are ungated); **A3 (P1, depends on A1)** an LFM-aware harness profile in `modules/coding/orchestration/HarnessSelector.ts`, A/B-validated via `HarnessSelectorAb.ts` golden tasks, including empirical resolution of the conflicting 32K-vs-128K context-length claim; **A2 (P2, depends on A1 + A3)** a benchmark bake-off of LFM2.5-8B-A1B against the incumbent 12-14 GB agentic entries, with a catalog entry only on a win. A P3 watchlist item (VL variants, PII-extract Nano) is recorded as notes only - no build.

Delivery stays entirely within existing mechanisms: the curated catalog (`core/registry/catalog.json` + `core/registry/recommended.json`), the SHA-256-pinned HF weights flow (`scripts/installer/build/pin-hf-weights.py`), the installer's catalog invariants and card copy (`scripts/installer/`), and the per-model harness selection layer (v1.12). Nothing is imported: no hosted Liquid inference (N1), no weight redistribution (N2), no vendor SDK (N3) - see "Out of scope / rejected". Vendor-reported benchmark numbers (ToolSandbox 77.83, BFCLv4 56.88, 220 tok/s) must not appear in `whyRecommended` copy until locally reproduced or independently corroborated (Section 9 general considerations). Success looks like: a sub-4-GB, CPU-capable agentic model is installable on every tier with an accurate, visible license-restriction label; the coding orchestrator selects a correct harness profile for it; and the 8B-A1B question is closed either way (adopted on a win or documented as evaluated-and-declined).

This is deliberately a SMALL, shippable cycle (catalog + license-label + harness-profile work), placed as a standalone v1.19.0 because v1.15 (in flight: installer + studio-chat), v1.16 (planned: serving gateway + OCR), v1.17 (motion identity), and v1.18 (comparison-only) are committed to other themes.

## Plan Family

This is subplan 0 of the v1.19 multi-subplan milestone. v2.0.0 is the convergence release: it ships only when every plan below is complete.

- [../../v1.18/plans/v1.18.0-adoption-agent-harness-and-governance.md](../../v1.18/plans/v1.18.0-adoption-agent-harness-and-governance.md) -- harness activation, catalog/registry governance, ask inbox + scheduler, ACP, OS exec sandbox.
- [v1.19.0-adoption-liquid-lfm-agentic.md](v1.19.0-adoption-liquid-lfm-agentic.md) (this plan) -- LFM2.5-2.6B low-VRAM agentic catalog entry, license label, harness profile, 8B-A1B bake-off.
- [v1.19.1-adoption-agent-loop-and-guardrail-hardening.md](v1.19.1-adoption-agent-loop-and-guardrail-hardening.md) -- skill-native wave + agent-loop and guardrail hardening.
- [v1.19.2-adoption-catalog-and-model-expansion.md](v1.19.2-adoption-catalog-and-model-expansion.md) -- modality/audio catalog schema, quantized-weights puller, Hermes family, patient-tier calibration.
- [../../../v2/v2.0/plans/v2.0.0-adoption-governed-autonomy-multimodal.md](../../../v2/v2.0/plans/v2.0.0-adoption-governed-autonomy-multimodal.md) -- multimodal chat + voice loop, browser tool surface, Video Lab continuation + avatar tier, stretch, refactor, and the v2.0.0 convergence/release gate.

## Constitution Check

*GATE: Must pass before Phase 1 work. Re-check after Phase 1.*

No constitution file found at `docs/archive/v1/v1.19/constitution.md` (nor elsewhere under `docs/`) - skipping check. Recommend running `/constitution` to establish project principles. This is informational, not blocking.

Project-level guardrails that nonetheless apply to this plan (from `AGENTS.md` / `CLAUDE.md`): local-first / zero-outbound (weights pull from the official Hugging Face repo or the Ollama registry at explicit install time only; inference is fully local; no telemetry, no new runtime dependency); the MCP Registry Policy reverse-engineer-first decision tree and its hard no on generation-as-a-service (grounds for rejecting N1-N3); license fidelity (the LFM Open License v1.0 use restriction must be labeled visibly and accurately, never silently dropped - it binds the user's commercial use, not the download); benchmark honesty (vendor-reported numbers are not asserted as Nexus copy until verified); every changed line traces to an adoption item (A1-A4); per-phase testing and CI updates; ASCII-only commit messages.

## Phases at a Glance

| Phase | Title | Outcome | Rec. model / effort |
|-------|-------|---------|---------------------|
| 1 | Catalog entry + license label (A1 + A4, P0) | LFM2.5-2.6B curated into the catalog with pinned weights, low-tier agentic defaults, and a visible LFM Open License use-restriction label; shippable on its own | Mid tier, medium - claude-sonnet-5 (medium): data curation over existing mechanisms; care is in pin/label accuracy, not invention |
| 2 | LFM harness profile (A3, P1) | LFM-aware profile in HarnessSelector, A/B-validated on golden tasks; context-length claim resolved empirically and the entry corrected | Mid-strong tier, high - claude-sonnet-5 (high): local selector logic against a new tool-call format, with empirical validation |
| 3 | 8B-A1B bake-off (A2, P2) + watchlist | LFM2.5-8B-A1B evaluated against incumbent 12-14 GB agentic entries; entry added only on a win, otherwise closed as evaluated-and-declined; P3 watchlist recorded | Mid tier, medium - claude-sonnet-5 (medium): structured evaluation with a pre-committed decision rule |
| 4 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Clean layout, carried gaps reconciled, CI green and optimized | Strong tier, high - claude-opus-4-8 (high): repo-wide refactor + reference repair |

---

## Phase 1: Catalog Entry + License Label (A1 + A4, P0)

**Goal**: Land LFM2.5-2.6B as the low-VRAM Agentic-tier catalog entry - correct metadata, pinned weights, low-tier default placement - together with an accurate, visible LFM Open License v1.0 use-restriction label, so the phase is shippable on its own per the comparison's Section 8 sequencing.
**Prerequisites**: None. A1 is the root dependency of every other item.
**Stability Gate**: `core/registry/catalog.json` carries a valid LFM2.5-2.6B entry (`task: "agentic"`, `agentic: true`, Q4_K_M sizing, verified SHA-256 pins or Ollama digest delegation, `license: "LFM Open License v1.0"` + `licenseUrl`, `requiresLicense: false`); `core/registry/recommended.json` places it as a low-tier agentic default; the installer's catalog invariants pass; the card copy presents the USD 10M revenue cap as a use restriction (not a download gate) and asserts no unverified vendor benchmark; the entry surfaces in the desktop model pickers.
**Recommended model**: Mid tier, medium effort - claude-sonnet-5 (medium). Rationale: catalog data + copy over already-implemented mechanisms (v1.8.0 weights manifests, v1.9.0 agentic metadata, v1.14 license surface); the risk is factual accuracy, not code complexity. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 1.1 - Curate the LFM2.5-2.6B catalog entry

**Objective**: Add the A1 entry to the catalog with verified metadata and pinned weights.

**Prompt**:
> Add an LFM2.5-2.6B entry to `core/registry/catalog.json` following the existing entry schema (see the gemma4 entries and the `_meta` conventions): `type: "llm"`, `task: "agentic"`, `agentic: true`, `origin` (Liquid AI, USA), `releaseDate` (verify from the release blog), `sizeGB: 1.67` (Q4_K_M from the official `LiquidAI/LFM2.5-2.6B-GGUF` repo), a `vramGB` value in the 3 GB class (CPU-capable - mirror how gemma4:e2b expresses that in its description), `license: "LFM Open License v1.0"`, and `licenseUrl` pointing at the official LFM License page. Source the weights preferentially via an HF `weights` manifest with real SHA-256 pins generated by `scripts/installer/build/pin-hf-weights.py` (no all-zero placeholder digests may ship); use an `ollama` source instead only if the official library carries the model at curation time. Write `description`/`strengths`/`whyRecommended`/`differentiators` copy that does NOT assert vendor benchmarks (ToolSandbox/BFCLv4/tok-per-s) as fact - phrase capability claims from what is locally verifiable (size, CPU capability, tool-calling design intent). Record the context length conservatively (32K, the docs-library value) with a note that Phase 2 verifies the 128K claim empirically. Acceptance: the entry validates against `core/registry/manifest.schema.json` conventions and the installer invariants (`scripts/installer/tests/test_catalog_invariants.py`), pins are real, and no unverified benchmark appears in card copy.

---

#### 1.2 - Low-tier agentic default placement

**Objective**: Make the new entry the agentic option on tiers that previously had no dedicated agentic-capable pick.

**Prompt**:
> Update `core/registry/recommended.json` to place the LFM2.5-2.6B entry in the `agentic` lists of the low tiers (at minimum `cpu` and `8`; judge intermediate tiers per the existing preference comment in `_meta` - the Gemma-preferred, coder-fallback ordering). Preserve the existing single-pick semantics and fit-gating behavior: the entry must be fit-gated by the wizard like any other id, and higher tiers where the 7B/14B coding specialists already win must not regress. Acceptance: low tiers now include a sub-4-GB tool-calling agentic option, higher-tier defaults are unchanged, and the installer's tier-resolution tests still pass.

---

#### 1.3 - LFM Open License use-restriction label (A4)

**Objective**: Present the license restriction visibly and accurately as a use restriction, not a download gate.

**Prompt**:
> Extend the license presentation for the new entry so the LFM Open License v1.0 restriction is visible wherever the model card renders. The weights are ungated, so `requiresLicense` stays `false` and the v1.14 guided token/acceptance flow (`scripts/installer/src/nexus_installer/engine/gated_auth.py` / `widgets/gated_auth_dialog.py`) must NOT trigger; instead, surface a use-restriction note on the card copy (installer picker card via `scripts/installer/src/nexus_installer/widgets/model_checkbox.py` and the catalog copy fields consumed by the desktop Settings UI): Apache-2.0-based, royalty-free, fine-tunes may stay proprietary, free commercial use limited to entities under USD 10M annual revenue, commercial license required above that, qualified non-profits exempt, with `licenseUrl` linking the first-party license page. If a small schema addition is the cleanest way to carry a use-restriction string (e.g. a `licenseNote` field), add it additively, document it in `catalog.json` `_meta`, and validate it in `catalog_invariants.py`; otherwise reuse existing copy fields. The label must be accurate and never silently dropped by any consumer (Section 5.2: the cap binds the user's commercial use). Acceptance: the restriction is visible on the installer card and in catalog data, the gated-auth flow does not fire for this entry, and the invariants enforce the label's presence for the LFM entry.

---

#### 1.4 - Testing and Stabilization

**Objective**: Cover the entry, placement, and label; wire CI.

**Prompt**:
> Add tests: extend `scripts/installer/tests/test_catalog_invariants.py` to cover the LFM entry (schema fields present, real pins, `requiresLicense: false` + `licenseUrl` + use-restriction label present, no vendor-benchmark strings in `whyRecommended`); extend the tier-defaults tests (`scripts/installer/tests/test_model_router.py` or the tier-resolution suite) for the new low-tier agentic placement; add or extend a desktop-side test that the entry surfaces in the model picker (`desktop/src/shared/chat/ModelSelector.tsx`) with its agentic task. Run the installer test suite (pytest under `scripts/installer/tests/`), `npm test` (vitest, `configs/vitest.config.ts`), `npm run test:shell`, and `npm run lint`; iterate to green. Update CI so catalog/registry and installer changes are covered (path filters, concurrency cancel-in-progress, dependency caching). Do not proceed to Phase 2 until stable. After tests pass, run `/generate-session-history` for Phase 1.

---

### Phase 1 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (catalog invariants + tier defaults + picker surface)
- [x] No known regressions (existing tiers and defaults unchanged where not targeted)
- [x] SHA-256 pins are real (no all-zero placeholders); license label visible and accurate; gated-auth flow does not fire
- [x] No vendor-reported benchmark asserted in card copy
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 2

---

## Phase 2: LFM Harness Profile (A3, P1)

**Goal**: The coding orchestrator selects a correct, A/B-validated harness profile for LFM models' tool-call output format, and the context-length discrepancy (32K docs vs 128K release blog) is resolved empirically with the catalog entry corrected to the verified value.
**Prerequisites**: Phase 1 (the A1 entry exists to select against and to correct).
**Stability Gate**: `modules/coding/orchestration/HarnessSelector.ts` carries an LFM-aware profile keyed to the new entry's family/id; `HarnessSelectorAb.ts` golden tasks demonstrate the LFM profile beats or matches the default profile on tool-call parse success for LFM output; the context length recorded in `catalog.json` is the empirically verified value with the verification noted; unit tests cover profile selection and fallback.
**Recommended model**: Mid-strong tier, high effort - claude-sonnet-5 (high). Rationale: pure local selector logic (re-full), but against a new model's emitted tool-call format with empirical A/B validation; moderate complexity, no security surface. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 2.1 - Characterize the LFM tool-call output format

**Objective**: Establish, from local runs, what the model actually emits before writing the profile.

**Prompt**:
> With the Phase 1 entry installed locally (Ollama/llama.cpp GGUF runtime - no hosted endpoint may be used, per N1), run a small battery of representative tool-calling prompts against LFM2.5-2.6B and capture the raw emitted tool-call format (delimiters, JSON shape, planning/multi-step patterns, failure modes). Document the findings as a short characterization note (format examples, quirks, parse hazards) checked in alongside the profile (e.g. a doc comment or a fixtures file under `tests/unit/orchestration/`). Also measure usable context empirically: probe beyond 32K to test the release blog's 128K claim and record the verified figure. Acceptance: a written format characterization with captured fixtures exists, and the verified context length is recorded.

---

#### 2.2 - Implement the LFM profile in HarnessSelector

**Objective**: Reverse-engineer the format fit into a local selector profile.

**Prompt**:
> Add an LFM-aware profile to `modules/coding/orchestration/HarnessSelector.ts`, following the existing per-model profile pattern from the v1.12 adoption cycle: match on the LFM family/id from the Phase 1 catalog entry, and configure the harness parameters the characterization (2.1) showed the format needs (tool-call parsing expectations, prompt scaffolding, stop/format handling). Preserve the existing default-profile fallback for unknown models; the LFM profile must be additive. Validate via `modules/coding/orchestration/HarnessSelectorAb.ts`: run the golden-task A/B comparing the LFM profile against the default profile on the captured fixtures and live local runs, and keep the profile only where it wins or ties. Correct the `catalog.json` context length to the value verified in 2.1 if it differs from the conservative Phase 1 value. Acceptance: the selector returns the LFM profile for the LFM entry and the default for everything else; the A/B result is recorded; the catalog context length matches the verified figure.

---

#### 2.3 - Testing and Stabilization

**Objective**: Lock profile selection and format handling in tests; wire CI.

**Prompt**:
> Add tests: extend `tests/unit/orchestration/HarnessSelector.test.ts` (LFM entry id/family yields the LFM profile; non-LFM ids keep their existing profiles; unknown ids fall back to default) and `tests/unit/orchestration/HarnessSelectorAb.test.ts` (the A/B path exercises the LFM profile against the captured fixtures); add fixture-driven parse tests for the characterized tool-call format, including at least one malformed-output case. Run `npm test` + `npm run lint`; iterate to green. Update CI to cover `modules/coding/orchestration` changes. Do not proceed to Phase 3 until stable. After tests pass, run `/generate-session-history` for Phase 2.

---

### Phase 2 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (profile selection + fallback + fixture parsing)
- [x] No known regressions (existing per-model profiles unchanged)
- [x] A/B golden-task result recorded; profile retained only on win/tie
- [x] Context-length claim resolved empirically and the catalog entry corrected
- [x] No hosted inference used anywhere in validation (N1)
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 3

---

## Phase 3: 8B-A1B Bake-off (A2, P2) + Watchlist

**Goal**: Close the LFM2.5-8B-A1B question with a pre-committed decision rule - a benchmark bake-off against the incumbent 12-14 GB agentic entries, catalog entry only on a win - and record the P3 watchlist items as notes so nothing dangles.
**Prerequisites**: Phases 1-2 (the family entry pattern and the LFM harness profile both exist; the bake-off depends on A1 + A3 per Section 7).
**Stability Gate**: A documented bake-off compares LFM2.5-8B-A1B (local GGUF) against the incumbent 12-14 GB agentic entries (qwen2.5-coder:14b class, deepseek-coder-v2:16b) on agentic golden tasks per GB of VRAM; the adopt/decline decision is recorded with the evidence; on a win, a catalog entry + tier placement + license label land under the Phase 1 conventions; on a loss, the item closes as evaluated-and-declined; the P3 watchlist (VL variants, PII-extract Nano) is recorded as a note only.
**Recommended model**: Mid tier, medium effort - claude-sonnet-5 (medium). Rationale: structured evaluation over existing harness/A-B machinery with a binary pre-committed outcome; low novelty. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 3.1 - Run the bake-off

**Objective**: Evaluate 8B-A1B against the incumbents with a decision rule fixed in advance.

**Prompt**:
> Fix the decision rule first: LFM2.5-8B-A1B earns a catalog entry only if it beats the incumbent 12-14 GB agentic entries (`qwen2.5-coder:14b` class and `deepseek-coder-v2:16b` in `core/registry/catalog.json`) on agentic golden-task quality per GB of VRAM. Then run the bake-off locally (official GGUF, Ollama/llama.cpp; reuse the `HarnessSelectorAb.ts` golden-task machinery and the Phase 2 LFM profile - extend the profile match to the 8B-A1B id if its emitted format warrants it). Verify its claimed 128K context empirically as part of the run. Record method, task set, raw results, and the verdict in a bake-off note under `docs/archive/v1/v1.19/` (e.g. `docs/archive/v1/v1.19/development/` alongside the phase session histories). Acceptance: a written, reproducible bake-off record exists with an explicit adopt/decline verdict against the pre-committed rule.

---

#### 3.2 - Act on the verdict + record the watchlist

**Objective**: Either land the entry under Phase 1 conventions or close the item, and pin the P3 watchlist.

**Prompt**:
> If the 3.1 verdict is ADOPT: add the LFM2.5-8B-A1B entry to `core/registry/catalog.json` and place it in `core/registry/recommended.json` following every Phase 1 convention (verified pins, `task: "agentic"`, verified context length, LFM Open License v1.0 use-restriction label per sub-task 1.3, no unverified vendor benchmarks in copy), and extend the invariants tests accordingly. If the verdict is DECLINE: make no catalog change and record the item as evaluated-and-declined with a pointer to the bake-off note (this feeds the Phase 4 known-gaps file). In both cases, record the P3 watchlist as a note (in the bake-off note or the known-gaps input): LFM2.5-VL vision-language variants (defer; existing multimodal coverage) and the PII-extract Nano (possible future local aid to `redactSecrets`-style scrubbing; no build). Acceptance: the catalog reflects the verdict exactly, and the watchlist is written down with rationale.

---

#### 3.3 - Testing and Stabilization

**Objective**: Cover whichever outcome landed; wire CI.

**Prompt**:
> Add tests matching the outcome: on ADOPT, extend the catalog-invariants, tier-defaults, and harness-selector tests for the new entry exactly as Phase 1.4/2.3 did for the 2.6B; on DECLINE, add a guard only if code changed (e.g. an extended profile match), otherwise confirm the suites still pass unchanged. Run the installer pytest suite, `npm test`, `npm run test:shell`, and `npm run lint`; iterate to green. Update CI for any touched paths. Do not proceed to Phase 4 until stable. After tests pass, run `/generate-session-history` for Phase 3.

---

### Phase 3 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing for the landed outcome
- [x] Bake-off record written and reproducible; verdict follows the pre-committed rule
- [x] Catalog reflects the verdict exactly (entry on win, untouched on decline)
- [x] P3 watchlist (VL variants, PII-extract Nano) recorded with rationale
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 4

---

## Out of Scope / Rejected

Carried verbatim in substance from the comparison's Section 9; each rejection is grounded in the MCP Registry Policy (`AGENTS.md`) and must NOT be revisited inside this cycle.

- **N1 - Hosted Liquid inference (LEAP platform, hosted API endpoints, playground).** Any routing of Nexus inference through Liquid-hosted endpoints is generation-as-a-service - an MCP Registry Policy hard no - and violates the local-first, zero-outbound posture. Local open weights are the only acceptable consumption path; even Phase 2/3 validation runs must be local-only.
- **N2 - Bundling or redistributing LFM weights inside the Nexus installer.** Redistribution would make Nexus a licensee-distributor under the LFM Open License v1.0 and inherit its commercial-use cap into the product. Rejected: keep the existing pull-at-install-from-official-repo model with SHA-256 pins (MCP Registry Policy bucket 1 local-only posture; v1.14 installer discipline).
- **N3 - Vendor SDK integration (Liquid edge/LEAP SDKs) for on-device execution.** The local internal equivalent already exists (Ollama / llama.cpp GGUF runtime); a vendor SDK duplicates it and adds supply-chain surface for zero capability gain (MCP Registry Policy bucket 3 preference: the internal equivalent wins).

Also out of scope for v1.19.0 (not-applicable, per Section 4): the small chat/instruct variants (1.2B/350M/230M - the chat ladder already reaches CPU-only hardware via gemma4:e2b), the vision-language and audio variants, and the Liquid Nanos beyond the recorded watchlist note.

---

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| (none - no constitution file; no violations) | | |

---

## Phase 4: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, and its CI/CD complete and optimized.
**Prerequisites**: All prior phases.
**Stability Gate**: The layout is clean (no deprecated/obsolete files, empty dirs, redundant files/dirs, or overcomplicated structure left un-triaged); the version's known gaps are reconciled; CI/CD covers every change and is optimized; project validation/tests pass.
**Recommended model**: Strong tier, high effort - claude-opus-4-8 (high). Repo-wide refactor + reference repair is high-risk - default to the strong reasoning tier.

### Sub-tasks

#### 4.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure introduced or exposed across Phases 1-3 (e.g. transitional characterization fixtures that belong under the orchestration test tree, any duplicated license-copy plumbing between installer and desktop consumers, superseded catalog notes), then refactor toward a clean, intuitive layout via `[[project-refactor]]` and `[[docs-layout-refactor]]` (propose-then-apply, with confirmation; repair every reference for anything that moves). Confirm the `docs/archive/v1/v1.19/` tree is complete (this plan, the seeding comparison, the bake-off note, phase session-histories, known-gaps).

#### 4.2 - Known-gaps reconciliation
**Objective**: Reconcile the version's open gaps.
**Prompt**:
> Reconcile this version's known gaps via `[[known-gaps-tracker]]`: create `docs/archive/v1/v1.19/known-gaps.md`, resolve/defer/transfer each still-open item carried from the latest existing known-gaps file (`docs/archive/v1/v1.15/known-gaps.md` at planning time - v1.16 through v1.18 have no known-gaps files yet; pick up any that exist by implementation time), and record any newly discovered v1.19 gaps: the A2 evaluated-and-declined record if the bake-off lost; the P3 watchlist items (LFM2.5-VL variants, PII-extract Nano); any unresolved context-length caveat; and the standing task of independently corroborating vendor benchmarks before card copy may cite them. Finalize the per-minor known-gaps file for v1.19.

#### 4.3 - CI/CD create/update/optimize
**Objective**: CI/CD covers all changes and is optimized.
**Prompt**:
> Create or update the CI/CD pipeline so it covers every change in this plan (catalog/registry data and its invariants, the installer test suite, the harness-selector profile and its unit tests, and any desktop picker-surface test), then optimize it to reduce action minutes (path filters scoped to `core/registry`, `scripts/installer`, `modules/coding/orchestration`, and the touched desktop paths; concurrency cancel-in-progress; dependency caching for npm and pip) while keeping comprehensive testing. Keep it platform-agnostic; GitHub Actions is the primary example. Note the standing GitHub Actions budget freeze if it still applies at implementation time.

#### 4.4 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and CI/CD is green.
**Prompt**:
> Run the full validation/test suite (installer pytest suite + `npm test` + `npm run test:shell` + `npm run lint`), confirm the refactor changed no behavior, confirm CI/CD passes and the action-minute reduction is real, and iterate until clean. Update `docs/todos.md` (dashboard metrics + checked-off items) via `[[dev-progress-tracker]]`. Generate a session-history entry for this phase.

---

### Phase 4 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (installer pytest + vitest + shell + lint)
- [x] No known regressions from prior phases
- [x] Known gaps reconciled (v1.18 stay-in-file + new v1.19 items, including the A2 verdict record and the P3 watchlist)
- [x] CI/CD covers all changes and is optimized
- [x] Session history generated for this phase
- [x] Ready to release v1.19.0 (hand off to `/update release`)
