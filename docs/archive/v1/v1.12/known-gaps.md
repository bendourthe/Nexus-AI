# v1.12.0 Known Gaps -- `adoption-ecosystem-2026-07`

Tracks unfinished work, deferrals, and coordination for the v1.12.0 ecosystem adoption ([plan](plans/adoption-ecosystem-2026-07.md), [comparison](comparison-ecosystem-2026-07.md)). One row per gap.

**Severity:** P0 (blocker) / P1 (high) / P2 (medium) / P3 (low).
**Category:** NI (note/info) / DF (deferred) / BG (bug) / MT (migration) / WN (warning) / QG (quality-gate) / CO (coordination).

---

## 1. Phase 1 -- per-model harness selector (H1) + low-cost-model skill (H2) (landed 2026-07-16)

| ID | Sev | Cat | Gap | Disposition |
|----|-----|-----|-----|-------------|
| EM.P1.A | P2 | DF | **The selector is not yet wired into the live prompt path.** [`HarnessSelector`](../../modules/coding/orchestration/HarnessSelector.ts) + its golden A/B ([`HarnessSelectorAb.ts`](../../modules/coding/orchestration/HarnessSelectorAb.ts)) ship and are unit-tested, but nothing yet spreads `overlayForModel()` into a live `PromptContext`. The overlay is composition-root-ready by construction (its keys/types match `PromptContext` exactly, so `{ ...base, ...overlay }` typechecks). | Wire at the composition root: apply `defaultHarnessSelector.overlayForModel(modelName)` in [`ToolActivationContext.buildPromptContext`](../../src/panels/ToolActivationContext.ts) gated on `settings.harnessSelectorEnabled`. Follow the `SO001.P1.A` deferral pattern (mechanism first, composition-root wiring second). |
| EM.P1.B | P2 | QG | **No live weak-model A/B has been run.** The A/B harness + `decideHarnessDefault` gate exist, but measuring a real weak/quantized model needs Ollama + a real `AgentDriver` (unavailable in CI). So `HARNESS_SELECTOR_SHIPPED_DEFAULT = false` and `nexus.coding.harnessSelector.enabled` defaults off -- the SO003.P3.A / no-degradation discipline. | Run `runHarnessAb` over the golden validation split with the live driver on a weak model (e.g. `llama3.2:3b`); flip the default only on a `decideHarnessDefault` net win, and record the measured result either way. |
| EM.P1.C | P3 | DF | **Deeper scaffold knobs are documented but not modeled.** Tool-exposure verbosity and retry / step granularity per tier are described in [docs/reference/low-cost-model-optimization.md](../../docs/reference/low-cost-model-optimization.md) but the `HarnessProfile` drives only the three knobs `PromptContext` already consumes (promptStyle, thinkingMode, systemPromptBudgetPercent). | Extend `HarnessProfile` + `PromptBuilder`/`PromptContext` once EM.P1.A lands (the profile is the natural home; persona text is still hardcoded to 3 styles in `PromptBuilder`, so a fourth requires a `PromptBuilder` change, not just a profile). |
| EM.P1.D | P3 | CO | **H2 shipped as an in-repo guidance doc, not a portable Hub skill.** [docs/reference/low-cost-model-optimization.md](../../docs/reference/low-cost-model-optimization.md) is the technique guidance `HarnessSelector` references; authoring the portable Nexus-Hub `low-cost-model-optimization` skill (or folding it into `model-routing`) is a Hub-repo change, not a Nexus-AI one. | Demand-gated Hub touchpoint (the SO009 precedent: build the runtime here, keep the reusable method in the Hub). Propose on the next `nexus skills sync` / Hub PR. |

## 2. Phase 2 -- surface the v1.7 skill optimizer (L1) (partial, landed 2026-07-16)

| ID | Sev | Cat | Gap | Disposition |
|----|-----|-----|-----|-------------|
| EM.P2.A | P2 | NI | **RESOLVED 2026-07-16 (live end-to-end app-verification pending).** The desktop preview/apply flow landed: a stateful two-call `skills.optimize.preview` / `.apply` sidecar protocol ([SkillOptimizerManager](../../desktop/sidecar/src/coding/skillOptimizerManager.ts) + protocol/handlers, wired in main.ts) + a React approval panel ([SkillOptimizerSettings.tsx](../../desktop/src/pages/settings/SkillOptimizerSettings.tsx), Settings > Skill Optimizer) over the generic Rust `ipc_call` (no Rust change). Approval binds to the EXACT previewed bytes (an additive optional `newContent` on the approval request + a `CapturingApprovalGate`). The engine was decoupled from vscode ([src/tools/commandBlocklist.ts](../../src/tools/commandBlocklist.ts), re-exported from terminal.ts, behavior unchanged) so it is plain-Node-loadable. | Every layer is unit/component-tested with fakes (541 desktop tests green; root suite 4620 green). The one remaining check is the LIVE end-to-end run (app -> preview against Ollama -> approve -> write), which needs the running Tauri app + a local model -- not exercisable in CI. The production preview runner resolves skills under the Nexus-Hub catalog layout and needs a resolvable golden tasks dir (`NEXUS_GOLDEN_TASKS_DIR`); unset -> preview reports "not configured" (never crashes). |
| EM.P2.B | P2 | NI | **RESOLVED 2026-07-16.** `nexus skills frontier` landed: `createHeadlessCandidateFrontier` (seeds diverse candidates by diagnosing failing train tasks, scores via the rollout body-override, wires `WorktreeCandidateManager` over a fail-closed `nodeGitRunner`, promotes only on approval) + `runSkillsFrontier` in [bin/nexus.mjs](../../bin). Resolves the v1.7 `SO005.P4.C`. | Done. Candidate git-branch isolation degrades to a body-override measurement when git is unavailable (fault-tolerant, unit-tested). |
| EM.P2.C | P3 | NI | **The CLI opt-in is by explicit invocation, not the vscode `skillOptimizer.enabled` setting.** The review confirmed no headless settings reader ships (`getSettings()` imports vscode); peers gate on env/flags. `nexus skills optimize` treats running the command as the opt-in and requires `--apply` (+ per-edit confirm) before any write. | Acceptable and safer than a headless vscode-settings reader. Revisit only if a headless settings path is added for another reason. |

## 3. Phase 3 -- extreme-low-bit (BitNet-class) tier gate (Q1) (landed 2026-07-16, fail-closed)

| ID | Sev | Cat | Gap | Disposition |
|----|-----|-----|-----|-------------|
| EM.P3.A | P2 | DF | **The runtime-support version threshold is an unconfirmed placeholder.** `EXTREME_LOW_BIT_MIN_OLLAMA_VERSION` in [core/registry/extremeLowBit.ts](../../core/registry/extremeLowBit.ts) is set to `999.0.0` so the probe is fail-closed (never claims support) until the real llama.cpp/Ollama version that exposes BitNet-class ternary/1-bit GGUF (`TQ1_0`/`TQ2_0`/`Q1_0`/`Q2_0`) is confirmed. | Confirm the support point against the bundled Ollama (v0.32.0 pin, see IO.P3.C) / llama.cpp release notes, then lower the constant. Until then the whole tier is a documented no-op. |
| EM.P3.B | P2 | DF | **No live extreme-low-bit catalog entry, and the TS catalog consumer is not wired to the gate.** The gate mechanism exists + is tested (TS + installer) but nothing populates the tier, and `core/registry/catalog.ts` `loadCatalog()` does not yet call `isExtremeLowBitModelVisible` (the installer's `load_catalog_models` does). | After `EM.P3.A`: add a legitimately-benchmarked BitNet-class entry (e.g. Microsoft `BitNet b1.58 2B4T`) to `catalog.json` with `quant` + `benchmark`, pull only the plain GGUF via Ollama (never the Bonsai vendor / its installer), and wire the TS catalog consumer through the gate for parity with the installer. |
| EM.P3.C | P3 | NI | **The installer opt-in is an env var (`NEXUS_EXTREME_LOW_BIT=1`), not a runtime probe** -- the installer cannot probe Ollama before it is installed, so an explicit operator opt-in is the practical capability gate (the TS probe covers the post-install app). | Acceptable. Revisit if the installer gains a way to check the bundled runtime's quant-format support at pick time. |

## 4. Phase 4 -- disk-offload "patient" tier (E1/E3 landed, E2 blocked) (2026-07-16)

| ID | Sev | Cat | Gap | Disposition |
|----|-----|-----|-----|-------------|
| EM.P4.A | P2 | DF | **No live large-MoE (GLM-5.2) catalog entry, and no bundled offload runtime.** The patient-tier mechanism landed ([core/registry/patientTier.ts](../../core/registry/patientTier.ts): flag + timeout + latency warning + visibility gate, mirrored in the installer) but nothing populates the tier, and the disk-offload runtime is a **user-registered loopback local adapter** (llama.cpp flash-MoE via the OpenAI/Ollama adapter protocol), not bundled. | Enablement: register the offload adapter via `nexus.llm.localAdapters` + set `nexus.llm.backend`, enable `nexus.llm.patientTier.enabled`, and add a `patient-tier`-tagged, hash-verified GLM-5.2 entry to `catalog.json` (`weights` manifest sha256, `NEXUS_PATIENT_TIER=1` to surface it in the installer). Kept hidden by the gate until then so an unrunnable ~370GB model never leaks into the picker. Adopt the flash-MoE path from the llama.cpp lineage, never Colibri's C engine (`D1`). |
| EM.P4.B | P1 | DF | **Warm-KV persistence (E2) is architecturally blocked on the current backend.** Nexus drives Ollama via `/api/chat` (message-array), which returns NO persistable `context` tokens; on resume the message history is re-sent and Ollama re-processes it (`sessionManager.resume` restores messages only). So "reopen with zero re-processing" cannot be delivered without a backend that exposes a persistable KV. | Build with the E1 disk-offload backend (which exposes a KV) or an `/api/generate` context-reuse path. Seam: add a `kvCacheRef?` to `JsonFileSessionStore`'s `PersistedSession`, dehydrated out-of-line via the existing `ArtifactStore` + `redactSecrets` + the secret-path denylist. |
| EM.P4.C | P3 | NI | **The patient timeout is a global flag, not tied to a specific patient backend.** When `nexus.llm.patientTier.enabled` is on, the large timeout applies to all requests (a large timeout never slows a fast request, it just doesn't abort early); the operator enables it only when pointed at a slow backend. | Acceptable. Tighten to per-adapter if a mixed fast/slow backend setup ever needs it. |

## 5. Phase 5 -- code-exec sandbox audit + hardening (H3) (2026-07-16)

| ID | Sev | Cat | Gap | Disposition |
|----|-----|-----|-----|-------------|
| EM.P5.A | P1 | DF | **No OS-level process sandbox for agent-run commands.** The [audit](exec-sandbox-audit.md) found `run_terminal` uses `spawn(shell:true)` at the app user's full privilege; isolation is tool-layer command-string filtering + a DANGEROUS-tier confirmation gate + env scrub + cwd/worktree re-root, none of which confine the process. An approved (or blocklist-evading, e.g. `python -c "..."`) command runs unconfined -- materially weaker than the Codex-lineage OS sandbox. | Multi-cycle native, cross-OS remediation (not landed here; not headless-verifiable). Approach: `sandbox-exec`/Seatbelt (macOS), Landlock+seccomp or bubblewrap (Linux), restricted token / job object / AppContainer (Windows), applied at the `terminal.ts` spawn behind an off-by-default `nexus.coding.execSandbox` + a per-OS capability probe (degrade to today's gate when unavailable), only-ever-tighten, tested on all three OSes before default-on. Interim: the SECURITY.md threat model must state that the defense is confirmation + denylists + env scrub, NOT process confinement; high-assurance users run in an external container/VM. |
| EM.P5.B | P3 | NI | **Landed this phase (bounded hardening):** the built-in secret-path denylist now gates `run_terminal` ([ToolRegistry._denyBySecretPath](../../src/tools/ToolRegistry.ts)), closing the `read_file`/`run_terminal` parity gap (`cat .env` / `echo x > .env` were previously ungated). Always-on, fail-closed, runs after the operator denylist, honors `nexus.secretPathDenyExtra`. | Done (4 tests). Note the inherent limit: paths hidden inside an interpreter's args (`python -c "open('/x')"`) or behind dynamic constructs are not enumerable -- those are covered only by `EM.P5.A`. |
| EM.P5.C | P3 | NI | **SECURITY.md is stale on the exec threat model** (still branded "Gemma Code", pre-rename keys; no statement that there is no OS sandbox). | Refresh SECURITY.md's code-execution section in the Phase 6 FINAL docs pass to reflect the audit's honest boundary + the new secret-path gate. |

## 6. Phase 6 -- FINAL: acceptance gate + reconciliation + docs (2026-07-16)

| ID | Sev | Cat | Gap | Disposition |
|----|-----|-----|-----|-------------|
| EM.P6.A | P3 | CO | **Demand-gated Nexus-Hub cross-link.** The runtime harness selector (H1) + the now-reachable local skill optimizer (L1) are Nexus-internal product code, not portable skills the Hub lacks (H2 shipped as in-repo guidance). A one-line Hub-side cross-link from `model-routing` / `loop-engineering` to these worked local-first examples would document a reusable pattern. | Not warranted as an in-repo change (SO009.P6.A precedent). Propose the one-line cross-link on the next `nexus skills sync` / Hub PR; do not duplicate logic into the Hub. |
| EM.P6.B | P2 | CO | **Release not cut.** v1.12.0's code is complete + green on `feat/v1.12.0-ecosystem-adoption` (7 commits), but the branch is unpushed, local `develop` is 35 ahead of `origin/develop`, and the CHANGELOG narrative + npm version tag are semantic-release-owned (cut on merge to `main`). | Operator: push the branch, open a PR, resolve the `develop`/origin divergence, and let semantic-release tag on merge -- via `/update release`, which keeps its own confirmation gates (no auto-tag/push). |
| EM.P5.C | P3 | NI | **RESOLVED 2026-07-16** -- SECURITY.md's code-execution threat model was refreshed (the honest "no OS process sandbox" boundary + the new `run_terminal` secret-path gate). | Done. A full SECURITY.md de-brand ("Gemma Code" -> Nexus elsewhere in the file) remains a general docs task, out of this cycle's scope. |

## 7. Plan-level deferrals (recorded so the next plan picks them up)

| ID | Sev | Cat | Gap | Disposition |
|----|-----|-----|-----|-------------|
| EM.L2 | P2 | DF | **Background autonomous self-optimization (= v1.7 S5)** remains demand-gated / off by default -- the one item where autonomy meets self-modification of skill files. Unchanged by this cycle. | If ever built: explicit opt-in, local-compute budget cap, human approval before any write, proposals queued for review. Not a v1.12.0 phase. |
| EM.C1 | P3 | DF | **Open Interpreter computer-use / desktop-vision QA** (browser + native-app driving) deferred -- large surface + GPU-vision cost, out of the Coding pillar's scope. | Revisit as a possible cross-pillar capability, not a Coding-pillar item. |
| EM.PENDING | P2 | NI | **All 6 phases complete (v1.12.0 code-complete 2026-07-16)**: H1 harness selector; L1 optimizer CLI+desktop; Q1 extreme-low-bit gate; E1/E3 patient-tier mechanism (E2 deferred, `EM.P4.B`); H3 exec-sandbox audit + secret-path gate (OS sandbox deferred, `EM.P5.A`); FINAL acceptance gate + reconciliation + docs. Remaining is release only (`EM.P6.B`). | Cut the release via `/update release` (push branch, PR, resolve `develop`/origin, semantic-release tag on merge). The deferred `EM.P*` items above are the v1.13+ backlog. |
