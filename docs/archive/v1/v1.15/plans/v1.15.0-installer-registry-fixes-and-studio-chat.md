# Plan - Post-Reinstall Fixes and Chat-Style Studios

**Project**: Nexus AI Studio
**Version**: v1.15.0 (ships as the next 2.x semantic-release)
**Slug**: installer-registry-fixes-and-studio-chat
**Plan Type**: Feature / Enhancement + Bug-fix campaign
**Created**: 2026-07-22
**Goal**: Resolve the five reinstall-reported issues so the installer launches correctly and downloads models reliably, the app truly reflects and manages installed models, the window behaves like a normal maximized desktop app, and Image Studio and Video Lab become intuitive chat interfaces for non-technical users.

## Overview

This milestone addresses five defects the user hit while reinstalling with the new installer. Investigation traced each to a concrete root cause. Issue 1 (installer opens on the "Installation Complete" page): a terminal `state.json` (`status:"completed"`) at `%LOCALAPPDATA%\NexusInstaller\` is never cleared, so the startup planner routes every cold launch to the Complete page, and the uninstaller does not remove it. Issue 2 (models fail to download): the two failures in the user's log (Gemma HTTP 400 from a broken Unsloth GGUF pull target; SANA int4 HTTP 401 from an access-gated repo) are already fixed in the current `core/registry/catalog.json`, meaning the user ran a build predating those fixes; the remaining work is guaranteeing the shipped installer carries the fixed catalog and giving non-technical users a clear, skippable gated-model token flow plus a post-install verify/retry. Issue 3 (app shows only 1 model): the Settings > Models page is still wired to a hardcoded mock client, `models.list` is an unimplemented IPC stub, and the installer's on-disk weights tree and Ollama store are never enumerated. Issue 4 (missing window controls, not maximized): the custom titlebar is fully built and wired but is buried under an opaque `z-index:0` backdrop, and nothing ever maximizes the window. Issue 5 (Image/Video tabs too technical): the parameter-heavy four-tab forms should become chat interfaces mirroring Local Chatbot, with an attachment-capable composer that infers intent (text-to-image, image-to-image, inpaint, outpaint; text-to-video, image-to-video) from attachments and prompt. Issue 6 (VS Code extension broken): the `nexus.coding.newChat` command reports "not found" and the Chat/Memory/Traces webviews load forever because activation throws before commands and webview providers are registered - the eagerly-imported `better-sqlite3` native module can fail to load (Electron ABI mismatch, worsened by the `package:quick` path that skips the electron-rebuild), and `activate()` has no error containment while the memory-subsystem constructor (on by default) is unguarded; the extension also needs renaming to "Nexus Code" and a UX rework to match the Claude Code VS Code extension.

Delivery is a Tauri desktop app (React frontend + TypeScript sidecar + Python diffusion runtimes) distributed through the Windows Python installer, all local-first via Ollama and on-disk diffusers weights. The sidecar diffusion/video protocol already supports every image and video mode via base64 parameters, so the studio redesign is a frontend effort with no protocol change. The registry fix (Issue 3) is sequenced before the studio redesign (Issue 5) because it produces the real model lists the new chat model selectors consume.

Success looks like: a fresh double-click of the installer starts at Welcome; a clean run installs the default model set with no unexplained failures and a plain-language summary; the app opens maximized with working minimize/maximize/close; the Models page lists exactly what is installed and can install/remove; and a non-technical user can open Image Studio or Video Lab, drag in an image (or none), type a request, and get a generation back in a chat thread with parameters tucked behind an optional Advanced panel.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at `docs/archive/v1/v1.15/constitution.md` (nor elsewhere under `docs/`) - skipping check. Recommend running `/constitution` to establish project principles. This is informational, not blocking.

Project-level guardrails that nonetheless apply to this plan (from `AGENTS.md` / `CLAUDE.md`): local-first / zero-outbound (no new outbound services; Ollama and HuggingFace weight fetches are the only network egress, and the gated-token flow stays user-supplied and never bundled); every changed line traces to one of the five issues; per-phase testing and CI updates; ASCII-only commit messages.

## Phases at a Glance

| Phase | Title | Outcome | Rec. model / effort |
|-------|-------|---------|---------------------|
| 1 | Desktop shell: window controls + maximized | Titlebar min/max/close visible and working; app opens maximized | Mid tier, medium - claude-sonnet-5 (medium): small, localized CSS/config change |
| 2 | Installer relaunch state machine (Issue 1) | Fresh launch starts at Welcome; uninstall clears state | Strong reasoning tier, high - claude-opus-4-8 (high): startup/state edge cases, single-instance interplay |
| 3 | Installer download reliability + gated-token UX (Issue 2) | Shipped catalog verified current; clear skippable token flow; post-install verify/retry | Strong reasoning tier, high - claude-opus-4-8 (high): packaging correctness + non-technical UX |
| 4 | Real model registry: IPC + disk/Ollama reconciliation (Issue 3) | Models page reflects and manages actually-installed models | Strong reasoning tier, high - claude-opus-4-8 (high): cross-cutting backend + three storage conventions |
| 5 | Image Studio chat redesign (Issue 5, image) | Chat UI with attachment composer, intent inference, Advanced popover | Strong reasoning tier, high - claude-opus-4-8 (high): net-new UI patterns and intent logic |
| 6 | Video Lab chat redesign (Issue 5, video) | Video chat UI reusing the Phase 5 scaffold | Mid-strong tier, medium-high - claude-opus-4-8 (medium): largely reuse of Phase 5 |
| 7 | VS Code extension: "Nexus Code" fix + Claude Code-style UX (Issue 6) | Extension activates reliably, renamed to Nexus Code, chat UX matches Claude Code | Strong reasoning tier, high - claude-opus-4-8 (high): native-module/activation resilience + UX rework |
| 8 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Clean layout, gaps reconciled (incl. 5.P1.BB), CI green and optimized | Strong reasoning tier, high - claude-opus-4-8 (high): repo-wide refactor + reference repair |

---

## Phase 1: Desktop Shell - Window Controls and Open Maximized

**Goal**: The app window shows working minimize / maximize / close controls and always opens maximized.
**Prerequisites**: None.
**Stability Gate**: On launch the window is maximized; the top-right minimize, maximize/restore, and close buttons are visible, clickable, and perform their action; a regression test asserts the titlebar renders above the backdrop.
**Recommended model**: Mid tier, medium effort - claude-sonnet-5 (medium). Rationale: a small, well-localized CSS stacking-context fix plus a one-line window-config change; re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 1.1 - Un-bury the custom titlebar (window controls)

**Objective**: Make the existing (correctly wired) min/max/close controls visible.

**Prompt**:
> In the Nexus AI Studio Tauri app, the custom titlebar `desktop/src/components/TitleBar.tsx` (mounted at `desktop/src/App.tsx:67`) already renders minimize/maximize/close buttons wired through `desktop/src/lib/windowControls.ts`, and the permissions in `desktop/src-tauri/capabilities/default.json` are already granted. The controls are invisible because the opaque full-viewport backdrop `.nexus-app-backdrop` (`desktop/src/styles/globals.css`, `position:fixed; z-index:0`) paints over the static, non-positioned titlebar. Fix the stacking context: give `.nexus-titlebar` (in `desktop/src/styles/globals.css`, ~lines 98-108) `position: relative; z-index: 1;` (or equivalently wrap `<TitleBar />` at `App.tsx:67` in a `div` with `position:relative; z-index:1`, mirroring how `app-content` is handled). Do not enable native `decorations` (that would double the chrome) and do not modify `TitleBar.tsx`, `windowControls.ts`, or `capabilities/default.json`. Acceptance: the three window buttons and the brand are visible and functional; nothing else in the layout shifts.

---

#### 1.2 - Open maximized by default

**Objective**: The window starts maximized on every launch while remaining resizable/restorable.

**Prompt**:
> In `desktop/src-tauri/tauri.conf.json` (window object, ~lines 13-24) add `"maximized": true` to the main window config. Keep `resizable: true` and the existing `minWidth`/`minHeight` so the user can still restore/resize. Do not add a Rust `maximize()` call unless the config field proves insufficient on the target Tauri v2 version - prefer the declarative config. Acceptance: launching the built app shows a maximized window; clicking restore returns it to a normal resizable window; the `allow-maximize`/`allow-toggle-maximize` capabilities (already present) need no change.

---

#### 1.3 - Testing and Stabilization

**Objective**: Generate and run all tests for this phase and cover it in CI.

**Prompt**:
> Add/extend tests for Phase 1. Unit-test that the titlebar container carries a stacking context above the backdrop (assert the computed/inline `z-index` and `position`, or a snapshot/DOM test in `desktop/tests/` alongside `TopBar.test.tsx`), and add a check that the window config declares `maximized: true`. Manually verify (or via a Tauri smoke check) that the built window opens maximized with functional controls. Run the desktop test suite (`npm run test:shell`) and lint (`npm run lint:shell`); fix failures and iterate until green. Then create or update the CI/CD pipeline to cover the desktop shell changes and optimize it (path filters so shell-only changes do not run installer jobs, concurrency cancel-in-progress, dependency caching); keep it platform-agnostic with GitHub Actions as the primary example. Do not proceed to Phase 2 until stable. After tests pass, run `/generate-session-history` to document Phase 1.

---

### Phase 1 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (unit + shell suite)
- [ ] No known regressions from prior phases
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 2

---

## Phase 2: Installer Relaunch State Machine (Issue 1)

**Goal**: A fresh installer launch always starts at Welcome; a completed install no longer permanently redirects to the Complete page, and uninstall clears installer state.
**Prerequisites**: None (installer is independent of the desktop app).
**Stability Gate**: After a successful install, relaunching the installer opens the Welcome page (not Complete); the in-session tray "reattach shows results" behavior still works; uninstalling removes `%LOCALAPPDATA%\NexusInstaller\`; unit tests cover each startup status.

**Recommended model**: Strong reasoning tier, high effort - claude-opus-4-8 (high). Rationale: startup decision has several states (completed/failed/cancelled/running/interrupted) and interacts with the single-instance path; correctness matters to avoid regressing the tray reattach.

### Sub-tasks

#### 2.1 - Clear terminal state on Complete-page acknowledgement

**Objective**: Treat a completed run as a one-time results view, not a permanent redirect.

**Prompt**:
> In the Nexus AI Studio installer (`scripts/installer/src/nexus_installer/`), a successful install writes `%LOCALAPPDATA%\NexusInstaller\state.json` with `status:"completed"` (`background/recorder.py:202-212`) and nothing deletes it. Make leaving the Complete page clear that state: in `pages/complete.py` `on_finish()` (~line 254), delete the state file via `background.paths.state_file()` (use `Path(...).unlink(missing_ok=True)`), and/or perform the same deletion immediately after `window.switch_page(...Complete)` in `main.py` (~line 554). Ensure deletion is best-effort and never crashes the finish flow. Acceptance: after clicking Finish (or closing from the Complete page), `state.json` is gone; a subsequent launch does not route to Complete.

---

#### 2.2 - Do not treat a cold terminal state as show-complete

**Objective**: Reserve the Complete-redirect for genuine in-session tray reattach, not future cold launches.

**Prompt**:
> In `scripts/installer/src/nexus_installer/background/resume.py`, `interpret_startup()` (~lines 48-61) currently returns `DECISION_SHOW_COMPLETE` for any `STATUS_COMPLETED`/`STATUS_FAILED` state (lines 54-56), which is what wrongly redirects cold relaunches. Change the logic so a cold relaunch (the persisted-state path, `primary_alive=False` in `background/startup.py:plan_startup`) with a terminal status returns `DECISION_FRESH`, while `DECISION_SHOW_COMPLETE` remains reachable only for the in-session reattach that arrives through the single-instance `signal_running_instance` path (see the T703 docstrings in `startup.py` and `single_instance.py`). If a freshness gate is cleaner, gate show-complete on `updated_at` recency plus matching pid/session instead. Verify `main.py:486-487` and `:553-560` still route correctly: `DECISION_FRESH` -> `show_first_page()` (Welcome). Do not break the explicit "Start over" cleanup at `main.py:505-512`. Acceptance: cold relaunch after a completed/failed run shows Welcome; the tray reattach still shows the Complete results view.

---

#### 2.3 - Uninstaller clears the installer state directory

**Objective**: Uninstall fully resets installer state so a reinstall is clean.

**Prompt**:
> Neither uninstall section removes the background-state dir `%LOCALAPPDATA%\NexusInstaller\`. Add its removal to both NSIS uninstall sections: `scripts/installer/legacy/nexus-setup.nsi` (`Section "Uninstall"`, ~lines 135-154) and `scripts/installer/legacy/setup.nsi` (`Section "Uninstall"`, ~lines 217-245), e.g. `RMDir /r "$LOCALAPPDATA\NexusInstaller"`. Confirm this directory name matches `background/paths.py` (`APP_DIR_NAME = "NexusInstaller"`, `_STATE_FILE = "state.json"`). Acceptance: after uninstall, `%LOCALAPPDATA%\NexusInstaller\` no longer exists, so a later installer launch starts fresh even without the 2.1/2.2 fixes.

---

#### 2.4 - Testing and Stabilization

**Objective**: Prove the startup decision across all statuses and cover it in CI.

**Prompt**:
> Add pytest coverage under `scripts/installer/` for the startup decision: table-driven tests over `interpret_startup` / `plan_startup` for each status (completed, failed, cancelled, running-owner-gone, interrupted) in both the cold (`primary_alive=False`) and in-session paths, asserting `DECISION_FRESH` for cold terminal states and `DECISION_SHOW_COMPLETE` only for in-session reattach. Add a test that `pages/complete.py on_finish()` unlinks the state file. Run the installer test suite (the `.venv` under `scripts/installer/` with pytest) and iterate until green. Update/extend CI to run installer tests, gated by a path filter on `scripts/installer/**` (with concurrency cancel-in-progress and pip caching) so desktop-only changes do not trigger them. After tests pass, run `/generate-session-history` for Phase 2.

---

### Phase 2 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (installer pytest suite)
- [ ] No known regressions (tray reattach + Start-over still work)
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 3

---

## Phase 3: Installer Download Reliability + Gated-Token UX (Issue 2)

**Goal**: The shipped installer provably carries the current fixed catalog, gated models present a clear skippable HuggingFace-token flow, and a post-install summary reports and lets the user retry any failed model.
**Prerequisites**: None.
**Stability Gate**: A build check fails if the packaged installer would ship a stale `catalog.json`; a gated model with no token produces an explained, skippable prompt (decline omits the model, no 401 loop); the installing page ends with a plain-language success/failure summary and a working retry for failed models.

**Recommended model**: Strong reasoning tier, high effort - claude-opus-4-8 (high). Rationale: packaging-correctness reasoning (what actually ships in the bundle) plus careful non-technical UX copy and retry semantics.

### Sub-tasks

#### 3.1 - Guarantee the shipped installer carries the current catalog + reliability fixes

**Objective**: Ensure users cannot run a build with the old broken catalog (the root of the user's log).

**Prompt**:
> The user's failing log predates the fixes in commits `e8b500c` (v1.13.0) and `ae3172d` (v1.14.0); the current `core/registry/catalog.json` already reroutes Gemma to `ollama://gemma4:12b` and flags `sana-1.6b-int4` as `gated`. Confirm the packaged installer bundles the current `core/registry/catalog.json` via `nexus_installer.registry_paths` (the `sys._MEIPASS` bundle path used by `engine/model_router.py:default_catalog_path()` and `pages/typed_catalog.py`). Add a packaging/build guard (a PyInstaller spec assertion or a `scripts/` check run in CI/build) that fails if the bundled catalog is missing or does not match the repo `core/registry/catalog.json` (e.g. hash compare), so a stale catalog can never ship. Document in the installer README/notes how the catalog is sourced at build time. Acceptance: building the installer embeds the live catalog; the guard fails a deliberately-staled catalog.

---

#### 3.2 - Clear, explained, skippable gated-model token flow

**Objective**: Non-technical users understand and can skip the HuggingFace token step without a cryptic failure.

**Prompt**:
> The gated-auth subsystem exists (`engine/gated_auth.py:ensure_gated_auth`, invoked from `pages/installing.py:_resolve_gated_auth` ~lines 197-207; token discovery in `engine/hf_auth.py`; puller fast-skip + permanent-401 in `engine/hf_weights_puller.py`). Improve the UX for non-technical users: (a) the gated-model dialog copy should explain in plain language what a gated model is, that it needs a free HuggingFace account + token, exactly where to get one, and that clicking Skip simply omits that one model and continues; (b) ensure a decline cleanly removes the model from the queue (no 401 retry loop) and is reflected in the final summary as "skipped (needs token)", distinct from "failed"; (c) if a token is entered, validate it against the repo (`hf_auth.validate_token_for_repo`) before queueing and show a clear success/failure message. Keep the token user-supplied and never logged (`mask_token`). Acceptance: installing with the default set that includes a gated model shows the explained prompt; Skip continues successfully; a valid token unlocks the download.

---

#### 3.3 - Post-install model verification and retry

**Objective**: End the install with a plain-language summary and a retry path for failures.

**Prompt**:
> On the installing/complete page, present a per-model outcome summary in non-technical language: succeeded, skipped-needs-token, and failed (with a short human reason, e.g. "download error" or "model server rejected the model" rather than raw "Error: 400"/"401"). Distinguish Ollama-pull failures from HuggingFace-download failures and from gated skips. Add a "Retry failed models" action that re-runs only the failed/queued ids through the existing `engine/model_router.py` install path (reusing `model_preflight` where available) without redownloading already-verified models. Ensure the summary and retry integrate with the background state/recorder so retries update `state.failed_models`. Acceptance: after a run with one failure, the user sees a clear summary and can retry just that model; a subsequent success clears it from the failed list.

---

#### 3.4 - Testing and Stabilization

**Objective**: Cover the packaging guard, gated flow, and retry; wire CI.

**Prompt**:
> Add tests: (a) a build/packaging test asserting the bundled catalog matches `core/registry/catalog.json`; (b) unit tests for the gated decline path (model removed, marked skipped, no retry loop) and the token-validate path; (c) unit tests for the retry action re-queuing only failed ids and updating `state.failed_models`; (d) a summary-formatting test mapping raw errors (400/401/network) to the plain-language reasons. Use the installer pytest suite and fakes for network/Ollama. Run until green, then update CI (installer path-filtered job, caching, cancel-in-progress). After tests pass, run `/generate-session-history` for Phase 3.

---

### Phase 3 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (packaging guard + installer suite)
- [ ] No known regressions in the download engine
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 4

---

## Phase 4: Real Model Registry - IPC + Disk/Ollama Reconciliation (Issue 3)

**Goal**: The Settings > Models page (and, downstream, the studio model selectors) reflect the models actually installed on disk and in Ollama, and can install/remove them.
**Prerequisites**: None strictly, but sequenced before Phase 5/6 because it produces their model lists.
**Stability Gate**: After an installer run, the Models page lists the real installed models (Ollama-resident LLM/embeds + on-disk diffusers weights) under Installed, catalog-only entries under Available, and `extra_model_paths.yaml` entries under External; install and remove work end to end; no mock client in the production render.

**Recommended model**: Strong reasoning tier, high effort - claude-opus-4-8 (high). Rationale: cross-cutting work spanning the sidecar, IPC protocol, three unreconciled storage conventions, and the frontend; correctness of enumeration is the crux of Issue 3.

### Sub-tasks

#### 4.1 - Implement the sidecar models.* handlers backed by the real registry

**Objective**: Replace the `NotImplementedError` stubs with real registry-backed handlers.

**Prompt**:
> In `desktop/sidecar/src/handlers.ts` (~lines 135-139) `models.list` and `models.install` are `NotImplementedError` stubs (and `protocol.ts:802` even marks `models.list` `implemented:true`). Implement `models.list`, `models.install`, `models.remove`, and `models.diskUsage` backed by `core/registry/NexusModelRegistry.ts` (`list()` at 152-214 already builds the installed/catalog-only/external buckets; `install()` at 216-226 routes Ollama vs HF/URL). Return the `ListedModelDto` shape the Settings page consumes (see `desktop/src/pages/settings/modelsTypes.ts`), NOT the dropdown-shaped `ModelDropdownEntry`/`ModelsListResponse` in `protocol.ts:585-599`. Add or correct the IPC method schemas in `desktop/sidecar/src/protocol.ts` (`models.list/install/remove/diskUsage`, ~lines 799-803) so the response schema matches `ListedModelDto`, and fix the `implemented` flag to reflect reality. Construct `NexusModelRegistry`/`ModelStorage`/`loadCatalog` in the sidecar composition root (they are currently never imported there). Acceptance: calling `models.list` over IPC returns real buckets; `models.install`/`models.remove` mutate state; `models.diskUsage` returns real usage.

---

#### 4.2 - Reconcile the on-disk contract (weights tree + Ollama store)

**Objective**: Make enumeration actually find what the installer wrote and what Ollama holds.

**Prompt**:
> The installer writes HuggingFace/diffusers models to `~/.nexus/models/weights/<model-id>/...` with no manifest (`scripts/installer/src/nexus_installer/engine/hf_weights_puller.py:128-142`), and Ollama-protocol models (gemma, nomic-embed, qwen) go into Ollama's own store; but `NexusModelRegistry`/`ModelStorage` (`core/registry/ModelStorage.ts:4-13,200-219`) only enumerate `~/.nexus/models/manifests/<family>/<name>/<tag>.json` + `blobs/`. Reconcile so the app sees both: (a) extend enumeration (or add an adapter) to scan `~/.nexus/models/weights/<id>/` and match those ids against `core/registry/catalog.json` to mark them Installed; (b) query the Ollama HTTP API `/api/tags` and surface Ollama-resident models whose ids/tags are in the catalog as Installed. Prefer a read reconciliation in the registry over changing the installer's layout; if you instead choose to have the installer also write `ModelStorage` manifests, keep it consistent for both HF and Ollama pulls. Ensure the catalog remains the single source of truth for display metadata (name, size, license, type). Acceptance: the six models from a successful installer run (nomic-embed-text, realvisxl-v5, sana-video-2b-720p, wan2.1-t2v-1.3b, faster-whisper-large-v3, kokoro-82m) plus any Ollama LLM appear under Installed.

---

#### 4.3 - Wire the real IPC models client into the app

**Objective**: Stop rendering the mock in production.

**Prompt**:
> Create `desktop/src/pages/settings/ipcModelsClient.ts` (`createIpcModelsClient`) mirroring the sibling `ipcSkillsClient.ts`, exposing `list()`, `install()`, `remove()`, `diskUsage()` over the Phase 4.1 IPC methods and returning `ListedModelDto`s. In `desktop/src/App.tsx` (~line 97) pass this real client as the `modelsClient` prop to `<SettingsPage>` (today it is omitted, so `SettingsPage.tsx:36-39` falls back to `createMockModelsClient()`). Keep `mockModelsClient.ts` available for tests/stories only. Verify `ModelsSettings.tsx` (62-113) routes `source: registry/catalog-only/external` into Installed/Available/External correctly against real data. Additionally, add a way to deep-link the Settings page directly to the Models tab (e.g. a route/query param or a shared navigation helper the Settings page reads to preselect the Models tab) so the studios' "Get more models" affordance (Phases 5.3/6.2) can jump straight there; and expose a convenience the studio selectors can consume for "installed and ready" models filtered by task type (image vs video), derived from `source: "registry"`/`"external"` plus catalog capability metadata. Acceptance: in the built app the Models page shows real data; the hardcoded "Gemma 4 E4B / Qwen 2.5 Coder 7B / SDXL Turbo" list no longer appears; navigating with the Models-tab deep-link opens the Models tab directly.

---

#### 4.4 - Testing and Stabilization

**Objective**: Cover enumeration, IPC, and install/remove; wire CI.

**Prompt**:
> Add tests: sidecar handler tests for `models.list/install/remove/diskUsage` (fake registry + fake Ollama `/api/tags` + a temp `~/.nexus/models/weights/<id>/` fixture) asserting correct buckets and `ListedModelDto` shape; a frontend test that `App.tsx` injects the real `modelsClient` and `ModelsSettings` renders installed/available/external from it; a protocol test that the `models.*` schemas match `ListedModelDto`. Run `npm run test:shell` and the sidecar tests; iterate to green. Update CI to exercise the sidecar + settings changes (path filters, caching, cancel-in-progress). After tests pass, run `/generate-session-history` for Phase 4.

---

### Phase 4 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (sidecar + settings + protocol)
- [ ] No known regressions (Skills/Skill Optimizer/Credentials tabs unaffected)
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 5

---

## Phase 5: Image Studio Chat Redesign (Issue 5, Image)

**Goal**: Image Studio becomes a chat interface (model selector + history + composer) where the user pastes/drags/uploads one or more images (or none) and asks to generate/inpaint/outpaint; intent is inferred; technical parameters live behind an Advanced popover with smart per-GPU-tier defaults.
**Prerequisites**: Phase 4 (real image-model list for the selector).
**Stability Gate**: With no image, a prompt does text-to-image; with an attached image it does image-to-image; with a painted mask it inpaints; with an outpaint request + image it outpaints; generated images render inline in the thread with download/copy-workflow/use-as-source actions; no mode tabs remain; the four sidecar endpoints are called unchanged.

**Recommended model**: Strong reasoning tier, high effort - claude-opus-4-8 (high). Rationale: net-new composer (multi-attachment, drag/paste), intent-inference logic, and media-message rendering; the reusable scaffold built here is consumed by Phase 6.

### Sub-tasks

#### 5.1 - Shared chat-studio scaffold: attachment composer + media messages

**Objective**: Build the reusable pieces the image (and later video) chat needs.

**Prompt**:
> Build a reusable "media chat" scaffold under `desktop/src/shared/chat/` (extending, not breaking, the existing `ModelSelector.tsx`, `MessageList.tsx`, `MessageBubble.tsx`, `ChatInput.tsx`, `types.ts`). Add: (a) an attachment-capable composer with a `+` button (multi-file open), drag-and-drop drop zone, and clipboard `onPaste` image handling, producing base64 data URLs via `FileReader.readAsDataURL` (the pattern already used in `ImageStudioPage.tsx:383-384,433-434`), showing removable thumbnail chips for pending attachments; (b) extend `ChatMessage` (`types.ts:13-27`) with an optional `attachments`/`media` field (base64 image or video src + kind); (c) extend `MessageBubble` (currently text-only at `:55`) or add a media bubble variant that renders user-attached image thumbnails and assistant-generated media (`<img>` for PNG, and a slot for video used in Phase 6), plus an in-progress/generating state. Keep the plain-text path intact for Local Chatbot. Acceptance: unit tests show the composer emits base64 attachments and the bubble renders image media.

---

#### 5.2 - Intent-inference layer for image generation

**Objective**: Map (prompt text + attachments + optional mask) to the correct diffusion endpoint.

**Prompt**:
> Implement an intent-inference function that maps composer state to a `DiffusionMode` and request for `desktop/src/modules/image/diffusionClient.ts` (`txt2img/img2img/inpaint/outpaint`, all already supported by the sidecar protocol `desktop/sidecar/src/protocol.ts:318,352-402` via base64 params): 0 images -> txt2img; >=1 image + a painted mask (or explicit "replace/fill" language) -> inpaint; >=1 image + "outpaint/extend/expand" language (with direction/pixels) -> outpaint; else >=1 image -> img2img. Reuse `MaskEditor.tsx` as an on-attachment "paint mask" affordance (not a mode/tab). Handle the protocol's `prompt` min-length-1 constraint (`protocol.ts:354`) for image-only requests by supplying a sensible default prompt or relaxing the schema deliberately (document the choice). Relocate the old form-level validations (source/mask required) from `ImageStudioPage.tsx:181-220` into this layer. Acceptance: unit tests cover each intent branch and the image-only prompt handling.

---

#### 5.3 - Recompose ImageStudioPage as a chat

**Objective**: Replace the four-tab form UI with the chat interface.

**Prompt**:
> Rebuild `desktop/src/modules/image/ImageStudioPage.tsx` (routed at `App.tsx:92` `/images`) to mirror `desktop/src/modules/chat/ChatPage.tsx`: a top `ModelSelector` fed by the real image-model list from the Phase 4 registry (filter to image-capable models; reconcile the old hardcoded `DEFAULT_MODELS` away), a `MessageList` history, and the Phase 5.1 composer at the bottom. The selector MUST list ONLY models that are downloaded and ready to use (registry `source: "registry"`/`"external"` for image-capable models), never catalog-only entries, and MUST append a "Get more models" item at the end of the list that navigates the user to the Settings > Models tab (deep-link to the Models tab, e.g. a route/tab param the Settings page reads) where undownloaded models can be installed. If no image model is installed yet, the selector shows an empty-state prompting "Get more models". Wire send -> Phase 5.2 intent inference -> `diffusionClient` call -> poll `client.drainEvents(jobId)` (reuse the existing polling from `ImageStudioPage.tsx:149-179`) -> append the generated image as an assistant media message. Move all technical parameters (steps, CFG, seed, resolution, sampler, negative prompt, LoRAs, ControlNet, fast-preview) into a collapsed "Advanced settings" popover with smart defaults from `core/config/DiffusionTier.ts` (reuse `DEFAULT_FORM_VALUES`); the popover is the only place `ImagePromptForm`'s fields survive. Remove the `MODE_LABELS` tab header entirely. Migrate the gallery actions (Copy Workflow via `extractWorkflow`, Use as Source, Download) into per-message controls. Acceptance: the page has no mode tabs; a non-technical user can generate from text, from an image, inpaint, and outpaint entirely through chat; Advanced is optional.

---

#### 5.4 - Testing and Stabilization

**Objective**: Cover composer, intent, rendering, and the page; wire CI.

**Prompt**:
> Update `desktop/tests/ImageStudioPage.test.tsx` and add tests for the composer (multi-file/drag/paste -> base64 chips), the intent-inference branches, media-message rendering, and the Advanced popover applying tier defaults. Assert the correct `diffusionClient` method is called per intent with the right base64 payloads, using the `InMemoryDiffusionClient` test double. Run `npm run test:shell` + lint; iterate to green. Update CI for the image module (path filters, caching, cancel-in-progress). After tests pass, run `/generate-session-history` for Phase 5.

---

### Phase 5 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (image module + shared chat scaffold)
- [ ] No known regressions (Local Chatbot text path intact)
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 6

---

## Phase 6: Video Lab Chat Redesign (Issue 5, Video)

**Goal**: Video Lab becomes a chat interface reusing the Phase 5 scaffold; no image -> text-to-video, attached image -> image-to-video; generated clips render inline; parameters live behind an Advanced popover.
**Prerequisites**: Phase 4 (real video-model list), Phase 5 (shared media-chat scaffold).
**Stability Gate**: With no image a prompt does text-to-video; with an attached image it does image-to-video; the generated clip plays inline via the timeline previewer; no mode select remains; the two video sidecar endpoints are called unchanged.

**Recommended model**: Mid-strong tier, medium-high effort - claude-opus-4-8 (medium). Rationale: largely a reuse/adaptation of the Phase 5 scaffold for two video modes; lower novelty than Phase 5.

### Sub-tasks

#### 6.1 - Video intent inference + clip rendering

**Objective**: Reuse the scaffold for video's two modes and render results.

**Prompt**:
> Using the Phase 5.1 media-chat scaffold, implement video intent inference for `desktop/src/modules/video/videoClient.ts` (`text2video`/`image2video`, both supported by `desktop/sidecar/src/protocol.ts:514-524`): 0 images -> text2video; >=1 image -> image2video (pass the base64 `sourceImage`). Render the generated clip inside an assistant media bubble by reusing `desktop/src/modules/video/TimelinePreviewer.tsx` (props `src`, `fps`). Handle the `prompt` min-length constraint consistently with the image path. Drop/reconcile the per-model `mode` field on the old `DEFAULT_VIDEO_MODELS` since intent is now attachment-inferred. Acceptance: unit tests cover both branches and clip rendering.

---

#### 6.2 - Recompose VideoLabPage as a chat

**Objective**: Replace the form UI with the chat interface.

**Prompt**:
> Rebuild `desktop/src/modules/video/VideoLabPage.tsx` (routed at `App.tsx:93` `/videos`) to mirror the Phase 5 image chat: top `ModelSelector` fed by the real video-model list from the Phase 4 registry (same rule as Phase 5.3 - list ONLY downloaded-and-ready video-capable models, append a "Get more models" item that deep-links to the Settings > Models tab, and show an empty-state prompting "Get more models" when none are installed), `MessageList` history, and the shared attachment composer; send -> 6.1 intent inference -> `videoClient` -> poll `drainEvents` (reuse `VideoLabPage.tsx:147-177`) -> append the clip as a media message. Move duration/fps/resolution/steps/cfg/seed/sampler and the presets (`VIDEO_PRESETS`) into a collapsed "Advanced settings" popover with tier defaults; remove the mode `<select>`. Migrate gallery actions (Open, Copy Workflow) into per-message controls. Acceptance: no mode select; a user can make a clip from text or from an image via chat; Advanced is optional.

---

#### 6.3 - Testing and Stabilization

**Objective**: Cover video intent, rendering, and the page; wire CI.

**Prompt**:
> Update `desktop/tests/VideoLabPage.test.tsx` and add tests for the two intent branches, clip rendering via `TimelinePreviewer`, and the Advanced popover defaults, using `InMemoryVideoClient`. Assert the correct `videoClient` method + base64 payloads per intent. Run `npm run test:shell` + lint; iterate to green. Update CI for the video module (path filters, caching, cancel-in-progress). After tests pass, run `/generate-session-history` for Phase 6.

---

### Phase 6 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (video module)
- [ ] No known regressions (image chat + Local Chatbot intact)
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 7

---

## Phase 7: VS Code Extension - "Nexus Code" Activation Fix + Claude Code-Style UX (Issue 6)

**Goal**: The VS Code extension activates reliably (commands register, sidebar views render), is renamed to "Nexus Code", and its chat experience matches the Claude Code VS Code extension.
**Prerequisites**: None (the extension is independent of the installer, registry, and studio work).
**Stability Gate**: On a clean VS Code install of the packaged VSIX, the extension activates without error even when the memory DB / native module is unavailable; `nexus.code.newChat` (and legacy `nexus.coding.newChat` via the shim) work; the sidebar renders its chat UI with no perpetual spinner; the product reads "Nexus Code" everywhere; the chat panel provides a Claude Code-style experience.
**Recommended model**: Strong reasoning tier, high effort - claude-opus-4-8 (high). Rationale: native-module/activation-resilience work plus a substantial UX rework; both are high-risk.

### Sub-tasks

#### 7.1 - Make activation crash-proof (fix "command not found" + forever-loading views)

**Objective**: Register commands and webview providers regardless of engine/DB/native-module failures.

**Prompt**:
> The extension fails because `activate()` throws before commands/providers register. Root cause: the eagerly-imported `better-sqlite3` native module can fail to load (Electron ABI mismatch), and `activate()` (`src/extension.ts:45-57`) has no try/catch; the only unguarded subsystem constructor `buildMemorySubsystem` (`src/panels/ChatPanelInit.ts:89-112`) runs by default (memory is on by default) and its throw aborts activation, so the late-registered command `nexus.coding.newChat` (`src/activation/extensionOnly.ts:320`) and the three webview providers (`extensionOnly.ts:263,:304,:313`) never register -> "command not found" + forever-spinning Chat/Memory/Traces views. Fix: (a) register ALL commands and webview providers BEFORE any heavy engine/DB construction (`new NexusCodingPanel` at `extensionOnly.ts:152`) so the UI surface always exists; (b) wrap the activation branch calls in `src/extension.ts` (`activateProxy`/`activateExtensionOnly`, ~lines 45-57) in try/catch that logs to the output channel and still registers the command/provider surface; (c) add try/catch to `buildMemorySubsystem` mirroring its guarded siblings (return `MemorySubsystem.disabled()` on failure); (d) lazy-load `better-sqlite3` behind the memory feature flag so a missing/ABI-mismatched native binary degrades to a working, memory-disabled extension instead of a dead one. Acceptance: with the memory DB unavailable or the native module unloadable, the extension still activates, all `nexus.*` commands run, and the sidebar renders content.

---

#### 7.2 - Fix native-module packaging + Windows daemon discovery

**Objective**: Ship a VSIX whose native module matches the host Electron, and stop mis-detecting the daemon on Windows.

**Prompt**:
> Two reliability fixes. (a) Packaging: `scripts/build-vsix.ps1` rebuilds `better-sqlite3` for a hardcoded Electron `36.4.0` (~line 105), while `package:quick` (`vsce package`, `package.json:947`) skips the rebuild entirely; either can ship a binary whose ABI mismatches the installed VS Code (`engines.vscode: ^1.90.0` spans several Electron majors). Parameterize the Electron version (derive it from the target VS Code / a config rather than hardcoding), ensure the release path always runs `@electron/rebuild`, and add a guard so a release VSIX cannot be built via the quick path. Document the supported VS Code/Electron range and consider bundling/prebuilding the binary. (b) Windows daemon discovery: `discoverDesktopDaemon` (`src/desktop/daemonDiscovery.ts:78-93`) uses `existsSync` on a named pipe `\\.\pipe\nexus.<user>.sock`, which its own comment says is unreliable; implement the live pipe ping the comment describes (or make extension-only the honest, explicit Windows default) so the activation branch is chosen correctly. Acceptance: a release VSIX activates on the supported VS Code versions; discovery returns the correct branch on Windows.

---

#### 7.3 - Rename "Nexus Coding" -> "Nexus Code" (user-facing) with compat shims for identifiers

**Objective**: Rebrand to "Nexus Code" without breaking installed users.

**Prompt**:
> Rename all user-facing strings to "Nexus Code": in `package.json` the `displayName` (`:3`), the view-container title (`:31`), the command titles (`:58,:62,:70,:74,:78,:82`), and the config section title (`:94`); the `"Nexus Coding"` output channel (`src/extension.ts:42`) and the `[Nexus Coding]` log prefixes; the editor panel title (`extensionOnly.ts:228`); the proxy placeholder (`proxy.ts:60,:144`); and the leftover legacy "Gemma Code" user-facing strings (`NexusCodingPanel.ts:233`, `SessionListPanel.ts:82`, `scaffold.ts:43`, `memoryView.ts:33`, `traceDashboard.ts:17`, `bodyMarkup.ts:38,:53`, `styles.ts:138`). For IDENTIFIERS (breaking to change): mint `nexus.code.*` command IDs, view IDs, and config keys, and add a `nexus.coding.* -> nexus.code.*` forwarder set modeled on the existing `src/activation/compatShim.ts` (which already shims `gemma-code.* -> nexus.coding.*`), plus `deprecationMessage` entries for renamed config keys so users are migrated, not silently reset. Leave the Marketplace identity (`name`/`publisher` -> a `nexus-code` identity) as an explicit product decision - changing it republishes the extension and orphans existing installs - documenting the tradeoff and a recommended path. Acceptance: the product reads "Nexus Code" everywhere user-facing; legacy `nexus.coding.*` command IDs and config keys still resolve via the shim with a one-time deprecation notice.

---

#### 7.4 - Claude Code-style chat UX

**Objective**: Make the extension look, feel, and function like the Claude Code VS Code extension.

**Prompt**:
> Rework the extension UX to match the Claude Code VS Code extension's UI, functionality, options, and feel: consolidate to a single chat-centric panel (rather than three separate Chat/Memory/Traces webviews) delivering the Claude Code experience - a conversational thread with streaming responses, file/selection/@-mention context, inline diff review with accept/reject, plan mode, slash commands, session history/resume, and a model/mode selector. Preserve the local-first engine (Ollama-backed) and the existing capabilities (memory, traces) but surface them the way Claude Code does (context and history affordances within the chat, not perpetual side panels). Reuse the desktop app's shared chat components (`desktop/src/shared/chat/`) where practical and reconcile with the webview scaffold (`src/panels/webview/`). Use the `claude-code-guide` capability to confirm the current Claude Code extension's feature set and layout before finalizing. Acceptance: a user familiar with the Claude Code extension finds the same core interactions here; the three-spinner layout is gone.

---

#### 7.5 - Testing and Stabilization

**Objective**: Cover activation resilience, packaging, rename shims, and the UX; wire CI.

**Prompt**:
> Add tests: an activation test with `better-sqlite3` / memory forced to fail asserting all `nexus.*` commands and the three webview providers STILL register (extend `tests/unit/extension.test.ts`, which today only exercises the happy path with memory disabled); a test that commands/providers register before heavy engine construction; compat-shim tests that legacy `nexus.coding.*` command IDs and config keys resolve to `nexus.code.*` (model on `tests/unit/config/SettingsCompat.test.ts`); a packaging check that the release path runs electron-rebuild and rejects the quick path; and an E2E/webview test that the sidebar renders content (extend `tests/e2e/extension-load.test.ts`). Run the suite + lint; iterate until green. Update CI to build/package the extension and run these tests (path-filtered to the extension surfaces, dependency-cached, concurrency cancel-in-progress). After tests pass, run `/generate-session-history` for Phase 7.

---

### Phase 7 Exit Checklist

- [ ] All sub-tasks completed
- [ ] Extension activates cleanly on a fresh VS Code install of the VSIX (including memory-disabled / native-module-missing degradation)
- [ ] All tests passing (activation resilience, compat shims, packaging, webview render)
- [ ] No known regressions (desktop app + installer unaffected)
- [ ] Session history generated for this phase
- [ ] Ready to advance to Phase 8

---

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| (none - no constitution file; no violations) | | |

---

## Phase 8: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, and its CI/CD complete and optimized.
**Prerequisites**: All prior phases.
**Stability Gate**: The layout is clean (no deprecated/obsolete files, empty dirs, redundant files/dirs, or overcomplicated structure left un-triaged); the version's known gaps are reconciled (including 5.P1.BB, now resolved by Phase 4); CI/CD covers every change and is optimized; project validation/tests pass.
**Recommended model**: Strong reasoning tier, high effort - claude-opus-4-8 (high). Repo-wide refactor + reference repair is high-risk - default to the strong reasoning tier.

### Sub-tasks

#### 8.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure introduced or exposed across Phases 1-7 (e.g. the retired image/video mode-tab code, the now test-only `mockModelsClient`, any dead form components, and any superseded extension panel/webview code from the Phase 7 UX rework), then refactor toward a clean, intuitive layout via `[[project-refactor]]` and `[[docs-layout-refactor]]` (propose-then-apply, with confirmation; repair every reference for anything that moves). Confirm the `docs/archive/v1/v1.15/` tree is complete (plan, history, known-gaps).

#### 8.2 - Known-gaps reconciliation
**Objective**: Reconcile the version's open gaps.
**Prompt**:
> Reconcile this version's known gaps via `[[known-gaps-tracker]]`: create `docs/archive/v1/v1.15/known-gaps.md`, mark gap 5.P1.BB (sidecar `models.*` IPC) resolved by Phase 4, resolve/defer/transfer each open item from `docs/archive/v1/v1.14/known-gaps.md`, and record any newly discovered gaps (e.g. gated-model catalog coverage, cross-platform installer parity beyond Windows). Finalize the per-minor known-gaps file for v1.15.

#### 8.3 - CI/CD create/update/optimize
**Objective**: CI/CD covers all changes and is optimized.
**Prompt**:
> Create or update the CI/CD pipeline so it covers every change in this plan (desktop shell, sidecar, settings/registry, image + video modules, the VS Code extension build/package, and the Python installer), then optimize it to reduce action minutes (path filters per surface, concurrency cancel-in-progress, dependency caching for npm + pip, gating expensive-OS or matrix jobs to merges/schedule) while keeping comprehensive testing. Keep it platform-agnostic; GitHub Actions is the primary example.

#### 8.4 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and CI/CD is green.
**Prompt**:
> Run the full validation/test suite (`npm run test:shell`, the sidecar tests, the extension `npm test` suite, and the installer pytest suite), confirm the refactor changed no behavior, confirm CI/CD passes and the action-minute reduction is real, and iterate until clean. Update `docs/todos.md` (dashboard metrics + checked-off items) via `[[dev-progress-tracker]]`. Generate a session-history entry for this phase.

---

### Phase 8 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing (desktop + sidecar + extension + installer)
- [ ] No known regressions from prior phases
- [ ] Known gaps reconciled (5.P1.BB resolved)
- [ ] CI/CD covers all changes and is optimized
- [ ] Session history generated for this phase
- [ ] Ready to release v1.15.0 (hand off to `/update release`)
