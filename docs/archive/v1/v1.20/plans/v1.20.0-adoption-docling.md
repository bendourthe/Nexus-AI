# Plan - Document Ingest for Desktop Attach (from Docling comparison)

**Project**: Nexus AI Studio
**Version**: v1.20.0
**Slug**: adoption-docling
**Filename**: v1.20.0-adoption-docling.md
**Plan Type**: Feature / Enhancement (adoption, reverse-engineer-first)
**Created**: 2026-08-19
**Generated**: 2026-08-19
**Seeded from**: [../comparisons/v1.20.0-comparison-docling.md](../comparisons/v1.20.0-comparison-docling.md)
**Goal**: Make Nexus Desktop treat an attached, uploaded, or pasted file as a local document-prep step on the existing parse spine (not as a Docling install): wire `parse_document`, route formats honestly, parse DOCX/PPTX/XLSX natively, give Coding the same attach control as Chat, and keep Docling off the default path unless an on-device bake-off proves RapidOCR plus Unlimited-OCR lose on layout.

## Overview

This milestone operationalizes Section 7 of the Docling comparison in reverse-engineer-first order (Section 6 / MCP Registry Policy). The comparison's product answer is the cycle's invariant: **do not run Docling as the default handler when a user attaches, uploads, or pastes a file.** Chat already parses PDF and images through RapidOCR (every OS) and Unlimited-OCR (NVIDIA), with parsed text shown so the user decides whether it enters a prompt (`desktop/src/modules/chat/ChatPage.tsx`). The holes are: Coding's `parse_document` tool is implemented and tested but unwired (LSO.P4.B); Coding's composer is text-only; Office files are silently rejected by `accept="application/pdf,image/*"`; expanding that list without a format router would shove a `.docx` through `load_pages`, which treats non-PDF as a single image.

Delivery stays on the existing spine: `core/documents/` (job manager, runtime factory), `runtimes/ocr/` (Python parse child), sidecar `ocr.*` IPC, `src/tools/handlers/parseDocument.ts` plus `modules/coding/runtime/headlessTools.ts`, and `MediaComposer`. Native Office ingest uses `python-docx`, `python-pptx`, and `openpyxl` (the same libraries Docling's `format-office` extras wrap), not the Docling SDK. Bytes in, markdown/text out, Nexus caps (64 MB, 200 pages, tool 50 pages), no URL sources, no `enable_remote_services`, output treated as untrusted.

A4 (optional local Docling layout engine) is **gated**, not scheduled as a build. Phase 4 runs the on-device OCR quality pass that v1.16 deferred (LSO.P3.C) and records adopt-or-decline. If RapidOCR plus Unlimited-OCR hold, A4 closes as evaluated-and-declined in this cycle. If they lose on tables/reading-order, A4 becomes a known-gap for a later cycle with the comparison's local-only mitigations; it does not silently `pip install docling` here.

Success looks like: a user drops a Word file, a deck, a spreadsheet, a PDF, or a screenshot in Chat and gets parsed text back locally; a coding agent can `parse_document` a workspace PDF behind CONFIRM with redaction and the inbound classifier; Coding's composer accepts the same file kinds; Docling, `docling-mcp`, `docling-serve`, IBM watsonx, Apify, and `convert(https://...)` never appear on the attach path.

## Goals

- **Target problem**: A local-first user who attaches a real document (invoice PDF, Word brief, slide deck, spreadsheet) cannot get that file into gen-AI context without leaving Nexus or being told to install a third-party converter whose happy path fetches URLs.
- **Persona**: The same single-machine developer/creator Nexus already serves (laptop, one consumer GPU, no subscription, no outbound by default).
- **Definition of done**: Chat and Coding accept PDF, images, DOCX, PPTX, and XLSX via click / drop / clipboard file paste; PDF and images still use RapidOCR or Unlimited-OCR; Office files use native parsers; unsupported types fail with a reason, not a silent drop; `parse_document` is registered when `nexus.coding.parseDocument.enabled` is on; parsed text never auto-enters a model prompt; no new outbound destination.

## Plan Family

v1.20.0 is a standalone document-ingest cycle. It does not block and is not absorbed by the in-flight v1.19.2 catalog plan or the locked v2.0.0 / v2.1.0 attachment work (those cover vision routing, audio, and short video, not Office ingest).

- [../../v1.16/plans/v1.16.0-adoption-local-serving-and-ocr.md](../../v1.16/plans/v1.16.0-adoption-local-serving-and-ocr.md) -- the OCR spine this plan extends; carry LSO.P4.B, LSO.P4.C, LSO.P3.C.
- [../../v1.19/plans/v1.19.2-adoption-catalog-and-model-expansion.md](../../v1.19/plans/v1.19.2-adoption-catalog-and-model-expansion.md) -- in-flight catalog schema; do not collide with `modalities` work.
- [../../../v2/v2.0/plans/v2.0.0-adoption-governed-autonomy-multimodal.md](../../../v2/v2.0/plans/v2.0.0-adoption-governed-autonomy-multimodal.md) -- vision/audio attachments; this plan must not steal that phase or regress Image Studio `accept="image/*"`.
- [v1.20.0-adoption-docling.md](v1.20.0-adoption-docling.md) (this plan).

## Grounding

No `STRATEGY.md` and no `docs/solutions/` store were found. Proceeding from README design principles (local-first, originality over wrappers) and the comparison's Section 8 attach table. The v1.16 known-gaps file remains the carry-forward source for LSO.P4.B / LSO.P4.C / LSO.P3.C.

## Constitution Check

*GATE: Must pass before Phase 1 work. Re-check after Phase 1.*

No constitution file found at `docs/archive/v1/v1.20/constitution.md` (nor elsewhere under `docs/`) - skipping check. Recommend running `/constitution` to establish project principles. This is informational, not blocking.

Project-level guardrails that nonetheless apply (from `AGENTS.md` / `README.md` and the seeding comparison): local-first / zero-outbound (bytes in, never `HttpSource`; install-time weights only); MCP Registry Policy reverse-engineer-first (A1 skill-native, A3/A2/A5 reverse-engineered internals, A4 vendor-intrinsic and gated, N1-N11 dropped); untrusted-document discipline (`redactSecrets`, inbound classifier, Chat does not auto-prompt on parse); portable OCR venv must not gain torch (`runtimes/ocr/requirements.txt`); OS parity (Office parsers are pure-Python wheels); every changed line traces to A1, A2, A3, A5, or the A4 decision; ASCII-only commit messages.

## Current model map

**Model map status**: fresh as of 2026-08-19; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.5-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

Notes: OpenAI's public 5.6 family is Sol / Terra / Luna; Terra covers both strong and standard rather than inventing a missing id. Google's 3.7 Flash is mapped to frontier because first-party changelog copy treats the Flash line from 3.5 onward as the intelligent agentic/coding tier; 3.5 Pro was announced and is not treated as an API id here. Cursor cells use the validator ids `cursor-grok-4.6` / `cursor-grok-4.5` for the Cursor Models pool.

### Model map sources

- Anthropic: https://www.anthropic.com/news/claude-opus-5, https://platform.claude.com/docs/en/about-claude/models/overview
- OpenAI: https://openai.com/index/gpt-5-6/, https://developers.openai.com/api/docs/models
- Google: https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/, https://ai.google.dev/gemini-api/docs/changelog
- Cursor: https://cursor.com/docs/models-and-pricing, https://cursor.com/help/models-and-usage/available-models

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|-------|-------|---------|------------------------|--------------------------|
| 1 | Wire `parse_document` (A1) | Sidecar, ACP, scheduler, and VS Code hosts register the existing tool behind the existing flags and guards | frontier | high |
| 2 | Format router + native Office (A3 + A2) | PDF/image/Office sniffing; DOCX/PPTX/XLSX parse to markdown; Chat accept list expanded; unsupported types fail loudly | frontier | high |
| 3 | Coding composer attach (A5) | Coding click / drop / paste uses the same accept list and parse-then-show contract as Chat | strong | high |
| 4 | Layout bake-off (A4 gate) | On-device RapidOCR vs Unlimited-OCR vs table-heavy PDF; A4 adopted only on a recorded loss, otherwise declined | frontier | high |
| 5 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Clean layout, v1.20 gaps reconciled, CI covers the new surfaces | frontier | max |

---

## Out of Scope / Rejected

Carried verbatim in spirit from comparison Section 9. Do not reopen inside a phase prompt.

- **N1** IBM watsonx / managed Docling SaaS / Apify actor
- **N2** Shipping `docling-mcp`
- **N3** `docling-serve` Desktop daemon
- **N4** `HttpSource` / `convert("https://...")` inside the parse path
- **N5** LangChain / LlamaIndex / Crew / Haystack wrappers
- **N6** `convert-remote` / `DOCLING_CONVERSION_MODE=remote`
- **N7** Triton `models-remote` / picture-description APIs
- **N8** Docling ASR / video extras (v2.0 / v2.1 audio-video plans)
- **N9** EasyOCR as a Nexus engine
- **N10** GraniteDocling as the default parser
- **N11** Playwright HTML-render, legacy DOC/XLS/PPT via LibreOffice, XBRL, USPTO, JATS, EBCDIC, Box Notes, EPUB, ODT, EML for this cycle
- **A4 this cycle** Gated Docling layout engine: Phase 4 decision is **DEFER** (do not install Docling). Bake-off note: [../development/ocr-layout-bakeoff-2026-08.md](../development/ocr-layout-bakeoff-2026-08.md). Known-gap DF-5.

Image Studio and Video Lab keep `accept="image/*"` (or their current generation accept lists). This plan does not route studio attachments through document parse.

---

## Phase 1: Wire `parse_document` (A1)

**Goal**: Register the already-written `parse_document` tool on every host that has a document runtime, gated on `nexus.coding.parseDocument.enabled`, with optional memory ingest gated on `nexus.coding.parseDocument.memoryIngest.enabled`.
**Prerequisites**: None. Skill-native / existing-build first per MCP Registry Policy.
**Stability Gate**: With the flag on, sidecar headless/ACP/scheduler and the VS Code coding panel expose `parse_document`; a workspace PDF is read as base64 by the tool (never by the parser as a raw path), redacted, classifier-screened, and CONFIRM-gated. With the flag off, the tool is absent. Memory ingest stays off unless its own flag is on. LSO.P4.B and LSO.P4.C close or are re-filed with a remaining-host note.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: One high signal (risk: untrusted OCR into agent context on production hosts). Wiring is a few composition-root files, but a missed guard is a prompt-injection hole.

### Sub-tasks

#### 1.1 - Sidecar / ACP / scheduler parser injection

**Objective**: Pass a `documentParser` into `createHeadlessTools` wherever the sidecar builds a headless tool set.

**Failure modes**: Malformed or absent `documentBase64` from the tool stays a tool error envelope (existing `DocumentError` codes), never an uncaught spawn; if the Python OCR child is unreachable or slow, the tool returns the existing engine-unavailable / timeout path and does not hang the ACP session past the runtime's request timeout; if `parseDocumentEnabled` is false while a parser object exists, the tool is not registered (flag wins over presence).

**Build class**: load-bearing.

**Prompt**:
> Close known gap LSO.P4.B on the desktop sidecar. `createOcrRuntimeBundle()` already lives in `desktop/sidecar/src/handlers.ts` via `core/documents/ocrRuntimeFactory.ts`. Build a `HeadlessDocumentParser` (see `modules/coding/runtime/headlessTools.ts`) that reads the tool's already-resolved bytes as base64 and calls `OcrParseManager` / the bundle's `parser.start` + `drain` (or the equivalent in-process call the Chat IPC uses). Inject it into every `createHeadlessTools({ ... })` call that is a production coding host: `desktop/sidecar/src/main.ts` (scheduler `runHeadless`), `desktop/sidecar/src/acp/AcpAgent.ts`, and any other sidecar `createHeadlessTools` site. Gate construction on the sidecar's settings equivalent of `nexus.coding.parseDocument.enabled` (default false). Do not pass filesystem paths into the Python runtime; the tool owns `resolveInsideWorkdir`. Do not add Docling. Acceptance: with the flag on and a parser supplied, `createHeadlessTools` includes `parse_document`; with the flag off, it does not; existing `tests/unit/runtime/headlessTools.test.ts` and sidecar ACP/headless tests still pass; add a test that the scheduler/ACP construction site supplies the parser when enabled.

---

#### 1.2 - VS Code composition-root wiring

**Objective**: Pass `parseDocument` into `buildToolRegistry` from `ChatPanelBootstrap.ts`.

**Failure modes**: If Python / the OCR runtime is missing, `resolveParser` fails with a user-visible "install RapidOCR from Settings > Models" (or engine-unavailable) rather than registering a tool that always throws; if `parseDocumentEnabled` is false, omit `parseDocument` from `buildToolRegistry` so the catalog trim and `has("parse_document")` stay consistent; if the user confirms a parse while a previous job is in flight, queue or reject the second call with a busy error rather than interleaving two Python `parse` RPCs on one synchronous child (document the chosen rule).

**Build class**: load-bearing.

**Prompt**:
> In `src/panels/ChatPanelBootstrap.ts`, the `buildToolRegistry({ ... })` call currently omits `parseDocument` (see `src/tools/ToolRegistryBuilder.ts` options). When `settings.parseDocumentEnabled` is true, pass a `ParseDocumentDeps` whose `resolveParser` returns a `DocumentParser` that spawns or reuses the Python OCR runtime the same way the sidecar does (`createOcrRuntimeBundle` / `ChildProcessOcrRuntime`), converting the handler's base64 payload into an `OcrParseManager` job. Do not import `desktop/` from `src/tools/` (the seam exists so the extension host supplies the adapter). Keep CONFIRM via the existing gate (`TOOL_PERMISSION_MAP.parse_document`). Extend `tests/integration/coding-pillar/parse-document-wiring.test.ts` so a bootstrap-shaped call actually registers the tool when the flag is on. Acceptance: flag off -> tool absent; flag on -> tool present, pathGuard + secret-path + redactSecrets still hold.

---

#### 1.3 - Optional memory ingest

**Objective**: Wire `createDocumentMemoryIngestor` when `parseDocumentMemoryIngestEnabled` is true.

**Failure modes**: If `MemoryStore.save` rejects scanned text as injection, report "not stored" with a reason and still return a successful parse (existing `documentMemoryIngestor.ts` contract); if the store is unreachable, same non-fatal outcome; if ingest is enabled but parse is disabled, do not register a dangling ingestor.

**Build class**: load-bearing.

**Prompt**:
> Close LSO.P4.C. `src/tools/handlers/documentMemoryIngestor.ts` is implemented and tested. Construct it in `ChatPanelBootstrap.ts` (and the sidecar parser path if that host has a `MemoryStore`) only when both `parseDocumentEnabled` and `parseDocumentMemoryIngestEnabled` are true. Pass live `sessionId` and provenance (source path, engine, tool name). Do not make a memory failure fail the tool. Acceptance: flag matrix tests (parse off / ingest off / both on); injection rejection is a stored=false reason, not a tool error.

---

#### 1.4 - Testing and Stabilization

**Objective**: Prove wiring on both hosts without pulling OCR weights in CI.

**Prompt**:
> Add/extend unit and integration tests for: flag-gated registration; parser sees base64 only; CONFIRM still required; classifier still listed in `INBOUND_EXTERNAL_DATA_TOOLS`; sidecar `createHeadlessTools` without a parser still omits the tool. Use `NEXUS_OCR_INMEMORY=1` / `InMemoryOcrRuntime` in CI. Run the affected Vitest files, `npm run lint` / typecheck on touched packages, and confirm `test-python-runtimes` is unchanged this phase. Update CI path filters only if new globs are required. Do not proceed to Phase 2 until green. After tests pass, run `/generate-session-history` for Phase 1.

---

### Phase 1 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 2

---

## Phase 2: Format Router + Native Office Ingest (A3 + A2)

**Goal**: Sniff the payload, route PDF/images to the existing OCR engines, parse DOCX/PPTX/XLSX with native libraries into the existing `{ text, markdown, pages }` envelope, and expand Chat's accept list only after the router exists.
**Prerequisites**: Phase 1 (Coding tool can consume the same runtime once Office engines exist).
**Stability Gate**: A dropped `.docx` / `.pptx` / `.xlsx` in Chat parses without RapidOCR installed; a PDF still uses RapidOCR/Unlimited-OCR; a `.html` or random bytes returns `unsupported-media` with a reason; Image Studio still rejects PDFs; 64 MB and zip-entry caps hold; no torch added to `runtimes/ocr/requirements.txt`.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: One high signal (risk: untrusted Office zip/XML parsers on user files). Scope is the existing OCR runtime plus composer accept tests.

### Sub-tasks

#### 2.1 - Magic-byte format router

**Objective**: Classify payloads without trusting `file.type` or a caller MIME.

**Failure modes**: Absent or empty payload keeps `invalid-params`; unknown bytes return `unsupported-media` (not a crash, not a silent image decode); PDF sniffed as `%PDF-` still wins even if the data URL said `image/png`.

**Build class**: load-bearing.

**Prompt**:
> Extend `runtimes/ocr/documents.py`. Today `looks_like_pdf` sniffs `%PDF-` and everything else becomes one `PageImage`. Add a `detect_kind(data: bytes) -> pdf | image | docx | pptx | xlsx | unsupported` helper using magic bytes / ZIP `[Content_Types].xml` (Office Open XML) rather than the caller MIME. Keep `MAX_DOCUMENT_BYTES = 64 * 1024 * 1024`. For OOXML, bound zip member count and decompressed size (copy the *idea* of Docling's iWork limits, not their package; add `defusedxml` only if XML is parsed). Wire `runtimes/ocr/parse.py` to dispatch: pdf/image -> existing `resolve_engine` OCR path; office -> new engines (2.2); unsupported -> `DocumentError("unsupported-media", ...)`. Thread an optional filename hint but never let it override a conflicting magic sniff. Extend `tests/python/ocr/test_documents.py` and `test_parse.py`. Do not import docling.

---

#### 2.2 - Native DOCX / PPTX / XLSX engines

**Objective**: Parse Office Open XML to markdown/text without OCR models.

**Failure modes**: Corrupt zip or missing document.xml returns `unsupported-media` with a stable code; encrypted / password Office files fail closed with a clear message (do not call home for a password cracker); a second parse on the same child process must not leak the previous workbook's sheets (no process-global cache).

**Build class**: load-bearing.

**Prompt**:
> Add `runtimes/ocr/engines/docx_engine.py`, `pptx_engine.py`, and `xlsx_engine.py` implementing `DocumentEngine`. Dependencies: add `python-docx`, `python-pptx`, and `openpyxl` to `runtimes/ocr/requirements.txt` with a comment that these are the Office extras, not Docling, and still no torch. Export markdown into `ParsedDocument.markdown` (headings, lists, tables as GitHub-flavored markdown where cheap) and page-like chunks (Word sections / PPT slides / Excel sheets as `pages[]`). Respect `max_pages` as max slides/sheets/sections. Register in `runtimes/ocr/engines/__init__.py` and `registry.py`. Office parse must succeed on a host with RapidOCR **not** installed. Update `core/documents/OcrParseManager.ts` / `OcrParseResult` only if a `kind` field is needed for UI copy; do not invent a DoclingDocument type. Tests: fixture zip/docx/pptx/xlsx in `tests/python/ocr/` (tiny files committed). Acceptance: three formats round-trip; encrypted files fail closed; byte cap still applies before zip open.

---

#### 2.3 - Chat accept list and honest errors

**Objective**: Chat accepts Office files only after 2.1/2.2, and unsupported drops are visible.

**Failure modes**: A file that fails `fileMatchesAccept` stays dropped (existing) but if we add a catch-all, do not silently swallow; parse errors from the runtime already patch the assistant message in `handleParseDocument` -- keep that; attaching both an image and a docx still uses the first attachment only unless you explicitly extend to N files (default: keep first-only, document it).

**Build class**: load-bearing.

**Prompt**:
> Update `DOCUMENT_ACCEPT` in `desktop/src/modules/chat/ChatPage.tsx` to include Office MIME types and extensions (`.docx`, `.pptx`, `.xlsx`, plus `application/pdf,image/*`). Extend `desktop/tests/MediaComposer.accept.test.tsx` and Chat page tests. Keep Image Studio / Video Lab on `image/*`. Placeholder copy should mention PDF, images, and Office documents. If no document *OCR* model is installed, Office parse must still run (do not block Office behind `documentModelInstalled`). For PDF/image, keep the existing "install RapidOCR" empty-state. Update `docs/install.md` document-parsing paragraph to name Office files and to say Docling is not required. Acceptance: drop a .docx in Chat tests with a fake document client; PDF path unchanged; Image Studio tests unchanged.

---

#### 2.4 - Testing and Stabilization

**Objective**: Python + desktop coverage for the router without weights.

**Prompt**:
> Run `tests/python/ocr/` (new office fixtures + existing PDF raster tests) and desktop MediaComposer/Chat tests. Confirm `runtimes/ocr/requirements.txt` still has no torch. If the installer venv provisioner lists OCR wheels (`REQUIRED_WHEEL_PREFIXES` or `runtimes/ocr/requirements.txt` consumers), add the three Office packages there the same way RapidOCR was declared -- do not restore the retired offline wheel payload. Update CI so `test-python-runtimes` picks up the new tests (existing job should already glob `tests/python/ocr`). Iterate to green. After tests pass, run `/generate-session-history` for Phase 2.

---

### Phase 2 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 3

---

## Phase 3: Coding Composer Attach (A5)

**Goal**: Coding's input accepts the same file kinds as Chat and parses them with the same "show text, do not auto-prompt the model" contract, without bypassing `parse_document` CONFIRM for workspace-path agent calls.
**Prerequisites**: Phase 1 (tool exists) and Phase 2 (router + Office + accept list).
**Stability Gate**: Coding click / drop / clipboard file paste parses through the document client; parsed text appears in the thread; the model is not invoked until the user sends a follow-up; `parse_document` on a workspace path still CONFIRM-gates; slash-command autocomplete still works for text-only turns.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: No high rubric signal; three or more medium (UI + coding page + reuse of Chat parse). CONFIRM bypass would be a regression -- the prompt must forbid it.

### Sub-tasks

#### 3.1 - Shared accept list and Coding MediaComposer

**Objective**: Replace text-only `CodingInput` file handling with `MediaComposer` (or equivalent) using Chat's accept list.

**Failure modes**: Empty text plus an attachment is a valid parse turn (mirror Chat); empty text and no attachment stays disabled; if document IPC fails, show the error in-thread like Chat, do not submit the raw base64 to the coding model.

**Build class**: load-bearing.

**Prompt**:
> `desktop/src/modules/coding/CodingInput.tsx` is text-only (slash-command autocomplete). Add file attach / drop / paste without destroying slash commands: either wrap `MediaComposer` from `desktop/src/shared/chat/MediaComposer.tsx` with `accept` matching Chat's Phase 2 `DOCUMENT_ACCEPT`, or extend `CodingInput` by reusing `fileMatchesAccept` / `onPaste` / `onDrop` from MediaComposer. Keep Enter-to-send and `/` suggestions for text. Export the accept constant from one module so Chat and Coding cannot drift. Tests: `desktop/tests` covering drop of pdf/docx and paste of an image; slash suggestions still render when value starts with `/`.

---

#### 3.2 - Parse-then-show on the coding thread

**Objective**: Attachments parse via the same sidecar `ocr.*` path as Chat; they do not become a silent `parse_document` auto-approve.

**Failure modes**: If `parseDocumentEnabled` is false, still allow Chat-like UI parse through `ocr.parseDocument` IPC (Chat already does this without that flag) -- do not confuse the settings flag (agent tool) with the desktop parse action; if the user attaches a file and types a question, parse first, then leave their question visible with the same "ask a follow-up" hint Chat uses (`ChatPage.tsx` `handleParseDocument`); never concatenate parsed text into the next LLM call automatically.

**Build class**: load-bearing.

**Prompt**:
> In `desktop/src/modules/coding/CodingPage.tsx`, when `onSubmit` receives attachments, call the existing `createIpcDocumentClient()` / `ocr.parseDocument` flow used by Chat (`desktop/src/modules/chat/documentClient.ts`). Render progress with activity `document-parse` (already in `desktop/src/components/agentState/mapping.ts`). Do not invoke `HeadlessAgentSession` / the coding model on that turn. Workspace-path agent parses remain `parse_document` behind CONFIRM (Phase 1). Add CodingPage tests with a fake DocumentClient. Acceptance: attachment turn never hits the model client; follow-up text turn does.

---

#### 3.3 - Testing and Stabilization

**Objective**: Desktop coding composer tests plus a regression that CONFIRM still wraps the tool.

**Prompt**:
> Run desktop Vitest for CodingInput / CodingPage / MediaComposer. Re-run `tests/integration/coding-pillar/parse-document-wiring.test.ts` to prove the agent tool is still CONFIRM. Lint and typecheck. After tests pass, run `/generate-session-history` for Phase 3.

---

### Phase 3 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 4

---

## Phase 4: Layout Bake-off (A4 Gate)

**Goal**: Close the comparison's "not proven here" on PDF layout quality. Record adopt-or-decline for a gated local Docling layout engine. Do not add Docling to the default attach path in this cycle.
**Prerequisites**: Phases 1-3 (product ingest is already useful without Docling).
**Stability Gate**: A dated bake-off note exists under `docs/archive/v1/v1.20/` with the fixtures used, engines compared, and a binary decision. If decline: A4 is listed under this plan's rejected/deferred items and v1.20 known-gaps. If adopt: a follow-up gap describes the local-only mitigations (artifacts_path under `~/.nexus/models`, no URL, no remote extras, separate venv slice, never merge torch into RapidOCR requirements) and **no code lands in this phase**.
**Recommended model tier**: frontier
**Recommended effort level**: high
**Rationale**: One high signal (reasoning: prove a vendor-intrinsic dependency is or is not justified). Implementation of Docling is explicitly out of this phase.

### Sub-tasks

#### 4.1 - On-device OCR quality pass (LSO.P3.C)

**Objective**: Run RapidOCR and, on NVIDIA, Unlimited-OCR against a real invoice and a scanned multi-page PDF.

**Failure modes**: If weights cannot be installed on this host, record "not proven here" with the host reason (no GPU / Downloader placeholder SHA) rather than claiming the engines are bad; if only RapidOCR runs, do not invent Unlimited-OCR scores.

**Build class**: scaffolding (the note is replaced by a later cycle only if A4 is opened as a build).

**Prompt**:
> Execute the on-device QA deferred as LSO.P3.C in `docs/archive/v1/v1.16/known-gaps.md`: install RapidOCR via Settings > Models if the in-app path works; otherwise note IRSC.P4.B / LSO.P3.A. Parse (1) a digital PDF with a table, (2) a scanned image/PDF. Save anonymized excerpts and qualitative notes (reading order, table grid, vs wall-of-text) to `docs/archive/v1/v1.20/development/ocr-layout-bakeoff-2026-08.md`. Do not commit user documents that contain secrets. This is measurement, not a Docling install.

---

#### 4.2 - A4 decision record

**Objective**: Adopt-or-decline the gated Docling layout engine in writing.

**Failure modes**: If the bake-off is incomplete, the decision must be "defer A4, do not install Docling" rather than a silent yes; if someone adds `docling` to requirements in this phase, revert it -- Phase 4 does not change `runtimes/ocr/requirements.txt`.

**Build class**: load-bearing (the decision is the deliverable).

**Prompt**:
> Using the Phase 4.1 note and comparison Section 6, write a short decision at the top of the bake-off file: DECLINE (Unlimited-OCR or RapidOCR is good enough for v1.20 attach) or DEFER-BUILD (layout loss is real; open a v1.21 known-gap describing a local-only `docling-slim` extra with `artifacts_path` under `~/.nexus/models`, `enable_remote_services` forced false, no HttpSource, no mcp/serve, separate from the RapidOCR venv). Cite the MCP Registry Policy. Do not `pip install docling` in this repository in v1.20.0.

---

#### 4.3 - Testing and Stabilization

**Objective**: No product-code change required; keep CI green and record the decision in known-gaps when Phase 5 runs.

**Prompt**:
> If Phase 4 added no code, run a smoke of existing OCR unit tests to ensure nothing drifted. If the bake-off markdown was added, it is docs-only. After the decision is recorded, run `/generate-session-history` for Phase 4.

---

### Phase 4 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 5

---

## Phase 5: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, and its CI/CD complete and optimized.
**Prerequisites**: All prior phases.
**Stability Gate**: The layout is clean (no deprecated/obsolete files, empty dirs, redundant files/dirs, or overcomplicated structure left un-triaged); the version's known gaps are reconciled; CI/CD covers every change and is optimized; project validation/tests pass.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, reference repair, known-gap reconciliation, and release gating carry high context volume and blast radius.

### Sub-tasks

#### 5.1 - Architecture refactor

**Objective**: Refactor toward a well-organized, intuitive layout.

**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure, then refactor toward a clean, intuitive layout via [[project-refactor]] and [[docs-layout-refactor]] (propose-then-apply, with confirmation; repair every reference for anything that moves). Likely check: whether `runtimes/ocr/` should be renamed to `runtimes/documents/` now that Office is not OCR -- only rename if references and CI are updated in the same change; otherwise record a DF. Confirm `core/documents` remains the vscode-free seam.

---

#### 5.2 - Known-gaps reconciliation

**Objective**: Reconcile the version's open gaps.

**Prompt**:
> Reconcile this version's known gaps via [[known-gaps-tracker]]: resolve, defer, or transfer each open item, and finalize the per-minor known-gaps file for the version. Create `docs/archive/v1/v1.20/known-gaps.md` if missing. Close or carry LSO.P4.B, LSO.P4.C, LSO.P3.C from v1.16 with citations to this cycle's commits. Record the A4 bake-off decision. Do not silently drop v1.19.2 in-flight items; those stay in `docs/archive/v1/v1.19/known-gaps.md`.

---

#### 5.3 - CI/CD create/update/optimize

**Objective**: CI/CD covers all changes and is optimized.

**Prompt**:
> Create or update the CI/CD pipeline so it covers every change in this plan (desktop MediaComposer/Chat/Coding tests, `tests/python/ocr/` including Office fixtures, parse-document wiring tests), then optimize it to reduce action minutes (path filters, concurrency cancel-in-progress, dependency caching, gating expensive-OS or matrix jobs to merges/schedule) while keeping comprehensive testing. Keep it platform-agnostic; GitHub Actions is the primary example. Do not add a GPU/weights job.

---

#### 5.4 - Cross-installer parity

**Objective**: Prevent operating-system installer drift when the repository ships multiple installers.

**Prompt**:
> If this repository ships more than one installer, run or add a declarative cross-installer parity gate covering distributed artifacts, supported platforms, named capability counterparts, and external-tool fallbacks, then execute the real installers on their target operating systems with identical postconditions. Cross-link [[platform-contract-verification]] so discovery and delivery are checked in the same pass. If the repository ships zero or one installer, record a silent no-op and continue. Nexus ships a Python Windows installer plus Tauri bundles: confirm Office wheels are declared wherever RapidOCR wheels are declared so macOS/Linux sidecars do not miss `python-docx`.

---

#### 5.5 - Testing and Stabilization

**Objective**: Prove the refactor preserved behavior and CI/CD is green.

**Prompt**:
> Run the full validation/test suite, confirm the refactor changed no behavior, confirm CI/CD passes and the action-minute reduction is real, and iterate until clean. Generate a session-history entry for this phase.

---

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| | | |

---

### Phase 5 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit, integration, and any phase-specific tests)
- [x] No known regressions from prior phases
- [x] Session history generated for this phase
- [x] Ready to cut v1.20.0 (or hand to `/update release`)
