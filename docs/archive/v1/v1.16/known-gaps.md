# Known Gaps - v1.16.0 (Local Model Serving Gateway and Document-OCR Capability)

**Project**: Nexus
**Status**: finalized
**Last updated**: 2026-08-16

Per-version tracker of unfinished work, deferrals, and follow-ups. The next `/plan` ingests this file to decide what carries forward. Classifications: `NI` not-implemented, `DF` deferred, `BG` bug/known-issue, `MT` missing-tests/coverage, `WN` warning/suppressed, `QG` bypassed-gate/CI.

Plan: [plans/v1.16.0-adoption-local-serving-and-ocr.md](plans/v1.16.0-adoption-local-serving-and-ocr.md)

Carry-forward source: [../v1.15/known-gaps.md](../v1.15/known-gaps.md) (reconciled in Phase 6, not this phase).

## v1.16.0

**Summary**: 19 open items after Phase 6 - 0 NI, 15 DF, 4 MT - plus 2 resolved (LSO.P1.A, LSO.P3.E) and 2 bugs found-and-fixed in-phase (LSO.P2.X, LSO.P4.X). No suppressed warnings, no bypassed gates in any phase. Phase 6 is verification-only (RETAINED-NOT-DEAD headers, no behaviour change). Gates: 0 failures across root 434 files / 4813 tests (6 skipped), desktop 95 files / 824 tests, and Python runtimes 196 tests. Root coverage 87.87% lines / 84.23% branches / 91.40% functions; desktop 92.50% lines / 85.56% branches / 84.57% functions. 0 lint errors; `tsc -b`, desktop `tsc --noEmit`, and `build:sidecar` all succeed; `deps:check` 0 errors. The macOS MLX smoke checklist is recorded but not yet run on Apple Silicon hardware (LSO.P5.A).

### Open Items (Phase 1)

| ID | Class | Source phase | Item | Reason | Suggested next step |
|----|-------|--------------|------|--------|---------------------|
| ~~LSO.P1.A~~ | ~~NI~~ | Phase 1 | ~~The gateway reports zero token `usage` on every response~~ | **RESOLVED in Phase 2.1.** `LLMStreamChunkSchema` was widened to keep the counters Ollama and OpenAI-compatible runtimes report on their final chunk (previously stripped by zod's default behavior), and `collectUsage` now threads real prompt/completion counts into both wire envelopes, including the streamed OpenAI finish chunk and the Anthropic `message_delta`. A runtime that reports nothing still yields a well-formed zeroed block, but that is now the genuine "backend told us nothing" case rather than an unconditional placeholder | Done. Seven gateway tests assert the real counts; `message_start` legitimately keeps zeros because nothing has been generated at that point in the stream |
| LSO.P1.B | DF | Phase 1 | Two vscode-free client factories now duplicate their vscode-bound twins' streaming loops: `headlessOpenAiClient.ts` mirrors `LmStudioClientImpl.streamChat` (SSE) exactly as `headlessOllamaClient.ts` already mirrors `OllamaClientImpl.streamChat` (NDJSON) | `LocalAdapterRegistry.ts` statically imports the concrete `OllamaClient` / `LmStudioClient`, which reach `config/settings` + `utils/logger` and therefore `vscode`; the esbuild sidecar bundle (`vscode` is not external) and any plain-Node host cannot load that graph. Mirroring the existing headless precedent was the in-scope option; decoupling the concrete clients from the vscode-bound settings/logger is a pre-existing recorded follow-up that `headlessOllamaClient.ts` itself notes | Decouple `OllamaClient.ts` + `LmStudioClient.ts` from `config/settings` and `utils/logger` (inject them instead), then collapse each pair behind one shared streaming helper. Fold into the Phase 6.1 architecture refactor if scope allows |
| LSO.P1.C | DF | Phase 1 | The gateway's real end-to-end path (Claude Code / Codex / Cursor pointed at `http://127.0.0.1:11500/v1` driving an actual local model) is verified by integration tests against a fake `LLMClient`, not against a running Ollama or LM Studio | The 26 gateway integration tests start a real `node:http` listener on an ephemeral loopback port and assert the wire shapes, auth, SSE framing, and the no-port-when-disabled guarantee - everything except a live model. A real model needs a running runtime with weights, which is deliberately kept out of CI | Point Claude Code (custom base URL + the local token) and Codex at the gateway against a running Ollama during the next on-device QA pass, and record the result as a smoke note alongside the Phase 5.1 macOS MLX smoke check |
| LSO.P1.D | DF | Phase 1 | `nexus.serving.host` and `nexus.serving.port` are settable only by editing `~/.nexus/settings.json` (or via `NEXUS_SERVING_HOST` / `NEXUS_SERVING_PORT`); the Settings section exposes the toggle plus read-only host/port/URL/token | Sub-task 1.5's acceptance criterion is that a non-technical user can toggle the server on and copy the base URL + token, which is met. A port editor invites bind-conflict and validation UX that the phase did not scope; `applyConfig` already handles a host/port delta by rebinding, so the backend is ready for it. Phase 5.2 polished the Models page, not this surface | Add host/port fields with inline validation (loopback-only host, free-port check) to the Local API server section in a later Settings pass |
| LSO.P1.E | MT | Phase 1 | `main.ts`'s serving wiring - the startup `serving.sync()` reconcile and the bounded `shutdown()` that stops the listener - has no direct test | `main.ts` is excluded from desktop coverage by design (`vitest.config.ts`), and the logic it delegates to IS covered: `servingRuntime.sync()` restore-on-relaunch, `gateway.stop()` idempotency, and the toggle-off "refuses further connections" path are all asserted (10 + 26 tests). The wiring itself is a few lines of glue over those | Assert the startup reconcile + shutdown ordering in an integration test that spawns the built sidecar bundle over stdio, during the next test pass |

Note (Phase 1): the plan's sub-task 1.3 says the OpenAI routes are "backed by ... `OllamaClient.ts`, `LmStudioClient.ts`, and any `localAdapters` runtime". They are backed by those *runtimes*, reached through the new vscode-free `ServingAdapter` seam (`desktop/sidecar/src/serving/adapters.ts`) rather than by importing those two modules - see LSO.P1.B for why. The seam reads the same `nexus.llm.localAdapters` manifest shape and enforces the same loopback rule, so a user-registered runtime (including the MLX server the Phase 5.1 docs will describe) is servable with no further code.

Note (Phase 1): the loopback predicate was extracted from `LocalAdapterRegistry.ts` into `modules/coding/llm/loopback.ts`, which the registry now re-exports. This keeps ONE definition shared by the outbound adapter check and the inbound bind check - a rule that drifted between "endpoints we will call" and "addresses we will bind" is how a local-first product would accidentally expose a model server. `tests/unit/llm/loopback.test.ts` asserts the re-export is the same function object.

Note (Phase 1): `shell-build.yml`'s path filter gained `modules/**` because the sidecar bundle now imports from that tree. Cost stays bounded - PRs still run ubuntu-only and the macOS (10x) + Windows (2x) matrix is still gated to push-to-main.

### Open Items (Phase 2)

| ID | Class | Source phase | Item | Reason | Suggested next step |
|----|-------|--------------|------|--------|---------------------|
| LSO.P2.A | DF | Phase 2 | `traceSubscribe()` (`desktop/sidecar/src/coding/panelData.ts`) still returns two hardcoded PLACEHOLDER trace events; the Traces tab's per-request event list is therefore still fake, even though the per-model analytics section above it is now real | Out of scope for A2, which is about per-model *performance analytics* - and the desktop sidecar has no `TraceStore` at all (it is constructed only in the VS Code extension's `activation/extensionOnly.ts`), so wiring real per-event traces means choosing and building a sidecar-side trace store, not just changing a query. The analytics section deliberately reads its own `metrics.inference` IPC rather than riding the placeholder path | Decide whether the sidecar gets its own `TraceStore` or a lighter in-process event ring like `InferenceMetricsRegistry`, then replace the placeholder. Worth pairing with the Phase 6.1 architecture pass |
| LSO.P2.B | DF | Phase 2 | The `model.inference.complete` telemetry-bus event kind and its `createTelemetryMetricPublisher` adapter exist and are tested, but nothing subscribes: no host constructs a bus and attaches the publisher | The bus deliberately does not buffer (an event with no subscriber is dropped), so it cannot satisfy the plan's "metrics accumulate per model across a session" - the registry's bounded ring buffer does that, and the Traces panel polls it. Constructing a bus purely to publish into the void would be dead code and a dep-cruiser orphan | Attach the publisher where a live push actually helps: a future streaming Traces view, or the desktop `LocalModelStatus` dock if per-model tokens/sec should appear there live (`LocalModelTelemetry` already has a `tokensPerWatt` field implying a rate source) |
| LSO.P2.C | DF | Phase 2 | Prompt-token counts are null for any runtime that does not report usage. The local estimate covers completion tokens only (from the accumulated response text), never the prompt | The stream carries the response but not the assembled prompt, so estimating prompt tokens at this boundary would mean re-serializing the request messages - real work with a real cost on the hot path, for a number the backend usually reports anyway | If prompt estimates prove necessary, tokenize the outbound `messages` once in `instrumentStream` behind an opt-in flag, and keep marking the result `estimated` |
| LSO.P2.D | DF | Phase 2 | Memory footprint is Ollama-only (`/api/ps`). An OpenAI-compatible runtime (LM Studio, mlx-vlm) reports `null`, so the analytics table shows a dash in the Memory column for those models | No OpenAI-compatible local runtime exposes a resident-size endpoint; inventing one from the GPU sampler would attribute all VRAM to whichever model happened to run last, which is worse than an honest dash | If per-model memory matters for non-Ollama runtimes, correlate `GpuTelemetrySource`'s `vramAllocatedGB` deltas against load/unload events rather than guessing per request |
| LSO.P2.E | MT | Phase 2 | The `AgentLoop` span-attribute addition (reading `sharedInferenceMetrics().lastFor(model)` and folding `metricSpanAttributes` into the existing `llm_call` `endSpan`) has no dedicated test | Both halves are covered independently - `lastFor` and `metricSpanAttributes` have direct unit tests, and the existing `AgentLoop` suite still passes - but the four-line junction between them is not asserted. `AgentLoop` is a large class whose tests need a full harness | Assert the span attributes in the next `AgentLoop` test pass by recording a metric into an injected registry and reading the captured span |

Note (Phase 2): a bug introduced by Phase 1 was found and fixed here (**LSO.P2.X**, closed same-phase, listed for the record rather than as an open item). `desktop/tests/servingRuntime.test.ts` used `Parameters<typeof createServingRuntime>[0]["models"]`, which does not typecheck because that parameter is optional (`| undefined`). The desktop `npm run typecheck` script therefore failed on commit `67257df`, and `shell-build.yml` runs that script - so Phase 1's CI would have gone red. The Phase 1 report claimed the typecheck gate was clean; that check had been run before the test file existed, and afterwards only vitest (which does not typecheck) and eslint were re-run. Fixed with `NonNullable<Parameters<...>[0]>["models"]`. Process lesson: `tsc --noEmit` must be re-run after adding test files, not only after touching source.

### Open Items (Phase 3)

| ID | Class | Source phase | Item | Reason | Suggested next step |
|----|-------|--------------|------|--------|---------------------|
| LSO.P3.A | DF | Phase 3 | The in-app `models.install` path still cannot install EITHER document model (or any other HuggingFace model): `core/registry/Downloader.ts` fails closed on a digest mismatch and every HF catalog entry carries an all-zero placeholder `source.sha256`, which can never match. Only the Python installer's `hf_weights_puller` tolerates placeholders | Pre-existing and repo-wide - this is v1.15's IRSC.P4.B, not new to the OCR entries. Real digests cannot be computed without downloading multi-GB weights, so the new entries follow the established placeholder convention rather than inventing a different one. The new `source.revision` pins are a genuine improvement in the meantime: they fix the exact bytes to a commit, which a floating `main` never did | Rotate the pins with `scripts/installer/build/pin-hf-weights.py`, OR teach `Downloader.ts` the puller's skip-on-placeholder behavior. One fix closes this and IRSC.P4.B together |
| LSO.P3.B | DF | Phase 3 | The OCR runtime's Python wheels are declared in `REQUIRED_WHEEL_PREFIXES` but no build step actually stages them: nothing in `scripts/` or `.github/` produces `payload/python/requirements.txt` or `payload/python/wheels/` | The offline payload embed was retired in v1.9.0 when the installer moved to a slim fetch-at-install exe (see `build/windows-pipeline.md` History). The same is already true of `diffusers`, `transformers`, `Pillow`, and every other entry in that tuple, so this declares OCR's needs correctly without adding new breakage | Decide whether the venv provisioner should pip-install online from `runtimes/*/requirements.txt` instead of `--no-index`, or restore payload staging. Either way it is one decision covering every runtime, not an OCR-specific fix |
| LSO.P3.C | DF | Phase 3 | Neither OCR engine has been run against real model weights. Every test uses the stub engine, a fake runtime, or an unavailable-host path | Weights are multi-GB and deliberately kept out of CI (the plan says so explicitly). The full pipeline IS exercised end to end - real multi-page PDF rasterization through pypdfium2, real per-page progress, the real JSON-RPC round trip - everything except the model forward pass | Install RapidOCR on any host and Unlimited-OCR on an NVIDIA box, parse a real invoice and a scanned multi-page PDF, and record the output quality as an on-device QA note |
| LSO.P3.D | DF | Phase 3 | Cancelling a parse stops Nexus waiting and discards the result, but does not interrupt an in-flight page inside Python: the runtime processes one `parse` call synchronously | Interrupting mid-inference needs either a worker-per-job model or a cooperative check between pages in the Python loop. The bounded guarantee is honest and documented in `parseManager.ts`; claiming a hard kill we do not perform would be worse | Add a cancellation token the Python page loop checks between pages, which is where a long parse actually spends its time |
| ~~LSO.P3.E~~ | ~~DF~~ | Phase 3 | ~~The desktop app still offers a document model on ANY host: `ModelsSettings.tsx` has no VRAM/tier gating~~ | **RESOLVED in Phase 5.2.** The Models page filters by tier-fit (fits / over-budget) using catalog `vramGB` versus host telemetry `vramTotalGB`, and over-budget rows show "Needs N GB VRAM" instead of Install. The page does not import `DiffusionTier.ts` or add a `requiredVramGB` IPC field; those were a suggested mechanism, and the list DTO already carried `vramGB` | Done. `modelLibrary.test.ts` and `ModelsSettings.test.tsx` assert the filter and the disabled-install state |
| LSO.P3.F | MT | Phase 3 | `ChildProcessOcrRuntime` (the real Python spawn, stdout line handling, and request timeout) has no direct test; every test uses `InMemoryOcrRuntime` | It is a near-copy of `ChildProcessDiffusionRuntime`, which is equally untested, and testing it properly means spawning a Python child in the desktop vitest environment | Add a spawn test with an injected `spawnFn` (the option already exists) asserting the id-vs-notification split and the timeout, covering both runtimes' shared shape |

Note (Phase 3): the plan's 3.1 asked for a recorded portability decision. **Both** models ship rather than one: **RapidOCR PP-OCRv4** (Apache-2.0, ONNX, CPU, ~20 MB, `requiredVramGB: 0`) is the portable tier that makes document parsing work on every supported OS, and **Unlimited-OCR 3B** (MIT, CUDA-only per its model card, `trust_remote_code`, `requiredVramGB: 12`) is the quality tier for capable NVIDIA hosts. Surya 2 was evaluated and rejected on licensing: GPL-3.0 is copyleft, whereas the shipped pair is Apache-2.0 + MIT. This satisfies the OS-parity principle in-cycle instead of deferring a portable fallback, which is what the plan offered as the minimum.

Note (Phase 3): supply-chain controls for the `trust_remote_code` model are three-layered and enforced, not advisory. A new `source.revision` field pins a 40-hex commit sha; `validateSpec` (TS) and `load_weights_manifest` (Python) BOTH refuse a `trustRemoteCode` entry that lacks one, and both reject a branch or tag name since those are mutable. `model_preflight` probes the same pinned commit the download targets, so a reachability check cannot pass against `main` while the pinned commit is gated. The model loads with `local_files_only=True` inside the sandboxed Python runtime only - never in the Node sidecar or the renderer.

Note (Phase 3): `tests/python/` gained its first CI job ever. The ~135 diffusion-runtime tests from v1.0.0 Phase 6 had never run in CI (a grep across all workflows returned nothing); the new `test-python-runtimes` job now covers both runtimes on a bare runner with no GPU, no weights, and no torch - which also keeps the "engine unavailable on this host" path continuously tested.

### Open Items (Phase 4)

| ID | Class | Source phase | Item | Reason | Suggested next step |
|----|-------|--------------|------|--------|---------------------|
| LSO.P4.A | DF | Phase 4 | Headless **tier enforcement is opt-in and no host opts in**. `screenHeadlessCall` enforces tiers only when the host supplies a `confirm` callback, and neither the desktop sidecar's `headlessAgentRunner` nor the `nexus` CLI supplies one - so in practice the headless surface still runs CONFIRM/DANGEROUS tools unprompted | Enforcing tiers without a prompt would refuse `write_file` and `run_terminal`, i.e. disable the headless agent outright (verified: an unconditional version broke 10 existing tests). The headless surface never had tier enforcement, so nothing was relaxed - the machinery now exists and is tested, it just needs a host to wire it. The secret-path half IS unconditional, because refusing `.env` costs nothing legitimate | Give the sidecar a confirm channel (the desktop already renders tool activity, so a prompt has somewhere to go) and pass `confirm` from `headlessAgentRunner`; then headless matches the VS Code registry |
| LSO.P4.B | DF | Phase 4 | No host wires `parseDocument` into `buildToolRegistry` yet, so `parse_document` is not registered anywhere at runtime. The tool, its guards, its tests, and the settings flag all exist; the composition-root wiring does not | The VS Code composition root (`ChatPanelBootstrap.ts`) needs a `DocumentParser` implementation that spawns the Python runtime, and the sidecar needs to pass its in-process `OcrParseManager`. Both are small, but each is a host-specific adapter with its own failure modes (missing Python, missing model), and shipping them unwired-but-tested is safer than shipping them wired-but-unexercised | Wire the sidecar first (the parser is already in-process there via `createOcrRuntimeBundle`), then the extension host with a spawning adapter. Gate both on `settings.parseDocumentEnabled` |
| LSO.P4.C | DF | Phase 4 | Memory ingestion is implemented and tested but also unwired: no host constructs `createDocumentMemoryIngestor`, so `nexus.coding.parseDocument.memoryIngest.enabled` has no effect yet | Same reason as LSO.P4.B - it hangs off the same composition-root wiring, and it needs a `MemoryStore` instance plus the active `sessionId`, which only the panel bootstrap has | Construct it alongside the parser in `ChatPanelBootstrap.ts`, passing the live `MemoryStore` and session id |
| LSO.P4.D | MT | Phase 4 | The headless `parse_document` tool body (base64 read, redaction, byte cap) has no direct test; only its guard wrapper and the VS Code twin are covered | The headless tool is a ~30-line mirror of the VS Code handler over `node:fs` instead of `vscode.workspace.fs`, and the shared logic (guards, redaction) is tested on both the guard module and the VS Code handler | Add a `createHeadlessTools({ documentParser })` test with a temp-dir fixture asserting the redaction and the byte cap, mirroring the existing `headlessTools.test.ts` cases |

Note (Phase 4): a bug introduced during this phase was found and fixed before commit (**LSO.P4.X**, closed same-phase, recorded for the process lesson). The first version of the headless guard wrapper enforced permission tiers unconditionally. Because every write/terminal tool on that surface is CONFIRM or DANGEROUS and no host supplies a `confirm` callback, that refused essentially every agent action - 10 existing tests failed, and in production it would have disabled the headless coding agent entirely. Redesigned so the secret-path check stays unconditional (it refuses nothing legitimate) while tier enforcement waits for a host callback. Lesson: when adding a gate to a surface that never had one, check what the gate's default verdict does to existing traffic before assuming fail-closed is the safe choice.

Note (Phase 4): the canonical permission-tier data moved to a new vscode-free `modules/coding/guardrails/permissionTierMap.ts`, which `PermissionTiers.ts` re-exports. `PermissionTiers.ts` reaches `vscode` transitively (via `utils/logger` and `src/tools/handlers/terminal`), so the headless surface could not import it - and keeping a second copy of the tier map is exactly how two surfaces drift apart. This is the same class of extraction as the Phase 1 loopback predicate, and it was caught the same way: by checking the transitive import graph before wiring, not after the bundle broke. `scripts/generate-tool-permission-table.mjs` now parses the new file; `security:check` verifies the generated SSOT still matches. A hand-transcription error in the extracted map (three tiers wrong) was caught by diffing the parsed maps programmatically rather than by eye.

Note (Phase 4): `parse_document` had to be made TRIMMABLE. `MAX_TOOL_COUNT` is 15 and built-ins were previously never trimmed, so adding a 25th catalog entry pushed the untrimmable core to 16 and broke the prompt-budget cap in two e2e tests. It now trims after MCP tools and BEFORE `codegraph_*`, on the reasoning that losing symbol navigation hurts the default coding path more than losing document OCR does.

### Open Items (Phase 5)

| ID | Class | Source phase | Item | Reason | Suggested next step |
|----|-------|--------------|------|--------|---------------------|
| LSO.P5.A | DF | Phase 5 | The macOS Apple-Silicon MLX smoke checklist exists at `docs/archive/v1/v1.16/testing/macos-mlx-smoke.md` but has not been run on hardware; every box is still blank | Windows cannot virtualize macOS. The how-to and the schema test of the documented manifests are what this host can prove; a live mlx-vlm / LM Studio MLX / nativ chat is `internal-compatible` until an operator fills the checklist | Run the checklist on an M-series Mac, record date / macOS / chip / server / per-step pass-fail in section D, and file any failure as a follow-up row |

Note (Phase 5): LSO.P1.D (host/port editors on the Local API server section) was deliberately left open. Sub-task 5.2 was model-library discovery and the chat/coding quick switcher, not serving settings.

Note (Phase 5): Image Studio and Video Lab already listed installed-and-ready models plus a "Get more models" sentinel. They now share `GET_MORE_MODELS_ID` from `installedFeed.ts` rather than duplicating the constant. They do not mount `QuickModelSwitcher` because their fallback is a single model, not the LLM catalog.

Note (Phase 5): twelve `GrepCodebaseTool` unit tests failed on this host because `rg` is on PATH. `grepWithRipgrep` treats ripgrep exit 1 (no matches) as a successful empty result, so the tests never reached the mocked `findFiles` fallback and asserted against zero hits. Classified ENV/TEST, not a Phase 5 product bug. The three test files now mock `child_process.spawn` to emit `ENOENT` so they keep exercising the fallback. No production grep change.

Note (Phase 5): CI needed no change. `shell-build.yml` already watches `desktop/**` (Models UX, switcher, frontend tests) with concurrency cancel-in-progress, npm cache, cargo cache, and the PR-only ubuntu matrix. `ci.yml` is unfiltered, so `tests/unit/docs/mlx-local-adapter-example.test.ts` rides the existing TypeScript test + 80% coverage-gate jobs. No new script, env var, or dependency.

Note (Phase 3): two pre-existing installer tests (`test_ollama_installer.py::TestExtractTarZst`) fail locally on a missing `zstandard` module. Verified pre-existing by stashing this phase's changes and re-running. CI installs it via `uv sync`, so this is a local-environment artifact, not a regression.

Note (Phase 2): CI needed no change. `shell-build.yml` already watches `desktop/**`, `core/**`, and `modules/**` (the last added in Phase 1), which covers every surface this phase touched on the desktop side, and `ci.yml` runs unfiltered so it covers the `core/`, `modules/`, and `src/` changes. Concurrency cancel-in-progress, npm caching, and the push-to-main gating of the expensive OS matrix were all already in place. Verified rather than assumed.

### Carried forward from v1.15.0 (still open)

v1.15 items stay in [../v1.15/known-gaps.md](../v1.15/known-gaps.md). This cycle only intersects one of them.

| ID | Class | Item | Status this cycle |
|----|-------|------|-------------------|
| IRSC.P4.B (and ICR.P1.C / ICR.P2.C) | BG / MT | HF-weight `sha256` pins are all-zero placeholders, so in-app HF install fails digest verification | Unchanged, and now ALSO the cause of LSO.P3.A (neither OCR model can install through `models.install`). Rotating the pins, or teaching `Downloader.ts` the installer's skip-on-placeholder behaviour, closes all three |
| Remaining IRSC.P2.* / P3.* / P4.A / P4.C / P5.* / P6.* / P7.A | mixed | Installer mypy, on-device QA, MaskEditor/TimelinePreviewer retainers, Claude Code-style extension UX | Unchanged. Out of this cycle's serving/OCR theme. P7.A remains its own follow-up plan |

### Open Items (Phase 6)

_No new product gaps._ The close-out added RETAINED-NOT-DEAD headers on unwired-but-tested surfaces (parse_document, memory ingest, telemetry publisher, Trace placeholder list) rather than deleting them. Dual vscode/headless LLM clients and the serving-adapter twin stay as LSO.P1.B.

| ID | Class | Source phase | Item | Reason | Suggested next step |
|----|-------|--------------|------|--------|---------------------|
| (none) | | Phase 6 | | | |

### Phase 6 reconciliation (terminal gate)

- **Architecture**: clean. No empty directories in v1.16-touched trees (`desktop/sidecar/src/serving`, `core/documents`, `desktop/src/shared/models`, `src/tools/handlers/parseDocument.ts`). `check:docs-layout` and `check:naming` clean. Unwired-but-tested surfaces (`parse_document`, memory ingest, `createTelemetryMetricPublisher`, `panelData` PLACEHOLDER_TRACE) resolved as **retain, not delete** -- each is still unit-tested and is the exact asset a documented deferred gap needs (LSO.P4.B/C, LSO.P2.A/B); each now carries a RETAINED-NOT-DEAD header. Dual vscode/headless LLM client pairs and `serving/adapters.ts` vs `LocalAdapterRegistry` stay as LSO.P1.B until the bound clients are decoupled from vscode settings/logger. `docs/archive/v1/v1.16/` is complete (plan, comparison, known-gaps, guides, testing, docs-cleanup, session histories for Phases 1-6).
- **Known gaps**: 19 open across Phases 1-5 (0 new in Phase 6), all non-blocking; v1.15 carry-forward reconciled above (IRSC.P4.B = LSO.P3.A). No release-blockers. Remaining work is on-device QA (gateway smoke, OCR weights, macOS MLX), composition-root wiring (parse_document), and the vscode-free client consolidation.
- **CI/CD**: no rewrite. `shell-build.yml` already watches `desktop/**` + `core/**` + `modules/**` (serving, OCR sidecar, Models UX) with concurrency cancel-in-progress, npm + cargo cache, and PR-only ubuntu. `ci.yml` is unfiltered so `parse_document`, MLX docs schema, and `test-python-runtimes` always run. Per-job `paths:` is not a GitHub Actions feature; splitting `test-python-runtimes` into its own workflow would skip OCR/diffusion tests on a cross-cutting PR, which is the wrong trade (same decision as v1.15 not path-filtering `ci.yml`). GPU/model-download work stays out of CI. Post-release (same day as the tag): merge-commit CI #362/#363 failed `docs/index.md sync check` (catalog drift from Phase 4 `parse_document` files) and `check:audit-prod` (12 non-allowlisted advisories). Catalog regenerated; in-range overrides bumped; the optional `@huggingface/transformers` -> `onnxruntime-node` -> `adm-zip` / `sharp` chain is allowlisted as ENV.CI.A.
- **Tests**: root 434 files / **4813 passed** / 6 skipped / 0 failed; desktop 95 files / **824 passed**; Python runtimes **196 passed**. One golden-runner timeout (5s) during a parallel desktop+root run reproduced as ENV (CPU contention); isolated re-run 3/3 in 2.3s, then a solo `npm test` 4813/0. `tsc -b`, desktop `tsc --noEmit`, `build:sidecar`, eslint (root + desktop), `deps:check` / `check-architecture` (0 errors / 10 pre-existing warnings), `check:docs-layout`, `check:naming` all clean. Coverage unchanged from Phase 5 (comment-only headers): root 87.87% lines / 84.23% branches / 91.40% functions; desktop 92.50% / 85.56% / 84.57%.
- **Release**: cut as git tag `v1.16.0` via `/update release` (2026-08-16). Not auto-pushed.

### Post-release CI (2026-08-16)

Merge-commit CI on `763fdc1` (runs #362 develop / #363 main) failed two jobs that Phase 6's local gates did not cover. Neither is a product regression; both are environmental gates that drifted after the cycle closed.

| ID | Class | Item | Reason | Suggested next step |
|----|-------|------|--------|---------------------|
| ENV.CI.A | QG | Optional `@huggingface/transformers@4.2.0` production chain reports high via nested `onnxruntime-node` (`adm-zip` GHSA-xcpc-8h2w-3j85, no 0.5.x patch) and `sharp` (libvips CVEs; fix is sharp@0.35, semver-major vs nested 0.34.x). npm marks the parent `fixAvailable: false`. | Inherited optional embedder transitive. The embedder loads bundled ONNX weights, never attacker-supplied zip or image archives. `adm-zip@0.6` and `sharp@0.35` are semver-major relative to the nested copies; an override would be the same class of move the gate forbids without a transformers bump. `protobufjs` on the same chain stays allowlisted as ENV.P5.A (v1.6). | Wait for `@huggingface/transformers` to drop or bump the nested `onnxruntime-node` / `sharp` copies. Until then the four package names (`@huggingface/transformers`, `onnxruntime-node`, `adm-zip`, `sharp`) stay in `scripts/check-prod-audit.mjs`. |
| (fixed, no ID) | -- | `docs/index.md` out of sync with `src/` (activation 3->4 files, tools 22->25) | Phase 4 added `parseDocument.ts` / `documentMemoryIngestor.ts` and activation files after the last catalog commit | Regenerated via `npm run catalog`; `activation` one-liner added to `MODULE_DESCRIPTIONS` |
| (fixed, no ID) | -- | In-range production advisories: `hono` <4.12.34, `dompurify` <=3.4.12, `undici` <7.29.0, `@hono/node-server` <1.19.15, `fast-uri` 3.0.0-3.1.4, `ip-address` <=10.3.0, `react-router` <7.18.2 | Existing `overrides` had gone stale against advisories published after the last bump; desktop `react-router-dom` was still `^7.1.1` | Overrides + desktop dep bumped in-range. `brace-expansion` stays allowlisted (a global 5.0.9 override would force nested 1.x/2.x copies onto 5.x) |

_Last updated: 2026-08-16 (finalized for the v1.16.0 release; post-release CI catalog + audit follow-up recorded as ENV.CI.A)._
