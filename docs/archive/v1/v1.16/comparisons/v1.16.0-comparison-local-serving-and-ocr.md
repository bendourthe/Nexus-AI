# Cross-Project Comparison: Nexus vs. Nativ + Baidu Unlimited-OCR

**Version**: v1.16.0
**Adoption target**: v1.16.0
**Generated**: 2026-07-22
**Analyzer**: Claude Code -- /compare command
**External Sources**:
- https://github.com/Blaizzy/nativ (Repository)
- https://huggingface.co/baidu/Unlimited-OCR (HuggingFace model card)
**Source Types**: Repository + Model card
**Pre-ingest source-security verdict**: CLEAR (both) -- see Section 5.0

---

## Section 1: Executive Summary

This report compares **Nexus** (this repo: a local-first desktop AI Studio, "Your Local AI Studio. Four Pillars, One Desktop, Zero Tokens Billed", formerly Gemma Code) against two external sources that both sit squarely inside Nexus's local-first thesis:

1. **Nativ** (`Blaizzy/nativ`), a native macOS app that runs, serves, monitors, and connects MLX models locally, exposing OpenAI- and Anthropic-compatible loopback endpoints so external coding tools (Claude Code, Codex, Cursor) can consume its local models.
2. **Baidu Unlimited-OCR**, a 3B open-weights (MIT) vision-language OCR model for one-shot long-horizon document/PDF parsing, "aiming to push Deepseek-OCR one step further".

The two sources probe two different Nexus gaps. Nativ probes an **architectural** gap: Nexus is a local-model *consumer* (its `LocalAdapterRegistry` speaks Ollama/OpenAI protocols as a client, and its MCP server exposes read-only tools) but it is not a local-model *provider* -- it has no `/v1/chat/completions` or `/v1/messages` gateway that would let the user's other agent tools reuse Nexus's already-downloaded local models. Unlimited-OCR probes a **capability** gap: Nexus ships open models for image generation (SANA), video (SANA-Video / Wan), ASR (faster-whisper), and TTS (kokoro), but has no document-OCR / PDF-parsing capability at all.

The analysis surfaces **6 adoption candidates** and **5 explicitly-rejected items**. The flagship candidate (A1) is a loopback OpenAI/Anthropic-compatible serving gateway: it is the exact inverse of the v1.6.0 `localAdapters` client, it is on-brand ("Zero Tokens Billed" extended to the user's whole toolchain), and it is local-only by construction (loopback-bound, zero-outbound). The OCR candidate (A5) adds a genuinely new pillar-adjacent capability using the same catalog + Python-sidecar spine that already runs SANA/Whisper/Kokoro, but it carries real supply-chain and portability constraints (CUDA-first, `trust_remote_code=True`) that gate *how* it ships.

**Overall recommendation: selectively adopt.** Both sources are MIT, reputable, and zero-outbound at their core, so adopting their *patterns* introduces essentially no new trust surface. Adopt the serving gateway and the OCR capability as reverse-engineered internal builds on Nexus's existing spine; adopt the metrics and library-UX cues as extensions of in-flight work; keep MLX support as an opt-in loopback `localAdapters` target (documented, not built). Do not adopt nativ's Swift/SwiftUI macOS-only shell, its macOS-26 / Apple-Silicon-only floor, a bundled first-class MLX runtime, or Unlimited-OCR as a default/hard dependency.

---

## Section 2: Project Profiles

| Attribute | Nexus (current) | Nativ (external) | Unlimited-OCR (external) |
|---|---|---|---|
| **Identity** | Local-first desktop AI Studio; "Four Pillars, One Desktop, Zero Tokens Billed" | "Local AI, native to your Mac. Chat, serve, monitor, and connect MLX models from one macOS app" | 3B open-weights VLM for one-shot long-horizon OCR / document parsing |
| **Kind of artifact** | A runtime product (agentic coding + chat + image + video) | A runtime app + local API server | A model (weights + inference code) |
| **Purpose** | Run local open models across four pillars on one desktop | Serve local MLX models to the Mac and to external tools | Parse images / multi-page PDFs into structured text |
| **Version / maturity** | v1.15.0 active cycle (ships as 2.x semantic-release); multi-cycle product | 767 stars; active | 3B; MIT; positions vs Deepseek-OCR |
| **License** | MIT (`LICENSE`) | MIT | MIT |
| **Author / owner** | bendourthe (Nexus-AI) | Blaizzy (Prince Canuma; mlx-vlm maintainer) | Baidu |
| **Primary language** | TypeScript (+ Rust/Tauri shell, Python installer + diffusion sidecar) | Swift 86% (SwiftUI + NativServerKit + embedded Python) | Python (transformers / vLLM / SGLang) |
| **Platform** | Windows installer today; OS parity principle (macOS Intel + Apple Silicon, Linux x86_64) | macOS 26+, Apple Silicon only | CUDA 12.9 / 13.0 (NVIDIA GPUs) tested; multilingual |
| **Network posture** | Local-first; no outbound runtime calls without opt-in | Local-first; loopback server at `127.0.0.1:8080` | Local inference; weights pulled from HF |
| **Relevance to Nexus** | -- | Same domain (local model runner); tests the *serving* gap | Adjacent domain (a model to run); tests the *OCR capability* gap |

---

## Section 3: Capability and Architecture Comparison

### 3a. Nexus vs. Nativ (same domain: local model runner)

| Aspect | Nexus (current) | Nativ (external) | Delta |
|---|---|---|---|
| **Consumes local models** | Yes: Ollama + LM Studio + config-driven `localAdapters` (loopback, `ollama`/`openai` protocol), `modules/coding/llm/LocalAdapterRegistry.ts` | Yes: bundled mlx-vlm server + MLX runtime | Both consume; Nexus is protocol-pluggable |
| **Serves local models to other tools** | **No.** MCP server exposes read-only *tools* (`read_file`, `list_directory`, `grep_codebase`), not an inference endpoint | **Yes.** OpenAI-compatible `/v1/chat/completions`, `/v1/responses`, `/v1/models`; Anthropic-compatible `/v1/messages`; loopback `127.0.0.1:8080` | **Gap -> A1 (flagship)** |
| **Backend runtimes** | Ollama (Metal on Apple Silicon), LM Studio, patient-tier disk-offload MoE via user adapter | MLX / mlx-vlm | Nexus is Ollama/GGUF-lineage; MLX via `localAdapters` only (A3) |
| **Model library UX** | Settings > Models page + `NexusModelRegistry` + `catalog.json` (being reconciled in v1.15 Phase 4) | Model library with discovery + management; menu-bar quick controls | Overlap -> A4 (extend in-flight work) |
| **Performance analytics** | Trace dashboard + observability (`core/observability/`) | Per-model token usage + inference metrics + `/metrics` endpoint + cache mgmt | Partial -> A2 (per-model tokens/sec, TTFT, memory) |
| **App shell** | Tauri 2.x + React 19 (cross-OS) | SwiftUI native (macOS-only) | Nexus keeps Tauri; nativ shell rejected (N1) |
| **Embedded Python + server lifecycle** | Yes (diffusion/video sidecar, embedded runtime) | Yes (NativServerKit wraps embedded Python) | Strong overlap; not a gap |

**Key contrast:** Nexus and nativ are the same *kind* of artifact (a local-model runner with an embedded Python server), pointed in opposite directions. Nexus's v1.6.0 work made it a flexible local-model *client* (register any loopback runtime via a manifest). Nativ's defining feature is the *server* side of that same coin (front your local models behind standard OpenAI/Anthropic APIs). Building that inverse is the single highest-value idea in this comparison.

### 3b. Nexus vs. Unlimited-OCR (adjacent domain: a model to run)

| Aspect | Nexus (current) | Unlimited-OCR (external) | Delta |
|---|---|---|---|
| **Document OCR / PDF parsing** | **None.** No OCR, PDF-to-text, or document-parse capability anywhere in the product | Core purpose: single-image parse (`base`/`gundam`), multi-page via 300 DPI image conversion, `infer_multi()`, `max_length=32768` | **Gap -> A5 (new capability)** |
| **How Nexus runs open models** | Catalog entry in `core/registry/catalog.json` + local Python sidecar runtime (SANA image, SANA-Video, faster-whisper ASR, kokoro TTS) | transformers / vLLM / SGLang; needs `trust_remote_code=True`; custom logit processors | Same *pattern* as existing pillars; new *content* |
| **Vision input today** | Chat accepts base64 image attachments (Local Chatbot / studios); no document understanding | Long-horizon document understanding, multilingual, formatting-preserving output | Complementary, not overlapping |
| **Hardware** | GPU-tiered (Tier 1 6-8 GB, Tier 2 12-16 GB, Tier 3 24+ GB); Ollama/diffusers; cross-OS | CUDA 12.9/13.0 tested; NVIDIA-first | Portability constraint -> N4 / condition on A5 |
| **"100-page PDF in one pass" claim** | -- | Real but bounded: multi-page batched at 300 DPI, generation capped at `max_length=32768`; "one-shot long-horizon" is the framing, not an unbounded guarantee | Report honestly; do not repeat the marketing figure as a hard spec |

---

## Section 4: Gap Analysis

### 4a. Present in External, Missing in Nexus (adoption candidates)

**From Nativ (architecture / serving):**
- **Loopback OpenAI/Anthropic-compatible serving gateway (A1, flagship).** Let external coding tools point at Nexus's already-downloaded local models via `/v1/chat/completions`, `/v1/messages`, `/v1/models`. Nexus has the client (`localAdapters`) and a tools-only MCP server, but no inference gateway.
- **Per-model performance analytics in the UI (A2).** Tokens/sec, time-to-first-token, memory footprint, cache stats per model, surfaced in the Traces panel.
- **MLX reachable as a first-class documented option (A3).** Not a new runtime -- mlx-vlm's server (or LM Studio's MLX mode, or nativ itself) is already consumable today by registering a loopback `localAdapters` manifest with `protocol: "openai"`. The gap is documentation + a macOS Apple-Silicon smoke path, not code.
- **Model-library discovery/management + quick-switch cues (A4).** Extends the v1.15 Phase 4 Models page and a possible quick model-switch affordance.

**From Unlimited-OCR (capability):**
- **Local document-OCR / PDF-parsing capability (A5).** A new catalog model + sidecar handler + a "parse document -> text/markdown" action, following the exact SANA/Whisper/Kokoro pattern.
- **Document-parse as a coding-agent tool + optional memory ingestion (A6).** Once A5 exists, expose it as an agent tool (drop a PDF/screenshot into context) and optionally feed parsed text into the memory subsystem.

### 4b. Present in Nexus, Missing in External (strengths to preserve)

- **Four pillars on one cross-OS desktop.** Nativ is chat/serve only and macOS-only; Unlimited-OCR is a single model. Nexus spans coding + chat + image + video with a Tauri cross-OS shell and OS-parity principle.
- **Protocol-pluggable local-client registry with a hard loopback guard** (`LocalAdapterRegistry.ts`): a manifest-only path to add any loopback runtime, with non-loopback endpoints rejected citing the MCP Registry Policy.
- **Four-layer hybrid-retrieval memory** (BM25 + dense + graph via RRF), GPU-tier scheduler, SSRF guard with per-hop DNS re-validation, permission tiers with floor-clamping, mutation testing, golden-task eval. None of these have a counterpart in either source.
- **Reverse-engineer-first governance** (AGENTS.md MCP Registry Policy) that keeps every addition local-only by construction.

### 4c. Present in Both (quality comparison)

| Capability | Nexus | External | Who does it better |
|---|---|---|---|
| Consume local models | Ollama + LM Studio + pluggable loopback adapters | Nativ: MLX only | Nexus (protocol-agnostic, cross-OS) |
| Embedded Python + server lifecycle | Diffusion/video sidecar | Nativ: NativServerKit | Comparable |
| Run open models locally | SANA / Whisper / Kokoro via catalog + sidecar | Unlimited-OCR via transformers/vLLM | Comparable pattern; Nexus lacks the OCR content (A5) |
| Model management UI | Models page (in-flight v1.15 P4) | Nativ: model library + menu bar | Nativ is more mature today -> A4 |

---

## Section 5: Security and Risk Assessment

*(MANDATORY. Section 5.0 is the pre-ingest source-security scan; Sections 5.1-5.2 are the post-ingest adoption risk assessment. Both reference the AGENTS.md MCP Registry Policy.)*

### 5.0 Pre-ingest source-security scan (Step 1.5)

Both sources were scanned before their content was used to shape any recommendation.

| Check | Nativ (repo/README) | Unlimited-OCR (model card) |
|---|---|---|
| Prompt injection / agent-directed instructions in fetched content | None detected | None detected |
| Malicious / destructive code in the ingested surface | None (README is documentation; no build steps executed) | None (model card is documentation) |
| Supply-chain provenance | Reputable author (mlx-vlm maintainer); MIT; 767 stars | Reputable org (Baidu); MIT |
| Residual supply-chain flags carried to adoption | Bundles an embedded Python distribution + mlx-vlm server (only relevant *if imported* -- we do not import) | Requires `trust_remote_code=True` (arbitrary repo Python) + custom logit processors; CUDA-first |

**Verdict: CLEAR (both).** Neither ingested surface contains prompt-injection, agent-directed instructions, or destructive code, so ingestion proceeded. The two residual flags (nativ's embedded-Python bundle, Unlimited-OCR's `trust_remote_code`) are *adoption-side* concerns and are carried into Section 5.2 and the rejections (N3, N4, N5), not reasons to block ingestion.

### 5.1 Threat-model delta

| Dimension | Adoption delta |
|---|---|
| New runtime dependencies introduced | **A1-A4: zero** (patterns reverse-engineered onto the existing TS sidecar + Rust shell). **A5-A6:** one new optional model + its inference code path. |
| Outbound destinations at runtime | **Zero.** A1's gateway is loopback-bound; A5 pulls weights from HF at install time only (same as SANA/Whisper today). |
| Credentials / API keys required | **Zero** for A1-A4. A5 may need an HF token only if a chosen OCR model is gated (Unlimited-OCR is MIT/ungated). |
| Source / prompts / query text leaving the machine | **No.** A1 is loopback; A5 runs in the local sidecar. |
| New inbound attack surface | **A1 only:** a local HTTP inference server. Must be loopback-bound, opt-in, and carry the existing permission/SSRF posture. |

### 5.2 Per-item risk scorecard

| Item | Risk tier | Justification |
|---|---|---|
| A1 Serving gateway | **Medium** | New *inbound* HTTP surface. Mitigations: loopback bind only (reuse the `LocalAdapterRegistry` loopback guard), opt-in via a setting defaulting off, a local bearer token, no filesystem/tool exposure through the endpoint, rate/size limits. |
| A2 Per-model metrics | Low | Reads local inference timings; surfaces in Traces. No outbound. |
| A3 MLX via localAdapters (doc) | None | Documentation + smoke test of an already-supported mechanism. |
| A4 Model-library UX cues | None | Local UI over the existing registry; overlaps in-flight v1.15 P4. |
| A5 Local OCR capability | **Medium** | Supply chain: `trust_remote_code=True` executes model-repo Python. Mitigations: pin to a specific model revision (never a floating `main`), run only in the sandboxed Python sidecar, GPU-tier-gate it, ship it optional/never-default, and evaluate a more portable open OCR model for non-CUDA hosts. |
| A6 Doc-parse agent tool + memory | Low-Med | Depends on A5; parsed text enters context/memory -> apply `redactSecrets` and treat parsed content as untrusted (route through the inbound classifier, as with `fetch_page`). |

No item is unconditionally High. A1 and A5 are Medium and gated on the mitigations above before they reach the adoption plan.

---

## Section 6: Reverse-Engineering Viability Analysis

*(MANDATORY. Classifies each candidate against the MCP Registry Policy decision tree: local-only / skill-native / reverse-engineer-into-internal / trusted-vendor-wrapper / drop.)*

| Item | Classification | Internal deliverable | Effort | Rationale |
|---|---|---|---|---|
| A1 Serving gateway | `re-full` (bucket 1 local-only + bucket 3) | New loopback inference server on the sidecar, fronting `NexusModelRegistry`, speaking `/v1/chat/completions` + `/v1/messages` + `/v1/models`; opt-in setting + local token | High | The inverse of the existing `localAdapters` client; fully local; no external logic. |
| A2 Per-model metrics | `re-partial` | Extend `core/observability/` + the Traces panel with per-model tokens/sec, TTFT, memory | Medium | Local timing capture; reproducible on the existing spine. |
| A3 MLX via localAdapters | `skill-native` / doc | Docs page + macOS Apple-Silicon smoke test showing an mlx-vlm loopback manifest; no runtime code | Low | Already achievable today; a new MLX runtime was rejected in v1.12 (D4). |
| A4 Model-library UX | `re-partial` | Extend the v1.15 Phase 4 Models page (discovery, quick-switch) | Low-Med | Overlaps in-flight work; polish, not a new subsystem. |
| A5 Local OCR capability | `re-full` (bucket 1 local model + bucket 3) | New `catalog.json` entry + a sidecar OCR handler + a "parse document" action; pinned revision, sandboxed | Medium-High | Same pattern as SANA/Whisper/Kokoro; new content, not new architecture. |
| A6 Doc-parse agent tool + memory | `re-full` | New agent tool wrapping A5 + optional memory ingestion with `redactSecrets` + inbound-classifier routing | Medium | Depends on A5; local-only. |

**Recommendation ordering (per MCP Registry Policy):**
1. **`skill-native` first:** A3 (document MLX-via-adapters).
2. **`re-partial` next:** A4 (library UX, extends in-flight), A2 (per-model metrics).
3. **`re-full` (internal builds):** A1 (serving gateway), A5 (OCR capability), A6 (doc-parse tool + memory).
4. **`vendor-intrinsic`:** none qualify (no candidate has an intrinsic third-party destination worth wrapping).
5. **`drop-outright`:** N1-N5 -> Section 9.

Priority tiers (P0-P3) operate *within* each bucket, not across it.

---

## Section 7: Adoption Plan

### Bucket 1 -- skill-native / doc (ship first, zero new code paths)

| What | Source | Target | Effort | Deps | Risk |
|---|---|---|---|---|---|
| **A3 (P2)** Document MLX support via a loopback `localAdapters` manifest + macOS Apple-Silicon smoke test | Nativ (mlx-vlm server) | New docs page + `docs/archive/v1/v1.16/` smoke note; reuse `LocalAdapterRegistry` | Low | None | None |

### Bucket 2 -- re-partial (extend existing subsystems)

| What | Source | Target | Effort | Deps | Risk |
|---|---|---|---|---|---|
| **A4 (P2)** Model-library discovery + quick-switch UX | Nativ model library | Extend v1.15 Phase 4 Models page | Low-Med | v1.15 P4 landed | None |
| **A2 (P2)** Per-model performance analytics | Nativ metrics / `/metrics` | Extend `core/observability/` + Traces panel | Medium | None | Low |

### Bucket 3 -- re-full (build internal equivalents)

| What | Source | Target | Effort | Deps | Risk |
|---|---|---|---|---|---|
| **A1 (P0, flagship)** Loopback OpenAI/Anthropic-compatible serving gateway | Nativ `/v1/chat/completions`, `/v1/messages`, `/v1/models` | New sidecar inference server fronting `NexusModelRegistry`; opt-in setting (default off) + local token + loopback guard | High | None | Medium |
| **A5 (P1)** Local document-OCR / PDF-parsing capability | Unlimited-OCR (or a more portable open OCR model) | New `catalog.json` entry + sidecar OCR handler + "parse document" action; pinned revision; GPU-tier-gated; optional | Medium-High | None | Medium |
| **A6 (P3)** Doc-parse agent tool + optional memory ingestion | Unlimited-OCR use-case | New agent tool wrapping A5; `redactSecrets` + inbound classifier on parsed text | Medium | A5 | Low-Med |

---

## Section 8: Implementation Sequence

```mermaid
flowchart TD
    subgraph SN["Bucket 1: skill-native / doc"]
        A3["A3 P2 - MLX via localAdapters (doc + smoke)"]
    end
    subgraph RP["Bucket 2: re-partial"]
        A4["A4 P2 - model-library UX"]
        A2["A2 P2 - per-model metrics"]
    end
    subgraph RF["Bucket 3: re-full"]
        A1["A1 P0 - serving gateway (flagship)"]
        A5["A5 P1 - local OCR capability"]
        A6["A6 P3 - doc-parse tool + memory"]
    end
    A1 --> A2
    A5 --> A6
    A4 -.extends v1.15 P4.-> A1
    A3 -.independent.-> A1
```

**Suggested phasing for the v1.16.0 cycle:**
1. **Phase A (P0, flagship):** A1 serving gateway. Highest leverage; makes "Zero Tokens Billed" reusable across the user's whole toolchain. Ship loopback-only, opt-in, token-guarded.
2. **Phase B (P1):** A5 local OCR capability. New user-visible capability on the proven catalog + sidecar spine; ship optional and GPU-tier-gated with a pinned revision.
3. **Phase C (P2):** A2 per-model metrics (pairs naturally with A1's server), A4 library UX (extends v1.15 P4), A3 MLX docs.
4. **Phase D (P3):** A6 doc-parse agent tool + memory ingestion (only after A5).

---

## Section 9: Risks and Explicit Rejections

**General considerations:**
- **Import behaviors, not architecture.** Every candidate is reimplemented in Nexus's existing TypeScript sidecar + Python-runtime + catalog spine. Do not import Swift, SwiftUI, NativServerKit, or a bundled MLX runtime.
- **A1 adds the only new inbound surface.** It must bind loopback only (reuse the `LocalAdapterRegistry` loopback guard), default off, require a local token, expose *only* inference (never file/tool access), and enforce request/size limits. Never bind `0.0.0.0`.
- **A5's model code is executable.** `trust_remote_code=True` runs repo Python; pin a specific revision, run only in the sandboxed sidecar, and prefer a portable open OCR model for non-CUDA / Apple-Silicon / CPU hosts so OS parity is not silently broken.
- **Report the OCR claim honestly.** "100-page PDF in one pass" is bounded by a 32K generation window over 300-DPI page images; document the real behavior, not the headline.

### Items explicitly NOT recommended for adoption

- **N1 -- Nativ's Swift/SwiftUI native macOS app shell.** Nexus is a Tauri cross-OS app with an OS-parity principle. A macOS-only Swift shell conflicts by construction. Adopt the *serving pattern* (A1), not the shell. **Rejected** (architectural).
- **N2 -- macOS 26 + Apple-Silicon-only floor.** Directly violates OS parity ("every claim that works on Windows also works on macOS Intel + Apple Silicon and Linux"). **Rejected** (architectural).
- **N3 -- Bundling mlx-vlm as a first-class Nexus runtime.** A dedicated MLX runtime was already dropped in the v1.12 comparison (D4) as "irrelevant to an Ollama/GGUF product". Keep MLX as an opt-in loopback `localAdapters` target (A3). **Rejected** (prior art + supply chain).
- **N4 -- Shipping Unlimited-OCR (or any OCR model) by default or as a hard dependency.** 3B VRAM cost, CUDA-first, `trust_remote_code`. Must be optional, user-pulled, and GPU-tier-gated. **Rejected as default**; accepted as optional (A5).
- **N5 -- Running `trust_remote_code=True` against a floating revision, unsandboxed.** If A5 ships Unlimited-OCR, pin to a specific commit/revision and run only inside the sandboxed Python sidecar. The naive "just set `trust_remote_code=True`" path is **Rejected** (supply-chain); it is a hard condition on A5.

---

## Appendix: Source Facts (as fetched 2026-07-22)

**Nativ (`Blaizzy/nativ`):** MIT; Swift 86%; 767 stars; macOS 26+, Apple Silicon; bundles mlx-vlm server + MLX runtime; OpenAI-compatible `/v1/chat/completions`, `/v1/responses`, `/v1/models` and Anthropic-compatible `/v1/messages`; default server `http://127.0.0.1:8080`; model library + per-model performance analytics + menu-bar controls; integrates with Codex / Claude Code.

**Baidu Unlimited-OCR:** MIT; 3B VLM; "One-shot Long-horizon Parsing", "aiming to push Deepseek-OCR one step further"; single-image (`base` / `gundam`) + multi-page via 300 DPI image conversion + `infer_multi()` (`max_length=32768`, `no_repeat_ngram_size=35`, `ngram_window=1024`); transformers / vLLM (Docker) / SGLang (custom logit processors); CUDA 12.9 / 13.0 tested; multilingual; requires `trust_remote_code`.
