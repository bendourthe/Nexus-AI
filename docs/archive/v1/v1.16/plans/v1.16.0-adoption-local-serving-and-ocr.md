# Plan - Local Model Serving Gateway and Document-OCR Capability

**Project**: Nexus AI Studio
**Version**: v1.16.0
**Slug**: local-serving-and-ocr
**Filename**: v1.16.0-adoption-local-serving-and-ocr.md
**Plan Type**: Feature / Enhancement (adoption)
**Created**: 2026-07-22
**Seeded from**: [../comparisons/v1.16.0-comparison-local-serving-and-ocr.md](../comparisons/v1.16.0-comparison-local-serving-and-ocr.md)
**Goal**: Turn Nexus from a local-model *consumer* into a local-model *provider* (a loopback OpenAI/Anthropic-compatible gateway fronting the model registry) and give it its first document-OCR / PDF-parsing capability, both reverse-engineered onto the existing sidecar + registry spine, local-only and zero-outbound.

## Overview

This milestone operationalizes the adoption plan from the Nativ + Baidu Unlimited-OCR comparison. It probes two gaps the comparison surfaced. The architectural gap: Nexus consumes local models (Ollama, LM Studio, and any loopback runtime registered through `modules/coding/llm/LocalAdapterRegistry.ts`) and exposes a read-only MCP tools server, but it has no inference gateway that would let the user's other agent tools (Claude Code, Codex, Cursor) reuse Nexus's already-downloaded local models. The capability gap: Nexus ships open models for image (SANA), video (SANA-Video / Wan), ASR (faster-whisper), and TTS (kokoro) via its catalog + Python sidecar, but has no document-OCR / PDF-parsing capability at all. The flagship of this cycle (Phase 1) is the loopback serving gateway, the exact inverse of the v1.6.0 `localAdapters` client and on-brand with "Zero Tokens Billed" extended to the whole toolchain; the second pillar (Phases 3-4) is a new, optional, GPU-tier-gated OCR model plus a document-parse agent tool.

Delivery stays within Nexus's existing stack: a TypeScript sidecar (`desktop/sidecar/`), the model registry (`core/registry/NexusModelRegistry.ts` + `core/registry/catalog.json` + `core/registry/ModelStorage.ts`), the Python runtimes tree (`runtimes/`, where SANA / whisper / kokoro already live), and the Tauri React desktop shell. No architecture is imported: nativ's Swift/SwiftUI macOS-only shell, its macOS-26 / Apple-Silicon-only floor, a bundled first-class MLX runtime, and Unlimited-OCR-as-a-default were all rejected in the comparison (N1-N5). Every candidate is a reverse-engineered internal build (A1, A5, A6) or an extension of an existing subsystem (A2 observability, A4 the Models page), except MLX support (A3), which is documentation of the already-supported loopback-adapter path rather than new code.

**Cross-version prerequisite**: this plan assumes v1.15.0 shipped. v1.15 Phase 4 implements the real `models.*` IPC handlers and the disk/Ollama registry reconciliation that Phase 1 (serving gateway fronting the registry), Phase 4 (OCR model in the registry), and Phase 5 (model-library UX) all build on. If v1.15 has not landed when this cycle starts, sequence its Phase 4 first or treat the registry surface as a hard dependency. Success looks like: the user flips an opt-in setting, points Claude Code / Codex / Cursor at `http://127.0.0.1:<port>/v1`, and drives Nexus's local models from those tools with a local token; the Traces panel shows per-model tokens/sec, TTFT, and memory; a user can install an optional OCR model on a capable GPU and parse a multi-page PDF into text/markdown from the app and from an agent tool; and macOS Apple-Silicon users have a documented, smoke-tested path to run an MLX server through `nexus.llm.localAdapters`.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at `docs/archive/v1/v1.16/constitution.md` (nor elsewhere under `docs/`) - skipping check. Recommend running `/constitution` to establish project principles. This is informational, not blocking.

Project-level guardrails that nonetheless apply to this plan (from `AGENTS.md` / `CLAUDE.md`): local-first / zero-outbound (the serving gateway binds loopback only and defaults off; OCR weights are pulled from HuggingFace at install time only, the same egress path SANA/Whisper already use; no new outbound runtime destination is introduced); OS parity (every claim that works on Windows also works on macOS Intel + Apple Silicon and Linux x86_64, or is explicitly documented in per-platform notes - this is why the CUDA-first OCR model is optional and GPU-tier-gated, and why MLX is reachable via loopback adapters rather than bundled); the MCP Registry Policy reverse-engineer-first decision tree (no candidate adds a third-party runtime dependency); every changed line traces to an adoption item (A1-A6); per-phase testing and CI updates; ASCII-only commit messages.

## Phases at a Glance

| Phase | Title | Outcome | Rec. model / effort |
|-------|-------|---------|---------------------|
| 1 | Local serving gateway (A1, flagship) | Loopback OpenAI/Anthropic API fronting `NexusModelRegistry`; opt-in default-off, local-token + loopback guard | Strong reasoning tier, high - claude-opus-4-8 (high): net-new inbound HTTP surface with real security requirements |
| 2 | Per-model performance analytics (A2) | Tokens/sec, TTFT, memory per model surfaced in the Traces panel | Mid tier, medium - claude-sonnet-5 (medium): local timing capture + a UI panel extension |
| 3 | Local document-OCR capability (A5) | New optional, GPU-tier-gated catalog model + sidecar OCR handler + parse-document action | Strong reasoning tier, high - claude-opus-4-8 (high): supply-chain + cross-platform + new runtime |
| 4 | Document-parse agent tool + memory ingestion (A6) | Parse a PDF/screenshot into agent context; optional memory ingest with redaction + inbound-classifier routing | Strong reasoning tier, medium-high - claude-opus-4-8 (medium): security-sensitive untrusted-content path |
| 5 | MLX-via-adapters docs + model-library UX (A3 + A4) | Documented MLX loopback path + macOS smoke; model-library discovery/quick-switch polish | Mid tier, medium - claude-sonnet-5 (medium): documentation + UI polish over existing subsystems |
| 6 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Clean layout, v1.15/v1.16 gaps reconciled, CI green and optimized | Strong reasoning tier, high - claude-opus-4-8 (high): repo-wide refactor + reference repair |

---

## Phase 1: Local Serving Gateway (A1, flagship)

**Goal**: Nexus exposes its registry-backed local models over a loopback OpenAI- and Anthropic-compatible HTTP API so external agent tools can drive them, without any code leaving the machine.
**Prerequisites**: v1.15 Phase 4 (real `models.*` registry) landed.
**Stability Gate**: With the opt-in setting on, `GET /v1/models` lists the installed models from `NexusModelRegistry`; `POST /v1/chat/completions` (OpenAI shape) and `POST /v1/messages` (Anthropic shape) stream a completion from the selected local model; the server binds a loopback address only and refuses to start on a non-loopback bind; requests without the configured local token are rejected; with the setting off, no listener is opened. Unit + integration tests cover the happy path, the auth rejection, and the loopback guard.
**Recommended model**: Strong reasoning tier, high effort - claude-opus-4-8 (high). Rationale: a net-new inbound HTTP surface with genuine security requirements (loopback binding, token auth, no tool/filesystem exposure, request limits); correctness and threat-model discipline matter. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 1.1 - Serving-gateway settings and lifecycle

**Objective**: Add the opt-in configuration surface and wire the server into the sidecar lifecycle, defaulting off.

**Prompt**:
> In the Nexus AI Studio repo, add a local-serving gateway configuration group to `package.json` `contributes.configuration.properties`, mirroring the existing `nexus.mcp.*` keys: `nexus.serving.enabled` (boolean, default `false`), `nexus.serving.port` (number, default e.g. `11500`), `nexus.serving.host` (string, default `127.0.0.1`), and `nexus.serving.token` (string, default `""`; when empty, generate a random loopback-only token at startup and surface it in the UI). Document each key's description in the local-first / opt-in style used by the surrounding keys. Add the mirrored settings compat entries if the repo's SettingsCompat pattern requires them. Wire a `ServingGateway` lifecycle into the sidecar composition root (the same place `NexusModelRegistry` is constructed for the v1.15 `models.*` handlers, `desktop/sidecar/src/`): start the listener only when `nexus.serving.enabled` is true, stop it on shutdown and when the setting is toggled off. Do NOT open any listener when disabled. Acceptance: toggling `nexus.serving.enabled` starts/stops the server; with it off, no port is bound; the resolved token is retrievable for display.

---

#### 1.2 - Loopback + auth guard

**Objective**: Enforce that the gateway is unreachable off-box and unusable without the local token.

**Prompt**:
> Implement the security guard for the serving gateway. Reuse the loopback-validation logic that `modules/coding/llm/LocalAdapterRegistry.ts` already applies to adapter endpoints (it rejects any non-loopback endpoint citing the AGENTS.md MCP Registry Policy; the check mirrors `modules/coding/utils/ssrf.ts`). Refuse to start the server if `nexus.serving.host` resolves to anything outside `127.0.0.0/8`, `::1`, or a loopback hostname, and log an error that cites the local-first policy. Enforce bearer-token auth on every request: reject with HTTP 401 when the `Authorization` header does not match the resolved `nexus.serving.token`. Add request-size and concurrency limits so a runaway client cannot exhaust the host. The gateway MUST expose only model inference; it must never proxy file access, terminal, or any Nexus tool. Acceptance: a request to a non-loopback bind is refused at startup; a request without/with a wrong token gets 401; oversized requests are rejected; there is no route that reaches the filesystem or tools.

---

#### 1.3 - OpenAI-compatible routes fronting the registry

**Objective**: Serve `/v1/models` and `/v1/chat/completions` (OpenAI shape) backed by real installed models.

**Prompt**:
> Implement the OpenAI-compatible routes on the serving gateway, backed by `core/registry/NexusModelRegistry` and the existing local LLM clients (`modules/coding/llm/OllamaClient.ts`, `LmStudioClient.ts`, and any `localAdapters` runtime). `GET /v1/models` returns the installed chat-capable models (registry `source: "registry"`/`"external"`), mapped to the OpenAI models list shape. `POST /v1/chat/completions` accepts the OpenAI request body (`model`, `messages`, `stream`, `temperature`, etc.), routes to the correct local client for that model, and returns an OpenAI-shaped response; when `stream: true`, emit Server-Sent Events in the OpenAI `chat.completion.chunk` format. Map Nexus/Ollama errors to OpenAI-shaped error bodies without leaking stack traces or file paths. Acceptance: pointing an OpenAI-compatible client (e.g. Codex, or `curl`) at the gateway lists the installed models and streams a completion from a local model.

---

#### 1.4 - Anthropic-compatible route

**Objective**: Serve `POST /v1/messages` (Anthropic shape) so Claude Code and Anthropic-SDK clients can use local models.

**Prompt**:
> Add the Anthropic-compatible `POST /v1/messages` route to the serving gateway. Accept the Anthropic Messages request body (`model`, `messages`, `system`, `max_tokens`, `stream`), translate it to the internal chat request, route to the selected local client, and return an Anthropic-shaped response; when `stream: true`, emit the Anthropic SSE event sequence (`message_start`, `content_block_delta`, `message_delta`, `message_stop`). Share the model-resolution, auth, and error-mapping logic with the OpenAI routes (1.3) rather than duplicating it. Acceptance: an Anthropic-SDK client (or Claude Code configured with a custom base URL + the local token) can list nothing/needs no models list and can stream a `/v1/messages` completion from a local model.

---

#### 1.5 - Desktop surface: enable, copy endpoint + token

**Objective**: Give the user a one-click way to turn the gateway on and copy the connection details.

**Prompt**:
> Add a "Local API server" section to the desktop Settings (near the existing MCP / model settings). Show a toggle bound to `nexus.serving.enabled`, the resolved base URL (`http://<host>:<port>/v1`), the OpenAI and Anthropic endpoint paths, and a copy-to-clipboard for the base URL and the local token (mask the token by default with a reveal control). Show the server's running/stopped state. Include a short inline note that this serves local models to other tools on this machine only. Do not expose the token in logs. Acceptance: a non-technical user can toggle the server on, copy the base URL + token, and paste them into another tool's config.

---

#### 1.6 - Testing and Stabilization

**Objective**: Prove the routes, auth, loopback guard, and lifecycle; cover them in CI.

**Prompt**:
> Add tests for the serving gateway: unit tests for the loopback guard (non-loopback host refused), token auth (missing/wrong -> 401, correct -> pass), and error mapping; integration tests that start the gateway against a fake LLM client and assert `/v1/models`, `/v1/chat/completions` (buffered + streamed SSE), and `/v1/messages` (buffered + streamed) return the correct shapes; a lifecycle test that no port is bound when `nexus.serving.enabled` is false and the listener stops on toggle-off. Use the repo's sidecar test setup and fakes. Run `npm run test:shell` and the sidecar tests; iterate to green. Then create/update CI to cover the sidecar serving changes and optimize it (path filters on the sidecar/serving surface, concurrency cancel-in-progress, dependency caching); keep it platform-agnostic with GitHub Actions as the primary example. Do not proceed to Phase 2 until stable. After tests pass, run `/generate-session-history` to document Phase 1.

---

### Phase 1 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (unit + integration + sidecar) -- desktop 87 files / 735 passed, root 424 files / 4646 passed; 154 new serving tests + 39 new root LLM tests; serving coverage 98.01% lines / 90.66% branches
- [x] No known regressions (MCP server, localAdapters client, existing chat unaffected) -- the 12 pre-existing `LocalAdapterRegistry` tests pass unchanged after the loopback extraction; `deps:check` 0 errors; `build:sidecar` clean
- [x] Session history generated for this phase -- [../development/history/2026-08-12_phase-1-local-serving-gateway.md](../development/history/2026-08-12_phase-1-local-serving-gateway.md)
- [x] Ready to advance to Phase 2

---

## Phase 2: Per-Model Performance Analytics (A2)

**Goal**: Nexus captures and surfaces per-model inference metrics (tokens/sec, time-to-first-token, memory footprint, and basic cache stats) in the Traces panel.
**Prerequisites**: None strictly; pairs naturally with Phase 1 (the gateway is an additional metric source).
**Stability Gate**: After a chat or a gateway request completes, the Traces panel shows that request's model, tokens/sec, TTFT, total tokens, and (where available) memory footprint; metrics accumulate per model across a session; no metric capture makes an outbound call.
**Recommended model**: Mid tier, medium effort - claude-sonnet-5 (medium). Rationale: local timing/telemetry capture plus a Traces-panel extension; moderate cross-cutting scope, low novelty. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 2.1 - Capture per-request inference metrics

**Objective**: Record timing and token metrics at the local-LLM-client boundary.

**Prompt**:
> Extend Nexus's observability layer (`core/observability/`) to capture per-request inference metrics at the LLM-client boundary (`modules/coding/llm/` clients and the Phase 1 serving gateway): model id, prompt/completion token counts, time-to-first-token, total wall time, derived tokens/sec, and memory footprint where the backend reports it (Ollama's `/api/ps` or response timing fields). Emit these through the existing trace/telemetry bus rather than a new sink, and apply `redactSecrets` to anything captured. Ensure capture is purely local (no OTLP export unless the existing `nexus.otlp.enabled` opt-in is on). Acceptance: each completed request produces a metrics record keyed by model; tokens/sec and TTFT are computed correctly against a fake timed client in a test.

---

#### 2.2 - Surface per-model analytics in the Traces panel

**Objective**: Show the captured metrics in the desktop Traces view.

**Prompt**:
> Extend the desktop Traces panel (the `nexus.coding.traceDashboard` webview) to display per-model performance analytics from the Phase 2.1 metrics: a per-model summary (average tokens/sec, median TTFT, request count, total tokens, last memory footprint) plus the per-request detail already shown. Keep the panel readable when metrics are absent (empty state). Do not add any new outbound dependency. Acceptance: after running a few requests across two models, the panel shows a correct per-model breakdown; with no data it shows a clean empty state.

---

#### 2.3 - Testing and Stabilization

**Objective**: Cover metric capture and rendering; wire CI.

**Prompt**:
> Add tests: unit tests for the metric derivations (tokens/sec, TTFT, aggregation per model) against a fake timed LLM client; a frontend test that the Traces panel renders the per-model summary from injected metrics and shows the empty state with none. Run `npm run test:shell` + lint; iterate to green. Update CI to cover the observability + Traces changes (path filters, caching, cancel-in-progress). Do not proceed to Phase 3 until stable. After tests pass, run `/generate-session-history` for Phase 2.

---

### Phase 2 Exit Checklist

- [x] All sub-tasks completed -- plus gap LSO.P1.A closed (real `usage` counts in the gateway) and a Phase 1 typecheck regression fixed
- [x] All tests passing (observability + Traces) -- root 429 files / 4754 passed, desktop 89 files / 764 passed; 91 new tests; new modules at 100% line coverage
- [x] No known regressions (existing trace dashboard behavior intact) -- the analytics section is additive behind an optional prop; all pre-existing LLM-client and panel tests pass unchanged; `deps:check` 0 errors
- [x] Session history generated for this phase -- [../development/history/2026-08-12_phase-2-per-model-analytics.md](../development/history/2026-08-12_phase-2-per-model-analytics.md)
- [x] Ready to advance to Phase 3

---

## Phase 3: Local Document-OCR Capability (A5)

**Goal**: Nexus gains an optional, GPU-tier-gated document-OCR / PDF-parsing capability: an open OCR/VLM model in the catalog, a Python sidecar runtime that runs it, and a "parse document -> text/markdown" action, wired the same way SANA/Whisper/Kokoro are.
**Prerequisites**: v1.15 Phase 4 (registry) landed.
**Stability Gate**: The OCR model appears in `core/registry/catalog.json` as an optional, GPU-tier-gated entry (never auto-installed); on a capable GPU it installs through the existing model-install path; a "parse document" action converts an image or a multi-page PDF (page-to-image) into text/markdown via the sidecar; on an incapable/incompatible host the capability is cleanly unavailable with an explained state, not a crash; the model's remote code is pinned to a specific revision and runs only inside the sandboxed Python runtime.
**Recommended model**: Strong reasoning tier, high effort - claude-opus-4-8 (high). Rationale: supply-chain discipline (`trust_remote_code` pinned + sandboxed), cross-platform gating (CUDA-first vs OS parity), and a net-new Python runtime + registry + action path. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 3.1 - Choose and catalog the OCR model (portability decision)

**Objective**: Add the OCR model to the catalog as an optional, GPU-tier-gated entry, choosing the model with the portability trade-off recorded.

**Prompt**:
> Evaluate and select the OCR model for Nexus. The seeding comparison names Baidu Unlimited-OCR (3B, MIT, CUDA-first, requires `trust_remote_code`), which fits capable NVIDIA hosts; because Nexus has an OS-parity principle, also assess a more portable open OCR option for non-CUDA / Apple-Silicon / CPU hosts (document the candidates and the choice; it is acceptable to ship Unlimited-OCR for the CUDA tier and note a portable fallback as a follow-up gap). Add the chosen model(s) to `core/registry/catalog.json` mirroring the existing SANA/whisper/kokoro entries, with: `type` reflecting an OCR/vision-parse capability, accurate size + license (MIT) + `releaseDate`, a GPU-tier gate so it is offered only on capable tiers, and NOT included in any default/auto-install set (optional, user-pulled only). If the model is gated on HF, wire the existing `requiresLicense`/`licenseUrl` fields; Unlimited-OCR is ungated. Pin the model to a specific HuggingFace revision (commit hash), never a floating `main`. Acceptance: the OCR model shows as an optional, tier-gated Available entry; it is never in a default install set; the catalog entry pins a concrete revision.

---

#### 3.2 - OCR Python runtime (pinned, sandboxed)

**Objective**: Add a Python runtime that loads and runs the OCR model, following the existing runtimes pattern.

**Prompt**:
> Add an OCR runtime under `runtimes/` mirroring the existing diffusion/ASR runtimes (`runtimes/diffusion/`, the whisper/kokoro runtimes): its own `requirements.txt`, a runner module, and a clear input/output contract. It must accept an image or a PDF (convert PDF pages to images at a sensible DPI, matching the model's expected pipeline, e.g. 300 DPI + the model's `infer_multi` for multi-page) and return parsed text/markdown with page boundaries. Load the model from the pinned revision recorded in the catalog (3.1) and run any `trust_remote_code` inside this sandboxed runtime only, never in the Node sidecar. Enforce a bounded generation window (the model's documented `max_length`) and stream progress where possible. Handle the incompatible-host case (no capable GPU / unsupported platform) by returning a clean "unavailable on this host" result, not a hard crash. Acceptance: given a sample image and a sample multi-page PDF, the runtime returns correct parsed text; on an unsupported host it reports unavailability cleanly.

---

#### 3.3 - Sidecar handler + parse-document action

**Objective**: Expose the OCR runtime through the sidecar and a desktop action.

**Prompt**:
> Add a sidecar handler (in `desktop/sidecar/src/handlers.ts`, with its schema in `desktop/sidecar/src/protocol.ts`) that invokes the Phase 3.2 OCR runtime: accept a base64 image or PDF (reuse the base64 attachment convention the studios already use), return parsed text/markdown + per-page structure, and surface progress events through the existing event/drain mechanism. Add a desktop "Parse document" affordance (a place a user can drop a PDF/image and get text back); if it fits the v1.15 studio-chat pattern, expose it as a chat action, otherwise a dedicated lightweight view. Gate the affordance on the OCR model being installed (empty-state -> "Get more models" deep-link to Settings > Models, reusing the v1.15 Phase 4 deep-link). Acceptance: dropping a PDF in the app returns its text; with no OCR model installed, the UI shows the install prompt rather than an error.

---

#### 3.4 - Testing and Stabilization

**Objective**: Cover the catalog entry, runtime contract, sidecar handler, and host-gating; wire CI.

**Prompt**:
> Add tests: a catalog test asserting the OCR entry is optional, tier-gated, pins a concrete revision, and is absent from any default set; Python runtime tests (with a stubbed/fake model or a tiny fixture) for the image path, the multi-page PDF path, and the unsupported-host path; a sidecar handler test (fake runtime) asserting the base64 in / text-out contract and progress events; a frontend test for the install-gated empty state. Keep model weights out of CI (fixture/stub only). Run `npm run test:shell`, the sidecar tests, and the Python runtime tests; iterate to green. Update CI to cover the OCR runtime + handler (path filters, pip + npm caching, cancel-in-progress; keep GPU/model-download work out of CI). Do not proceed to Phase 4 until stable. After tests pass, run `/generate-session-history` for Phase 3.

---

### Phase 3 Exit Checklist

- [x] All sub-tasks completed -- plus the full installer integration and the first-ever `tests/python` CI job
- [x] All tests passing (catalog + runtime + sidecar + frontend) -- root 429 files / 4754, desktop 93 files / 808, Python runtimes 196, installer suite clean (2 pre-existing `zstandard` failures are a missing local dep)
- [x] No known regressions (existing SANA/whisper/kokoro runtimes + Models page intact) -- the diffusion runtime's 135 tests pass unchanged and now run in CI for the first time; `deps:check` 0 errors
- [x] OCR model is optional, tier-gated, revision-pinned, and never auto-installed -- **two** models ship (portable Apache-2.0 CPU + CUDA MIT VLM), neither in `recommended.json`, both pinned to a commit sha, with the pin ENFORCED for the `trust_remote_code` entry by both the TS and Python validators
- [x] Session history generated for this phase -- [../development/history/2026-08-13_phase-3-document-ocr.md](../development/history/2026-08-13_phase-3-document-ocr.md)
- [x] Ready to advance to Phase 4

---

## Phase 4: Document-Parse Agent Tool + Memory Ingestion (A6)

**Goal**: The OCR capability becomes usable by the coding agent (parse a PDF/screenshot into context) and, optionally, feeds parsed text into the memory subsystem, with parsed content treated as untrusted.
**Prerequisites**: Phase 3 (OCR capability).
**Stability Gate**: The agent can call a "parse document" tool that returns parsed text for a given local file; parsed content is routed through the inbound content classifier (as `fetch_page` output is) and `redactSecrets` before it enters context or memory; optional memory ingestion is opt-in; the tool respects the existing permission tier and path-guard rules.
**Recommended model**: Strong reasoning tier, medium-high effort - claude-opus-4-8 (medium). Rationale: a security-sensitive untrusted-content path (parsed documents entering agent context and memory); the reuse of Phase 3 lowers novelty but the trust boundary raises the bar. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 4.1 - Document-parse agent tool

**Objective**: Expose OCR as a governed agent tool.

**Prompt**:
> Add a `parse_document` agent tool that wraps the Phase 3 OCR path: input is a workspace-relative file path (image or PDF); output is parsed text/markdown. Register it with the correct permission tier (it reads a file and runs a model - align it with the existing read/confirmation tiers) and route the path through the existing `pathGuard` (symlink-aware, workspace-scoped) so it cannot read outside the workspace or secret paths without the explicit `allow_secrets` override. Treat the returned text as untrusted external content: pass it through the inbound content classifier (`nexus.coding.inboundClassifier`, the same gate `fetch_page`/`web_search` use) so injection markers are annotated before the text reaches the agent's context, and apply `redactSecrets`. Acceptance: the agent can parse a PDF in the workspace into context; a path-traversal or secret-path attempt is blocked; parsed content carries the untrusted-content annotation when the classifier flags it.

---

#### 4.2 - Optional memory ingestion

**Objective**: Let parsed documents optionally enter the memory subsystem, opt-in and redacted.

**Prompt**:
> Add an opt-in path to ingest parsed-document text into the memory subsystem (`core/memory/`), gated behind a setting (default off, mirroring the conservative defaults of `nexus.memory.consolidation.enabled` / `compression.enabled`). When on, store parsed text as observations with provenance (source file + that it came from OCR), after `redactSecrets` and the inbound-classifier pass from 4.1. Never ingest automatically without the opt-in. Acceptance: with the setting on, parsing a document adds redacted, provenance-tagged memory entries; with it off, no memory write occurs; secrets in the document are redacted before storage.

---

#### 4.3 - Testing and Stabilization

**Objective**: Cover the tool, its guards, and memory ingestion; wire CI.

**Prompt**:
> Add tests: a tool test asserting `parse_document` returns text for a fixture file, is blocked on path traversal / secret paths, and annotates classifier-flagged content; a test that parsed content is redacted; memory-ingestion tests for both the opt-in-on (redacted, provenance-tagged write) and opt-in-off (no write) paths. Use fakes for the OCR runtime and the classifier. Run `npm run test:shell` + the relevant suites; iterate to green. Update CI for the tool + memory changes (path filters, caching, cancel-in-progress). Do not proceed to Phase 5 until stable. After tests pass, run `/generate-session-history` for Phase 4.

---

### Phase 4 Exit Checklist

- [x] All sub-tasks completed -- on BOTH tool surfaces, which required building tier enforcement, secret-path screening, and classifier routing into the headless surface from scratch
- [x] All tests passing (tool guards + memory ingestion) -- 57 new tests; root 433 files / 4811, desktop 93 / 808, Python 196; new modules at 99-100% line coverage
- [x] No known regressions (existing tools, path guard, memory writes intact) -- `deps:check` 0 errors, `security:check` in sync, prompt-budget compliance passing after making `parse_document` trimmable
- [x] Parsed content is classifier-screened and redacted before context/memory -- `parse_document` joins `INBOUND_EXTERNAL_DATA_TOOLS` (VS Code) and `HEADLESS_INBOUND_TOOLS` (headless); `redactSecrets` runs before the model sees the text AND again at the memory-store boundary
- [x] Session history generated for this phase -- [../development/history/2026-08-14_phase-4-document-parse-tool.md](../development/history/2026-08-14_phase-4-document-parse-tool.md)
- [x] Ready to advance to Phase 5 -- with LSO.P4.B noted: the tool is tested but not yet wired into a composition root

---

## Phase 5: MLX-via-Adapters Docs + Model-Library UX (A3 + A4)

**Goal**: Document and smoke-test the already-supported path for running an MLX model server through `nexus.llm.localAdapters` on Apple Silicon, and polish the model-library discovery / quick-switch UX on the Models page.
**Prerequisites**: v1.15 Phase 4 (Models page + registry) landed; Phase 1 helps (the gateway and adapters share the loopback story) but is not strictly required.
**Stability Gate**: A docs page shows how to register an mlx-vlm (or LM Studio MLX / nativ) loopback server as a `nexus.llm.localAdapters` manifest and select it, with a macOS Apple-Silicon smoke check recorded; the Models page has improved discovery (search/filter) and a quick model-switch affordance; no new runtime is added.
**Recommended model**: Mid tier, medium effort - claude-sonnet-5 (medium). Rationale: documentation + a smoke note + UI polish over existing subsystems; low novelty, no new runtime. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 5.1 - Document MLX via localAdapters (+ macOS smoke)

**Objective**: Make the existing loopback-adapter path to MLX discoverable and verified, without building a runtime.

**Prompt**:
> Write a documentation page (under `docs/archive/v1/v1.16/` and linked from the user docs) showing how to run an MLX model on Apple Silicon and consume it from Nexus via the existing `nexus.llm.localAdapters` mechanism (`modules/coding/llm/LocalAdapterRegistry.ts`): register a loopback manifest with `protocol: "openai"` pointing at an mlx-vlm server (or LM Studio's MLX mode, or nativ) at a `127.0.0.1` endpoint, then set `nexus.llm.backend` to that manifest's name. Explain that this needs no new Nexus runtime (a bundled MLX runtime was deliberately rejected; see the v1.12 comparison and the v1.16 comparison N3), and that the loopback guard enforces local-only. Record a macOS Apple-Silicon smoke check (register an OpenAI-compatible MLX loopback server, confirm Nexus lists and chats with it) as an on-device QA note, consistent with the project's existing macOS install-checklist pattern. Acceptance: a macOS Apple-Silicon user can follow the page to drive an MLX model through Nexus; the smoke note is recorded.

---

#### 5.2 - Model-library discovery + quick-switch UX

**Objective**: Polish the Models page and add a fast model switch.

**Prompt**:
> Extend the v1.15 Phase 4 Settings > Models page with discovery affordances inspired by nativ's model library: a search/filter over the catalog (by name, type/capability, tier-fit, installed/available/external), clearer install/remove states, and disk-usage display (reusing `models.diskUsage`). Add a quick model-switch affordance reachable from the chat/studio surfaces (a compact switcher listing installed-and-ready models for the active task type, reusing the v1.15 "installed and ready" filter), so switching the active model does not require opening full Settings. Do not duplicate the v1.15 registry logic; build on it. Acceptance: a user can search/filter models and see accurate states; the quick switcher changes the active model from chat without a full Settings round-trip.

---

#### 5.3 - Testing and Stabilization

**Objective**: Cover the UX additions and validate the docs' technical claims; wire CI.

**Prompt**:
> Add frontend tests for the Models-page search/filter (correct results across installed/available/external + tier-fit) and the quick model-switcher (lists only installed-and-ready models for the task type, switches the active model). Add or extend a test/lint that validates the localAdapters manifest example in the docs parses against the `nexus.llm.localAdapters` schema (so the documented example cannot drift from the schema). Run `npm run test:shell` + lint; iterate to green. Update CI for the settings/docs changes (path filters, caching, cancel-in-progress). Do not proceed to Phase 6 until stable. After tests pass, run `/generate-session-history` for Phase 5.

---

### Phase 5 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (Models page + switcher + docs-example schema check)
- [x] No known regressions (v1.15 Models page behavior intact)
- [x] macOS Apple-Silicon MLX smoke note recorded
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 6 -- with LSO.P5.A noted: the smoke checklist exists but has not been run on Apple Silicon hardware

---

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| (none - no constitution file; no violations) | | |

---

## Phase 6: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, and its CI/CD complete and optimized.
**Prerequisites**: All prior phases.
**Stability Gate**: The layout is clean (no deprecated/obsolete files, empty dirs, redundant files/dirs, or overcomplicated structure left un-triaged); the version's known gaps are reconciled; CI/CD covers every change and is optimized; project validation/tests pass.
**Recommended model**: Strong reasoning tier, high effort - claude-opus-4-8 (high). Repo-wide refactor + reference repair is high-risk - default to the strong reasoning tier.

### Sub-tasks

#### 6.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure introduced or exposed across Phases 1-5 (e.g. any transitional serving-gateway or OCR scaffolding, dead settings, duplicated route/error helpers), then refactor toward a clean, intuitive layout via `[[project-refactor]]` and `[[docs-layout-refactor]]` (propose-then-apply, with confirmation; repair every reference for anything that moves). Confirm the `docs/archive/v1/v1.16/` tree is complete (this plan, the seeding comparison, phase session-histories, known-gaps).

#### 6.2 - Known-gaps reconciliation
**Objective**: Reconcile the version's open gaps.
**Prompt**:
> Reconcile this version's known gaps via `[[known-gaps-tracker]]`: create `docs/archive/v1/v1.16/known-gaps.md`, resolve/defer/transfer each still-open item carried from `docs/archive/v1/v1.15/known-gaps.md`, and record any newly discovered v1.16 gaps (e.g. a portable non-CUDA OCR fallback if only the CUDA model shipped in Phase 3; the on-device macOS MLX smoke if it stayed a note; serving-gateway hardening follow-ups; any deferred cross-platform parity). Finalize the per-minor known-gaps file for v1.16.

#### 6.3 - CI/CD create/update/optimize
**Objective**: CI/CD covers all changes and is optimized.
**Prompt**:
> Create or update the CI/CD pipeline so it covers every change in this plan (the sidecar serving gateway, the observability/Traces additions, the OCR Python runtime + sidecar handler, the agent tool + memory ingestion, and the Models-page/docs changes), then optimize it to reduce action minutes (path filters per surface, concurrency cancel-in-progress, dependency caching for npm + pip, gating expensive-OS or matrix jobs to merges/schedule; keep GPU/model-download work out of CI) while keeping comprehensive testing. Keep it platform-agnostic; GitHub Actions is the primary example. Note the standing GitHub Actions budget freeze if it still applies at implementation time.

#### 6.4 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and CI/CD is green.
**Prompt**:
> Run the full validation/test suite (`npm run test:shell`, the sidecar tests, and the Python runtime tests), confirm the refactor changed no behavior, confirm CI/CD passes and the action-minute reduction is real, and iterate until clean. Update `docs/todos.md` (dashboard metrics + checked-off items) via `[[dev-progress-tracker]]`. Generate a session-history entry for this phase.

---

### Phase 6 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (desktop + sidecar + Python runtimes)
- [x] No known regressions from prior phases
- [x] Known gaps reconciled (v1.15 carry-forward + new v1.16 items)
- [x] CI/CD covers all changes and is optimized
- [x] Session history generated for this phase
- [x] Ready to release v1.16.0 (hand off to `/update release`)
