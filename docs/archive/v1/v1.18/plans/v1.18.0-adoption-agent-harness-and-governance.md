# Plan - Agent Harness Activation, Autonomy Governance, and Sandbox

**Project**: Nexus AI Studio
**Version**: v1.18.0 (cut 2026-08-17)
**Slug**: agent-harness-and-governance
**Filename**: v1.18.0-adoption-agent-harness-and-governance.md
**Plan Type**: Feature / Enhancement (adoption)
**Generated**: 2026-08-05
**Sources**:
- [../comparisons/v1.18.0-comparison-openworker.md](../comparisons/v1.18.0-comparison-openworker.md) (OpenWorker; items prefixed OW-)
- [../comparisons/v1.18.1-comparison-openinterpreter.md](../comparisons/v1.18.1-comparison-openinterpreter.md) (Open Interpreter; items prefixed OI-)
- [../comparisons/v1.18.2-comparison-laguna-s-2-1.md](../comparisons/v1.18.2-comparison-laguna-s-2-1.md) (Laguna-S-2.1; items prefixed LG-)
**Goal**: Turn the dormant per-model harness selector on and feed it named profiles (OI-A5/OI-A2), make the model catalog and MCP registry govern what agents may actually use (OW-A4, LG-A2, LG-A3, OW-A5), make unattended and scheduled agent runs safe via a persistent ask inbox and a local scheduler that always re-enter the permission tiers (OW-A1, OW-A2), expose the coding engine over a loopback-only ACP surface coordinated with the v1.16 serving gateway (OI-A3), and close the top-ranked EM.P5.A gap with a per-OS process sandbox around `run_terminal` (OI-A1) - all reverse-engineered internally, with zero new outbound surfaces and every rejection of the sources' cloud/connector posture carried forward explicitly.

## Overview

This milestone operationalizes the merged adoption plans of the three v1.18 comparisons: OpenWorker (a local-first Tauri sibling whose adoptable delta is autonomy UX and registry governance, not its 25+ SaaS connectors), Open Interpreter (a mature reference implementation of Nexus's own v1.12 harness-per-model thesis, plus the OS-level sandbox Nexus documents as its single most important open gap), and Laguna-S-2.1 (a 118B/8B-active MoE model whose adoptable delta is catalog *preparation* - quant taxonomy, MoE schema, a llama.cpp loopback recipe - while the model entry itself stays behind the EM.P3/EM.P4 gates).

The cycle theme is agent harness and governance: everything in this plan either activates the harness machinery Nexus already built (`modules/coding/orchestration/HarnessSelector.ts`, shipped opt-in and off per EM.P1.A/B, `docs/archive/v1/v1.12/known-gaps.md`), tightens what agents are allowed to use (catalog flags, quant gates, MoE schema, per-tool MCP control on top of `modules/coding/mcp/HubRegistryPolicyFilter.ts`), makes autonomy safe (ask inbox + scheduler that route every wake through `modules/coding/guardrails/PermissionTiers.ts` and `src/tools/ConfirmationGate.ts`), or confines what agents execute (the per-OS sandbox around `run_terminal`, EM.P5.A, `docs/archive/v1/v1.12/exec-sandbox-audit.md`).

Ordering follows the MCP Registry Policy's reverse-engineer-first discipline: skill-native items ship first (Phase 1: zero-code confirmations plus the documented llama.cpp adapter recipe), then the re-full builds by P-tier and dependency order (Phase 2: OI-A5 P0 harness wiring then OI-A2 P1 profiles; Phase 3: the P1 catalog/registry items OW-A4 + LG-A2 + OW-A5 plus the P2 LG-A3 schema they share files with; Phase 4: OW-A1 P1 ask inbox then OW-A2 P2 scheduler, which depends on it; Phase 5: OI-A3 P2 ACP), then the one in-cycle re-partial build (Phase 6: OI-A1 OS sandbox, started as its own workstream because it is high-effort and independent). Gated items (LG-A1 catalog entry, LG-A4 1M-context budget, OI-A4-native app driver) are tracked, not built - see "Deferred and gated items". Every rejection from the three Section 9s is carried forward under "Out of scope / rejected".

Dedup and overlap notes (merged across the three comparisons):
- **OW-A4 + LG-A3 share one schema change.** The tool-calling-verified flag and the MoE `activeParams`/`totalParams` fields both extend `core/registry/catalog.json`; they are implemented together in Phase 3 as a single backward-compatible schema revision rather than two.
- **OI-A3 overlaps the queued v1.16.0 serving-gateway candidate.** Both add an external control surface over the coding engine (`docs/archive/v1/v1.16/comparisons/v1.16.0-comparison-local-serving-and-ocr.md`). They are complementary (ACP is editor-driving; the gateway is model-serving) but MUST share one loopback transport/auth layer - Phase 5 builds that shared layer; do not build two.
- **Two items are already skill-native in Nexus-Hub and need no code.** OpenWorker's "morning brief" content maps onto the existing `agent-presets` morning-briefing preset (only OW-A2's scheduling mechanism is a build), and Open Interpreter's browser-QA half of OI-A4 maps onto the existing `browser-testing-with-devtools` skill. Phase 1 confirms and documents both instead of rebuilding them.
- **No literal duplicates exist across the three sources.** The A-numbers collide (each comparison numbers its own A1..A5), so this plan disambiguates with OW-/OI-/LG- prefixes; e.g. OW-A1 (ask inbox), OI-A1 (OS sandbox), and LG-A1 (gated catalog entry) are three different items.

Success looks like: the harness selector is live behind `settings.harnessSelectorEnabled` with named per-family profiles and a `/harness`-style inspect/switch; the catalog distinguishes tool-calling-verified models and models MoE resident-vs-active cost; the MCP registry offers per-tool allow/deny that only ever tightens the default-deny; a headless or scheduled agent run parks its approvals in a persistent inbox instead of dying on the 60s `ConfirmationGate` timeout; external editors can drive the engine over loopback ACP with local auth; and `run_terminal` executes inside OS-level confinement on all three OSes.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at `docs/archive/v1/v1.18/constitution.md` (nor elsewhere under `docs/`) - skipping check. Recommend running `/constitution` to establish project principles. This is informational, not blocking.

Project-level guardrails that nonetheless apply to this plan (from `AGENTS.md` / `CLAUDE.md` / `README.md`): local-first / zero-outbound (every item is a local build; the ACP surface binds to loopback with local auth; the llama.cpp adapter stays user-registered and loopback-only per `modules/coding/llm/LocalAdapterRegistry.ts`); the MCP Registry Policy reverse-engineer-first decision tree (no OpenWorker or Open Interpreter code is vendored; harness profiles use generic descriptive names with external attribution stripped; the "hard no" on as-a-service integrations drives the rejections below); safety never weakens (the scheduler and ask inbox re-enter `PermissionTiers`/`ConfirmationGate` on every wake and never auto-approve; per-tool MCP control tightens, never loosens, the default-deny; the sandbox is a strict net risk reduction); single-GPU ceiling (Design Principle 3: no Laguna default pick, gates stay closed); OS parity (the sandbox needs all three OS backends before it ships as default); every changed line traces to an adoption item (OW-A1/A2/A4/A5, OI-A1/A2/A3/A5, LG-A2/A3/A5); per-phase testing and CI updates; ASCII-only commit messages.

## Phases at a Glance

| Phase | Title | Outcome | Rec. model / effort |
|-------|-------|---------|---------------------|
| 1 | Skill-native adoptions + llama.cpp recipe (OW-B1, OI-A4-web, LG-A5) | Zero-code confirmations documented; a first-class llama.cpp loopback adapter recipe | Mid tier, low - claude-sonnet-5 (medium): docs + manifest work, no runtime change |
| 2 | Live harness activation (OI-A5 flagship, OI-A2) | Harness overlay live in the prompt path behind its setting; named per-family profiles; `/harness` inspect/switch; closes EM.P1.A, fleshes EM.P1.C | Mid-strong tier, high - claude-sonnet-5 (high): wiring an existing mechanism plus profile design |
| 3 | Catalog + registry governance (OW-A4, LG-A2, LG-A3, OW-A5) | One backward-compatible catalog schema revision (verified flag + MoE fields), extended quant taxonomy, per-tool MCP allow/deny | Mid tier, medium - claude-sonnet-5 (medium): additive data/schema + registry UI |
| 4 | Unattended autonomy: ask inbox + scheduler (OW-A1, OW-A2) | Persistent approval queue + desktop inbox panel; local cron-style scheduler that parks asks and re-enters the permission tiers | Mid-strong tier, high - claude-sonnet-5 (high): safety-critical queue/replay semantics |
| 5 | ACP agent surface (OI-A3) | Loopback + local-auth ACP endpoint over the sidecar, on a transport/auth layer shared with the v1.16 gateway | Strong tier, high - claude-opus-4-8 (high): new local control surface, auth design |
| 6 | OS process sandbox (OI-A1, re-partial, closes EM.P5.A) | Per-OS confinement (Seatbelt / Landlock+seccomp / job object) wrapping `run_terminal`, fail-closed with an explicit degraded mode | Strong tier, high - claude-opus-4-8 (high): three OS backends, security-critical |
| 7 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Clean layout, v1.17/v1.18 gaps reconciled (incl. the gated LG/OI items), CI green and optimized | Strong tier, high - claude-opus-4-8 (high): repo-wide refactor + reference repair |

---

## Phase 1: Skill-Native Adoptions + llama.cpp Recipe (OW-B1, OI-A4-web, LG-A5)

**Goal**: Ship every zero-code win first, per the reverse-engineer-first ordering: confirm the two capabilities already covered by Nexus-Hub skills (morning-brief content, browser GUI QA) and promote the llama.cpp loopback adapter to a documented, first-class recipe.
**Prerequisites**: None. Ships first because it is skill-native and unblocks nothing downstream except documentation references.
**Stability Gate**: A written mapping exists showing OpenWorker's morning-brief content is covered by the `agent-presets` morning-briefing preset and Open Interpreter's browser-QA is covered by `browser-testing-with-devtools` (no new code, no duplicate skills); a llama.cpp loopback adapter recipe (manifest example + guide) exists under `docs/reference/` and validates against `modules/coding/llm/LocalAdapterRegistry.ts` constraints (loopback-only, user-registered runtime, no bundling); lint/docs checks pass.
**Recommended model**: Mid tier, low effort - claude-sonnet-5 (medium). Rationale: documentation and manifest work over existing mechanisms; no runtime change. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 1.1 - Confirm the two skill-native coverages

**Objective**: Prevent rebuilding what the skill catalog already provides (dedup: OW Bucket 1 and OI Bucket 1).

**Prompt**:
> Document the two skill-native adoptions in a short reference note (e.g. `docs/reference/skill-native-adoptions-v1.18.md`): (1) OpenWorker's "morning brief" recurring-work *content* is covered by the Nexus-Hub `agent-presets` morning-briefing preset - only the scheduling *mechanism* is a Nexus build (Phase 4, OW-A2); (2) Open Interpreter's browser-half GUI QA (OI-A4-web) is covered by the Nexus-Hub `browser-testing-with-devtools` skill as the QA driver - the native-app half (OI-A4-native) is explicitly deferred (see "Deferred and gated items"). For each, cite the source comparison section and state that no new skill, MCP, or code is created. Acceptance: the note exists, both coverages are verifiable against the skill catalog, and no duplicate capability is scaffolded.

---

#### 1.2 - llama.cpp loopback adapter recipe (LG-A5)

**Objective**: Make the "run a large-MoE GGUF in llama.cpp" path a supported, local, no-outbound configuration.

**Prompt**:
> Promote the user-registered llama.cpp adapter referenced by EM.P4.A into a documented, first-class loopback adapter recipe: an example adapter manifest (protocol `openai`, loopback host only, matching the manifest shape `modules/coding/llm/LocalAdapterRegistry.ts` consumes and its hard non-loopback reject) plus a guide at `docs/reference/llamacpp-loopback-adapter.md` covering llama-server startup flags for large-MoE offload, where the manifest lives, and how registration stays user-side (the runtime is NEVER bundled or auto-installed). Cross-reference the gated patient tier (`core/registry/patientTier.ts`) and note that this recipe does not open the EM.P4 gate. Acceptance: the guide + manifest example exist; the example validates against the registry's loopback constraint; no runtime, binary, or auto-download is added.

---

#### 1.3 - Testing and Stabilization

**Objective**: Validate the docs and manifest; wire CI.

**Prompt**:
> Add validation: a unit test that the example llama.cpp adapter manifest parses and passes `LocalAdapterRegistry` validation (loopback accepted; a non-loopback mutation of it rejected), and a docs-consistency check that the two new reference docs' internal links resolve. Run `npm test` + `npm run lint`; iterate to green. Update CI path filters to cover `docs/reference/` link checks if a docs job exists. Do not proceed to Phase 2 until stable. After tests pass, run `/generate-session-history` for Phase 1.

---

### Phase 1 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (manifest validation + docs links)
- [x] No new runtime, dependency, or outbound surface added
- [x] Skill-native coverages documented; no duplicate capability built
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 2

---

## Phase 2: Live Harness Activation (OI-A5 flagship, OI-A2)

**Goal**: Turn the built-but-dormant per-model harness selector on (closes EM.P1.A) and feed it named per-family profiles reverse-engineered from Open Interpreter's harness catalog (fleshes out EM.P1.C), with a `/harness`-style inspect/switch surface.
**Prerequisites**: Phase 1 complete (ordering only; no hard dependency).
**Stability Gate**: `overlayForModel()` output is spread into the live `PromptContext` at `src/panels/ToolActivationContext.ts` (`buildPromptContext`), gated on `settings.harnessSelectorEnabled` (`modules/coding/config/settings.ts`) with the safe fallback intact; named per-family `HarnessProfile` data (generic names, no external attribution) exists for at least the weak/mid/strong tiers and the major local model families in `core/registry/catalog.json`; a `/harness` inspect/switch surface shows the active profile and allows a session override; the `HarnessSelectorAb.ts` golden A/B still passes and covers the live path; setting off means byte-identical prompts to today.
**Recommended model**: Mid-strong tier, high effort - claude-sonnet-5 (high). Rationale: the mechanism exists and is tested; the risk is regression in the live prompt path plus judgment in profile design. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 2.1 - Wire the harness overlay into the live prompt path (OI-A5, P0)

**Objective**: The single change that turns the dormant selector on - this IS EM.P1.A (`docs/archive/v1/v1.12/known-gaps.md`).

**Prompt**:
> Spread the `HarnessSelector.overlayForModel()` result into the live `PromptContext` inside `buildPromptContext` at `src/panels/ToolActivationContext.ts`, gated on `settings.harnessSelectorEnabled` (`modules/coding/config/settings.ts`). Preserve the existing safe-fallback semantics in `modules/coding/orchestration/HarnessSelector.ts` (unknown model or missing profile falls back to the neutral behavior, never throws into the prompt path). When the setting is off, the produced prompt context must be byte-identical to today's. Do not change `HARNESS_SELECTOR_SHIPPED_DEFAULT` in this sub-task; flipping the shipped default is a separate decision recorded in the phase session history after the A/B evidence. Acceptance: with the setting on, the overlay demonstrably alters the prompt context per model tier; with it off, no diff; the golden A/B harness (`modules/coding/orchestration/HarnessSelectorAb.ts`, `tests/unit/orchestration/HarnessSelectorAb.test.ts`) passes against the live path.

---

#### 2.2 - Named per-family harness profiles (OI-A2, P1)

**Objective**: Import the deeper scaffold knobs deferred in EM.P1.C as internal profile data.

**Prompt**:
> Reverse-engineer the *behavioral shape* of Open Interpreter's named harnesses (concise-tool-loop for Kimi-class, plan-first for Qwen-class, terse-diff for DeepSeek-class, SWE-agent-style structured edit loop, and a minimal profile) into internal `HarnessProfile` data consumed by `modules/coding/orchestration/HarnessSelector.ts`. Use generic, descriptive Nexus names (e.g. `concise-loop`, `plan-first`, `structured-edit`, `minimal`) and strip ALL external attribution per the MCP Registry Policy and Design Principle 2 - no code is vendored, no OI names appear. Key profiles to model-family/tier via the existing catalog tags in `core/registry/catalog.json`; align guidance with `docs/reference/low-cost-model-optimization.md` (H2) and update that doc to point at the profiles. Acceptance: profiles are data (not code branches), selectable per family/tier, fall back safely for unknown families, and contain no external attribution.

---

#### 2.3 - `/harness` inspect/switch surface

**Objective**: Make the active harness a first-class, inspectable, switchable object.

**Prompt**:
> Add a `/harness`-style surface to the coding session: inspect (show the active profile, why it was selected - model, tier, tags - and the effective overlay) and switch (a session-scoped manual override to any named profile, reverting on model change or new session). Wire it through the existing panel/command infrastructure under `src/panels/` and expose the active harness name near the model indicator in `desktop/src/shared/chat/ModelSelector.tsx` (a small label/badge, no redesign). The override must never bypass `settings.harnessSelectorEnabled` (off means no overlay, period). Acceptance: a user can see which harness is live and why, switch it for the session, and the override respects the master setting.

---

#### 2.4 - Testing and Stabilization

**Objective**: Lock the live wiring, profiles, and switch in tests; wire CI.

**Prompt**:
> Add tests: unit tests that `buildPromptContext` includes the overlay when enabled and is byte-identical when disabled; profile-selection tests (each catalog family/tier yields its expected named profile; unknown families fall back); override tests (session switch applies and reverts; disabled setting wins); extend the `HarnessSelectorAb` golden tests to run against the live path. Run `npm test` + `npm run lint`; for the ModelSelector badge run `npm run test:shell` + `npm run lint:shell`; iterate to green. Update CI to cover `src/panels/` + `modules/coding/orchestration/` changes. Do not proceed to Phase 3 until stable. After tests pass, run `/generate-session-history` for Phase 2 and record the go/no-go evidence for flipping `HARNESS_SELECTOR_SHIPPED_DEFAULT`.

---

### Phase 2 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (wiring + profiles + override + golden A/B)
- [x] Setting off produces byte-identical prompts (no regression)
- [x] Profiles are internal data with external attribution stripped
- [x] EM.P1.A closed; EM.P1.C progress recorded for known-gaps reconciliation
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 3

---

## Phase 3: Catalog + Registry Governance (OW-A4, LG-A2, LG-A3, OW-A5)

**Goal**: One coherent governance pass over what agents may use: a tool-calling-verified catalog flag, MoE active-vs-total schema fields (merged with the flag into a single backward-compatible schema revision), recognition of UD dynamic quant labels in the extreme-low-bit gate, and per-tool MCP allow/deny that only tightens the default-deny.
**Prerequisites**: Phase 2 (the MoE fields feed `HarnessSelector` tier logic, so the live path should exist first).
**Stability Gate**: `core/registry/catalog.json` carries `toolCallingVerified` (+ benchmark provenance) and `activeParams`/`totalParams` in one additive schema revision that leaves every existing entry valid; `desktop/src/shared/chat/ModelSelector.tsx` badges verified models; `HarnessSelector` tiering can reason about MoE resident-vs-total footprint without behavior change for dense models; `core/registry/extremeLowBit.ts` recognizes UD dynamic quant labels (`UD-IQ1_S`, `UD-IQ1_M`, `UD-IQ2_*`, `UD-Q2_K_XL`, `MXFP4_MOE` style) while remaining fail-closed; the per-project MCP registry supports per-tool allow/deny layered on `HubRegistryPolicyFilter` classification, deny-only relative to the default (no per-tool toggle can enable what policy denies); tests cover all four.
**Recommended model**: Mid tier, medium effort - claude-sonnet-5 (medium). Rationale: additive data/schema work plus a bounded registry/settings surface; low novelty, high care for backward compatibility. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 3.1 - Catalog schema revision: verified flag + MoE fields (OW-A4 + LG-A3, merged)

**Objective**: One schema change instead of two (dedup flagged in the Overview).

**Prompt**:
> Extend the `core/registry/catalog.json` schema additively with: `toolCallingVerified: boolean` plus a `toolCallingBenchmark` provenance field (what was run, when, result) per OW-A4; and optional `activeParams` / `totalParams` fields per LG-A3 so MoE models expose modest active compute vs large resident footprint. Every existing entry must remain valid without edits (fields optional, defaults conservative: unverified, dense assumed when MoE fields absent). Populate `toolCallingVerified` only for models Nexus has actually validated for agentic tool use (existing `agentic` tags + benchmark fields are the starting evidence; do not mass-flag). Acceptance: schema validates old and new entries; at least the currently-validated agentic defaults carry the flag with provenance; MoE fields exist and are unused by dense entries.

---

#### 3.2 - Consume the new fields: ModelSelector badge + HarnessSelector/scheduler awareness

**Objective**: Make the schema fields visible and actionable.

**Prompt**:
> Surface `toolCallingVerified` as a badge in `desktop/src/shared/chat/ModelSelector.tsx` (distinguish "verified for tool-calling" from "merely runs"; tooltip cites the benchmark provenance). Teach the tier derivation in `modules/coding/orchestration/HarnessSelector.ts` to prefer `activeParams` for compute-tier reasoning and `totalParams`/`vramGb` for residency when the MoE fields are present, with EXACTLY the current behavior when they are absent (dense models unchanged - lock this with a regression test). Where the GPU scheduler consults model footprint, thread `totalParams` residency through the same conservative path. Acceptance: verified badge renders with provenance; dense-model tiering is bit-identical to before; an MoE test entry tiers by active compute while flagging its resident footprint.

---

#### 3.3 - UD dynamic quant taxonomy in the extreme-low-bit gate (LG-A2)

**Objective**: Let the fail-closed gate reason about Unsloth UD dynamic quantization labels.

**Prompt**:
> Extend the recognized quant-label set in `core/registry/extremeLowBit.ts` (currently `Q1_0/Q2_0/TQ1_0/TQ2_0/I2_S/1bit/ternary` style) to also classify Unsloth UD dynamic quant labels: `UD-IQ1_S`, `UD-IQ1_M`, `UD-IQ2_XXS`, `UD-IQ2_M`, `UD-Q2_K_XL`, and `MXFP4_MOE` style labels, mapping each to the correct sub-4-bit classification. The gate's fail-closed posture is untouched: recognizing a label means the gate can REASON about it (and correctly block it while EM.P3 is closed), not that anything is enabled - the placeholder min-version gate stays as-is. Acceptance: each new label classifies as extreme-low-bit; unknown labels still fail closed; no gate is opened.

---

#### 3.4 - Per-tool MCP allow/deny (OW-A5)

**Objective**: Per-tool granularity on top of the per-project registry and default-deny classification.

**Prompt**:
> Add per-tool allow/deny to the per-project MCP registry, layered ON TOP of the `modules/coding/mcp/HubRegistryPolicyFilter.ts` classification: for each registered MCP server, the user can disable individual tools; a per-tool toggle can only further restrict - it can NEVER enable a tool the policy filter classifies as denied (tightens-only invariant, enforced in code, not UI). Persist per-project; surface the toggles in the existing MCP settings area of the desktop shell. Acceptance: a disabled tool is absent from the agent's tool registry for that project; a policy-denied tool cannot be enabled by any toggle combination; the invariant has a dedicated test.

---

#### 3.5 - Testing and Stabilization

**Objective**: Cover schema compatibility, gate labels, and the tightens-only invariant; wire CI.

**Prompt**:
> Add tests: schema validation over the full existing `catalog.json` (zero entries broken) plus new-field round-trips; dense-model tiering regression (before/after identical); MoE tiering with active/total fields; UD label classification incl. fail-closed unknowns; per-tool deny removes the tool from the registry and the tightens-only invariant holds under adversarial toggle states; ModelSelector badge render test via the shell suite. Run `npm test` + `npm run lint` + `npm run test:shell` + `npm run lint:shell`; iterate to green. Update CI for `core/registry/` + `modules/coding/mcp/` paths. Do not proceed to Phase 4 until stable. After tests pass, run `/generate-session-history` for Phase 3.

---

### Phase 3 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (schema + tiering + quant labels + MCP invariant + badge)
- [x] Catalog schema change is additive; every pre-existing entry valid unmodified
- [x] EM.P3/EM.P4 gates remain closed; nothing enabled by this phase
- [x] Per-tool MCP control provably tightens-only
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 4

---

## Phase 4: Unattended Autonomy - Ask Inbox + Scheduler (OW-A1, OW-A2)

**Goal**: Make headless and recurring agent runs safe: approval requests park in a persistent inbox instead of dying on the 60s `ConfirmationGate` timeout, and a local cron-style scheduler enqueues agent runs that re-enter the full permission stack on every wake.
**Prerequisites**: Phase 3 (ordering); OW-A2 hard-depends on OW-A1 (scheduled runs need somewhere to park asks).
**Stability Gate**: A persistent local approval queue exists; when a run is headless/scheduled, `src/tools/ConfirmationGate.ts` parks the request (with full action context from `modules/coding/guardrails/ActionClassifier.ts`) instead of timing out, and the run suspends or continues per tier semantics; a desktop inbox panel lists parked asks with approve/deny/expire; a later approval replays through the SAME gate + `modules/coding/guardrails/PermissionTiers.ts` tier check (floor-clamp intact) - approval is never a bypass; a local scheduler triggers recurring agent runs (reusing the weekly skills-auto-sync worker pattern), every wake routes through `PermissionTiers`/`ConfirmationGate` with NO auto-approve path, and `modules/coding/guardrails/GitSafetyNet.ts` checkpoints apply to scheduled runs exactly as to interactive ones; tests cover parking, replay, expiry, and the no-auto-approve invariant.
**Recommended model**: Mid-strong tier, high effort - claude-sonnet-5 (high). Rationale: safety-critical queue/replay semantics over existing guardrails; the code is local and additive but the invariants must be airtight. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 4.1 - Persistent approval queue + gate integration (OW-A1)

**Objective**: The missing precondition for any safe autonomous run.

**Prompt**:
> Build a local persistent approval queue and integrate it with `src/tools/ConfirmationGate.ts`: when the session is headless or scheduled (an explicit run mode, not an inference), a confirmation request is parked in the queue - capturing the classified action (`modules/coding/guardrails/ActionClassifier.ts`), tier, tool, arguments, and session/run identity - instead of blocking on the 60s timeout. Parked requests have states (pending/approved/denied/expired) and a configurable expiry. Approving a parked request REPLAYS it through the confirmation gate and the `PermissionTiers` floor-clamp at approval time (never a stored "pre-approval" that skips the gate); if the session is gone, approval executes in a fresh gated context or fails safe. Interactive sessions are unchanged. Acceptance: headless runs park instead of timing out; approval replays through gate + tiers; expiry fails safe; interactive behavior is untouched.

---

#### 4.2 - Desktop ask-inbox panel

**Objective**: The user-facing surface for parked approvals.

**Prompt**:
> Add an ask-inbox panel to the desktop shell (`desktop/src/`, following the existing panel/dock conventions): list parked requests with the classified action, tier, requesting run, and age; approve/deny per item; show resolved/expired history; surface a non-intrusive pending-count indicator in the shell chrome. Denials and expiries propagate to the suspended run as a clean refusal (the run continues or ends per its tier semantics, never crashes). Acceptance: parked asks are visible and actionable; approve executes via the replay path from 4.1; deny/expire produce clean refusals; the indicator reflects pending count.

---

#### 4.3 - Local agent-run scheduler (OW-A2)

**Objective**: Recurring agent runs with zero weakening of the permission stack.

**Prompt**:
> Build a local cron-style scheduler for recurring agent runs, extending the existing weekly skills-auto-sync worker pattern (`README.md` weekly sync): define schedules locally (no cloud, no new inbound network surface), enqueue an agent run at trigger time in the headless run mode from 4.1. INVARIANT: a scheduled wake re-enters `PermissionTiers` and `ConfirmationGate` exactly like a user-initiated run - there is NO auto-approve, no elevated tier, no gate bypass for scheduled work; consequential actions park in the ask inbox. `GitSafetyNet` checkpoints apply unchanged. Ship with the morning-brief use case wired to the `agent-presets` morning-briefing content (Phase 1.1) as the reference schedule. Acceptance: a schedule fires, runs headless, parks its asks, and the no-auto-approve invariant is enforced in code and tested.

---

#### 4.4 - Testing and Stabilization

**Objective**: Lock the safety invariants; wire CI.

**Prompt**:
> Add tests: parking (headless request parks with full context; interactive request still prompts); replay (approval re-runs gate + tier floor-clamp; a tier downgrade between park and approve is honored); expiry and deny fail safe; scheduler fires and produces a headless run; the no-auto-approve invariant (attempt to construct an auto-approved scheduled action fails); inbox panel tests via the shell suite (list, approve, deny, pending indicator). Run `npm test` + `npm run lint` + `npm run test:shell` + `npm run lint:shell`; iterate to green. Update CI for the queue, scheduler, and panel paths. Do not proceed to Phase 5 until stable. After tests pass, run `/generate-session-history` for Phase 4.

---

### Phase 4 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (park + replay + expiry + scheduler + invariants + panel)
- [x] No auto-approve path exists for scheduled/headless runs (tested, not asserted)
- [x] Approval replays through gate + tiers; floor-clamp intact
- [x] Interactive confirmation behavior unchanged
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 5 (Phase 5 already landed earlier this cycle; park path wired into ACP)

---

## Phase 5: ACP Agent Surface (OI-A3)

**Goal**: Expose the Nexus coding engine as an Agent Client Protocol agent over a loopback-only, locally-authenticated transport, built as the SINGLE shared control-surface layer that the queued v1.16 serving gateway will also use.
**Prerequisites**: Phase 4 (the ACP surface drives agent runs, which must already park asks safely when the driving editor is not attended).
**Stability Gate**: An internal ACP implementation runs over the sidecar (`desktop/src-tauri` bridges intact), binds to loopback ONLY, and requires a local auth token minted per install/session; the transport/auth layer is factored as a shared module explicitly designed for reuse by the v1.16 serving-gateway candidate (one layer, not two - the Section 9 overlap flag); an external editor can drive a coding session end-to-end (open, prompt, tool events, diff/patch results); every action arriving via ACP passes through the SAME `PermissionTiers`/`ConfirmationGate`/`ActionClassifier` stack as UI-originated actions, and unattended ACP-driven consequential actions park in the Phase 4 ask inbox; no code from Open Interpreter is vendored (the protocol is implemented natively from its open spec).
**Recommended model**: Strong tier, high effort - claude-opus-4-8 (high). Rationale: a new local control surface with auth design and cross-cycle architectural constraints; security-sensitive. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 5.1 - Shared loopback transport + local auth layer

**Objective**: One control-surface layer for ACP now and the v1.16 gateway later.

**Prompt**:
> Design and build the shared local control-surface layer: a loopback-only listener with a hard non-loopback reject (mirror the posture of `modules/coding/llm/LocalAdapterRegistry.ts`), a local auth token (generated locally, stored via the existing local secret handling, required on every connection; unauthenticated connections rejected before any protocol handling), and a small session/connection lifecycle module. Factor it so BOTH the ACP surface (this phase) and the queued v1.16 OpenAI/Anthropic-compatible serving gateway (`docs/archive/v1/v1.16/comparisons/v1.16.0-comparison-local-serving-and-ocr.md`) can mount on it - document this contract in the module. Acceptance: loopback-only enforced and tested; auth required and tested; the layer's reuse contract for the v1.16 gateway is documented.

---

#### 5.2 - Native ACP protocol implementation over the sidecar

**Objective**: Implement ACP natively; do not shell out to or vendor any external agent binary (rejection OI-N1).

**Prompt**:
> Implement the Agent Client Protocol natively over the Nexus sidecar: session initialization, prompt submission, streaming agent events (tool calls, output, diffs), and cancellation, mapped onto the existing coding-engine loop (`src/tools/AgentLoop.ts`, tool registry under `src/tools/`). Every consequential action arriving via ACP is classified (`ActionClassifier`) and gated (`PermissionTiers` + `ConfirmationGate`) identically to UI-originated actions; when the ACP client cannot answer a confirmation, the request parks in the Phase 4 ask inbox rather than auto-approving or hard-failing. Implement from the open ACP spec; vendor NO Open Interpreter code and embed no second agent engine. Acceptance: an ACP client can drive a session end-to-end on loopback with auth; gating parity with the UI is tested; no external agent code is embedded.

---

#### 5.3 - Testing and Stabilization

**Objective**: Prove the surface is loopback-only, authenticated, and gate-pariteed; wire CI.

**Prompt**:
> Add tests: transport (non-loopback bind attempts rejected; unauthenticated and bad-token connections rejected pre-protocol); protocol round-trip (init, prompt, streamed events, cancel) against a stub client; gating parity (an ACP-originated DANGEROUS-tier action requires the same confirmation as UI; unattended confirmation parks in the inbox); a contract test locking the shared-layer API the v1.16 gateway will mount on. Run `npm test` + `npm run lint`; iterate to green. Update CI for the new modules. Do not proceed to Phase 6 until stable. After tests pass, run `/generate-session-history` for Phase 5 and record the shared-layer contract decision for the v1.16 cycle.

---

### Phase 5 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (transport + auth + protocol + gating parity + contract)
- [x] Loopback-only and local-auth enforced in code and tests
- [x] One shared control-surface layer; v1.16 gateway reuse contract documented
- [x] No external agent code vendored
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 6

---

## Phase 6: OS Process Sandbox (OI-A1, re-partial, closes EM.P5.A)

**Goal**: Wrap `run_terminal` execution in OS-level confinement on all three OSes (macOS Seatbelt, Linux Landlock+seccomp, Windows job object + restricted token), closing Nexus's top-ranked open gap (EM.P5.A) using Open Interpreter strictly as an Apache-2.0 design reference - no Rust code vendored.
**Prerequisites**: Independent of Phases 2-5 (the comparisons recommend starting it early; it is sequenced last among build phases only because it is the re-partial bucket per the policy ordering - implementation MAY begin in parallel once Phase 1 lands).
**Stability Gate**: A sandbox abstraction wraps the `run_terminal` spawn path (currently `spawn(shell:true)` at user privilege, guarded only by confirmation + touched-path/secret-path denylists + env scrub per `docs/archive/v1/v1.12/exec-sandbox-audit.md`); per-OS backends confine filesystem writes to the project/workspace scope and block or gate network egress per policy; capability detection is explicit - where a backend is unavailable the system runs in a DOCUMENTED degraded mode that is clearly surfaced to the user and recorded, never silently unconfined; existing guardrails (confirmation, denylists, env scrub, `GitSafetyNet`) remain in force IN ADDITION to the sandbox; the existing tool-call contract is unchanged (same inputs/outputs, sandbox is transparent to well-behaved commands); cross-OS CI exercises each backend's policy tests.
**Recommended model**: Strong tier, high effort - claude-opus-4-8 (high). Rationale: three OS security backends with fail-closed semantics; the highest-value and highest-complexity item of the cycle. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 6.1 - Sandbox abstraction + policy model

**Objective**: One policy, three backends.

**Prompt**:
> Design the sandbox abstraction around the `run_terminal` execution path (tool implementation under `src/tools/`, per the registry in `src/tools/ToolRegistry.ts`): a policy object (writable roots = project/workspace + declared temp dirs; readable scope; network allow/deny; process/resource limits) and a backend interface (`prepare/spawn/teardown`, capability report). Derive default policy from the existing session's project root and the touched-path/secret-path denylists so the sandbox and denylists agree. Define the degraded-mode contract: if no backend is available, execution continues ONLY with the pre-existing guardrails, the session UI and logs state "unconfined" explicitly, and the classifier can require a higher confirmation tier for that state. Study `docs/archive/v1/v1.12/exec-sandbox-audit.md` first and reconcile its findings. Acceptance: abstraction + policy + degraded-mode contract exist with unit tests; no behavior change yet for callers.

---

#### 6.2 - macOS backend (Seatbelt)

**Objective**: Confinement via `sandbox-exec`/Seatbelt profiles.

**Prompt**:
> Implement the macOS backend using Seatbelt: generate a per-run profile from the policy object (write access limited to the writable roots, read scope per policy, network per policy), launch the `run_terminal` child under it, and surface profile-violation failures as clean, classified tool errors (not crashes). Use Open Interpreter's approach as a design reference only - write Nexus's own profile generation, vendor no code. Handle Seatbelt deprecation caveats with a capability check and fall back to degraded mode per the 6.1 contract. Acceptance: on macOS, a command writing outside the writable roots fails inside the sandbox while in-scope work succeeds; violations surface cleanly; capability detection is honest.

---

#### 6.3 - Linux backend (Landlock + seccomp)

**Objective**: Confinement via Landlock filesystem rules plus a seccomp filter.

**Prompt**:
> Implement the Linux backend: apply Landlock rules for the policy's filesystem scope and a seccomp filter for the network policy (block socket creation when egress is denied) in the child before exec. Detect kernel support (Landlock ABI level) and fall back to degraded mode per the 6.1 contract on old kernels. Same clean-error surfacing as 6.2. Acceptance: on a Landlock-capable kernel, out-of-scope writes and denied network calls fail inside the sandbox; unsupported kernels degrade loudly per contract.

---

#### 6.4 - Windows backend (job object + restricted token)

**Objective**: Confinement on Nexus's first-class platform.

**Prompt**:
> Implement the Windows backend: launch the `run_terminal` child in a job object (kill-on-close, process/resource limits) with a restricted token and, where available, an AppContainer or equivalent filesystem/network restriction honoring the policy object; document precisely which policy dimensions each Windows mechanism can and cannot enforce, and route unenforceable dimensions through the degraded-mode surfacing (partial confinement is stated, not implied). Same clean-error surfacing as 6.2/6.3. Acceptance: on Windows, the child is job-confined with resource limits; the enforced-vs-unenforced policy matrix is documented; partial confinement is surfaced to the user.

---

#### 6.5 - Testing and Stabilization

**Objective**: Prove confinement per OS; wire cross-OS CI.

**Prompt**:
> Add tests: unit tests for policy derivation and the degraded-mode contract (mock backends); per-OS integration tests (in-scope command succeeds; out-of-scope write fails; denied network fails where enforceable; violation surfaces as a classified tool error; capability detection matches the host) tagged to run on their matching CI OS; a regression test that the `run_terminal` tool contract (inputs/outputs, confirmation flow) is unchanged for well-behaved commands; verify existing denylist/env-scrub guardrails still apply inside the sandbox. Run `npm test` + `npm run lint`; iterate to green. Extend CI to a three-OS matrix for the sandbox test suite (scoped path filters to keep minutes bounded). Do not proceed to Phase 7 until stable. After tests pass, run `/generate-session-history` for Phase 6 and record which OS/backend combinations ship confined vs degraded.

---

### Phase 6 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (policy + three backends + degraded mode + contract regression)
- [x] Existing guardrails (confirmation, denylists, env scrub, GitSafetyNet) unchanged and still active
- [x] Degraded mode is loud, documented, and classifier-visible - never silent
- [x] No external (Rust/OI) code vendored
- [x] EM.P5.A closure status recorded per OS for known-gaps reconciliation
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 7

---

## Deferred and Gated Items (tracked, not built this cycle)

These are adoption items from the comparisons that stay behind explicit gates; Phase 7 records each in `docs/archive/v1/v1.18/known-gaps.md` so the next plan picks them up automatically.

| Item | Source | Gate / reason | Unblocks when |
|------|--------|---------------|---------------|
| LG-A1 - Laguna-S-2.1 gated catalog entry (P2) | Laguna Section 7 | EM.P3 (extreme-low-bit, `core/registry/extremeLowBit.ts` fail-closed) + EM.P4 (patient tier, `core/registry/patientTier.ts` disabled) both closed; independent benchmark required (`docs/archive/v1/v1.12/known-gaps.md`) | Gates opened (natural home: the v1.16.0 local-serving cycle) + independent benchmark; Phase 3's schema/taxonomy work is its preparation |
| LG-A4 - 1M-context budget policy (P3) | Laguna Section 7 | Depends on LG-A1 landing | LG-A1 ships |
| OI-A4-native - native-app computer-use driver (P3) | Open Interpreter Section 7 | High effort, Medium risk, new local capability; must be internal (no `trycua` bundle, rejection OI-N3), permission-tiered, and confirmation-gated | A future cycle after the browser-half (skill-native, Phase 1.1) proves the QA workflow |

## Out of Scope / Rejected (carried forward from the three Section 9s)

Every rejection below is carried forward verbatim in substance and cites the MCP Registry Policy (`AGENTS.md`; decision tree summarized in `CLAUDE.md`) and/or Nexus design principles. Do not let "the source does more" pressure these.

From the OpenWorker comparison:
- **OW-N1 - The 25+ hosted SaaS connectors** (GitHub, Slack, Jira, Notion, Linear, HubSpot, Gmail, Google Calendar, ...). Rejected: MCP Registry Policy "hard no" on as-a-service integrations; third-party data processors; credential sprawl; local-first design principle.
- **OW-N2 - The cloud OAuth broker relay.** Rejected: a new cloud dependency and outbound relay; conflicts with no-outbound-without-opt-in; `HubRegistryPolicyFilter.ts` default-deny.
- **OW-N3 - Cloud provider breadth via aisuite (11 hosted providers).** Rejected: already formally dropped as D1 in v1.6.0 (`docs/archive/v1/v1.6/plans/adoption-aisuite-harness.md`); conflicts with local-first / "Zero Tokens Billed".

From the Open Interpreter comparison:
- **OI-N1 - Vendoring Open Interpreter's Rust harness code.** Rejected: a Rust harness engine does not fit the Node-sidecar spine; violates originality-over-wrappers; policy bucket 3 mandates reverse-engineering the behavior instead (done in Phases 2 and 6).
- **OI-N2 - The "any hosted provider" dynamic catalog** (`write_provider_catalog.py` across cloud providers). Rejected: conflicts with the `LocalAdapterRegistry` loopback guard and local-first posture; local-only provider registration stands.
- **OI-N3 - Bundling `trycua` for native computer-use.** Rejected/gated: heavy dependency and new attack surface; any future native driver is an internal, opt-in, permission-tiered build (see OI-A4-native above).

From the Laguna-S-2.1 comparison:
- **LG-N1 - Bundling Laguna weights or making it a default `recommended.json` pick.** Rejected: 118B total exceeds the single-GPU 24 GB ceiling even at low bit (Design Principle 3); gated patient/offload tier only.
- **LG-N2 - Consuming Laguna via the Poolside API or OpenRouter.** Rejected: outbound third-party generation with per-token billing; MCP Registry Policy "hard no" on generation-as-a-service; conflicts with "Zero Tokens Billed".
- **LG-N3 - Opening the EM.P3 / EM.P4 gates specifically to ship Laguna ahead of independent benchmarks.** Rejected/gated: the v1.12 roadmap requires runtime support + independent benchmarks before flipping either gate (`docs/archive/v1/v1.12/known-gaps.md`); premature enablement risks OOM and unbounded latency on target hardware.

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| (none - no constitution file; no violations) | | |

---

## Phase 7: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled (including the gated LG/OI items above), and its CI/CD complete and optimized.
**Prerequisites**: All prior phases.
**Stability Gate**: The layout is clean (no deprecated/obsolete files, empty dirs, redundant files/dirs, or overcomplicated structure left un-triaged); the version's known gaps are reconciled; CI/CD covers every change (including the three-OS sandbox matrix) and is optimized; project validation/tests pass.
**Recommended model**: Strong tier, high effort - claude-opus-4-8 (high). Repo-wide refactor + reference repair is high-risk - default to the strong reasoning tier.

### Sub-tasks

#### 7.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure introduced or exposed across Phases 1-6 (e.g. transitional scaffolding around the confirmation gate once the queue landed, duplicated loopback/auth helpers superseded by the Phase 5 shared layer, dead settings paths from the harness wiring), then refactor toward a clean, intuitive layout via `[[project-refactor]]` and `[[docs-layout-refactor]]` (propose-then-apply, with confirmation; repair every reference for anything that moves). Confirm the `docs/archive/v1/v1.18/` tree is complete (this plan, the three seeding comparisons, phase session-histories, known-gaps).

#### 7.2 - Known-gaps reconciliation
**Objective**: Reconcile the version's open gaps.
**Prompt**:
> Reconcile this version's known gaps via `[[known-gaps-tracker]]`: create `docs/archive/v1/v1.18/known-gaps.md`, resolve/defer/transfer each still-open item carried from `docs/archive/v1/v1.17/known-gaps.md` (and any earlier carry-forward, notably the v1.12 EM items this cycle touches: mark EM.P1.A resolved, record EM.P1.C progress, and set EM.P5.A's per-OS closure status from Phase 6), and record the new v1.18 gaps: the three deferred/gated items (LG-A1 gated catalog entry, LG-A4 1M-context budget, OI-A4-native driver), the `HARNESS_SELECTOR_SHIPPED_DEFAULT` flip decision if still pending, any OS/backend combination that shipped in degraded (unconfined) mode, and the v1.16 shared-transport contract obligation from Phase 5. Finalize the per-minor known-gaps file for v1.18.

#### 7.3 - CI/CD create/update/optimize
**Objective**: CI/CD covers all changes and is optimized.
**Prompt**:
> Create or update the CI/CD pipeline so it covers every change in this plan (harness wiring + profiles, catalog/registry governance, ask inbox + scheduler, the ACP surface, and the three-OS sandbox matrix from Phase 6.5), then optimize it to reduce action minutes (path filters per touched tree - `src/`, `modules/`, `core/registry/`, `desktop/src/` - concurrency cancel-in-progress, dependency caching for npm, and OS-matrix jobs gated to sandbox-relevant paths only) while keeping comprehensive testing. Keep it platform-agnostic; GitHub Actions is the primary example. Note the standing GitHub Actions budget freeze if it still applies at implementation time.

#### 7.4 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and CI/CD is green.
**Prompt**:
> Run the full validation/test suite (`npm test` + `npm run lint` + `npm run test:shell` + `npm run lint:shell`), confirm the refactor changed no behavior, confirm CI/CD passes and the action-minute reduction is real, and iterate until clean. Update `docs/todos.md` (dashboard metrics + checked-off items) via `[[dev-progress-tracker]]`. Generate a session-history entry for this phase.

---

### Phase 7 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (core: unit + lint; desktop shell: unit + lint)
- [x] No known regressions from prior phases
- [x] Known gaps reconciled (v1.17 carry-forward + v1.12 EM updates + new v1.18 items incl. the gated LG-A1/LG-A4/OI-A4-native)
- [x] CI/CD covers all changes (incl. the three-OS sandbox matrix) and is optimized
- [x] Session history generated for this phase
- [x] Ready to release v1.18.0 (hand off to `/update release`) -- Phase 4 landed; DF-9 resolved. Version bump + CHANGELOG in this release commit. Tag / push / GitHub Release stay behind confirmation.
