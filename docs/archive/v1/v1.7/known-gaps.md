# v1.7.0 -- Known Gaps, Deferrals, and Carryovers

**Status**: COMPLETE -- all 6 phases closed (Phase 6 FINAL closed 2026-07-02; the whole-plan acceptance gate passed with no feature-code change -- verification + close-out only). v1.7.0 is the "local skill self-optimization loop + opencode harness hardening" cycle ([plans/adoption-self-optimizing-skills.md](plans/adoption-self-optimizing-skills.md), derived from [comparison-self-optimizing-skills.md](comparison-self-optimizing-skills.md) (primary, S1-S6) and [comparison-opencode.md](comparison-opencode.md) (secondary, O-A)). Phase 1 ships S1: a TS-native golden-task **live runner** that revives the golden suite as a working evaluator (the optimization loop's hard prerequisite, and a restoration of the live golden runs deleted by [ADR-0001](../../adr/0001-python-backend-disposition.md)) -- four vscode-free `modules/coding/evaluation/` modules (a declarative `success_criteria` evaluator, a copy-into-temp + `git init` snapshot materializer, a dependency-free YAML-subset task loader, and a runner that materializes -> runs the agent via an injected driver -> evaluates -> emits a scored `GoldenTaskResult`) (SO001, closed 2026-06-29). Phase 2 ships S4: the regression-safety scaffolding the optimizer cannot be trusted without -- a locked train/validation/test split (additive `split` field + a deterministic default-by-category assignment) whose `test` split is structurally unreachable from the optimizer loader (`optimizerVisibleTasks` + `assertNoTestSplit` guard), a pure held-out `validationGate` comparator (strict aggregate-improvement + per-task regression tolerance over scored batches, reusing `RegressionReport`), and a content-addressed, redaction-on-write `RejectedEditBuffer` (`core/memory/`, keyed by `skillId + editHash`, edit/trajectory text stored via `ArtifactStore` so it never lands on disk unredacted) (SO002, closed 2026-06-30). Phase 3 ships S2 + S6: the bounded-edit `SkillOptimizer` (the headline loop -- rollout the train split -> reflect on failing trajectories via the existing `ReflexionEngine`/`CriticAgent` -> propose a bounded add/delete/replace skill edit under a textual learning-rate budget -> accept only on a held-out `validationGate` win -> buffer rejects -> overwrite only after explicit human approval, the whole loop capped by the v0.3.0 runaway `BudgetMiddleware`) plus the optimizer-quality A/B (`SkillOptimizerAb` reusing the Fusion F4 `PanelAbHarness` + a net-win default gate); built as eight vscode-free `modules/coding/skilloptimizer/` modules, opt-in default-off (SO003 + SO004, closed 2026-06-30). This file is appended phase-by-phase; items move to `## 2. Resolved` when closed; the `## 3. Summary` is recomputed each pass.

**Audience**: v1.7.0 phase authors, code reviewer, future-cycle planners
**Last updated**: 2026-07-02 (post-cycle -- vscode-free headless agent runtime + optimizer production wiring; resolves SO001.P1.A / SO003.P3.B / SO005.P4.A/B / SO001.P1.B)
**Sibling reviews**: [../v1.6.0/known-gaps.md](../v1.6/known-gaps.md) (the prior cycle's gap log) and [plans/adoption-self-optimizing-skills.md](plans/adoption-self-optimizing-skills.md) (the active plan).

**Cycle context**: This file is created in Phase 1 (rather than at the FINAL Phase 6 named in plan sub-task SO008) because the implement-phase post-phase sequence appends gaps every phase. Phase 1 introduces no bug, test failure, coverage shortfall, suppressed lint, or bypassed quality gate; the seeded entries below are forward-tier follow-ups (`candidate` / `future`), not defects.

**Wording convention**: every prose claim follows the evidence-and-support-tiers discipline ([../v1.4.0/development/evidence-and-support-tiers.md](../v1.4/development/evidence-and-support-tiers.md), A7): a gap describes its unbuilt capability at tier `future` or `candidate` (never `supported`); the "Suggested next step" states what cited evidence would raise the tier. "not_observed != absent" applies throughout.

Severity tags: **P0** release-blocker; **P1** should-fix; **P2** nice-to-have; **P3** out-of-scope for v1.7.0 / recorded for future planning.
Category tags: **NI** not implemented; **DF** deferred; **BG** bug; **MT** missing tests; **WN** warning; **QG** quality gate.

---

## 0. Adoption Ledger

Per-sub-task closure ledger for the self-optimizing-skills adoption plan. Rows land as each phase closes.

| Plan sub-task | Item | Status | Closing reference |
|---|---|---|---|
| SO007 + SO008 + SO009 | FINAL -- whole-plan acceptance gate + docs + Nexus-Hub-touchpoint assessment | Resolved | Phase 6 (2026-07-02); verification + close-out only, no feature code changed. **SO007 acceptance gate**: all seven Definition-of-pass items verified -- items 1-6 (S1 live runner; S4 split + held-out gate + rejected-edit buffer; S2+S6 bounded-edit optimizer + A/B; S3 Pareto frontier; O-A shell introspection) confirmed physically present, item 7 (testing + docs) delivered this phase. Gate green: root suite **4498 passed / 6 skipped / 0 failed**; coverage lines 88.22% / branches 84.01% / functions 91.35% (all above the 80/75/80 CI gate); `tsc -b` clean; `npm run lint` 0 errors; `npm run check-architecture` 0 errors / 10 pre-existing warnings (no new orphan/cycle); `npm run security:check` in sync; `npm run check:tampering` 0 findings. **SO008 docs**: this known-gaps ledger, [DEVLOG](../../DEVLOG.md), [todos](../../todos.md), and the [plan](plans/adoption-self-optimizing-skills.md) finalized; a Phase 6 history file written; README/ARCHITECTURE carry no per-version content and the CHANGELOG narrative + npm version tag are semantic-release-owned (cut on merge to `main`), so no manual edit to those three. The demand-gated backlog (S5; opencode O-B/O-D/O-E) is recorded in the plan's Out-of-Scope appendix and the summary below. **SO009 Hub touchpoint**: assessed **not warranted** as an in-repo change (`SO009.P6.A`) -- the runtime skill-optimization loop is Nexus-internal product code, not a portable skill artifact the Hub's `skill-eval-loop` / `loop-engineering` / `continuous-learning` skills lack, and the plan forbids duplicating loop logic into the Hub; a one-line Hub-side cross-link is recorded as a demand-gated suggestion for a future Hub sync (precedent: v1.6.0 `OF015.P5.A` / `AS010.P6.A`, both assessed not-warranted). |
| SO006 | O-A -- tree-sitter shell-command introspection for permission gating | Resolved | Phase 5 (2026-07-01); built local-only as the vscode-free, dependency-free [shellIntrospection.ts](../../../modules/coding/guardrails/shellIntrospection.ts) `introspectShellCommand` reverse-engineering opencode's `tool/shell/` command introspection as a lean structural parser (per the AGENTS.md MCP Registry Policy, bucket 3: no new dependency -- the bundled `tree-sitter-wasms` ships a bash grammar only, and pulling native PowerShell / cmd grammars conflicts with no-runtime-download). A **quote-aware tokenizer** splits chained sub-commands (`;`/`&&`/`||`/`|`/newline), keeps quoted spans intact, and emits redirection operators (`>`/`>>`/`2>`/`2>>`/`<`, fd-dup `2>&1` targets skipped) as their own tokens; per-dialect **file-command tables** (bash `rm`/`cp`/`mv`/`touch`/`mkdir`/`tee`/`cat`..., cmd `del`/`copy`/`move`/`type`..., PowerShell `Remove-Item`/`Set-Content`/`Out-File`/`Copy-Item`/`Get-Content`... incl. `-Path`/`-FilePath`/`-LiteralPath`/`-Destination` named-param values) classify each argument as read / write / delete, and the `cd`-family as a cwd change. **Fails closed** (`parsed: false`) on any construct that could hide a path from static analysis: command / process substitution (`$(`, `<(`, `>(`), variable expansion (`$` for bash/PowerShell, `%`/`!` for cmd), backtick, `Invoke-Expression`, or an unbalanced quote -- so the caller relies on the existing gate and never auto-allows. Wired into the [ToolRegistry](../../../src/tools/ToolRegistry.ts) deny-first gate (`_denyByTouchedPath`): for a `run_terminal` call, besides the existing whole-command-string match, each enumerated write path is matched against `write_file` deny rules, each delete against `delete_file`, each read against `read_file` (`evaluateDeny` also honors blanket `*:` rules) -- so `write_file: secrets/**` now also blocks `echo x > secrets/prod.env`; a non-parseable command logs the fallback and fabricates no path rule. The [terminal](../../../src/tools/handlers/terminal.ts) dry-run report gains a `Touched paths:` line (structural answer to "what will this command touch?"). Only ever *tightens* the surface (additive refusal; never downgrades a tier or approves). Tests: [shellIntrospection.test.ts](../../../tests/unit/guardrails/shellIntrospection.test.ts) (29), [ToolRegistry.test.ts](../../../tests/unit/tools/ToolRegistry.test.ts) (+5), [terminal.dry_run.test.ts](../../../tests/unit/tools/handlers/terminal.dry_run.test.ts) (+3). Verification: `tsc -b` clean, lint 0 errors, check-architecture 0 errors / 10 pre-existing warnings (no new orphan/cycle; +1 module), check:tampering 0, security:check in sync, root suite 4498 passed / 6 skipped / 0 failed, new-module coverage shellIntrospection 100% lines / 92.59% branches / 100% functions. |
| SO005 | S3 -- Pareto-frontier candidate management on git branches | Resolved | Phase 4 (2026-07-01); built local-only as three vscode-free [`modules/coding/skilloptimizer/`](../../../modules/coding/skilloptimizer) modules on the Phase 3 spine, reverse-engineering the genetic-Pareto selector as a lean local module (no DSPy/GEPA/EvoSkill dependency). **Pure selector** ([pareto.ts](../../../modules/coding/skilloptimizer/pareto.ts)): `dominates` (strict Pareto domination over shared per-task score vectors -- worse-anywhere loses, strictly-better-somewhere + never-worse wins; disjoint/empty vectors do not dominate), `paretoFrontier` (the non-dominated set: diverse winners each strong on a different task all survive, identical vectors are mutually non-dominated), and `lowestByHeldOut`/`highestByHeldOut` (the eviction target + the winner, deterministic id tie-break). **Orchestrator** ([CandidateFrontier.ts](../../../modules/coding/skilloptimizer/CandidateFrontier.ts)): for each produced candidate (injected `CandidateProducer` over the Phase 3 optimizer) -> materialize on its own git branch (injected `CandidateWorkspaceManager`) -> score across the diverse task set (injected `CandidateScorer` over the Phase 1 runner) -> auto-clean the ephemeral worktree immediately (one worktree live at a time, the single-GPU discipline; the branch ref survives for promotion) -> admit into a bounded population under the **hard candidate cap** (`maxCandidates`, sourced from the hardware tier's `maxConcurrentSubAgents` at a composition root -- the swarm-worker/VRAM gate). The **EvoSkill replacement rule**: at the cap, a challenger displaces the lowest-held-out incumbent ONLY when it strictly beats it (by a configurable margin) on the held-out split, else it is rejected. The non-dominated (Pareto) set is selected and the winner (highest held-out among the frontier) is surfaced for **explicit human approval** -- reusing the Phase 3 `SkillEditApprovalGate` and classifying the merge as a DESTRUCTIVE `write_file` (`classifyAction`). **No auto-merge**: the `CandidatePromoter` seam (the only merge path) is structurally unreachable unless approval returns true -- proven by a test that a denying gate promotes nothing. **Real branch-worktree materializer** ([frontierWorktree.ts](../../../modules/coding/skilloptimizer/frontierWorktree.ts) `WorktreeCandidateManager`): reuses the v1.5.0 worktree-swarm `GitRunner` contract (type-only import, so no vscode-coupled logger is pulled in -- the subtree stays plain-Node loadable, the Phase 1/3 invariant), materializing each candidate on `nexus/skill-candidate/<skill>/<id>` via `git worktree add -b ... HEAD`, committing the candidate body (frontmatter preserved), and fault-tolerantly failing closed to null when git is unavailable or any step fails (a failed commit removes the half-made worktree). Frontier types added to [types.ts](../../../modules/coding/skilloptimizer/types.ts). Tests: [pareto.test.ts](../../../tests/unit/skilloptimizer/pareto.test.ts) (13), [CandidateFrontier.test.ts](../../../tests/unit/skilloptimizer/CandidateFrontier.test.ts) (9), [frontierWorktree.test.ts](../../../tests/unit/skilloptimizer/frontierWorktree.test.ts) (8). Verification: `tsc -b` clean, lint 0 errors, check-architecture 0 errors / 10 pre-existing warnings (no new orphan/cycle; +3 modules), check:tampering 0, security:check in sync, root suite 4461 passed / 6 skipped / 0 failed, new-module coverage pareto 100/100/100, CandidateFrontier 100/94.11/100, frontierWorktree 96.61/90/100 (skilloptimizer subtree 99.47/91.51/100). |
| SO003 + SO004 | S2 bounded-edit optimizer + S6 optimizer-quality A/B | Resolved | Phase 3 (2026-06-30); built local-only as eight vscode-free [`modules/coding/skilloptimizer/`](../../../modules/coding/skilloptimizer) modules. **Loop** ([SkillOptimizer.ts](../../../modules/coding/skilloptimizer/SkillOptimizer.ts)): rollout the train split via an injected `OptimizerRollout` (over the Phase 1 runner + the deferred real driver, `SO001.P1.A`) -> reflect on a minibatch of failing trajectories ([ReflexionDiagnoser.ts](../../../modules/coding/skilloptimizer/ReflexionDiagnoser.ts), composes `ReflexionEngine`, redacts trajectory text) -> propose a bounded add/delete/replace edit ([SkillEditProposer.ts](../../../modules/coding/skilloptimizer/SkillEditProposer.ts), local `OllamaClient` port, fail-closed JSON parse) optionally pre-filtered by a cheap critic ([EditCritic.ts](../../../modules/coding/skilloptimizer/EditCritic.ts), composes `CriticAgent`) -> accept only on the Phase 2 held-out `evaluateValidationGate` -> [RejectedEditBuffer](../../../core/memory/RejectedEditBuffer.ts) on reject. The whole loop is capped by the v0.3.0 runaway [`BudgetMiddleware`](../../../src/tools/BudgetMiddleware.ts); a per-round textual learning-rate budget caps edit volume ([skillEdit.ts](../../../modules/coding/skilloptimizer/skillEdit.ts) `withinLearningRate`); a re-proposed (already attempted or buffered) edit halts the loop. **Approval-before-overwrite**: every accepted edit is `classifyAction`-classified (write_file -> DESTRUCTIVE), path-resolved inside the catalog root (fail-closed via [io.ts](../../../modules/coding/skilloptimizer/io.ts) `RootSkillPathResolver`), and written ONLY on an explicit `SkillEditApprovalGate.requestApproval` signal -- the file write is the loop's only side effect and is unreachable without approval (the composition root adapts `ConfirmationGate`/`pathGuard`; `SO003.P3.C`). **A/B** ([SkillOptimizerAb.ts](../../../modules/coding/skilloptimizer/SkillOptimizerAb.ts)): reuses the v1.6.0 Fusion F4 [`PanelAbHarness`](../../../modules/coding/orchestration/PanelAbHarness.ts) (baseline = single arm, optimized skill = panel arm; quality = validation pass signal, latency = rollout wall-clock) + `decideSkillOptimizerDefault` (net-win + quality + latency gate). Opt-in flag `nexus.coding.skillOptimizer.enabled` ships **default off** (no live local A/B run here, so no measured net win exists to flip it; `SO003.P3.A`). Tests: [SkillOptimizer.test.ts](../../../tests/unit/skilloptimizer/SkillOptimizer.test.ts) (11), [skillEdit.test.ts](../../../tests/unit/skilloptimizer/skillEdit.test.ts) (14), [SkillEditProposer.test.ts](../../../tests/unit/skilloptimizer/SkillEditProposer.test.ts) (6), [ReflexionDiagnoser.test.ts](../../../tests/unit/skilloptimizer/ReflexionDiagnoser.test.ts) (4), [EditCritic.test.ts](../../../tests/unit/skilloptimizer/EditCritic.test.ts) (2), [io.test.ts](../../../tests/unit/skilloptimizer/io.test.ts) (5), [SkillOptimizerAb.test.ts](../../../tests/unit/skilloptimizer/SkillOptimizerAb.test.ts) (6). Verification: `tsc -b` clean, lint 0 errors, check-architecture 0 errors / 10 pre-existing warnings (no new orphan/cycle), check:tampering 0, security:check in sync, root suite 4431 passed / 6 skipped / 0 failed, new-module coverage 99.62% lines / 89.61% branches / 100% functions across the `skilloptimizer` subtree. |
| SO002 | S4 -- split + held-out gate + rejected-edit buffer | Resolved | Phase 2 (2026-06-30); three concerns built local-only. **Split**: an additive optional `split: train\|validation\|test` field on `GoldenTaskSpec` ([../../../../modules/coding/evaluation/goldenTaskLoader.ts](../../../modules/coding/evaluation/goldenTaskLoader.ts), fail-closed on unknown values) + [../../../../modules/coding/evaluation/goldenSplit.ts](../../../modules/coding/evaluation/goldenSplit.ts): `assignDefaultSplits` derives a missing split deterministically per category (tasks sorted by id, cycled train -> validation -> test, explicit splits honored) so each split is representative across families; `splitGoldenTasks` partitions; `optimizerVisibleTasks`/`loadOptimizerVisibleTasks` are the only loaders the optimizer path uses and return train + validation only, re-checked by `assertNoTestSplit` so a `test` task can never reach the loop (the article's anti-contamination requirement). **Held-out gate**: [../../../../modules/coding/evaluation/validationGate.ts](../../../modules/coding/evaluation/validationGate.ts) -- a pure `validationGate(before, after) -> boolean` (+ `evaluateValidationGate` report) over scored `GoldenTaskResult` batches that accepts only on a strict aggregate validation-pass-rate improvement (configurable margin) with per-task pass->fail regressions within a tolerance (default 0), reusing the existing `RegressionReport` shape and emitting a reason string. **Rejected-edit buffer**: [../../../../core/memory/RejectedEditBuffer.ts](../../../core/memory/RejectedEditBuffer.ts) -- content-addressed + redaction-on-write via [../../../../core/memory/ArtifactStore.ts](../../../core/memory/ArtifactStore.ts) (edit/trajectory text redacted before it is hashed + written, so it never lands on disk unredacted; reason redacted in a JSON index), keyed by `skillId + editHash`, idempotent (first write wins), tolerant of a missing/corrupt index or pruned artifact. `core/`-layer only (composes `ArtifactStore` + `redactSecrets`; primitive inputs, so no `no-core-from-modules` violation). Tests: [../../../../tests/unit/evaluation/goldenSplit.test.ts](../../../tests/unit/evaluation/goldenSplit.test.ts) (13), [../../../../tests/unit/evaluation/validationGate.test.ts](../../../tests/unit/evaluation/validationGate.test.ts) (8), [../../../../tests/unit/core/memory/RejectedEditBuffer.test.ts](../../../tests/unit/core/memory/RejectedEditBuffer.test.ts) (8). Verification: `tsc -b` clean, lint 0 errors, check-architecture 0 errors / 10 pre-existing warnings (no new cycle), check:tampering 0 findings, security:check in sync, root suite 4382 passed / 6 skipped / 0 failed, new-module coverage goldenSplit 100/100/100, validationGate 100/100/100, RejectedEditBuffer 100/100/100. |
| SO001 | S1 -- TS-native golden-task live runner | Resolved | Phase 1 (2026-06-29); four vscode-free `modules/coding/evaluation/` modules. [goldenCriteria.ts](../../../modules/coding/evaluation/goldenCriteria.ts): TS port of `tests/golden/framework/evaluator.py` -- all 8 declarative criterion types (`file_contains`/`file_exists`/`file_deleted`/`output_contains`/`test_passes`/`lint_passes`/`no_errors`/`diff_matches`) with regex-or-literal matching and an injected `CommandRunner` (default shells out with a timeout; tests inject a fake for determinism + cross-platform). [goldenSnapshot.ts](../../../modules/coding/evaluation/goldenSnapshot.ts): TS port of `snapshot.py` `prepare_worktree`+`init_git_repo` -- copies a snapshot into a throwaway temp dir (pruning `node_modules`/`.git`/`.worktrees`/`__pycache__`) and `git init`s a clean baseline via an injected, fault-tolerant `GitRunner`. [goldenTaskLoader.ts](../../../modules/coding/evaluation/goldenTaskLoader.ts): dependency-free YAML-subset parser for the fixed `tests/golden/tasks/*.yaml` schema -> `GoldenTaskSpec` (block scalars, escaped double-quoted scalars, single-quoted scalars, flow lists, mapping sequences; fail-closed throws on unsupported constructs), validated against the full 28-task corpus. [GoldenTaskRunner.ts](../../../modules/coding/evaluation/GoldenTaskRunner.ts): materialize -> (dry: skip / live: injected `AgentDriver`) -> `evaluateCriteria` -> scored `GoldenTaskResult`, with a per-task timeout (`AbortController`/`AbortSignal`) and workspace cleanup. The agent loop + concrete `OllamaClient` are vscode-coupled (`ConversationManager`, the logger) and live behind the `no-llm-outside-llm-folder` rule, so the runner depends on an injected `AgentDriver` seam (mirroring the `WorktreeManager.GitRunner` / `TraceDbReader` injection pattern) rather than importing them. Tests: [goldenCriteria.test.ts](../../../tests/unit/evaluation/goldenCriteria.test.ts) (12), [goldenTaskLoader.test.ts](../../../tests/unit/evaluation/goldenTaskLoader.test.ts) (13, incl. the full corpus + the escaped-scalar decode), [goldenSnapshot.test.ts](../../../tests/unit/evaluation/goldenSnapshot.test.ts) (9), [GoldenTaskRunner.test.ts](../../../tests/unit/evaluation/GoldenTaskRunner.test.ts) (8), [golden-runner-end-to-end.test.ts](../../../tests/integration/golden/golden-runner-end-to-end.test.ts) (3: real-snapshot dry + mock-live), [golden-runner.live.test.ts](../../../tests/integration/golden/golden-runner.live.test.ts) (env-gated `GOLDEN_LIVE_OLLAMA` real-Ollama smoke). Verification: `tsc -b` clean, lint 0 errors, check-architecture 0 errors / 10 pre-existing warnings, check:tampering 0 findings, security:check in sync, root suite 4354 passed / 6 skipped / 0 failed, new-module coverage 97.11% lines / 83.25% branches / 100% functions. |

---

## 1. Open Items

### Post-cycle headless-runtime hardening (2026-07-02) -- RESOLVED forward-tier gaps

A post-cycle build landed the deferred **vscode-free headless agent runtime** (the "NexusCodingRuntime" root several gaps were gated behind), resolving five forward-tier items. Built as `modules/coding/runtime/` (`headlessTools.ts`, `HeadlessAgentSession.ts`, `HeadlessAgentDriver.ts`) + `modules/coding/skilloptimizer/` (`HeadlessOptimizerRollout.ts`, `HeadlessCandidateSeams.ts`) + a `nexus golden run` CLI. Full suite 4533 passed / 6 skipped / 0 failed; all static gates green. Commits `b2ba301`, `5240fa9`, `2fb0a75`.

| ID | Was | Now resolved by |
|---|---|---|
| `SO001.P1.A` | P2 DF -- no production `AgentDriver` over the real `AgentLoop` | `HeadlessAgentDriver` (vscode-free agentic loop reusing the pure `Gemma4ToolFormat` codec + a workdir-scoped tool set) implements the `GoldenTaskRunner` seam; `runGoldenTask` live mode drives the real agent. Integration-tested end-to-end (pass + fail paths). |
| `SO003.P3.B` | P2 DF -- no production `OptimizerRollout` over the real agent | `HeadlessOptimizerRollout` over `runGoldenTask` + the driver + the skill-body override; integration test proves the candidate body reaches the agent prompt. |
| `SO005.P4.A` | P2 DF -- no production candidate producer + scorer | `HeadlessCandidateProducer` (real `SkillEditProposer` + `applySkillEditOps`, dedup) + `HeadlessCandidateScorer` (Pareto per-task vector + held-out aggregate over the rollout). |
| `SO005.P4.B` | P3 DF -- no live branch->catalog promoter | `HeadlessCandidatePromoter` (fail-closed catalog merge behind an optional checkpoint; reachable only after the frontier's human-approval gate -- the no-auto-merge guarantee stays structural). |
| `SO001.P1.B` | P3 NI -- no `nexus golden run` CLI | `nexus golden run [--task <id>] [--mode dry\|live] [--model <name>]` (dry offline / live over Ollama), mirroring the `nexus trace export` `loadCompiled` precedent. |

### Post-cycle open follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `RT.P7.A` | P2 | DF | The desktop sidecar Coding pillar (`desktop/sidecar/src/coding/sessionManager.ts`) still returns the Phase 3.1 canned event stub; it is **not** yet wired to the headless runtime. A wiring was prototyped but reverted: the `desktop -> modules/coding/runtime -> src/tools` import chain drags repo-root `src/` into the desktop `tsc --noEmit` typecheck (a `shell-build.yml` CI gate), surfacing a latent unused-var in `src/storage/GraphQueryEngine.ts`. Tier: headless runtime `supported`; desktop consumption `future`. | Land `RT.P7.B` (relocate the tool-call primitives into `modules/coding` so the sidecar chain never reaches `src/`), then inject a `createHeadlessAgentRunner()` into `CodingSessionManager` (mapping `HeadlessAgentEvent` -> the `CodingSessionEvent` IPC union) and plumb a per-session workspace path from the frontend. Verify via the Tauri e2e (needs the Rust toolchain). |
| `RT.P7.B` | P2 | DF | `HeadlessAgentSession` reuses the pure vscode-free `src/tools/{Gemma4ToolFormat,ToolCatalog,types}` via a leaf `modules -> src` import. This passes check-architecture (no forbidden rule, no cycle) but is architecturally inverted and blocks the desktop typecheck from staying `src/`-free (`RT.P7.A`). Tier: functional reuse `supported`; clean boundary `future`. | Relocate `Gemma4ToolFormat` / `ToolCallParser` / `ToolCatalog` / the shared tool `types` from `src/tools/` into `modules/coding/tools/` and repoint the ~20 `src/tools` consumers (a `src -> modules` edge is allowed); then the runtime + sidecar type graph never reaches repo-root `src/`. |
| `RT.P7.C` | P3 | NI | No `nexus skills optimize` (SO003.P3.D) / `nexus skills frontier` (SO005.P4.C) CLI yet. The production rollout / producer / scorer / promoter seams they compose are now built (above), but the CLIs need the full optimizer/frontier composition root (diagnoser + proposer + critic + buffer + approval gate + catalog + splits) and a live local model. Tier: seams `supported`; CLIs `future`. | Build a composition-root factory that assembles `SkillOptimizer` / `CandidateFrontier` from the shipped seams + `createOllamaClient`, then add the two subcommands mirroring `nexus golden run`; gate live runs on Ollama availability like the golden live smoke. **RESOLVED v1.12.0 (Phase 2 / L1)**: `createHeadlessSkillOptimizer` + `createHeadlessCandidateFrontier` composition roots + the `nexus skills optimize` / `nexus skills frontier` CLIs shipped (the CLI substance of this item; RT.P7.A/B -- coding-pillar sidecar wiring + tool-primitive relocation -- remain separate). |

### Nexus-Hub v3.10.0 adoption -- COMPLETE (Phase 0 sync + Phase 1 hardening, 2026-07-02)

A separate adoption cycle ([plans/adoption-nexus-hub-v3.10.0.md](plans/adoption-nexus-hub-v3.10.0.md)) evaluated the Hub v3.10.0 delta and drove it to completion. **Phase 0 gate CLEAN**: all six wired consumption surfaces (skills / data-index / rules / agents / commands / hooks / MCP-registry-filter) parse v3.10.0 content unchanged. **Phase 0 sync DONE**: the active `devai-hub` bundle is rotated to v3.10.0 (259 skills). **Phase 2 DEFERRED by design** (demand-gated -- see `HUB310.T2`). **Phase 1 (delivered this pass)** landed the policy-clean (local-only, zero new outbound/credentials) supply-chain + import hardening items below:

- **4.2** -- [DevAIHubSyncer.ts](../../../core/skills/DevAIHubSyncer.ts) cross-verifies the cloned bundle against the Hub's published `MANIFEST.sha256` (rides at the release-tag repo root; standard `sha256sum` text). **ADVISORY, not fail-closed** (revised after the live validation, see `HUB310.4.2.ADV`): the current upstream manifest is not EOL-deterministic, so a byte-level mismatch against a git-cloned bundle is expected and must not block a sync -- the result is surfaced for review and becomes a hard signal only once the Hub publishes a deterministic manifest. Verification is scoped to the sparse subset by iterating manifest entries (clone artifacts like `.git` are never flagged). New exports `verifyReleaseManifest` / `parseSha256Manifest`; `manifestVerification` on `SyncResult`; the `nexus skills sync` CLI prints one concise advisory line (`N/M file(s) differ ... not blocking`) or `verified N file(s)`. The injection scanner remains the fail-closed content gate. The root `MANIFEST.sha256` is fetched via cone-mode's automatic root-file inclusion, not an explicit sparse arg (see the cone-mode fix below).
- **Sparse-checkout cone-mode + canonical-LF fix (root cause, surfaced by the live-sync validation)** -- the pre-existing `defaultSparseClone` passed FILE paths (`data/skills.json`, plus the newly-added manifest) to `git sparse-checkout set`, which git **>= 2.36 rejects in cone mode** (`fatal: 'data/skills.json' is not a directory`). This silently broke every live sync on modern git (the HUB.P3.DATA index feature was only ever exercised via directory-copy test fixtures, never a real sparse-checkout). Fixed by extracting a directory-only `HUB_SPARSE_CHECKOUT_PATHS` (`data` dir instead of `data/skills.json`; the root `MANIFEST.sha256` relies on cone mode's automatic root-file inclusion) and by persisting `core.autocrlf=false` + `core.eol=lf` in the clone before sparse-checkout so the bundle materializes as canonical LF (byte-faithful to the Hub tarball + manifest, not Windows-CRLF-mangled). Verified manually against the real `bendourthe/Nexus-Hub` v3.10.0 release (clone + sparse-checkout exit 0; `MANIFEST.sha256` + `data/skills.json` both materialize) and guarded by a new unit test asserting the path list is file-arg-free.
- **4.3a** -- [installAllowlist.ts](../../../core/skills/installAllowlist.ts) `checkInstallUrl` is now **HTTPS-only** (plain `http://` rejected even for an allowlisted host); `ftp://` etc. keep the `unsupported protocol` reason; `file://` still test-mode-only.
- **4.3b** -- [SkillInstaller.ts](../../../core/skills/SkillInstaller.ts) `installSkill` records a SHA-256 **`contentHash`** of the fetched body (hash-on-import), returned on success and on a scanner block, and printed on the `nexus skills install` success line.
- **Scanner allowlist (`HUB310.SCAN` resolution)** -- [hubSkillScanAllowlist.ts](../../../core/skills/hubSkillScanAllowlist.ts) + a `ScanSuppression` mechanism on [PromptInjectionScanner.ts](../../../core/skills/PromptInjectionScanner.ts). A reviewed, narrow allowlist waives exactly the 9 known-safe (skill, rule) matches in the Hub's own security-education / config-example content (each entry waives ONE rule for ONE skill; a new/different pattern still blocks). Applied ONLY to the trusted `DevAIHubSyncer` sync source; `SkillInstaller` (third-party imports) stays fully strict. This unblocked the live `--apply`.
- **Phase 0 runtime sync (`HUB310.T0` resolution)** -- ran `nexus skills sync --apply --tag v3.10.0`: scan clean (allowlist), manifest advisory (342/687 differ, not blocking), **applied** -> active bundle `~/.nexus/skills/devai-hub/v3.10.0` (259 skills; `direct-corpus-interaction` + `agent-presets` present). Resolves the v1.5.0 carryforwards `1.1.P3.B` / `T023.P3.A`.

Verification: `tsc -b` clean, lint 0 errors, check-architecture 0 errors / 10 pre-existing warnings (+0 new orphan/cycle), check:tampering 0, catalog:check in sync, security:check in sync, check:audit-prod 0 blocking, root suite green; affected unit files (`DevAIHubSyncer` incl. the cone-mode guard + advisory + allowlist tests = 44, `PromptInjectionScanner` incl. suppression + allowlist tests = 23, `installAllowlist` +1, `SkillInstaller` +2) all green. The live `nexus skills sync --apply --tag v3.10.0` was exercised end-to-end: the cone-mode + LF fix fetched all 259 skills, the allowlist cleared the 9 security-education findings, the manifest verification ran advisory (not blocking), and the bundle applied.

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `HUB310.SCAN` | P2 | DF | **RESOLVED (2026-07-02).** The Tier 0 `--apply` sync was blocked by the injection scanner on 9 findings, all benign security-education / config-example content in the Hub's own skills (`security/ai-attack-patterns`, `security/prompt-injection-defense`, `security/skill-security-scan`, `compliance/ai-agent-governance`, `language-specialists/powershell-expert`, `infrastructure/platform-engineer`, `ai-development/claude-agent-sdk`, `workflow/skill-eval-loop`). Resolved via **option (a)**: a reviewed, surgical per-(skill, rule) allowlist ([hubSkillScanAllowlist.ts](../../../core/skills/hubSkillScanAllowlist.ts)) applied only to the trusted `DevAIHubSyncer` source; third-party imports stay strict. Each of the 9 findings was read in context and adjudicated benign before waiving. | Done. Re-review the allowlist when bumping the pinned Hub release (the `reason` fields record each adjudication). |
| `HUB310.4.2.ADV` | P2 | DF | **The Hub's published `MANIFEST.sha256` is not EOL-deterministic** -- it was generated from a Windows working tree, so some entries are hashed over CRLF and some over LF (proven: `architect.md` matches LF, `adversarial-reviewer.md` matches CRLF). No single client-side EOL setting can match a mixed manifest against a git-cloned bundle (~45% mismatch either way), which is why 4.2 is advisory rather than fail-closed. Tier: verify infrastructure `supported`; fail-closed enforcement `future`. | Upstream (Hub) fix: make `generate_manifest.py` deterministic -- normalize to LF before hashing (or run manifest generation on a Linux/LF checkout) and re-cut the release. Once the manifest is LF-deterministic, flip 4.2 back to fail-closed in a follow-up cycle. No further Nexus-AI code needed until then. |
| `HUB310.T0` | P2 | DF | **RESOLVED (2026-07-02).** Ran `nexus skills sync --apply --tag v3.10.0` after the allowlist landed: scan clean, manifest advisory (not blocking), **applied**. Active bundle is now `~/.nexus/skills/devai-hub/v3.10.0` (259 skills verified; `direct-corpus-interaction` + `agent-presets` present). Resolves the v1.5.0 carryforward `1.1.P3.B` / `T023.P3.A`. | Done. The bundle rotates forward on the next `nexus skills sync --apply` when the Hub cuts a newer release. |
| `HUB310.T2` | P3 | DF | **Tier 2 product-feature adoptions deferred.** In-product `model-routing` (local-model auto-selection) + a revisit of the Hub `extensions/` MCPs (`nexus-context-compressor` etc.), which are already covered by in-process equivalents. Tier: `future`. | Evaluate on demand per plan Sections 4.4 / 4.5; the skills themselves flow via the Tier 0 sync, so no code is required to consume them. |
| `HUB310.HUBSIDE` | P3 | DF | **Hub-side content gap (not Nexus-AI's to fix in-repo):** `catalog/rules/bash/testing.md` is absent on Hub v3.10.0 (go / python / typescript ship it). `LanguageRuleBuilder` skips a missing rule file gracefully (catch / continue), so there is no Nexus-AI defect. | Add `bash/testing.md` upstream in a future Hub cycle for language parity; no Nexus-AI action. |

### Phase 1 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `SO001.P1.A` | P2 | DF | The runner drives the agent through an injected `AgentDriver` seam; this phase does **not** ship a production driver that wires the real full `AgentLoop` end-to-end. The Coding-pillar agent loop is constructible headlessly, but two of its dependencies (`ConversationManager` and the `logger`) import `vscode` and so cannot load in a plain-Node CLI -- the same coupling that forced the v1.6.0 A4 `TraceDbReader`. The dry path (no driver) and the mock-live path (test-injected driver) are fully exercised, and the env-gated live smoke proves the live path runs against a real Ollama backend via the vscode-free `OllamaClient`; only the full real-loop driver is deferred. Tier: runner + criteria + snapshot + dry/mock-live + live-backend smoke `supported`; full real-`AgentLoop` driver `future`. | When Phase 3's `SkillOptimizer` needs real rollouts (or sooner if the desktop sidecar's headless agent path is factored out), supply a vscode-free `AgentDriver` -- either by extracting a `ConversationLike` port + a headless logger, or by reusing the sidecar's existing Node agent-driving path behind the seam -- and add an integration test that runs one real task to completion. |
| `SO001.P1.B` | P3 | NI | No `nexus golden run` CLI subcommand was added this phase. The runner is fully consumable programmatically + from tests (the acceptance was unit + integration + live-smoke tests, not a CLI), and a CLI's value is gated on the production driver (`SO001.P1.A`). The A4 `nexus trace export` pattern (a thin `.mjs` that loads compiled `out/...` artifacts) is the template when it lands. Tier: programmatic runner `supported`; `nexus golden run` CLI `future`. | Once `SO001.P1.A` lands, add a `nexus golden run [--task <id>] [--mode dry|live]` subcommand to `bin/nexus.mjs` mirroring `runTraceExport`, loading the compiled `GoldenTaskRunner` + driver. |
| `SO001.P1.C` | P3 | MT | Command-based criteria (`output_contains`, `*_passes`, `diff_matches`) shell out via the default runner; the live corpus's shell targets use unix tools (`grep`, `node -e`) that are absent or differ on Windows, so on Windows those specific criteria fail rather than evaluating. This does not affect `file_contains`/`file_exists`/`file_deleted` (the bulk of the corpus) and the runner never throws on a missing binary (fail-closed to a recorded failure). Tier: file criteria + injected-runner command criteria cross-platform `supported`; native Windows shell-command criteria `candidate`. | If Windows-native live runs become a target, normalize the corpus's shell targets (or add a portable shim) so `grep`-style criteria resolve on cmd/PowerShell, or run live evaluation inside a bundled POSIX shell. |

### Phase 2 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `SO002.P2.A` | P3 | NI | The `validationGate` gates on the aggregate **pass rate** plus per-task **pass->fail** regressions only; it does not gate on time/token regressions the way the Python `tests/golden/framework/regression.py` baseline comparator also does. Pass/fail is the load-bearing signal for "did this skill edit help", and the scored `GoldenTaskResult` carries `metrics` (duration/tokens) the gate could later read, but no metric-regression check ships this phase (the optimizer that consumes the gate does not exist until Phase 3, so there is no consumer to tune the thresholds against). Tier: pass-rate + per-task pass-regression gate `supported`; latency/token regression gating `future`. | When Phase 3's `SkillOptimizer` lands and a measured run shows edits trading correctness for speed/cost, extend `evaluateValidationGate` with optional `maxLatencyDeltaMs` / `maxTokenDelta` checks over the per-task `metrics`, mirroring `regression.py`'s time/token-efficiency bands. |
| `SO002.P2.B` | P3 | DF | The `RejectedEditBuffer` is append-only (like the `ArtifactStore` it composes): there is no garbage collection of an edit's stored content when an index row is later removed, so orphaned blobs could accumulate under the buffer dir. This mirrors `AS005.P3.A` (the dehydration store shipped without a sweep) -- buffer pruning is not a surfaced operation yet, and content-addressing means a re-recorded identical edit dedupes rather than duplicates. Tier: record/has/get/list + round-trip `supported`; orphan reclamation `future`. | When a buffer-prune or retention surface lands (or as a periodic sweep), add a mark-and-sweep that walks live index `artifactRef`s and unlinks unreferenced blobs, mirroring the `AS005.P3.A` / `CommandCompressor.pruneOldTees` discipline. |
| `SO002.P2.C` | P3 | NI | No corpus task YAML declares an explicit `split` this phase; every task's split is derived by `assignDefaultSplits` (round-robin within category by sorted id). That guarantees each split spans multiple categories but does not difficulty-stratify (the `tags` carry easy/hard but the default ignores them). The explicit per-task `split` field + override path is built and unit-tested, just unused by the shipped corpus. Tier: deterministic representative-by-category default + explicit override `supported`; difficulty-stratified / curated splits `future`. | If split balance by difficulty matters once the optimizer runs, either pin `split:` on the harder tasks in the corpus YAML (honored automatically) or extend `assignDefaultSplits` to stratify by a difficulty tag while preserving determinism. |

### Phase 3 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `SO003.P3.A` | P2 | DF | The optimizer ships **opt-in, default off** (`nexus.coding.skillOptimizer.enabled` = false). The S6 A/B (`SkillOptimizerAb` + `decideSkillOptimizerDefault`) is built and unit-tested, but no A/B run against **live local models** happened in this environment, so there is no measured net win to flip the default on -- the no-degradation gate keeps it off (the same posture as v1.6.0 `OF010.P4.A` for panel routing). Whether a small resident model produces net-positive skill edits on Nexus coding tasks remains unproven (`candidate`). Tier: opt-in optimizer + A/B harness + default gate `supported`; default-on `future` (gated on a measured net win). | When a live local A/B can run (it needs the production rollout, `SO003.P3.B`), run `runSkillOptimizerAb` over the validation split with the resident model, feed the report to `decideSkillOptimizerDefault`, and flip the package.json default only if it returns `enableByDefault: true`; record the measured report either way. |
| `SO003.P3.B` | P2 | DF | This phase does **not** wire a production `OptimizerRollout` over the real full `AgentLoop` -- the loop, the A/B, and every guardrail are exercised through injected fakes (the same vscode-coupling deferral that gave the Phase 1 runner its `AgentDriver` seam). The rollout seam is the single integration point; the live implementation depends on the deferred vscode-free agent substrate from `SO001.P1.A`. Tier: loop + seams + gate + buffer + A/B (fakes) `supported`; live rollout over the real agent `future`. | Land the `SO001.P1.A` vscode-free `AgentDriver`, then implement `OptimizerRollout.run` over the Phase 1 `GoldenTaskRunner` (materializing the candidate skill body into the agent's catalog for the override path) and add an integration test that runs one real optimization round end-to-end. |
| `SO003.P3.C` | P3 | DF | The composition-root wiring of the live `SkillEditApprovalGate` (over the webview `ConfirmationGate.request`) and `SkillPathResolver` (over `pathGuard.resolveInsideWorkspace(p, catalogRoot)`) is not done this phase -- there is no live consumer yet (same gating as the rollout). The bundled vscode-free `RootSkillPathResolver` enforces the identical catalog-root containment guarantee (fail-closed on traversal) and is unit-tested; `classifyAction` already runs in-loop. Tier: seams + bundled fail-closed defaults `supported`; live `ConfirmationGate`/`pathGuard` adapters `future`. | When the optimizer gains a desktop/CLI surface, adapt `ConfirmationGate.request` (showing the rendered edit diff) and `resolveInsideWorkspace` into the two seams; the loop already routes every overwrite through them. |
| `SO003.P3.D` | P3 | NI | No `nexus skills optimize` CLI subcommand was added this phase (mirrors `SO001.P1.B`): the optimizer is fully consumable programmatically + from tests, and a CLI's value is gated on the production rollout (`SO003.P3.B`). Tier: programmatic optimizer `supported`; CLI `future`. | Once `SO003.P3.B` lands, add a `nexus skills optimize <skillId> [--max-rounds N]` subcommand mirroring the A4 `nexus trace export` thin-`.mjs` template, loading the compiled `SkillOptimizer` + the real rollout + an approval prompt. **RESOLVED v1.12.0 (L1)**: `nexus skills optimize <id> [--apply] [--yes] [--model] [--max-rounds]` shipped. |

### Phase 4 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `SO005.P4.A` | P2 | DF | This phase does **not** wire a production `CandidateProducer` over the real Phase 3 `SkillOptimizer` (running it with varied minibatches/seeds to yield >= 2 diverse candidates) or a production `CandidateScorer` over the real Phase 1 runner -- the frontier's produce/score seams are exercised through injected fakes (the same vscode-coupling deferral that gave the Phase 1 runner its `AgentDriver` seam and the Phase 3 loop its `OptimizerRollout` seam). Both live implementations depend on the deferred vscode-free agent substrate (`SO001.P1.A`) and the production rollout (`SO003.P3.B`). Tier: frontier orchestrator + Pareto selector + cap + replacement + approval gate (fakes) `supported`; live candidate production + scoring over the real agent `future`. | Land `SO001.P1.A`/`SO003.P3.B`, then implement a `CandidateProducer` that runs `SkillOptimizer` to produce candidate bodies and a `CandidateScorer` over the Phase 1 `GoldenTaskRunner` (materializing each candidate body on its branch), and add an integration test that ranks >= 2 real candidates end-to-end. |
| `SO005.P4.B` | P3 | DF | No live `CandidatePromoter` (the winning-branch -> live-catalog merge, over `GitSafetyNet`) is wired at a composition root this phase -- there is no live consumer yet (same gating as the producer, mirrors `SO003.P3.C`). The `WorktreeCandidateManager` real adapter *materializes* candidate branches (and is unit-tested with an injected `GitRunner`), and the promoter seam is structurally reachable only after human approval, but the actual `git merge`/checkpoint of an approved branch is deferred. Tier: seams + branch materializer + no-auto-merge guarantee `supported`; live branch->catalog merge `future`. | When the frontier gains a desktop/CLI surface, implement a `CandidatePromoter` that checkpoints via `GitSafetyNet` and merges the approved candidate branch into the live catalog, and add a test that a merge happens only after approval. |
| `SO005.P4.C` | P3 | NI | No `nexus skills frontier` (candidate-management) CLI subcommand was added this phase (mirrors `SO001.P1.B`/`SO003.P3.D`): the frontier is fully consumable programmatically + from tests, and a CLI's value is gated on the production producer/scorer (`SO005.P4.A`). The `maxCandidates` cap is a constructor config the composition root sets from the hardware tier's `maxConcurrentSubAgents` (the VRAM gate); it is not auto-derived inside the vscode-free module. Tier: programmatic frontier `supported`; CLI + auto-tier-cap wiring `future`. | Once `SO005.P4.A` lands, add a `nexus skills frontier <skillId> [--max-candidates N]` subcommand mirroring the `nexus trace export` thin-`.mjs` template, defaulting `--max-candidates` from the resolved hardware tier. **RESOLVED v1.12.0 (L1)**: `nexus skills frontier <id> [--apply] [--yes] [--model] [--max-candidates]` shipped. |

### Phase 5 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `SO006.P5.A` | P2 | DF | The introspector enumerates touched paths with a **reverse-engineered structural parser**, not a tree-sitter AST parse. The bundled `tree-sitter-wasms` package ships a `tree-sitter-bash.wasm` grammar (used by the codegraph scanner) but **no PowerShell / cmd grammar**, and pulling native grammars conflicts with the no-runtime-download / reverse-engineer-first principle -- so all three dialects use the structural tokenizer, which is the always-available, fail-closed core. An AST-backed *bash* upgrade over the already-bundled grammar would harden the bash path against exotic quoting the tokenizer bails on (it fails closed, so this is a precision gain, never a safety gap). Tier: structural enumeration + fail-closed gate `supported`; tree-sitter-bash AST parse `future`. | If bash-command precision becomes a target, add an injected `BashAstParser` seam over `web-tree-sitter` + the bundled `tree-sitter-bash.wasm` (warmed like the codegraph scanner's `initTreeSitter`), have `introspectShellCommand` prefer it for the bash dialect and fall back to the structural tokenizer, and add a parity test over the corpus of bash commands. |
| `SO006.P5.B` | P3 | DF | A **workspace-boundary hard-block** for enumerated write / delete paths that resolve *outside* the workspace root was considered but not shipped: many legitimate commands write outside cwd (package managers, `git`), so an unconditional escape-block risks false positives, and `run_terminal` already sits at the DANGEROUS tier (every call confirmed) with `GitSafetyNet` checkpointing for rollback. The enumeration currently feeds the operator `.nexus/permissions.deny` gate + the dry-run surface only. Tier: denylist path-gating + dry-run enumeration `supported`; workspace-escape hard-block `future`. | If a stricter posture is wanted, add an opt-in setting that blocks a `run_terminal` write / delete whose enumerated path resolves outside the workspace root (reusing `pathGuard.resolveInsideWorkspace`), defaulting off to avoid breaking out-of-root build workflows. |
| `SO006.P5.C` | P3 | NI | Path enumeration is **over-approximate for read gating and glob targets**: an argument to a read command maps to a `read_file` deny check (and blanket `*:` rules), but read tools are not otherwise deny-gated in the tool loop, and a glob token (`*.env`) is enumerated verbatim rather than expanded, so a glob-vs-glob denylist match is approximate. This never loosens the surface (unmatched globs simply are not blocked; the DANGEROUS-tier confirmation still applies). Tier: write / delete path-gating `supported`; precise glob expansion + read-scope gating `candidate`. | If glob precision matters, expand enumerated glob tokens against the workspace (bounded) before matching, or narrow read-path gating to the cases the denylist is written for. |

### Phase 6 (FINAL) follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `SO009.P6.A` | P3 | DF | The Nexus-Hub touchpoint (plan sub-task SO009) was assessed **not warranted** as an in-repo change this cycle. The Hub already ships the *method-level* skill-authoring / loop guidance (`skill-eval-loop`, `loop-engineering`, `continuous-learning`, `skill-create`, `skill-stocktake`, `skill-description-authoring`); this cycle built the *runtime* loop those skills describe (live golden-task runner -> held-out gate -> bounded-edit optimizer -> Pareto frontier), which is Nexus-internal product code, not a portable skill artifact the Hub catalog lacks. The plan explicitly forbids duplicating loop logic into the Hub, and the Hub is a sibling repository (not editable from this repo's commit), so no cross-link is added here. This mirrors the prior two cycles' Hub-touchpoint verdicts (v1.6.0 `OF015.P5.A`, v1.6.0-aisuite `AS010.P6.A`). Tier: runtime loop as an internal capability `supported`; a Hub-side cross-link `future` (demand-gated). | If a future Hub sync wants to document this as a worked local-first example, add a one-line cross-link from the Hub's `skill-eval-loop` / `loop-engineering` SKILL.md to this cycle's plan / DEVLOG entry (a pointer only -- do not duplicate the loop logic into the Hub). |

---

## 2. Resolved

(Phase 1 SO001 + Phase 2 SO002 are recorded in the Adoption Ledger above; resolved cross-phase items will move here as later phases close.)

---

## 3. Summary

- **Phases closed**: 6 of 6 -- **cycle complete** (Phase 1 -- S1 TS-native golden-task live runner, SO001, 2026-06-29; Phase 2 -- S4 split + held-out gate + rejected-edit buffer, SO002, 2026-06-30; Phase 3 -- S2 bounded-edit optimizer + S6 optimizer-quality A/B, SO003 + SO004, 2026-06-30; Phase 4 -- S3 Pareto-frontier candidate management on git branches, SO005, 2026-07-01; Phase 5 -- O-A tree-sitter shell-command introspection for permission gating, SO006, 2026-07-01; Phase 6 FINAL -- whole-plan acceptance gate + docs + Nexus-Hub-touchpoint assessment, SO007-SO009, 2026-07-02).
- **Open forward-tier follow-ups**: 17 (`SO001.P1.A` P2 deferred full-loop driver, `SO001.P1.B` P3 no CLI yet, `SO001.P1.C` P3 Windows-native shell criteria; `SO002.P2.A` P3 pass-rate-only gate, `SO002.P2.B` P3 buffer orphan GC, `SO002.P2.C` P3 non-stratified default split; `SO003.P3.A` P2 optimizer opt-in default-off pending a live A/B, `SO003.P3.B` P2 deferred production rollout over the real AgentLoop, `SO003.P3.C` P3 deferred live ConfirmationGate/pathGuard adapters, `SO003.P3.D` P3 no optimize CLI; `SO005.P4.A` P2 deferred production candidate producer + scorer, `SO005.P4.B` P3 deferred live branch->catalog promoter, `SO005.P4.C` P3 no frontier CLI + tier-cap wiring; `SO006.P5.A` P2 tree-sitter-bash AST upgrade over the structural parser, `SO006.P5.B` P3 workspace-escape hard-block, `SO006.P5.C` P3 glob-expansion / read-scope precision; `SO009.P6.A` P3 demand-gated Hub-side cross-link) -- all `candidate`/`future`, none defects, none release-blocking. **Post-cycle update (2026-07-02)**: `SO001.P1.A`, `SO003.P3.B`, `SO005.P4.A`, `SO005.P4.B`, and `SO001.P1.B` are now RESOLVED by the headless-runtime build (see the Post-cycle section above); 3 new forward-tier follow-ups were added (`RT.P7.A` desktop-sidecar wiring, `RT.P7.B` tool-primitive relocation, `RT.P7.C` optimize/frontier CLIs). Net open forward-tier follow-ups: 15.
- **Defects / failing gates / suppressed checks**: 0. Phases 1-6 introduced no bug, test failure, coverage shortfall, suppressed lint, or bypassed quality gate. Phase 6 (FINAL) changed no feature code -- verification + close-out only: the whole-plan acceptance gate passed with root suite 4498 passed / 6 skipped / 0 failed; coverage lines 88.22% / branches 84.01% / functions 91.35% (all above the 80/75/80 CI gate); `tsc -b` clean, lint 0 errors, check-architecture 0 errors / 10 pre-existing warnings, security:check in sync, check:tampering 0 findings. All six delivered items (S1/S4/S2+S6/S3/O-A) were verified physically present. **Post-cycle (2026-07-02)**: the headless-runtime build added 35 tests (full suite 4533 passed / 6 skipped / 0 failed) and introduced no defect, failing gate, or suppressed check -- all static gates (lint, tsc, check-architecture 0-errors, security:check, check:tampering, catalog, prod-audit) stayed green.
- **Demand-gated backlog (recorded, FINAL Phase 6 SO008 sweep complete)**: S5 (background autonomous self-optimization routine, off by default) and opencode O-B (local-dir + opt-in Git-repo read-only references), O-D (ACP editor interop), O-E (guarded local plugin auto-loader) -- all recorded in the plan's Out-of-Scope appendix, none implemented this cycle; plus the Hub-side cross-link (`SO009.P6.A`) above. Each remains demand-gated and off by default; the load-bearing guardrail is "the loop proposes; the human accepts", which S5's deferral preserves.
