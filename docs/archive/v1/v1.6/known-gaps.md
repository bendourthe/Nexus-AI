# v1.6.0 -- Known Gaps, Deferrals, and Carryovers

**Status**: complete (all 6 phases closed; Phases 1-5 on 2026-06-15, the FINAL Phase 6 whole-plan acceptance gate on 2026-06-16). v1.6.0 is the "aisuite harness adoptions + Nexus-AI interactive guide" cycle ([plans/adoption-aisuite-harness.md](plans/adoption-aisuite-harness.md), derived from [comparison-aisuite.md](comparison-aisuite.md)). Phase 1 ships the user-directed headline artifact: the self-contained, offline Nexus-AI interactive guide ([../../../../guides/interactive-guide/nexus-ai-guide.html](../../../guides/interactive-guide/nexus-ai-guide.html)) -- a 7-page user guide reusing the Nexus-Hub interactive-guide design tokens and constellation background (AS001 + AS002, delivered 2026-06-14), now CI-guarded for offline integrity + reduced motion (AS003, closed 2026-06-15). Phase 2 ships A4: the standalone shareable session/trace viewer ([../../../../modules/coding/observability/TraceHtmlExport.ts](../../../modules/coding/observability/TraceHtmlExport.ts) + a `nexus trace export` CLI subcommand), a self-contained offline HTML export reusing the Phase 1 design tokens and the `InteractiveArtifact` sanitisation rules (AS004, closed 2026-06-15). Phase 3 ships A1: session-state artifact dehydration -- large message fields (>20KB) are moved out-of-line to a content-addressed, redaction-on-write artifact store under `<nexusHome>/session-artifacts/` and rehydrated on resume, with a one-way v1->v2 migration and a pre-migration-tolerant read path (AS005, closed 2026-06-15). Phase 4 ships A2: hierarchical sub-run trace nesting (AS006, closed 2026-06-15). Phase 5 ships A3: the manifest-driven, loopback-only `LocalAdapterRegistry` -- the 2 built-in adapters (Ollama, LM Studio) migrated off the hand-edited switch onto validated manifests, a new `nexus.llm.localAdapters` setting adds a local runtime by config, and non-local manifests are rejected citing the MCP Registry Policy (AS007, closed 2026-06-15 as a demand-gated forward-looking guarded refactor; no third runtime ships this cycle). Phase 6 (FINAL, 2026-06-16) ran the whole-plan acceptance gate: definition-of-pass items 1-6 all verified, every quality gate green (`npm run test` 4197 passed / 5 skipped / 0 failed, desktop suite 449 passed / 0 failed, `tsc -b` clean, lint 0 errors, check-architecture 0 errors / 10 pre-existing warnings, security:check in sync, check:tampering 0 findings), and the cycle closed (AS008-AS010). This file is appended phase-by-phase; items move to `## 2. Resolved` when closed; the `## 3. Summary` is recomputed each pass.

**Audience**: v1.6.0 phase authors, code reviewer, future-cycle planners
**Last updated**: 2026-06-16 (Phase 6 FINAL -- whole-plan acceptance gate + cycle closure AS008-AS010; plus the optimizerDuck cross-comparison candidate `O1` logged from [comparison-optimizerduck.md](comparison-optimizerduck.md))
**Sibling reviews**: [../v1.5.0/known-gaps.md](../v1.5/known-gaps.md) (the prior cycle's gap log) and [plans/adoption-aisuite-harness.md](plans/adoption-aisuite-harness.md) (the active plan).

**Cycle context**: This file is created in Phase 1 (rather than at a later phase) because the implement-phase post-phase sequence appends gaps every phase. Phase 1 introduces no bug, test failure, coverage shortfall, suppressed lint, or bypassed quality gate; the single seeded entry below is a forward-tier follow-up (`candidate`), not a defect.

**Wording convention**: every prose claim follows [../v1.4.0/development/evidence-and-support-tiers.md](../v1.4/development/evidence-and-support-tiers.md) (A7): a gap describes its unbuilt capability at tier `future` or `candidate` (never `supported`); the "Suggested next step" states what cited evidence would raise the tier. "not_observed != absent" applies throughout.

Severity tags: **P0** release-blocker; **P1** should-fix; **P2** nice-to-have; **P3** out-of-scope for v1.6.0 / recorded for future planning.
Category tags: **NI** not implemented; **DF** deferred; **BG** bug; **MT** missing tests; **WN** warning; **QG** quality gate.

---

## 0. Adoption Ledger

Per-sub-task closure ledger for the aisuite-harness adoption plan. Rows land as each phase closes.

| Plan sub-task | Item | Status | Closing reference |
|---|---|---|---|
| AS001-AS002 | H1 -- Nexus-AI interactive guide shell + content | Resolved | Phase 1 (2026-06-14); [../../../../guides/interactive-guide/nexus-ai-guide.html](../../../guides/interactive-guide/nexus-ai-guide.html) -- single self-contained 7-page user guide, design tokens + constellation canvas ported from the Nexus-Hub reference guide, all CSS/JS inline, system-stack fonts, no remote assets. |
| AS003 | H1 -- offline-integrity + reduced-motion test | Resolved | Phase 1 (2026-06-15); [../../../../tests/unit/guides/nexus-ai-guide.offline.test.ts](../../../tests/unit/guides/nexus-ai-guide.offline.test.ts) (18 cases). DOM-aware `findRemoteAssetRefs` over `node-html-parser` (no new dependency) asserts no remote asset references in load-fetched positions (`src`, `<link href>`, `srcset`, `poster`, `object[data]`, CSS `url()`/`@import`), with `data:` URIs and navigational `<a href>` correctly excluded; asserts the `#constellation` canvas is present and `aria-hidden`; asserts reduced motion takes the `frame(false)` static branch so the lone `requestAnimationFrame` (inside `loop()`) is unreachable. Positive + negative controls guard the checker. Runs in CI via the existing `test-ts` job. Verification: lint 0 errors, check:tampering 0 findings, root suite 4098 passed / 5 skipped / 0 failed, coverage 87.18% lines / 83.02% branches / 90.6% functions (above 80/75/80). |
| AS005 | A1 -- session-state artifact dehydration/hydration | Resolved | Phase 3 (2026-06-15); [../../../../core/memory/ArtifactStore.ts](../../../core/memory/ArtifactStore.ts) (content-addressed file store under `<nexusHome>/session-artifacts/`; SHA-256 key + dedupe; `redactSecrets` applied before every write so a captured secret never lands on disk; atomic temp+rename; path-traversal-safe reads) + [../../../../core/memory/sessionArtifacts.ts](../../../core/memory/sessionArtifacts.ts) (`dehydrateMessages`/`hydrateMessages`, threshold-gated at 20KB, idempotent, `{ nexusArtifact, artifact_ref, preview, kind, bytes }` marker, redacted previews, diff/patch/stderr/content kind inference) + [../../../../core/memory/migrateSessionsDehydrate.ts](../../../core/memory/migrateSessionsDehydrate.ts) (one-way v1->v2 migration: idempotent, backup-on-write, decoupled from the sidecar's session type) + [../../../../scripts/migrate-sessions-dehydrate.mjs](../../../scripts/migrate-sessions-dehydrate.mjs) CLI wrapper, wired into [../../../../desktop/sidecar/src/coding/sessionStore.ts](../../../desktop/sidecar/src/coding/sessionStore.ts) at the disk I/O boundary (dehydrate-on-persist / hydrate-on-load) so the in-memory + public `messages: string[]` surfaces are unchanged and pre-v2 inline-only files load verbatim (the tolerant read path). Tests: [../../../../tests/unit/core/memory/ArtifactStore.test.ts](../../../tests/unit/core/memory/ArtifactStore.test.ts) (7), [../../../../tests/unit/core/memory/sessionArtifacts.test.ts](../../../tests/unit/core/memory/sessionArtifacts.test.ts) (12), [../../../../tests/unit/core/memory/migrateSessionsDehydrate.test.ts](../../../tests/unit/core/memory/migrateSessionsDehydrate.test.ts) (7), [../../../../desktop/tests/coding-session-dehydration.test.ts](../../../desktop/tests/coding-session-dehydration.test.ts) (4: out-of-line round-trip, pre-migration v1 load, redaction, small-message inline), and the size-delta benchmark [../../../../tests/integration/session-state/dehydration-size.test.ts](../../../tests/integration/session-state/dehydration-size.test.ts). Verification: tsc + desktop typecheck clean, lint 0 errors, check-architecture 0 errors (core never imports modules), check:tampering 0 findings, new-module coverage 97.81% lines / 91.66% branches / 100% functions; root suite 4155 passed / 5 skipped, desktop suite 449 passed / 0 failed. |
| AS006 | A2 -- hierarchical sub-run trace nesting | Resolved | Phase 4 (2026-06-15); additive `group_id`/`parent_run_id` columns on the `spans` table of [../../../../modules/coding/observability/TraceStore.ts](../../../modules/coding/observability/TraceStore.ts) (CREATE for fresh stores + an idempotent `PRAGMA table_info`-guarded `ALTER TABLE` migration so a pre-Phase-4 DB backfills both as null = flat fallback) + a new `critic` `SpanKind`; a shared, render-agnostic nesting helper [../../../../modules/coding/observability/spanNesting.ts](../../../modules/coding/observability/spanNesting.ts) (`flattenSpanForest` returns a pre-ordered, depth-annotated forest nested by `parentRunId`, with a flat start-time fallback when no span carries run-nesting metadata, plus cycle/self-reference/orphan guards). The swarm orchestrator [../../../../modules/coding/orchestration/Orchestrator.ts](../../../modules/coding/orchestration/Orchestrator.ts) opens one trace + `groupId` per `execute()` (gated on `tracer.enabled`, so the default/ReAct path is byte-equivalent) and reuses the trace root span as the planner run, threading a `SwarmTraceContext` through [../../../../modules/coding/orchestration/DAGExecutor.ts](../../../modules/coding/orchestration/DAGExecutor.ts) so each worker stamps `parentRunId`=planner + `groupId` and each critic review emits a `critic` span nested under its worker run. [../../../../modules/coding/agents/SubAgentManager.ts](../../../modules/coding/agents/SubAgentManager.ts) `run` now takes a `SubAgentTraceContext` (replacing the dormant positional `parentTraceId`/`parentSpanId`) and returns its `runId`. Both renderers indent by depth via the shared helper: [../../../../src/panels/TraceDashboardPanel.ts](../../../src/panels/TraceDashboardPanel.ts) + webview, and the A4 [../../../../modules/coding/observability/TraceHtmlExport.ts](../../../modules/coding/observability/TraceHtmlExport.ts) (with [../../../../modules/coding/observability/TraceDbReader.ts](../../../modules/coding/observability/TraceDbReader.ts) reading the new columns tolerant of pre-migration DBs). Tests: [../../../../tests/unit/observability/spanNesting.test.ts](../../../tests/unit/observability/spanNesting.test.ts) (9), [../../../../tests/unit/observability/TraceStore.nesting.test.ts](../../../tests/unit/observability/TraceStore.nesting.test.ts) (5, incl. the pre-Phase-4 migration), [../../../../tests/unit/observability/TraceHtmlExport.nesting.test.ts](../../../tests/unit/observability/TraceHtmlExport.nesting.test.ts) (2), [../../../../tests/unit/agents/SubAgentManager.nesting.test.ts](../../../tests/unit/agents/SubAgentManager.nesting.test.ts) (3), [../../../../tests/unit/orchestration/Orchestrator.swarmTrace.test.ts](../../../tests/unit/orchestration/Orchestrator.swarmTrace.test.ts) (2), [../../../../tests/unit/orchestration/DAGExecutor.swarmTrace.test.ts](../../../tests/unit/orchestration/DAGExecutor.swarmTrace.test.ts) (3). Verification: lint 0 errors, check-architecture 0 errors, check:tampering 0 findings, `tsc -b` clean, root suite 4181 passed / 5 skipped / 0 failed, coverage spanNesting 100% lines / 96.3% branches / 100% functions, TraceStore 97.9% / 84.8%, TraceHtmlExport 100% / 90.9%, DAGExecutor 97.1% / 91.1%, Orchestrator 99.4% / 85.4%. |
| AS007 | A3 -- local-runtime adapter registry | Resolved | Phase 5 (2026-06-15); manifest-driven, loopback-only [../../../../modules/coding/llm/LocalAdapterRegistry.ts](../../../modules/coding/llm/LocalAdapterRegistry.ts) -- `validateLocalAdapterManifest` runs a strict zod schema then a loopback-only endpoint guard (`127.0.0.0/8`, `::1`, `localhost`/`ip6-localhost`/`ip6-loopback`; intentionally stricter than `ssrf.isBlockedIp`, which also matches RFC-1918 LAN), rejecting any non-loopback endpoint with an error that cites the AGENTS.md MCP Registry Policy. `LocalAdapterRegistry` maps a manifest's `protocol` (`ollama`/`openai`) to the existing `createOllamaClient`/`createLmStudioClient` factories; the 2 shipped adapters are expressed as built-in manifests rather than `if/else` branches. Wired into [../../../../modules/coding/runtime/NexusCodingRuntime.ts](../../../modules/coding/runtime/NexusCodingRuntime.ts): the runtime seeds the registry with the built-ins plus user manifests from the new `nexus.llm.localAdapters` setting ([../../../../package.json](../../../package.json) + [../../../../modules/coding/config/settings.ts](../../../modules/coding/config/settings.ts)), skipping invalid/non-local entries with a warning rather than aborting startup, rebuilds it on a live settings change, and `getOllamaClient()` delegates construction to `registry.createClient(name, ...)`; `nexus.llm.backend` widened to any string so a user-registered adapter is selectable by name. Built under `modules/coding/llm/` (not `core/registry/`) because the `no-llm-outside-llm-folder` + `no-core-from-modules` boundary rules require it there to reuse the `ssrf.ts` loopback primitive without drift. Decision in [ADR-0019](../../adr/0019-local-adapter-registry.md) (extends ADR-0016). Tests: [../../../../tests/unit/llm/LocalAdapterRegistry.test.ts](../../../tests/unit/llm/LocalAdapterRegistry.test.ts) (12) + [../../../../tests/unit/runtime/NexusCodingRuntime.test.ts](../../../tests/unit/runtime/NexusCodingRuntime.test.ts) (+4). Verification: `tsc -b` clean, lint 0 errors, check-architecture 0 errors (10 pre-existing warnings), check:tampering 0 findings, root suite 4197 passed / 5 skipped / 0 failed, coverage LocalAdapterRegistry 100% lines / 100% branches / 100% functions, NexusCodingRuntime 78.9% / 64.5% (residual = pre-existing composition-root boilerplate + platform-gated `darwin` branch). Demand-gated forward-looking refactor (user-confirmed via `/implement phase 5`); no third runtime ships this cycle. |
| AS008-AS010 | FINAL -- whole-plan acceptance gate + Nexus-Hub sync | Resolved | Phase 6 (2026-06-16); ran the whole-plan acceptance gate and verified all six definition-of-pass items. **AS008**: every gate green -- `npm run test` 4197 passed / 5 skipped / 0 failed (376 files), desktop suite 449 passed / 0 failed (53 files), `tsc -b` clean, `npm run lint` 0 errors, `npm run check-architecture` 0 errors / 10 pre-existing warnings, `npm run security:check` "All safety surfaces in sync", `npm run check:tampering` 0 findings; definition-of-pass artifacts 1-5 all present on disk (the H1 guide + offline test, the A4 `TraceHtmlExport`/`TraceDbReader` + `nexus trace export` CLI + test, the A1 `ArtifactStore`/`sessionArtifacts`/`migrateSessionsDehydrate` + `.mjs` wrapper, the A2 `spanNesting`, the A3 `LocalAdapterRegistry` + ADR-0019), item 6 (all test tiers) green. **AS009**: this known-gaps ledger closed to "all 6 phases complete"; the plan ([plans/adoption-aisuite-harness.md](plans/adoption-aisuite-harness.md)) Phase 6 sub-tasks checked off and definition-of-pass item 6 marked delivered; [../../../../docs/todos.md](../../todos.md) updated. README/ARCHITECTURE/CHANGELOG narrative sync + the npm version tag are owned by the semantic-release pipeline (`.releaserc`, `tagFormat: v${version}`) on merge to `main`, not hand-edited here. **AS010**: assessed the Nexus-Hub touchpoint -- no sync warranted this cycle (see the Phase 6 follow-ups below). The FINAL sweep kept scope to verification + closure and did **not** add the deferred in-dashboard "Export trace" button (`AS004.P2.B`), which carries forward. |
| AS004 | A4 -- standalone shareable session/trace viewer | Resolved | Phase 2 (2026-06-15); [../../../../modules/coding/observability/TraceHtmlExport.ts](../../../modules/coding/observability/TraceHtmlExport.ts) (serializer) + [../../../../modules/coding/observability/TraceDbReader.ts](../../../modules/coding/observability/TraceDbReader.ts) (vscode-free read-only store reader, since `TraceStore` transitively requires `vscode`) + `nexus trace export` CLI subcommand ([../../../../bin/nexus.mjs](../../../bin/nexus.mjs)). Lifts the Phase 1 `:root` tokens verbatim; embeds the full span list inline as JSON-safe data (`<` / `>` / `&` / U+2028 / U+2029 escaped, so an injected `</script>` cannot break out); statically pre-renders the timeline (legible with zero JavaScript) plus an inert bundled boot script (kind-filter + expand/collapse, no fetch / eval / timers / remote). All untrusted trace content is HTML-escaped into text positions, so an injected `<img onerror>` renders inert. Offline-integrity checker extracted to the shared [../../../../tests/helpers/offlineIntegrity.ts](../../../tests/helpers/offlineIntegrity.ts) and reused by [../../../../tests/unit/observability/TraceHtmlExport.test.ts](../../../tests/unit/observability/TraceHtmlExport.test.ts) (26 cases) + [../../../../tests/unit/observability/TraceDbReader.test.ts](../../../tests/unit/observability/TraceDbReader.test.ts) (5 cases, temp-file round-trip + corrupt-row tolerance). Verification: lint 0 errors, check:tampering 0 findings, check-architecture 0 errors, tsc build clean, end-to-end CLI smoke against a real SQLite trace DB green, root suite 4130 passed / 5 skipped / 0 failed, coverage TraceHtmlExport 100% lines / 88.23% branches / 100% functions and TraceDbReader 100% / 100% / 100%. |

---

## 1. Open Items

### Phase 1 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `AS003.P1.A` | P3 | MT | The reduced-motion no-animation guarantee is verified by static analysis of the boot script (the sole `requestAnimationFrame` lives in `loop()`, and `start()` takes the `frame(false)` branch when `REDUCE` is true) rather than by a live browser render. The plan prompt mentioned a "Playwright/e2e smoke" as one option, but the project ships no browser-e2e harness and adding Playwright for one static HTML file is disproportionate to the local-first / minimal-dependency ethos. Tier: offline-integrity + structural reduced-motion `supported`; live-browser render `candidate`. | If a browser-e2e harness is later introduced (e.g. for the Phase 2 A4 viewer), add a smoke that loads the guide with `prefers-reduced-motion` forced and asserts no `requestAnimationFrame` callback fires, raising the live-render half to `supported`. |

### Phase 2 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `AS004.P2.A` | P3 | MT | The A4 export's "opens offline and renders the timeline" property is verified structurally over the parsed DOM (node-html-parser asserts the static `[data-testid="timeline"]` rows, span names/kinds/durations, inline-JSON round-trip, and zero remote refs) rather than by a live browser render of the bundled boot script's filter / expand interactions. This mirrors the AS003.P1.A rationale: the project ships no browser-e2e harness. The static-first render means the timeline is legible with zero JavaScript, so the structural check covers the load-bearing acceptance; only the JS-enhancement interactions (kind-filter chips, expand/collapse) are unverified by a live render. Tier: offline-integrity + static-timeline render + sanitisation `supported`; live JS-interaction render `candidate`. | When a browser-e2e harness lands (shared with AS003.P1.A), add a smoke that opens an exported viewer offline, clicks a kind-filter chip, and asserts spans of other kinds become `hidden`, raising the JS-interaction half to `supported`. |
| `AS004.P2.B` | P3 | Resolved | **RESOLVED 2026-06-23** (later-cycle pickup). Originally (Phase 2) only the CLI subcommand (`nexus trace export`) shipped; the in-dashboard "Export trace" button is now wired in [../../../../src/panels/TraceDashboardPanel.ts](../../../src/panels/TraceDashboardPanel.ts) (`serializeTrace` + `_handleExportTrace`, reusing the same `serializeTraceToHtml` serializer as the CLI) with the webview control in [../../../../src/panels/webview/traceDashboard.ts](../../../src/panels/webview/traceDashboard.ts) (an "Export" button shown in the trace-detail view that posts `exportTrace` over the existing message channel); the handler prompts via `vscode.window.showSaveDialog` and writes through `vscode.workspace.fs.writeFile` (local-only, no network). Tests: [../../../../tests/unit/panels/TraceDashboardPanel.export.test.ts](../../../tests/unit/panels/TraceDashboardPanel.export.test.ts) (8 cases: serialize round-trip, missing-trace + no-store nulls, save-dialog write + success message, default `nexus-trace-<shortId>.html` filename, cancel no-op, write-failure surfaced). Verified: tsc -b clean, lint 0 errors, check-architecture 0 errors / 10 pre-existing warnings, full suite 4309 passed / 5 skipped / 0 failed. Tier: CLI export + in-dashboard one-click export both `supported`. | Done -- no further step. |

### Phase 3 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `AS005.P3.A` | P3 | DF | The `ArtifactStore` is content-addressed and append-only: when a session is overwritten or deleted, the artifacts it referenced are not garbage-collected, so orphaned blobs can accumulate under `<nexusHome>/session-artifacts/`. This mirrors how `CommandCompressor` tee files were initially append-only before `pruneOldTees` was added; the dehydration store ships without an equivalent sweep because session deletion is not yet a surfaced operation and content-addressing means re-saved identical content dedupes rather than duplicates. Tier: dehydrate/hydrate + migration `supported`; orphan reclamation `future`. | When a session-delete surface lands (or as a periodic sweep at sidecar startup, mirroring `CommandCompressor.pruneOldTees`), add a mark-and-sweep that walks live session refs and unlinks unreferenced artifacts. |
| `AS005.P3.B` | P3 | MT | Dehydration operates at whole-message granularity (any message string over the byte threshold) rather than on structured named sub-fields, because the Coding-pillar session persists each turn as an opaque string (`messages: string[]`), not a record with discrete `stdout`/`stderr`/`diff`/`patch`/`content` fields. The `kind` is inferred from content instead. This matches the source comparison's "large message fields (>20KB)" description, but if message turns later become structured records, per-field dehydration (and a precise `kind`) would be tighter. Tier: whole-message dehydration `supported`; per-named-field dehydration `future`. | If/when the session message shape becomes a structured record, extend `dehydrateMessages` to walk named fields and dehydrate each over-threshold field independently, carrying the field name as `kind`. |

### Phase 4 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `AS006.P4.A` | P3 | MT | The Trace Dashboard's depth-indented render is verified via the shared `flattenSpanForest` helper unit tests (nested + flat fallback), the panel's depth-annotated payload, and the A4 export's parsed-DOM indentation -- not by a live VS Code webview render of the indented waterfall. This mirrors the AS003.P1.A / AS004.P2.A rationale: the project ships no webview-e2e harness, and the nesting/render logic lives in a pure, fully-tested TS helper the webview consumes. Tier: nesting logic + payload + export-DOM render `supported`; live webview render `candidate`. | If a VS Code webview-e2e harness later lands, add a smoke that opens the dashboard on a swarm trace and asserts worker/critic `.span-row`s carry the expected `.span-indent` width, raising the live-render half to `supported`. |
| `AS006.P4.B` | P3 | DF | The planner run reuses the trace's auto-created root span (name `root`, kind `agent_turn`) rather than a dedicated, relabeled `planning` span; the planner -> worker -> critic topology is correct and legible but the top node's label is generic. Separately, a worker sub-run renders as a single `sub_agent` span because the sub-agent's internal `AgentLoop` runs without a tracer (its tool/LLM calls are not traced as children), so the tree is 3 levels deep, not deeper. Tier: planner/worker/critic run-nesting `supported`; relabeled planner node + intra-worker span subtree `future`. | When richer intra-run detail is wanted, give the planner a dedicated labeled span (ending it in `execute()`'s finally) and wire the sub-agent `AgentLoop` to the shared tracer with the worker `sub_agent` span as parent, so each worker expands into its own tool/LLM subtree. |
| `AS004.P2.B` (carry) | P3 | Resolved | The in-dashboard "Export trace" button was deferred through Phase 4 (which kept its scope to A2 sub-run nesting). **RESOLVED 2026-06-23** as a later-cycle pickup -- see the primary `AS004.P2.B` row in the Phase 2 follow-ups section above for the closing reference. | Done. |

### Phase 5 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `AS007.P5.A` | P3 | DF | Adapter discovery reads manifests from the `nexus.llm.localAdapters` settings array only; the plan prompt also floated a directory of `*.json` manifest files (`<nexusHome>/adapters/`). The settings array is the lower-risk, fully-unit-testable surface and covers the "add a local runtime by config" payoff; a filesystem auto-discovery sweep adds fs I/O + lifecycle for no exercised consumer this cycle (no third runtime ships). Tier: settings-array discovery `supported`; filesystem manifest-directory discovery `future`. | If a packaged third-party local adapter ever wants drop-in registration, add a `loadAdapterManifests(dir)` helper that reads `<nexusHome>/adapters/*.json` and feeds each through `registry.tryRegister`, mirroring the existing skip-and-warn discipline. |
| `AS007.P5.B` | P3 | NI | The registry maps two wire protocols (`ollama`, `openai`) to the two existing client factories; a genuinely new wire shape (e.g. a raw llama.cpp `/completion` protocol) still needs a new `LLMClient` implementation plus a `protocol` enum entry -- the registry removes the per-runtime switch edit, not the per-protocol adapter. This is by design (no speculative protocol adapters built without demand). Tier: same-protocol registration `supported`; new-protocol adapter `future`. | When a runtime with an unsupported wire shape is needed, implement its `LLMClient`, add the `protocol` value to `LocalAdapterProtocol` + the `createClient` switch, and document it; existing manifests are unaffected. |
| `AS007.P5.C` | P3 | MT | `NexusCodingRuntime` coverage is 64.5% branches: the `process.platform === "darwin"` branch in `_resolveBackend` and several OR short-circuits in the settings-change cache-invalidation guard are unexercised. The darwin branch is platform-gated (the CI host is not macOS) and was equivalently uncovered before this phase; the new registry logic (`_buildAdapterRegistry`, registry routing, live-reconfigure rebuild) is covered. Tier: registry wiring `supported`; full composition-root branch coverage `candidate`. | Stub `process.platform` in a focused test to cover the macOS auto-resolution branch, and add change-handler cases flipping `ollamaUrl`/`requestTimeout` to cover the remaining guard short-circuits. |

### Phase 6 follow-ups (FINAL -- carryovers, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `AS004.P2.B` (carry) | P3 | Resolved | The in-dashboard "Export trace" button was deferred through the Phase 6 FINAL sweep (verification + cycle closure only). **RESOLVED 2026-06-23** as a later-cycle pickup -- the "Export" webview action now ships in [../../../../src/panels/TraceDashboardPanel.ts](../../../src/panels/TraceDashboardPanel.ts); see the primary `AS004.P2.B` row in the Phase 2 follow-ups section above for the full closing reference. | Done. |
| `AS010.P6.A` | P3 | DF | AS010 assessed whether the v1.6.0 guide/viewer artifacts warrant a Nexus-Hub touchpoint (e.g. an `html-output-conventions` cross-link). No sync was performed this cycle: both artifacts are self-contained, project-local Nexus-AI deliverables that reuse the Nexus-Hub *reference* guide's design tokens (a one-way dependency already documented in the plan), and they introduce no reusable Hub skill, command, or convention that the Hub catalog lacks. A cross-link is optional polish, not a missing capability. Tier: project-local artifacts `supported`; bidirectional Hub cross-reference `future`. | If the Hub later grows a "shareable artifact" pattern page, add a one-line cross-link from the Hub's `html-output-conventions` skill to this guide/viewer as a worked local-first example. |

### Cross-comparison candidates (forward-tier, not defects)

These items originate from this cycle's *other* single-source comparisons, not from the aisuite-harness plan's phases. They are recorded here so a future-cycle planner picks them up; none is a defect or an in-flight commitment.

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `O1` (optimizerDuck) | P3 | NI | From [comparison-optimizerduck.md](comparison-optimizerduck.md): a typed, per-action **revert ledger** ("mutation journal") with **selective** one-click rollback over destructive mutations that `GitSafetyNet`'s coarse, repo-scoped `git reset --hard` cannot undo -- out-of-repo / terminal side effects, non-git workspaces, and the `write/edit/create`/MCP actions [../../../../modules/coding/guardrails/ActionClassifier.ts](../../../modules/coding/guardrails/ActionClassifier.ts) flags `DESTRUCTIVE` but sets `requiresCheckpoint:false`. Local-only, zero-outbound, zero-credential, zero-new-processor (clean under the MCP Registry Policy). The comparison's other findings are convergent strengths already shipped (risk-rated actions, manifest discovery, no-telemetry, build integrity) and need no work; O1 is the lone granularity/coverage gap. Tier: coarse session checkpoint + git rollback `supported`; per-action selective revert over non-git state `candidate`. **Sequenced strictly after the queued [adoption-openrouter-fusion](plans/adoption-openrouter-fusion.md) plan; not a competing cycle.** | In a later cycle (after Fusion), build a `MutationJournal` under [../../../../modules/coding/guardrails/](../../../modules/coding/guardrails) capturing typed pre-state for `DESTRUCTIVE` actions, reusing the A1 [../../../../core/memory/ArtifactStore.ts](../../../core/memory/ArtifactStore.ts) (content-addressed + `redactSecrets`-on-write) and `ActionClassifier`'s `requiresCheckpoint` signal; scope it to the non-git / per-action cases so it does not duplicate `GitSafetyNet`; add a mark-and-sweep mirroring `AS005.P3.A` to bound disk. |

---

## 2. Resolved

(none beyond the Adoption Ledger rows above)

---

## 3. Summary

| Metric | Count |
|---|---|
| Phases closed | 6 of 6 (Phases 1-6 -- cycle complete) |
| Adoption sub-tasks resolved | AS001, AS002, AS003, AS004, AS005, AS006, AS007, AS008, AS009, AS010 |
| Open items | 11 (`AS003.P1.A`, `AS004.P2.A`, `AS005.P3.A`, `AS005.P3.B`, `AS006.P4.A`, `AS006.P4.B`, `AS007.P5.A`, `AS007.P5.B`, `AS007.P5.C`, `AS010.P6.A`, `O1` -- all P3 / forward-tier; `O1` is a cross-comparison candidate, the rest aisuite-harness carryovers). `AS004.P2.B` resolved 2026-06-23 (later-cycle pickup -- in-dashboard "Export trace" button). |
| Defects / quality-gate bypasses | 0 |

---

> **Merged in v1.10.0 Phase 8 (T043 -- one known-gaps.md per minor).** The v1.6.0 cycle ran two plans as deliberately-distinct ledgers; the openrouter-fusion companion ledger (formerly `known-gaps-openrouter-fusion.md`) is preserved verbatim below.

# v1.6.0 (openrouter-fusion) -- Known Gaps, Deferrals, and Carryovers

**Status**: COMPLETE (Phase 5 of 5 closed 2026-06-17; all five phases landed). This is the gap ledger for the **local panel + judge-fusion** companion plan ([plans/adoption-openrouter-fusion.md](plans/adoption-openrouter-fusion.md), derived from [comparison-openrouter-fusion.md](comparison-openrouter-fusion.md)). It is kept **separate** from the sibling [known-gaps.md](known-gaps.md) (which is owned by the aisuite-harness plan and already closed) because the two plans run as distinct ledgers in the same cycle; this file is seeded in Phase 1 because the implement-phase post-phase sequence appends gaps every phase. Phase 1 shipped F1: a `fuse` skill emitting the structured judge-fusion schema and an upgrade of `council`'s synthesis to the same analysis vocabulary. Phase 2 shipped F2 + F5: a `FusionAgent` (the judge, consuming the F1 schema) and a `PanelExecutor` that fans one prompt across N distinct registry models (sequential fan-out MVP) and fuses the survivors, with the judge hardened as an untrusted-input boundary (candidate text redacted + treated as data, single-judge SPOF documented) and panelists granted no per-panelist tools. Phase 3 ships F3: `GpuScheduler.enqueuePanel` admits a bounded panel as one scheduler job that runs concurrently when summed VRAM fits free VRAM and **degrades to sequential** when it does not (no OOM, no rejection), under a hard panel-size cap, with `ModelPinRegistry.holdForPanel` keeping the panel's models resident for the run and releasing them after. Phase 4 ships F4: a local A/B harness (`PanelAbHarness`) that measures a small-model panel against the single best resident model on coding tasks; an opt-in `nexus.llm.panelRouting` routing heuristic (`PanelRouter` + the pure `decidePanelRoute`) that escalates a reliability-flagged task to the panel; and the Phase 3 co-residency primitive wired into `PanelExecutor` (optional `concurrency` backend through `GpuScheduler.enqueuePanel`, closing OF007.P3.A's parallelised-run-path gap). Per the no-degradation gate the routing default ships **opt-in (off)**: the A/B has not been run against live local models in this environment, so no measured net win exists to flip it (recorded as `OF010.P4.A`). Phase 5 (FINAL) ran the whole-plan acceptance gate -- all six definition-of-pass items verified, no feature code changed -- and closed the plan: OF013 (acceptance gate, recorded in Section 4 below), OF014 (docs: this ledger finalized; README/ARCHITECTURE/CHANGELOG narrative + the npm version tag are semantic-release-owned and cut on merge to `main`, matching the sibling aisuite-harness Phase 6 and every P1-P4 DEVLOG note), OF015 (Nexus-Hub touchpoint assessed -- not warranted, recorded as `OF015.P5.A`).

**Audience**: openrouter-fusion phase authors, code reviewer, future-cycle planners
**Last updated**: 2026-06-17 (v2.0.0 prep -- resolved the forward-tier carryovers `OF010.P4.A` live A/B run, `OF011.P4.A` live route wiring, `OF004.P2.A` live consumer, `ENV.P5.A` prod-audit, `ENV.P1.A` catalog; see Section 3. Prior: 2026-06-17 Phase 5 FINAL -- whole-plan acceptance gate + cycle close-out, OF013-OF015)
**Sibling ledgers**: [known-gaps.md](known-gaps.md) (the aisuite-harness plan's gap log; see its `O1` cross-comparison candidate, which is sequenced strictly after this plan) and [plans/adoption-openrouter-fusion.md](plans/adoption-openrouter-fusion.md) (the active plan).

**Cycle context**: Phase 1 introduces no bug, test failure, coverage shortfall, or bypassed quality gate. It adds two prompt-asset markdown files plus one test; no `.ts` source, dependency, credential, or outbound call. The two seeded entries below are a soft-warning follow-up (`OF002.P1.A`, introduced by the required council edit) and a pre-existing environmental note (`ENV.P1.A`, not owned by this plan).

**Wording convention**: every prose claim follows [../v1.4.0/development/evidence-and-support-tiers.md](../v1.4/development/evidence-and-support-tiers.md): a gap describes its unbuilt capability at tier `future` or `candidate` (never `supported`); the "Suggested next step" states what cited evidence would raise the tier.

Severity tags: **P0** release-blocker; **P1** should-fix; **P2** nice-to-have; **P3** out-of-scope for this plan / recorded for future planning.
Category tags: **NI** not implemented; **DF** deferred; **BG** bug; **MT** missing tests; **WN** warning; **QG** quality gate.

---

## 0. Adoption Ledger

Per-sub-task closure ledger for the openrouter-fusion adoption plan. Rows land as each phase closes.

| Plan sub-task | Item | Status | Closing reference |
|---|---|---|---|
| OF001 | F1 -- `fuse` skill + fusion output schema | Resolved | Phase 1 (2026-06-16); [../../../../modules/coding/skills/catalog/fuse/SKILL.md](../../../modules/coding/skills/catalog/fuse/SKILL.md) -- judge skill ingesting N labeled candidate answers and emitting the five-section structured analysis (Consensus / Contradictions / Partial coverage / Unique insights / Blind spots) followed by a grounded Fused answer, with reconcile-not-average discipline and a candidate-text-as-untrusted-data instruction (F5 groundwork consumed in Phase 2). Front-matter mirrors `council`; `metadata.related_skills: [council, critique, lens]`. ASCII, logical punctuation, no em-dashes. |
| OF002 | F1 -- align `council` synthesis with the fuse schema | Resolved | Phase 1 (2026-06-16); [../../../../modules/coding/skills/catalog/council/SKILL.md](../../../modules/coding/skills/catalog/council/SKILL.md) -- the Synthesis section now reconciles the three passes through the shared consensus / contradictions / blind-spots vocabulary **before** committing to its decision output (the SHIP / SHIP-WITH-CHANGES / DEFER / DROP verdict + 1-3 acceptance criteria + 1-3 explicit risks are preserved), with a one-line cross-link noting `fuse` generalises the same synthesis over distinct models; `fuse` added to `metadata.related_skills`; latency note kept. |
| OF003 | F1 -- schema-conformance check for the fuse output | Resolved | Phase 1 (2026-06-16); [../../../../tests/unit/skills/fuse.test.ts](../../../tests/unit/skills/fuse.test.ts) (8 cases) -- loads the real catalog `fuse` skill, asserts its prompt declares all six sections in order and the reconcile / labeled-candidate / untrusted-input discipline, and validates recorded/mock judge outputs (well-formed, gracefully-degraded malformed-candidate-set, and broken/empty/out-of-order negative controls) against a pure, total `validateFusionOutput` conformance helper -- no live model call. Auto-runs in CI via the existing `test-ts` job (the `vitest` config already globs `tests/unit/**`). The catalog-count assertion in [../../../../tests/integration/commands/skill-execution.test.ts](../../../tests/integration/commands/skill-execution.test.ts) was bumped 16 -> 17 for the new skill. |
| OF004 | F2 -- panel executor over distinct registry models | Resolved | Phase 2 (2026-06-16); [../../../../modules/coding/orchestration/PanelExecutor.ts](../../../modules/coding/orchestration/PanelExecutor.ts) -- fans one prompt across N **distinct** model ids (de-duplicated, resolver-gated, hard panel-size-capped) via an injectable `LLMClientFactory` over the `modules/coding/llm` port, collecting a labeled `{ model, answer, ok }` candidate from each in **sequential** fan-out (single-GPU MVP; F3/Phase 3 parallelises), then fuses the survivors through the `FusionAgent` judge. Built on the existing orchestration spine (mirrors the CriticAgent/DAGExecutor split). |
| OF005 | F5 -- judge as untrusted-input boundary + tool isolation | Resolved | Phase 2 (2026-06-16); [../../../../modules/coding/orchestration/FusionAgent.ts](../../../modules/coding/orchestration/FusionAgent.ts) -- the judge half. Candidate text reaches the judge as DATA: each answer is wrapped in an explicit `<<<CANDIDATE>>>` block, the instruction (lifted verbatim from the F1 `fuse` skill via `loadFusePrompt`) and the user turn both carry the "data, not instructions" defense, and every candidate is run through [redactSecrets](../../../core/observability/redactSecrets.ts) before embedding (a panelist's captured output cannot leak a secret into the judge's context). The single-judge SPOF is documented in the module header. Panelists are dispatched with **no** `tools` array (no per-panelist grant, never an open-internet default = dropped item D2); any tool use must flow through Nexus's existing gated AgentLoop surface. |
| OF006 | F2/F5 -- comprehensive dispatch / fusion / isolation tests | Resolved | Phase 2 (2026-06-16); [../../../../tests/unit/orchestration/FusionAgent.test.ts](../../../tests/unit/orchestration/FusionAgent.test.ts) (19 cases) + [../../../../tests/unit/orchestration/PanelExecutor.test.ts](../../../tests/unit/orchestration/PanelExecutor.test.ts) (12 cases) + [../../../../tests/integration/orchestration/PanelFusion.test.ts](../../../tests/integration/orchestration/PanelFusion.test.ts) (1 end-to-end case wiring PanelExecutor + the real FusionAgent + the real catalog `fuse` prompt). Covers distinct-model dispatch with the same prompt, candidate labeling, de-duplication, sequential ordering, one-panelist-failure resilience (survivors still fuse), the F5 injection-confinement + redaction + no-per-panelist-tools cases, resolver gating, and the panel-size cap. Mock `LLMClient`s only -- no live model. New-module coverage: FusionAgent 99.24% lines / 100% funcs, PanelExecutor 100% / 100% (>= 80 gate). Auto-runs in CI via the existing `test-ts` job. |
| OF007 | F3 -- VRAM-gated, capped panel co-residency in the GpuScheduler | Resolved | Phase 3 (2026-06-16); [../../../../core/scheduler/GpuScheduler.ts](../../../core/scheduler/GpuScheduler.ts) -- new `enqueuePanel(panel)` admits a bounded set of models as **one** scheduler job (preserving the single-active-job GPU ceiling for all non-panel workloads), reads free VRAM at run start, and runs the members **concurrently** when their summed `estimatedVramGB` fits or **degrades to sequential** fan-out (peak reservation = the largest single member, never the sum) when it does not. Never OOMs and -- unlike a plain `enqueue`, which still rejects a single over-budget job -- never rejects on summed VRAM. A hard panel-size cap (`DEFAULT_PANEL_SIZE_CAP = 3`, overridable per panel via `maxPanelSize`) drops members beyond it (reported in the outcome's `droppedByCap`). One member throwing is captured as a non-`ok` result so the panel survives. Emits a `panel.scheduled` telemetry marker carrying the mode + size. The `enqueue`/`enqueuePanel` shared admission path was extracted into a private `_admit` helper (existing single-model scheduling behavior unchanged). |
| OF008 | F3 -- ModelPinRegistry keep-alive coordination for a panel run | Resolved | Phase 3 (2026-06-16); [../../../../core/registry/ModelPinRegistry.ts](../../../core/registry/ModelPinRegistry.ts) -- new `holdForPanel(models)` places a transient, **ref-counted**, in-memory (never persisted) keep-alive hold so held models report `keepAliveFor === -1` (kept resident) for the run's duration, released via the returned handle (idempotent) afterward. The hold is layered **over** the pin set: a user's explicit pin (`-1`) survives a panel hold/release unchanged, and overlapping panels sharing a model release independently. The registry structurally satisfies the scheduler's `PanelKeepAliveCoordinator` port, so `enqueuePanel({ keepAlive: registry })` holds for the run and releases in a `finally`; the scheduler depends only on the minimal port, never the concrete registry. |
| OF009 | F3 -- VRAM-gate / OOM-degrade / concurrency / keep-alive tests | Resolved | Phase 3 (2026-06-16); [../../../../tests/unit/core/scheduler/GpuScheduler.test.ts](../../../tests/unit/core/scheduler/GpuScheduler.test.ts) (+10 panel cases) + [../../../../tests/unit/core/registry/ModelPinRegistry.test.ts](../../../tests/unit/core/registry/ModelPinRegistry.test.ts) (+7 hold cases) + [../../../../tests/integration/scheduler/panel-residency.test.ts](../../../tests/integration/scheduler/panel-residency.test.ts) (3 end-to-end cases wiring the real GpuScheduler + the real ModelPinRegistry). Covers concurrent residency when VRAM fits, degrade-to-sequential (no OOM, no rejection) when it does not, the panel-size cap (explicit + default) and `droppedByCap`, one-member-failure resilience, the empty-panel guard, the `panel.scheduled` telemetry, the single-active-job ceiling (a regular job waits for the panel), and the keep-alive lifecycle (held `-1` during the run, released after, user pin + env defaults preserved). Mock VRAM source; no live model. New-code coverage: GpuScheduler 99.07% lines / 100% funcs, ModelPinRegistry 100% / 100% (>= 80 gate). Auto-runs in CI via the existing `test-ts` job. |
| OF010 | F4 -- local A/B harness (panel vs single best resident model) | Resolved | Phase 4 (2026-06-17); [../../../../modules/coding/orchestration/PanelAbHarness.ts](../../../modules/coding/orchestration/PanelAbHarness.ts) (pure aggregation + decision: `scoreByKeywords` local oracle, `compareArm`/`buildAbReport`, `measurePanelRun` adapter over a `PanelRunResult`, `runAbHarness` orchestration, and `decidePanelRoutingDefault` -- the no-degradation gate) + the live runner [../../../../scripts/run-panel-ab.mjs](../../../scripts/run-panel-ab.mjs) (gated on `OLLAMA_URL`, writes a structured report to `tests/benchmarks/results/`, gitignored). F5 eval integrity: each task's keyword oracle scores the answer **after** generation and is never concatenated into the prompt; panelists get no tools, so no arm can reach the expected output (the local analogue of OpenRouter's DRACO source-exclusion fix). Local-only, no cloud. New-module coverage: PanelAbHarness 100% lines / 100% funcs / 100% branch (>= 80 gate). |
| OF011 | F4 -- opt-in budget-panel routing heuristic | Resolved | Phase 4 (2026-06-17); [../../../../modules/coding/llm/PanelRouter.ts](../../../modules/coding/llm/PanelRouter.ts) -- the pure `decidePanelRoute` (escalate to a panel only when the opt-in switch is on AND the task is flagged for higher reliability AND >= 2 distinct panelists exist; otherwise the single model) + the `PanelRouter` wrapper that delegates a `panel` decision to an injected `PanelExecutor` (which the composition root builds with the Phase 3 `GpuScheduler` concurrency backend). Setting `nexus.llm.panelRouting` (default `false`) added to [../../../../modules/coding/config/settings.ts](../../../modules/coding/config/settings.ts) (`panelRoutingEnabled`) + [../../../../package.json](../../../package.json) with an honest latency disclosure (the local analogue of Fusion's 2-3x). The shipped default (`PANEL_ROUTING_SHIPPED_DEFAULT = false`) is the recorded A/B decision (`OF010.P4.A`): opt-in until a measured net win. New-module coverage: PanelRouter 100% lines / 100% funcs / 100% branch. Also closes OF007.P3.A's parallelised-run gap: [../../../../modules/coding/orchestration/PanelExecutor.ts](../../../modules/coding/orchestration/PanelExecutor.ts) gained an optional `concurrency` backend that routes the fan-out through `GpuScheduler.enqueuePanel({ keepAlive })`. |
| OF012 | F4 -- routing/harness tests + recorded A/B decision | Resolved | Phase 4 (2026-06-17); [../../../../tests/unit/orchestration/PanelAbHarness.test.ts](../../../tests/unit/orchestration/PanelAbHarness.test.ts) (17 cases) + [../../../../tests/unit/llm/PanelRouter.test.ts](../../../tests/unit/llm/PanelRouter.test.ts) (10 cases) + [../../../../tests/unit/orchestration/PanelExecutorConcurrency.test.ts](../../../tests/unit/orchestration/PanelExecutorConcurrency.test.ts) (5 cases, fake scheduler) + [../../../../tests/integration/orchestration/PanelRouting.test.ts](../../../tests/integration/orchestration/PanelRouting.test.ts) (3 end-to-end cases wiring the REAL GpuScheduler + REAL ModelPinRegistry into PanelExecutor: concurrent-when-VRAM-fits, degrade-to-sequential-when-not, and PanelRouter end-to-end). Covers all `decidePanelRoute` branches (disabled / not-flagged / too-small / panel), the enabled-vs-disabled route, the default-state derivation from the A/B (net-win-on, no-net-win-off, quality-below-off, latency-exceeds-off, empty-off), and the concurrency candidate-mapping + cap + defensive failed-member path. Mock LLM clients only; no live model. The A/B decision (`OF010.P4.A`) is recorded in this ledger + the DEVLOG. Full suite 4291 passed / 5 skipped / 0 failed; `tsc -b` + lint + check:tampering (0) clean; check-architecture 0 errors (10 pre-existing warnings, no new orphan/circular). |
| OF013 | FINAL -- whole-plan acceptance gate (def-of-pass 1-6) | Resolved | Phase 5 (2026-06-17); all six definition-of-pass items verified (recorded in Section 4 below). Gates: `npm run test` 4291 passed / 5 skipped / 0 failed; `npm run lint` 0 errors; `tsc -b` clean; `npm run check-architecture` 0 errors (10 pre-existing warnings, no new orphan/circular); `npm run security:check` in sync; `npm run check:tampering` 0 findings; `npm run check:prompts` 0 errors (2 pre-existing oversized warnings, `OF002.P1.A`). `npm run check:audit-prod` flags 2 pre-existing prod advisories (dompurify, protobufjs) not owned by this plan -- recorded as `ENV.P5.A`. No feature code changed in Phase 5 (verification + close-out). |
| OF014 | FINAL -- docs (README/ARCHITECTURE/CHANGELOG/known-gaps) | Resolved | Phase 5 (2026-06-17); this per-plan known-gaps ledger is finalized (acceptance-gate record + `ENV.P5.A` + `OF015.P5.A` + summary). README/ARCHITECTURE/CHANGELOG narrative + the npm version tag are **semantic-release-owned** and cut on merge to `main` from the conventional commits (P1-P5), matching the sibling [aisuite-harness Phase 6 FINAL](known-gaps.md) and every P1-P4 DEVLOG note; not hand-edited here (manual edits would fight the generator). The plan + DEVLOG + `docs/todos.md` carry the per-phase narrative. |
| OF015 | FINAL -- Nexus-Hub touchpoint (if warranted) | Resolved | Phase 5 (2026-06-17); assessed -- **not warranted** (recorded as `OF015.P5.A`). The local panel-fusion capability is internal Nexus-AI code (PanelExecutor/FusionAgent/PanelRouter/PanelAbHarness), not a portable Hub skill/command; the Hub's `agent-orchestration-primitives` + `competitive-generation` skills already describe the pattern at the orchestration-primitive altitude, and the on-device analogue does not change their guidance. Same disposition as the sibling plan's `AS010.P6.A`. No change pushed to the separate `bendourthe/Nexus-Hub` repo. |

---

## 1. Open Items

### Phase 1 follow-ups (forward-tier / environmental, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `OF002.P1.A` | P3 | WN | The OF002-required additions (the consensus / contradictions / blind-spots synthesis vocabulary plus the `fuse` cross-link) pushed `council/SKILL.md`'s prompt body from ~790 to ~881 tokens, over the soft 800-token `prompt-oversized` budget reported by `npm run check:prompts`. This is a **warning, not an error**: the gate exits 0 (warnings do not flip the exit code), and the sibling `review-pr` skill already ships at ~811 tokens as an accepted warning. Trimming pre-existing council prose to compensate would be out of scope (changed lines must trace to the request); the added content is mandated by OF002. Tier: synthesis-vocabulary upgrade `supported`; under-budget prompt `future`. | If a hard token budget is ever enforced on the catalog, extract council's three-pass synthesis-vocabulary guidance into a shared reference snippet (or tighten the three pass-description sections), rather than dropping the fusion alignment. |
| `ENV.P1.A` | P3 (Resolved) | QG | **Resolved (v2.0.0 prep).** `npm run catalog` was run and the regenerated [../../../../docs/index.md](../../index.md) is committed in this change. The `src/panels` LOC line now reflects the OF011 `ChatController` / `ChatPanelBootstrap` additions (and the prior drift), so `npm run catalog:check` passes -- this change legitimately owns the `src/panels` LOC delta. Tier: catalog in sync `supported`. | None (closed). |

### Phase 2 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `OF004.P2.A` | P3 (Resolved) | NI | **Resolved (v2.0.0 prep).** The `PanelExecutor` / `FusionAgent` capability now has a LIVE consumer: `ChatController.submitUserMessage` consults the `ChatPanelBootstrap`-constructed opt-in `PanelRouter`, which invokes the scheduler-backed `PanelExecutor` (see `OF011.P4.A`). No longer "built ahead of its live consumer". Tier: F2 capability + router + live route `supported`. | None (closed). See `OF011.P4.A`. |
| `OF005.P2.A` | P3 | NI | The Phase 2 MVP runs each panelist as a **single chat completion** (no autonomous tool loop), so the F5 tool-isolation guarantee is currently vacuous-by-construction (panelists are granted no tools at all). If a future cycle lets panelists run the gated AgentLoop tool surface to gather context before answering, the "route through the existing gated registry, no per-panelist grant" contract must be re-verified against that live tool path, not just the no-tools dispatch. Tier: no-per-panelist-grant contract `supported` for the completion MVP; the gated-tool-loop panelist `future`. | When/if panelists gain tool access, add an integration test that dispatches a panelist through the real `ToolRegistry` + `.nexus/permissions.deny` denylist and asserts a denied tool is refused with no per-panelist override. |

### Phase 3 follow-ups (forward-tier, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `OF007.P3.A` | P3 (Resolved) | NI | **Resolved in Phase 4 (OF011).** `PanelExecutor` gained an optional `concurrency` backend: when supplied a `GpuScheduler` (+ per-model `vramFor` and an optional `ModelPinRegistry` keep-alive coordinator), `run` routes the fan-out through `enqueuePanel({ keepAlive })`, so a fitting panel is gathered **concurrently** and a too-large one **degrades to sequential** (no OOM); when omitted, the sequential MVP loop is unchanged. The integration test [../../../../tests/integration/orchestration/PanelRouting.test.ts](../../../tests/integration/orchestration/PanelRouting.test.ts) wires the REAL GpuScheduler + REAL ModelPinRegistry into the PanelExecutor and asserts concurrent-when-VRAM-fits (max 2 panelists resident), degrade-to-sequential-when-not (max 1, no OOM), and the keep-alive lifecycle. Tier: parallelised PanelExecutor run path `supported`. | None (closed). The residual live composition-root route wiring is tracked under `OF011.P4.A`. |

### Phase 4 follow-ups (forward-tier / recorded decision, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `OF010.P4.A` | P3 (Resolved) | DF | **Recorded A/B decision -- the live A/B was RUN (v2.0.0 prep).** `scripts/run-panel-ab.mjs` was executed against live local Ollama models (single = `gemma4:latest`; panel = `qwen2.5-coder:3b` + `llama3.2:3b`; judge = `gemma4:latest`) and wrote a structured report to `tests/benchmarks/results/` (gitignored). Result: 3 coding tasks, **0 panel wins / 0 single wins / 3 ties**, mean quality delta **0.000** (below the 0.05 tie epsilon), latency multiplier **0.83x**. `decidePanelRoutingDefault` therefore returns `enableByDefault: false`, so the routing default correctly **stays opt-in (off)** (`nexus.llm.panelRouting` `false`, `PANEL_ROUTING_SHIPPED_DEFAULT === false`) -- now a measured no-net-win, not an un-run assumption. Running it required fixing a latent runner bug: `scripts/run-panel-ab.mjs` imported the settings-coupled `createOllamaClient` (which `require`s `vscode` and cannot load in plain Node), so it could never have run as shipped; it now uses a vscode-free `fetch` Ollama client + a benchmark-only `scripts/_vscode-stub.cjs` preload. Tier: A/B harness + live run `supported`; the budget-panel quality claim for small local models on coding tasks now has one local data point (no win on this small fixture). | Re-run on a larger / more representative coding fixture or a different model panel; only if `decidePanelRoutingDefault` returns `enableByDefault: true` should the `nexus.llm.panelRouting` default + `PANEL_ROUTING_SHIPPED_DEFAULT` flip to `true`. |
| `OF011.P4.A` | P3 (Resolved) | NI | **Resolved -- live route wiring landed (v2.0.0 prep).** The composition root now constructs the scheduler-backed `PanelRouter` and the live chat turn consults it, OPT-IN / DEFAULT-OFF. [../../../../src/panels/ChatPanelBootstrap.ts](../../../src/panels/ChatPanelBootstrap.ts) builds the router only when `settings.panelRoutingEnabled` is true (wiring `InProcessTelemetryBus` -> `GpuScheduler` with a `GpuDetector`-backed free-VRAM provider -> `FusionAgent(getOllamaClient(), modelName, ollamaOptions, loadFusePrompt(catalogDir))` -> `ModelPinRegistry` keep-alive -> `PanelExecutor` with the Phase 3 co-residency backend -> `PanelRouter`), inside a fail-safe try/catch that leaves the router `null` (panel routing simply disabled) if anything fails, so chat startup never breaks. [../../../../src/panels/ChatController.ts](../../../src/panels/ChatController.ts) `submitUserMessage` consults it via `_consultPanel` BEFORE the single-model pipeline: a `panel` decision renders the fused answer (add user + assistant message, `messageComplete` rendered from history) and short-circuits; a `single` decision, a `null` router (the default), or any thrown error falls through to the unchanged single-model path (turn never lost). When `panelRoutingEnabled` is off (the default) NOTHING is constructed and the turn is byte-identical to the pre-OF011 path. Tests: [../../../../tests/unit/panels/ChatController.panelRouting.test.ts](../../../tests/unit/panels/ChatController.panelRouting.test.ts) (4: fused-render-skips-pipeline, single-falls-through, null-router-default-unchanged, router-throws-falls-through). Verified: `tsc -b` clean, lint clean, check-architecture 0 errors, full suite green. The shipped default stays opt-in (off) per the measured `OF010.P4.A` A/B. Tier: live ChatController route `supported`. | None (closed). If the A/B later measures a win, flip the default per `OF010.P4.A`. |

### Phase 5 follow-ups (environmental / recorded decision, not defects)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `ENV.P5.A` | P3 (Resolved) | QG | **Resolved (v2.0.0 prep).** `npm run check:audit-prod` flagged two moderate production advisories: `dompurify` (via the direct `isomorphic-dompurify` dep) and `protobufjs` (optional transitive via `@huggingface/transformers` -> `onnxruntime-web`). **`dompurify` is fixed in-range**: an `overrides` bump to `dompurify@^3.4.11` ([../../../../package.json](../../../package.json)) clears all 7 DOMPurify advisories (vulnerable range was `<=3.4.8`); `isomorphic-dompurify@3.9.0` depends on `dompurify@^3.4.0`, so the bump is compatible. **`protobufjs` is allowlisted** in [../../../../scripts/check-prod-audit.mjs](../../../scripts/check-prod-audit.mjs) with a reachability note: no in-range fix exists (the 7.x line ends at the vulnerable 7.6.2 and `onnxruntime-web` pins `protobufjs@^7.2.4`, so the only patch -- 8.x -- is a semver-major move outside that range), and it is an OPTIONAL transitive whose advisory (GHSA-f38q-mgvj-vph7) requires parsing an attacker-controlled `.proto` schema, which the ONNX runtime never does with untrusted input. `npm run check:audit-prod` now exits 0 (2 allowlisted -- `brace-expansion` + `protobufjs` -- 0 blocking). | If `onnxruntime-web` later widens its `protobufjs` range to admit a patched 8.x, drop the `protobufjs` allowlist entry and let the bump flow through. |
| `OF015.P5.A` | P3 | DF | **Nexus-Hub touchpoint assessed -- not warranted.** The local panel-fusion capability is internal Nexus-AI code, not a portable Hub skill/command, and the Hub's `agent-orchestration-primitives` + `competitive-generation` skills already describe the multi-candidate/judge pattern at the orchestration-primitive altitude; the on-device analogue does not change their guidance. Same disposition as the sibling aisuite-harness plan's `AS010.P6.A`. Tier: no Hub change `supported` (assessed). | If a future cycle wants discoverability, add a one-line cross-link in the Hub's `agent-orchestration-primitives` / `competitive-generation` skills noting Nexus-AI ships an on-device `PanelExecutor`/`FusionAgent` analogue -- a doc-only change against the separate `bendourthe/Nexus-Hub` repo, not blocking. |

---

## 2. Resolved

(none beyond the Adoption Ledger rows above)

---

## 3. Summary

| Metric | Count |
|---|---|
| Phases closed | **5 of 5 (cycle COMPLETE)** -- Phase 1 (F1 fuse skill + council synthesis); Phase 2 (F2 PanelExecutor/FusionAgent + F5 hardening); Phase 3 (F3 GpuScheduler panel co-residency + ModelPinRegistry keep-alive); Phase 4 (F4 PanelAbHarness A/B + opt-in PanelRouter + PanelExecutor concurrency wiring); Phase 5 (FINAL whole-plan acceptance gate + close-out) |
| Adoption sub-tasks resolved | OF001-OF015 (all 15) |
| Open items | 3 (`OF002.P1.A` soft prompt-size warning, `OF005.P2.A` completion-MVP tool-isolation `future`, `OF015.P5.A` Hub touchpoint not-warranted / Hub-side) -- all P3 / forward-tier. **Resolved in v2.0.0 prep**: `OF010.P4.A` (live A/B RUN -- 3 ties, default stays opt-in), `OF011.P4.A` (live route wiring landed), `OF004.P2.A` (live consumer landed), `ENV.P5.A` (audit fixed: dompurify override + protobufjs allowlist), `ENV.P1.A` (catalog regenerated). `OF007.P3.A` was resolved in Phase 4. |
| Defects / quality-gate bypasses | 0 |

---

## 4. Whole-plan acceptance gate (OF013, Phase 5)

The plan's Definition of pass (six items) verified at Phase 5 close-out. No feature code changed in Phase 5 -- this is verification + cycle closure over the Phases 1-4 builds.

| # | Definition-of-pass item | Verdict | Evidence |
|---|---|---|---|
| 1 | `fuse` skill emits the five-section structure + grounded answer, ingests N labeled distinct-panelist candidates; `council` synthesis upgraded to the same vocabulary (keeping its verdict). | PASS | [fuse/SKILL.md](../../../modules/coding/skills/catalog/fuse/SKILL.md) + [council/SKILL.md](../../../modules/coding/skills/catalog/council/SKILL.md); [tests/unit/skills/fuse.test.ts](../../../tests/unit/skills/fuse.test.ts) (OF001-OF003). |
| 2 | A `PanelExecutor`/`FusionAgent` fans one prompt across >= 2 distinct registry models (sequential acceptable; now **parallel** via the GpuScheduler concurrency backend) -> F1 judge -> one fused answer. | PASS | [PanelExecutor.ts](../../../modules/coding/orchestration/PanelExecutor.ts) + [FusionAgent.ts](../../../modules/coding/orchestration/FusionAgent.ts); concurrent path proven in [tests/integration/orchestration/PanelRouting.test.ts](../../../tests/integration/orchestration/PanelRouting.test.ts) (OF004-OF006, OF011). |
| 3 | Panelists share the gated local tool surface (no per-panelist grants); judge hardened as an untrusted-input boundary; candidate text redacted; single-judge SPOF documented. | PASS | F5 in [FusionAgent.ts](../../../modules/coding/orchestration/FusionAgent.ts) (redaction + data-not-instructions + SPOF doc); panelists dispatched with no `tools` (OF005). |
| 4 | `GpuScheduler` keeps a small panel co-resident within a VRAM budget under a hard cap; **degrades to sequential** when VRAM is insufficient (no OOM). | PASS | [GpuScheduler.enqueuePanel](../../../core/scheduler/GpuScheduler.ts) + [ModelPinRegistry.holdForPanel](../../../core/registry/ModelPinRegistry.ts); [tests/integration/scheduler/panel-residency.test.ts](../../../tests/integration/scheduler/panel-residency.test.ts) (OF007-OF009). |
| 5 | A local A/B harness compares panel vs single best resident model on a fixed task set; routing heuristic ships **opt-in**, default-on **only** on a measured net win; result recorded regardless. | PASS | [PanelAbHarness.ts](../../../modules/coding/orchestration/PanelAbHarness.ts) + [PanelRouter.ts](../../../modules/coding/llm/PanelRouter.ts); default ships opt-in (off) -- no live A/B win measured here, recorded as `OF010.P4.A` (OF010-OF012). |
| 6 | Updated testing across unit / static / integration / e2e / CI with strong coverage, all passing; README/ARCHITECTURE/CHANGELOG/known-gaps updated. | PASS | Full suite 4291 passed / 5 skipped / 0 failed; static gates green (see OF013 ledger row); known-gaps finalized; README/ARCHITECTURE/CHANGELOG narrative + version are semantic-release-owned at merge (`OF014`). |

One pre-existing environmental gate failure is recorded and not owned by this plan: `npm run check:audit-prod` flags `dompurify` + `protobufjs` advisories (`ENV.P5.A`).

