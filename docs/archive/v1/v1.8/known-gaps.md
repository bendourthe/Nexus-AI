# v1.8.0 -- Known Gaps, Deferrals, and Carryovers

**Status**: IN PROGRESS -- Phases 1-5 of 6 closed; Phase 6's in-session legs closed 2026-07-03 (T601 + T605), its rehearsal legs recorded below as operator / post-freeze actions (T602-T604). v1.8.0 is the "one-shot end-user installer" cycle ([plans/one-shot-installer.md](plans/one-shot-installer.md)): download one file, get dependencies + VS Code extension + models + the Nexus desktop app installed and launchable. Phase 1 makes the release pipeline emit versioned, checksummed desktop bundles (the fetch-from-release prerequisite): a `desktop-bundle` 3-OS job set in `release.yml` (T101), a single `SHA256SUMS.txt` verification asset (T102), the `GemmaCodeSetup.*` -> `NexusSetup.*` artifact rename (T103), and a local Windows `tauri build` proof with the NSIS bundle stashed as the Phase 2 fixture (T104). Phase 2 makes the installer install Nexus: a fail-closed `desktop_provisioner` (T201), engine step 5 + component threading (T202), a first-run health check + "Launch Nexus" finish hook (T203), and a 39-test suite incl. a real fixture-install integration leg (T204). Phase 3 makes image/video model selection real (gap G2): per-model `weights` manifests in catalog.json + a documented destination-layout contract + a pin-rotation script (T301), a resumable SHA-256-verifying `hf_weights_puller` (T302), protocol routing with weighted mixed progress + per-model failure isolation (T303), and 62 new tests incl. an env-gated GPU-box smoke (T304). Phase 4 closes gap G3 (catalog curation + selection UX): the `task` / copy schema + Chat vs Agentic Coding split (T401), curated text-side copy (T402), four verified uncensored image/video entries + a per-VRAM-tier defaults matrix (T403), the tier-defaults matrix test suite (T404), and the typed catalog page wired into the GUI chain as the `selected_model_ids` producer (closes `OSI003.P3.D`). Phase 5 closes gap G4 (installer looks like the product): the desktop design-token port as the single palette source + catalog section accents (T501), per-phase grouped progress with collapsible logs on the installing page over new engine step signals (T502), and welcome/complete product polish with before/after captures archived (T503). Phase 6 closes gap G5's buildable half: the Windows pipeline is real end-to-end locally -- PyInstaller wizard + NSIS outer -> `NexusSetup.exe` + silent-install smoke (T601, also closing the `OSI004.P4.C` registry-packaging P1), the mac/linux workflows aligned to the canonical spec-driven build scripts, and the end-user download/install page (T605); the whole-plan DoD (3-platform rehearsal from published pre-release artifacts) rides the T602-T604 entries below. This file is appended phase-by-phase; items move to `## 2. Resolved` when closed.

**Audience**: v1.8.0 phase authors, code reviewer, future-cycle planners
**Last updated**: 2026-07-03 (Phase 6 in-session close)
**Sibling reviews**: [../v1.7.0/known-gaps.md](../v1.7/known-gaps.md) (the prior cycle's gap log; `RT.P7.A`/`RT.P7.B` interact with Phase 2 below) and [plans/one-shot-installer.md](plans/one-shot-installer.md) (the active plan).

**Cycle-wide constraint**: GitHub Actions minutes are frozen ($0 budget) until 2026-08-01 (2026-07-02 CI incident). Every CI-leg claim below is therefore `future` tier until the post-freeze rehearsal (Phase 6, T603); local proofs are the only `supported` evidence this cycle.

Severity tags: **P0** release-blocker; **P1** should-fix; **P2** nice-to-have; **P3** out-of-scope for v1.8.0 / recorded for future planning.
Category tags: **NI** not implemented; **DF** deferred; **BG** bug; **MT** missing tests; **WN** warning; **QG** quality gate.

---

## 0. Adoption Ledger

| Plan sub-task | Item | Status | Closing reference |
|---|---|---|---|
| T601 + T605 | Phase 6 -- Windows .exe completion + download docs (gap G5's buildable half; T602-T604 = operator / post-freeze rehearsals) | Resolved (in-session legs) | Phase 6 (2026-07-03). **T601**: [installer-build.yml](../../../.github/workflows/installer-build.yml) rewritten from the v1.0.0 TODO skeleton to the real dispatch-gated pipeline -- NSIS verify -> resolve pinned versions -> root-workspace VSIX build (`npm ci` + `npm run build` + `npx vsce package --no-dependencies`, matching release.yml; the old mac/linux `cd extensions/nexus-coding` path never existed) -> optional `include_payload` offline embed (blocked on the pin rotation, `OSI006.P6.D`) -> [build-windows.ps1](../../../scripts/installer/pyqt/build/build-windows.ps1) (uv sync -> spec-driven PyInstaller `dist/nexus-installer.exe` -> `makensis /DAPP_VERSION=<package.json>` -> `dist/NexusSetup.exe`, fail-loud at each stage) -> [smoke-windows-exe.ps1](../../../scripts/installer/pyqt/build/smoke-windows-exe.ps1) (silent install -> wizard `--version` boot probe -> `--check-registry` bundled-data probe -> silent uninstall + cleanup assertions). The stale v1.1.0 [nexus-setup.nsi](../../../scripts/installer/build/nsis/nexus-setup.nsi) is rewritten: slim per-user shell (`RequestExecutionLevel user`, `$LOCALAPPDATA\Nexus\Setup`), version-parameterized, outputs `NexusSetup.exe` (release.yml's existing upload path -- the frozen wizard renamed to `nexus-installer.exe` in the spec), MUI finish-page wizard launch, HKCU uninstall entry, silent-mode extract-only, silent uninstall always preserves `~/.nexus`; the unsupported `--verify-only` invocation and the misplaced `nexus://` / file-association registrations (they belong to the desktop app) are gone. `installer-macos.yml` / `installer-linux.yml` mirror-aligned: canonical spec-driven build scripts instead of hand-rolled `pyinstaller` (the spec's datas -- VSIX + registry files -- now apply), root VSIX packaging, v1.0.0-era payload embed dropped (slim fetch-at-install-time design, operator decision 2026-07-03), macOS staple->re-DMG ordering fixed. **`OSI004.P4.C` closed**: new [registry_paths.py](../../../scripts/installer/pyqt/src/nexus_installer/registry_paths.py) (`sys._MEIPASS`-first + walk-up, 100% lines) now backs both the model router and the typed catalog page, the spec bundles `core/registry/{catalog,recommended}.json`, and a new `--check-registry` CLI diagnostic asserts them inside the frozen exe. Local DoD proof on the dev box: `NexusSetup.exe` 64.7 MB (wizard 65.2 MB, lzma outer) built end-to-end; smoke all-green (silent install exit 0, wizard extracted + boots, bundled registry resolves, silent uninstall clean; HKCU + Start Menu verified clean after). **T605**: end-user [docs/install.md](../../install.md) (per-OS steps, SmartScreen / Gatekeeper unsigned-binary click-throughs per `OSI001.P1.D`, FUSE + glibc caveats, SHA256SUMS verification, silent/headless usage, uninstall + data notes) + README Quick Start pointer replacing the stale "installers ship in v1.1.0 Phase 14" line. Gates: installer suite **590 passed / 2 skipped / 0 failed** (+26: registry-paths resolution incl. frozen-mode monkeypatched `_MEIPASS`, `check_registry` exit codes + windowed no-stdout, CLI wiring, packaging assertions over spec datas / NSIS invariants / smoke script / workflow reality checks); ruff 0 new findings; `tsc -b` clean; root Vitest **4569 / 6 / 0** unchanged; PyInstaller workpath gitignore fix (`scripts/installer/pyqt/build/work/`). |
| T501 + T502 + T503 | Phase 5 -- desktop-token restyle + per-phase progress UX (gap G4) | Resolved | Phase 5 (2026-07-03). **T501**: [constants.py](../../../scripts/installer/pyqt/src/nexus_installer/constants.py) becomes a direct port of the desktop's [tokens.css](../../../desktop/src/styles/tokens.css) (bg `#0a0d14`/`#11151f`/`#181d2a` + elevated, fg 4-tier incl. new `TEXT_BODY #d6dbe7`, white-alpha borders composited to solid `#191c22`/`#272a30`, lead accent -> chatbot cyan `#22d3ee`, module accents `ACCENT_CHAT/CODING/IMAGE/VIDEO` + `SECTION_ACCENTS`) and is documented as the single palette source; [theme.py](../../../scripts/installer/pyqt/src/nexus_installer/theme.py) gains QTabWidget/QTabBar styling + dark-on-bright button/checkmark treatment + ASCII comments (was mojibake); catalog tabs carry per-section accent rules and the model cards per-section pills/copy/checkbox accents; the `_ModelCard` boxed-pill wart fixed via scoped `QWidget#modelCard` selector; stray hardcoded hexes aligned to constants. **T502**: new [phase_group.py](../../../scripts/installer/pyqt/src/nexus_installer/widgets/phase_group.py) widget (status icon, per-group bar, collapsible Details log) + [installing.py](../../../scripts/installer/pyqt/src/nexus_installer/pages/installing.py) grouping (Dependencies = ollama+venv -> VS Code Extension -> Models -> Nexus Desktop, overall bar on top, active-group log routing, component-driven rebuild) over three new engine signals (`step_started`/`step_progress`/`step_failed`; existing signals unchanged). **T503**: welcome lockup with the desktop app icon + product copy + four pillar accent chips; complete-page "Managing Nexus" naming + `nexus-install.log`; the header brand mark's dead fixed-depth path (landed on nonexistent `scripts/assets/`) replaced with walk-up resolution; 8 before/after captures archived under [development/history/assets/2026-07_phase-5/](development/history/assets/2026-07_phase-5). Gates: installer suite **564 passed / 2 skipped / 0 failed** (+26); `phase_group` 96% / `engine/installer` 93% / `installing` 82% lines; ruff 0 new findings; root Vitest **4569 / 6 / 0** unchanged; `tsc -b` clean. |
| T401 + T402 + T403 + T404 | Phase 4 -- catalog curation + selection UX (gap G3) | Resolved (data + UI legs; pin rotation + GPU-box load verification = operator actions) | Phase 4 (2026-07-03). **T401**: catalog schema gains `task` (chat / agentic / image / video / audio / embed), `strengths[]`, `whyRecommended`, `differentiators`, `provenance` -- typed + validated in [catalog.ts](../../../core/registry/catalog.ts) (`ModelTask`, invalid-task rejection, and a fail-loud rule that `uncensored: true` requires `provenance`); the installer's Text tab is split into **Chat** and **Agentic Coding** sections ([typed_catalog.py](../../../scripts/installer/pyqt/src/nexus_installer/pages/typed_catalog.py) `TYPE_TABS` = Chat / Agentic Coding / Image / Video / Audio, task-driven with a type fallback, embed entries rendering under Chat) and the model cards render the new copy (strengths, "Why this one" on defaults, differentiators) plus a Recommended pill. **T402**: all 13 text-side entries (gemma4 ladder incl. the 12B GGUF, llama, qwen, qwen-coder, deepseek-coder, nomic-embed) carry curated descriptions + strengths + differentiators, with `whyRecommended` on every tier default. **T403**: four uncensored entries curated with web-verified license + provenance recorded per entry (2026-07-03): `juggernaut-xl-v9` (SDXL fine-tune, KandooAI x RunDiffusion, CreativeML Open RAIL-M + no-paid-API rider), `realvisxl-v5` (SDXL fine-tune, SG161222, OpenRAIL++), `wan2.1-t2v-1.3b` and `wan2.2-ti2v-5b` (Alibaba Wan-AI originals, Apache-2.0); [recommended.json](../../../core/registry/recommended.json) rewritten to a v2 per-VRAM-tier matrix (cpu / 8 / 12 / 16 / 24) with **uncensored image + video defaults on every GPU tier** and censored alternatives left listed; new pure [tier_defaults.py](../../../scripts/installer/pyqt/src/nexus_installer/tier_defaults.py) resolves the tier and fit-gates the selection (chat + agentic guaranteed via largest-fitting fallback, image/video fit-gated with no substitution, cpu tier never selects image/video, cumulative disk reserve honored). **OSI003.P3.D closed**: `TypedCatalogPage` replaces `ModelSelectionPage` at wizard step 4 ("Models"), recomputes defaults on `showEvent` until the user touches a checkbox, and writes `selected_model_ids` (section-ordered; unknown ids preserved for the `--model` -> ollama contract) + the legacy `selected_model` chat pick + `selected_models_gb`; the review page renders the multi-selection. Also fixed en route: the page's `_repo_root()` used `parents[5]` (landed on `scripts/`, so the default catalog path never resolved -- latent while unwired); replaced with the `model_router` walk-up. **T404**: new [test_tier_defaults.py](../../../scripts/installer/pyqt/tests/test_tier_defaults.py) runs the REAL catalog + matrix per simulated tier (8/12/16/24 GB + cpu) asserting VRAM fit, disk fit, one-chat + one-agentic + embed composition, uncensored image/video defaults, and that every matrix id is real + downloadable; synthetic fallback/disk-gating/edge suites alongside; offscreen end-to-end smoke of all five tiers against the real files verified. Gates: installer suite **538 passed / 2 skipped / 0 failed** (540 collected, +46), `tier_defaults` 94% / `typed_catalog` 93% / `review` 91% lines, ruff 0 new findings, `tsc -b` clean, root Vitest **4569 / 6 / 0** (+4 new schema tests). |
| T301 + T302 + T303 + T304 | Phase 3 -- Hugging Face weights downloader: image/video selection becomes real | Resolved (engine legs; GPU-box smoke + pin rotation = operator actions) | Phase 3 (2026-07-03). **T301**: `weights` manifests (`layoutVersion` + `files[].path/sha256`) on all 16 `source.protocol: "huggingface"` entries in [catalog.json](../../../core/registry/catalog.json); the runtime model-path contract verified as *undefined* (the diffusion pipelines' real executors are OA-09 stubs; no consumer resolved local weights paths) and therefore **defined + documented** in catalog `_meta`: files land at `<models_root>/weights/<model-id>/{repo-relative path}` (`models_root` defaults `~/.nexus/models`), the directory diffusers `from_pretrained` loads; typed in [catalog.ts](../../../core/registry/catalog.ts) (`ModelWeightsManifest` + `validateSpec` checks); pins carried over as the documented all-zero placeholders (no HF egress from the dev sandbox -- proxy 401 on the HF API) with [pin-hf-weights.py](../../../scripts/installer/build/pin-hf-weights.py) shipped for rotation (API `lfs.oid` mode, `--from-dir` hash-local mode, `--check` gate). **T302**: new [hf_weights_puller.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/hf_weights_puller.py) -- per-file `.partial` + Range resume (206 append / 200 restart / 416 promote; cancel keeps partials), 3-attempt backoff retry, SHA-256 verify (real-pin mismatch deletes + fails closed; placeholder pin warns + logs the computed digest, the fetch-payload.py discipline), traversal-safe manifest paths (rejects `..` / absolute / `\\` / drive-colon), disk pre-check vs the 10 GB reserve. **T303**: new [model_router.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/model_router.py) in engine step 4 **and** the headless step -- catalog-protocol routing (`ollama` -> `ModelPuller.pull_model`, unchanged; `huggingface` -> weights puller; unknown ids -> ollama, the historical behavior), sizeGB-weighted aggregate progress, per-model failure isolation onto new `InstallerState.failed_models` + named per-model bullets on the complete page; `selected_model_ids` multi-select state field added (UI wiring = Phase 4). **T304**: 62 new tests (manifest parsing incl. traversal cases, resume semantics, fail-closed hash handling, retry/backoff, disk pre-check, weighted mixed aggregation, routing, isolation, cancel) + engine-suite updates; env-gated `NEXUS_HF_WEIGHTS_SMOKE=1` integration smoke downloads the smallest real entry. Gates: installer suite **494 passed / 2 skipped / 0 failed** (+61), `hf_weights_puller` 94% / `model_router` 98% lines, ruff 0 new findings, `tsc -b` clean, root Vitest **4565 / 6 / 0** unchanged. |
| T201 + T202 + T203 + T204 | Phase 2 -- desktop provisioner: the installer installs Nexus | Resolved (local legs) | Phase 2 (2026-07-02). **T201**: new [desktop_provisioner.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/desktop_provisioner.py) -- pinned tag `v2.1.0` -> per-OS/arch asset resolution (`Nexus-Desktop_{version}_*`, fail-closed on Windows-ARM64 / Linux-non-x86_64) -> resumable download (`.partial` + Range; 206 append / 200 restart / 416 promote; cancel keeps the partial) -> `SHA256SUMS.txt` verification **before any install dispatch** (missing manifest, missing entry, or digest mismatch all abort; mismatches deleted) -> per-OS install (NSIS `/S` [+ `/D=` last-unquoted], `hdiutil attach`/`ditto`/`detach`-in-finally, AppImage + `.desktop` + icon). Windows exe resolution prefers `Nexus.exe` then the actual `nexus-shell.exe` (T104 finding) then any non-uninstaller exe. `desktop_bundle_override` / `--desktop-bundle` installs a local bundle (fetch + checksum skipped, warn-logged) -- the freeze-era path. **T202**: engine step 5 with own progress band + cancel wiring (also fixed the model step's hardcoded final-step progress base); `"desktop"` in default components; 6 new `InstallerState` fields; default-checked ConfigurationPage toggle; friendly ReviewPage labels; headless step + `--skip-desktop` (passed by all 3 smoke scripts). **T203**: `first_run_health_check` launch-probe (exit-0 or alive-past-5s-grace = pass; health failure warns but does not fail the step); complete-page "Nexus Desktop" services row + default-checked "Launch Nexus when I click Finish" checkbox; window Finish invokes the page `on_finish` hook. **T204**: 39 new tests + engine/state/pages suite updates; env-gated Windows integration test installs the T104 fixture, asserts binary + **health check passed**, uninstalls in `finally`. Local DoD proof: fixture silent install -> `Nexus.lnk` in Start menu -> app launches, health check passes -> clean uninstall. Gates: installer suite **433 passed / 1 skipped / 0 failed** (+42), `desktop_provisioner` 89% lines, ruff 0 new findings, root Vitest **4565 / 6 / 0** unchanged. |
| T101 + T102 + T103 + T104 | Phase 1 -- release pipeline produces desktop bundles | Resolved (local legs) | Phase 1 (2026-07-02). **T101**: `desktop-bundle` 3-OS matrix job set added to [release.yml](../../../.github/workflows/release.yml) (tag-push is the workflow's only trigger, satisfying "tag-triggered only" under the freeze) -- checkout -> setup-node (root workspace lockfile cache) -> Linux webkit2gtk-4.1 prerequisites -> pinned `dtolnay/rust-toolchain` (same SHA as `shell-build.yml`) -> cargo cache -> root `npm ci` (npm workspaces cover `desktop/`) -> `node scripts/sync-tauri-version.mjs` -> fail-fast `npm run build:sidecar` -> `tauri build` (macOS leg builds `--target universal-apple-darwin` after `rustup target add` for both arches) -> stage under canonical names `Nexus-Desktop_{version}_x64-setup.exe` / `_universal.dmg` / `_amd64.AppImage` / `_amd64.deb` -> upload. The build-time version sync ([scripts/sync-tauri-version.mjs](../../../scripts/sync-tauri-version.mjs), exported pure functions + `--check` mode, 7 unit tests) fixes the stale-at-1.5.0 `tauri.conf.json` version from the semantic-release-owned root package.json (committed value bumped to 2.1.0; future staleness is harmless because the sync runs before every bundle build). **T102**: `create-release` now needs `desktop-bundle`, downloads the three bundle artifacts, and generates one `SHA256SUMS.txt` over an explicit list of every attached asset (VSIX + 3 wizard installers + 4 desktop bundles) -- fails loudly if any expected asset is missing; attached to the release as the installer's fail-closed verification source. **T103**: grep-audited rename -- `release.yml` was the only workflow still carrying `GemmaCodeSetup.*` (the PyQt build scripts already emit `NexusSetup.*`, so the workflow upload paths were dead-broken); fixed to `NexusSetup.exe` / `NexusSetup.dmg` / `NexusSetup-x86_64.AppImage` / `Nexus Installer` / `nexus-setup`, plus the stale `gemma-code-*.vsix` asset name (vsce emits `nexus-coding-*.vsix` from the package name) and the "Gemma Code v..." release title; the wizard's own `setApplicationName("Gemma Code Installer")` + argparse description ([main.py](../../../scripts/installer/pyqt/src/nexus_installer/main.py)) and one integration-test banner renamed to "Nexus Installer". `installer-macos.yml` / `installer-linux.yml` / `installer-build.yml` carried no old names (audit result, not omission). **T104**: local Windows proof -- rustup (stable-msvc 1.96.1, minimal profile) installed on the dev box (MSVC VS2022 + NSIS were already present); `npm run build:shell` produced `Nexus_2.1.0_x64-setup.exe` (NSIS, 1.6 MB) + `Nexus_2.1.0_x64_en-US.msi` (2.1 MB) in 2m27s; silent install (`/S /D=<scratch>`) landed `nexus-shell.exe` (3.5 MB) + `uninstall.exe`, silent uninstall removed them cleanly; the NSIS bundle is stashed at `.local-fixtures/Nexus_2.1.0_x64-setup.exe` (new gitignored dir) as the Phase 2 / T204 integration fixture. SmartScreen: not triggerable locally (a locally-built exe carries no Mark-of-the-Web); the unsigned-download warning for end users is recorded as `OSI001.P1.D`. Gate green: root suite **4565 passed / 6 skipped / 0 failed** (+7 new), `tsc -b` clean, lint 0 errors, `release.yml` YAML-parse validated. |

---

## 1. Open Items

### Phase 1 (2026-07-02)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `OSI001.P1.A` | P1 | DF | The `desktop-bundle` job set has never executed in CI (Actions freeze until 2026-08-01). The 3-OS matrix, the macOS universal leg, the bundle-path globs, and the SHA256SUMS asset are `future` tier until a real run. The Windows leg's layout (`bundle/nsis/*-setup.exe`) is locally verified; macOS/Linux paths follow Tauri 2's documented bundle layout. | Phase 6 / T603: post-freeze, push a pre-release tag on a scratch branch (`workflow_dispatch` is not available on `release.yml`; its only trigger is tags) and verify all 8 assets + SHA256SUMS attach. Fix any mac/linux glob drift there. |
| `OSI001.P1.B` | P1 | DF | The NSIS bundle contains the Tauri shell only -- the Node sidecar dist (`desktop/sidecar/dist/`) is **not** packaged (no `bundle.resources` / `externalBin` in `tauri.conf.json`), and the shell expects a system Node to spawn it. This is the v1.7.0 `RT.P7.A`/`RT.P7.B` carryover surfacing in the installer path: an installed Nexus desktop cannot serve the Coding pillar yet. **Phase 2 outcome (2026-07-02)**: T203 shipped the health check as a **launch-probe** (spawn + exit-0-or-alive-past-grace), which passes against the shell-only bundle -- the Phase 2 DoD's observables (installs, Start menu, launches, health check passes) all closed without the sidecar. The deeper sidecar ping is split out as `OSI002.P2.B` and stays blocked on this item. | Bundle the sidecar dist as a Tauri resource + spawn via the installer-provisioned Node, or land `RT.P7.B` first; then upgrade the health check per `OSI002.P2.B`. **Phase 6 disposition (2026-07-03)**: assessed as a T601 rider and deferred -- the fix is desktop-bundle-side (tauri.conf resources + shell spawn path, blocked on `RT.P7.B`), orthogonal to the wizard-exe pipeline T601 delivered; next natural slot is the post-freeze cycle alongside the `OSI006.P6.B` bundle rehearsal. |
| `OSI001.P1.C` | P2 | WN | `desktop/src-tauri/Cargo.lock` is gitignored (pre-existing `.gitignore` policy), so desktop-bundle builds are not crate-pinned -- each CI run resolves crates fresh, violating the repo's versions.lock.json pinning discipline in spirit. The local T104 build resolved tauri 2.11.5 / tauri-build 2.6.3. | Track `Cargo.lock` for `desktop/src-tauri` (remove the ignore rule, commit the lockfile) in a follow-up; standard practice for application (non-library) crates. |
| `OSI001.P1.D` | P3 | WN | Release binaries are unsigned this cycle (plan Section 4 out-of-scope): downloaded copies of `Nexus-Desktop_*_x64-setup.exe` / `NexusSetup.exe` will trip SmartScreen ("Windows protected your PC"), and the unsigned DMG will trip Gatekeeper. Not locally reproducible (no Mark-of-the-Web on a locally-built exe). | Phase 6 / T605 documents the warning + click-through on the download page; the signing/notarization purchase decision is deferred per plan. |
| `NAME.P1.A` | P2 | DF | **Residual `gemma-code` references repo-wide** (operator-raised, 2026-07-02). The T103 grep-audit fixed the live user-visible stragglers (workflow artifact names, wizard app name, release title), but ~180 tracked files still contain `gemma[ -]?code`, in four deliberate-at-the-time classes: (1) the **backward-compat surface** -- legacy `gemma-code.*` settings keys (SettingsCompat, settingsKeyMap), legacy command IDs (compatShim), the `GemmaCodeSettings` type alias, and package.json descriptions -- whose removal was promised "until v1.2.0" but never executed (the repo now ships 2.1.0: the compat window is ~5 majors overdue); (2) **migration code** that must name the old paths (`~/.gemma-code/` -> `~/.nexus/` StorageMigration, marker files); (3) **history** (DEVLOG, CHANGELOG, ADRs, docs/archive, docs/versions) -- append-only records, correct to keep; (4) `scripts/installer/legacy/` -- explicitly legacy. Class 1 is the actionable debt; classes 2-4 are by-design. (Note: "Gemma 4" / `gemma4:*` references are the Google LLM the product runs, not the old project name.) | Schedule a dedicated **compat-retirement sweep** (candidate for the next hygiene cycle, or a v1.8.0 Phase 6 rider if the operator wants it sooner): delete the compat shim + SettingsCompat resolution + `GemmaCodeSettings` alias + legacy key descriptions, with a CHANGELOG `### Removed` entry and a migration note; keep classes 2-4. |

### Phase 2 (2026-07-02)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `OSI002.P2.A` | P1 | DF | The release-fetch path (SHA256SUMS fetch, resumable asset download, checksum verify) is fully unit-tested (mocked httpx over real temp files) but has **never run against a real published release**: the pinned tag `v2.1.0` has no GitHub release under the Actions freeze. The local proof used `--desktop-bundle` (which skips the fetch + checksum by design, warn-logged). | Phase 6 / T603: after the freeze, push the pre-release tag and run the wizard's release-fetch path against the real `SHA256SUMS.txt` + bundle assets; re-pin `NEXUS_DESKTOP_PINNED_TAG` to the rehearsal tag. |
| `OSI002.P2.B` | P2 | DF | The first-run health check is a **launch-probe** (spawn `--version`, pass on exit-0 or alive-past-5s-grace), not a sidecar ping -- a sidecar ping would fail by construction while the bundle ships the shell only (`OSI001.P1.B`). A launch-probe cannot distinguish "app fully functional" from "shell renders but pillars are dead". | When `OSI001.P1.B` closes, extend `first_run_health_check` with a sidecar health ping (localhost port probe or `--healthcheck` CLI in the shell) and keep the launch-probe as the fallback tier. |
| `OSI002.P2.C` | P2 | MT | The macOS DMG (`hdiutil`/`ditto`) and Linux AppImage (`~/.local/bin` + `.desktop`) install legs are unit-tested with mocked subprocess / tmp-home only -- no real-hardware execution this phase (Windows was the only local rig; the plan's GPU-box commitment covers Phase 3, not mac/linux). FUSE-less AppImage hosts and Gatekeeper quarantine are untested. | Phase 6 / T604: clean-VM rehearsals on macOS + Linux; record Gatekeeper/FUSE caveats in the download docs per plan. |
| `OSI002.P2.D` | P3 | WN | The GUI wizard chain ([main.py](../../../scripts/installer/pyqt/src/nexus_installer/main.py)) still registers a 9-page sequence; **Phase 4 wired `TypedCatalogPage` in** (it replaced `ModelSelectionPage` at step 4), but `VsCodeExtensionPage` and `StoragePage` remain tested-but-unwired, and the desktop choice still lives on the ConfigurationPage. **Phase 5 disposition (2026-07-03)**: the restyle kept the existing 9-page flow (tokens + progress UX only, no flow rework), so the pages remain unwired. | Wire the remaining standalone pages in a later flow pass; move the desktop choice to its natural page then if the UX calls for it. **Phase 6 disposition (2026-07-03)**: not wired -- T601's scope was packaging/CI, no flow rework; rides the `NAME.P1.A` sweep or a dedicated flow pass. |

### Phase 3 (2026-07-03)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `OSI003.P3.A` | P1 | DF | **All 16 weights pins are placeholders** (all-zero sha256, carried over from the pre-existing catalog/versions.lock.json convention), so the puller's verification is warn-and-skip (with the computed digest logged) rather than fail-closed for every current HF entry. Rotation was blocked in this session: the dev sandbox has no Hugging Face egress (the HF API returns a proxy 401), and hashing multi-GB downloads locally was not feasible. Until pins rotate, HF weight downloads carry the same trust level as the existing `fetch-payload.py` placeholder path. | Operator action (now also covers the four Phase 4 uncensored entries, 20 HF entries total -- see `OSI004.P4.A`): run `python scripts/installer/build/pin-hf-weights.py` from an unproxied network (API `lfs.oid` mode), or `--from-dir ~/.nexus/models` after the GPU-box smoke; `--check` then gates that no placeholders remain for default entries. |
| `OSI003.P3.B` | P1 | DF | **Manifests list the primary weights blob only** (1 file per entry, derived from the pre-existing `source.url`). Diffusers multi-file repos (SANA `_diffusers` layouts: `model_index.json`, `text_encoder/`, `tokenizer/`, `scheduler/`, `vae/`) need their full file lists enumerated before a pipeline can load purely from the manifest-installed directory; non-LFS config files also expose no sha256 via the HF API (pin policy needed: pin-by-download or accept-unpinned-configs). File lists could not be enumerated offline. | Operator action (Phase 4 curated the defaults without HF egress, so enumeration remains open -- tracked with `OSI004.P4.A`): extend `weights.files[]` to the full repo file list per default entry (the pin script's API mode reports the tree) and decide the config-file pin policy; the `OSI004.P4.B` load verification then proves the manifest is complete. |
| `OSI003.P3.C` | P2 | MT | The **GPU-box integration smoke did not run** this session (no GPU box, no HF egress): the env-gated `NEXUS_HF_WEIGHTS_SMOKE=1` test (downloads the smallest real entry, default `sana-1.6b-int4`) and the DoD's "diffusion runtime loads the weights" leg are unexercised. Note the runtime-load leg is additionally bounded by the runtime's executors still being OA-09 stubs off-GPU. | Operator action per the plan's Phase 3 DoD commitment: run the smoke on the GPU box (`NEXUS_HF_WEIGHTS_SMOKE=1 pytest tests/test_hf_weights_puller.py -k Smoke`), then load the entry via the diffusion runtime; feed the resulting digests into `OSI003.P3.A`'s `--from-dir` rotation. |
### Phase 4 (2026-07-03)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `OSI004.P4.A` | P1 | DF | The four new uncensored entries follow the existing placeholder-pin + primary-blobs-only manifest convention (extends `OSI003.P3.A`/`B`): all-zero sha256 pins, and the Wan manifests list only the large weights blobs (`diffusion_pytorch_model*.safetensors`, `models_t5_umt5-xxl-enc-bf16.pth`, `Wan2.x_VAE.pth`) -- config/tokenizer files and the `wan2.2-ti2v-5b` shard `index.json` are not yet enumerated (no HF egress from the dev sandbox; file names verified via web search against the HF trees). Until rotation, downloads of the new defaults verify warn-and-skip rather than fail-closed. | Operator action, folded into the existing `OSI003.P3.A`/`B` run: `pin-hf-weights.py` from an unproxied network (API mode also reports the full repo tree for the manifest extension), then `--check` gates that no default entry keeps a placeholder. |
| `OSI004.P4.B` | P1 | MT | T403's **"verify each default entry actually loads in the diffusion runtime" leg did not run** (no GPU box this session; extends `OSI003.P3.C`). The runtime-load leg is additionally bounded by the diffusion executors still being OA-09 stubs off-GPU. The T404 matrix suite proves catalog/manifests/fit logic, not weight-level loadability. | Operator action per the plan's GPU-box commitment: after pin rotation, load `juggernaut-xl-v9`, `realvisxl-v5`, `wan2.1-t2v-1.3b`, `wan2.2-ti2v-5b` via the diffusion runtime; demote any entry that fails to a non-default (matrix edit in recommended.json) and record the outcome here. |
| `OSI004.P4.D` | P3 | WN | `ModelSelectionPage` (the old single-model radio page) is now **unwired legacy**: module + tests retained (they still pass) but nothing constructs it in the GUI chain. Kept to avoid out-of-scope deletion during the phase. **Phase 5 disposition (2026-07-03)**: the restyle confirmed no page reuse. | Remove the module + its tests (and the review page's legacy `_MODEL_SIZES` fallback) as a rider on the `NAME.P1.A` compat-retirement sweep. |
| `OSI004.P4.E` | P2 | WN | The new entries' `sizeGB` / `requiredVramGB` are **catalog estimates from published repo metadata** (Juggernaut/RealVis fp16 blobs 6.9 GB; Wan 2.1 repo ~17.6 GB total incl. the 11.4 GB umt5-xxl encoder; Wan 2.2 TI2V-5B repo 34.2 GB; VRAM 8/10/8/24 GB from vendor claims like "8.19 GB on consumer GPUs" and "720p24 on a 4090"). Real disk/VRAM telemetry may differ, which shifts tier fit at the margins. | True-up during the `OSI004.P4.B` GPU-box run (the downloaded tree gives exact sizes; runtime load gives real VRAM); adjust catalog + matrix if any default stops fitting its tier. |

### Phase 5 (2026-07-03)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `OSI005.P5.A` | P3 | WN | Dependency-step group bars are **completion-quantized**: the ollama / extension / venv installers expose no within-step progress callback, so their groups sit at 0% (or 50% for the two-step Dependencies group) until a step completes; only the Models and Nexus Desktop steps stream live `step_progress`. Honest but chunky feedback on slow dependency installs. | Thread a progress callback through `OllamaInstaller` (its download loop already reads content-length) and, where meaningful, the venv provisioner, then emit `step_progress` for them in the engine. **Phase 6 disposition (2026-07-03)**: not taken -- the phase's code path was packaging/CI, not the engine; stays a cosmetic-priority carryover for a future polish pass. |
| `OSI005.P5.B` | P3 | WN | Typography parity with the desktop app is **size/weight-level only**: the desktop renders Inter / JetBrains Mono (web-loaded), while the installer keeps platform-native faces (Segoe UI / SF Pro / Cantarell + Consolas / SF Mono / Ubuntu Mono). Bundling the font files was out of the phase's scope. | If exact face parity is wanted, ship the Inter + JetBrains Mono TTFs as PyInstaller data and register them via `QFontDatabase.addApplicationFont` at startup. **Phase 6 decision (2026-07-03, T601)**: NOT bundled -- the TTFs are not repo assets (the desktop web-loads them), so bundling would add a build-time outbound fetch + new pins for a cosmetic gain; platform-native faces stay. Revisit only if a future cycle vendors the fonts. |

### Phase 6 (2026-07-03)

| ID | Sev | Cat | Description | Suggested next step |
|---|---|---|---|---|
| `OSI006.P6.A` | P1 | MT | **T602 (Windows clean-VM end-to-end rehearsal) did not run this session** -- no clean VM in the sandbox. The local T601 proof is the packaging round-trip on the (non-clean) dev box: silent install, wizard boot + bundled-registry probes, silent uninstall. Unexercised: the full double-click GUI flow from `NexusSetup.exe` through deps -> extension -> models -> desktop on a machine with none of the dependencies present, SmartScreen on a downloaded (Mark-of-the-Web) exe, and a VSIX-bundled artifact (the dev box had no built VSIX, so the local exe took the warn-and-skip path; the CI workflows build + bundle it, and the spec glob is unit-asserted). | Operator action per the plan: clean Windows VM, download (do not copy) the exe so MotW applies, run the full flow, log every rough edge as fix-or-gap; feeds the whole-plan DoD. |
| `OSI006.P6.B` | P1 | DF | **T603 (CI legs) is freeze-blocked until 2026-08-01.** Nothing CI-side has executed: the rewritten `installer-build.yml`, the mirror-aligned `installer-macos.yml` / `installer-linux.yml` (canonical build scripts + root VSIX packaging + slim no-payload design), the macOS staple->re-DMG ordering, the `release.yml` desktop-bundle matrix (`OSI001.P1.A`), and the wizard's release-fetch path against a real release (`OSI002.P2.A`) are all `future` tier. `shell-build.yml` also remains disabled from the 2026-07-02 incident. | Post-freeze, per T603: `workflow_dispatch` the three installer workflows; push the release dry-run pre-release tag on a scratch branch and verify all assets + `SHA256SUMS.txt` attach; re-pin `NEXUS_DESKTOP_PINNED_TAG` to the rehearsal tag and run the wizard's release-fetch path; re-enable `shell-build.yml`; attach everything to a pre-release for download testing. |
| `OSI006.P6.C` | P1 | MT | **T604 (macOS + Linux clean-machine rehearsals) did not run** (no mac/linux hardware this session; extends `OSI002.P2.C`). The DMG drag-install + Gatekeeper unsigned click-through, AppImage FUSE/glibc behavior, and the per-OS wizard flows are documented in [docs/install.md](../../install.md) from published platform behavior, not from a rehearsal. | Operator action per the plan: clean VM / spare machine per OS, run the full flow, then true-up the caveat sections of docs/install.md against observed behavior. |
| `OSI006.P6.D` | P2 | DF | **The `include_payload` offline-embed path is untestable until the versions.lock.json rotation**: the `devai_hub` entry still carries the placeholder `example.invalid` URL (fetch-payload.py fails closed on it), the lock's model entries predate the Phase 3 install-time HF puller (stale SANA-era pins), and `cuda_provisioner` expects an extracted `payload/cuda-12.1-runtime/` tree while fetch-payload stages the raw CUDA installer exe under `payload/cuda_runtime/` (layout mismatch). The default slim path is unaffected -- the exe ships without payload and every provisioner downloads or degrades gracefully. | Fold into the `OSI003.P3.A` operator rotation: fix the devai_hub URL + rotate pins, reconcile the payload layout with the provisioners' expectations (or retire fetch-payload.py's model/devai entries in favor of the install-time pulls), then rehearse `include_payload=true` via `workflow_dispatch`. |

---

## 2. Resolved

| ID | Sev | Cat | Description | Resolution |
|---|---|---|---|---|
| `OSI004.P4.C` | P1 | DF | The typed catalog + the model router located `core/registry/catalog.json` / `recommended.json` by walking up the source tree only; the PyInstaller bundle did not package the two files, so a packaged `NexusSetup.exe` would render an empty catalog and route every id to ollama. | Phase 6 (2026-07-03, T601): new shared [registry_paths.py](../../../scripts/installer/pyqt/src/nexus_installer/registry_paths.py) resolver (`sys._MEIPASS`-first when frozen, then the walk-up; 100% lines) now backs both consumers; the spec bundles both files under `core/registry/`; a new `--check-registry` CLI diagnostic + the [smoke-windows-exe.ps1](../../../scripts/installer/pyqt/build/smoke-windows-exe.ps1) packaging smoke assert them inside the frozen exe. Proven end-to-end on the local `NexusSetup.exe` build (probe exit 0). |
| `OSI003.P3.D` | P3 | WN | The wizard UI populated only the single `selected_model`; `selected_model_ids` (the multi-select surface the router consumes) had no wired producer. | Phase 4 (2026-07-03): `TypedCatalogPage` wired into the GUI chain at step 4 ("Models"), replacing `ModelSelectionPage`; it writes the section-ordered `selected_model_ids`, keeps `selected_model` on the chat pick for legacy consumers, updates `selected_models_gb`, and recomputes hardware-tier defaults on show until the user touches a checkbox. The "select an image and a video model in the wizard" DoD observable is now reachable (engine leg was already protocol-complete). |

---

## 3. Summary

- **Phases closed**: 6 / 6 in-session legs (Phases 1-2 on 2026-07-02, Phases 3-6 on 2026-07-03). The whole-plan DoD (Section 0 of the plan holding on all three platforms from published pre-release artifacts) rides the recorded rehearsal legs: `OSI006.P6.A` (Windows clean VM, T602), `OSI006.P6.B` (post-freeze CI + pre-release tag, T603), `OSI006.P6.C` (mac/linux hardware, T604), plus the standing operator actions (pin rotation `OSI003.P3.A`/`OSI004.P4.A`, manifest enumeration `OSI003.P3.B`, GPU-box load verification `OSI003.P3.C`/`OSI004.P4.B`).
- **Open**: 10 x P1 (freeze / rehearsal / sidecar-carryover / placeholder-pin items -- none is a phase defect), 6 x P2, 5 x P3. 2 resolved (`OSI003.P3.D`, `OSI004.P4.C`).
- **Open MT**: `OSI002.P2.C` + `OSI006.P6.C` (mac/linux legs pending hardware), `OSI006.P6.A` (Windows clean-VM rehearsal), `OSI003.P3.C` (GPU-box weights smoke), and `OSI004.P4.B` (diffusion-runtime load verification). No open BG / QG items: no bug, test failure, coverage shortfall, suppressed lint, or bypassed gate was introduced.
