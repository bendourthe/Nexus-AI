# Plan - Agent-State Motion Identity for the Desktop Shell

**Project**: Nexus AI Studio
**Version**: v1.17.0 (cut 2026-08-16)
**Slug**: ui-motion-identity
**Filename**: v1.17.0-adoption-ui-motion-identity.md
**Plan Type**: Feature / Enhancement (adoption)
**Created**: 2026-07-23
**Seeded from**: [../comparisons/v1.17.0-comparison-ui-motion-identity-and-tokenizer.md](../comparisons/v1.17.0-comparison-ui-motion-identity-and-tokenizer.md)
**Goal**: Make the Nexus desktop shell feel unmistakably modern and pleasant by reverse-engineering all three effect libraries (thinking-orbs, border-beam, metal-fx) into an internal motion system with one job each - orbs express agent state, the beam expresses surface liveness, metal expresses a premium hero action - wired onto the existing Tailwind v4 design tokens, with no new frontend dependency, coordinated so only one motion competes per surface, and hardened for reduced-motion.

## Overview

This milestone operationalizes the (revised) adoption plan from the thinking-orbs + border-beam + metal-fx + gigatoken comparison. The comparison found that Nexus already ships a deliberate ambient identity (forced-dark, a cyan/blue glow layer, a signature gradient, an animated constellation background via `desktop/src/components/ConstellationBackground.tsx`, and four locked per-module accent colors in `desktop/src/styles/tokens.css`), but its *interaction* motion is thin: agent activity is signalled by ad-hoc loaders (`LocalModelStatus.tsx`, `LocalModelStatusDock.tsx`, panel spinners, `GenerationCanvas.tsx`), focus/streaming surfaces are mostly static, and premium actions rely on the gradient alone. The three libraries map cleanly onto those three thin layers.

Per the maintainer's decision, all three are adopted and reverse-engineered (no `npm install`), each given a single semantic job so a maximalist look stays coherent rather than noisy: **orbs = agent state** (A1, flagship), **beam = surface liveness** (A2), **metal = hero action** (A3). Two calibration decisions are locked in: metal is used on hero controls only (send button, primary Generate, New session), and the ambient glow/constellation **recedes when a surface has an active effect** so motion never stacks. A cross-cutting foundation-and-coordination item (A4) provides the shared motion tokens, a centralized `prefers-reduced-motion` mechanism, the recede-when-active primitive, and the final one-motion-per-element + battery/perf audit.

Delivery stays entirely within Nexus's existing frontend stack: the Tauri 2.x + React 19 + Vite + Tailwind v4 desktop shell (`desktop/src/`), its design tokens (`desktop/src/styles/tokens.css`, `globals.css`), and its component tree (`desktop/src/components/`). No architecture is imported: the three effect libraries are not added as dependencies (N1), their non-Nexus palettes (ocean/sunset/gold/silver/chromatic) are not used (N3), metal-fx is confined to hero controls rather than a blanket treatment (N2), and gigatoken is not adopted at all (N4/N5: Python-only against a JS token path, per-request rather than bulk workload, incomplete Windows testing against a Windows-first product with an OS-parity principle). Success looks like: a single on-brand motion vocabulary expresses agent activity across coding, chat, image, and video; the input composer, active-model dock, and generation canvas feel alive on focus/streaming/rendering; the send, Generate, and New-session controls get a premium tactile ring; the ambient glow gracefully steps back on active surfaces; and every motion degrades cleanly under `prefers-reduced-motion`.

This is a shell-only cycle placed as a standalone v1.17.0 because v1.15 (in flight: installer + studio-chat) and v1.16 (planned: serving gateway + OCR) are committed to other themes.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at `docs/archive/v1/v1.17/constitution.md` (nor elsewhere under `docs/`) - skipping check. Recommend running `/constitution` to establish project principles. This is informational, not blocking.

Project-level guardrails that nonetheless apply to this plan (from `AGENTS.md` / `CLAUDE.md`): local-first / zero-outbound (all effects render client-side; no network, no telemetry, no new dependency under the reverse-engineered path); OS parity (the shell is cross-OS Tauri; effects must not assume a single platform, must respect forced-dark, and the WebGL metal must have a graceful fallback where WebGL is unavailable); the MCP Registry Policy reverse-engineer-first decision tree (import the techniques, not the packages; strip external palettes; use Nexus tokens); accessibility (honor `prefers-reduced-motion`, including a static fallback for WebGL); every changed line traces to an adoption item (A1-A4); per-phase testing and CI updates; ASCII-only commit messages.

## Phases at a Glance

| Phase | Title | Outcome | Rec. model / effort |
|-------|-------|---------|---------------------|
| 1 | Motion foundation (A4-foundation) | Shared motion tokens + centralized reduced-motion + recede-when-active primitive; unblocks all effects | Mid tier, medium - claude-sonnet-5 (medium): low-novelty foundation over the existing token layer |
| 2 | Agent-state orbs (A1, flagship) | Internal on-brand agent-state motion component + state->activity mapping across the four pillars; replaces ad-hoc loaders | Mid-strong tier, high - claude-sonnet-5 (high): design-sensitive Canvas motion, cross-cutting mapping, no security surface |
| 3 | Surface-liveness beam (A2) | Internal traveling/breathing beam on the input composer, active-model dock, generation-canvas frame | Mid tier, medium - claude-sonnet-5 (medium): CSS technique + placement judgment |
| 4 | Hero-action metal (A3) | Internal WebGL liquid-metal ring on send / primary Generate / New session; offscreen pause + reduced-motion fallback | Mid-strong tier, high - claude-sonnet-5 (high): WebGL reverse-engineering + perf discipline |
| 5 | Motion coordination + polish (A4-completion) | Recede-when-active wired across surfaces; one-motion-per-element enforced; final a11y + battery/perf audit | Mid tier, medium - claude-sonnet-5 (medium): coordination + audit over existing surfaces |
| 6 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Clean layout, v1.15/v1.16/v1.17 gaps reconciled, CI green and optimized | Strong tier, high - claude-opus-4-8 (high): repo-wide refactor + reference repair |

---

## Phase 1: Motion Foundation (A4-foundation)

**Goal**: Provide the shared primitives every effect depends on - motion tokens, a centralized `prefers-reduced-motion` mechanism, and a recede-when-active primitive that lets the ambient glow step back on active surfaces - so no effect ships uncoordinated or inaccessible.
**Prerequisites**: None. Ships first because it unblocks Phases 2-4.
**Stability Gate**: Motion tokens (durations, easings, state-accent aliases mapped to the locked accents/gradient) exist in `desktop/src/styles/tokens.css` and resolve via Tailwind v4 `@theme inline`; a single `useReducedMotion` hook + a CSS media block centralize reduced-motion, and the existing motion surfaces (constellation, generation canvas, floating logo) are refactored onto it with no change to their current reduced-motion outcome; a recede-when-active primitive (a shared state/context + a CSS affordance) exists that a surface can use to signal "I have an active effect, dim the ambient glow near me"; tests cover the hook and the token presence.
**Recommended model**: Mid tier, medium effort - claude-sonnet-5 (medium). Rationale: low-novelty foundation/token work over an existing layer; value is in centralization and coordination, not invention. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 1.1 - Shared motion tokens

**Objective**: Centralize motion values as design tokens A1-A3 consume.

**Prompt**:
> Extend `desktop/src/styles/tokens.css` with a motion-token group mirroring the existing token style (documented, grouped, consumed via Tailwind v4 `@theme inline`): durations (fast/base/slow), standard easings, and state-accent aliases that map to the existing `--accent-coding`, `--accent-chatbot`, `--accent-image`, `--accent-video`, and `--grad-signature`. Do NOT introduce new palettes. Keep everything additive; nothing in the existing token set changes. Acceptance: the motion tokens exist, resolve in the shell through the Tailwind theme, and reuse the locked accent/gradient tokens.

---

#### 1.2 - Centralized reduced-motion mechanism

**Objective**: One reduced-motion source of truth for CSS and JS-driven motion.

**Prompt**:
> Centralize `prefers-reduced-motion` handling. Add a `useReducedMotion` hook (subscribes to `matchMedia`, SSR/jsdom-safe) and a single CSS media block in `desktop/src/styles/globals.css`. Refactor the existing per-surface checks in `desktop/src/components/constellation.ts`, `ConstellationBackground.tsx`, `GenerationCanvas.tsx`, and `FloatingLogo.tsx` to use the shared mechanism WITHOUT changing their current reduced-motion outcome (animation fully paused/reduced, not merely slowed). Acceptance: reduced-motion is centralized; every existing animated surface halts under `prefers-reduced-motion: reduce`; prior behavior is preserved where it was already correct.

---

#### 1.3 - Recede-when-active primitive

**Objective**: Let a surface with an active effect dim the ambient glow near it, so motion never stacks.

**Prompt**:
> Implement a lightweight recede-when-active primitive: a shared React context (or a small store) that tracks which surface currently has an active orb/beam/metal effect, plus a CSS affordance the ambient layer (`ConstellationBackground` / radial-glow) reads to reduce its intensity around or during an active-surface effect. Keep it declarative (a surface wraps itself or sets a flag when it activates an effect) and cheap (no per-frame work in the ambient layer beyond an opacity transition). Do not wire every surface yet - that is Phase 5; here, deliver the primitive plus one reference integration to prove it. Acceptance: when the reference surface marks itself active, the ambient glow measurably recedes and restores on deactivate, with a smooth transition and no layout shift.

---

#### 1.4 - Testing and Stabilization

**Objective**: Cover the foundation; wire CI.

**Prompt**:
> Add tests: a unit test for `useReducedMotion` (correct value + updates on `matchMedia` change via the shell's jsdom setup); a test that a representative animated component halts under reduced-motion through the shared mechanism; a test that the motion tokens resolve; a test that the recede-when-active primitive toggles the ambient intensity flag on activate/deactivate. Run `npm run test:shell` + `npm run lint --workspace @nexus/desktop`; iterate to green. Update CI to cover `desktop/src/styles` + the touched components (path filters, concurrency cancel-in-progress, dependency caching). Do not proceed to Phase 2 until stable. After tests pass, run `/generate-session-history` for Phase 1.

---

### Phase 1 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (hook + reduced-motion + tokens + recede primitive)
- [x] No known regressions (existing constellation/glow/generation motion intact when motion is allowed)
- [x] Reduced-motion centralized and shell-wide; recede-when-active primitive proven on one surface
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 2

---

## Phase 2: Agent-State Orbs (A1, flagship)

**Goal**: A single internal component expresses agent activity as on-brand motion, mapped to Nexus's pillars and activities, replacing today's ad-hoc per-surface loaders.
**Prerequisites**: Phase 1 (motion tokens + reduced-motion + recede primitive).
**Stability Gate**: An internal agent-state motion component renders the mapped states, at a larger hero size and a small inline size, using only Nexus accent/gradient tokens; it honors reduced-motion (static fallback) and pauses offscreen; the primary agent surfaces (coding activity, chat streaming, one media-generation surface, and the model-status dock) render it in place of their prior ad-hoc loader; unit tests cover state selection, sizing, and reduced-motion.
**Recommended model**: Mid-strong tier, high effort - claude-sonnet-5 (high). Rationale: design-sensitive frontend motion (a distinctive, non-generic look matched to the locked identity) with a cross-cutting state->activity mapping; moderate complexity, no security surface. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 2.1 - Define the agent-state -> activity mapping

**Objective**: Fix the vocabulary of states and how they map to Nexus's pillars and agent activities.

**Prompt**:
> Define the agent-state vocabulary for Nexus, adapting thinking-orbs's six states (working, searching, solving, listening, composing, shaping) to Nexus's actual activities without importing its code. Produce a typed mapping (e.g. a `desktop/src/components/agentState/` module) from a Nexus activity to a state and an accent token: coding/tool-use -> working/solving (`--accent-coding`), memory/RAG retrieval and web/grep search -> searching, chat token streaming -> composing (`--accent-chatbot`), ASR/whisper capture -> listening (`--accent-chatbot`), image/video generation -> shaping (`--accent-image` / `--accent-video`), and an idle/default. Keep it data-driven so surfaces pass their activity and get the right state + accent. Document the rationale for each mapping. Acceptance: the mapping is typed, exhaustive over Nexus's activities, and references only locked accent tokens.

---

#### 2.2 - Build the internal agent-state motion component

**Objective**: Reverse-engineer the orb-style motion into an on-brand internal component.

**Prompt**:
> Build an internal agent-state motion component (e.g. `desktop/src/components/agentState/AgentStateOrb.tsx`) that reverse-engineers thinking-orbs's dotted thought-orb technique - do NOT add the `thinking-orbs` npm package. Use HTML5 Canvas 2D (no WebGL); drive color from the Phase 2.1 mapping's accent tokens and the signature gradient; support two size presets (a ~64px hero/avatar size and a ~20px inline size); cap device-pixel-ratio at 2; pause the animation loop when offscreen (IntersectionObserver) and when reduced-motion is set (Phase 1 hook), falling back to a static state indicator. Match Nexus's forced-dark identity; the look must read as bespoke to Nexus, not a generic imported widget (apply `[[hallmark-design]]` and `[[frontend-ui-engineering]]`). Acceptance: the component renders each mapped state distinctly at both sizes using Nexus tokens, pauses offscreen and under reduced-motion, and holds up visually against the existing shell.

---

#### 2.3 - Adopt it on the primary agent surfaces

**Objective**: Replace ad-hoc loaders on the main surfaces with the unified component.

**Prompt**:
> Replace ad-hoc loading/activity indicators on the primary agent surfaces with the Phase 2.2 component, passing each surface's current activity so the correct state renders: the coding activity indicator, chat streaming (where `isStreaming` is signalled), at least one media-generation surface (`desktop/src/components/GenerationCanvas.tsx` for Image Studio / Video Lab), and the model-status surfaces (`LocalModelStatus.tsx` / `LocalModelStatusDock.tsx`) where a working/loading state is shown. Have each adopting surface mark itself active via the Phase 1 recede-when-active primitive. Remove the superseded ad-hoc spinner markup you replace (scope: only the indicators you are replacing; do not refactor unrelated code). Use the inline size for dense surfaces and the larger size for hero/empty states. Acceptance: the same motion vocabulary appears across coding, chat, and media surfaces; replaced ad-hoc loaders are gone; no surface shows two competing indicators.

---

#### 2.4 - Testing and Stabilization

**Objective**: Cover the mapping, component behavior, and surface adoption; wire CI.

**Prompt**:
> Add tests: unit tests for the state->activity mapping (each activity yields the expected state + accent), component tests (renders the correct state, both sizes, static fallback under reduced-motion via the Phase 1 hook, offscreen pause logic where testable), and a test per adopted surface asserting the component renders with the surface's activity and that the prior ad-hoc loader is no longer present. Use `@testing-library/react` and the shell's jsdom setup; query by role/label, not canvas internals. Run `npm run test:shell` + lint; iterate to green. Update CI for the new component + adopted surfaces. Do not proceed to Phase 3 until stable. After tests pass, run `/generate-session-history` for Phase 2.

---

### Phase 2 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (mapping + component + surface adoption)
- [x] No known regressions (surfaces still show activity; no duplicate indicators)
- [x] No new frontend dependency added; only Nexus tokens used
- [x] Component honors reduced-motion and offscreen pause
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 3

---

## Phase 3: Surface-Liveness Beam (A2)

**Goal**: The input composer, active-model dock, and generation canvas feel alive via an internal traveling/breathing border beam, driven by Nexus accent tokens and coordinated with the ambient glow.
**Prerequisites**: Phase 1 (tokens + reduced-motion + recede primitive).
**Stability Gate**: An internal beam component (CSS `@property`) renders a breathing beam on input-composer focus, a traveling beam while the model streams, a subtle beam on the active-model dock, and a beam framing the generation canvas during a render; it uses accent tokens (per surface), auto-fits border-radius from the `--radius-*` tokens, is play/pause aware with a fade, honors reduced-motion (static border), and marks its surface active via the recede primitive; unit tests cover the on/off/reduced-motion states.
**Recommended model**: Mid tier, medium effort - claude-sonnet-5 (medium). Rationale: a well-understood CSS technique plus placement judgment over existing surfaces; low novelty, no new runtime. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 3.1 - Build the internal beam component

**Objective**: Reverse-engineer the traveling/breathing beam as an internal accent.

**Prompt**:
> Build an internal beam component (e.g. `desktop/src/components/AccentBeam.tsx`) that reverse-engineers border-beam's CSS `@property` traveling-beam and breathing-pulse - do NOT add the `border-beam` npm package and do NOT use its ocean/sunset palettes. Drive color from Nexus accent tokens, support both a breathing (focus) and traveling (streaming/active) mode, a strength/opacity control, a play/pause state with fade, and auto border-radius fit via the `--radius-*` tokens; honor reduced-motion (Phase 1) by rendering a static border. Have it register with the recede-when-active primitive when active. Acceptance: the component renders both modes on-brand, fades on idle, renders a static border under reduced-motion, and dims the ambient glow on its surface while active.

---

#### 3.2 - Apply the beam to the liveness surfaces

**Objective**: Place the beam where liveness matters.

**Prompt**:
> Apply the Phase 3.1 beam: a breathing beam around the chat/coding input composer on focus; a traveling beam around it while the model streams a response (drive from the streaming signal); a subtle beam on the active-model dock (`LocalModelStatus.tsx` / `LocalModelStatusDock.tsx`) when a model is loaded and ready; and a beam framing `GenerationCanvas.tsx` while an image/video render is in progress. Use the per-surface accent (coding/chatbot for the composer, image/video for the canvas). Ensure each surface uses the recede primitive so the ambient glow steps back while the beam is active. Do not stack the beam with the Phase 2 orb on the same element at the same time (defer full one-motion-per-element enforcement to Phase 5, but avoid obvious double-motion here). Acceptance: the composer, active-model dock, and generation canvas visibly come alive on focus/streaming/rendering, on-brand, without competing with the glow.

---

#### 3.3 - Testing and Stabilization

**Objective**: Cover the beam's states and placement; wire CI.

**Prompt**:
> Add tests: unit tests for the beam component (breathing vs traveling mode, strength prop, static border under reduced-motion, recede-primitive registration) and a test per surface asserting the beam mounts only in the active state (focus/streaming/rendering/model-ready). Use the shell's jsdom setup. Run `npm run test:shell` + lint; iterate to green. Update CI for the beam component + surfaces. Do not proceed to Phase 4 until stable. After tests pass, run `/generate-session-history` for Phase 3.

---

### Phase 3 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (beam states + placement)
- [x] No new frontend dependency; only Nexus tokens used
- [x] Beam honors reduced-motion and registers recede-when-active
- [x] No obvious double-motion on any single element
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 4

---

## Phase 4: Hero-Action Metal (A3)

**Goal**: The hero controls (send button, primary Generate button, New-session CTA) get a premium tactile WebGL liquid-metal ring, restrained to those controls, with a graceful fallback where WebGL or motion is unavailable.
**Prerequisites**: Phase 1 (tokens + reduced-motion + recede primitive).
**Stability Gate**: An internal WebGL liquid-metal component renders a ring on the send / primary Generate / New-session controls using Nexus accent tokens (not gold/silver/chromatic); it pauses offscreen (IntersectionObserver), caps its active instances, falls back to a static styled control under reduced-motion AND where a WebGL context is unavailable, and marks its surface active via the recede primitive; unit tests cover mount, fallback, and offscreen pause; a manual perf/battery note is recorded.
**Recommended model**: Mid-strong tier, high effort - claude-sonnet-5 (high). Rationale: WebGL reverse-engineering (shader/technique) plus real perf/battery and fallback discipline; higher complexity than the CSS/Canvas effects, no security surface. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 4.1 - Build the internal metal component (with fallback)

**Objective**: Reverse-engineer the liquid-metal ring as an internal, bounded WebGL accent.

**Prompt**:
> Build an internal liquid-metal component (e.g. `desktop/src/components/MetalAccent.tsx`) that reverse-engineers metal-fx's WebGL liquid-metal ring - do NOT add the `metal-fx` npm package and do NOT use its gold/silver/chromatic palettes. Drive tint from Nexus accent tokens; support a strength/opacity control and a paused state; pause the WebGL render loop when offscreen (IntersectionObserver) and cap the number of simultaneously animating instances (a shared registry) so hero controls do not oversubscribe the GPU. Provide a graceful fallback to a static, on-brand styled control when `prefers-reduced-motion` is set OR a WebGL context cannot be created (feature-detect; never crash). Acceptance: the component renders an on-brand metal ring, pauses offscreen, respects the instance cap, and degrades to a clean static control under reduced-motion and on WebGL-unavailable hosts.

---

#### 4.2 - Apply metal to the hero controls only

**Objective**: Place metal on the three hero controls, and nowhere else.

**Prompt**:
> Apply the Phase 4.1 metal accent to the hero controls only: the chat/coding send button, the primary Generate button in Image Studio / Video Lab, and the New-session CTA (`nexus.coding.newChat` surface / the shell's primary new-session control). Do NOT apply it to secondary buttons, chips, list items, or icons (N2: hero actions only). Use the per-surface accent token. Register each with the recede primitive while animating. Acceptance: exactly the three hero controls carry the metal ring; no secondary control does; the effect reads as premium, not ubiquitous.

---

#### 4.3 - Testing and Stabilization

**Objective**: Cover mount, fallback, instance cap, and placement; wire CI; record a perf note.

**Prompt**:
> Add tests: unit tests for the metal component (mounts a ring in the animating state; falls back to the static control under reduced-motion and when WebGL is unavailable - stub `getContext('webgl')` to return null; respects the instance cap) and a test asserting metal is present on the three hero controls and absent from a sampled secondary control. Use the shell's jsdom setup (WebGL is not available in jsdom, so exercise the fallback path there and keep the animating path behind a feature guard). Run `npm run test:shell` + lint; iterate to green. Record a short manual perf/battery observation (frame cost with all hero controls visible; confirm offscreen pause works) in the phase session history. Update CI for the metal component + hero surfaces. Do not proceed to Phase 5 until stable. After tests pass, run `/generate-session-history` for Phase 4.

---

### Phase 4 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (mount + fallback + instance cap + placement)
- [x] Metal is on hero controls only; no secondary control carries it
- [x] Graceful fallback under reduced-motion AND WebGL-unavailable; offscreen pause verified
- [x] No new frontend dependency; only Nexus tokens used
- [x] Perf/battery note recorded
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 5

---

## Phase 5: Motion Coordination + Polish (A4-completion)

**Goal**: Wire the recede-when-active rule across every adopting surface, enforce one primary motion per element, and run the final accessibility + battery/perf audit so the whole system reads as one intentional identity.
**Prerequisites**: Phases 2-4 (all three effects landed).
**Stability Gate**: Every surface that runs an orb/beam/metal effect participates in recede-when-active (the ambient glow steps back consistently); no element runs two primary motions simultaneously (enforced, not incidental); a documented accessibility pass confirms reduced-motion fallbacks everywhere; a documented battery/perf pass confirms offscreen pause and the WebGL instance cap hold under a realistic multi-effect screen; the shell has no visual double-motion regressions.
**Recommended model**: Mid tier, medium effort - claude-sonnet-5 (medium). Rationale: coordination + audit over the surfaces built in Phases 2-4; low novelty, high attention to consistency. Re-confirm against the then-current model set at implementation.

### Sub-tasks

#### 5.1 - Wire recede-when-active everywhere + enforce one-motion-per-element

**Objective**: Make the coordination rules global, not per-surface guesswork.

**Prompt**:
> Audit every surface that adopts an orb (Phase 2), beam (Phase 3), or metal (Phase 4) effect and ensure each participates in the recede-when-active primitive (Phase 1), so the ambient constellation/glow consistently steps back on an active surface. Enforce one primary motion per element: where a surface could show two effects at once (e.g. an active-model dock that is both "ready" (beam) and "working" (orb)), define and apply a precedence rule so only one renders at a time. Centralize the precedence so it is not re-decided per component. Acceptance: no element shows two primary motions simultaneously; the ambient glow recedes uniformly on active surfaces; precedence is defined in one place.

---

#### 5.2 - Final accessibility + battery/perf audit

**Objective**: Prove the whole motion system is accessible and bounded.

**Prompt**:
> Run a final pass over the complete motion system. Accessibility: confirm every effect (orb, beam, metal, ambient) has a correct `prefers-reduced-motion` fallback and that the shell is fully usable with motion reduced; apply `[[frontend-ui-engineering]]` and `[[hallmark-design]]` to confirm the result reads as bespoke and modern, not generic. Battery/perf: with a realistic screen that shows multiple effects at once (e.g. streaming chat with the composer beam, an active-model dock, and a visible hero button), confirm offscreen pause works, the WebGL instance cap holds, and there is no runaway frame cost; record the observations. Fix any gap found. Acceptance: documented accessibility and perf passes with no open regressions.

---

#### 5.3 - Testing and Stabilization

**Objective**: Lock the coordination behavior in tests; wire CI.

**Prompt**:
> Add tests: a test that the precedence rule yields exactly one primary motion for a surface signalling multiple states; a test that an active surface toggles the ambient recede flag; extend/adjust the reduced-motion tests to cover all three effect types. Run `npm run test:shell` + `npm run lint --workspace @nexus/desktop` + `npm run typecheck --workspace @nexus/desktop`; iterate to green. Update CI for the coordination layer. Do not proceed to Phase 6 until stable. After tests pass, run `/generate-session-history` for Phase 5.

---

### Phase 5 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (precedence + recede + reduced-motion across all three effects)
- [x] One motion per element enforced; ambient recede uniform
- [x] Accessibility + battery/perf audits documented with no open regressions
- [x] Session history generated for this phase
- [x] Ready to advance to Phase 6

---

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| (none - no constitution file; no violations) | | |

---

## Phase 6: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, and its CI/CD complete and optimized.
**Prerequisites**: All prior phases.
**Stability Gate**: The layout is clean (no deprecated/obsolete files, empty dirs, redundant files/dirs, or overcomplicated structure left un-triaged); the version's known gaps are reconciled; CI/CD covers every change and is optimized; project validation/tests pass.
**Recommended model**: Strong tier, high effort - claude-opus-4-8 (high). Repo-wide refactor + reference repair is high-risk - default to the strong reasoning tier.

### Sub-tasks

#### 6.1 - Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure introduced or exposed across Phases 1-5 (e.g. superseded loader components fully replaced by the agent-state system, duplicated motion helpers, dead style rules, any transitional scaffolding), then refactor toward a clean, intuitive layout via `[[project-refactor]]` and `[[docs-layout-refactor]]` (propose-then-apply, with confirmation; repair every reference for anything that moves). Confirm the `docs/archive/v1/v1.17/` tree is complete (this plan, the seeding comparison, phase session-histories, known-gaps).

#### 6.2 - Known-gaps reconciliation
**Objective**: Reconcile the version's open gaps.
**Prompt**:
> Reconcile this version's known gaps via `[[known-gaps-tracker]]`: create `docs/archive/v1/v1.17/known-gaps.md`, resolve/defer/transfer each still-open item carried from `docs/archive/v1/v1.16/known-gaps.md` (and any earlier carry-forward), and record any newly discovered v1.17 gaps (e.g. surfaces not yet migrated to the agent-state component; any surface where a beam or metal placement was intentionally deferred; the gigatoken "watch" item for a future Python-side bulk-tokenization need per N5; any WebGL fallback edge cases found in Phase 4/5). Finalize the per-minor known-gaps file for v1.17.

#### 6.3 - CI/CD create/update/optimize
**Objective**: CI/CD covers all changes and is optimized.
**Prompt**:
> Create or update the CI/CD pipeline so it covers every change in this plan (the motion foundation, the orb/beam/metal components and their adopted surfaces, and the coordination layer), then optimize it to reduce action minutes (path filters scoped to `desktop/src`, concurrency cancel-in-progress, dependency caching for npm) while keeping comprehensive testing. Keep it platform-agnostic; GitHub Actions is the primary example. Note the standing GitHub Actions budget freeze if it still applies at implementation time.

#### 6.4 - Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and CI/CD is green.
**Prompt**:
> Run the full validation/test suite (`npm run test:shell` + `npm run lint --workspace @nexus/desktop` + `npm run typecheck --workspace @nexus/desktop`), confirm the refactor changed no behavior, confirm CI/CD passes and the action-minute reduction is real, and iterate until clean. Update `docs/todos.md` (dashboard metrics + checked-off items) via `[[dev-progress-tracker]]`. Generate a session-history entry for this phase.

---

### Phase 6 Exit Checklist

- [x] All sub-tasks completed
- [x] All tests passing (desktop shell: unit + lint + typecheck)
- [x] No known regressions from prior phases
- [x] Known gaps reconciled (v1.16 carry-forward + new v1.17 items, including the gigatoken N5 watch item)
- [x] CI/CD covers all changes and is optimized
- [x] Session history generated for this phase
- [x] Ready to release v1.17.0 (cut as tag `v1.17.0` via `/update release`)
