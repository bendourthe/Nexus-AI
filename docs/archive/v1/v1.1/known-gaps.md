# v1.1.0 -- Known Gaps, Deferrals, and Carryovers

**Status**: finalized at v1.1.0 release (Phase 15.9, 2026-05-26). Cycle opened at Phase 1 (2026-05-18); Phases 2-14 (2026-05-19 through 2026-05-21); Phase 15 hardening + release gate (2026-05-26). At cycle close, every open item below was triaged. Items requiring operator action moved to [operator-actions.md](operator-actions.md) under stable IDs (OA-V1.1.0-15A through F + OA-V1.1.0-15-DR-A through D) and remain "Open" here as carryforward; architectural items rolling into the v1.2.0 cycle remain "Open" here and will be re-listed under `docs/versions/v1/v1.2.0/known-gaps.md` (with cross-references back to the v1.1.0 entry codes) when that cycle opens. No P0 / P1 release blockers remain. See `## 4. Carryforward map (v1.0.0 -> v1.1.0)` for the full triage.

**Phase 15.9 closure note**: The v1.0.0 known-gaps file at [../v1.0.0/known-gaps.md](../v1.0/known-gaps.md) is already finalized; no status flip required there. This file's status flip from `live` to `finalized` is the cycle-close gesture; the v1.2.0 cycle picks up every "Open" item below by code reference.

**Audience**: v1.1.0 phase authors, code reviewer, security reviewer, ops engineer, future-cycle planners
**Last updated**: 2026-05-26
**Sibling reviews**: [docs/versions/v1/v1.0.0/known-gaps.md](../v1.0/known-gaps.md) (the upstream cycle gap log this file inherits from); [docs/versions/v1/v1.1.0/plans/v1.1.0-cycle.md](plans/v1.1.0-cycle.md) (the active plan); [docs/versions/v1/v1.1.0/plans/phase-01-shared-core-and-carryforward-closure.md](plans/phase-01-shared-core-and-carryforward-closure.md) (Phase 1 detail); [docs/versions/v1/v1.1.0/plans/phase-02-rebrand-and-core-extraction.md](plans/phase-02-rebrand-and-core-extraction.md) (Phase 2 detail); [docs/versions/v1/v1.1.0/plans/phase-03-coding-module.md](plans/phase-03-coding-module.md) (Phase 3 detail); [docs/versions/v1/v1.1.0/plans/phase-05-hybrid-retrieval-and-local-embedder.md](plans/phase-05-hybrid-retrieval-and-local-embedder.md) (Phase 5 detail).

**Cycle context**: v1.1.0 is the stabilization-plus-expansion cycle. Phase 1 (commit `ec3ff0e`) opened the carryforward closure sweep with bounded items (storage-path rename, deprecationMessage injection, curator-cadence delete, CRLF/LF snapshot normalization, shared-core decision document). Phase 2 (commit `de219a5`) closed the rebrand + sidecar core-extraction half of the deferred Phase 1 work (manifest IDs, npm package rename, sidecar duplicate model catalogs). Phase 3 (this commit) lands the `src/` -> `modules/coding/` import-rewriting codemod and the first sub-tree migration (`src/utils/` -> `modules/coding/utils/`), partially closing 1.4.P1.B; the remaining 12 sub-tree moves stay deferred but the rest of the cycle now has the mechanical pipeline ready to consume. The heavier deferred items (TypeScript project-references wiring, the rest of the wholesale `src/` -> `modules/coding/` move, `NexusCodingRuntime` wiring, Tailwind v4) stay open. The remaining cycle phases (4-15) then layer agentmemory + SANA adoptions plus the cross-OS installer and Nexus VS Code extension on top of the v1.0.0 four-pillar app. The known-gaps file is appended phase-by-phase; items move to `## 2. Resolved` when closed in a later phase, and the `## 3. Summary` at the bottom is recomputed each pass. The file is finalized at v1.1.0 release (Phase 15 RTM).

Each entry has a severity tag:

- **P0** -- release-blocker for v1.1.0 (must close)
- **P1** -- should-fix in v1.1.0
- **P2** -- nice-to-have; documented for completeness
- **P3** -- out-of-scope for v1.1.0; explicitly recorded for future planning

Each entry has a category tag:

- **NI** (not implemented) -- a plan sub-task that was skipped
- **DF** (deferred) -- a plan sub-task explicitly deferred to a later phase / cycle
- **BG** (bug) -- a deviation that revealed a real defect
- **MT** (missing tests) -- a coverage shortfall
- **WN** (warning) -- a suppressed lint or runtime warning
- **QG** (quality gate) -- a Phase 7 gate the cycle author bypassed with "Proceed anyway"

---

## 1. Open Items

### 1.1.P1.A -- TypeScript project-references wiring deferred (DF, P1)

- **Source phase**: Phase 1 (1.1)
- **Plan reference**: [phase-01-shared-core-and-carryforward-closure.md](plans/phase-01-shared-core-and-carryforward-closure.md) sub-task 1.1 ("Land the TypeScript project-references infrastructure ... `tsc -b` from the repo root builds `core/`, `src/`, and `desktop/sidecar/` in dependency order").
- **Reason**: The decision document at [docs/versions/v1/v1.1.0/development/decisions/shared-core-build.md](development/decisions/shared-core-build.md) records option (a) -- project references with `composite: true` on `core/` -- as the chosen strategy. The actual wiring (new `core/tsconfig.json`, `references` arrays in the root and `desktop/tsconfig.json`, switching `npm run build` to `tsc -b`) was not landed in the Phase 1 commit because it interacts with the sub-task 1.4 wholesale `src/` -> `modules/coding/` move: the root tsconfig's `include` set currently emits `core/` into `out/core` AND `core/tsconfig.json` with `composite: true, outDir: "../out/core"` would emit to the same location, producing a double-emit conflict until either the root tsconfig is narrowed to `src/` only (which requires 1.4 to land first so the `modules/coding/` reference is the canonical entry point) or the build script is split into `build:core` + `build:src` chains. Doing it in the wrong order would either break `npm run build` on day one or leave a broken intermediate state that other phases would have to step around.
- **Suggested next step**: Land 1.1's wiring as the first commit cluster of the 1.4 follow-up (the wholesale `src/` move). The pre-req sequence is: (a) narrow root `tsconfig.json` `include` to only the moved `modules/coding/**/*` and `src/extension.ts` (if it still exists post-move), (b) add `core/tsconfig.json` with `composite: true`, (c) add `references: [{ "path": "./core" }]` to the root and to `desktop/tsconfig.json`, (d) rename `npm run build` to invoke `tsc -b`, (e) verify `npm run check-architecture` still passes against the new reference graph.

### 1.4.P1.B -- src/ -> modules/coding/ wholesale move partially closed; 12 sub-trees remain deferred (DF, P1)

- **Source phase**: Phase 1 (1.4)
- **Plan reference**: [phase-01-shared-core-and-carryforward-closure.md](plans/phase-01-shared-core-and-carryforward-closure.md) sub-task 1.4 ("Perform the wholesale move with `git mv`: `src/llm/` -> `core/llm/` (already exists -- merge) ... [13 source sub-tree moves listed]"); closes v1.0.0 carryforward 2.P2.I.
- **Reason**: The wholesale move is a ~190-source-file + 500+ test-import operation that cannot be validated in a single session without per-step CI runs. Each `git mv` cluster (a single sub-tree, e.g. `src/agents/` -> `modules/coding/agents/`) needs its own `npm test` + `npm run check-architecture` pass before the next sub-tree moves, otherwise compounding import-rewrite errors are hard to bisect. Phase 3 (this commit) lands the codemod infrastructure ([scripts/dev/rewrite-imports.mjs](../../../scripts/dev/rewrite-imports.mjs), see 3.P1.A) plus the first leaf-tree migration -- `src/utils/` -> `modules/coding/utils/` -- so the remaining sub-tree moves now become near-pure renames driven by the same script. The 12 remaining sub-trees stay open because each subsequent move benefits from its own commit + CI run, both for review readability and to keep the import-rewrite blast radius bounded per phase.
- **Status table (per sub-tree)**:
  | Sub-tree | Status | Closed in | Notes |
  |---|---|---|---|
  | `src/utils/` -> `modules/coding/utils/` | Closed | Phase 3 (this commit) | 6 files moved; 65 importers rewritten by codemod. |
  | `src/config/` -> `modules/coding/config/` | Open | -- | 9 files; candidate for Phase 4 (low coupling). |
  | `src/llm/` -> merge into `core/llm/` | Open | -- | 5 files; coordinate with `core/llm/{PromptFormat,ToolCallFormat}.ts` already in place. |
  | `src/observability/` -> `modules/coding/observability/` | Open | -- | 3 files; candidate for Phase 4. |
  | `src/orchestration/` -> `modules/coding/orchestration/` | Open | -- | 2 files; candidate for Phase 4. |
  | `src/guardrails/` -> `modules/coding/guardrails/` | Open | -- | 2 files. |
  | `src/mcp/` -> `modules/coding/mcp/` | Open | -- | small. |
  | `src/commands/` -> `modules/coding/commands/` | Open | -- | small. |
  | `src/agents/` -> `modules/coding/agents/` | Open | -- | medium. |
  | `src/chat/` -> `modules/coding/chat/` | Open | -- | medium; depends on `src/storage/`. |
  | `src/evaluation/` -> `modules/coding/evaluation/` | Open | -- | medium; check `generate-golden-tasks.mjs` output path. |
  | `src/skills/` -> `modules/coding/skills/` | Open | -- | medium; keep coding-side loader adapter under `modules/coding/skills/`. |
  | `src/runtime/` -> `modules/coding/runtime/` | Open | -- | unlocks 1.10.P1.F (NexusCodingRuntime sidecar wiring). |
  | `src/storage/` -> merge into `core/storage/` | Open | -- | heaviest; many consumers. |
  | `src/tools/` -> `modules/coding/tools/` | Open | -- | heaviest leaf; many consumers. |
  | `src/panels/` -> `modules/coding/panels/` | Open | -- | webview side; touches manifest paths. |
  | `src/extension.ts` -> `modules/coding/extension.ts` | Open | -- | Last move; flips `main` in package.json. |
- **Suggested next step**: Pick a Phase 4 candidate from the table above (suggest `src/config/` next: 9 files, leaf-shaped). Drive the next move with `node scripts/dev/rewrite-imports.mjs --moves <phase-4-manifest.json>` after `git mv`-ing the sub-tree. Land each move as its own commit + CI run. `src/extension.ts` is last; it triggers the manifest `main` field flip and unlocks the Phase 10 thin-adapter rewrite.

### 1.10.P1.F -- NexusCodingRuntime wiring into sidecar sessionManager deferred (DF, P1)

- **Source phase**: Phase 1 (1.10)
- **Plan reference**: [phase-01-shared-core-and-carryforward-closure.md](plans/phase-01-shared-core-and-carryforward-closure.md) sub-task 1.10 ("Replace the placeholder `sendMessage` body in `desktop/sidecar/src/coding/sessionManager.ts` with: instantiate `NexusCodingRuntime` once per session ..."); closes v1.0.0 carryforward 3.P1.M.
- **Reason**: `NexusCodingRuntime` is defined in `src/runtime/NexusCodingRuntime.ts`, which moves into `modules/coding/runtime/` as part of 1.4. Wiring it into the sidecar before the move would create an `import "../../../src/runtime/NexusCodingRuntime"` from `desktop/sidecar/`, which is exactly the brittle relative-path pattern 1.1 is designed to eliminate.
- **Suggested next step**: Land after 1.4 in "Phase 1b"; the wiring is one constructor call + one event-stream pump.

### 1.11.P1.G -- Tailwind v4 wiring deferred (DF, P2)

- **Source phase**: Phase 1 (1.11)
- **Plan reference**: [phase-01-shared-core-and-carryforward-closure.md](plans/phase-01-shared-core-and-carryforward-closure.md) sub-task 1.11 ("Add Tailwind v4 to `desktop/package.json` ..."); v1.0.0 carryforward 1.P2.B.
- **Reason**: The desktop workspace currently consumes CSS variables directly (`var(--token)`). Adding Tailwind v4 + PostCSS would re-expose the same variables as utility classes, but the build-pipeline change interacts with Phase 11 (the Nexus VS Code extension) since its webview also consumes these tokens. Doing it in Phase 1 would force a re-run of every visual-regression snapshot for net-zero behavioural change.
- **Suggested next step**: Fold into Phase 11 (Nexus VS Code extension) when the webview build pipeline is otherwise touched.

### 1.12.P2.H -- Phase 1 regression test for curator IdleTimeScheduler exclusivity deferred (MT, P2)

- **Source phase**: Phase 1 (1.7)
- **Plan reference**: [phase-01-shared-core-and-carryforward-closure.md](plans/phase-01-shared-core-and-carryforward-closure.md) sub-task 1.7 acceptance ("Add a regression test ... that asserts no curator runs occur outside `IdleTimeScheduler` invocations.").
- **Reason**: The Phase 1 commit removed the `_runOneIteration` curator block; an explicit regression test asserting "AgentLoop never dispatches a `curator-worker` sub-agent regardless of `curatorWorkerEnabled`" was not added because the field was made dead (`void options?.curatorWorkerEnabled`) and the existing test suite has no test that fires the curator from AgentLoop. The risk of regression therefore stems from a future re-introduction of the block, which a code review would catch.
- **Suggested next step**: Add `tests/integration/curator-scheduler-only-entry.test.ts` in "Phase 1b" with two cases: (a) construct AgentLoop with `curatorWorkerEnabled: true`, run 100 turns, assert `subAgentManager.run` is never called with `type: "curator-worker"`; (b) instantiate an `IdleTimeScheduler`, register a curator task gated by `nexus.curator.enabled`, advance the fake clock, assert it fires.

### 1.12.P2.I -- Operator action items inherited from v1.0.0 (DF, P2)

- **Source phase**: Phase 1 (carryforward)
- **Plan reference**: [docs/versions/v1/v1.0.0/operator-actions.md](../v1.0/operator-actions.md) OA-01 through OA-12; the v1.1.0 cycle plan defers their resolution to Phase 15 (release hardening).
- **Reason**: 12 operator-driven items (Authenticode signing, macOS notarization, AppImage assembly, DevAI-Hub baseline SHA rotation, final brand icons, live golden-task replay, GPU bench, live DevAI-Hub sync smoke, RTM smoke checklists, plus four others) require external infrastructure that is operator-procured (EV certs) or hardware-bound (RTX 4070 rig). Phase 1 inherits them as carryforward.
- **Suggested next step**: They surface in Phase 15's stability gate. No code change required in Phase 1.

### 4.3.P1.J -- HookBus emit sites partially wired (DF, P1)

- **Source phase**: Phase 4 (4.3)
- **Plan reference**: [phase-04-memory-provenance-and-hooks.md](plans/phase-04-memory-provenance-and-hooks.md) sub-task 4.3 ("Find the equivalents in [modules/coding/](../../../../src) (post-Phase-1.4 layout): ... emit `lifecycle.session.start` ... `lifecycle.session.stop` ... `lifecycle.user.prompt` ... `lifecycle.tool.pre` + `.post` (or `.failed`) ... `lifecycle.subagent.start` / `.stop` ... `lifecycle.context.preCompact` ... `SessionStore.close(...)` -> emit `lifecycle.session.end`").
- **Reason**: Phase 4 wired the five highest-blast-radius emit sites end-to-end (`AgentLoop.run` -> `lifecycle.session.start`/`.stop`; `_runToolCall` -> `lifecycle.tool.pre`/`.post`/`.failed`; `AgentLoop.spawnSubAgent` -> `lifecycle.subagent.start`/`.stop`). Four sites stay deferred because each touches a surface that has not been centralized in `core/` yet and would force a Phase 1.4 sub-tree move as a pre-req: (1) `lifecycle.user.prompt` lives in `ChatController.handleUserMessage` whose move into `modules/coding/` is still tracked under 1.4.P1.B; (2) `lifecycle.context.preCompact` ties into `Tracer.snapshot()` / `ContextCompactor.compact()` which spans two modules; (3) `lifecycle.session.end` requires a `SessionStore.close()` hook the sidecar's session manager does not yet expose (a Phase 1.10 dependency, see 1.10.P1.F); (4) `lifecycle.skill.entry` is intentionally deferred to Phase 8 per the plan's own note ("The `lifecycle.skill.entry` event is fired from Phase 8's `AgentLoop.setCurrentSkill(...)`").
- **Suggested next step**: Cluster the four remaining emit sites into a Phase 1b follow-up commit alongside the `src/runtime/` -> `modules/coding/runtime/` move (which unblocks (3)). For (2), thread the HookBus into `Tracer.snapshot()` via a constructor injection; the existing `_compactor.setTraceContext` call site is the natural anchor. For (4), wait for Phase 8.

### 4.1.P2.K -- v1.0.0 fixture SQLite database generated in-test rather than checked in (NI, P2)

- **Source phase**: Phase 4 (4.1)
- **Plan reference**: [phase-04-memory-provenance-and-hooks.md](plans/phase-04-memory-provenance-and-hooks.md) sub-task 4.1 prompt ("Write a fixture test that loads a v1.0.0 snapshot DB (commit a small one under `tests/fixtures/v1.0.0/memory.sqlite`) ...").
- **Reason**: The fixture is generated programmatically inside [tests/unit/storage/MemoryStore.provenance.test.ts](../../../tests/unit/storage/MemoryStore.provenance.test.ts) via `seedV1Db()` rather than checked in as a binary. The acceptance criterion ("migration runs idempotently and a fixture-based test loads a v1.0.0 database and verifies the new column is NULL-backfilled") is satisfied; the deviation is to avoid binary churn in git (the same approach the existing `MemoryStore.migration.test.ts` already uses for the v0.4.0 -> v0.5.0 migration).
- **Suggested next step**: Keep the in-test generator unless a multi-version fixture matrix becomes valuable; at that point, codify the schemas under `tests/fixtures/<version>/seed.ts` and have each test import the appropriate seeder.

### 5.1.P1.M -- LocalEmbedder runs against the deterministic hash fallback in CI (DF, P1)

- **Source phase**: Phase 5 (5.1)
- **Plan reference**: [phase-05-hybrid-retrieval-and-local-embedder.md](plans/phase-05-hybrid-retrieval-and-local-embedder.md) sub-task 5.1 ("Add `@xenova/transformers` to `package.json` dependencies. Add `core/memory/LocalEmbedder.ts` with `class LocalEmbedder { async embed(text: string): Promise<Float32Array> }` ... The installer (Phase 14) packs the weights so production hosts never hit the Hub.").
- **Reason**: `@xenova/transformers` is the binary-heavy ONNX runtime (`onnxruntime-node` transitive, plus the ~80 MB `all-MiniLM-L6-v2` weights downloaded on first use). The Phase 5 commit adds it as an `optionalDependencies` entry (so `npm ci` does not require it on CI hosts that lack the native build toolchain) and the `LocalEmbedder` class lazy-loads it via a guarded dynamic `import()`. When the import fails, the class transparently switches to a deterministic 384-dim hash sketch (`hashEmbed` in [core/memory/LocalEmbedder.ts](../../../core/memory/LocalEmbedder.ts)). The sketch is contractually identical (384-dim, deterministic, L2-normalized, batch-callable) so the rest of the pipeline -- `DenseIndex`, `HybridRetriever`, `WarmRebuildWorker` -- can be exercised end-to-end without the runtime payload, but it is NOT a substitute for real semantic embeddings in production. The unit tests (`tests/unit/core/memory/LocalEmbedder.test.ts`) run against the fallback by passing `forceFallback: true`, so CI assertions cover the fallback path verbatim; the real ONNX pipeline is exercised manually on the operator's GPU rig under OA-09 and again as part of Phase 14's installer payload smoke.
- **Suggested next step**: When the cross-OS installer lands in Phase 14, add an integration test that runs against the bundled weights at `~/.nexus/runtimes/embedder/all-MiniLM-L6-v2/` and asserts: (a) `backend === "transformers"` after first use; (b) deterministic vector for "hello world" matches a fixture; (c) cosine similarity between semantically-similar sentence pairs is materially higher than the hash sketch produces. The test gates the v1.1.0 RTM by depending on the installer's weight payload, so it cannot run earlier than Phase 14.

### 5.5.P1.N -- `src/storage/UnifiedMemoryRetriever` not yet migrated; delegation happens at the `MemoryHub` level instead (DF, P1)

- **Source phase**: Phase 5 (5.5)
- **Plan reference**: [phase-05-hybrid-retrieval-and-local-embedder.md](plans/phase-05-hybrid-retrieval-and-local-embedder.md) sub-task 5.5 ("Replace the substring path in `UnifiedMemoryRetriever` with a delegating call to `HybridRetriever`. Keep the substring path as a fast-path-fallback for very small corpora (<100 entries) where BM25 is overkill.").
- **Reason**: `src/storage/UnifiedMemoryRetriever` is a 312-line legacy class whose retrieval path is not substring-based: it dispatches per-layer token budgets, fuses a hybrid HNSW + FTS5 + recency ranking via the v0.9.0 Phase 2.2 `MemoryStore.retrieveHybrid`, and falls back to the v0.7.0 keyword + cosine merge on the legacy route. The literal substring path the plan describes lives in `core/memory/MemoryHub.ts::InMemoryMemoryHub.retrieve` -- the four-layer in-memory facade that pillars (Chat, Image, Video) consume during Phase 2-5 before the SQLite stack is wired in. Phase 5 wires the new `HybridRetriever` into that hub (with the documented `<100`-entry substring fast-path fallback gated by `nexus.memory.hybridMinCorpus`) so the user-visible semantics of "substring on tiny corpora, hybrid on real ones" are satisfied; the deeper migration of `src/storage/UnifiedMemoryRetriever` will fold into the Phase 6/9 work where `MemoryStore` itself gains the BM25 / dense indexes alongside its existing HNSW path. The deviation note: the plan over-simplified the existing retriever's role; the corrected interpretation is that the hybrid path lands in the `MemoryHub` first (where it has consumers today) and migrates into the SQLite-backed `UnifiedMemoryRetriever` second.
- **Suggested next step**: In Phase 6 (memory CLI + decay) or Phase 9 (consolidation), add a `MemoryStore`-backed adapter that builds `Bm25Index` + `DenseIndex` from the persistent rows on startup and wires them into a `HybridRetriever` consumed by `UnifiedMemoryRetriever.retrieve(query)` for the semantic layer's budget. The substring path stays as a fast-path fallback for corpora <100 entries via `nexus.memory.hybridMinCorpus`.

### 5.6.P2.O -- IdleTimeScheduler binding for `memory.warm-rebuild` not yet wired in the sidecar (DF, P2)

- **Source phase**: Phase 5 (5.6)
- **Plan reference**: [phase-05-hybrid-retrieval-and-local-embedder.md](plans/phase-05-hybrid-retrieval-and-local-embedder.md) sub-task 5.6 ("Add an `IdleTimeScheduler` worker `memory.warm-rebuild` that on first launch (or when the indexes are detected as stale -- a hash-of-row-count mismatch) reads all `memory_entries` rows and embeds them in batches of 32.").
- **Reason**: The worker code (`core/memory/WarmRebuildWorker.ts::warmRebuild` plus `createWarmRebuildTask`) ships with full unit-test coverage (11 tests including the 10,000-row latency budget). The `createWarmRebuildTask` helper returns the exact `{id, idleThresholdMs, cadenceMs, run()}` shape that `IdleTimeScheduler.register` expects, but the actual `scheduler.register(createWarmRebuildTask(...))` call site in `desktop/sidecar/` has not been wired because it needs (a) a `MemoryStore`-backed `WarmRebuildSource` implementation (currently the worker only knows about an in-memory rows array; see 5.5.P1.N for the related migration story) and (b) a singleton `LocalEmbedder` injected at sidecar boot. Both prerequisites cluster naturally with the Phase 6 CLI work (which adds the persistent-memory adapters anyway) or the Phase 10 thin-adapter rewrite (which is where the sidecar's `IdleTimeScheduler` is materialized).
- **Suggested next step**: In Phase 6, build the `MemoryStoreWarmRebuildSource` (wraps `MemoryStore.iterateAllRows()`, hashes row count + last-updated timestamp into `fingerprint()`), instantiate one shared `LocalEmbedder` per sidecar (`LocalEmbedder.fromInstallPath()`), and register the task on the existing `IdleTimeScheduler` instance. Acceptance: a fresh-from-defaults sidecar fires the warm-rebuild within ~5 s of becoming idle, surfaces progress via `lifecycle.notification`, and the Memory panel reflects the rebuild status.

### 6.1.P2.P -- SQLite-backed `memory_audit_log` table not yet wired; CLI consumes captured JSONL snapshots (DF, P2)

- **Source phase**: Phase 6 (6.1)
- **Plan reference**: [phase-06-memory-cli-decay-and-slash-commands.md](plans/phase-06-memory-cli-decay-and-slash-commands.md) sub-task 6.1 ("Reads from `memory_entries` + `memory_audit_log` (a new lightweight log table written on every read/write/delete; add to the Phase 4 migration).").
- **Reason**: Phase 6 ships the audit log as an in-memory `InMemoryAuditLog` plus a pure formatter (`formatAuditTable` / `formatAuditJsonl`) and threads the log through the slash-command handlers so `/recall` / `/remember` / `/forget` already write audit rows. The persistence boundary -- a SQLite-backed `MemoryAuditLog` implementation against a new `memory_audit_log` table in the v1.1.0 Phase 4 migration -- was not landed because Phase 4's `_runMigrations()` block already bumped `MEMORY_SCHEMA_VERSION` to 3 for the provenance + scope_id columns, and adding the audit table without a coordinated migration version bump would split the schema upgrade across two commits with no clean rollback. Phase 6's CLI surface therefore reads from a captured JSONL snapshot via `--source <path.jsonl>`; the sidecar wires the in-memory log directly into the slash-command handlers (no JSONL hop) so the production path is fully exercised end-to-end without the persistence layer.
- **Suggested next step**: In Phase 7 or 8, bump `MEMORY_SCHEMA_VERSION` to 4 and add the `memory_audit_log` table (columns mirror `MemoryAuditRow`: `timestamp INTEGER NOT NULL, op TEXT NOT NULL, tier TEXT NOT NULL, entry_id TEXT NOT NULL, session_id TEXT NULL, hook_kind TEXT NULL, tool_name TEXT NULL, text_preview TEXT NOT NULL, PRIMARY KEY (timestamp, entry_id, op)`). Implement `SqliteMemoryAuditLog implements MemoryAuditLog` against it, and have the sidecar swap the in-memory instance for the SQLite one at boot. The CLI's `--source` flag remains useful for forensic replay against captured snapshots.

### 6.2.P2.Q -- `nexus memory export` / `nexus memory import` CLI surface consumes injected JSONL rather than the live store (DF, P2)

- **Source phase**: Phase 6 (6.2)
- **Plan reference**: [phase-06-memory-cli-decay-and-slash-commands.md](plans/phase-06-memory-cli-decay-and-slash-commands.md) sub-task 6.2 ("Output: one JSONL line per row, full row contents (text, vector base64, provenance, tier, timestamps).").
- **Reason**: The core export/import logic lives in [core/memory/MemoryExport.ts](../../../core/memory/MemoryExport.ts) (`exportToJsonl(source, filter)` + `importFromJsonl(text, sink)` + `encodeVectorB64` / `decodeVectorB64` + `isPathInside` path-traversal guard) and is fully exercised by [tests/unit/core/memory/MemoryExport.test.ts](../../../tests/unit/core/memory/MemoryExport.test.ts) (12 cases including the round-trip integrity assertion). The CLI surface in [bin/nexus.mjs](../../../bin/nexus.mjs) calls into the same code path with a `--source <jsonl>` injection. The end-to-end "CLI binds to the live `MemoryStore` and walks every row" wiring is deferred for the same schema-coordination reason as 6.1.P2.P: the production path runs through the desktop daemon's sidecar where the export/import code is already wired (the sidecar instantiates `MemoryStoreExportSource` directly), so the CLI's `--source` injection is the operator-facing surface for forensic replay rather than a primary user path. The acceptance criterion ("a small corpus exports and reimports via `nexus memory import` with round-trip integrity") is satisfied at the unit level.
- **Suggested next step**: Add a `MemoryStoreExportSource` adapter under [src/storage/MemoryExportAdapters.ts](../../../src/storage) (wraps `MemoryStore.listAll(limit)` and projects each row into `ExportableRow`); add a matching `MemoryStoreImportSink`; have the sidecar inject them at boot so the desktop app's export button writes the live store without any JSONL intermediate. Land alongside 6.1.P2.P.

### 6.5.P2.R -- Memory panel "Forget" button signals via callback; IPC delete pipeline deferred (DF, P2)

- **Source phase**: Phase 6 (6.5)
- **Plan reference**: [phase-06-memory-cli-decay-and-slash-commands.md](plans/phase-06-memory-cli-decay-and-slash-commands.md) sub-task 6.5 ("Add a 'Forget' button on each row in the Memory panel ... that calls `memory.delete(id)` IPC -> sidecar handler -> `MemoryHub.delete(id)`.").
- **Reason**: Phase 6 adds the `onForget` callback on `MemoryPanel` (rendered as a "Forget" button per row) plus the two panel tests asserting render + click-dispatch. The actual IPC pipeline (`memory.delete(id)` in the protocol -> sidecar handler -> `MemoryHub.delete(id)`) is deferred because the `MemoryHub` interface in [core/memory/MemoryHub.ts](../../../core/memory/MemoryHub.ts) does not yet expose a `delete(id)` method -- the four-layer in-memory facade was designed write-add-only with bulk `clear()` and `retagScope()` semantics. Adding `delete(id)` requires per-layer wiring (`WorkingMemory.deleteById`, `EpisodicMemory.deleteById`, `SemanticMemory.deleteById`, `GraphMemory.deleteByEntryId`), the matching protocol schema field, the sidecar handler, and the `desktop/sidecar/src/coding/panelData.ts` route -- a non-trivial cross-cutting change. The `/forget` slash command exercises the same `MemoryWritePort.delete(id)` interface at the handler layer (covered by [tests/unit/core/memory/MemorySlashCommands.test.ts](../../../tests/unit/core/memory/MemorySlashCommands.test.ts)) so the contract is proven; only the panel-button-to-sidecar path is left.
- **Suggested next step**: Extend `MemoryHub` with `delete(id: string): Promise<boolean>` (return `true` when the row existed). Add `memory.delete` to the sidecar protocol; route it to the live hub. In the desktop panel, swap the placeholder `onForget` consumer for an IPC call that confirms via the existing `ConfirmationGate` then dispatches `memory.delete(id)`. Cluster with 6.1.P2.P / 6.2.P2.Q so the schema migration and IPC additions land together.

### 6.6.P2.S -- DecaySweep `IdleTimeScheduler` binding deferred to sidecar wiring (DF, P2)

- **Source phase**: Phase 6 (6.6)
- **Plan reference**: [phase-06-memory-cli-decay-and-slash-commands.md](plans/phase-06-memory-cli-decay-and-slash-commands.md) sub-task 6.6 ("Register the sweep as an `IdleTimeScheduler` worker with a 24-hour cadence.").
- **Reason**: The sweep worker ([core/memory/DecaySweep.ts](../../../core/memory/DecaySweep.ts)) is feature-complete with the closed-form Ebbinghaus retention curve (`retentionAt(elapsed, halfLife) = exp(-elapsed / halfLife * ln(2))`), per-tier half-lives (24h / 7d / 30d / 365d), the `retention < 0.05 AND accessCount < 3` eviction rule, and full unit-test coverage including the "5% of the math" assertion against a 100-row synthetic corpus. The `nexus memory decay --now` CLI surface fires the sweep manually for debugging. The `IdleTimeScheduler.register({id: "memory.decay-sweep", idleThresholdMs: 5 * 60_000, cadenceMs: 24 * 60 * 60_000, run: () => sweep.sweep()})` call site in the sidecar is deferred for the same reason as 5.6.P2.O ("warm-rebuild" worker): the sidecar needs a `MemoryStore`-backed `DecayProvider` adapter (currently the worker only knows about an array of `DecayableEntry` rows) and the corresponding tombstone table.
- **Suggested next step**: In Phase 7 or 8 (alongside the schema migration that lands 6.1.P2.P + 6.2.P2.Q), add `MemoryStoreDecayProvider` (wraps `MemoryStore.listAll().map(rowToDecayableEntry)` and `MemoryStore.deleteById` for eviction) plus a `memory_tombstones` table (30-day retention). Register the worker at sidecar boot.

### 8.1.P2.T -- `fs.watch` on the ACTIVE pointer not yet wired into the sidecar daemon entry point (DF, P2)

- **Source phase**: Phase 8 (8.1)
- **Plan reference**: [phase-08-devai-hub-closures.md](plans/phase-08-devai-hub-closures.md) sub-task 8.1 ("In `desktop/sidecar/src/runtime/codingBootstrap.ts`, add `fs.watch(activeTagPointerPath(...), ...)` ... guarded with debounce (200 ms)").
- **Reason**: Phase 8 ships the watcher core in [core/skills/SkillsReloader.ts](../../../core/skills/SkillsReloader.ts) with 200 ms debounce, `onReload`/`onError` callbacks, and graceful behaviour when the pointer file does not yet exist. `bootstrapCoding()` accepts an optional `skillCatalog` and constructs the reloader when supplied. The remaining wiring is to (a) instantiate the actual `SkillCatalog`-backed adapter in the sidecar daemon entry point (currently still placeholder) and (b) pass it to `bootstrapCoding()`. That wiring blocks on the same `MV` adapter that the v1.0.0 `SkillCatalog` doc-comment references; the bootstrap surface is ready when the adapter lands. The "Sync now" UI feedback ("Loaded N new skills") plumbing reuses the `onReload(tag)` callback and is a one-line addition once the IPC channel is widened in Phase 10.
- **Suggested next step**: After the SkillCatalog-backed adapter lands (tracked in the v1.0.0 known-gaps under `MV`), pass `{skillCatalog: adapter}` to `bootstrapCoding()` from the sidecar entry point. No production code outside the entry point needs to change; the reloader is already constructed-and-started by `bootstrapCoding()` when the catalog is supplied. Add an end-to-end integration test that runs a real `nexus skills sync --apply` against a fixture upstream and asserts the running daemon's catalog log reports a reload within 500 ms (matches the plan's acceptance phrasing).

### 8.3.P2.U -- `nexus skills install --from <url>` integration test against a live HTTPS source deferred to Phase 15 (DF, P2)

- **Source phase**: Phase 8 (8.3)
- **Plan reference**: [phase-08-devai-hub-closures.md](plans/phase-08-devai-hub-closures.md) sub-task 8.3 ("Acceptance: end-to-end test installs a tiny fixture skill from a `file://` URL (allowlisted in test mode), confirms the scanner runs, confirms the file lands in the right place.").
- **Reason**: The unit tests in [tests/unit/core/skills/SkillInstaller.test.ts](../../../tests/unit/core/skills/SkillInstaller.test.ts) cover the full pipeline (parseSkillSpec -> allowlist -> fetch -> scanner -> path-clamped write) via the injectable `Fetcher` port + the `NEXUS_SKILLS_TEST_MODE=1` `file://` allow gate. The acceptance criterion (file:// fixture install) is satisfied at the unit level. The remaining work is a live HTTPS install against a real `bendourthe.com` or `raw.githubusercontent.com` URL exercised on the RTM smoke checklist. Phase 15 (RTM) is the natural home for the live-network smoke; the unit + injection scanner coverage is sufficient until then.
- **Suggested next step**: In Phase 15, add `tests/integration/skills-install-live.test.ts` (gated by an env-var so CI does not require network egress) that fetches a small SKILL.md from `raw.githubusercontent.com/bendourthe/...`, runs the scanner, and asserts the file lands in the expected user-skills directory.

### 8.4.P2.V -- `nexus.skills.preferUpstream` consumer wired in the slash-command autocomplete catalog but not in the daemon's slash dispatcher (DF, P2)

- **Source phase**: Phase 8 (8.4)
- **Plan reference**: [phase-08-devai-hub-closures.md](plans/phase-08-devai-hub-closures.md) sub-task 8.4 ("Modify the slash-command autocomplete in `modules/coding/chat/SlashCommandRouter.ts` ... to consult `SettingsStore.get('nexus.skills.preferUpstream')`.").
- **Reason**: Phase 8 ships `filterSlashCommandsWithSkills(input, skills, {preferUpstream})` in [desktop/src/modules/coding/slashCommands.ts](../../../desktop/src/modules/coding/slashCommands.ts) so the autocomplete UI surfaces the preferred variant first when the chat composer reads the setting. The daemon-side slash-command dispatcher (the path that actually resolves a chosen `/<name>` to a `SkillRecord` when both `user/<name>` and `devai-hub/<name>` exist) still uses the legacy `SkillLoader.getSkill(name)` lookup keyed by display name. The dispatcher path is not exercised today because the post-Phase-1.4 `modules/coding/chat/SlashCommandRouter.ts` layout the plan references is one of the 12 sub-tree migrations still tracked under 1.4.P1.B. When `src/chat/` moves to `modules/coding/chat/`, the dispatcher should read the same setting from the `SettingsStore` injected via `ChatCommandContext`. The user-visible behaviour (autocomplete ordering) ships in this phase; the under-the-hood resolution is unblocked once the chat sub-tree migrates.
- **Suggested next step**: After `src/chat/` -> `modules/coding/chat/` migrates (open under 1.4.P1.B), update the slash-command dispatcher to read `nexus.skills.preferUpstream` and resolve same-named candidates accordingly. Add a regression test pair (`preferUpstream=true` -> picks devai-hub variant; `=false` -> picks user variant) against a fixture catalog with both namespaces populated.

### 4.5.P2.L -- Sidecar producer for `MemorySnapshot.provenance` / `TraceEvent.hookKind` not yet wired (DF, P2)

- **Source phase**: Phase 4 (4.5)
- **Plan reference**: [phase-04-memory-provenance-and-hooks.md](plans/phase-04-memory-provenance-and-hooks.md) sub-task 4.5 acceptance ("provenance chips render correctly on a fresh session's memory entries; trace filter narrows correctly").
- **Reason**: Phase 4 added the consumer-side surface (Memory panel "Show provenance" toggle + chips; TraceDashboard `hookKind` filter dropdown) plus the protocol schema fields (`MemorySnapshot.provenance` optional map; `TraceEvent.hookKind` optional string). The sidecar producer at [desktop/sidecar/src/coding/panelData.ts](../../../desktop/sidecar/src/coding/panelData.ts) still ships placeholder snapshots and a placeholder trace stream; both omit the new fields, so against a fresh-from-defaults sidecar the chips do not appear and the dropdown is empty. The UI degrades gracefully (toggle still toggles; dropdown shows `(all)`), so the acceptance is met for the synthetic test fixtures the panels are exercised against.
- **Suggested next step**: Wire the `MemoryHub` -> `panelData.memorySnapshot()` pipeline to read `LifecycleProvenance` off each layer entry, and have the `TelemetryBus` -> `traceSubscribe()` pipeline copy `event.payload.kind` (when it starts with `lifecycle.`) onto `TraceEventT.hookKind`. Cluster with Phase 5's hybrid retrieval since `HybridRetriever` will be the natural producer of layer entries with full provenance.

### 9.1.P2.W -- `memory.contradiction-sweep` IdleTimeScheduler binding deferred to sidecar wiring (DF, P2)

- **Source phase**: Phase 9 (9.1)
- **Plan reference**: [phase-09-memory-consolidation-optin.md](plans/phase-09-memory-consolidation-optin.md) sub-task 9.1 ("Register `core/memory/ContradictionResolver.ts` as an `IdleTimeScheduler` worker `memory.contradiction-sweep` on a 1-hour cadence; only runs when `nexus.memory.consolidation.enabled=true`.").
- **Reason**: The resolver ([core/memory/ContradictionResolver.ts](../../../core/memory/ContradictionResolver.ts)) ships with the full sweep + adjudication pipeline and `createContradictionSweepTask(resolver)` returns the exact `{id: "memory.contradiction-sweep", idleThresholdMs: 5 * 60_000, cadenceMs: 60 * 60_000, run()}` shape `IdleTimeScheduler.register` expects. The sidecar wiring (instantiating `SemanticTierProvider` against the live `MemoryStore`, injecting the production `OllamaChatLike` against `OllamaClient`, registering the task with the desktop daemon's `IdleTimeScheduler` instance) clusters with the same `MemoryStore`-backed adapter cluster that holds `memory.warm-rebuild` (5.6.P2.O) and `memory.decay-sweep` (6.6.P2.S) -- the four-worker family shares a single boot path. Internal `sweep()` is exercised end-to-end by [tests/integration/memory-consolidation-optin.test.ts](../../../tests/integration/memory-consolidation-optin.test.ts); the toggle-off short-circuit path is asserted there and in the unit suite.
- **Suggested next step**: Cluster with 5.6.P2.O / 6.6.P2.S. Once `MemoryStoreSemanticTierProvider` lands (a thin adapter that wraps `MemoryStore.semanticRows()` + a `markSuperseded(id, supersederId, log)` method backed by a new `superseded_by TEXT NULL` column + a `memory_resolution_log` JSON column), instantiate one `ContradictionResolver` per sidecar with the live `OllamaClient`, register the task, and have the Memory tab settings UI surface the `nexus.memory.consolidation.enabled` toggle.

### 9.2.P2.X -- `/memory-compress` sidecar dispatch + Memory panel surface deferred (DF, P2)

- **Source phase**: Phase 9 (9.2)
- **Plan reference**: [phase-09-memory-consolidation-optin.md](plans/phase-09-memory-consolidation-optin.md) sub-task 9.2 ("Add `/memory-compress <path>` slash command that wraps the same code.").
- **Reason**: Phase 9 ships `handleMemoryCompress(input, ctx)` ([core/memory/MemorySlashCommands.ts](../../../core/memory/MemorySlashCommands.ts)) and the `memory-compress` autocomplete catalog entry ([desktop/src/modules/coding/slashCommands.ts](../../../desktop/src/modules/coding/slashCommands.ts)). Direct unit coverage exercises the contract (missing path, missing compressor, gate-off, happy-path). The daemon-side slash dispatcher path that resolves a chosen `/memory-compress` to the `handleMemoryCompress` body uses the same `ChatCommandContext` plumbing that 8.4.P2.V references; the legacy `src/chat/SlashCommandRouter.ts` only knows about the v1.0.0 catalog, so wiring the new handler at the daemon clusters with the `src/chat/` -> `modules/coding/chat/` sub-tree migration tracked under 1.4.P1.B.
- **Suggested next step**: After `src/chat/` migrates (under 1.4.P1.B), have the dispatcher import `handleMemoryCompress` from `core/memory/MemorySlashCommands.ts` and add a `compressor: FileCompressor | null` field to the chat context built at sidecar boot. The compressor is constructed once per sidecar with the live `LocalEmbedder` + `SemanticWriter` + `OllamaClient`; the toggle reads from `SettingsStore` on every command invocation so the user can flip it without restarting.

### 10.1.P1.Z -- Proxy-branch daemon IPC client deferred (DF, P1)

- **Source phase**: Phase 10 (10.1)
- **Plan reference**: [phase-10-vscode-thin-adapter-and-republish.md](plans/phase-10-vscode-thin-adapter-and-republish.md) sub-task 10.1 prompt ("every command handler, every panel (`NexusCodingPanel`, `MemoryPanel`, `TraceDashboardPanel`) is a thin webview shell that forwards `postMessage` calls into the IPC client").
- **Reason**: Phase 10 lands the activation-shape rewrite: `extension.ts` shrinks from 478 lines to 64; `activateProxy` and `activateExtensionOnly` are separated; `discoverDesktopDaemon()` drives the dispatch. The proxy branch today registers all six `nexus.coding.<cmd>` IDs and surfaces an "open the desktop app" hint per invocation. The actual sidecar IPC client (named pipe on Windows, Unix domain socket on macOS / Linux, `tauri::Channel` bridge for streaming) is the upstream Phase 2 deliverable; per the v1.1.0 cycle plan the IPC widening is tracked separately (see Phase 2 known-gap cluster 3.P1.N / 3.P1.Q / 3.P2.U / 4.P1.W / etc. carried forward from v1.0.0). When that client lands, the proxy branch's six handlers swap their information-message body for an IPC `client.send(commandId, ...args)` call; the activation surface (status bar item, command IDs, compat shim, output channel) is already correct.
- **Suggested next step**: Cluster with the Phase 2 IPC widening commit; ship a `src/desktop/ipcClient.ts` (named pipe / domain socket round-trip), wire it into the six proxy handlers, replace the desktop-app hint surface with daemon-streamed responses, and add an integration test that round-trips `nexus.coding.ping` through a stub daemon.

### 10.1.P2.AA -- Thin-adapter webview shells for `NexusCodingPanel` / `MemoryPanel` / `TraceDashboardPanel` deferred (DF, P2)

- **Source phase**: Phase 10 (10.1)
- **Plan reference**: [phase-10-vscode-thin-adapter-and-republish.md](plans/phase-10-vscode-thin-adapter-and-republish.md) sub-task 10.1 ("every panel ... is a thin webview shell that forwards `postMessage` calls into the IPC client").
- **Reason**: The three panels currently live under `src/panels/` and own their own composition graph (memory hub, tracer, conversation manager); turning them into thin webview shells requires the daemon IPC client (10.1.P1.Z) to be present so the shells have something to forward to. Today the proxy branch registers no webview view providers at all (verified by the proxy unit test); the extension-only branch keeps the full panels intact. The activation surface is correct; only the daemon-backed panel rewrites remain.
- **Suggested next step**: After 10.1.P1.Z lands, extract three thin `src/panels/proxy/<PanelName>Proxy.ts` shells that host the same HTML / CSS payloads as today but route every `postMessage` through the IPC client. Wire them into `activateProxy(...)` behind the same view IDs (`nexus.coding.chatView`, `nexus.coding.memoryPanel`, `nexus.coding.traceDashboard`).

### 10.3.P2.BB -- Marketplace publish + legacy listing transition note are operator actions (DF, P2)

- **Source phase**: Phase 10 (10.3)
- **Plan reference**: [phase-10-vscode-thin-adapter-and-republish.md](plans/phase-10-vscode-thin-adapter-and-republish.md) sub-task 10.3 acceptance ("both listings are visible; clicking through 'Get' on the new listing installs the rebranded extension; existing `gemma-code` users see the transition note").
- **Reason**: Publishing a VSIX to the Marketplace requires the publisher PAT, which is operator-procured and must not be committed. The code-level rename (manifest IDs, npm package name, publisher, command IDs, view-container ID, settings keys) landed in Phase 1.6 (commit `de219a5`); the build artefact is reproducible via `npm run package`; the operator-action steps are documented at [docs/versions/v1/v1.1.0/marketplace-transition.md](marketplace-transition.md). The legacy `gemma-code` listing's transition note edit also requires the legacy publisher's PAT.
- **Suggested next step**: Operator runs the OA-V1.1.0-10A + OA-V1.1.0-10B checklist from [marketplace-transition.md](marketplace-transition.md) prior to Phase 15 RTM. Surfaces in Phase 15's operator-actions consolidation.

### 11.1.P2.CC -- Phase 11 proxy panels render the placeholder shell pending the daemon IPC client (DF, P2)

- **Source phase**: Phase 11 (11.1 - 11.7)
- **Plan reference**: [phase-11-nexus-vscode-extension.md](plans/phase-11-nexus-vscode-extension.md) sub-tasks 11.1 - 11.7 (multi-model picker, plan mode, auto mode, memory panel, slash autocomplete, sub-agent + sessions, MCP).
- **Reason**: Phase 11 lands the seven Nexus-VS-Code-extension surfaces as pure projectors / reducers under [core/coding/](../../../core/coding) (`ModelDropdown.ts`, `PlanArtifact.ts`, `AutoModeStream.ts`, `MemorySnapshotView.ts`, `SlashAutocomplete.ts`, `SessionList.ts`, `McpBridge.ts`), each with structural ports and a full unit test (100 new tests; 11 new test files; full suite still 3387 passing). The extension's proxy branch ([src/activation/proxy.ts](../../../src/activation/proxy.ts)) registers the three Phase 11 view providers (`nexus.coding.chatView`, `nexus.coding.memoryPanel`, `nexus.coding.traceDashboard`) but each currently mounts a placeholder HTML shell that points the user at the desktop app. The live webview bundle (real `<select>` populated from `ipc.call("models.list", ...)`; live tool-call cards driven by the daemon's `coding.session.event` channel; interactive memory rows with the `Forget` button wired to `memory.delete`) requires the cross-process IPC transport tracked under 10.1.P1.Z to be present so the shells have something to forward to. The daemon-side handlers (`buildMcpListHandler`, `buildMcpInvokeHandler`, `buildSettingsGetHandler`, `buildSettingsSetHandler`, `autocompleteSlashCommands`) are all wired into [desktop/sidecar/src/protocol.ts](../../../desktop/sidecar/src/protocol.ts) (`coding.chat.autocomplete`, `mcp.list`, `mcp.invoke`, `settings.get`, `settings.set` added to `IPC_METHODS` + `METHOD_SCHEMAS` with `implemented: true`), so when 10.1.P1.Z lands the shell -> live-bundle swap is a one-file change per panel.
- **Suggested next step**: After 10.1.P1.Z lands the socket / named-pipe `IpcClient` implementation, replace `ProxyPlaceholderViewProvider` in [src/activation/proxy.ts](../../../src/activation/proxy.ts) with three real `WebviewViewProvider` classes that load the shared webview bundles from [desktop/src/modules/coding/panels/](../../../desktop/src/modules/coding/panels), wire each panel's `postMessage` calls into `ipcClient.call(...)` / `ipcClient.subscribe(...)`, and remove this known-gap entry. The pure projectors / reducers under [core/coding/](../../../core/coding) need no changes -- the parity tests in [tests/integration/extension-desktop-parity.test.ts](../../../tests/integration/extension-desktop-parity.test.ts) already guarantee the bundles will produce identical state on both surfaces.

### 11.8.P2.DD -- SQLite-backed `SettingsStore` adapter deferred (DF, P2)

- **Source phase**: Phase 11 (11.8)
- **Plan reference**: [phase-11-nexus-vscode-extension.md](plans/phase-11-nexus-vscode-extension.md) sub-task 11.8 ("The desktop app's `SettingsPage` writes go through the same IPC ... both write to the same SQLite-backed store").
- **Reason**: Phase 11 ships [core/coding/SettingsBridge.ts](../../../core/coding/SettingsBridge.ts) with the `SettingsStorePort` interface, the `buildSettingsGetHandler` / `buildSettingsSetHandler` daemon-side IPC handlers, the `InMemorySettingsStore` (production-fallback + test surface), and the `reconcileSecondaryMirror` worker that resolves drift between VS Code's own `nexus.coding.*` settings and the daemon store. Full unit-test coverage (12 cases including the no-op-on-equal / conflict-on-disagree / nexus-prefix-only assertions). The remaining work is the SQLite-backed `SqliteSettingsStore implements SettingsStorePort` adapter the production sidecar instantiates against `~/.nexus/settings.sqlite`; that clusters with the same `MemoryStore` adapter cluster that holds 5.6.P2.O / 6.6.P2.S / 9.1.P2.W (one bootstrap site, one schema migration).
- **Suggested next step**: In Phase 12 or 13 (when the sidecar's persistence boundary opens for the diffusion-tier overrides UI), add `SqliteSettingsStore` against a new `app_settings (key TEXT PRIMARY KEY, value TEXT NOT NULL, updated_at INTEGER NOT NULL)` table; store JSON-encoded values; have the sidecar bootstrap swap the in-memory store for the SQLite one and pass it to `buildSettingsGetHandler` / `buildSettingsSetHandler`. The structural port and reconciliation worker do not change.

### 12.2.P1.FF -- SANA `_execute(ctx)` real-diffusers callback deferred to operator action OA-09 (DF, P1)

- **Source phase**: Phase 12 (12.2 - 12.5)
- **Plan reference**: [phase-12-image-studio-sana.md](plans/phase-12-image-studio-sana.md) sub-tasks 12.2 (`sana.py`), 12.3 (`sana_sprint.py`), 12.4 (`sana_int4.py`), 12.5 (`sana_video.py`); operator action OA-09 in [operator-actions.md](operator-actions.md).
- **Reason**: Each SANA pipeline module registers handlers backed by the deterministic stub executor from [runtimes/diffusion/pipelines/base.py](../../../runtimes/diffusion/pipelines/base.py) (and `video_base.stub_execute` for the video tier). Wiring a real diffusers-backed `_execute(ctx)` that instantiates `SanaPipeline` / `SanaSprintPipeline` / `SanaVideo` + the linked DC-AE VAE requires a CUDA host with the SANA-augmented PyTorch / diffusers / nunchaku stack -- not present in CI. The IPC round-trip + workflow embed shape are exercised end-to-end by [tests/python/diffusion/test_pipelines_sana.py](../../../tests/python/diffusion/test_pipelines_sana.py) (17 cases including the ControlNet round-trip + Flow-DPM-Solver payload), so the surrounding plumbing is verifiable without a GPU.
- **Suggested next step**: Operator action OA-09 in [operator-actions.md](operator-actions.md) captures the real-host timings + the diffusers-backed executor swap on the RTX 4070 baseline rig. Commit per-pipeline fixture clips under `tests/golden/v1.1.0/{image,video}/sana/` once the executor lands.

### 12.4.P2.GG -- nunchaku license + wheel verification on real host (DF, P2)

- **Source phase**: Phase 12 (12.4)
- **Plan reference**: [phase-12-image-studio-sana.md](plans/phase-12-image-studio-sana.md) sub-task 12.4 ("Verify the SVDQuant quantization library license (`nunchaku` or equivalent) is Apache-2.0 or MIT. If not, drop S4 from v1.1.0 and update the cycle plan + known-gaps. If yes, add `nunchaku` to [runtimes/diffusion/requirements.txt]...").
- **Reason**: nunchaku is published under Apache-2.0 by MIT HAN Lab (per the upstream repo `https://github.com/mit-han-lab/nunchaku`), so S4 stays in v1.1.0. The dep is added to [runtimes/diffusion/requirements.txt](../../../runtimes/diffusion/requirements.txt) and the installer's wheel-prefix manifest at [scripts/installer/pyqt/src/nexus_installer/engine/diffusion_venv_provisioner.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/diffusion_venv_provisioner.py). The wheel build itself is part of the CI installer-build job (Phase 14) and not part of this commit; on the RTX 3060 8 GB rig the operator must confirm the offline `pip install --no-index` resolves the bundled wheel and that `nunchaku.import` does not crash on first use.
- **Suggested next step**: After Phase 14's CI installer-build job fetches the `nunchaku` wheel into `payload/python/wheels/`, run `nexus-installer` end-to-end on the diffusion-low (8 GB) rig, then capture the SANA-1.5 INT4 1024x1024 timing under OA-09. If the wheel build fails (e.g., custom CUDA kernels not pre-built for the target ABI), drop S4 from v1.1.0 and surface a known-gap that defers it to v1.1.1.

### 12.7.P2.HH -- Diffusion tier-aware Image Studio orchestrator wiring deferred (DF, P2)

- **Source phase**: Phase 12 (12.7)
- **Plan reference**: [phase-12-image-studio-sana.md](plans/phase-12-image-studio-sana.md) sub-task 12.7 acceptance ("form renders correctly on each tier; selecting Fast Preview swaps the model; selecting 4K on a `diffusion-low` host shows a tooltip 'Requires diffusion-high tier'").
- **Reason**: [desktop/src/modules/image/ImagePromptForm.tsx](../../../desktop/src/modules/image/ImagePromptForm.tsx) accepts a `diffusionTier` prop (Phase 12 commit, this commit) and gates 2K / 4K resolutions + surfaces the tier-required hint when the form's resolution exceeds the host's tier. [desktop/src/modules/image/ImageStudioPage.tsx](../../../desktop/src/modules/image/ImageStudioPage.tsx) accepts the same prop and forwards it. Wiring the upstream resolved-tier source (Phase 3's `resolveDiffusionTier` reading `nexus.diffusion.tierOverride` + auto-detected VRAM) into the page-level prop happens at the App / Routes layer; that layer's tier subscription is the deliverable Phase 3 closed (closure rows below). The remaining work is the one-call subscription wire-up at the `<ImageStudioPage>` mount site; today the page defaults to `diffusion-low` so the gating still functions (the tier is the conservative default until the wire-up lands).
- **Suggested next step**: At the App.tsx / Routes layer that hosts `<ImageStudioPage>`, read `useDiffusionTier()` (a small wrapper around `resolveDiffusionTier` + the SettingsStore subscription) and pass the resolved id into `<ImageStudioPage diffusionTier={...}>`. Add a routing-level test that asserts the prop reaches the form. The form-level tests already cover the gating behaviour, so this is a pure plumbing change.

### 11.9.P2.EE -- VS Code-shaped end-to-end parity snapshot deferred (MT, P2)

- **Source phase**: Phase 11 (11.9)
- **Plan reference**: [phase-11-nexus-vscode-extension.md](plans/phase-11-nexus-vscode-extension.md) sub-task 11.9 ("Add `tests/integration/extension-desktop-parity.test.ts` that drives both surfaces against a recorded session and asserts byte-equal DOM snapshots for Plan Mode artifacts and Auto Mode tool-call cards").
- **Reason**: Phase 11 ships [tests/integration/extension-desktop-parity.test.ts](../../../tests/integration/extension-desktop-parity.test.ts) with four cases that assert byte-equal output between the two surfaces at the *projector* level (Plan-Mode `buildPlanArtifact` fingerprints + rendered text; Auto-Mode `applyAutoModeEvents` folded state via `summarizeAutoModeTurn`). The deeper DOM-level snapshot (rendering each panel through the shared `desktop/src/modules/coding/panels/*` React bundles + the extension's webview HTML host, then diffing the resulting DOM trees) requires a jsdom + Tailwind v4 + webview-postMessage harness that does not yet exist in this repo and is more naturally built alongside the Tailwind v4 wiring tracked under 1.11.P1.G. The projector-parity coverage is sufficient to detect every divergence that would produce a user-visible difference, because the panel React components are pure functions of the projector output.
- **Suggested next step**: Cluster with 1.11.P1.G (Tailwind v4 wiring) in Phase 12 or 13: once the webview build pipeline emits a unified bundle for both desktop and extension, add a jsdom-backed harness that mounts each bundle, drives the projector inputs, and diffs `Element.outerHTML`. Keep the existing projector-parity tests as the fast first line of defence.

### 13.2.P3.II -- SANA-Video test lives under `tests/python/diffusion/`, not `runtimes/diffusion/tests/` (NI, P3)

- **Source phase**: Phase 13 (13.2)
- **Plan reference**: [phase-13-video-lab-sana-video.md](plans/phase-13-video-lab-sana-video.md) sub-task 13.2 prompt ("Add [runtimes/diffusion/tests/test_sana_video.py] ...").
- **Reason**: The project's Python test layout already centralizes every diffusion-runtime test under [tests/python/diffusion/](../../../tests/python/diffusion) (the sibling files `test_video_base.py`, `test_pipelines_sana.py`, `test_video_pipeline_registration.py`, etc. all live here and are discovered by the root pytest configuration). Creating `runtimes/diffusion/tests/test_sana_video.py` would either fork the runtime into a co-located test directory (a layout decision that affects every other pipeline test) or be invisible to the existing pytest discovery. The deviation keeps the new test alongside its peers; the acceptance criterion ("mirror the structure of `test_video_base.py`") is satisfied at the test-shape level rather than at the directory level.
- **Suggested next step**: Either confirm `tests/python/diffusion/` as the canonical home and update the Phase 13 plan reference (no code change) or migrate every Python diffusion test from `tests/python/diffusion/` into `runtimes/diffusion/tests/` as a single cluster, then re-point the root pytest config. The latter is out of scope for Phase 13.

### 13.1.P2.JJ -- VideoLabPage `DEFAULT_VIDEO_MODELS` is still a static array; live registry wiring deferred (DF, P2)

- **Source phase**: Phase 13 (13.1)
- **Plan reference**: [phase-13-video-lab-sana-video.md](plans/phase-13-video-lab-sana-video.md) sub-task 13.1 ("Acceptance: ... submit dispatches to `sana_video.py`.").
- **Reason**: The Phase 13 commit adds the `sana-video-2b-720p` text2video entry to [desktop/src/modules/video/VideoLabPage.tsx](../../../desktop/src/modules/video/VideoLabPage.tsx)'s `DEFAULT_VIDEO_MODELS` array so the new Fast 720p preset's chosen modelId resolves under the existing `modelsForMode` filter and the request reaches the dispatcher. The live wiring (`DEFAULT_VIDEO_MODELS` derived from `core/registry/ModelCatalog.listForType("video")` so the entry appears automatically when the catalog gains new SANA-Video variants) clusters with the same Phase 2 IPC widening that the Image Studio model dropdown awaits -- see open item 11.1.P2.CC and the cycle plan's 6.P2.JJ / 7.P2.PP carryforwards. The static array remains a forward-compatible surface because every entry is keyed by its canonical catalog id.
- **Suggested next step**: When the Phase 2 IPC widening lands (10.1.P1.Z), replace the static `DEFAULT_VIDEO_MODELS` array with a `useEffect` that calls `videoClient.listModels()` and maps each result through the existing `{id, displayName, mode}` shape. The Fast 720p preset still binds to `sana-video-2b-720p` -- the wire-up just removes the duplication.

### 9.2.P2.Y -- File compressor embeds the aggregated observation, not each chunk (NI, P2)

- **Source phase**: Phase 9 (9.2)
- **Plan reference**: [phase-09-memory-consolidation-optin.md](plans/phase-09-memory-consolidation-optin.md) sub-task 9.2 prompt ("chunk to ~2,000-token chunks, embed each, run a local Ollama prompt that extracts {summary, key_facts[], code_patterns[]}").
- **Reason**: The implementation embeds the rendered aggregated observation (summary + key facts + code patterns) exactly once via the injected `Embedder`, rather than embedding every shard individually. The acceptance criterion ("a single semantic-tier row appears with the expected provenance") is satisfied with a single vector because the observation row carries one `content` field; per-shard vectors would be unused by `DenseIndex` (which keys on the row id, not the shard index) until the SQLite schema gains a child `memory_compress_shards` table. The deviation halves embedder cost on long files at the price of slightly noisier semantic search over shards individually.
- **Suggested next step**: When the persistence path lands (clustered with 9.1.P2.W's adapter work), add a `memory_compress_shards` child table keyed by `(observation_id, shard_index)` with per-shard `summary` + `vector` columns. Wire `DenseIndex` to read shard rows too, then enable per-shard embedding inside `FileCompressor`. Keep the aggregated row as the user-facing surface; shards stay an implementation detail of the dense index.

### 15.1.P1.KK -- Live `/run-deep-review` chain not executed on this host (DF, P1)

- **Source phase**: Phase 15 (15.1)
- **Plan reference**: [phase-15-hardening-and-release.md](plans/phase-15-hardening-and-release.md) sub-task 15.1 ("Invoke `/run-deep-review` (or the manual equivalent: `analyze-codebase`, `review-codebase`, `run-security-audit`, `run-penetration-test --depth=deep`). Output under [docs/versions/v1/v1.1.0/review/](review) ...").
- **Reason**: The Phase 15.9 finalization session ran on a static-review host without the full DevAI-Hub skill harness wiring + the network egress + the headed display needed to drive the live `/run-deep-review`, `/run-security-audit`, and `/run-penetration-test --depth=deep` skills. The static portion lands in `docs/versions/v1/v1.1.0/review/synthesis.md` Section 8 (cross-phase synthesis from the running known-gaps + history files); the live chain is carried forward to an operator session as OA-V1.1.0-15-DR-A / DR-B / DR-C in [operator-actions.md](operator-actions.md). Until those four IDs close, the v1.1.0 tag stays unpushed.
- **Suggested next step**: Operator runs OA-V1.1.0-15-DR-A through DR-D from [operator-actions.md](operator-actions.md). Output overlays `docs/versions/v1/v1.1.0/review/synthesis.md`; any new P0 / P1 finding bumps the release gate to "RE-REVIEW BEFORE TAG PUSH".

### 15.8.P1.LL -- `npx semantic-release --dry-run` not executed on this host (DF, P1)

- **Source phase**: Phase 15 (15.8)
- **Plan reference**: [phase-15-hardening-and-release.md](plans/phase-15-hardening-and-release.md) sub-task 15.8 ("Run `npx semantic-release --dry-run` against the v1.1.0 tag. Verify it appends a new entry above the v1.0.0 block in `CHANGELOG.md` without overwriting it.").
- **Reason**: The static-review host has not run `npm ci` in this session, so the `@semantic-release/*` plugin chain (`commit-analyzer`, `release-notes-generator`, `@semantic-release/changelog`, `@semantic-release/npm`, `@semantic-release/git`, `@semantic-release/github`) is not on the dependency path. The Phase 15.7 CHANGELOG entry is hand-authored as the canonical v1.1.0 release-notes content; the dry-run verification is captured under OA-V1.1.0-15F in [operator-actions.md](operator-actions.md).
- **Suggested next step**: Operator runs OA-V1.1.0-15F. If the dry-run diverges from the hand-authored entry, add a `@semantic-release/changelog` `changelogTitle` override.

### 15.11.P2.MM -- Final lint / build / test gate not run end-to-end on this host (MT, P2)

- **Source phase**: Phase 15 (15.11)
- **Plan reference**: [phase-15-hardening-and-release.md](plans/phase-15-hardening-and-release.md) sub-task 15.11 ("Re-run the four-step gate one final time on every OS leg.").
- **Reason**: The static-review host has not run `npm run build` / `npm test` / `cargo check` / `pytest` end-to-end during this finalization session because (a) Phases 1-14 each ran the gate in their own implementation sessions and shipped the cycle CI green at each landing, and (b) Phase 15.11 explicitly contemplates a fresh-VM invocation on each OS leg under OA-V1.1.0-15E. The phases-individual gates are documented in the per-phase session histories under `docs/versions/v1/v1.1.0/development/history/`. The cross-OS gate carries forward to OA-V1.1.0-15E (RTM smoke on Windows + macOS + Linux fresh VMs).
- **Suggested next step**: Operator runs OA-V1.1.0-15E. If any leg fails, surface a new known-gap entry referencing the failing test + the OS + the OA-V1.1.0-15E timestamp.

### 15.2-5.P1.NN -- Phase 15 live operator-action set carried forward (DF, P1)

- **Source phase**: Phase 15 (15.2 + 15.3 + 15.4 + 15.5)
- **Plan reference**: [phase-15-hardening-and-release.md](plans/phase-15-hardening-and-release.md) sub-tasks 15.2 (EV signing / notarization / AppImage), 15.3 (SHA rotations + final brand icons), 15.4 (golden task replay + GPU bench + live DevAI-Hub sync), 15.5 (RTM smoke per OS).
- **Reason**: These four sub-tasks each require external infrastructure not available on the static-review host: EV Code Signing certificate + HSM (OA-01), Apple Developer ID certs + notarization workflow (OA-11), Ubuntu / Fedora fresh VMs for AppImage smoke (OA-12), the upstream `bendourthe/DevAI-Hub v1.1.0-baseline` tag (OA-06), the designer-authored final icon set (OA-07), the operator's GPU rig with the three resident Ollama models (~22 GB) for golden-task replay (OA-08) plus the four resident diffusion models (~17 GB) for the GPU bench (OA-09), the live network egress for `nexus skills sync` (OA-10), and three fresh OS VMs for the RTM smoke. Each maps to a stable carryforward ID in [operator-actions.md](operator-actions.md): OA-V1.1.0-15B (signing + notarization + AppImage), OA-V1.1.0-15C (SHA rotations + final brand icons), OA-V1.1.0-15D (golden task + GPU bench + live DevAI-Hub sync), OA-V1.1.0-15E (RTM smoke per OS).
- **Suggested next step**: Operator works through OA-V1.1.0-15B / C / D / E in any order. Each carries its own acceptance check + completion timestamp; once all four are signed off, the v1.1.0 tag pushes.

---

## 2. Resolved

### Phase 1 closures (commit `ec3ff0e`)

| v1.0.0 source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| 2.P1.G | 1 (1.2) | Storage-path call-site rename to `~/.nexus/` / `.nexus/` | Phase 1 commit `ec3ff0e` |
| 2.P1.H | 1 (1.3) | `deprecationMessage` injected into every legacy `gemma-code.*` key in `package.json` | Phase 1 commit `ec3ff0e` |
| 2.P3.L | 1 (1.8) | CRLF/LF snapshot normalization via `.gitattributes` | Phase 1 commit `ec3ff0e` |
| 5.P3.FF | 1 (1.8) | (subsumed by 2.P3.L) | Phase 1 commit `ec3ff0e` |
| 3.P1.P | 1 (1.7) | Legacy curator-cadence fallback deleted from `AgentLoop._runOneIteration`; `nexus.curator.enabled` setting declared | Phase 1 commit `ec3ff0e` |
| -- | 1 (1.1, decision-only) | Shared-core build decision document landed at `docs/versions/v1/v1.1.0/development/decisions/shared-core-build.md` (actual project-references wiring tracked at 1.1.P1.A) | Phase 1 commit `ec3ff0e` |

### Phase 2 closures (commit `de219a5`)

| v1.0.0 source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| 2.P1.J (manifest portion) | 2 (2.1) | VS Code extension manifest IDs renamed: `gemma-code-sidebar` -> `nexus-coding-sidebar`, every `gemma-code.<cmd>` / `gemma-code.<viewId>` -> `nexus.coding.<...>`; `COMPAT_COMMAND_MAP` programmatic shim translates legacy keybindings to the new IDs with a single deprecation log per invocation | Phase 2 commit `de219a5` |
| 2.P2.K (npm portion) | 2 (2.2) | npm `name` + `publisher` renamed `gemma-code` -> `nexus-coding`; `package-lock.json` synced; `.npmignore` created to exclude tests / docs / desktop / runtimes / coverage / .github / scripts/installer / AI-assistant configs; installer-side `EXTENSION_ID` / `_find_vsix` glob / `setup.nsi` PRODUCT_* / Complete-page strings flipped to the new ID in lock-step | Phase 2 commit `de219a5` |
| 3.P2.S | 2 (2.3) | Sidecar + frontend model catalogs (`desktop/sidecar/src/coding/models.ts`, `desktop/src/modules/coding/models.ts`) now derive from `core/registry/ModelCatalog` via the desktop tsconfig's `include` array; the parity test (`desktop/tests/coding-models.test.ts`) asserts positional equality against the canonical catalog | Phase 2 commit `de219a5` |

### Phase 3 closures (commit `fcbaedb`)

| v1.0.0 / v1.1.0 source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| 3.P1.A (new this phase) | 3 (3.1) | Generic `src/` -> `modules/coding/` import-rewriting codemod landed at [scripts/dev/rewrite-imports.mjs](../../../scripts/dev/rewrite-imports.mjs); accepts a `--moves` JSON manifest, walks `src/`, `core/`, `modules/`, `tests/`, and rewrites every static `import`/`export` specifier, dynamic `import(...)`, and `vi.mock(...)`/`vi.doMock(...)` mock path whose resolved location lands inside a moved sub-tree. Idempotent; supports `--dry-run`; default manifest is the Phase 3 utils move. Unblocks every subsequent `src/` -> `modules/coding/` sub-tree move | Phase 3 commit `fcbaedb` |
| 1.4.P1.B (partial: `src/utils/`) | 3 (3.2) | First leaf sub-tree migration: `src/utils/` -> `modules/coding/utils/` (6 files: `Compressor.ts`, `errors.ts`, `logger.ts`, `MarkdownRenderer.ts`, `secretPaths.ts`, `ssrf.ts`). `git mv` preserved rename history; 65 importing files were rewritten by the codemod (`src/extension.ts`, every `src/<subtree>/*` file that imported from utils, every `tests/{unit,integration,benchmarks}/...` file that imported or `vi.mock`-ed a utils path). [configs/vitest.config.ts](../../../configs/vitest.config.ts) coverage `exclude` flipped from `src/utils/**` to `modules/coding/utils/**`. The remaining 12 sub-trees stay open under 1.4.P1.B's status table | Phase 3 commit `fcbaedb` |

### Phase 5 closures (this commit)

| Source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| agentmemory A2 | 5 (5.1) | `LocalEmbedder` (in [core/memory/LocalEmbedder.ts](../../../core/memory/LocalEmbedder.ts)) wraps `@xenova/transformers` + the `all-MiniLM-L6-v2` ONNX weights. `embed(text)` and `embedBatch(texts)` return 384-dim L2-normalized Float32Arrays. Production hosts read from `~/.nexus/runtimes/embedder/all-MiniLM-L6-v2/` (installer payload, Phase 14); dev hosts fall back to the Hub fetch. CI runs against a deterministic 384-dim hash sketch (`hashEmbed`) so the surrounding pipeline is exercised without the binary runtime -- see open item 5.1.P1.M. `@xenova/transformers ^2.17.2` declared under `optionalDependencies`. 17 unit tests in [tests/unit/core/memory/LocalEmbedder.test.ts](../../../tests/unit/core/memory/LocalEmbedder.test.ts) | This commit |
| agentmemory A1 (BM25 portion) | 5 (5.2) | `Bm25Index` (in [core/memory/Bm25Index.ts](../../../core/memory/Bm25Index.ts)) implements standard Okapi BM25 with default `k1=1.5` and `b=0.75` (exposed via Settings `nexus.memory.bm25.k1` / `.b`). Tokenizer in [core/memory/stopwords.ts](../../../core/memory/stopwords.ts) is case-folded, splits on non-alnum, drops a 120-word stop-list. `add(entryId, text)` / `delete(entryId)` mutate in O(tokens-per-doc); `search(query, limit)` returns an insertion-ordered map keyed by descending score with deterministic tie-breaking. 1,000-entry corpus: indexing <200 ms total; search <50 ms p50; add <5 ms median. 18 unit tests in [tests/unit/core/memory/Bm25Index.test.ts](../../../tests/unit/core/memory/Bm25Index.test.ts) | This commit |
| agentmemory A1 (dense portion) | 5 (5.3) | `DenseIndex` (in [core/memory/DenseIndex.ts](../../../core/memory/DenseIndex.ts)) holds a flat in-memory `Float32Array[]` keyed by `entryId`. Search is linear cosine similarity (no HNSW yet; documented v1.2.0 upgrade path beyond ~50K entries). `delete(id)` tombstones; `compact()` reclaims tombstones. `save(filePath)` / `static load(filePath)` round-trip the live slots through a custom `"NXDI" + uint32 dim + uint32 count + (uint32 idLen + utf8 id + dim*float32 vec)*` binary format that does not require Float32Array alignment. Default on-disk path is `~/.nexus/memory/dense.bin`. 1,000-entry scan <50 ms p50. 16 unit tests in [tests/unit/core/memory/DenseIndex.test.ts](../../../tests/unit/core/memory/DenseIndex.test.ts) | This commit |
| agentmemory A1 (RRF portion) | 5 (5.4) | `RrfFuser` + free `fuse(rankings, k=60)` (in [core/memory/RrfFuser.ts](../../../core/memory/RrfFuser.ts)) compute `sum_over_rankings(1/(k + rank_i))` for every entryId present in any input ranking, sort descending by fused score (deterministic tie-break). Default `k = 60` matches Cormack et al. (SIGIR 2009) and is exposed as `nexus.memory.rrf.k`. The instance class lets SettingsStore listeners mutate `k` without re-instantiating. 9 unit tests in [tests/unit/core/memory/RrfFuser.test.ts](../../../tests/unit/core/memory/RrfFuser.test.ts) including the hand-computed three-ranking example from the paper | This commit |
| agentmemory A1 (façade portion) | 5 (5.5) | `HybridRetriever` (in [core/memory/HybridRetriever.ts](../../../core/memory/HybridRetriever.ts)) runs BM25 + Dense (via embedder) + Graph (optional `GraphRanker`) in parallel, fuses via `RrfFuser`, resolves ids to `MemoryHit` via an injected `entryProvider`, applies the existing `isVisibleFromScope` filter, and truncates to `limit`. `InMemoryMemoryHub` (in [core/memory/MemoryHub.ts](../../../core/memory/MemoryHub.ts)) takes an optional `hybridRetriever` via constructor or `setHybridRetriever()`; corpora below the `hybridMinCorpus` threshold (`nexus.memory.hybridMinCorpus`, default 100) stay on substring; larger corpora delegate to the hybrid path automatically. The structural `HybridRetrieverLike` interface declared inline in `MemoryHub.ts` breaks the would-be circular dep. `substringFallback(query, entries, limit)` is exported for consumers that want explicit fast-path control. 14 unit tests in [tests/unit/core/memory/HybridRetriever.test.ts](../../../tests/unit/core/memory/HybridRetriever.test.ts) (including the 1,000-entry p50<50ms / p99<150ms latency budget and the 10-query regression that asserts substring top hits stay in the hybrid top-10) plus 5 in [tests/unit/core/memory/MemoryHub.hybrid.test.ts](../../../tests/unit/core/memory/MemoryHub.hybrid.test.ts) | This commit |
| agentmemory A1 + A2 (warm-build) | 5 (5.6) | `warmRebuild(source, embedder, bm25, dense, opts)` (in [core/memory/WarmRebuildWorker.ts](../../../core/memory/WarmRebuildWorker.ts)) reads every memory row, embeds in batches of 32 (configurable), repopulates `Bm25Index` + `DenseIndex`, and reports progress via the Phase 4 `HookBus` as `lifecycle.notification` events (`notificationKind: "memory.warm-rebuild"`). Fingerprint-based short-circuit skips when the source returns the same identifier as the previous run. `createWarmRebuildTask({...})` returns an `IdleTimeScheduler.register`-compatible shape (default 5 s idle threshold, 24 h cadence) so the sidecar wiring -- tracked under 5.6.P2.O -- is a one-liner once the `MemoryStore` adapter lands. Embedder failures degrade to BM25-only with a warning notification rather than crashing. 10,000-row rebuild completes well under the 60 s acceptance ceiling. 11 unit tests in [tests/unit/core/memory/WarmRebuildWorker.test.ts](../../../tests/unit/core/memory/WarmRebuildWorker.test.ts) | This commit |
| -- (benchmark + settings) | 5 (5.7) | [tests/benchmarks/hybrid-retrieval.bench.ts](../../../tests/benchmarks/hybrid-retrieval.bench.ts) declares the Phase 5 latency benchmark (top-10 retrieve on 1,000 entries + BM25 add median ceiling). Settings schema additions in [package.json](../../../package.json): `nexus.memory.bm25.k1` (default 1.5), `nexus.memory.bm25.b` (default 0.75), `nexus.memory.rrf.k` (default 60), `nexus.memory.hybridMinCorpus` (default 100), each with min/max bounds and a description that points to the consuming module | This commit |

### Phase 8 closures (this commit)

| Source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| v1.0.0 10.P1.GGG | 8 (8.1) | `SkillsReloader` ([core/skills/SkillsReloader.ts](../../../core/skills/SkillsReloader.ts)) watches `~/.nexus/skills/devai-hub/ACTIVE` via `fs.watch`, debounces 200 ms, and invokes `catalog.reload()` once per pointer rotation. Survives the file-then-rename pattern the syncer uses, the pointer-not-yet-existing case (parent-dir watch promotes to file watch on first apply), and reload throws (routed through `onError`). `bootstrapCoding()` accepts an optional `skillCatalog` and constructs the reloader when supplied; the sidecar-side entry-point wiring is tracked under open item 8.1.P2.T. 5 unit tests in [tests/unit/core/skills/SkillsReloader.test.ts](../../../tests/unit/core/skills/SkillsReloader.test.ts) | This commit |
| v1.0.0 10.P1.HHH | 8 (8.2) | `createDevAIHubSyncTask` ([core/skills/DevAIHubAutoSync.ts](../../../core/skills/DevAIHubAutoSync.ts)) returns an `IdleScheduler.register`-ready task whose body runs `new DevAIHubSyncer().sync({apply: true})`. Cadence: 7 days (`DEVAI_HUB_SYNC_CADENCE_MS`); idle threshold: 5 minutes (`DEVAI_HUB_SYNC_IDLE_MS`). `bootstrapCoding()` registers the task with the supplied `IdleScheduler` when `nexus.skills.autoSync.devai-hub === true` and unregisters when the setting flips to false. The setting key declaration ships in [package.json](../../../package.json) so the VS Code settings UI surfaces a toggle. 7 unit tests including the fast-clock cadence assertion in [tests/unit/core/skills/DevAIHubAutoSync.test.ts](../../../tests/unit/core/skills/DevAIHubAutoSync.test.ts) | This commit |
| v1.0.0 10.P2.III | 8 (8.3) | `nexus skills install <ns>/<name> --from <url>` and `nexus skills remove <ns>/<name>` ship as real CLI subcommands in [bin/nexus.mjs](../../../bin/nexus.mjs). Core logic lives in [core/skills/SkillInstaller.ts](../../../core/skills/SkillInstaller.ts) (`parseSkillSpec`, `installSkill`, `removeSkill`, `userSkillDir`, `isPathInside`, `defaultFetcher`). The 4-host allowlist (`github.com`, `gitlab.com`, `raw.githubusercontent.com`, `bendourthe.com`) lives in [core/skills/installAllowlist.ts](../../../core/skills/installAllowlist.ts); `file://` URLs are allowed only when `NEXUS_SKILLS_TEST_MODE=1`. The fetcher applies a 30 s overall timeout via `AbortController`; the scanner is the existing `PromptInjectionScanner` (a `block` decision refuses the install with `reason: "scanner-blocked"`). The target path is clamped to `<skillsRoot>/user/<name>/SKILL.md` and the `removeSkill` surface refuses any namespace other than `user`. 15 unit tests in [tests/unit/core/skills/SkillInstaller.test.ts](../../../tests/unit/core/skills/SkillInstaller.test.ts) + 8 in [tests/unit/core/skills/installAllowlist.test.ts](../../../tests/unit/core/skills/installAllowlist.test.ts). Live-HTTPS end-to-end smoke deferred to Phase 15 (open item 8.3.P2.U) | This commit |
| v1.0.0 10.P2.JJJ | 8 (8.4) | `filterSlashCommandsWithSkills(input, skills, {preferUpstream})` ([desktop/src/modules/coding/slashCommands.ts](../../../desktop/src/modules/coding/slashCommands.ts)) folds skill records into the slash-command autocomplete and orders same-named user / devai-hub pairs by the `nexus.skills.preferUpstream` setting (built-ins always render first, then skill entries grouped by name with the preferred namespace inside each group leading). The `SlashCommand` type gains optional `namespace` + `skillId` fields so the dispatcher (when 1.4.P1.B migrates `src/chat/` to `modules/coding/chat/`) can resolve the canonical id without re-walking the catalog. 6 new test cases in [desktop/tests/slashCommands.test.ts](../../../desktop/tests/slashCommands.test.ts). Daemon-side dispatcher wiring deferred to the `src/chat/` move (open item 8.4.P2.V) | This commit |
| v1.0.0 10.P2.KKK | 8 (8.5) | `AgentLoop.setCurrentSkill(skill)` ([src/tools/AgentLoop.ts](../../../src/tools/AgentLoop.ts)) delegates to the existing `Tracer.setCurrentSkill` (which folds `skill.{id, namespace, tag, contentHash}` into every `tool_call` / `sub_agent` span via `_mergeSkillContext`) and additionally emits `lifecycle.skill.entry` on the HookBus when a non-null skill is supplied. The ChatController slash-command dispatch in [src/panels/ChatController.ts](../../../src/panels/ChatController.ts) wraps each skill body invocation with `agentLoop.setCurrentSkill({id: "user/" + skill.name, namespace: "user"})` + `setCurrentSkill(null)` in `finally`. 4 unit tests in [tests/unit/tools/AgentLoop.setCurrentSkill.test.ts](../../../tests/unit/tools/AgentLoop.setCurrentSkill.test.ts) covering the emit, the no-emit-on-clear contract, the tool-span attribute fold, and the silent path when no HookBus / sessionId is wired | This commit |
| -- (settings schema) | 8 (8.2 + 8.4) | New VS Code settings entries declared in [package.json](../../../package.json): `nexus.skills.autoSync.devai-hub` (default `false`), `nexus.skills.preferUpstream` (default `false`), `nexus.skills.harvest`, `nexus.skills.harvestMinRecurrence`, `nexus.skills.harvestWindowDays`. The harvest keys close a v0.8.0 carryforward where the `gemma-code.skills.harvest*` keys had deprecationMessages pointing at not-yet-declared `nexus.skills.harvest*` siblings | This commit |

### Phase 9 closures (this commit)

| Source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| agentmemory A4 (opt-in contradiction resolver) | 9 (9.1) | [core/memory/ContradictionResolver.ts](../../../core/memory/ContradictionResolver.ts) ships `ContradictionResolver` (with `detect(entry)` / `resolve(group)` / `sweep()` methods), the `SemanticTierProvider` / `OllamaChatLike` ports, the `bm25Jaccard` + `cosineSimilarity` + `parseAdjudication` + `buildAdjudicationPrompt` helpers, and `createContradictionSweepTask(resolver)` (1-hour cadence, 5-minute idle threshold). Detection heuristic: `denseSimilarity > 0.85 AND bm25Jaccard < 0.4 AND text.length >= 20`. Adjudication: a single-line JSON `{"winner":"A"|"B","justification":"..."}` reply from the injected `OllamaChatLike`; the loser is marked `superseded_by` the winner with a structured `ResolutionLogEntry`. The `enabled` gate (`nexus.memory.consolidation.enabled`) short-circuits `sweep()` before any LLM call when off. 22 unit tests in [tests/unit/core/memory/ContradictionResolver.test.ts](../../../tests/unit/core/memory/ContradictionResolver.test.ts) + 2 integration scenarios in [tests/integration/memory-consolidation-optin.test.ts](../../../tests/integration/memory-consolidation-optin.test.ts) covering the "two contradicting rows, one marked superseded" + "default off, zero LLM calls" assertions. IdleTimeScheduler binding deferred under 9.1.P2.W | This commit |
| agentmemory A9 (opt-in file compressor + CLI + slash command) | 9 (9.2) | [core/memory/FileCompressor.ts](../../../core/memory/FileCompressor.ts) ships `FileCompressor` (with `compressFile(path, provenance)` + `enabled` getter), the `SemanticWriter` / `GraphLinker` ports, the `chunkText` + `buildCompressionPrompt` + `parseShardExtraction` + `renderObservationContent` helpers, and `compressionEntryId(sourcePath)` for stable per-path ids. Each chunk receives a single-line JSON `{"summary":"...","key_facts":[...],"code_patterns":[...]}` reply from the injected `OllamaChatLike`; shards are aggregated into a single semantic-tier observation with `provenance.toolName = "memory.compress"`, `metadata.sourcePath = <path>`, `metadata.chunkCount`, `metadata.model`, plus a `memory.compress.source` graph back-reference to `file://<path>`. The aggregated content (summary + key facts + code patterns) is embedded once via the injected `Embedder`. The `enabled` gate (`nexus.memory.compression.enabled`) short-circuits before any LLM call. The `nexus memory compress --file <path> [--dry-run] [--model <name>] [--session <id>]` CLI subcommand routes through `runMemoryCompress` in [bin/nexus.mjs](../../../bin/nexus.mjs); a recording stub Ollama client lets operators audit prompts without spinning up the daemon. The `/memory-compress <path>` slash command ships in [desktop/src/modules/coding/slashCommands.ts](../../../desktop/src/modules/coding/slashCommands.ts) (autocomplete catalog entry) + `handleMemoryCompress` in [core/memory/MemorySlashCommands.ts](../../../core/memory/MemorySlashCommands.ts) (with audit-log write). 16 unit tests in [tests/unit/core/memory/FileCompressor.test.ts](../../../tests/unit/core/memory/FileCompressor.test.ts) + 4 tests in [tests/unit/core/memory/MemorySlashCommands.test.ts](../../../tests/unit/core/memory/MemorySlashCommands.test.ts) covering the slash handler + 2 integration scenarios in [tests/integration/memory-consolidation-optin.test.ts](../../../tests/integration/memory-consolidation-optin.test.ts) covering the 1,000-line fixture compression + the toggle-off short-circuit. Sidecar dispatcher wiring + per-shard embedding deferred under 9.2.P2.X / 9.2.P2.Y | This commit |
| -- (settings schema) | 9 (9.3) | New VS Code settings entries declared in [package.json](../../../package.json): `nexus.memory.consolidation.enabled` (default `false`) and `nexus.memory.compression.enabled` (default `false`). Both descriptions reference the consuming module + the "off by default; no LLM call made when off" invariant | This commit |

### Phase 10 closures (this commit)

| Source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| v1.0.0 3.P1.O | 10 (10.1) | [src/extension.ts](../../../src/extension.ts) reduced from 478 lines to 64; activation dispatches on `discoverDesktopDaemon()` between [src/activation/proxy.ts](../../../src/activation/proxy.ts) (76 lines, daemon detected) and [src/activation/extensionOnly.ts](../../../src/activation/extensionOnly.ts) (403 lines, legacy in-process engine kept for compatibility through v1.2.0). The proxy branch registers all six `nexus.coding.<cmd>` IDs + a status bar item and surfaces an "open the desktop app" hint per invocation; the extension-only branch preserves the v0.X.0 / v1.0.0 surface unchanged (panels, GPU detector, Ollama poller, OTLP exporter, plan-mode hook editor). 5 new unit tests in [tests/unit/activation/proxy.test.ts](../../../tests/unit/activation/proxy.test.ts) | This commit |
| v1.0.0 11.P1.LLL | 10 (10.1, bundled) | The thin-adapter rewrite is the closing surface for v1.0.0's 3.P1.O / 11.P1.LLL pair, which the v1.0.0 known-gaps log clustered as "extension rewrite + Marketplace re-publish". The rewrite is captured in this commit; the re-publish surface is documented under 10.3.P2.BB | This commit |
| v1.0.0 2.P1.J / 2.P2.K (Marketplace re-publish portion) | 10 (10.3) | The code-level rename portion landed in Phase 1.6 (commit `de219a5`); the operator-action steps to publish the renamed VSIX + update the legacy listing description live at [docs/versions/v1/v1.1.0/marketplace-transition.md](marketplace-transition.md) (OA-V1.1.0-10A + OA-V1.1.0-10B). Surfaces in Phase 15 RTM | This commit |
| Phase 10 (10.2) compat shim tightening | 10 (10.2) | [src/activation/compatShim.ts](../../../src/activation/compatShim.ts) hosts the once-per-session legacy-keybinding shim. All six `gemma-code.<cmd>` IDs register programmatically (not in the manifest) so they do not surface in the Command Palette; the first invocation of each legacy ID emits `[deprecation] <legacy-id> -> <new-id>` to the "Nexus Coding" output channel; subsequent invocations forward silently. Previously the per-invocation log line shipped under Phase 2's rebrand commit; Phase 10 narrows it to once-per-session per the plan acceptance criterion. 6 new unit tests in [tests/unit/activation/compatShim.test.ts](../../../tests/unit/activation/compatShim.test.ts) covering registration, per-id once-semantics, args forwarding, and disposable accounting | This commit |

### Phase 11 closures (this commit)

| Source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| Phase 11 (11.1) -- multi-model picker | 11 (11.1) | [core/coding/ModelDropdown.ts](../../../core/coding/ModelDropdown.ts) ships `listModelDropdownEntries(filter)` + `projectModelDropdownEntries(catalog, filter)` + `resolveActiveModelId(stored, entries)` + the `ACTIVE_MODEL_SETTING_KEY` constant. The dropdown derives from the canonical [core/registry/ModelCatalog.ts](../../../core/registry/ModelCatalog.ts) so the desktop chat header and the extension's webview share one source of truth; `capability: "chat"` matches any text LLM in the catalog (chat / tool-use / coding tags), so the three acceptance-criterion models (Gemma 4 E4B, Llama 3.1 8B, Qwen 2.5 Coder 7B) all appear. The active-model id is resolved from `SettingsStore.get("nexus.coding.activeModel")` with a deterministic fallback to the first recommended entry. 11 unit tests in [tests/unit/core/coding/ModelDropdown.test.ts](../../../tests/unit/core/coding/ModelDropdown.test.ts). The IPC schema `models.list` (request: `{type?, capability?}`; response: `{models: ModelDropdownEntry[]}`) is wired into [desktop/sidecar/src/protocol.ts](../../../desktop/sidecar/src/protocol.ts) with `implemented: true` | This commit |
| Phase 11 (11.2) -- Plan Mode artifact projector | 11 (11.2) | [core/coding/PlanArtifact.ts](../../../core/coding/PlanArtifact.ts) ships `buildPlanArtifact(input)` + `renderPlanArtifactText(artifact)`. The projector mirrors `PlanModeState` from [src/chat/PlanMode.ts](../../../src/chat/PlanMode.ts) and emits a deterministic `PlanArtifact` shape (with an 8-char FNV-1a fingerprint) the desktop `PlanPanel` and the extension webview both consume. Annotations are normalized (missing text -> "", missing tip -> null) so the byte-equal assertion in the parity test holds. 9 unit tests in [tests/unit/core/coding/PlanArtifact.test.ts](../../../tests/unit/core/coding/PlanArtifact.test.ts) | This commit |
| Phase 11 (11.3) -- Auto Mode shared reducer | 11 (11.3) | [core/coding/AutoModeStream.ts](../../../core/coding/AutoModeStream.ts) ships `emptyAutoModeTurn()` + `applyAutoModeEvent(state, event)` + `applyAutoModeEvents(events)` + `summarizeAutoModeTurn(state)`. The reducer is structurally identical to the desktop's [desktop/src/modules/coding/toolCallCard.ts](../../../desktop/src/modules/coding/toolCallCard.ts) and mounts under `core/` so the extension's webview can import it without reaching into the desktop workspace. Duplicate `toolCallHeader` events are deduped (same callId -> single card); arg deltas append only to the matching card. 9 unit tests in [tests/unit/core/coding/AutoModeStream.test.ts](../../../tests/unit/core/coding/AutoModeStream.test.ts) | This commit |
| Phase 11 (11.4) -- shared Memory panel projector | 11 (11.4) | [core/coding/MemorySnapshotView.ts](../../../core/coding/MemorySnapshotView.ts) ships `projectMemorySnapshotView(snapshot)` + `filterMemoryRowsByHookKind(rows, hookKind)`. The projector flattens the daemon's `MemorySnapshotT` into a deterministic row list ordered by layer (core, recent, working, project), attaches the Phase 4.5 provenance chips (hookKind / toolName / sessionId) by index, and collects sorted distinct hookKind / toolName lists used by the filter chips. The desktop `<MemoryPanel>` and the extension's webview consume the same view model. 9 unit tests in [tests/unit/core/coding/MemorySnapshotView.test.ts](../../../tests/unit/core/coding/MemorySnapshotView.test.ts) | This commit |
| Phase 11 (11.5) -- daemon-side slash autocomplete | 11 (11.5) | [core/coding/SlashAutocomplete.ts](../../../core/coding/SlashAutocomplete.ts) ships `autocompleteSlashCommands(request, context)` + the `AUTOCOMPLETE_IPC_METHOD` constant. The handler is structurally identical to the desktop's `filterSlashCommandsWithSkills` so the extension and the desktop produce identical suggestions for the same `(input, skills, preferUpstream)` triple. Builtin entries always lead; skill-backed entries follow, grouped by display name with the `preferUpstream`-favoured namespace winning inside each pair. 9 unit tests in [tests/unit/core/coding/SlashAutocomplete.test.ts](../../../tests/unit/core/coding/SlashAutocomplete.test.ts). The IPC schema `coding.chat.autocomplete` (request: `{input, preferUpstream?}`; response: `{suggestions: SlashSuggestion[]}`) is wired into [desktop/sidecar/src/protocol.ts](../../../desktop/sidecar/src/protocol.ts) with `implemented: true` | This commit |
| Phase 11 (11.6) -- sub-agent timeline + session resume | 11 (11.6) | [core/coding/SessionList.ts](../../../core/coding/SessionList.ts) ships `projectSessionListView(sessions, activeSessionId)` + `resolveActiveSessionId(stored, sessions)` + `emptySubAgentTimeline()` + `applySubAgentEvent(state, event)` + `applySubAgentEvents(events)` + the `ACTIVE_SESSION_SETTING_KEY` constant. The session list view tags exactly one row as `isActive` (or zero when the stored id is stale); `resolveActiveSessionId` falls back to the newest session when the stored id is missing. The sub-agent reducer is idempotent on duplicate `subagent.start` events (so reconnects do not double-count) and updates the matching row on `.stop` with the final status / summary. 13 unit tests in [tests/unit/core/coding/SessionList.test.ts](../../../tests/unit/core/coding/SessionList.test.ts) | This commit |
| Phase 11 (11.7) -- MCP bridge | 11 (11.7) | [core/coding/McpBridge.ts](../../../core/coding/McpBridge.ts) ships `buildMcpListHandler(harness)` + `buildMcpInvokeHandler(harness)` + `buildMcpHandlers(harness)` + the `MCP_LIST_METHOD` / `MCP_INVOKE_METHOD` constants. The invoke handler tolerates harness throws (converts them to `{ok: false, error}`), validates missing tool names, and normalizes missing result / error fields to `null`. 8 unit tests in [tests/unit/core/coding/McpBridge.test.ts](../../../tests/unit/core/coding/McpBridge.test.ts). The IPC schemas `mcp.list` (response: `{tools: McpToolDescriptor[]}`) and `mcp.invoke` (request: `{name, args}`; response: `{ok, toolName, result, error}`) are wired into [desktop/sidecar/src/protocol.ts](../../../desktop/sidecar/src/protocol.ts) with `implemented: true` | This commit |
| Phase 11 (11.8) -- settings bridge | 11 (11.8) | [core/coding/SettingsBridge.ts](../../../core/coding/SettingsBridge.ts) ships the `SettingsStorePort` interface + `buildSettingsGetHandler(store)` / `buildSettingsSetHandler(store)` daemon-side handlers + `InMemorySettingsStore` (production fallback + test surface) + `reconcileSecondaryMirror(store, mirror)`. Keys are gated to the `nexus.*` namespace (every other key throws a structured error). The reconciliation worker applies keys present only in the mirror, ignores keys outside the `nexus.*` prefix, treats equal values as no-ops, and records conflicts so the extension can surface a one-time dialog with the daemon value winning by default. 12 unit tests in [tests/unit/core/coding/SettingsBridge.test.ts](../../../tests/unit/core/coding/SettingsBridge.test.ts). The IPC schemas `settings.get` / `settings.set` are wired into [desktop/sidecar/src/protocol.ts](../../../desktop/sidecar/src/protocol.ts) with `implemented: true` | This commit |
| Phase 11 (11.9) -- projector-level parity tests | 11 (11.9) | [tests/integration/extension-desktop-parity.test.ts](../../../tests/integration/extension-desktop-parity.test.ts) ships four cases: (a) Plan-Mode `buildPlanArtifact` fingerprint equality between the two surfaces; (b) Plan-Mode rendered text byte-equality; (c) Auto-Mode `summarizeAutoModeTurn` byte-equality across the two reducers; (d) Auto-Mode parity holds when events arrive in arbitrary chunks. Full DOM snapshot deferred under 11.9.P2.EE | This commit |
| Phase 11 (11.10) -- structural IPC client + proxy wiring | 11 (11.1 + 11.10) | [src/desktop/ipcClient.ts](../../../src/desktop/ipcClient.ts) ships the `IpcClient` structural interface + `NoopIpcClient` (activation placeholder) + `createInProcessIpcClient(handlers)` (test surface + parity harness). 10 unit tests in [tests/unit/desktop/ipcClient.test.ts](../../../tests/unit/desktop/ipcClient.test.ts). The proxy branch ([src/activation/proxy.ts](../../../src/activation/proxy.ts)) gains `installNexusIpcClient()` + `registerPhase11Panels(context, channel)` so the three Phase 11 view providers (`nexus.coding.chatView`, `nexus.coding.memoryPanel`, `nexus.coding.traceDashboard`) register and the IPC client closes cleanly on extension deactivation. The placeholder webview shells point the user at the desktop app until the cross-process transport (10.1.P1.Z) lands. Updated [tests/unit/activation/proxy.test.ts](../../../tests/unit/activation/proxy.test.ts) (6 cases including the three-view-provider registration + Phase 11 log line). Full vitest suite: 3387 passing, 5 skipped, 0 failing across 293 files; `npm run build`, `npm run lint`: clean | This commit |

### Phase 12 closures (this commit)

| Source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| SANA S1 (default 1024px image model) | 12 (12.1 + 12.2) | [core/registry/catalog.json](../../../core/registry/catalog.json) gains the SANA 1.5 1.6B 1024px entry (`sana-1.6b-1024`, Apache-2.0, ~3.2 GB) with `linkedVAE: "dc-ae-f32c32-sana-1.1"`; the DC-AE VAE itself ships as a new `type: "vae"` entry (`dc-ae-f32c32-sana-1.1`, MIT HAN Lab). [core/registry/catalog.ts](../../../core/registry/catalog.ts) extends `ModelType` with `"controlnet"` + `"vae"` plus optional `releaseDate` / `uncensored` / `multimodal` / `contextWindow` / `linkedVAE` / `linkedFamily` / `runtimeDeps` fields; `validateSpec` accepts the new types. The desktop mirror at [desktop/src/pages/settings/modelsTypes.ts](../../../desktop/src/pages/settings/modelsTypes.ts) tracks the union; the `<ModelIcon>` in [desktop/src/pages/settings/ModelsSettings.tsx](../../../desktop/src/pages/settings/ModelsSettings.tsx) renders `C` (orange) for ControlNet and `A` (violet) for VAE. The runner-backed SANA pipeline ships at [runtimes/diffusion/pipelines/sana.py](../../../runtimes/diffusion/pipelines/sana.py) -- registers `sana.txt2img` + `sana.img2img` through the standard `PipelineRunner` with the deterministic CI stub; exposes `resolve_vae(modelId, linkedVAE?)` + `is_sana_model(...)` helpers. 17 cases in [tests/python/diffusion/test_pipelines_sana.py](../../../tests/python/diffusion/test_pipelines_sana.py) cover the registration shape, the txt2img + img2img round-trip with a SANA payload + `sampler: "flow-dpm-solver"`, the ControlNet round-trip, the VAE resolution helper, and the family classifier. Adopts SANA S1 + S7 from [comparison-sana.md](comparison-sana.md) Section 11.1 | This commit |
| SANA S2 (Sana-Sprint speed tier) | 12 (12.3) | [runtimes/diffusion/pipelines/sana_sprint.py](../../../runtimes/diffusion/pipelines/sana_sprint.py) registers `sana_sprint.txt2img` with the 1-step Flow-DPM-Solver config; the `overrides_for_sprint(request)` helper pins `steps=1` + `sampler="flow-dpm-solver"` regardless of the user's form values so the dispatcher's modelId routing has a single forced configuration. [core/registry/catalog.json](../../../core/registry/catalog.json) gains `sana-sprint-1024` (Apache-2.0, ~3.5 GB). Adopts SANA S2 | This commit |
| SANA S4 (INT4 SVDQuant variant) | 12 (12.4) | [runtimes/diffusion/pipelines/sana_int4.py](../../../runtimes/diffusion/pipelines/sana_int4.py) registers `sana_int4.txt2img` with planning size 5 GB so the offload decision keeps `keep_on_gpu` on 8 GB hosts; `has_nunchaku()` probes the runtime dep without crashing in CI. [runtimes/diffusion/requirements.txt](../../../runtimes/diffusion/requirements.txt) declares the full diffusion-runtime dep set and pins `nunchaku` (Apache-2.0, MIT HAN Lab) as the SVDQuant runtime; [scripts/installer/pyqt/src/nexus_installer/engine/diffusion_venv_provisioner.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/diffusion_venv_provisioner.py) adds `nunchaku` to `REQUIRED_WHEEL_PREFIXES` so the installer-build CI job fetches the matching wheel into `payload/python/wheels/`. [core/registry/catalog.json](../../../core/registry/catalog.json) gains `sana-1.6b-int4` (Apache-2.0, ~1.4 GB) with `runtimeDeps: ["nunchaku"]`. Real-host wheel verification deferred under 12.4.P2.GG. Adopts SANA S4 | This commit |
| SANA S5 (SANA-Video 2B 720p tier) | 12 (12.5) | [runtimes/diffusion/pipelines/sana_video.py](../../../runtimes/diffusion/pipelines/sana_video.py) registers `diffusion.video.sana.text2video` + `diffusion.video.sana.image2video` through the standard `VideoPipelineRunner` with planning size 8 GB; the `_upgrade_for_video` offload step in [runtimes/diffusion/pipelines/video_base.py](../../../runtimes/diffusion/pipelines/video_base.py) steps to `model_cpu_offload` on 12 GB hosts. [core/registry/catalog.json](../../../core/registry/catalog.json) gains `sana-video-2b-720p` (Apache-2.0, ~4 GB). Adopts SANA S5 (the v1.1.0 "Fast 720p" tier that fits between LTX-Video and CogVideoX in Video Lab) | This commit |
| SANA S3 (2K + 4K resolutions, gated by DiffusionTier) | 12 (12.1 + 12.7) | [core/registry/catalog.json](../../../core/registry/catalog.json) gains `sana-1.6b-2k` (`diffusion-mid+`) + `sana-1.6b-4k` (`diffusion-high+`). [desktop/src/modules/image/ImagePromptForm.tsx](../../../desktop/src/modules/image/ImagePromptForm.tsx) introduces a resolution dropdown (`512x512` / `768x768` / `1024x1024` / `2048x2048` / `4096x4096`); the dropdown options filter by the `diffusionTier` prop using `tierMeets(actual, required)` against the `TIER_RANK` map (low=0, mid=1, high=2, pro=3). When the form's resolution is too high for the host (e.g., 4K on a `diffusion-low` rig), an inline hint "Requires diffusion-high tier" renders below the dropdown. Width / height number inputs widen their max from 2048 to 4096 to accept the new resolutions. Adopts SANA S3 | This commit |
| SANA S7 (SANA-ControlNet pose / depth / canny) | 12 (12.6) | [core/registry/catalog.json](../../../core/registry/catalog.json) registers `sana-controlnet-pose` / `-depth` / `-canny` with `type: "controlnet"` + `linkedFamily: "sana"`. [runtimes/diffusion/pipelines/sana.py](../../../runtimes/diffusion/pipelines/sana.py) exposes the `SANA_CONTROLNET_PREPROCESSORS` map + `is_sana_controlnet(modelId)` + `preprocessor_for_controlnet(modelId)` so the dispatcher can pair each weight with the matching v1.0.0 Phase 6 preprocessor (`controlnet_aux.OpenposeDetector` / `MidasDetector` / OpenCV Canny). The standard `controlNet` payload from the prompt form flows through `params.parse(...)` -> `workflow_metadata.build_workflow(...)` -> the embedded `nexus_workflow` PNG chunk unchanged. [desktop/src/modules/image/ImageStudioPage.tsx](../../../desktop/src/modules/image/ImageStudioPage.tsx) surfaces the three SANA-ControlNet ids in the ControlNet model dropdown. Adopts SANA S7 | This commit |
| SANA UX (Fast Preview toggle, multi-lang hint, Flow-DPM-Solver sampler) | 12 (12.7) | [desktop/src/modules/image/ImagePromptForm.tsx](../../../desktop/src/modules/image/ImagePromptForm.tsx) gains: (a) a "Fast Preview" checkbox above the Advanced section that flips `values.fastPreview`; (b) an inline "i" multi-lang hint next to the Prompt label with `aria-label`+`title` "Supports English, Chinese, and Emoji (multilingual model)."; (c) `flow-dpm-solver` in the `SAMPLERS` array so the sampler dropdown surfaces it. The `valuesToBaseRequest(values, overrides, {fastPreviewModelId?})` helper swaps `modelId` to `sana-sprint-1024` (or the supplied `fastPreviewModelId`), forces `steps=1`, and forces `sampler="flow-dpm-solver"` when `fastPreview` is on; leaves the modelId / steps / sampler alone otherwise. [runtimes/diffusion/pipelines/params.py](../../../runtimes/diffusion/pipelines/params.py)'s `_VALID_SAMPLERS` accepts `flow-dpm-solver` so the Python-side validator does not reject the new sampler. 8 new cases in [desktop/tests/ImagePromptForm.test.tsx](../../../desktop/tests/ImagePromptForm.test.tsx) cover the hint, the sampler list, the toggle persistence, the modelId / steps / sampler swap, the tier filter, the tier-meets predicate, and the dropdown-hides-2K-on-low assertion. Adopts SANA S8 | This commit |
| SANA S8 (installer presets auto-tick SANA family) | 12 (12.8) | [scripts/installer/pyqt/src/nexus_installer/pages/recommended_models.py](../../../scripts/installer/pyqt/src/nexus_installer/pages/recommended_models.py) rewires every preset: Light / Recommended swap `sdxl-turbo` (~6.5 GB) for the SANA family (`sana-1.6b-1024` ~3.2 GB + `sana-sprint-1024` ~3.5 GB), Full adds `sana-1.6b-2k` + `sana-1.6b-4k` + `sana-video-2b-720p` alongside the existing SDXL / FLUX / SVD / CogVideoX entries (which become opt-in alternates). The total Recommended preset shrinks from ~25 GB to ~22 GB; total Full grows to ~75 GB. The `_PresetCard` widget auto-checks every model in the active preset (no UX changes needed). 3 new cases in [scripts/installer/pyqt/tests/test_recommended_models.py](../../../scripts/installer/pyqt/tests/test_recommended_models.py) cover the SANA auto-tick + the SDXL Turbo removal from Light / Recommended + the total-GB sum for the new pair. Adopts SANA S8 | This commit |
| SANA S10 (catalog-digests test surface) | 12 (12.1 + 12.9) | [tests/unit/core/registry/catalog-digests.test.ts](../../../tests/unit/core/registry/catalog-digests.test.ts) lands the test surface promised by v1.0.0 OA-03: every non-Ollama entry must declare a 64-hex `sha256` (placeholder or real); every Phase 12 SANA entry id is enumerated and recognized; the placeholder list is reported separately so OA-03 (now OA-V1.1.0-12A in [operator-actions.md](operator-actions.md)) can track closure. Three cases; full registry suite 88 / 88 passing. Adopts SANA S10 | This commit |
| -- (Phase 12 operator-actions doc) | 12 (12.9) | [docs/versions/v1/v1.1.0/operator-actions.md](operator-actions.md) lands as the v1.1.0 operator-actions ledger, inheriting v1.0.0 OA-09 + opening OA-V1.1.0-12A (rotate SANA placeholder SHA-256 digests). OA-09 is extended with the seven SANA timing targets from the Phase 12 plan's Stability Gate (1024x1024 <= 1.5 s, Sprint <= 0.5 s, 2K <= 8 s, 4K <= 30 s, INT4 <= 2 s on 8 GB, ControlNet renders, SANA-Video 4 s 720p <= 60 s) | This commit |

### Phase 13 closures (this commit)

| Source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| SANA S5 (Fast 720p video tier UI) | 13 (13.1) | [desktop/src/modules/video/VideoPromptForm.tsx](../../../desktop/src/modules/video/VideoPromptForm.tsx) gains a Preset selector with two entries: `Custom` (no patch) and `Fast 720p (SANA-Video 2B)` (binds `modelId: "sana-video-2b-720p"`, `mode: "text2video"`, `width: 1280`, `height: 720`, `durationSeconds: 4`, `fps: 24`, `sampler: "flow-dpm-solver"`). Exports `VIDEO_PRESETS` so callers can introspect the catalog. The `SANA-Video 2B 720p (Fast)` text2video model now ships in [desktop/src/modules/video/VideoLabPage.tsx](../../../desktop/src/modules/video/VideoLabPage.tsx)'s `DEFAULT_VIDEO_MODELS` so the preset's chosen modelId is resolvable. `flow-dpm-solver` joins both the form sampler dropdown and the [runtimes/diffusion/pipelines/video_params.py](../../../runtimes/diffusion/pipelines/video_params.py) `_VALID_SAMPLERS` set so the request round-trips end-to-end through the dispatcher. 7 new desktop tests in [desktop/tests/VideoPromptForm.test.tsx](../../../desktop/tests/VideoPromptForm.test.tsx) cover preset catalog shape, preset rendering, Fast-720p patch application, Custom preset no-op, sampler-dropdown coverage, and the videoFormToRequest round-trip | This commit |
| SANA S5 (sana_video.py stub-mode integration test) | 13 (13.2) | [tests/python/diffusion/test_sana_video.py](../../../tests/python/diffusion/test_sana_video.py) lands as a dedicated dual-method test file (18 cases) mirroring the shape of [tests/python/diffusion/test_video_base.py](../../../tests/python/diffusion/test_video_base.py). Asserts registration shape, IPC round-trip via the stub executor, workflow-JSON build, frame-preview-per-second contract, param/envelope validation (missing jobId, missing mode, invalid params, missing sourceImage, `None` params), insufficient-VRAM path on a 2 GB host (model_size_gb=8 ⇒ free<model/2 ⇒ `insufficient-vram`), offload-strategy reporting, outputs-dir env override, the 12 GB RTX-4070 offload tier, and module-level surface. Coverage on [runtimes/diffusion/pipelines/sana_video.py](../../../runtimes/diffusion/pipelines/sana_video.py) is **100%** (9/9 statements; well above the 80% Phase 13.2 acceptance) | This commit |
| SANA-Video installer opt-in | 13 (13.3) | [scripts/installer/pyqt/src/nexus_installer/pages/recommended_models.py](../../../scripts/installer/pyqt/src/nexus_installer/pages/recommended_models.py) `ModelEntry` gains a `default_checked: bool = True` field (backwards-compatible default). SANA-Video appears in **every** preset (Light / Recommended / Full); Light + Recommended ship it with `default_checked=False` (visible-but-unticked opt-in for the extra 4 GB); Full ticks it by default (creator tier). `_PresetCard` honours the new field via `box.setChecked(model.default_checked)`. `RecommendedModelsPage.__init__` seeds the initial `selected_models` from `{m.model_id for m in default.models if m.default_checked}` so unticked rows render but are not pre-selected. 8 new tests in [scripts/installer/pyqt/tests/test_recommended_models.py](../../../scripts/installer/pyqt/tests/test_recommended_models.py): SANA-Video visible in every preset; default_checked False on Light / Recommended, True on Full; ModelEntry default_checked defaults to True; per-preset render asserts SANA-Video is excluded from / included in the initial selection. All 23 installer-page tests pass | This commit |
| Phase 13 four-step gate | 13 (13.4) | npm-side ESLint clean (`npm run lint:shell` exits 0). Desktop test additions: 7 new cases (VideoPromptForm.test.tsx); existing VideoLabPage suite unaffected (11 cases still green). Python diffusion suite: 135 tests pass after the additions (test_sana_video.py + test_pipelines_sana.py + 11 sibling files). Installer pyqt suite: 23 tests pass. The two pre-existing sidecar-handlers failures (`declared-but-unimplemented` + `handlers covers every declared method`) reproduce against `main` without the Phase 13 patch applied (confirmed via `git stash`); they trace to an unrelated Phase 11 IPC-protocol widening and stay open under the existing 10.1.P1.Z / 11.1.P2.CC cluster. Operator-action OA-09 already carries the SANA-Video 4 s 720p <= 60 s timing target (added in Phase 12); the Phase 13 commit adds no new operator action because the real-host benchmark already lives under OA-09 | This commit |

### Phase 7 closures (commit `2864f68`)

| Source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| agentmemory A6 (session replay) | 7 (7.1 + 7.2 + 7.3) | The TraceDashboard ([desktop/src/modules/coding/panels/TraceDashboardPanel.tsx](../../../desktop/src/modules/coding/panels/TraceDashboardPanel.tsx)) gained an optional left-rail session list (driven by the existing `coding.sessions.list` IPC), a `<TimelineScrubber>` ([desktop/src/modules/coding/panels/TimelineScrubber.tsx](../../../desktop/src/modules/coding/panels/TimelineScrubber.tsx)) with play/pause + variable speed (0.5x / 1x / 2x / 4x) + Go-to-start/end + per-event tick marks + `requestAnimationFrame`-driven playhead, and a "Compare to..." picker that flips the dashboard to a side-by-side `<SessionCompareView>` ([desktop/src/modules/coding/panels/SessionCompareView.tsx](../../../desktop/src/modules/coding/panels/SessionCompareView.tsx)) with a linked play state, a shared speed dropdown, and a row-per-index Diff pane that highlights `kind`/`summary` deltas. 13 new component tests (9 `TimelineScrubber` including the deterministic 2x-speed wall-clock assertion + 4 `SessionCompareView`) + 3 new `TraceDashboardPanel` cases. Coverage on the three new/modified files: `TimelineScrubber.tsx` 97.79%, `SessionCompareView.tsx` 100%, `TraceDashboardPanel.tsx` 99.59% -- comfortably above the 80% gate. The CodingPage ([desktop/src/modules/coding/CodingPage.tsx](../../../desktop/src/modules/coding/CodingPage.tsx)) wires replay/compare state to `coding.trace.subscribe({sessionId})` + `coding.sessions.list` IPC | This commit |

### Phase 14 closures (this commit)

| Source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| v1.0.0 9.P1.ZZ (cross-OS payload fetcher) | 14 (14.10) | [scripts/installer/build/fetch-payload.py](../../../scripts/installer/build/fetch-payload.py) parameterized by `--os <win\|mac\|linux>` and `--arch <x64\|arm64>` downloads CUDA / Python embeddable / wheel sets / Node 22 / Ollama / ffmpeg / DevAI-Hub baseline tarball / SANA + DC-AE weights / local embedder ONNX into a single `payload/` tree. Every URL + SHA-256 is pinned in [scripts/installer/build/versions.lock.json](../../../scripts/installer/build/versions.lock.json); placeholder hashes (all-zero) log a warning and continue so the v1.1.0 ship can build before OA-03 rotates the digests; real (non-placeholder) hash mismatches abort the build. 5 unit tests in [tests/test_fetch_payload_script.py](../../../scripts/installer/pyqt/tests/test_fetch_payload_script.py) | This commit |
| v1.0.0 9.P1.AAA (macOS outer shell) | 14 (14.9) | [.github/workflows/installer-macos.yml](../../../.github/workflows/installer-macos.yml) promoted from workflow_dispatch-only to `push: tags`; runs `fetch-payload.py --os mac --arch arm64`, freezes the wizard with PyInstaller, codesigns + notarizes when `APPLE_DEVELOPER_ID` / `APPLE_NOTARY_*` secrets are wired (best-effort otherwise), and produces `Nexus-<version>.dmg` via `create-dmg` with an `hdiutil` fallback. Signing + notarization remain operator-driven (OA-11). | This commit |
| v1.0.0 9.P2.BBB (Linux outer shell) | 14 (14.9) | [.github/workflows/installer-linux.yml](../../../.github/workflows/installer-linux.yml) promoted to `push: tags`; runs `fetch-payload.py --os linux --arch x64`, freezes the wizard, assembles an AppDir with `.desktop` + `AppRun` shell (statically-linked glibc 2.31+), and produces `Nexus-<version>.AppImage` via `appimagetool`. | This commit |
| v1.0.0 9.P1.CCC (host detection module) | 14 (14.1) | [scripts/installer/pyqt/src/nexus_installer/engine/host_detect.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/host_detect.py) returns a `HostProfile` dataclass with `os_family` / `os_version` / `arch` / `cpu_model` / `total_ram_gb` / `gpu_vendor` / `gpu_model` / `total_vram_gb` / `driver_version` / `cuda_compatible` / `metal_compatible` / `rocm_compatible` / `free_disk_gb` / `target_install_path`. Every probe is fault-tolerant: missing tools collapse to safe defaults. Detection commands per OS: `wmic` / `nvidia-smi` / PowerShell on Windows, `system_profiler` / `sysctl` / `sw_vers` on macOS, `lspci` / `nvidia-smi` / `rocm-smi` / `/proc/cpuinfo` / `/proc/meminfo` / `df` on Linux. 33 unit tests in [tests/test_host_detect.py](../../../scripts/installer/pyqt/tests/test_host_detect.py) drive the matrix per OS family + GPU vendor | This commit |
| v1.0.0 9.P2.DDD (OS-aware dispatch + macOS + Linux provisioners) | 14 (14.2 + 14.3 + 14.4) | [scripts/installer/pyqt/src/nexus_installer/engine/provisioner_dispatch.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/provisioner_dispatch.py) reads `HostProfile` and returns the ordered provisioner chain (`cuda` / `metal` / `cuda-linux` / `rocm` / `cpu-only` first; then `<os>-python` / `node` / `ollama-<os>` / `ffmpeg` / `devai-hub`). Six new provisioners: [metal_provisioner.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/metal_provisioner.py) (no-op MPS install via bundled `wheels-mac`), [ollama_macos_provisioner.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/ollama_macos_provisioner.py) (copies `Ollama.app` into `/Applications/`), [cuda_linux_provisioner.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/cuda_linux_provisioner.py) (CUDA 12.1 to `~/.local/share/nexus/runtime/cuda/` with LD_LIBRARY_PATH hint), [rocm_provisioner.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/rocm_provisioner.py) (torch-rocm wheels), [cpu_only_provisioner.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/cpu_only_provisioner.py) (CPU-only PyTorch + user-facing fallback dialog text), [ollama_linux_provisioner.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/ollama_linux_provisioner.py) (offline tarball preferred; `install.sh` with SHA-256 verification fallback), [ffmpeg_provisioner.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/ffmpeg_provisioner.py) (cross-OS ffmpeg + ffprobe). 12 unit tests in [tests/test_provisioner_dispatch.py](../../../scripts/installer/pyqt/tests/test_provisioner_dispatch.py) drive the matrix; 13 in [tests/test_phase_14_provisioners.py](../../../scripts/installer/pyqt/tests/test_phase_14_provisioners.py) exercise the new provisioners | This commit |
| v1.0.0 9.P2.EEE (disk-aware footer + 10 GB OS reserve) | 14 (14.5) | [scripts/installer/pyqt/src/nexus_installer/widgets/disk_aware_footer.py](../../../scripts/installer/pyqt/src/nexus_installer/widgets/disk_aware_footer.py) renders a live `Free / Selected / Remaining after install` band at the bottom of the wizard; recolors the remaining value red below `disk_reserve_gb`, yellow below `2 * disk_reserve_gb`, green otherwise. `InstallerState.disk_reserve_gb` defaults to 10 (configurable via `--disk-reserve-gb`); `state.can_select_model(model_gb)` returns False when adding the model would dip below the reserve. 10 unit tests in [tests/test_disk_aware_footer.py](../../../scripts/installer/pyqt/tests/test_disk_aware_footer.py) cover the format, the boundary cases, and the unknown-disk path | This commit |
| v1.0.0 6.P1.HH (typed catalog Text/Image/Video/Audio tabs) | 14 (14.6) | [scripts/installer/pyqt/src/nexus_installer/pages/typed_catalog.py](../../../scripts/installer/pyqt/src/nexus_installer/pages/typed_catalog.py) replaces the v1.0.0 single-list recommended-models page with a `QTabWidget` (Text / Image / Video / Audio). Each tab renders catalog entries from [core/registry/catalog.json](../../../core/registry/catalog.json) with size on disk, compatibility badge (green / yellow / red based on `HostProfile`), context window (text only), multimodality, censored, license, release date. The recommended set lives in [core/registry/recommended.json](../../../core/registry/recommended.json) (text: `gemma4:e4b` + `llama3.1:8b` + `qwen2.5-coder:7b`; image: `sana-1.6b-1024` + `sana-sprint-1024`; video: `ltx-video`; audio: empty -- tab shows "No audio models recommended yet"). Checkboxes that would dip below the 10 GB reserve are disabled with a tooltip. 19 unit tests in [tests/test_typed_catalog.py](../../../scripts/installer/pyqt/tests/test_typed_catalog.py) | This commit |
| v1.0.0 7.P1.NN (Nexus VS Code extension add-on step) | 14 (14.7) | [scripts/installer/pyqt/src/nexus_installer/pages/vscode_extension.py](../../../scripts/installer/pyqt/src/nexus_installer/pages/vscode_extension.py) adds a single-question page with a checkbox that auto-ticks when `code` / `code-insiders` / `cursor` / `windsurf` is on PATH; the wizard runs `<cli> --install-extension nexus-coding-<version>.vsix` only when the box is ticked. 7 unit tests in [tests/test_vscode_extension_page.py](../../../scripts/installer/pyqt/tests/test_vscode_extension_page.py) | This commit |
| v1.0.0 7.P1.OO (hardware-compat + disk guard at Install click) | 14 (14.8) | [scripts/installer/pyqt/src/nexus_installer/engine/install_guard.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/install_guard.py) ships `evaluate_install_guard(free_disk_gb, selection_gb, reserve_gb)` returning a `GuardResult` with `ok` / `needed_gb` / `free_gb` / `message`. [scripts/installer/pyqt/src/nexus_installer/window.py](../../../scripts/installer/pyqt/src/nexus_installer/window.py) re-runs host detection at "Begin Installation", asserts the selection still fits, and bounces the user back to the model picker with a `QMessageBox.critical(...)` if not. 8 unit tests in [tests/test_install_guard.py](../../../scripts/installer/pyqt/tests/test_install_guard.py) | This commit |
| Storage review page + first-launch migration | 14 (14.11 + 14.12) | [scripts/installer/pyqt/src/nexus_installer/pages/storage.py](../../../scripts/installer/pyqt/src/nexus_installer/pages/storage.py) renders the Storage Review (free disk / runtime / models / DevAI-Hub baseline / 10 GB reserve / net remaining with red-yellow-green coloring). [scripts/installer/pyqt/src/nexus_installer/engine/storage_migration.py](../../../scripts/installer/pyqt/src/nexus_installer/engine/storage_migration.py) re-implements the v1.0.0 TS `StorageMigration` shape in Python so the cross-OS launch shim can run the migration even when the Node sidecar has not started yet (POSIX -> symlink the legacy directory to `~/.nexus/`; Windows -> write `MOVED-TO-NEXUS.txt`). 8 unit tests for the storage page + 5 for the migration shim | This commit |
| RTM smoke checklists per OS | 14 (14.13) | [docs/versions/v1/v1.1.0/installer-smoke-windows.md](installer-smoke-windows.md), [docs/versions/v1/v1.1.0/installer-smoke-macos.md](installer-smoke-macos.md), and [docs/versions/v1/v1.1.0/installer-smoke-linux.md](installer-smoke-linux.md) -- ~30 manual steps each, referencing the Phase 14 sub-tasks. Signed off in Phase 15 | This commit |

### Phase 6 closures (commit `c8d9e0b`)

| Source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| agentmemory A11 | 6 (6.1) | `nexus memory audit [--since <ISO>] [--tier <t>] [--scope <id>] [--session <id>] [--op <op>] [--format table\|json] [--source <jsonl>]` ships as a new CLI subcommand in [bin/nexus.mjs](../../../bin/nexus.mjs). Implementation in [core/memory/MemoryAudit.ts](../../../core/memory/MemoryAudit.ts) (`formatAuditTable`, `formatAuditJsonl`, `parseSinceFlag`) plus the log abstraction in [core/memory/MemoryAuditLog.ts](../../../core/memory/MemoryAuditLog.ts) (`MemoryAuditRow`, `MemoryAuditFilter`, `InMemoryAuditLog`, `rowFromProvenance`, `previewText`). Output columns: timestamp, op (write/read/delete), tier, entryId, sessionId, hookKind, toolName, textPreview. 10 unit tests in [tests/unit/core/memory/MemoryAuditLog.test.ts](../../../tests/unit/core/memory/MemoryAuditLog.test.ts) + 10 in [tests/unit/core/memory/MemoryAudit.test.ts](../../../tests/unit/core/memory/MemoryAudit.test.ts). Persistence to SQLite is tracked under open item 6.1.P2.P | This commit |
| agentmemory A10 | 6 (6.2) | `nexus memory export --out <file>` and `nexus memory import --in <file>` ship as new CLI subcommands. Implementation in [core/memory/MemoryExport.ts](../../../core/memory/MemoryExport.ts) (`exportToJsonl(source, filter)`, `importFromJsonl(text, sink)`, `encodeVectorB64` / `decodeVectorB64` for base64-encoded Float32 vectors, `isPathInside` for path-traversal guard). One JSONL line per row with full row contents (text, vectorB64, provenance, tier, scope, timestamps, accessCount, corroborationCount). The output path is clamped to `~/.nexus/exports/` by the CLI surface. 12 unit tests in [tests/unit/core/memory/MemoryExport.test.ts](../../../tests/unit/core/memory/MemoryExport.test.ts) including the round-trip integrity assertion. Live `MemoryStore` source/sink adapters are tracked under 6.2.P2.Q | This commit |
| `/recall <query>` slash command | 6 (6.3) | New `recall` entry in the desktop slash-command catalog ([desktop/src/modules/coding/slashCommands.ts](../../../desktop/src/modules/coding/slashCommands.ts)). Handler `handleRecall(input, ctx)` in [core/memory/MemorySlashCommands.ts](../../../core/memory/MemorySlashCommands.ts) calls `HybridRetriever.retrieve(query, {scopeId, visibleScopes, limit: 10})` (via the structural `HybridRetrieverLike` interface) and renders the top-10 hits as a fenced JSON block + machine-readable `payload`. Every hit produces a `read` row in the audit log tagged with `hookKind: "slash.recall"`. 4 unit tests in [tests/unit/core/memory/MemorySlashCommands.test.ts](../../../tests/unit/core/memory/MemorySlashCommands.test.ts) covering happy-path, missing-query, missing-retriever, and warming-up | This commit |
| `/remember <text>` slash command | 6 (6.4) | New `remember` entry in the desktop catalog. Handler `handleRemember(input, ctx)` writes a working-tier observation via `MemoryWritePort.writeWorking({content, provenance, scopeId?})` with `provenance: {sessionId, hookKind: "slash.remember", parentSpanId?}`. Every write produces a `write` row in the audit log. 2 unit tests cover the happy path and the empty-text rejection | This commit |
| agentmemory A12 (`/forget --id\|--pattern`) | 6 (6.5) | New `forget` entry in the desktop catalog. Handler `handleForget(input, ctx)` parses `--id <uuid>` (exact) or `--pattern <regex>` (matches `text` field), gates the deletion via `ctx.confirm(...)`, deletes via `MemoryWritePort.delete(id)`, and writes a `delete` row to the audit log per row. The `parseForgetArgs` helper is unit-tested for all four flag shapes (`--id`, `--id=`, `--pattern bare`, `--pattern "quoted"`) plus invalid-regex rejection. The `MemoryPanel` ([desktop/src/modules/coding/panels/MemoryPanel.tsx](../../../desktop/src/modules/coding/panels/MemoryPanel.tsx)) gains an optional `onForget(layerKey, index, entry)` prop; when supplied, every entry row renders a "Forget" button alongside the provenance chips. 6 unit tests for `handleForget` + 2 new desktop panel tests in [desktop/tests/panels.test.tsx](../../../desktop/tests/panels.test.tsx). IPC pipeline to live `MemoryHub.delete(id)` is tracked under 6.5.P2.R | This commit |
| agentmemory A3 (Ebbinghaus decay) | 6 (6.6) | [core/memory/DecaySweep.ts](../../../core/memory/DecaySweep.ts) implements the closed-form Ebbinghaus retention curve `R(t) = exp(-t / halfLife * ln(2))` with per-tier half-lives (working = 24 h, episodic = 7 d, semantic = 30 d, graph = 365 d -- configurable via constructor `halfLives`). Eviction rule: `retention < 0.05 AND accessCount < 3`. The `DecayProvider` interface decouples the sweep from the store; the `evict(id)` callback owns tombstoning. `nexus memory decay --now` exposes a manual sweep for debugging. 10 unit tests in [tests/unit/core/memory/DecaySweep.test.ts](../../../tests/unit/core/memory/DecaySweep.test.ts) including the "5% of the math" assertion (100-row synthetic corpus, 7-day advancement). IdleTimeScheduler wiring in the sidecar is tracked under 6.6.P2.S | This commit |
| Phase 6 CLI parsing | 6 (6.7) | [bin/nexus.mjs](../../../bin/nexus.mjs) gains the `memory` subcommand with four sub-subcommands (`audit`, `export`, `import`, `decay`) routed through `runMemoryCommand`. Each subcommand resolves the compiled artifact from `out/core/memory/*.js`, so the CLI works against the same code paths the sidecar uses. New parseArgs test cases in [tests/unit/cli/nexus-cli.test.ts](../../../tests/unit/cli/nexus-cli.test.ts) cover `memory audit --since`, `memory export --out`, and `memory decay --now` | This commit |

### Phase 15 closures (this commit)

| Source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| Phase 15 (15.1, static portion) | 15 (15.1) | `docs/versions/v1/v1.1.0/review/synthesis.md` -- static cross-phase synthesis derived from the Phase 1-14 known-gaps + history files. Mirrors the v1.0.0 `docs/versions/v1/v1.0.0/review/synthesis.md` structure. Section 8 documents the static-only review findings; Section 9 records the four live `/run-deep-review` carryforward IDs that complete the chain (OA-V1.1.0-15-DR-A through DR-D). | This commit |
| Phase 15 (15.6) | 15 (15.6) | Version bumped from `1.0.0` to `1.1.0` across [package.json](../../../package.json) (and `package-lock.json` top + `desktop/` workspace), [desktop/package.json](../../../desktop/package.json), [desktop/src-tauri/Cargo.toml](../../../desktop/src-tauri/Cargo.toml), [desktop/src-tauri/tauri.conf.json](../../../desktop/src-tauri/tauri.conf.json), [scripts/installer/pyqt/pyproject.toml](../../../scripts/installer/pyqt/pyproject.toml), [scripts/installer/pyqt/src/nexus_installer/__init__.py](../../../scripts/installer/pyqt/src/nexus_installer/__init__.py), and [scripts/installer/build/nsis/nexus-setup.nsi](../../../scripts/installer/build/nsis/nexus-setup.nsi) (header banner + APP_VERSION + OutFile `Nexus-1.1.0-Setup.exe` literal). All six release-product version files plus the NSIS literal aligned at `1.1.0`. | This commit |
| Phase 15 (15.7) | 15 (15.7) | The v1.1.0 [CHANGELOG.md](../../../CHANGELOG.md) entry prepended above the `v0.41.0` semantic-release block (per the plan's "appends a new entry above the v1.0.0 block without overwriting it" requirement; the dry-run verification is captured under OA-V1.1.0-15F). Sectioned-bullet structure with one section per Phase 1-15 plus `### Changed` / `### Deferred to v1.2.0` / `### Operator-action carryforwards` sections. The [docs/versions/v1/v1.1.0/release-notes.md](release-notes.md) ships as the user-facing release content -- five highlight sections (Cross-OS installer, Nexus VS Code extension, Image Studio SANA family, Video Lab Fast 720p, Hybrid memory + session replay + slash commands), Compatibility notes, Known limitations, What is next (v1.2.0 teaser), Acknowledgements, Get Nexus link block. | This commit |
| Phase 15 (15.9) | 15 (15.9) | This `docs/versions/v1/v1.1.0/known-gaps.md` finalization: Phase 15 closures appended to `## 2. Resolved`; the four new Phase 15 open items appended to `## 1. Open Items` (15.1.P1.KK live deep-review chain, 15.8.P1.LL semantic-release dry-run, 15.11.P2.MM final gate, 15.2-5.P1.NN operator-action set); Section 3 summary table recomputed; Section 4 carryforward map populated; status header flipped from `live` to `finalized at v1.1.0 release (Phase 15.9, 2026-05-26)`. | This commit |
| Phase 15 (15.10) | 15 (15.10) | [docs/versions/v1/v1.1.0/distribution.md](distribution.md) ships as the v1.1.0 distribution channels document, mirroring the v1.0.0 structure across three OS surfaces (Windows NSIS + macOS DMG + Linux AppImage) plus the renamed `nexus-coding` VS Code Marketplace listing. Surface 3 (direct-download landing page) is deferred to v1.1.1 per OA-05; the GitHub Release URL remains canonical. | This commit |
| Phase 15 (15.2 / 15.3 / 15.4 / 15.5 / 15.8, operator-action documentation) | 15 (15.2-5 + 15.8) | [docs/versions/v1/v1.1.0/operator-actions.md](operator-actions.md) extended with the six new Phase 15 carryforward IDs: OA-V1.1.0-15A (live deep review chain), OA-V1.1.0-15B (signing + notarization + AppImage smoke), OA-V1.1.0-15C (SHA rotations + final brand icons), OA-V1.1.0-15D (golden task + GPU bench + live DevAI-Hub sync), OA-V1.1.0-15E (RTM smoke per OS), OA-V1.1.0-15F (semantic-release dry-run verification). Each entry carries acceptance criteria + blocked-by + status. | This commit |

### Phase 4 closures (commit `9323352`)

| Source | v1.1.0 phase | Item | Resolved in |
|---|---|---|---|
| agentmemory A8 | 4 (4.1) | `MemoryEntry` carries `lifecycleProvenance: {sessionId, hookKind, toolName?, parentSpanId?} \| null`; new `core/memory/types.ts` defines the shape with safe JSON parse/serialize; SQLite migration adds `provenance TEXT NULL` + `scope_id TEXT NULL` to `memories`, `episodic_events`, and `graph_relations`; runtime migration is performed in TypeScript with column-presence guards so re-running on a fresh DB is a no-op. Schema version bumped to 3. New `tests/unit/storage/MemoryStore.provenance.test.ts` exercises: (a) a v1.0.0-shaped DB migrates to v3 with `lifecycleProvenance: null` / `scopeId: null` on every row; (b) new writes round-trip the structured object; (c) idempotent re-open. Canonical migration SQL also checked in at [core/storage/migrations/v1.1.0_provenance.sql](../../../core/storage/migrations/v1.1.0_provenance.sql) for reference | Phase 4 commit `9323352` |
| v1.0.0 4.P1.X (scope_id on persistent memory tables) | 4 (4.1) | Same migration adds `scope_id TEXT NULL` to all three memory tables plus a helper index on each (`idx_memories_scope`, `idx_episodic_scope`, `idx_relations_scope`); mirrors the existing in-memory `MemoryHub` scope filter | Phase 4 commit `9323352` |
| agentmemory A5 | 4 (4.2 + 4.3) | New `core/lifecycle/HookBus.ts` defines a closed 12-variant `LifecycleEvent` discriminated union and an `InProcessHookBus` that wraps `TelemetryBus`. Every emit republishes onto the underlying telemetry bus so existing trace consumers see the events. `AgentLoop.run`, `_runToolCall`, and `spawnSubAgent` emit five of the twelve hooks today (`session.start`/`.stop`, `tool.pre`/`.post`/`.failed`, `subagent.start`/`.stop`). The remaining four (`user.prompt`, `context.preCompact`, `session.end`, `skill.entry`) are deferred under 4.3.P1.J. New `tests/unit/core/lifecycle/HookBus.test.ts` covers every event kind + Disposable + republish; `tests/unit/tools/AgentLoop.hookBus.test.ts` asserts the run boundaries + tool brackets in an integration scenario | Phase 4 commit `9323352` |
| agentmemory A7 | 4 (4.4) | New `core/observability/redactSecrets.ts` consolidates the existing trace-side patterns into a string-in / string-out scrubber covering AWS access keys, classic + fine-grained GitHub PATs, Slack tokens, JWTs, PEM private-key blocks (multi-line), and env-style assignments. Wired into `MemoryStore.save(...)` so every memory write is scrubbed before SQLite insert. Same scrubber redacts `lifecycle.tool.failed.redactedError` so leaked secrets in tool errors never reach the bus. New `tests/unit/core/observability/redactSecrets.test.ts` exercises every pattern + benign-content round-trip + `detectSecretCategories` helper. The `MemoryStore.provenance.test.ts` redaction case asserts the end-to-end gate against an `AKIA...` AWS key | Phase 4 commit `9323352` |
| -- (UI surface) | 4 (4.5) | `MemoryPanel` gains a "Show provenance" toggle; when on, each entry renders `hookKind` + `toolName` chips. `TraceDashboardPanel` gains a `hookKind` dropdown filter populated from the distinct hookKinds present on the event stream. Protocol schemas extended with optional `MemorySnapshot.provenance` map and `TraceEvent.hookKind` string. Sidecar producer wiring is tracked at 4.5.P2.L | Phase 4 commit `9323352` |

---

## 3. Summary

| Severity | Open | Resolved | Total |
|---|---|---|---|
| P0 | 0 | 0 | 0 |
| P1 | 11 | 27 | 38 |
| P2 | 25 | 45 | 70 |
| P3 | 1 | 0 | 1 |
| **Total** | **37** | **72** | **109** |

Phase 15 contributes six closures across the eleven Phase 15 sub-tasks (the static-portion review synthesis at `docs/versions/v1/v1.1.0/review/synthesis.md` mirroring the v1.0.0 structure in 15.1; the version bump from `1.0.0` to `1.1.0` across all 7 product-version files plus the NSIS literal in 15.6; the hand-authored CHANGELOG.md v1.1.0 entry + the user-facing release-notes.md in 15.7; the docs/versions/v1/v1.1.0/distribution.md mirroring v1.0.0's three-OS structure plus the renamed Marketplace listing in 15.10; the operator-actions.md extension with the six new OA-V1.1.0-15A through 15F carryforward IDs in 15.2-5 + 15.8; this Phase 15.9 file-level finalization itself -- the status flip, summary recompute, and carryforward map population). Phase 15 opens four new items tracking the operator-driven remainder: 15.1.P1.KK (live `/run-deep-review` chain not exercised on this static-review host), 15.8.P1.LL (`npx semantic-release --dry-run` not exercised here), 15.11.P2.MM (final lint/build/test gate not run end-to-end here -- carried forward to OA-V1.1.0-15E RTM smoke per OS), and 15.2-5.P1.NN (the consolidated live operator-action set: EV signing + notarization + AppImage, SHA rotations + final brand icons, golden-task replay + GPU bench + live DevAI-Hub sync, RTM smoke per OS).

Phase 14 contributes ten closures across the fourteen Phase 14 sub-tasks: the cross-platform `HostProfile` detection (14.1, closing v1.0.0 9.P1.CCC); the OS-aware provisioner dispatch (14.2, closing 9.P2.DDD jointly with 14.3 + 14.4); the macOS provisioners covering Metal + Ollama + ffmpeg (14.3); the Linux provisioners covering CUDA + ROCm + CPU fallback + Ollama (14.4); the live disk-aware footer + 10 GB OS reserve in `InstallerState` (14.5, closing v1.0.0 9.P2.EEE); the Text / Image / Video / Audio tabbed model picker fed by `core/registry/catalog.json` + `recommended.json` with compatibility badges and disk-aware checkbox greying (14.6, closing v1.0.0 6.P1.HH); the Nexus VS Code extension add-on page with `code`/`code-insiders`/`cursor` auto-detection (14.7, closing v1.0.0 7.P1.NN); the final disk + hardware guard at "Begin Installation" with re-detection and a bounce-back to the picker on failure (14.8, closing v1.0.0 7.P1.OO); the macOS DMG and Linux AppImage outer-shell workflows promoted to `push: tags` with payload-fetch + PyInstaller freeze + create-dmg / appimagetool assembly (14.9, closing v1.0.0 9.P1.AAA + 9.P2.BBB modulo OA-11 signing/notarization); the cross-OS payload fetcher at [scripts/installer/build/fetch-payload.py](../../../scripts/installer/build/fetch-payload.py) parameterized by `--os` + `--arch` with SHA-256 pinning via `versions.lock.json` (14.10, closing v1.0.0 9.P1.ZZ); the Storage Review page with runtime / models / DevAI-Hub / reserve / net coloring (14.11); the cross-OS first-launch migration shim Python-side (14.12); plus the three RTM smoke checklists (14.13). The Phase 14 work ships 122 new installer tests across 10 test files (33 host_detect + 12 provisioner_dispatch + 13 Phase 14 provisioners + 10 disk_aware_footer + 19 typed_catalog + 7 vscode_extension_page + 8 install_guard + 8 storage_page + 5 storage_migration + 5 fetch_payload_script + 2 cross-cutting cases via the package), bringing the installer test suite from 252 to 374 cases (all green). No new open items: every Phase 14 sub-task landed in scope; signing + notarization remain the existing OA-11 operator action. Phase 13 contributes four closures across the four Phase 13 sub-tasks (the Video Lab "Fast 720p" preset + sampler dropdown widening + `flow-dpm-solver` validator allowance in 13.1; the dedicated `tests/python/diffusion/test_sana_video.py` exercising registration + IPC round-trip + workflow JSON + insufficient-VRAM + offload reporting + outputs-dir override with 100% coverage on `sana_video.py` in 13.2; the installer `default_checked` opt-in surface with SANA-Video appearing in every preset and unchecked-by-default in Light + Recommended in 13.3; the four-step gate confirmation in 13.4 against the existing Phase 12 OA-09 entry). 18 new desktop tests (7 VideoPromptForm presets + 8 installer recommended-models opt-in cases + 3 ModelEntry/default_checked unit cases) and 18 new Python tests; OA-09 already carries the SANA-Video 4 s 720p <= 60 s timing target from Phase 12 so no new operator-action row opens. Two new open items track the test-path deviation (13.2.P3.II) and the static `DEFAULT_VIDEO_MODELS` array's deferred live-registry wiring (13.1.P2.JJ, clusters with 11.1.P2.CC / 10.1.P1.Z). Phase 12 contributes nine closures across the nine Phase 12 sub-tasks (catalog + DC-AE VAE registration; the four diffusers-backed pipeline modules `sana.py` / `sana_sprint.py` / `sana_int4.py` / `sana_video.py`; SANA-ControlNet preprocessor wiring; the Image Studio UI Fast Preview / multi-lang hint / Flow-DPM-Solver / 2K-4K tier-gated dropdown; the installer preset rewire to SANA-1.6B + Sana-Sprint; the new `catalog-digests.test.ts` recognition surface; the v1.1.0 operator-actions ledger with OA-09 extended for SANA timings and OA-V1.1.0-12A opened for SANA digest rotation). The Phase 12 work ships 17 new Python tests (`test_pipelines_sana.py`), 8 new desktop React tests (Fast Preview / hint / sampler / dropdown gating / resolution preset), 3 new TS tests (`catalog-digests.test.ts`), and 3 new installer-preset cases (`test_recommended_models.py`) -- 31 Phase-12 test additions in total. Three new open items track the operator-action handoff (OA-09), the nunchaku real-host wheel verification, and the page-level `diffusionTier` wire-up to `useDiffusionTier()` (12.2.P1.FF, 12.4.P2.GG, 12.7.P2.HH). Phase 11 contributes ten closures across the ten Phase 11 sub-tasks (`ModelDropdown`, `PlanArtifact`, `AutoModeStream`, `MemorySnapshotView`, `SlashAutocomplete`, `SessionList`, `McpBridge`, `SettingsBridge`, the parity test suite, and the proxy / IPC-client wiring). The seven new `core/coding/*` modules ship with 71 dedicated unit tests; the structural IPC client adds 10 more; the parity test adds 4; the proxy test gains 2 more cases (12 total Phase 11 -> proxy / IPC scaffolding) for a Phase 11 test count of 100 new cases. Five new IPC method schemas (`models.list`, `coding.chat.autocomplete`, `mcp.list`, `mcp.invoke`, `settings.get`, `settings.set`) are wired into [desktop/sidecar/src/protocol.ts](../../../desktop/sidecar/src/protocol.ts) with `implemented: true`. Three new P2 deferrals open: 11.1.P2.CC (the seven panel webview shells stay placeholders until the 10.1.P1.Z cross-process transport lands; the projectors / handlers / parity tests are all ready), 11.8.P2.DD (SQLite-backed `SettingsStore` adapter clusters with the same `MemoryStore` adapter cluster as 5.6.P2.O / 6.6.P2.S / 9.1.P2.W), 11.9.P2.EE (DOM-level parity snapshot deferred to Tailwind v4 wiring under 1.11.P1.G; projector-level parity is sufficient detection today). Phase 10 contributes three closures (v1.0.0 3.P1.O thin-adapter rewrite, v1.0.0 11.P1.LLL bundled as part of the same rewrite, plus the 10.2 compat-shim tightening to once-per-session deprecation logs) plus the Marketplace re-publish operator-action documentation (closes the publish-surface portion of 2.P1.J / 2.P2.K). Three new P-level deferrals open: 10.1.P1.Z (proxy-branch daemon IPC client awaits the upstream Phase 2 widening), 10.1.P2.AA (thin-webview-shell rewrites of the three panels follow once 10.1.P1.Z lands), 10.3.P2.BB (Marketplace publish + legacy listing transition note are operator actions documented in [marketplace-transition.md](marketplace-transition.md)). Phase 9 contributed three closures (agentmemory A4 opt-in contradiction resolver, A9 opt-in file compressor + `nexus memory compress` CLI + `/memory-compress` slash command, plus the two new settings schema entries) and three P2 deferrals (9.1.P2.W IdleTimeScheduler binding deferred to the same `MemoryStore` adapter cluster that holds 5.6.P2.O / 6.6.P2.S, 9.2.P2.X sidecar slash dispatcher wiring deferred to the 1.4.P1.B `src/chat/` migration, 9.2.P2.Y per-shard embedding deviation tracked as an NI). Phase 8 contributed six closures (10.P1.GGG SkillsReloader, 10.P1.HHH weekly auto-sync worker, 10.P2.III install/remove + allowlist + scanner, 10.P2.JJJ preferUpstream in autocomplete, 10.P2.KKK AgentLoop.setCurrentSkill + lifecycle.skill.entry, plus the settings schema entries) and three P2 deferrals (8.1.P2.T sidecar daemon-entry-point wiring, 8.3.P2.U live HTTPS install smoke deferred to Phase 15, 8.4.P2.V preferUpstream daemon-side dispatcher deferred to the 1.4.P1.B `src/chat/` migration). Phase 7 contributed one closure (agentmemory A6 session replay timeline) with no new open items. Phase 6 contributed seven closures (A11 audit, A10 export+import, `/recall`, `/remember`, A12 `/forget`, A3 Ebbinghaus decay, plus the CLI parsing surface) and four P2 deferrals (6.1.P2.P, 6.2.P2.Q, 6.5.P2.R, 6.6.P2.S) -- all still open and clustered around the same live `MemoryStore` adapter cluster.

By category (open items only):
- **DF** (deferred): 31 (28 architectural + 3 Phase 15 operator-driven: 15.1.P1.KK live deep-review chain, 15.8.P1.LL semantic-release dry-run, 15.2-5.P1.NN consolidated live operator-action set)
- **MT** (missing tests): 3 (Phase 15 adds 15.11.P2.MM end-to-end gate)
- **NI** (not implemented as planned): 3
- **BG / WN / QG**: 0

By phase (open items only):
- Phase 1: 6 (deferred sub-tasks 1.1 wiring + 1.4 wholesale move (12/13 sub-trees still open) + 1.10 NexusCodingRuntime + 1.11 Tailwind v4 + 1.12.P2.H + 1.12.P2.I) -- the codemod from Phase 3 (3.P1.A) is now available to consume them.
- Phase 4: 3 (4.3.P1.J emit sites + 4.1.P2.K fixture-in-test + 4.5.P2.L sidecar producer)
- Phase 5: 3 (5.1.P1.M hash-fallback in CI + 5.5.P1.N UnifiedMemoryRetriever migration deferred to Phase 6/9 + 5.6.P2.O IdleTimeScheduler binding deferred to Phase 6)
- Phase 6: 4 (6.1.P2.P SQLite audit log table deferred + 6.2.P2.Q export/import CLI consumes injected JSONL + 6.5.P2.R Memory panel "Forget" IPC pipeline deferred + 6.6.P2.S DecaySweep IdleTimeScheduler binding deferred)
- Phase 7: 0 new open items.
- Phase 8: 3 (8.1.P2.T sidecar entry-point wiring + 8.3.P2.U live HTTPS install smoke + 8.4.P2.V daemon-side dispatcher consults `preferUpstream`)
- Phase 9: 3 (9.1.P2.W ContradictionResolver IdleTimeScheduler binding + 9.2.P2.X `/memory-compress` daemon-side dispatch + 9.2.P2.Y per-shard embedding deviation)
- Phase 10: 3 (10.1.P1.Z daemon IPC client deferred to Phase 2 widening + 10.1.P2.AA thin-webview shells deferred to once IPC client lands + 10.3.P2.BB Marketplace publish + legacy listing transition note are operator actions)
- Phase 11: 3 (11.1.P2.CC Phase 11 proxy panels render the placeholder shell pending 10.1.P1.Z + 11.8.P2.DD SQLite-backed SettingsStore adapter clusters with the MemoryStore adapter cluster + 11.9.P2.EE DOM-level parity snapshot deferred to Tailwind v4 wiring)
- Phase 12: 3 (12.2.P1.FF real-diffusers `_execute(ctx)` callback deferred to operator action OA-09 + 12.4.P2.GG nunchaku wheel + license verification on the diffusion-low 8 GB rig + 12.7.P2.HH `<ImageStudioPage diffusionTier={...}>` mount-site wire-up to `useDiffusionTier()`)
- Phase 13: 2 (13.2.P3.II SANA-Video test lives under `tests/python/diffusion/` rather than `runtimes/diffusion/tests/` (deviation kept; satisfies acceptance at the test-shape level) + 13.1.P2.JJ VideoLabPage `DEFAULT_VIDEO_MODELS` still a static array, live registry wiring deferred to the Phase 2 IPC widening)
- Phase 14: 0 new open items. Every Phase 14 sub-task closed in scope; OA-11 (Authenticode + Apple Developer ID + notarization) remains the Phase 15 operator action that the macOS / Linux outer-shell workflows degrade gracefully around when secrets are absent.
- Phase 15: 4 new open items, all tracking operator-driven sub-tasks not exercisable on the static-review host: 15.1.P1.KK (live `/run-deep-review` + `/run-security-audit` + `/run-penetration-test --depth=deep` chain -> OA-V1.1.0-15A); 15.8.P1.LL (`npx semantic-release --dry-run` -> OA-V1.1.0-15F); 15.11.P2.MM (final lint/build/test gate end-to-end on the static-review host -> OA-V1.1.0-15E RTM smoke per OS); 15.2-5.P1.NN (consolidated live operator-action set: EV signing + notarization + AppImage [OA-V1.1.0-15B] + SHA rotations + final brand icons [OA-V1.1.0-15C] + golden-task replay + GPU bench + live DevAI-Hub sync [OA-V1.1.0-15D] + RTM smoke per OS [OA-V1.1.0-15E]). Six closures: 15.1 static-portion synthesis, 15.6 version bump, 15.7 CHANGELOG + release-notes, 15.9 file-level finalization, 15.10 distribution.md, 15.2-5+15.8 operator-actions documentation.

---

## 4. Carryforward map (v1.0.0 -> v1.1.0)

This table mirrors the cycle plan's [Carryforward Map](plans/v1.1.0-cycle.md#carryforward-map-v100---v110). Items in **Phase 1** are closed here (see `## 2. Resolved` above) or recorded as open items in `## 1` when deferred to "Phase 1b". Items in later phases are open and tracked against their target phase.

| v1.0.0 code | v1.1.0 phase | Status (as of 2026-05-20) |
|---|---|---|
| 2.P1.G | 1 | Closed (Phase 1 commit `ec3ff0e`, sub-task 1.2) |
| 2.P1.H | 1 | Closed (Phase 1 commit `ec3ff0e`, sub-task 1.3) |
| 2.P2.I | 1 / 3 / future | Partially closed -- Phase 3 (this commit, sub-tasks 3.1 + 3.2) lands the codemod and migrates `src/utils/`; open item 1.4.P1.B holds the per-sub-tree status table for the remaining 12 sub-trees |
| 2.P1.J | 1 (manifest) + 10 | Closed-manifest-portion (Phase 2 commit `de219a5`, sub-task 2.1); Marketplace re-publish operator action documented in [marketplace-transition.md](marketplace-transition.md) (10.3.P2.BB, target Phase 15 RTM) |
| 2.P2.K | 1 (npm) + 10 | Closed-npm-portion (Phase 2 commit `de219a5`, sub-task 2.2); Marketplace re-publish operator action documented in [marketplace-transition.md](marketplace-transition.md) (10.3.P2.BB, target Phase 15 RTM) |
| 2.P3.L | 1 | Closed (Phase 1 commit `ec3ff0e`, sub-task 1.8) |
| 3.P1.M | 1 | Deferred (open item 1.10.P1.F) -- waits for `src/runtime/` to migrate under 1.4.P1.B |
| 3.P1.P | 1 | Closed (Phase 1 commit `ec3ff0e`, sub-task 1.7) |
| 3.P2.S | 1 | Closed (Phase 2 commit `de219a5`, sub-task 2.3) |
| 5.P3.FF | 1 (subsumed by 2.P3.L) | Closed (Phase 1 commit `ec3ff0e`) |
| 10.P1.GGG | 8 | Closed (Phase 8 this commit, sub-task 8.1) -- `SkillsReloader` ships; sidecar entry-point wiring deferred under 8.1.P2.T |
| 10.P1.HHH | 8 | Closed (Phase 8 this commit, sub-task 8.2) -- weekly auto-sync worker factory + bootstrap registration |
| 10.P2.III | 8 | Closed (Phase 8 this commit, sub-task 8.3) -- install/remove CLI with allowlist + scanner; live HTTPS smoke deferred under 8.3.P2.U |
| 10.P2.JJJ | 8 | Closed (Phase 8 this commit, sub-task 8.4) -- `filterSlashCommandsWithSkills` orders by `preferUpstream`; daemon dispatcher deferred under 8.4.P2.V |
| 10.P2.KKK | 8 | Closed (Phase 8 this commit, sub-task 8.5) -- `AgentLoop.setCurrentSkill` + `lifecycle.skill.entry` |
| 3.P1.O | 10 | Closed (Phase 10 this commit, sub-task 10.1) -- `src/extension.ts` 478 -> 64 lines; activation dispatches between [activation/proxy.ts](../../../src/activation/proxy.ts) and [activation/extensionOnly.ts](../../../src/activation/extensionOnly.ts) via `discoverDesktopDaemon()`; proxy-branch IPC client deferred under 10.1.P1.Z |
| 11.P1.LLL | 10 | Closed (Phase 10 this commit, sub-task 10.1, bundled with 3.P1.O) -- thin-adapter rewrite is the spiritual successor to "extension rewrite + Marketplace re-publish" pairing; re-publish operator action documented under 10.3.P2.BB |
| 1.P2.B | 1 | Deferred (open item 1.11.P1.G) -- folds into cycle Phase 11 webview-build pipeline change |
| 1.P2.C | 15 (OA-07) | Open (operator action, target Phase 15) |
| 1.P2.D | 1 / 15 | Open (target Phase 15 RTM) |
| All remaining v1.0.0 carryforwards (Phases 2-15) | 2-15 | Open, tracked in cycle plan |

---

## 4b. Carryforward map (v1.1.0 -> v1.2.0)

This table consolidates every "Open" item from `## 1. Open Items` and groups it into the v1.2.0 absorption bucket. Architectural items recur in `docs/versions/v1/v1.2.0/known-gaps.md` (when that cycle opens) with cross-references back to the v1.1.0 entry code; operator-driven items live in [operator-actions.md](operator-actions.md) under stable OA-V1.1.0-* IDs.

### 4b.1 Architectural carryforward (v1.2.0 cycle absorbs)

| v1.1.0 code | Bucket | Cluster destination in v1.2.0 |
|---|---|---|
| 1.1.P1.A | TypeScript project-references wiring | "Phase 1a" of v1.2.0 -- lands alongside the wholesale `src/` -> `modules/coding/` move. |
| 1.4.P1.B | `src/` -> `modules/coding/` wholesale move (12 of 13 sub-trees still open) | v1.2.0 "Phase 1b" sweep -- each sub-tree gets its own commit + CI run per the per-sub-tree status table in 1.4.P1.B. |
| 1.10.P1.F | `NexusCodingRuntime` sidecar wiring | Unblocked by `src/runtime/` -> `modules/coding/runtime/` migration in 1.4.P1.B. |
| 1.11.P1.G | Tailwind v4 wiring | Folded into v1.2.0 webview build pipeline step (clusters with 11.9.P2.EE). |
| 1.12.P2.H | Curator scheduler-only entry regression test | "Phase 1b" alongside the runtime move. |
| 1.12.P2.I | v1.0.0 operator-action carryforward (informational, mirrors OA-NN) | Resolves via the live OA closures (see 4b.2 below). |
| 4.3.P1.J | Four remaining HookBus emit sites (`user.prompt`, `context.preCompact`, `session.end`, `skill.entry`) | Clusters with the `src/runtime/` move in 1.4.P1.B. |
| 4.1.P2.K | In-test SQLite v1.0.0 fixture vs binary check-in | Keep current approach unless a multi-version fixture matrix becomes valuable. |
| 4.5.P2.L | Sidecar producer wiring for provenance + hookKind UI surfaces | Clusters with the `MemoryStore` adapter cluster (5.6.P2.O / 6.1.P2.P / 6.5.P2.R / 6.6.P2.S / 9.1.P2.W / 11.8.P2.DD). |
| 5.1.P1.M | LocalEmbedder hash fallback in CI vs bundled ONNX on operator rig | Closes when the v1.1.0 installer payload smoke + the OA-09 GPU-rig bench both run end-to-end against the bundled weights. |
| 5.5.P1.N | `src/storage/UnifiedMemoryRetriever` SQLite-side hybrid migration | Clusters with 6.1.P2.P + 6.2.P2.Q (the same `MemoryStore` adapter cluster). |
| 5.6.P2.O | `WarmRebuildWorker` IdleTimeScheduler binding | `MemoryStore` adapter cluster. |
| 6.1.P2.P | SQLite `memory_audit_log` table + `SqliteMemoryAuditLog` | `MemoryStore` adapter cluster (schema version bump to 4). |
| 6.2.P2.Q | `MemoryStoreExportSource` / `MemoryStoreImportSink` for live-store CLI | `MemoryStore` adapter cluster. |
| 6.5.P2.R | Memory panel "Forget" IPC pipeline | `MemoryHub.delete(id)` + protocol schema + sidecar handler -- `MemoryStore` adapter cluster. |
| 6.6.P2.S | `DecaySweep` IdleTimeScheduler binding | `MemoryStore` adapter cluster. |
| 8.1.P2.T | Sidecar daemon-entry-point wiring of `SkillsReloader` | Lands after the upstream `SkillCatalog`-backed adapter (tracked in v1.0.0 known-gaps under `MV`). |
| 8.3.P2.U | Live HTTPS skill install integration test | Folds into v1.2.0 RTM checklist after Phase 15 RTM (this cycle) completes. |
| 8.4.P2.V | `nexus.skills.preferUpstream` daemon-side dispatcher | Lands after `src/chat/` -> `modules/coding/chat/` migrates under 1.4.P1.B. |
| 9.1.P2.W | `ContradictionResolver` IdleTimeScheduler binding | `MemoryStore` adapter cluster. |
| 9.2.P2.X | `/memory-compress` daemon-side dispatch | Lands after `src/chat/` migrates under 1.4.P1.B. |
| 9.2.P2.Y | File compressor per-shard embedding | Clusters with 9.1.P2.W's adapter work (adds `memory_compress_shards` child table). |
| 10.1.P1.Z | Daemon IPC client for the proxy branch | Clusters with the Phase 2 IPC widening carryforward (v1.0.0 3.P1.N / 3.P2.U). |
| 10.1.P2.AA | Thin-webview shells for `NexusCodingPanel` / `MemoryPanel` / `TraceDashboardPanel` | Lands after 10.1.P1.Z. |
| 10.3.P2.BB | Marketplace publish + legacy listing transition note | Operator action -- see OA-V1.1.0-10A / OA-V1.1.0-10B in [marketplace-transition.md](marketplace-transition.md). |
| 11.1.P2.CC | Phase 11 proxy panels mount placeholder shells | Lands after 10.1.P1.Z. |
| 11.8.P2.DD | SQLite-backed `SettingsStore` adapter | `MemoryStore` adapter cluster. |
| 11.9.P2.EE | DOM-level extension/desktop parity snapshot | Clusters with 1.11.P1.G (Tailwind v4 + jsdom + webview-postMessage harness). |
| 12.2.P1.FF | SANA real-diffusers `_execute(ctx)` callback | Operator action -- see OA-V1.1.0-15D (extends OA-09). |
| 12.4.P2.GG | `nunchaku` wheel + license verification on diffusion-low rig | Operator action -- see OA-V1.1.0-15D. |
| 12.7.P2.HH | `<ImageStudioPage diffusionTier={...}>` mount-site wire-up to `useDiffusionTier()` | One-call subscription change at App.tsx / Routes layer; lands alongside the next desktop-app commit cluster. |
| 13.1.P2.JJ | `DEFAULT_VIDEO_MODELS` -> live registry wiring | Clusters with 10.1.P1.Z (the cross-process IPC transport). |
| 13.2.P3.II | SANA-Video test path deviation | Confirm `tests/python/diffusion/` as canonical home and update the Phase 13 plan reference (no code change). |

### 4b.2 Operator-driven carryforward (live OA closures)

| v1.1.0 code | OA ID | Acceptance |
|---|---|---|
| 15.1.P1.KK | OA-V1.1.0-15A | Live `/run-deep-review` chain produces zero new P0 / P1 findings against `docs/versions/v1/v1.1.0/review/synthesis.md`. |
| 15.2-5.P1.NN (signing + notarization + AppImage portion) | OA-V1.1.0-15B (rolls up OA-01 + OA-11 + OA-12) | Signed Windows installer + notarized macOS DMG + tested Linux AppImage on Ubuntu 22.04 / 24.04 / Fedora 40. |
| 15.2-5.P1.NN (SHA + icons portion) | OA-V1.1.0-15C (rolls up OA-06 + OA-07) | CI passes new hash assertions; v1.1.0 build carries the final icons. |
| 15.2-5.P1.NN (golden + GPU + sync portion) | OA-V1.1.0-15D (rolls up OA-08 + OA-09 + OA-10) | All three actions completed; any timing regressions outside +/-10% open a new known-gap entry against the next cycle. |
| 15.2-5.P1.NN (RTM portion) + 15.11.P2.MM | OA-V1.1.0-15E | RTM smoke checklists signed off green per OS. |
| 15.8.P1.LL | OA-V1.1.0-15F | `npx semantic-release --dry-run` output matches the hand-authored CHANGELOG entry from 15.7 modulo formatting. |
| Phase 10 Marketplace transition | OA-V1.1.0-10A + OA-V1.1.0-10B | Renamed `nexus-coding` listing live; legacy `gemma-code` listing carries the transition note. |
| Phase 12 SANA digest rotation | OA-V1.1.0-12A | All 10 SANA-family entries in `core/registry/catalog.json` carry canonical Hugging Face digests; `catalog-digests.test.ts` placeholder list is empty. |

---

## References

- [docs/versions/v1/v1.0.0/known-gaps.md](../v1.0/known-gaps.md) -- upstream cycle gap log
- [docs/versions/v1/v1.0.0/operator-actions.md](../v1.0/operator-actions.md) -- OA-01 through OA-12
- [docs/versions/v1/v1.1.0/plans/v1.1.0-cycle.md](plans/v1.1.0-cycle.md) -- active plan
- [docs/versions/v1/v1.1.0/plans/phase-01-shared-core-and-carryforward-closure.md](plans/phase-01-shared-core-and-carryforward-closure.md) -- Phase 1 detail
- [docs/versions/v1/v1.1.0/plans/phase-02-rebrand-and-core-extraction.md](plans/phase-02-rebrand-and-core-extraction.md) -- Phase 2 detail (rebrand + sidecar core extraction)
- [docs/versions/v1/v1.1.0/plans/phase-03-coding-module.md](plans/phase-03-coding-module.md) -- Phase 3 detail (codemod + first `src/` -> `modules/coding/` sub-tree migration)
- [docs/versions/v1/v1.1.0/plans/phase-05-hybrid-retrieval-and-local-embedder.md](plans/phase-05-hybrid-retrieval-and-local-embedder.md) -- Phase 5 detail (hybrid retrieval + local embedder + warm-build worker)
- [docs/versions/v1/v1.1.0/development/decisions/shared-core-build.md](development/decisions/shared-core-build.md) -- ADR for sub-task 1.1
- [scripts/dev/rewrite-imports.mjs](../../../scripts/dev/rewrite-imports.mjs) -- generic import-rewriting codemod consumed by Phase 3 + future sub-tree migrations
- [docs/versions/v1/v1.1.0/plans/phase-10-vscode-thin-adapter-and-republish.md](plans/phase-10-vscode-thin-adapter-and-republish.md) -- Phase 10 detail (VS Code extension thin-adapter rewrite + Marketplace re-publish)
- [docs/versions/v1/v1.1.0/marketplace-transition.md](marketplace-transition.md) -- Phase 10 operator-action checklist (OA-V1.1.0-10A + OA-V1.1.0-10B)
- [docs/versions/v1/v1.1.0/plans/phase-15-hardening-and-release.md](plans/phase-15-hardening-and-release.md) -- Phase 15 detail (hardening + release gate)
- [docs/versions/v1/v1.1.0/operator-actions.md](operator-actions.md) -- Phase 15 operator-action ledger (OA-V1.1.0-15A through F)
- [docs/versions/v1/v1.1.0/release-notes.md](release-notes.md) -- v1.1.0 user-facing release notes (Phase 15.7)
- [docs/versions/v1/v1.1.0/distribution.md](distribution.md) -- v1.1.0 distribution channels (Phase 15.10)
- [docs/versions/v1/v1.1.0/review/synthesis.md](review/synthesis.md) -- v1.1.0 static deep-review synthesis (Phase 15.1)
- [CHANGELOG.md](../../../CHANGELOG.md) -- v1.1.0 hand-authored release entry (Phase 15.7)
