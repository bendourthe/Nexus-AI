# Plan -- VoiceStudio Field-Discipline Adoption

**Project**: Nexus AI Studio
**Version**: v2.4.9
**Slug**: adoption-voicestudio-field-discipline
**Filename**: v2.4.9-adoption-voicestudio-field-discipline.md
**Plan Type**: Feature (engineering-discipline adoption)
**Created**: 2026-09-09
**Seeded from**: [v2.4.9-comparison-voicestudio.md](../comparisons/v2.4.9-comparison-voicestudio.md)
**Goal**: Close the loop between a shipped Nexus build and the repository that produced it -- a feature inventory CI actually enforces, a written bar every new model must clear, security scanning that covers the Python half of the tree, bug reports that carry the build they came from, and databases that survive their own migrations.

> **This plan was critiqued before it was presented.** Three review lenses (coherence, feasibility, scope) returned 36 findings against the first draft; two were P0. All mechanical findings are folded in. Three decisions the lenses raised need a human and are collected in `## Blocking decisions` below -- **read that section before starting Phase 1.** The interactive grill stage did not run, so those three are open rather than resolved. See `## Review record`.

## Blocking decisions

These are not implementation slices. Each changes what this plan is, and none can be settled from the codebase alone.

### D1 -- Does Phase 5 belong in a patch release at all? **RESOLVED: option 1 (split), 2026-09-10.**

**The question.** v2.4.9 is a patch number. Phase 5 adds a user-visible refuse-to-start path, and (per the P0 below) needs a new `needsMigration()` predicate in two stores plus a completion marker written into existing user databases. `semantic-release` computes the version from commit types on `main`, and this work produces a `feat`, not a `fix`. The plan would therefore compute a minor while claiming to be v2.4.9.

**Options.**

1. **Split.** Phases 1-4 ship as v2.4.9 (all additive, no product behavior change, which is what a patch number means). Phase 5 becomes its own minor. Consequence: A11 (the updater) is gated on A5, so deferring Phase 5 also defers A11 further.
2. **Retarget the whole plan** to the next minor and let all six phases ship together.
3. **Keep as written** and reconcile the version at publication. Rejected by the reviewer as discovering the collision at 5.10 rather than deciding it at Phase 1.

**Recommendation: option 1.** It matches what the version numbers already mean, and Phases 1-4 are genuinely shippable without Phase 5. But this is a scope decision the owner makes, not the implementer.

**Resolution, 2026-09-10: option 1.** Phase 5 was moved verbatim into [v2.5.0-migration-durability.md](../../v2.5/plans/v2.5.0-migration-durability.md) and the terminal wrap-up phase was renumbered from Phase 6 to Phase 5. This plan is now four delivery slices plus the wrap-up, all additive, which is what the patch number v2.4.9 asserts. Two consequences are carried, not dropped: Definition-of-Done clause 5 is **deferred scope** here and travels with the work (see `## Goals First and Definition of Done`), and A11 is now deferred twice over (see `## Non-Goals`). Task ids `T024`-`T031` left with Phase 5, so this plan's ids are deliberately non-contiguous.

### D2 -- Do the new security checks gate in this release?

Detailed as sub-task 2.5, which cannot be answered until 2.1-2.4 produce their finding counts. Recorded here because the answer changes branch protection, which is a repository-settings change.

### D3 -- What does `feature_list.json` assert, and against which README region?

Detailed as sub-task 3.1. The reviewer added a constraint the first draft missed: `README.md` has no single features region (four `###` children under `## The Four Pillars`, then seven standalone `###` sections, then `### CLI tools (already shipped)` at line 340, interleaved with a ~180-line changelog whose prose contains feature-like names that will produce false passes). Naming the exact heading boundaries the checker parses is part of D3, not a detail of 3.3.

## Goals First and Definition of Done

**The problem this solves.** Nexus-AI has strong CI breadth (19 workflows) and strong release documentation, but almost nothing that carries information *back* from a shipped build. `feature_list.json` claims v0.8.0 while `package.json` says 2.4.1 and the release line is at v2.4.8, and no job reads the file. `codeql.yml` scans `javascript-typescript` only, across 304 tracked Python files that include the entire PyQt installer and all three model runtimes. `pip-audit` covers `scripts/installer` and not the three runtime requirement sets that actually reach user machines. There are no issue templates, so a bug report does not say which build it came from. And migrations run against user databases with no snapshot taken first. Every one of those is a place where a field problem either cannot be measured or cannot be recovered from.

**The persona.** Two of them, wanting different things. The **operator** running field tests before a v2.4 release, who currently has to judge "field testing passed" rather than measure it. And the **user on a shipped build** whose database is one bad migration away from being unrecoverable.

**Definition of Done, phrased from those seats.**

1. A bug filed against Nexus states its build version because the form required it.
2. `scripts/crash-class-report.mjs` runs, bucketizes correctly against fixtures, and reports its unknown-version count truthfully. (Not "the top failure class is a number" -- see the honesty note below.)
3. A CI run fails when a feature named in `README.md` no longer exists in the tree.
4. CodeQL reports Python results with a non-zero scanned-file count.
5. *(Deferred by D1.)* A deliberately corrupted migration, run against a populated database, leaves the original intact and stops the app -- with fail-before evidence recorded. This clause moved to [v2.5.0-migration-durability.md](../../v2.5/plans/v2.5.0-migration-durability.md) with the work that owns it. It is not a miss of this release, and not verified by this release either.

**Clause 2 is deliberately weaker than the first draft's.** Every issue that exists today predates the form, so the first run of the crash-class report will put essentially everything in the unknown bucket. A Definition of Done that promised per-version measurement would be satisfiable only trivially, and the real measurement arrives weeks later in a different release. Phase 5.2 records "first non-empty crash-class report" as a known gap with a date-based exit condition.

**Slice-awareness.** If the release stops after Phase N, clauses owned by later phases are recorded as **deferred scope, not misses**. Phase 5.5 and 5.7 verify only the clauses whose owning phase landed. Clause ownership: 1 and 2 -> Phase 4; 3 -> Phase 3; 4 -> Phase 2. **Clause 5 is no longer owned by any phase of this plan** -- the D1 split moved it to the v2.5.0 migration-durability plan, so it is recorded as deferred scope rather than verified or missed.

**What this plan is not.** It is not a security remediation cycle. Phases 2 and 3 turn *visibility* on; the findings they surface are triaged and recorded, not necessarily fixed in this release.

## Non-Goals

| Out of scope | Reason |
|---|---|
| PostHog or any product analytics | Rejected in the seeding comparison under MCP Registry Policy bucket 5. Adds a third-party data processor to a local-first app. |
| Signed auto-updater with Stable/Preview channels (A11) | Comparison item A11, deferred. Needs signing-key custody, release-manifest publishing, and a rollback story. Gated on A5. **D1 resolved to option 1, so A11 is now deferred twice over** -- once as an out-of-scope comparison item, and again because A5 itself moved to the v2.5.0 plan. Record both deferrals in 5.2. |
| Fixing whatever CodeQL Python and gitleaks find | Phase 2 lands both non-gating precisely so the backlog can be sized before anyone commits to clearing it. The gate-flip is D2; the remediation is a later cycle. |
| Copying any file from the VoiceStudio clone | VoiceStudio is AGPL-3.0, Nexus-AI is MIT. Every item is implemented clean-room from described behavior. The clone was deleted when the comparison was written. |
| `skills-lock.json`-style content-hash pinning of skills | Wrong repository. Nexus-AI consumes Nexus-Hub skills; the integrity concern belongs to the distributor. |
| bun / turbo monorepo migration | A whole-toolchain change with no defect it fixes. |
| `LICENSE-NOTICE.md` for bundled-model terms | An unnumbered `+` candidate from the comparison (Section 3, dimension 1). Real, but it is a licensing-review task with its own research, not a field-discipline item. Recorded in 5.2. |
| `taze --maturity-period` dependency cooling-off window | Unnumbered `+` candidate (comparison dimension 2). Would change how every dependency upgrade is proposed; out of proportion to this plan. Recorded in 5.2. |
| Wiring `desktop-prod:upgrade` install-over-existing-data into CI | Unnumbered `+` candidate (comparison dimension 9). Overlaps the manual upgrade testing that left with Phase 5 under D1; revisit alongside the v2.5.0 plan. Recorded in 5.2. |
| Applying unreconciled canonical-contract CI fields | 5.5 confirms this plan's own checks and *records* the rest. Applying them is a maintenance task of unknown size. |
| Repo-wide known-gaps sweep | Same reason. 5.2 is bounded to this version plus what this plan created. |

## The dirty-tree constraint (read this before Phase 1)

This plan was authored while the working tree carried **32 modified and 6 untracked files**, including an unplanned installer wizard rework (`scripts/installer/src/nexus_installer/pages/*.py`, two new `widgets/` files) belonging to no plan.

- **Stage named paths only. Never `git add -A`, never `git add .`** Each phase's commit names exact files.
- **Phase 1 does not begin until v2.4.8 has landed.** Starting on top of in-flight v2.4.8 work interleaves two releases in one branch.
- **Preferred: commit or stash the installer rework before Phase 1.** It predates this plan and blocks nothing. Sub-task 1.3's mechanical guard exists only because that may not happen; if the rework is cleared first, **cut 1.3** and say so.

## Overview

Four delivery slices plus a terminal wrap-up phase, ordered so the cheapest and least invasive land first. The fifth slice -- the one that touched running code -- left this plan under D1 and now ships as its own minor.

Phases 1 and 2 change no product behavior: one writes two documents, the other adds CI coverage in reporting mode. Phase 3 makes an existing dead artifact true and then enforces it. Phase 4 gives field reports a shape. **No phase of this plan modifies code on the session hot path** -- that was Phase 5, moved out by D1, and with it went this plan's only fail-before/pass-after regression requirement. Every remaining slice is additive.

**Dependencies.** A6 (issue templates) must land before A7 (crash-class report) has real data. A5 (snapshot) must land before A11 (updater, out of scope) is safe -- and A5 left this plan under D1, which is why A11 is deferred twice over. A5 and A7 are **independent** -- the crash report reads issue bodies, not snapshots. The seeding comparison's first draft drew a spurious `A5 -> A7` edge; it has been corrected there.

**Shippable slices.** Phase 1 alone is a complete outcome. So are 1-2, 1-3, and 1-4. Phase 5, the wrap-up, is slice-aware by construction (see Definition of Done).

**Reverse-engineer-first ordering.** Per comparison Section 5.4, `skill-native` items (Phase 1) precede `re-full` items (Phases 2-5); the single `re-partial` item (A11) is out of scope. This plan adds no new outbound call, no new runtime dependency, and no third-party data processor. It does require **the GitHub token an operator already holds**, for the `gh`-based crash-class report in 4.2 -- that is not a new credential, but the first draft's "zero credentials" phrasing was too strong and is corrected here.

## Constitution Check

*GATE: Must pass before Phase 1 begins. Re-check at the Phase 1 exit checklist.*

No constitution file found at `docs/v2/v2.4/constitution.md` - skipping check. Recommend running `/constitution` to establish project principles. This is informational, not blocking.

Four **AGENTS.md `## Critical Rules`** bind this plan and are treated as gates. (The first draft attributed them to `CLAUDE.md`. There is no `CLAUDE.md` in this repository and its absence is deliberate -- AGENTS.md is the single canonical agent directive, and `tests/unit/docs/AGENTS-md.test.ts` asserts the file does not exist. The rules are real; only the attribution was wrong. Corrected in Phase 1 after the standards judge flagged the identical misattribution in the sibling v2.4.10 plan.)

- **Find root causes; no temporary fixes.** Honored. `feature_list.json` is made true *and* enforced. The `pip-audit` gap is closed by a sibling job covering the runtime sets, not by renaming the existing job.
- **Every changed line must trace directly to the user's request.** Honored, with one declared exception in Complexity Tracking (3.2 rewrites nearly all of `feature_list.json`).
- **Destructive git commands require user confirmation.** Binding on 5.10 and on the dirty-tree constraint.
- **Verify work before marking complete.** Every phase names an artifact to open or a command to run. Where an observation genuinely requires a merged branch, it is deferred to 5.10 explicitly rather than demanded of a phase forbidden to push.

## Current model map

**Model map status**: fresh as of 2026-09-09; sources cited below.

**Provenance note**: validated on 2026-09-09 for the sibling plan `v2.4.10-adoption-minicpm5-mistral-models.md` against the sources below, and carried here unchanged because both plans were authored the same day. Re-confirm if implemented later; `/implement` re-checks regardless.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5-1` | `gpt-6-astra` | `gemini-3.8-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/models/overview
- OpenAI: https://developers.openai.com/api/docs/models
- Google: https://ai.google.dev/gemini-api/docs/models
- Cursor: https://cursor.com/docs/models-and-pricing

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|-------|-------|---------|------------------------|--------------------------|
| 1 | The written bars | A model-acceptance bar every future adoption must clear, and a repo-local standards critic that attacks work instead of agreeing with it | strong | medium |
| 2 | Security coverage, reporting first | CodeQL sees Python, a sibling job audits the runtime requirement sets, gitleaks runs non-gating, and installer smoke fires on installer paths | strong | high |
| 3 | The inventory becomes true, then enforced | `feature_list.json` describes the shipped product, a checker fails CI on drift, and a daily job keeps one rolling issue | strong | high |
| 4 | Field feedback has a shape | Bug reports carry their build version, and a correct-by-fixtures crash-class report exists ready for data | standard | medium |
| 5 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Repo left clean, gaps reconciled, pipeline reconciled, branch published and integrated | frontier | max |

**Shared-file note.** Phase 2 (T007) and Phase 3 (T016) both edit `.github/workflows/ci.yml`. They are additive at disjoint anchors -- Phase 2 adds a sibling audit job, Phase 3 adds a drift-check step -- but they are **not** freely parallelizable on that file. Sequence Phase 3's `ci.yml` edit after Phase 2's, or rebase.

**Test-file extension.** `configs/vitest.config.ts` includes only `tests/unit/**/*.test.ts` and `tests/integration/**/*.test.ts`. A `.test.mjs` file is **silently never collected**. Every test this plan adds is `.test.ts`, testing `.mjs` scripts -- which is already the repo convention (`tests/unit/scripts/check-pr-checklist.test.ts`, `check-architecture.test.ts`, `sync-tauri-version.test.ts`).

---

## Phase 1: The written bars

**Goal**: Put two documents in place that constrain every later decision -- a bar new models must clear, and a critic that reviews against this project's own standards.
**Prerequisites**: v2.4.8 merged. See the dirty-tree constraint.
**Stability Gate**: `docs/reference/model-acceptance.md` assigns every model in `core/registry/catalog.json` to at least one named job, one holder per job, no orphans. `.claude/agents/nexus-standards-judge.md` exists and returns a substantiated finding or an explicit account of what it checked.
**Verification Expectation**: *(fully local; nothing here needs a push.)* Open `docs/reference/model-acceptance.md` and confirm every `id` in `core/registry/catalog.json` appears in exactly one job row with exactly one holder. Then invoke the standards judge against the v2.4.8 change range -- pin both endpoints as `989107c7..<the v2.4.8 merge commit>`, resolved at implementation time, because `HEAD` moves once v2.4.8 merges -- and read its output. A finding must name a specific input or state and the wrong result it produces. "Looks good" with no account of what was examined fails this gate.
**Recommended model tier**: strong
**Recommended effort level**: medium
**Rationale**: Prose and an agent definition, but the bar must reconcile against a real catalog and the judge must be grounded in this project's documented rules rather than generic review advice.

### Sub-tasks

#### 1.1 -- The model and runtime acceptance bar

**Objective**: Write the bar that v2.5.0's adoption cycle, and every one after it, must clear.

**Prompt**:
> Write `docs/reference/model-acceptance.md`, defining when a new model or runtime is allowed into the Nexus catalog.
>
> Structure it as a **job map**: every model and runtime in `core/registry/catalog.json` and `core/registry/ModelCatalog.ts` owns at least one named job, and each job has exactly one holder. Build the map from what is actually there. Do not invent jobs to make the table look complete. A model you cannot assign a job to is itself a finding -- record it as an open question.
>
> Derive job names from the catalog's own structure (task kinds, tier defaults, VRAM classes, license posture), not a generic list. The names must be this project's.
>
> State the bar: a proposal must either take a job from its holder **with measured numbers**, or claim an uncovered job. "It benchmarks well" is not a job. Breadth is only an asset while every entry still works on every supported platform.
>
> Include the license dimension. This project has already made license-driven catalog decisions -- see `v2.4.10-adoption-minicpm5-mistral-models.md`, whose entire argument is a revenue-capped license on the low-VRAM agentic default. Say what license posture is acceptable for a pre-ticked default versus an opt-in entry.
>
> Link it from `CONTRIBUTING.md` and `AGENTS.md`.
>
> Acceptance: every catalog id appears exactly once as a holder or as a recorded open question; every job has exactly one holder; the document says what evidence a challenger must bring.

- [ ] T001 Write the model and runtime acceptance bar docs/reference/model-acceptance.md
- [ ] T002 [P] Link the acceptance bar from CONTRIBUTING.md and AGENTS.md CONTRIBUTING.md
- [ ] T003 [P] Link the acceptance bar from the agent instruction file AGENTS.md

---

#### 1.2 -- The repo-local standards critic

**Objective**: Add an agent that reviews against Nexus-AI's own documented standards and is structurally unable to approve a release.

**Failure modes**: Asked to approve a push, release, or merge, it states plainly that it cannot consent on the owner's behalf and gives its technical verdict instead. If a standards file (`AGENTS.md`, `CONTRIBUTING.md`, `docs/reference/model-acceptance.md`) is missing or unreadable, it reports which and reviews against the remainder rather than silently reviewing against nothing. If it finds nothing, it accounts for what it examined; an unaccounted "looks good" is a failed review. If two standards conflict, it names both and does not silently pick one.

**Build class**: load-bearing. This is what makes 1.1's bar enforceable by something other than memory.

**Prompt**:
> Create `.claude/agents/nexus-standards-judge.md`, a repo-local reviewer agent carrying **this project's** documented standards -- `AGENTS.md` (the single canonical directive, whose `## Critical Rules` carries the binding rules; **there is no `CLAUDE.md` in this repository and a meta-test asserts its absence**), `CONTRIBUTING.md`, `docs/reference/model-acceptance.md` (from 1.1), and the branching and plan-lifecycle rules. It is a critic, not an approver.
>
> Two properties are load-bearing and must appear in the definition itself:
>
> 1. **It never authorizes an irreversible or outward-facing action** -- publishing a release, pushing to a protected branch, deleting data, merging a PR. It may say "this meets the bar"; it may not say "go ahead".
> 2. **Its job is to find what is wrong.** A review returning "looks good" has usually not been done. Assume the author has a blind spot and go looking.
>
> Balance with fairness: a finding it cannot substantiate is noise, and noise trains readers to ignore it. Every finding names a concrete failure -- specific input or state, and the wrong result.
>
> Give it `Bash, Read, Grep, Glob` and no write tools. Set a model tier in frontmatter.
>
> Write from described behavior, not by copying VoiceStudio's `owner-judge.md`; that source is AGPL and this repository is MIT.
>
> Acceptance: invoked against the pinned v2.4.8 range, it produces a substantiated finding or an explicit account of what it examined.

- [ ] T004 Add the repo-local standards critic agent .claude/agents/nexus-standards-judge.md

---

#### 1.3 -- The installer-rework staging guard (cut this if the tree is clean)

**Objective**: Make it mechanically impossible for this plan's commits to sweep in the uncommitted installer rework.

**Cut criterion**: if the installer rework has been committed or stashed before Phase 1 starts, **skip this sub-task entirely** and note the skip. It defends against a state that no longer exists, and every commit prompt already names its staging paths.

**Failure modes**: If the staged file set cannot be determined (not a git repo, git unavailable), it fails closed and blocks with a message saying it could not verify. If a later phase legitimately needs a guarded path, the guard blocks it and the operator removes the path deliberately -- that friction is the point. Note that 2.4 edits `.github/workflows/installer-smoke.yml`, which is **not** under a guarded path, so no conflict arises.

**Build class**: **scaffolding.** It exists only while the rework sits uncommitted. Phase 5.4 removes it and the Phase 5 exit checklist verifies removal.

**Prompt**:
> Append a guard to `.husky/pre-commit` that fails when a commit stages any path under `scripts/installer/src/nexus_installer/pages/` or `scripts/installer/src/nexus_installer/widgets/`.
>
> `.husky/pre-commit` currently contains exactly one line, `npx lint-staged`. There is no script-dispatch convention to follow, so do not go looking for one. Append the guard **ahead of** the `npx lint-staged` line so it fails before lint-staged spends time, and leave that line intact.
>
> The failure message must say why: an unplanned installer wizard rework is sitting uncommitted, it belongs to no plan, and a v2.4.9 commit must not carry it. Point at this sub-task.
>
> Fail closed: if the staged set cannot be determined, block and say so.
>
> Mark the added lines with a comment naming them as v2.4.9 scaffolding removed by Phase 5.4.
>
> Acceptance: staging a guarded path then committing is blocked with the message; a commit staging only this plan's paths succeeds; `npx lint-staged` still runs.

- [ ] T005 Add the scaffolding staging guard ahead of lint-staged .husky/pre-commit

---

#### 1.4 -- Testing and Stabilization

**Objective**: Verify Phase 1 and commit it.

**Prompt**:
> Write `docs/v2/v2.4/development/v2.4.9-phase1-evidence.md` and record into it: the mechanical catalog-vs-job-map check (every id exactly once, no job with two holders), quoting the command and its output.
>
> Exercise the Phase 1 Verification Expectation through [[functional-verification]]: invoke `nexus-standards-judge` against the pinned v2.4.8 range and record its output verbatim. Confirm it names a concrete failure or accounts for what it examined. Confirm the staging guard blocks a guarded path and permits this plan's paths -- or record that 1.3 was cut because the tree was clean.
>
> Re-check the Constitution Check section against what Phase 1 actually produced.
>
> Record this phase's CI IMPACT against [[cicd-architect]] without running remote CI. Expect near no-op: two documents, one agent definition, one local hook.
>
> Run `/generate-session-history` for Phase 1, then create ONE local commit staging only: `docs/reference/model-acceptance.md`, `CONTRIBUTING.md`, `AGENTS.md`, `.claude/agents/nexus-standards-judge.md`, `.husky/pre-commit` (if 1.3 ran), `docs/v2/v2.4/development/v2.4.9-phase1-evidence.md`, and the session-history file. Name every path. Do NOT use `git add -A`.
>
> Do NOT push, do NOT open a pull request, do NOT start remote CI.

- [ ] T006 Verify and commit Phase 1 docs/v2/v2.4/development/v2.4.9-phase1-evidence.md

---

### Phase 1 Exit Checklist

- [ ] All sub-tasks completed (or 1.3 recorded as cut)
- [ ] Every catalog id assigned exactly one job, or recorded as an open question
- [ ] Standards judge invoked against the pinned v2.4.8 range and output recorded
- [ ] Constitution Check re-checked against delivered work
- [ ] Verification expectation exercised and its observable result recorded
- [ ] CI impact recorded, no remote CI run
- [ ] Session history generated
- [ ] One local commit, named paths only
- [ ] No push, PR, or remote CI
- [ ] Ready to advance to Phase 2

---

## Phase 2: Security coverage, reporting first

**Goal**: Make the Python half of the tree visible to security tooling and make installer smoke fire on installer changes, without gating a merge until the backlog is known.
**Prerequisites**: Phase 1.
**Stability Gate**: CodeQL analyzes Python with a non-zero scanned-file count and a per-language check name. A sibling job audits the three runtime requirement sets. gitleaks runs with a bounded, justified allowlist. `installer-smoke.yml` fires on a `paths` filter. No check is required before D2 is recorded.
**Verification Expectation**: **Split, because the first draft demanded observations that require a push while the same phase forbids one.**
> *Locally verifiable (this phase):* run the CodeQL CLI and gitleaks locally to obtain the finding counts D2 needs, and record the scanned-file count -- confirming it is plausible against **304 tracked Python files**, not against a virtualenv-inflated number. Validate all four workflow files parse (`actionlint` or equivalent). Confirm the CodeQL job name templates over `matrix.language`.
> *Deferred to 5.10 (post-publication):* the GitHub Security tab showing Python results, the runtime `pip-audit` job log, and observing `installer-smoke` fire on an installer path and not on an unrelated one. Record these as deferred in the evidence file; do not claim them at Phase 2.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Workflow authorship across four jobs where the first run is likely red for reasons unrelated to correctness; the judgement about what to gate is the substance.

### Sub-tasks

#### 2.1 -- CodeQL sees Python

**Objective**: Extend CodeQL to the tracked Python surface it currently ignores.

**Failure modes**: A first Python run surfacing a backlog must not become a merge blocker; it stays reporting-only until D2. If the extractor fails to build the tree, the job records the extraction failure rather than reporting a false clean -- a green check on an unscanned tree is worse than a red one. If the added language materially lengthens the run, record the new duration so D2 can weigh it.

**Build class**: load-bearing.

**Prompt**:
> Add `python` to the language matrix in `.github/workflows/codeql.yml` (line 38 currently reads `language: ["javascript-typescript"]`).
>
> Three mechanical facts the first draft of this plan got wrong or missed:
>
> 1. **The tracked Python surface is 304 files**, not 3,447. The larger figure swept `scripts/installer/.venv/`, gitignored at `.gitignore:147` and never present in a CI checkout. Calibrate the acceptance criterion to 304 (190 installer, 53 runtimes, 51 tests). A run reporting thousands of files means it is scanning a vendored virtualenv and is wrong.
> 2. **The job name is a literal**, `name: Analyze (javascript-typescript)` at line 26 -- not templated. Adding a matrix entry yields two identically named checks, which makes D2's "gate Python but not TS" unexpressible in branch protection. Change it to `name: Analyze (${{ matrix.language }})`. **This renames the existing TS check**, so branch protection must be updated deliberately.
> 3. **The workflow triggers only on `main`** (`push`/`pull_request` branches `["main"]` plus a weekly cron), while this plan publishes to `develop`. Either add `develop` to the trigger branches, or gather the Phase 2 counts locally with the CodeQL CLI. Decide and say which.
>
> The runtimes carry heavy ML dependency trees; configure the extractor to scan source without installing them, and confirm from the log that extraction succeeded.
>
> **Escape hatch**: if extraction cannot be made to succeed within a stated budget, scope the Python matrix to a named subtree (for example `scripts/installer/src` plus `runtimes/*/[!.]*.py`, excluding anything requiring a built ML environment), record the excluded file count as a known gap, and ship partial coverage rather than blocking Phase 2.
>
> Keep it reporting-only. D2 owns the flip.
>
> Acceptance: Python results exist, the scanned-file count is non-zero and plausible against 304, the check name is per-language, and no merge is blocked.

- [ ] T007 Add Python to the CodeQL matrix and template the job name .github/workflows/codeql.yml

---

#### 2.2 -- A sibling job audits what ships to users

**Objective**: Audit the three runtime requirement sets that reach user machines.

**Failure modes**: An ML dependency with a known advisory and no upstream fix must be recorded, not made a merge blocker -- an unfixable advisory blocking every merge trains the team to bypass the job. An unparseable or missing requirements file fails loudly naming the file rather than skipping silently. Two runtime sets pinning conflicting versions of one package is reported as a finding, not resolved.

**Build class**: load-bearing.

**Prompt**:
> Add runtime dependency auditing for `runtimes/audio/requirements.txt`, `runtimes/diffusion/requirements.txt`, and `runtimes/ocr/requirements.txt`.
>
> **This is a sibling job, not a matrix dimension on the existing one.** The existing `audit-py` job (`.github/workflows/ci.yml:509-539`, named `pip-audit (installer venv)`) is built entirely around a uv project: its cache key is `hashFiles('scripts/installer/uv.lock')` and it runs `uv export --no-hashes --no-annotate --format requirements-txt --no-emit-project`. The runtime sets are plain unlocked `requirements.txt` with no `uv.lock`, so neither the cache key nor the export step transfers. The first draft of this plan said to "follow its shape", which would mislead you.
>
> Decide and state: are the unlocked requirements audited **as written**, or **resolved first**? They give different answers, and the resolved set is the one that matches what installs on a user machine.
>
> Rename the existing job so it no longer says "installer venv" while a sibling audits four sources between them, or leave it and name the new one unambiguously.
>
> Keep the runtime audits **reporting-only**, distinct from the existing installer audit which already runs `--strict`. The installer venv is a controlled set; the ML runtimes are not.
>
> Record the finding count per requirement source.
>
> Acceptance: all three runtime sources produce a finding count; the existing installer audit still gates as today; the runtime audits do not.

- [ ] T008 Add a sibling pip-audit job for the runtime requirement sets .github/workflows/ci.yml

---

#### 2.3 -- Secret scanning, non-gating, with a bounded allowlist

**Objective**: Add gitleaks and size the historical backlog without committing to clear it.

**Failure modes**: A first full-history scan commonly flags historical fixtures and example values, so the job starts non-gating. An allowlist entry covering a real credential is a security failure disguised as configuration -- every entry carries a justification. If history cannot be read (shallow clone), it fails loudly rather than reporting clean on truncated history.

**Build class**: load-bearing.

**Prompt**:
> Add a `gitleaks` job in a new `.github/workflows/secrets.yml`. (The first draft left the location undecided while a task line asserted it; it is decided here. If reading the existing workflow structure makes `ci.yml` clearly better, move it and update T009's path.)
>
> Two modes: full-history scan on push to the integration branch, PR-diff scan on pull requests. Note that unlike `codeql.yml` this is new, so choose trigger branches that include `develop` -- do not inherit the main-only pattern that blocks 2.1.
>
> Pin the action by SHA per repository convention.
>
> Run it **non-gating** and record the first full-history finding count.
>
> **Bound the allowlist work.** Cluster the findings, then author `.gitleaks.toml` entries for the **top 10 clusters by count**, each with a one-line justification saying why the matched value is not a live secret. Anything beyond that is recorded in 5.2 as a known gap with the residual count as its exit condition. The Non-Goals table excludes fixing what gitleaks finds; hand-justifying an unbounded backlog would reintroduce exactly that work.
>
> If the scan finds a value that **is** a live secret, stop and escalate before anything else. Never commit a `.gitleaks.toml` that allowlists a real credential.
>
> Acceptance: the job runs on both triggers; the first count is recorded; every written allowlist entry is justified; the residual is a recorded gap; no merge is blocked yet.

- [ ] T009 Add a non-gating gitleaks job .github/workflows/secrets.yml
- [ ] T010 Author the bounded, justified gitleaks allowlist .gitleaks.toml

---

#### 2.4 -- Installer smoke fires when installers change

**Objective**: Trade a calendar trigger for a change trigger, with the cost stated honestly.

**Failure modes**: A `paths` filter narrower than the real surface means an installer broken by a change outside those paths gets no run. Too broad, and an expensive job fires on unrelated commits. If a required-check name changes as a side effect, branch protection silently stops enforcing it.

**Build class**: load-bearing.

**Prompt**:
> `.github/workflows/installer-smoke.yml` triggers on `workflow_dispatch` plus a monthly cron. An installer edit can merge with no smoke run for up to 30 days.
>
> Add a `paths` filter so it fires when installers change. Set the paths wide enough to catch the real surface: installer entrypoints, `scripts/installer/src/**`, the installer's dependency manifests, and the Tauri bundle configuration. A filter covering only entrypoint scripts reproduces the gap in a new form.
>
> **State the CI-minute delta honestly, and do not claim this is cheaper.** The workflow runs four jobs at a 30-minute timeout across Windows, macOS, and Linux, with `choco install vscode` / `brew install --cask visual-studio-code` / apt setup, and macOS runners bill at 10x. Its header records that the monthly throttle exists specifically to consume no minutes under an Actions budget freeze. Firing on every PR touching `scripts/installer/src/**` -- in a repo currently carrying an uncommitted installer rework -- is a net increase.
>
> Given that, **prefer `push` to the integration branch plus `workflow_dispatch` over `pull_request`** unless the operator accepts the PR-trigger cost. Say which you chose and what it costs.
>
> Note also that `preflight-reachability` (lines 99-114) makes live Hugging Face and Ollama registry calls; putting it on a PR trigger imports network flakiness into PR feedback. Consider gating that step to scheduled or dispatch runs only.
>
> Confirm the check name is unchanged, or update branch protection deliberately.
>
> This sub-task changes only the workflow file, never installer sources -- so it does not collide with the 1.3 guard.
>
> Acceptance: an installer-path change triggers the job on the chosen trigger; an unrelated change does not; `workflow_dispatch` still works; the minute delta is recorded.

- [ ] T011 Trigger installer smoke on installer paths with a recorded cost .github/workflows/installer-smoke.yml

---

#### 2.5 -- decision: which of these gate in this release? (D2)

**Objective**: Resolve, with counts in hand, which new checks become required.

**Prompt**:
> A decision sub-task, not an implementation slice. Do not start it until 2.1-2.4 have produced counts.
>
> **The question**: which of CodeQL-Python, the runtime `pip-audit` sources, and gitleaks become required checks, and which stay reporting-only?
>
> **The evidence**: CodeQL Python finding count by severity and the run-duration delta; per-source runtime advisory counts and how many have an available fix; the gitleaks count after the bounded allowlist, plus the residual.
>
> **Options.** (1) Gate all three -- correct only if all backlogs are near zero. (2) Gate gitleaks only. (3) Gate nothing this release; record each with an exit condition.
>
> **Recommendation: option 2**, unless the post-allowlist gitleaks count is non-zero. Secret leakage is unrecoverable once pushed, while an unfixable transitive ML advisory blocking every merge trains the team to bypass the check.
>
> Note the dependency on 2.1: if CodeQL still triggers only on `main`, its count comes from a local CLI run, and gating a check whose CI trigger excludes the integration branch is incoherent. Resolve that before gating it.
>
> Whatever is not gated becomes a 5.2 known gap with a named exit condition.
>
> Record the decision, the counts, and the reasoning in `docs/v2/v2.4/development/v2.4.9-security-coverage-evidence.md`.

- [ ] T012 Decide and record which new security checks gate docs/v2/v2.4/development/v2.4.9-security-coverage-evidence.md

---

#### 2.6 -- Testing and Stabilization

**Objective**: Verify Phase 2 locally and commit it.

**Prompt**:
> Exercise the **locally verifiable half** of the Phase 2 Verification Expectation through [[functional-verification]] and record each observation verbatim in `v2.4.9-security-coverage-evidence.md`: local CodeQL CLI and gitleaks counts, the scanned-file count against 304, workflow-file parse validation, and the per-language check name.
>
> Record the **deferred half** explicitly as deferred to 5.10 -- Security-tab observation, runtime `pip-audit` job log, and the installer-smoke trigger behavior. Do not claim them here. (T012 and T013 both append sections to the same evidence file; that is intended.)
>
> Record CI IMPACT against [[cicd-architect]]: this phase legitimately authors pipeline files. Name the new jobs, triggers, gating status after D2, and any renamed check. The terminal reconciliation is 5.5.
>
> Run `/generate-session-history` for Phase 2, then ONE local commit staging only: `.github/workflows/codeql.yml`, `.github/workflows/ci.yml`, `.github/workflows/installer-smoke.yml`, `.github/workflows/secrets.yml`, `.gitleaks.toml`, the evidence file, and the session-history file. Name every path.
>
> Do NOT push and do NOT open a pull request.

- [ ] T013 Verify locally and commit Phase 2 docs/v2/v2.4/development/v2.4.9-security-coverage-evidence.md

---

### Phase 2 Exit Checklist

- [ ] All sub-tasks completed
- [ ] CodeQL Python extraction confirmed non-zero and plausible against 304 tracked files
- [ ] CodeQL job name templated per language; branch-protection impact of the rename noted
- [ ] CodeQL trigger-branch problem resolved or counts sourced locally, and which is recorded
- [ ] Runtime audit implemented as a sibling job with a stated resolution strategy
- [ ] gitleaks allowlist bounded to the top 10 clusters, every entry justified, residual recorded
- [ ] No live secret allowlisted
- [ ] Installer-smoke trigger chosen with its CI-minute delta recorded; no "costs less" claim
- [ ] D2 recorded with the counts it rested on
- [ ] Locally verifiable expectations exercised; deferred ones recorded as deferred
- [ ] CI impact recorded; terminal reconciliation deferred to Phase 5
- [ ] Session history generated
- [ ] One local commit, named paths only
- [ ] No PR opened
- [ ] Ready to advance to Phase 3

---

## Phase 3: The inventory becomes true, then enforced

**Goal**: Make `feature_list.json` describe the product that actually ships, then make CI fail when it drifts again.
**Prerequisites**: Phase 1. **Sequence its `ci.yml` edit after Phase 2's** (shared file, disjoint anchors).
**Stability Gate**: `feature_list.json` describes the shipped surface. `scripts/check-feature-drift.mjs` fails on induced drift and passes on the true tree, and gates in `ci.yml`.
**Verification Expectation**: *(local for the checker; the rolling-issue behavior is deferred.)*
> *Locally verifiable:* delete one feature name from `README.md` (uncommitted), run `node scripts/check-feature-drift.mjs`, observe a non-zero exit naming that feature; restore and observe zero. Corrupt `feature_list.json` and observe it fail rather than pass.
> *Deferred to 5.10:* the rolling-issue behavior. A scheduled workflow that opens and updates GitHub issues cannot run locally, and this phase may not push. Record as deferred.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Requires reading the real shipped surface to rebuild the inventory honestly, plus a checker whose failure mode -- passing on a tree it did not really inspect -- is the failure mode that made the current file useless.

### Sub-tasks

#### 3.1 -- decision: what does the inventory assert, and against which README region? (D3)

**Objective**: Fix the contract and the parse target before rewriting contents.

**Prompt**:
> A decision sub-task. Resolve before 3.2.
>
> **Question A -- the schema.** The existing file carries `id`, `name`, `description`, `status`, `evidence`, `testedAt`, and `verificationCommand`. Which survive into the enforced inventory?
>
> A checker that shells out to every `verificationCommand` is a full test run wearing an inventory's clothes -- slow, and duplicating `ci.yml`. A checker that verifies `evidence` paths exist and `name` appears in `README.md` is fast enough to actually run on every PR, but cannot detect a feature that exists as a file and is broken.
>
> Options: (1) keep the schema, enforce paths and names only, `verificationCommand` becomes documentation; (2) drop `verificationCommand` and `testedAt` for a pure existence-and-naming inventory; (3) two tiers -- PR job checks paths and names, a nightly job runs the commands.
>
> **Recommendation: option 1**, provided the file says plainly which fields CI enforces and which are advisory. Option 3 is right later if the advisory fields go stale; that condition is a 5.2 known gap.
>
> **Do not put the rationale in a JSON comment.** `feature_list.json` is strict JSON with no comment syntax, and a comment would break the very checker 3.3 builds. Use a top-level `_comment` string field, or an adjacent `docs/reference/feature-inventory.md`, and name which you chose.
>
> **Question B -- the README region.** `README.md` has no single features region. It carries `## The Four Pillars` (line 30) with four `###` children, then seven standalone `###` sections (Document parsing (OCR) line 48, Local API server line 52, Ask inbox line 58, MLX line 62, llama.cpp line 66, Always-on telemetry line 70), then `### CLI tools (already shipped)` at line 340 -- interleaved with a ~180-line "What's new in vX" changelog (lines 122-306) whose prose contains feature-like names that will produce **false passes** if matched.
>
> Name the exact heading boundaries the checker parses, and explicitly exclude the changelog range. This is a design decision with real ambiguity, not a detail of 3.3.
>
> **Question C -- the version field.** Do not make version matching a gating assertion. `package.json:5` reads `2.4.1` while the release line is at v2.4.8, and `semantic-release` bumps the version on `main` without touching `feature_list.json` -- so a gating version check breaks `main` on the first release after it lands, unfixably from within the release commit. Either drop the assertion from the gating check, have the release flow rewrite the field, or derive it from `package.json` at read time. Choose and record.
>
> **What it unblocks**: 3.2 and 3.3. Do not start either until this is recorded.

- [ ] T014 Decide and record the inventory contract, README region, and version handling docs/reference/feature-inventory.md

---

#### 3.2 -- Rebuild the inventory against the shipped product

**Objective**: Replace a v0.8.0 description with a true one.

**Failure modes**: A feature in the tree with no `README.md` line must be recorded as a finding, never fixed by inventing a README line to satisfy the checker -- that makes the checker pass while the documentation gap persists. An ambiguous `evidence` path resolves to the single file that would be deleted if the feature were removed, and says so. A listed feature that no longer exists is removed and recorded, not marked `status: unknown`.

**Build class**: load-bearing.

**Prompt**:
> `feature_list.json` reads `"version": "v0.8.0"` with `testedAt: 2026-05-06` and describes an architecture that no longer exists (4-layer memory subsystem, 6-stage compaction pipeline). No CI job references it.
>
> Rebuild it against the shipped product, applying the 3.1 decisions.
>
> **Bound the scope.** Enumerate features at exactly the level the README region chosen in 3.1 Question B already names them -- one inventory entry per named feature in that region, no finer. Do not decompose a pillar into its sub-capabilities. Cap the first inventory at that set and record deeper coverage as a 5.2 known gap. Without this bound the sub-task has no exit criterion.
>
> Ground every entry in the tree, not the changelog. For each: a name appearing verbatim in the chosen README region, a one-line description, and an `evidence` path that exists.
>
> This is a replacement, not a repair; nearly every entry changes. That is the declared exception in Complexity Tracking.
>
> Where a feature exists but the README does not name it, record it as a finding for 5.2 rather than editing `README.md`. Note `README.md` is currently modified in the working tree; read its committed state and coordinate rather than fighting an in-flight edit.
>
> Acceptance: every `evidence` path resolves; every `name` appears in the chosen region or is a recorded gap; the entry count matches the region's named features.

- [ ] T015 Rebuild the feature inventory against the shipped surface feature_list.json

---

#### 3.3 -- The drift checker, and its CI gate

**Objective**: Make drift fail a build instead of accumulating silently.

**Failure modes**: A malformed or missing inventory exits non-zero naming the parse failure -- never treated as an empty inventory, which would trivially pass and is exactly how a checker becomes decorative. Same for an unreadable README. A feature name matching unrelated prose is a false pass, which is why 3.1 Question B pins the parsed region and excludes the changelog.

**Build class**: load-bearing.

**Prompt**:
> Write `scripts/check-feature-drift.mjs`, following the conventions of `check-docs-layout.mjs`, `check-architecture.*`, and `check-no-devai-hub.mjs` -- read them first.
>
> It asserts, per 3.1: every inventory `name` appears verbatim in the README region chosen in Question B (changelog range excluded); every `evidence` path exists; and the version handling chosen in Question C.
>
> Fail closed on malformed or missing inputs. An unreadable input must never produce a pass.
>
> Support `--output <file>` for a Markdown report, consumed by 3.4.
>
> Wire it into `.github/workflows/ci.yml` as a gating check. Unlike the Phase 2 jobs this gates immediately: its backlog is what 3.2 just made zero. **Sequence this edit after Phase 2's `ci.yml` change.**
>
> Acceptance: removing a feature name from the README exits non-zero naming it; restoring exits zero; a malformed inventory exits non-zero; the CI job blocks a drifting PR.

- [ ] T016 Write the feature-drift checker scripts/check-feature-drift.mjs
- [ ] T017 Gate the drift checker in CI .github/workflows/ci.yml

---

#### 3.4 -- One rolling drift issue, not a daily pile

**Objective**: Surface slow drift daily without issue spam.

**Failure modes**: Opening a new issue per run produces 30 a month and everyone mutes the label -- update-in-place is the point, not a nicety. A failed issue-search step must not fall back to creating a new issue; it fails and reports. When drift clears it closes the issue; if it cannot, it says so rather than exiting green.

**Build class**: load-bearing.

**Prompt**:
> Add `.github/workflows/docs-drift.yml`: a daily scheduled job plus `workflow_dispatch` that runs `scripts/check-feature-drift.mjs --output` and maintains exactly **one** rolling issue labeled `docs-drift`, updated in place, created if absent, closed automatically when clean.
>
> Use `actions/github-script` against this repository's own issues with the built-in token. No third-party service. Pin by SHA. Grant `issues: write` and nothing else.
>
> Do not fall back to creating a new issue when the search step fails.
>
> **This is the cuttable slice of Phase 3.** If the phase runs long, 3.1-3.3 alone are complete: drift already fails CI on every PR, and this job only adds detection for drift arriving without a PR.
>
> Acceptance (deferred to 5.10 for live verification): two consecutive runs against a drifted tree produce one issue, updated in place; a clean run closes it.

- [ ] T018 Add the daily rolling docs-drift issue job .github/workflows/docs-drift.yml

---

#### 3.5 -- Testing and Stabilization

**Objective**: Verify Phase 3 and commit it.

**Prompt**:
> Add unit tests for `check-feature-drift.mjs` at `tests/unit/scripts/check-feature-drift.test.ts` -- **`.test.ts`, not `.test.mjs`**. `configs/vitest.config.ts` includes only `tests/unit/**/*.test.ts` and `tests/integration/**/*.test.ts`, so a `.mjs` test is silently never collected: green CI, zero tests run, which is precisely the decorative-checker failure this phase exists to prevent. The repo convention is already `.test.ts` files testing `.mjs` scripts.
>
> Cover: clean tree passes; a missing README name fails naming it; a missing evidence path fails naming it; a malformed inventory fails rather than passes; changelog prose containing a feature-like name does **not** produce a false pass.
>
> Exercise the locally verifiable Verification Expectation through [[functional-verification]] and record it. Record the rolling-issue behavior as deferred to 5.10.
>
> Record CI IMPACT against [[cicd-architect]]: one new gating check, one scheduled workflow, one script path, one new test path, one label.
>
> Run `/generate-session-history` for Phase 3, then ONE local commit staging only: `feature_list.json`, `docs/reference/feature-inventory.md` (if 3.1 chose it), `scripts/check-feature-drift.mjs`, `tests/unit/scripts/check-feature-drift.test.ts`, `.github/workflows/ci.yml`, `.github/workflows/docs-drift.yml`, the session-history file, and `README.md` **only if** 3.2 recorded an agreed documentation gap requiring it. Name every path.
>
> Do NOT push, do NOT open a pull request.

- [ ] T019 Test, verify, and commit Phase 3 tests/unit/scripts/check-feature-drift.test.ts

---

### Phase 3 Exit Checklist

- [ ] All sub-tasks completed (3.4 may be recorded as cut)
- [ ] D3 recorded before the rebuild began, including the README region and version handling
- [ ] Inventory scope bounded to the chosen region's named features
- [ ] Every evidence path resolves
- [ ] Checker fails closed on malformed inputs and does not false-pass on changelog prose
- [ ] Tests are `.test.ts` and confirmed collected by vitest
- [ ] Checker gates in CI, sequenced after Phase 2's `ci.yml` edit
- [ ] Locally verifiable expectations exercised; rolling-issue behavior recorded as deferred
- [ ] CI impact recorded
- [ ] Session history generated
- [ ] One local commit, named paths only
- [ ] No PR opened
- [ ] Ready to advance to Phase 4

---

## Phase 4: Field feedback has a shape

**Goal**: Make every bug report carry the build it came from, and ship a crash-class report that is correct by construction and ready for data.
**Prerequisites**: Phase 1. Independent of Phases 2 and 3; may run in parallel with either (no shared files).
**Stability Gate**: `.github/ISSUE_TEMPLATE/` exists with a bug form requiring app version, OS, GPU, and install source. `scripts/crash-class-report.mjs` bucketizes correctly against fixtures and reports its unknown-version count truthfully.
**Verification Expectation**: *(fixture-based locally; live behavior deferred.)*
> *Locally verifiable:* run the report against committed fixture issue bodies and observe correct per-version bucketing and a truthful unknown count; run it against the live repository and observe it complete, with essentially everything in the unknown bucket (see the honesty note).
> *Deferred to 5.10:* the rendered New-issue form. A GitHub issue form only renders once merged to the default branch, and this phase may not push. Validate the YAML parses locally and record the render as deferred.
**Recommended model tier**: standard
**Recommended effort level**: medium
**Rationale**: Static YAML plus a read-only reporting script against a documented CLI; the judgement is in the bucketing rule, not the mechanics.

**Honesty note.** Every issue that exists today predates the form, so the first live run puts essentially everything in the unknown bucket. That is the correct output, not a defect. The value -- a per-version failure trend -- arrives weeks later. 5.2 records "first non-empty crash-class report" as a known gap with a date-based exit condition.

### Sub-tasks

#### 4.1 -- Issue templates that capture the build

**Objective**: Stop accepting bug reports that cannot be attributed to a build.

**Failure modes**: An optional version field means most reports omit it and 4.2 has nothing to bucket -- it must be `required: true`. A form long enough to be abandoned lowers the report rate and makes the data worse; keep it to fields 4.2 consumes. Reports filed through channels bypassing the template (direct API, converted discussion) are counted as version-unknown, never dropped.

**Build class**: load-bearing.

**Prompt**:
> Create `.github/ISSUE_TEMPLATE/` -- it does not exist. (`.github/` currently holds only `CODEOWNERS`, `dependabot.yml`, `PULL_REQUEST_TEMPLATE.md`, and `workflows/`.)
>
> Add `bug_report.yml` as a GitHub issue **form**, not a Markdown template, so fields can be genuinely required. Required: app version, OS and version, GPU or "CPU only", install source (installer, VSIX, source build). Then free text: what happened, what was expected, steps to reproduce.
>
> Keep it short. Every field must be one a triager or `crash-class-report.mjs` consumes.
>
> Add `config.yml` routing non-bug traffic elsewhere.
>
> **Do not add a feature-request template.** The first draft left it to "only if genuinely useful", which is not a criterion; `config.yml` already routes that traffic.
>
> Where the app can prefill the version, prefer a prefilled-URL approach from the user's own browser. This repository must not gain a token-based GitHub client. Record as a follow-up in 5.2; do not implement here.
>
> Acceptance: the form YAML parses; the version field is `required: true`; its answer is machine-readable in the issue body in a shape 4.2 parses.

- [ ] T020 Add the bug-report issue form requiring build version .github/ISSUE_TEMPLATE/bug_report.yml
- [ ] T021 [P] Add the issue-template chooser config .github/ISSUE_TEMPLATE/config.yml

---

#### 4.2 -- The crash-class recurrence report

**Objective**: Make the top failure class measurable per build, correct by construction now and useful once data arrives.

**Failure modes**: Counting unversioned reports as current-build overstates recurrence and makes the release gate read as failing when it is not -- they get their own bucket, reported as a count, never merged into a version. An unauthenticated or rate-limited `gh` fails naming which, rather than returning an empty result that reads as "no crashes". An over-broad classifier puts every issue in the top class; per-class match counts are printed so that is visible.

**Build class**: load-bearing.

**Prompt**:
> Write `scripts/crash-class-report.mjs`, reading this repository's issues through the authenticated `gh` CLI -- read-only, no posting, no third-party service. This uses the GitHub token an operator already holds; it is not a new credential.
>
> **Bound the classification work.** Sample the **50 most recent issues**, derive **at most 5 classes**, record the sample and the classes, and treat refinement as a later pass. Do not assume Nexus-AI's top class matches VoiceStudio's ("the backend died or never came up"); it may be installer failure, model-runtime cold start, or GPU handoff. The first draft left this sampling unbounded on the critical path.
>
> Bucket by the build version in the issue body, which 4.1 makes present and machine-readable. Unversioned reports go in an explicit unknown bucket, reported as a count -- never folded into a version, never assumed current. A report from an obsolete build must not count against a build that already fixed it.
>
> Support `--since <date>` and `--version <v>`. Default to since-latest-release.
>
> Print per-class match counts so an over-broad classifier is visible.
>
> Fail loudly naming the cause if `gh` is unauthenticated or rate-limited.
>
> **Acceptance is fixture-based**, per the honesty note: against committed fixture bodies it bucketizes correctly and reports the unknown count truthfully; against the live repository it completes and reports an honest (near-total) unknown count. Do not assert a non-empty version distribution.

- [ ] T022 Write the crash-class recurrence report scripts/crash-class-report.mjs

---

#### 4.3 -- Testing and Stabilization

**Objective**: Verify Phase 4 and commit it.

**Prompt**:
> Add unit tests at `tests/unit/scripts/crash-class-report.test.ts` -- **`.test.ts`, not `.test.mjs`** (see the Phase 3 note; `.mjs` is never collected). Cover against fixture issue bodies: a versioned report buckets to its version; an unversioned one lands in the unknown bucket and is never counted as current; a malformed body does not crash the run; per-class counts are emitted.
>
> Exercise the locally verifiable Verification Expectation through [[functional-verification]] and record it, including the live-run unknown count. Record the rendered New-issue form as deferred to 5.10, and validate the form YAML parses locally.
>
> Record CI IMPACT against [[cicd-architect]]: a new script path and test path; issue templates are not CI surface.
>
> Run `/generate-session-history` for Phase 4, then ONE local commit staging only: `.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/config.yml`, `scripts/crash-class-report.mjs`, `tests/unit/scripts/crash-class-report.test.ts`, its fixtures, and the session-history file. Name every path.
>
> Do NOT push, do NOT open a pull request.

- [ ] T023 Test, verify, and commit Phase 4 tests/unit/scripts/crash-class-report.test.ts

---

### Phase 4 Exit Checklist

- [ ] All sub-tasks completed
- [ ] Version field genuinely required by the form; form YAML parses
- [ ] Classification bounded to 50 issues and at most 5 classes, with the sample recorded
- [ ] Unversioned reports bucketed separately, never counted as current
- [ ] Acceptance asserted against fixtures, not against a non-empty live distribution
- [ ] Tests are `.test.ts` and confirmed collected
- [ ] Locally verifiable expectations exercised; form render recorded as deferred
- [ ] CI impact recorded
- [ ] Session history generated
- [ ] One local commit, named paths only
- [ ] No PR opened
- [ ] Ready to advance to Phase 5 (the terminal wrap-up phase)

---

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| Sub-task 3.2 rewrites nearly all of `feature_list.json` rather than making a traceable minimal edit | The file describes a v0.8.0 architecture (4-layer memory subsystem, 6-stage compaction pipeline) that no longer exists in a product shipping 2.4.x. There is no minimal edit that makes it true. | Deleting the file discards a schema and a maintainer's intent that Phase 3 can make useful again, and leaves the project with no inventory at all rather than a stale one. Leaving it stale was rejected because an unenforced inventory is what created the gap. |

---

## Phase 5: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, gaps reconciled, living docs current, the artifact set exercised through the Tier 3 deep pass, and the pipeline reconciled, with independent evidence the Goal landed, then publish and integrate.
**Prerequisites**: Phases 1-4, whichever were run (slice-aware -- see Definition of Done). The former Phase 5 is out of scope under D1.
**Stability Gate**: `docs/v2/v2.4/development/v2.4.9-last-phase-evidence.md` exists with one quoted section per duty, including `## Tier 3 deep pass`. The 1.3 scaffolding guard is removed. A missing Goal clause owned by a landed phase, or a missing evidence section, blocks `/update release`.
**Verification Expectation**: Run [[functional-verification]]'s Tier 3 procedure against every artifact the plan produced and observe either a clean terminal result or a bounded one whose remaining findings are recorded as owned known gaps; quote it under `## Tier 3 deep pass`. Additionally, execute every observation deferred from Phases 2, 3, and 4 once the integration PR exists, and record each.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide scan, Tier 3 exercise, Goal review, gap reconciliation, and release gating carry high context volume and blast radius.

> **Evidence filename.** This plan writes `v2.4.9-last-phase-evidence.md`, **not** the unprefixed `last-phase-evidence.md`. The v2.4.10 plan sits in the same version directory and writes its own last-phase evidence; an unprefixed name collides.

### Sub-tasks

#### 5.1 - Architecture refactor (report-only)
**Objective**: Report layout findings without moving anything.
**Prompt**:
> Run the [[project-refactor]] and [[docs-layout-refactor]] detectors for deprecated or obsolete files, empty directories, redundant files and dirs, and overcomplicated structure. Write the output into `docs/v2/v2.4/development/v2.4.9-last-phase-evidence.md` under `## Architecture refactor`. An empty finding is allowed only when the scan output is quoted.
>
> **Report-only.** A repo-wide move-and-repair has no exit criterion and is the highest-risk action available on a tree that may still carry an uncommitted installer rework. Record findings as known gaps via [[known-gaps-tracker]] with an owner and next step. Do not move or delete anything.
>
> Two findings are known in advance and must appear: the root-level `.vsix` build artifacts and stray logs; and `core/storage/StorageMigration.ts`, whose header says "Removed in v1.1.0" while the file and its tests are still present.

- [ ] T032 Run and record the architecture-refactor detectors docs/v2/v2.4/development/v2.4.9-last-phase-evidence.md

#### 5.2 - Known-gaps reconciliation
**Objective**: Reconcile this version's gaps and the ones this plan created.
**Prompt**:
> Via [[known-gaps-tracker]], reconcile `docs/v2/v2.4/known-gaps.md`: carried-forward v2.4.8 open `MT` and `DF` rows, plus every gap this plan created. Record at minimum:
>
> - whichever security checks D2 left non-gating, each with its exit condition (triaged backlog reaching zero);
> - the CodeQL Python and runtime `pip-audit` finding backlogs, with counts;
> - the gitleaks allowlist residual beyond the top 10 clusters, with its count;
> - A11 (signed updater), deferred -- and **D1 resolved to option 1, so state that A11 is now deferred twice over**, since it was already gated on A5 and A5 has itself moved to the v2.5.0 plan;
> - the option-3 two-tier inventory enforcement from D3, if D3 chose option 1;
> - deeper feature-inventory coverage beyond the chosen README region;
> - any `README.md` documentation gap 3.2 surfaced;
> - "first non-empty crash-class report", with a date-based exit condition;
> - the in-app prefilled-URL bug-reporting follow-up from 4.1;
> - the three unnumbered comparison candidates held in Non-Goals: `LICENSE-NOTICE.md`, the `taze --maturity-period` window, and wiring `desktop-prod:upgrade` into CI;
> - every finding from 5.1.
>
> Each gap names an exit condition this project can actually evaluate. "Revisit later" with no mechanism is not acceptable.
>
> **Bounded** to this version's file plus the gaps named. If you glob and find other `known-gaps.md` files, list them and leave them.
>
> Write the disposition under `## Known-gaps reconciliation`.

- [ ] T033 Reconcile known gaps for this version docs/v2/v2.4/known-gaps.md

#### 5.3 - Living docs architecture
**Objective**: Prove handbooks, decisions, and generated HTML match the required living tree.
**Prompt**:
> Check `docs/handbooks/` (markdown source of truth, generated `html/`, one atlas walkthrough, technical companions), `docs/decisions/`, and living `docs/README.md` / `docs/DEVLOG.md` / `docs/todos.md`. Self-gated: never invent `docs/testing/` or `docs/validation/`. Markdown wins if HTML disagrees; regenerate or fail.
>
> Confirm `docs/reference/model-acceptance.md` and, if 3.1 created it, `docs/reference/feature-inventory.md` are reachable from the living docs entry points rather than orphaned.
>
> Quote under `## Living docs architecture`.

- [ ] T034 Verify the living docs architecture docs/v2/v2.4/development/v2.4.9-last-phase-evidence.md

#### 5.4 - Git-tree hygiene and scaffolding removal
**Objective**: Report branch hygiene and remove this plan's scaffolding.
**Prompt**:
> Run `python scripts/check_release_preconditions.py --branches --repo-settings`. Quote under `## Git-tree hygiene`. Report only; never delete branches.
>
> **Remove the 1.3 staging guard** from `.husky/pre-commit`, leaving `npx lint-staged` intact. It was declared scaffolding and must not outlive this plan. (If 1.3 was cut, record that instead.)
>
> State what the tree looks like afterward. If the installer wizard rework is still uncommitted, say so explicitly -- publishing with it uncommitted is a decision the operator must make knowingly rather than discover.

- [ ] T035 Report git hygiene and remove the staging-guard scaffolding .husky/pre-commit

#### 5.5 - Terminal CI/CD reconciliation (scoped)
**Objective**: Confirm this plan's checks are covered; record other differences without applying them.
**Prompt**:
> Invoke [[cicd-architect]] in existing-pipeline comparison mode. DETECT the active provider, recording "none detected" rather than assuming.
>
> **Confirm exactly these**, for phases that landed: CodeQL Python present and extracting with a per-language check name; a sibling job auditing the three runtime requirement sets; gitleaks running on both triggers; `installer-smoke` on paths, not cron; `check-feature-drift.mjs` gating; `docs-drift.yml` scheduled; and every new test path (`tests/unit/scripts/`, `tests/unit/core/storage/`) actually run by an existing required check **and using `.test.ts` so vitest collects it**.
>
> Confirm each new check's gating status matches D2 exactly. A check that silently became required, or silently did not, is a finding. Confirm the CodeQL job rename did not silently drop the old required check from branch protection.
>
> **Bounded.** The Non-Goals table excludes *applying* unreconciled canonical-contract fields. Cap this sub-task at the confirmations above plus a recorded list of remaining differences; do not perform an exhaustive fourteen-field comparison across 19 workflows. If a difference is genuinely dangerous, PROPOSE it with its cost and obtain explicit approval; silence is not approval.
>
> Quote under `## CI/CD coverage`.

- [ ] T036 Reconcile the pipeline against this plan's checks docs/v2/v2.4/development/v2.4.9-last-phase-evidence.md

#### 5.6 - Tier 3 deep pass
**Objective**: Exercise every artifact the plan produced and record the bounded terminal result.
**Prompt**:
> Invoke [[functional-verification]] and follow its Tier 3 runbook at `references/deep-pass.md`. Size with its blast-radius decision: the artifact set is two documents, one agent definition, five workflow edits or additions, one rebuilt inventory, two scripts with tests, and two issue templates. The storage change that dominated this set left the plan under D1.
>
> **Weight by blast radius rather than treating artifacts equally.** With the storage change gone, every remaining artifact is additive and already carries phase-level verification, so the highest radius now sits in the two things that can **fail CI**: `check-feature-drift.mjs` and whichever security checks D2 made gating. Give those the full procedure and take the reduced path for the documents, templates, and reporting-only workflows. Say which shape you took.
>
> Quote under `## Tier 3 deep pass`. Findings left when the budget ends go to [[known-gaps-tracker]] with an owner and next step.

- [ ] T037 Run the Tier 3 deep pass weighted by blast radius docs/v2/v2.4/development/v2.4.9-last-phase-evidence.md

#### 5.7 - Independent Goal-vs-codebase review
**Objective**: Prove the Goal landed, not that checkboxes were ticked.
**Prompt**:
> Re-read the header Goal and `## Goals First and Definition of Done`. Inspect the codebase as if you had not implemented the phases. Write `## Goal-vs-codebase review` listing the Goal restated, the artifacts satisfying it, and any gap.
>
> **Verify only the clauses whose owning phase landed** (ownership: 1 and 2 -> Phase 4; 3 -> Phase 3; 4 -> Phase 2). **Clause 5 has no owning phase in this plan** -- D1 moved it to the v2.5.0 migration-durability plan, so record it as deferred scope and do not verify it here. A clause whose phase was cut is **deferred scope, not a miss** -- record it as such. This is what makes the shippable-slice claim honest.
>
> For landed clauses, check artifacts rather than intent:
>
> 1. A bug cannot be filed without a build version -- check the form, not the intention.
> 2. The crash-class report runs, bucketizes correctly against fixtures, and reports its unknown count truthfully. Do **not** require a non-empty version distribution; see the Phase 4 honesty note.
> 3. `check-feature-drift.mjs` gates in CI and fails on a real induced drift.
> 4. CodeQL reports Python with a non-zero scanned-file count **plausible against 304 tracked files**. A count in the thousands means it scanned a vendored virtualenv and is a miss, not a pass.
> 5. *(Not applicable -- deferred to the v2.5.0 plan by D1. Record as deferred scope; do not look for migration-snapshot artifacts in this tree.)*
>
> A clause satisfied only in intent, or by a check passing because it scanned nothing, is a miss. Every miss is a blocking finding or a new known gap.

- [ ] T038 Run the independent Goal-vs-codebase review docs/v2/v2.4/development/v2.4.9-last-phase-evidence.md

#### 5.8 - Human/manual testing suggestions (last phase only)
**Objective**: Ask a human to exercise only what automated tests cannot.
**Prompt**:
> Write suggestions under `## Human testing suggestions`. At minimum:
>
> - File a real test issue through the new bug form; confirm the version field cannot be skipped and lands machine-readable.
> - Open the Security tab and confirm Python results are present and plausible against 304 files.
>
> Do not invent a fake walkthrough. Last phase only.

- [ ] T039 Write the human testing suggestions docs/v2/v2.4/development/v2.4.9-last-phase-evidence.md

#### 5.9 - Testing and Stabilization
**Objective**: Prove the complete LOCAL gate is green before anything is published.
**Prompt**:
> Run the full local validation and test suite, iterating until clean against the known `better-sqlite3` Electron-ABI baseline, stating which failures are baseline and which are new. Confirm the 1.3 staging guard is removed and `npx lint-staged` still runs. Confirm every test file this plan added is `.test.ts` and actually collected -- run the suite and check the collected-file list, do not assume.
>
> This gate is local by construction and cannot depend on a remote run.
>
> Quote under `## Full-suite testing and stabilization`. Generate a session-history entry.

- [ ] T040 Run the full local gate and confirm test collection docs/v2/v2.4/development/v2.4.9-last-phase-evidence.md

#### 5.10 - Publication, deferred verification, and integration
**Objective**: Publish once, execute every deferred observation, validate the merge result, and hand a green integration to the release flow.
**Prompt**:
> Create the final local commit, staging named paths only.
>
> Present the resolved branching model, remote, branch name, and PR target, and obtain EXPLICIT approval before the plan's first branch push. This repository uses a develop-plus-main model: the integration target is `develop`, never `main` directly.
>
> Push once and open the integration pull request against `develop`.
>
> **Then execute every observation deferred from earlier phases**, which is why they were deferred here, and record each under `## Deferred verification`:
>
> - Phase 2: the Security tab showing Python results; the runtime `pip-audit` job log naming all three sources; `installer-smoke` firing on an installer path and not on an unrelated one.
> - Phase 3: the rolling drift issue -- two runs against a drifted tree produce one issue updated in place; a clean run closes it.
> - Phase 4: the New-issue page rendering the bug form with the version field required.
>
> Note that some of these render only after merge to the default branch; record which ones remain unverifiable until then and carry them as a gap rather than claiming them.
>
> Report the exact required checks expected, including whichever D2 made required. Wait until every required check reaches a terminal state.
>
> A red check REOPENS this phase: classify, reproduce locally, apply the narrow fix, re-run the local gate, update known gaps and session history, then amend or add one narrowly scoped stabilization commit and push again with approval. Never re-run a red check without a local reproduction.
>
> Merge only after every required check is green and the user approves.
>
> `main` auto-releases via semantic-release and a manual bump can collide with the computed version. **D1 resolved this.** With Phase 5 removed, every remaining slice is additive and should compute a patch. Confirm the computed version is `2.4.9` before merging: a computed minor here means something `feat`-shaped slipped into a phase that was supposed to be additive, and that is a finding, not a version to accept. Coordinate with that flow rather than bumping manually. Only then hand off to `/update release`.
>
> Quote the required-check results under `## Publication and integration`.

- [ ] T041 Publish, execute deferred verification, and integrate docs/v2/v2.4/development/v2.4.9-last-phase-evidence.md

---

### Phase 5 Exit Checklist

- [ ] All sub-tasks completed
- [ ] All tests passing against the known ABI baseline, new failures distinguished
- [ ] Every added test file confirmed collected by vitest, not assumed
- [ ] 1.3 scaffolding guard removed; `npx lint-staged` intact
- [ ] Uncommitted installer rework status explicitly stated before publication
- [ ] Evidence file is `v2.4.9-last-phase-evidence.md`, not the colliding unprefixed name
- [ ] One quoted section per duty, including `## Tier 3 deep pass` and `## Deferred verification`
- [ ] Goal review checked only landed clauses; cut clauses recorded as deferred scope, not misses
- [ ] CodeQL scanned-file count validated against 304, not a virtualenv-inflated number
- [ ] Known gaps reconciled with evaluable exit conditions
- [ ] Gating status of every new check matches D2; CodeQL rename did not drop a required check
- [ ] All deferred observations from Phases 2-4 executed or recorded as still-unverifiable
- [ ] Session history generated
- [ ] Branch published once, with explicit approval, against `develop`
- [ ] Integration PR green before merge
- [ ] semantic-release computes a patch (`2.4.9`); a computed minor is a finding, per D1
- [ ] Release work handed to `/update release` only after a green, merged integration

---

## Review record

**Stage 1 (autonomous critique): ran.** Three lenses reviewed the first draft and returned 36 findings.

| Lens | Findings | Severity high-water |
|---|---|---|
| Coherence | 21 | P1 (x2) |
| Feasibility | 15 | P0 (x2) |
| Scope | 12 | P1 (x3) |

The two P0s and their disposition:

- **Snapshot-on-every-launch.** Only `MemoryStore` has a migration-pending predicate; the other two stores migrate unconditionally on every construction, so retain-three would evict the real pre-migration snapshot after three launches. Folded in as new work in the then-Phase 5, and it travelled to the v2.5.0 plan with that phase (now sub-task 1.2, its Complexity Tracking row, an acceptance case in 1.4, and a Verification Expectation clause).
- **Phase 2 unverifiable before merge.** `codeql.yml` triggers only on `main` while this plan publishes to `develop`, and the phase forbids pushing. Folded in by splitting every affected Verification Expectation into a locally verifiable half and a half deferred to 5.10, and by making the trigger-branch problem explicit work in 2.1.

Four findings corrected the **seeding comparison** rather than the plan: the Python file count (3,447 -> 304), the installer-smoke "costs less" claim, a spurious `A5 -> A7` dependency edge, and the `pip-audit` "follow its shape" framing. All four are recorded in the comparison's new Section 8.5.

**Stage 2 (interactive grill): DID NOT RUN.** The plan was generated unattended, and the grill requires a human. Its frontier is not empty -- it is the three items in `## Blocking decisions`. D1 in particular (does Phase 5 belong in a patch release) was a scope decision with a recommendation attached and no owner's answer. **D1 was answered on 2026-09-10 as option 1 (split)**, which moved Phase 5 to [v2.5.0-migration-durability.md](../../v2.5/plans/v2.5.0-migration-durability.md) and renumbered the wrap-up phase to 5. D2 and D3 remain open by design and are resolved inside Phases 2 and 3 from the counts those phases produce. Treat this plan as critiqued but not grilled.
