# Plan -- Local Image Studio 3D Preview and Qwen3.8 Unsloth Watchlist

**Project**: Nexus AI Studio
**Version**: v2.4.0
**Slug**: adoption-unsloth-qwen38-gaussian-splatting
**Filename**: v2.4.0-adoption-unsloth-qwen38-gaussian-splatting.md
**Plan Type**: Feature / Enhancement (from comparison)
**Status**: Phases 1-5 complete (T001-T016). Phase 6 release gate not started.
**Created**: 2026-08-28
**Confirmed**: 2026-08-28
**Seeded from**: `docs/v2/v2.4/comparisons/v2.4.0-comparison-unsloth-qwen38-gaussian-splatting.md`
**Reverse-engineer-first**: true
**Goal**: By the end of v2.4.0, Image Studio can orbit a local Gaussian splat or PLY beside a 2D generation without any network call, optionally create that splat on supported NVIDIA hardware through a cancellable local job that never replaces the source image, and tells the user that unseen geometry is synthesized rather than recovered; Unsloth Qwen3.8-Flash-Next remains excluded from installer and Settings catalogs behind the existing admission gates plus Unsloth-specific evidence; and no 3D AI Studio, Unsloth Desktop, or generation-as-a-service path is introduced.

## Overview

This plan turns the comparison's highest-value local capability into a Nexus feature. Image Studio gains a 3D preview path: a Nexus-owned WebGL viewer for local `.splat` and `.ply` files, then an optional feed-forward generator behind a backend-neutral contract. The first generator adapter may call MIT-licensed TripoSplat weights on NVIDIA CUDA after a capability preflight. Photos never leave the machine. The original 2D file stays downloadable. Copy must say object-level 3D preview, not a photorealistic property tour.

The plan also closes the Unsloth Qwen question without expanding catalog scope. Unsloth's 72.5 GB GGUF and thinking-control docs do not flip the v2.3.0 admission gates: the RAM floor is still about 75 GB, the published file list still omits `LICENSE`, the architecture is still `qwen4exp`, and Nexus tool-calling evidence is still absent. v2.4.0 records that Unsloth delta and keeps every installer and Settings surface free of Flash-Next. Qwen `chat_template_kwargs` / `reasoning_effort` plumbing stays deferred until some Qwen thinking artifact actually clears admission.

Success is a packaged local viewer with optional generate, automated contract/queue/UI/metadata evidence, and a fail-closed catalog. Human perceptual review and NVIDIA-only generate checks wait until the mandatory final phase.

## Goals First

**Target problem**: Users can generate a private 2D image but cannot inspect it as an orbitable 3D preview inside Nexus, while cloud splat tools and oversized experimental LLMs create pressure to add uploads or unlistable models.

**Target persona**: A privacy-focused developer or creator on a consumer Windows, Linux, or macOS computer who wants honest local 3D preview from an Image Studio still, without sending photos to a host and without being told a laptop can run a 75 GB Qwen4-exp checkpoint.

**Observable definition of done**: Image Studio opens a local `.splat` or `.ply` and orbits it offline; a supported NVIDIA host can optionally generate a splat from a completed or uploaded still as a cancellable child job; the 2D original remains intact; unsupported hardware fails closed with actionable copy; Unsloth Qwen3.8-Flash-Next is absent from user-facing catalogs; and no 3D AI Studio URL, API key, or credit flow exists.

**Strategy grounding**: No `STRATEGY.md` or version-scoped strategy anchor exists. The scope is grounded in AGENTS.md local-first product identity, the MCP Registry Policy (no generation-as-a-service), and the seeding comparison. No `docs/solutions/` store exists, so no solved-problem entry changed the phase design. Open v2.2 known gaps (packaged field QA DF-36, live GPU generate DF-4, raw Tauri Node DF-38, per-phase coverage MT-4) are ingested as last-phase reconciliation work, not as this feature's scope.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution file found at docs/<version>/constitution.md - skipping check. Recommend running /constitution to establish project principles.

## Current model map

**Model map status**: fresh as of 2026-08-28; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.5-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

Map JSON validated on 2026-08-28 with `model-routing/scripts/model-map.ps1` (`valid: true`, four tiers, four providers). Cursor remains a manual picker surface for implementation-time switching.

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/about-claude/models/overview, https://platform.claude.com/docs/en/about-claude/models/choosing-a-model
- OpenAI: https://developers.openai.com/api/docs/models, https://developers.openai.com/api/docs/models/gpt-5.6-sol, https://developers.openai.com/api/docs/guides/latest-model
- Google: https://ai.google.dev/gemini-api/docs/models, https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/, https://docs.cloud.google.com/gemini-enterprise-agent-platform/models/model-versions
- Cursor: https://cursor.com/docs/models-and-pricing, https://cursor.com/help/models-and-usage/grok-4-6

## Phases at a Glance

| Phase | Title | Outcome | Recommended model tier | Recommended effort level |
|---|---|---|---|---|
| 1 | Admission, Honesty Copy, and Splat Contract | Locks Unsloth Qwen non-admission, deferred thinking kwargs, and the viewer-first generate contract before code | frontier | max |
| 2 | Safe Local Splat Viewer Core | Parses local `.splat`/`.ply` and rasterizes them in an internal WebGL2 viewer with no network | frontier | max |
| 3 | Image Studio 3D Preview Experience | Adds orbit, screenshot, download, honesty copy, and source-linked provenance in Image Studio | strong | high |
| 4 | Generate Contract, Queue, and Preflight | Runs optional splat generate as a cancellable child job with CUDA detection and preserved originals | frontier | max |
| 5 | TripoSplat Adapter, Catalog, and Evidence | Wires the optional MIT weight backend, installer/desktop parity, and measured support claims | frontier | max |
| 6 | Architecture Refactor, Known-Gaps Reconciliation, and CI/CD | Reconciles architecture, evidence, pipeline coverage, human testing, publication, and integration | frontier | max |

## Task Inventory

- [x] T001 [US3] Record the Unsloth Qwen3.8-Flash-Next catalog non-admission delta docs/v2/v2.4/development/model-admission-qwen38-unsloth.md
- [x] T002 [US1] Resolve the Gaussian splat viewer, generate, license, and platform contract docs/v2/v2.4/development/gaussian-splat-contract.md
- [x] T003 [US1] Verify Phase 1 decisions and record CI impact docs/v2/v2.4/development/history/2026-08-28_v2.4.0-phase-1-decisions.md
- [x] T004 [US1] Implement local splat/PLY decode and typed viewer requests core/image/GaussianSplat.ts
- [x] T005 [US1] Implement the internal WebGL2 splat rasterizer desktop/src/modules/image/SplatViewerCanvas.tsx
- [x] T006 [US1] Test and stabilize the viewer core tests/unit/core/image/GaussianSplat.test.ts
- [x] T007 [US1] Add Image Studio 3D preview actions and honesty copy desktop/src/modules/image/ImageStudioPage.tsx
- [x] T008 [US1] Add screenshot, download, and source-linked splat provenance core/image/WorkflowMetadata.ts
- [x] T009 [US1] Test and stabilize the Image Studio 3D preview experience desktop/tests/ImageStudioPage.test.tsx
- [x] T010 [US2] Add splat generate child-job queue, cancellation, and CUDA preflight core/generations/GenerationQueue.ts
- [x] T011 [US2] Stage splat output without replacing the source image desktop/sidecar/src/image/GaussianSplatRuntime.ts
- [x] T012 [US2] Test and stabilize generate queue and preflight desktop/tests/gaussian-splat-runtime.test.ts
- [x] T013 [US2] Implement the guarded TripoSplat local adapter desktop/sidecar/src/image/TripoSplatAdapter.ts
- [x] T014 [US2] Add the opt-in TripoSplat catalog row and installer capability checks core/registry/catalog.json
- [x] T015 [US2] Add deterministic splat benchmarks and packaging parity docs/v2/v2.4/benchmarks/gaussian-splat-baseline.md
- [x] T016 [US2] Test and stabilize adapter, catalog, and packaging gates tests/gaussian-splat-packaging.test.ts
- [ ] T017 Refactor architecture and record evidence docs/v2/v2.4/development/last-phase-evidence.md
- [ ] T018 Reconcile known gaps across active releases docs/v2/v2.4/development/last-phase-evidence.md
- [ ] T019 Verify living documentation architecture docs/v2/v2.4/development/last-phase-evidence.md
- [ ] T020 Report git-tree hygiene docs/v2/v2.4/development/last-phase-evidence.md
- [ ] T021 Reconcile terminal CI/CD coverage docs/v2/v2.4/development/last-phase-evidence.md
- [ ] T022 Perform an independent Goal-vs-codebase review docs/v2/v2.4/development/last-phase-evidence.md
- [ ] T023 Run last-phase-only human testing docs/v2/v2.4/development/last-phase-evidence.md
- [ ] T024 Run full local stabilization docs/v2/v2.4/development/last-phase-evidence.md
- [ ] T025 Publish and integrate the completed branch docs/v2/v2.4/development/last-phase-evidence.md

---

## Phase 1: Admission, Honesty Copy, and Splat Contract

**Goal**: Resolve every consequential boundary before implementation: Unsloth Qwen3.8-Flash-Next stays out of catalogs, thinking-control kwargs stay deferred, and local 3D preview has a viewer-first contract with a lawful optional generate path.
**Prerequisites**: None.
**Stability Gate**: The two decision documents exist, cite the comparison, contain no unresolved implementation-blocking question, and the splat contract names supported/unsupported platforms, local-only file rules, failure modes, original-file preservation, honesty copy, and provenance fields.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Signal scores were scope medium, structural complexity high, context volume high, risk high, and reasoning high; catalog admission, generation-as-a-service rejection, and CUDA-only honesty make uncertainty unacceptable.

### Sub-tasks

#### 1.1 -- Unsloth Qwen3.8-Flash-Next catalog non-admission delta (T001)

**Objective**: Record a fail-closed, evidence-based decision that Unsloth GGUF and thinking-control docs do not admit Flash-Next to installer or Settings.

**Prompt**:
> Read `docs/v2/v2.4/comparisons/v2.4.0-comparison-unsloth-qwen38-gaussian-splatting.md` and `docs/v2/v2.3/development/model-admission-qwen38.md`. Write `docs/v2/v2.4/development/model-admission-qwen38-unsloth.md` as a delta, not a replacement. Record Unsloth Dynamic GGUF sizes (UD-IQ1_S 72.5 GB through BF16 354 GB), the vendor RAM table (75 GB minimum, 96 GB recommended), `qwen4exp` architecture, missing in-repo `LICENSE` file despite `license_link: LICENSE`, Qwen Community License 1.0 inheritance, chat-template kwargs (`enable_thinking`, `preserve_thinking`, `reasoning_effort`), and mmproj files as unverified vision. Re-evaluate the six v2.3.0 gates against that artifact and leave each fail or unproven. Assert that v2.4.0 makes no `core/registry/catalog.json`, installer, Settings, recommended-tier, or `patient-tier` entry for Flash-Next, and does not ship Unsloth Desktop. Record Qwen `chat_template_kwargs` mapping as deferred until some Qwen thinking model clears admission. Record Qwen3.8-27B as a separate watch item, not this release. Do not state a legal conclusion.

#### 1.2 -- decision: Gaussian splat viewer, generate, license, and platform contract (T002)

**Objective**: Select the smallest lawful first-release 3D-preview boundary and define the load-bearing internal contract that later phases implement.

**Prompt**:
> Write `docs/v2/v2.4/development/gaussian-splat-contract.md` as a decision record. Compare four generate options: wrap 3D AI Studio HTTP, bundle ComfyUI, separately installed third-party splat CLI, and an internal adapter over MIT TripoSplat weights. Reject the first three. Choose viewer-first: ship a local `.splat`/`.ply` viewer even when no generator is present. Default the viewer to an internal TypeScript decoder plus WebGL2 rasterizer with no new npm Gaussian-splat package unless this record names a specific MIT decoder, pins version and integrity, and proves it makes no network calls. Forbid remote splat URLs, blob: fetches of untrusted hosts, and 3daistudio.com. Define `GaussianSplatViewRequest`, `GaussianSplatGenerateRequest`, capability result, progress, cancellation, typed errors, source/output identity, provenance fields (source hash, backend/version, Gaussian count, seed, duration), platform support (viewer cross-platform; generate NVIDIA CUDA only, fail closed on macOS/AMD/no-GPU), GPU scheduler sharing with diffusion, and honesty copy: "This is a generated 3D preview. Unseen sides are invented. It is not a measured property tour." Preserve the 2D original. Reject shell-string execution and automatic weight download outside the existing Hugging Face installer puller. Name the later tasks this decision unblocks.

**Failure modes**: Malformed or absent paths/formats fail schema validation before a job is queued or a canvas is created; a missing, incompatible, or slow generate backend returns a typed unavailable/timeout result without deleting the source image; conflicting view/generate requests for the same source receive distinct job/output identities and never overwrite each other or the 2D original.

**Build class**: load-bearing, because every runtime and UI phase depends on this contract.

#### 1.3 -- Testing and Stabilization (T003)

**Objective**: Verify the decision artifacts are complete and internally consistent before code begins.

**Prompt**:
> Validate links and ASCII safety for the two Phase 1 documents. Add a focused documentation or catalog test that proves every comparison acceptance criterion is assigned to a later phase and that `core/registry/catalog.json` contains no `qwen3.8-flash-next`, `Qwen3.8-Flash-Next`, or `unsloth/Qwen3.8-Flash-Next-GGUF` id. Re-read the Constitution Check after the design is fixed. Run the relevant scoped checks, fix failures, and do not proceed until the decisions are stable. Record Phase 1 CI IMPACT against `[[cicd-architect]]` without running remote CI: name any new command, dependency, environment variable, test path, or artifact and whether existing CI covers it. Do not edit pipeline files unless CI/CD is the explicit deliverable. Do not ask for human testing. Generate the Phase 1 session history at `docs/v2/v2.4/development/history/2026-08-28_v2.4.0-phase-1-decisions.md`, then create one local ASCII-only commit. Do not push, open a pull request, or start remote CI.

### Phase 1 Exit Checklist

- [x] Phase task range T001-T003 complete
- [x] Unsloth Qwen3.8-Flash-Next non-admission and deferred thinking kwargs are explicit
- [x] Gaussian splat contract, honesty copy, and license/platform boundary are resolved
- [x] Automated document/catalog checks pass
- [x] CI impact is recorded with no remote CI run
- [x] Session history is generated
- [x] One local commit exists and nothing was pushed

---

## Phase 2: Safe Local Splat Viewer Core

**Goal**: Add a backend-neutral local splat/PLY decoder and an internal WebGL2 rasterizer that never fetches remote assets.
**Prerequisites**: Phase 1.
**Stability Gate**: Unit and canvas tests prove typed view requests, local-only path/buffer loading, rejection of remote URLs, decode of fixture `.splat` and `.ply`, orbit/zoom camera bounds, and no network during view.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Signal scores were scope high, structural complexity high, context volume medium, risk high, and reasoning high because binary parsing and GPU canvas code create a security and correctness boundary.

### Sub-tasks

#### 2.1 -- Local splat/PLY decode and typed viewer requests (T004)

**Objective**: Implement the view contract without binding core code to WebGL, TripoSplat, or a desktop canvas.

**Prompt**:
> Implement `core/image/GaussianSplat.ts` from the Phase 1 contract. Define typed view requests, supported formats (`.splat`, `.ply`), capability descriptors that distinguish viewer-ready vs generate-ready, and typed decode errors. Accept only canonical local filesystem paths or already-read ArrayBuffers supplied by the sidecar; reject `http:`, `https:`, `ftp:`, and protocol-relative URLs. Decode a versioned tiny fixture under `tests/fixtures/gaussian-splat/` into positions, scales, rotations, opacities, and colors. Keep `core/image/GaussianSplat.ts` free of desktop, DOM, WebGL, and child-process imports. Export through a narrow core image index. Add unit tests under `tests/unit/core/image/GaussianSplat.test.ts`.

**Failure modes**: Malformed magic bytes, truncated files, or oversized Gaussian counts return typed validation errors without allocating unbounded arrays; an unreachable file is represented as unavailable without retrying in core; concurrent decodes use immutable request identities and cannot share mutable output buffers.

**Build class**: load-bearing, because it is the vendor-neutral view boundary.

#### 2.2 -- Internal WebGL2 splat rasterizer (T005)

**Objective**: Rasterize decoded Gaussians in Image Studio without a third-party viewer runtime or remote texture loads.

**Prompt**:
> Create `desktop/src/modules/image/SplatViewerCanvas.tsx` and the smallest supporting renderer module. Use WebGL2 only. Implement orbit, zoom, pan, and a bounded camera so the scene cannot be lost. Honor `prefers-reduced-motion` by disabling inertial spin. Do not fetch shaders, textures, or splat files from the network. Do not add SparkJS, SuperSplat, Three.js Gaussian plugins, or 3D AI Studio scripts as dependencies. If WebGL2 is missing, render a still-frame fallback and a typed unsupported message. Cap Gaussian count and viewport size from the Phase 1 contract. Screenshot exports a PNG from the canvas without uploading it.

**Failure modes**: Missing/malformed decoded buffers show a typed empty-state rather than a black hang; a lost WebGL context recovers to the fallback still and exposes retry; two canvases on screen do not share a single GL context if that would exceed the existing metal-ring instance cap.

**Build class**: load-bearing viewer; it can later display generator output without API changes.

#### 2.3 -- Testing and Stabilization (T006)

**Objective**: Prove decode and rasterizer safety before Image Studio chrome.

**Prompt**:
> Add and run unit tests for URL rejection, fixture decode, Gaussian-count caps, and typed errors. Add desktop component tests that mount the canvas with a fixture buffer, assert no `fetch`/`WebSocket` to non-local URLs, and cover missing WebGL2 fallback. Use jsdom or the existing desktop test harness; do not require a GPU in CI. Run relevant lint, typecheck, and tests until green. Record Phase 2 CI IMPACT against `[[cicd-architect]]` without remote CI, including the fixture path. Do not edit pipeline files or ask for human testing. Generate Phase 2 session history, then create one local ASCII-only commit. Do not push, open a pull request, or start remote CI.

### Phase 2 Exit Checklist

- [x] Phase task range T004-T006 complete
- [x] Core decode has no WebGL, desktop, or network dependency
- [x] Viewer rejects remote URLs and has a no-WebGL fallback
- [x] Automated core and desktop viewer tests pass
- [x] CI impact and session history are recorded
- [x] One local commit exists and nothing was pushed

---

## Phase 3: Image Studio 3D Preview Experience

**Goal**: Let users open a local splat beside a 2D Image Studio generation, orbit it, screenshot, download, and read honesty and provenance copy.
**Prerequisites**: Phase 2.
**Stability Gate**: Component tests prove 3D preview appears for eligible images and local splat files, unsupported states explain why, original downloads remain distinct, honesty copy is visible, and controls have keyboard access and accessible names.
**Recommended model tier**: strong
**Recommended effort level**: high
**Rationale**: Signal scores were medium across scope, structure, context, risk, and reasoning; the UI composes known Image Studio chat, queue, and recall patterns.

### Sub-tasks

#### 3.1 -- 3D preview actions and honesty copy (T007)

**Objective**: Add the user-facing entry point without obscuring txt2img / img2img / inpaint workflows.

**Prompt**:
> Extend `desktop/src/modules/image/ImageStudioPage.tsx` with a 3D preview action beside completed image download/recall actions. Add `desktop/src/modules/image/SplatPreviewPanel.tsx` using existing field, button, dialog/sheet, and composer patterns. Allow opening a sidecar-canonical local `.splat`/`.ply` or, once Phase 4 exists, a generated splat linked to the source message. Show the Phase 1 honesty string in the panel, not only in docs. Hide or disable generate until Phase 4 capability data exists; in this phase generate remains absent or visibly "coming from local backend" disabled with reason "generator not wired". Do not construct remote viewer URLs. Route file open through the typed sidecar client.

**Failure modes**: Missing/malformed capability or file data disables opening with typed copy; a slow/unreachable sidecar retains the 2D message and reports retryable failure; simultaneous panels remain bound to their own source and cannot display another image's splat by accident.

**Build class**: load-bearing user surface over the view contract.

#### 3.2 -- Screenshot, download, and source-linked provenance (T008)

**Objective**: Keep 2D and 3D artifacts distinct and attributable.

**Prompt**:
> Extend `core/image/WorkflowMetadata.ts` (or a sibling splat provenance document stored next to the file) with splat fields from the Phase 1 contract: source image hash, format, Gaussian count, backend/version when known, seed, timestamps. Screenshot saves a PNG locally. Download saves the `.splat` or `.ply` without replacing the source PNG. Recall actions on the 2D image remain unchanged. Do not embed multi-megabyte splat data in PNG tEXt; store a sidecar provenance record and a path/hash link.

**Failure modes**: Malformed/absent provenance writes a typed incomplete record rather than a fake backend/version; a failed screenshot leaves the splat file untouched; concurrent downloads use distinct filenames and never overwrite the source PNG.

#### 3.3 -- Testing and Stabilization (T009)

**Objective**: Prove the complete Image Studio view interaction with automated component tests.

**Prompt**:
> Extend `desktop/tests/ImageStudioPage.test.tsx` and supporting tests for eligible/ineligible outputs, fixture open, honesty copy presence, screenshot/download distinct from source, keyboard navigation, accessible names, reduced motion, and no remote URL construction. Run desktop lint, typecheck, and scoped tests until green. Record Phase 3 CI IMPACT against `[[cicd-architect]]` without remote CI. Do not edit pipeline files or ask for human testing. Generate Phase 3 session history, then create one local ASCII-only commit. Do not push, open a pull request, or start remote CI.

### Phase 3 Exit Checklist

- [x] Phase task range T007-T009 complete
- [x] 3D preview is available for local splat/PLY and eligible Image Studio outputs
- [x] Honesty, distinct downloads, and accessibility work in tests
- [x] Automated desktop checks pass
- [x] CI impact and session history are recorded
- [x] One local commit exists and nothing was pushed

---

## Phase 4: Generate Contract, Queue, and Preflight

**Goal**: Run optional splat generation as a cancellable Image Studio child job on the existing generation queue, with CUDA preflight and preserved 2D originals.
**Prerequisites**: Phase 3.
**Stability Gate**: Queue and runtime tests prove typed generate requests, capability classification, cancellation, timeout, isolated staging, source-byte preservation, and fail-closed behavior when CUDA/NVIDIA is absent.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Signal scores were high across scope, structure, context, risk, and reasoning because GPU scheduling, untrusted image decode, and child-job lifecycle have release-wide blast radius.

### Sub-tasks

#### 4.1 -- Child-job queue, cancellation, and CUDA preflight (T010)

**Objective**: Reuse the existing generation queue rather than starting a second untracked CUDA process.

**Prompt**:
> Extend `core/generations/GenerationQueue.ts` and `desktop/sidecar` image handlers from the Phase 1 contract, following the video-enhancement child-job pattern already in that queue (distinct job identity, progress stages, cancel of running vs queued). Add a generate job type for pillar `image` that does not start until `core/scheduler/GpuScheduler.ts` grants the GPU. Implement CUDA/NVIDIA preflight that returns typed `unsupported-platform`, `cuda-missing`, or `ready` without downloading weights. macOS, AMD-only, and CPU-only hosts are unavailable, not silently CPU-fallback. Wire `desktop/src/shared/studio/generationQueueClient.ts` so Image Studio can enqueue/cancel. Do not invoke TripoSplat yet; a fake/stub backend that writes a tiny valid splat from the fixture is enough for this phase.

**Failure modes**: Malformed requests never reach the GPU; an unreachable scheduler or hung job is cancelled and leaves the source PNG byte-identical; two generate jobs for the same source use isolated staging dirs and cannot complete into the same output path.

**Build class**: load-bearing queue integration; the Phase 5 adapter replaces the stub backend.

#### 4.2 -- Staged output without replacing the source (T011)

**Objective**: Publish a new splat artifact only after validation, keeping the 2D original authoritative.

**Prompt**:
> Create `desktop/sidecar/src/image/GaussianSplatRuntime.ts`. Stage output under the generation job directory. Validate magic bytes, non-zero Gaussian count within cap, and path canonicalization before promoting to the user-visible file. On success, attach the splat to the Image Studio thread as a child of the source message. On failure, quarantine or delete the staged file and keep the PNG. Record provenance from Phase 3. Do not overwrite `~/.nexus/generations` source bytes.

**Failure modes**: Missing/malformed staged output fails closed without touching the source; a slow backend hits the contract timeout and cancels the process tree; a cancel/complete race resolves from the authoritative terminal job state.

**Build class**: scaffolding for the output lifecycle; Phase 5 replaces the stub writer with the TripoSplat adapter while keeping this promotion path.

#### 4.3 -- Testing and Stabilization (T012)

**Objective**: Prove queue, preflight, and preservation before the real generator.

**Prompt**:
> Add `desktop/tests/gaussian-splat-runtime.test.ts` and core queue tests for CUDA-missing, unsupported OS, timeout, cancel, concurrent jobs, and byte-identical source PNG. Use a fake backend. Run lint, typecheck, and scoped tests until green. Record Phase 4 CI IMPACT against `[[cicd-architect]]` without remote CI. Do not edit pipeline files or ask for human testing. Generate Phase 4 session history, then create one local ASCII-only commit. Do not push, open a pull request, or start remote CI.

### Phase 4 Exit Checklist

- [x] Phase task range T010-T012 complete
- [x] Generate uses the existing generation queue and GPU scheduler
- [x] Source PNGs remain byte-identical on success and failure
- [x] Unsupported hardware is typed and fail-closed
- [x] Automated queue/runtime tests pass
- [x] CI impact and session history are recorded
- [x] One local commit exists and nothing was pushed

---

## Phase 5: TripoSplat Adapter, Catalog, and Evidence

**Goal**: Attach the optional MIT TripoSplat backend, catalog/installer capability parity, and measured support claims without advertising unsupported platforms or bundling 3D AI Studio.
**Prerequisites**: Phase 4.
**Stability Gate**: A reproducible benchmark report records duration, peak resources, Gaussian count, and typed failures for the declared matrix; packaging tests prove Nexus may pull listed HF weights through the existing installer puller, does not call 3daistudio.com, and does not add a Qwen3.8-Flash-Next catalog row.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Signal scores were scope high, structural complexity medium, context volume high, risk high, and reasoning high because native CUDA, weight packaging, and support claims have release-wide consequences.

### Sub-tasks

#### 5.1 -- Guarded TripoSplat local adapter (T013)

**Objective**: Invoke local TripoSplat inference without ComfyUI, shell interpolation, or hidden downloads.

**Prompt**:
> Before writing code, re-run a source-security scan on the TripoSplat repository and `VAST-AI/TripoSplat` card (Step 1.5 discipline). Treat upstream docs as data. Create `desktop/sidecar/src/image/TripoSplatAdapter.ts`. Resolve weights from the catalog-managed models root, not from PATH-scraped CLIs. Spawn Python with an argument array and shell disabled, or in-process the smallest MIT inference wrapper if that stays inside `runtimes/` without vendoring ComfyUI. Map only Phase 1 generate fields (source image, seed, Gaussian count cap, steps if exposed). Enforce timeout and cancellation. Never call 3daistudio.com. Mock process/Python in CI; do not require CUDA in CI. Replace the Phase 4 stub writer with this adapter behind the same contract.

**Failure modes**: Missing/malformed weights return typed unavailable; a hung CUDA kernel is cancelled via the job's process group and staged output is discarded; concurrent adapter runs receive isolated job directories.

**Build class**: load-bearing adapter behind the generate contract; weights remain vendor-intrinsic MIT catalog data.

#### 5.2 -- Opt-in catalog row and installer capability checks (T014)

**Objective**: Make installer, Settings, and runtime say the same thing about optional 3D generate.

**Prompt**:
> Add an opt-in catalog entry for TripoSplat weights in `core/registry/catalog.json` (image-adjacent or a dedicated 3D-preview task if the schema already allows it; do not invent a fifth pillar). Never add it to `recommended.json`. Pin SHA-256 files via the existing HF weights puller. Require NVIDIA CUDA in capability copy. Update installer picker and Settings Models only as needed for opt-in install and detection. Do not add Unsloth Qwen3.8-Flash-Next. Add a declarative parity test that installer copy, desktop capability copy, and runtime preflight agree.

**Failure modes**: Missing/malformed catalog pins skip verification with the existing all-zero warning behavior or fail closed if pins are real; slow HF pull is user-initiated and cancellable; concurrent Settings/installer edits resolve through one canonical model id.

#### 5.3 -- Benchmarks and packaging parity (T015)

**Objective**: Measure cost without turning subjective photorealism into marketing.

**Prompt**:
> Add `scripts/bench-gaussian-splat.mjs` with tiny versioned still fixtures (object-centric, simple background). Support a fake deterministic backend for CI and an explicitly invoked real-backend mode for local NVIDIA measurement. Record wall time, peak RAM/VRAM when observable, Gaussian count, output size, validation result, and typed failure. Write `docs/v2/v2.4/benchmarks/gaussian-splat-baseline.md` and label untested combinations `not proven here`. Document NVIDIA generate envelope, macOS viewer-only status, limitations, and troubleshooting. Do not invent a perceptual-quality score; human review belongs in Phase 6.

**Failure modes**: Malformed/missing fixtures fail before measurement; unavailable telemetry records `not observed` rather than fabricated zeros; concurrent benchmark runs use isolated output dirs.

**Build class**: load-bearing evidence harness for support claims.

#### 5.4 -- Testing and Stabilization (T016)

**Objective**: Verify adapter mocks, catalog pins, packaging claims, and Qwen absence before the final release gate.

**Prompt**:
> Run fake-backend splat tests, packaging/configuration parity tests, catalog absence of Flash-Next, installer scoped tests if catalog changed, desktop tests, lint, and typecheck. Verify repository artifacts contain no 3D AI Studio client, no Unsloth Desktop, no automatic splat-service URL, and no Qwen3.8-Flash-Next catalog entry. Iterate until stable. Record Phase 5 CI IMPACT against `[[cicd-architect]]` without remote CI. Do not edit pipeline files unless this phase explicitly owns a packaging gate; do not ask for human testing. Generate Phase 5 session history, then create one local ASCII-only commit. Do not push, open a pull request, or start remote CI.

### Phase 5 Exit Checklist

- [x] Phase task range T013-T016 complete
- [x] TripoSplat adapter is optional, local, and mocked in CI
- [x] Catalog/installer/desktop support claims match
- [x] No 3D AI Studio client, Unsloth Desktop, or Qwen3.8-Flash-Next catalog entry exists
- [x] Benchmark evidence distinguishes observed, failed, and not-proven cases
- [x] Automated packaging checks pass
- [x] CI impact and session history are recorded
- [x] One local commit exists and nothing was pushed

---

## Phase 6: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, its living docs architecture current, and its pipeline reconciled against the canonical contract, with independent evidence that the plan Goal landed, then publish and integrate the completed branch.
**Prerequisites**: All prior phases.
**Stability Gate**: `docs/v2/v2.4/development/last-phase-evidence.md` exists with one quoted section per duty below. A missing Goal, a Goal-review miss without a recorded known-gap, or a missing evidence section blocks `/update release`.
**Recommended model tier**: frontier
**Recommended effort level**: max
**Rationale**: Repo-wide refactor, Goal-vs-codebase review, known-gap reconciliation, and release gating carry high context volume and blast radius.

### Sub-tasks

#### 6.1 -- Architecture refactor
**Objective**: Refactor toward a well-organized, intuitive layout and quote the scan.
**Prompt**:
> Identify deprecated/obsolete files, empty directories, redundant files/dirs, and overcomplicated structure, then refactor toward a clean, intuitive layout via [[project-refactor]] and [[docs-layout-refactor]] (propose-then-apply, with confirmation; repair every reference for anything that moves). Write the detector output into `docs/v2/v2.4/development/last-phase-evidence.md` under `## Architecture refactor`. An empty finding is allowed only when the scan output is quoted.

#### 6.2 -- Known-gaps reconciliation
**Objective**: Reconcile this version and every other still-open known-gaps file.
**Prompt**:
> Via [[known-gaps-tracker]], reconcile this version's known gaps AND every other `docs/**/known-gaps.md` whose Status is in-progress or whose Open Items remain. Glob both canonical (`docs/releases/v<MAJOR>/v<MAJOR>.<MINOR>/known-gaps.md`) and legacy layouts. If a file is unreachable, record the glob result and continue with what was found. Write the disposition into the evidence file under `## Known-gaps reconciliation`.

#### 6.3 -- Living docs architecture
**Objective**: Prove handbooks, decisions, and generated HTML match the required living tree.
**Prompt**:
> Check `docs/handbooks/` (markdown source of truth, generated `html/`, one atlas walkthrough, technical companions for key components), `docs/decisions/`, and living `docs/README.md` / `docs/DEVLOG.md` / `docs/todos.md`. Self-gated: never invent `docs/testing/` or `docs/validation/`. Markdown wins if HTML disagrees; regenerate or fail the check. Quote the check in the evidence file under `## Living docs architecture`. Details live in [[docs-layout-refactor]].

#### 6.4 -- Git-tree hygiene
**Objective**: Report branch and repository-settings hygiene without deleting anything.
**Prompt**:
> Run `python scripts/check_release_preconditions.py --branches --repo-settings`. Quote the output in the evidence file under `## Git-tree hygiene`. Report only; never delete branches.

#### 6.5 -- Terminal CI/CD reconciliation
**Objective**: The repository's pipeline is compared field by field against the canonical contract, and every difference is either applied with approval or recorded as a known gap.
**Prompt**:
> Invoke [[cicd-architect]] in existing-pipeline comparison mode and run its six steps in order: DETECT the active CI provider (recording "none detected" rather than assuming one); COMPARE the existing pipeline against every field of the canonical contract - repository-native profiles, event separation, runner selection, the always-resolving aggregate required check, permissions, immutable action or plugin references, caching, concurrency, path scoping, artifact retention, structured reports, deployment boundaries, and failure recovery; PROPOSE each difference with its cost, its risk, and the smallest change that closes it; obtain explicit APPROVAL per change (silence is not approval); APPLY the approved changes and re-run the local gate; and RECORD every declined or environment-only difference as a known gap with an owner and a next step via [[known-gaps-tracker]]. The comparison concludes PASS only when every required field has observable evidence; a green pipeline run is not evidence, because a pipeline that checks nothing is green for the wrong reason. If this repository ships more than one installer, run or add a declarative cross-installer parity gate covering distributed artifacts, supported platforms, named capability counterparts, and external-tool fallbacks, then execute the real installers on their target operating systems with identical postconditions. Cross-link [[platform-contract-verification]] so discovery and delivery are checked in the same pass. If this repository ships zero or one installer, record a silent no-op and continue. Quote the per-field comparison result in the evidence file under `## CI/CD coverage`.

#### 6.6 -- Independent Goal-vs-codebase review
**Objective**: Prove the plan Goal landed in the codebase, not that checkboxes were ticked.
**Prompt**:
> Re-read the plan header Goal and the Goals First / Definition of Done. Inspect the codebase as if this agent had not implemented the phases. Write `## Goal-vs-codebase review` in `docs/v2/v2.4/development/last-phase-evidence.md` listing: the plan Goal restated; the code/docs artifacts that satisfy it; and any gap. A missing or malformed Goal is a fail-closed finding that names the missing Goal. Completing every prior-phase sub-task is not evidence the Goal landed. A miss is a blocking finding or a new known-gap, not a pass.

#### 6.7 -- Human/manual testing suggestions (last phase only)
**Objective**: Ask a human to exercise only what automated tests cannot cover, now that the feature is complete.
**Prompt**:
> Emit human/manual testing suggestions for user-facing workflows, external integrations, and environment-specific cases that automated tests cannot cover. Include orbit/screenshot on a real GPU canvas; honesty copy visibility; generate on a supported NVIDIA host if present; truthful unavailable behavior on macOS or AMD-only; cancellation under load; source PNG vs splat download; and confirmation that no photo upload to 3D AI Studio occurs. Do not invent a fake walkthrough. This duty exists only on the last phase.

#### 6.8 -- Testing and Stabilization
**Objective**: Prove the refactor preserved behavior and the complete LOCAL gate is green before anything is published.
**Prompt**:
> Run the full local validation/test suite, confirm the refactor changed no behavior, and iterate until clean. This gate is local by construction: it must pass before the branch is published, so it cannot depend on a remote run. Quote the suite in the evidence file under `## Full-suite testing and stabilization`. Generate a session-history entry for this phase.

#### 6.9 -- Publication and integration
**Objective**: Publish the completed branch once, validate the merge result remotely, and hand a green integration to the release flow.
**Prompt**:
> Create the final local commit for this phase. Then present the resolved branching model, remote, branch name, and pull-request target, and obtain EXPLICIT approval before the plan's first branch push. Push once, open the integration pull request against the integration branch, and report the exact required checks expected. Wait until every required check reaches a terminal state. A red check REOPENS this phase: classify the failure, reproduce it locally, apply the narrow fix, re-run the local gate, update known gaps and session history, then amend the final commit or add one narrowly scoped stabilization commit and push again with approval. Never re-run a red check without a local reproduction; a re-run without one is a guess. Merge only after every required check is green and the user approves. Confirm the post-merge workflow performed only its intended smoke, publication, or provenance work and did not rerun the complete suite. Only then hand off to `/update release`, which owns the version bump, changelog, tag, push, and GitHub Release behind its own confirmation gates. Quote the required-check results in the evidence file under `## Publication and integration`.

### Phase 6 Exit Checklist

- [ ] Phase task range T017-T025 complete
- [ ] Every last-phase duty has quoted evidence or a structured known gap
- [ ] Goal-vs-codebase review passes or records a blocking gap
- [ ] Last-phase-only human testing is complete
- [ ] Full local suite passes before publication
- [ ] One final local phase commit exists
- [ ] User approved the first push and integration pull request
- [ ] Required remote checks are green and merge is approved
- [ ] Release flow begins only after green integration

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|

## Out of Scope

- Adding Qwen3.8-Flash-Next, Unsloth Flash-Next GGUF, or Unsloth Desktop to any catalog, recommendation, patient-tier, or automatic installer path.
- Implementing Qwen `chat_template_kwargs` / `reasoning_effort` / `preserve_thinking` until a Qwen thinking model clears admission.
- Cataloguing Qwen3.8-27B as a substitute for Flash-Next.
- Wrapping 3D AI Studio, its credits, accounts, or remote viewer URLs.
- Multi-view COLMAP / OpenSplat property-tour reconstruction.
- Apple SHARP weights (non-commercial research license).
- Promising photorealistic real-estate walkthroughs from one photo, recovered architecture, or Metal/CUDA generate parity without measurement.
- Replacing the existing generation queue, GPU scheduler, Image Studio chat surface, or PNG workflow metadata.

## Residual Risks

- TripoSplat is NVIDIA CUDA in upstream demos; macOS may remain viewer-only for the whole release.
- Single-image splats invent unseen geometry; honesty copy can still be ignored if marketing language slips.
- Splat generate contends with SANA/SDXL for VRAM; the GPU scheduler must remain the single owner.
- Qwen artifacts, licenses, and Unsloth LICENSE files can change after this plan. Re-evaluation must use fresh official evidence and Nexus benchmarks.
- Live generate quality is not proven in CI; Phase 6 human testing and DF-style hardware gaps remain likely.
