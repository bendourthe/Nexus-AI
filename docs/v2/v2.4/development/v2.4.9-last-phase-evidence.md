# v2.4.9 last-phase evidence

**Plan**: [v2.4.9-adoption-voicestudio-field-discipline.md](../plans/v2.4.9-adoption-voicestudio-field-discipline.md), Phase 5 (renumbered from 6 by D1)
**Date**: 2026-09-10
**Filename note**: prefixed `v2.4.9-` deliberately. The v2.4.10 plan sits in the same version directory and writes its own last-phase evidence; an unprefixed name would collide.

Every duty below quotes the command or scan that proves it. An empty finding is recorded with its output rather than asserted.

---

## Architecture refactor

Report-only, per the plan. A repo-wide move-and-repair has no exit criterion and is the highest-risk action available; findings are recorded, nothing was moved or deleted.

Both findings the plan predicted in advance are present.

**Finding 1 -- stale root-level build artifacts.**

```
$ git ls-files | grep -E '^[^/]+\.(vsix|log|tmp|bak)$'
  (none tracked)

$ ls -1 *.vsix
nexus-coding-2.1.0.vsix
nexus-coding-2.2.8.vsix
nexus-coding-2.2.9-win32-x64.vsix
nexus-coding-2.2.9.vsix
nexus-coding-2.3.1-win32-x64.vsix
nexus-coding-2.4.0.vsix
nexus-coding-2.4.1-win32-x64.vsix
```

Seven `.vsix` files spanning 2.1.0 to 2.4.1 sit in the repository root. All are **untracked and gitignored**, so they never reach a clone; this is local working-tree litter, not a repository defect. Left in place: deleting a developer's local build output is not this gate's call.

**Finding 2 -- `core/storage/StorageMigration.ts` outlived its own removal note.**

```
$ grep -n -i 'removed' core/storage/StorageMigration.ts
18: * Removed in v1.1.0.
```

The file and `tests/unit/core/storage/StorageMigration.test.ts` are both still present. Checked whether anything calls it:

```
$ grep -n 'StorageMigration' core/storage/paths.ts core/diagnostics/DoctorReport.ts
core/storage/paths.ts:6:   * canonical path is `~/.nexus/`. Until `StorageMigration` has run, callers
core/storage/paths.ts:31:  * Legacy Gemma Code per-user data root. Used by `StorageMigration` to detect
core/diagnostics/DoctorReport.ts:95: /** The marker `StorageMigration` drops after copying ... */
core/diagnostics/DoctorReport.ts:217: ... StorageMigration stopped reading it in v1.1.0 ...
```

Every reference is a comment or a doc string. **Nothing calls it.** It is genuinely orphaned code carrying a test that also runs. Report-only; recorded as a gap rather than removed, because deleting a migration path is a decision about users still on `~/.gemma-code/`.

**Docs layout:**

```
$ npm run check:docs-layout
check-docs-layout: canonical layout OK (no docs/versions|docs/archive/versions wrappers)
```

---

## Known-gaps reconciliation

Bounded to this version plus the gaps this plan created, per the plan's own scoping. The glob was run and the other files are listed and left.

```
$ find docs -name 'known-gaps.md' | wc -l
30
```

Five carry `Status: in-progress` and are **listed and left untouched**, exactly as the plan directs:

| File | Items |
|---|---:|
| `docs/v1/v1.19/known-gaps.md` | 22 |
| `docs/v1/v1.20/known-gaps.md` | 2 |
| `docs/v2/v2.0/known-gaps.md` | 9 |
| `docs/v2/v2.3/known-gaps.md` | 12 |
| `docs/v2/v2.4/known-gaps.md` | 81 (this version) |

### What this plan added to the v2.4.9 subsection

**Resolved (2):** `BG-17` the image-path VRAM release that released nothing; `BG-18` the eviction fit test that asked about a per-pillar constant instead of the job's model. Both were out-of-plan fixes taken by operator decision after the standards judge found them.

**Open (9):**

| ID | Summary | Exit condition |
|---|---|---|
| `NI-1` | The two model registries are untested against each other; `inkling-small` parses tool calls with a Gemma grammar | A test asserts `catalog.json` against `ModelCatalog.ts`; all 13 divergent ids bound, removed, or recorded |
| `NI-2` | Crash classes not derived from data, because the repository has zero issues | Re-derive from 20+ form-filed reports |
| `MT-11` | The standards judge was never dispatched through the agent registry, only by adopting its definition | One by-name dispatch in a fresh session |
| `DF-9` | `prompt-*` budget checks do not cover `.claude/` | `check:prompts` reports a budget line for a `.claude/agents/` file, or 10.N.H records the exemption |
| `DF-10` | The inventory covers two README regions, not the whole file | A third region added, or the two-region scope recorded as final |
| `DF-11` | The crash-class report is operator-run, not scheduled | A scheduled job, or the decision recorded |
| `BG-16` | 216 stale relative links in the living `docs/DEVLOG.md` | A link checker reports zero, or pre-rename entries carry a stated convention |
| `BG-19` | `npm audit (production deps)` is red; `sharp` has no upstream fix | `npm audit --omit=dev` exits zero, or residuals accepted with a dated review |
| `QG-2` | The two newly gating checks are not yet required in branch protection | Both green once, then added to the required list |
| `QG-3` | 17 individual required contexts, not the contract's aggregate gate | One always-resolving required context covering the path-filtered workflows |

Items the plan asked for by name and their disposition:

- **A11 (signed updater), deferred twice over.** Recorded in the plan's Non-Goals: once as an out-of-scope comparison item, and again because A5 (the snapshot work it is gated on) moved to the v2.5.0 plan under D1.
- **CodeQL Python and runtime `pip-audit` backlogs with counts.** The runtime audit is measured at **180 packages, 0 advisories**. CodeQL Python is unmeasured; see `## CI/CD coverage` and `## Deferred verification`.
- **gitleaks allowlist residual.** **Zero.** The backlog was 16 findings across 9 clusters, fewer than the 10 the plan budgeted, so the allowlist covers all of it. No residual gap to record.
- **The D3 option-3 two-tier inventory enforcement.** D3 chose option 1, so the nightly-command tier is not built; the condition under which it becomes right is recorded in `docs/reference/feature-inventory.md`.
- **"First non-empty crash-class report."** Folded into `NI-2`, which clears on the same trigger.
- **The in-app prefilled-URL bug-reporting follow-up from 4.1.** Not implemented, per the plan's instruction not to implement it there. It is the one named item **not** separately filed, because `NI-2` and `DF-11` already gate on reports arriving and a third entry would fragment one exit condition; recorded here instead.
- **The three unnumbered comparison candidates** (`LICENSE-NOTICE.md`, the `taze --maturity-period` window, wiring `desktop-prod:upgrade` into CI) remain in the plan's Non-Goals table with their reasons.
- **Every finding from the architecture refactor above**: the seven root `.vsix` files (local litter, no action) and `StorageMigration.ts`.

---

## Living docs architecture

```
docs/handbooks       present (6 md)      docs/README.md   present
docs/decisions       present (1 md)      docs/DEVLOG.md   present
docs/reference       present (6 md)      docs/todos.md    present
docs/guides          absent
docs/standards       absent
docs/runbooks        absent
```

Self-gated as the runbook requires: `docs/testing/` and `docs/validation/` were checked and are **correctly absent**. They were not invented.

`docs/handbooks/` carries `markdown/`, `html/` and `technical/` alongside its README, so the markdown-source plus generated-HTML shape holds.

**One finding, found and fixed in this pass.** The duty explicitly checks that the two new reference documents are reachable rather than orphaned:

```
model-acceptance     linked from: AGENTS.md CONTRIBUTING.md
feature-inventory    ORPHANED
```

`docs/reference/feature-inventory.md` was reachable from nothing. Fixed by linking it from both entry points, in the sections that already cover the adjacent contract:

```
model-acceptance     AGENTS.md CONTRIBUTING.md
feature-inventory    AGENTS.md CONTRIBUTING.md
```

The absent `guides/`, `standards/` and `runbooks/` roots are not created. Nothing in this plan produced content for them, and inventing empty directories to satisfy a checklist is the failure this duty is self-gated against.

---

## Git-tree hygiene

```
$ python scripts/check_release_preconditions.py --branches --repo-settings
[branches]
current=develop
head=d2cea1486fb9
protected_checkout=yes
working_tree=clean
origin=https://github.com/bendourthe/Nexus-AI.git
upstream=origin/develop
local_count=7
merged_into_head_count=3

[repo-settings]
status=observed
repository=bendourthe/Nexus-AI
default_branch=main
private=false
archived=false
issues_enabled=true
delete_branch_on_merge=false
default_branch_protection=observed
```

Report only. No branch was deleted. Three of seven local branches are merged into HEAD and are cleanup candidates for the operator.

**`protected_checkout=yes` is new this cycle and it changes the publication mechanics.** Branch protection was applied during Phase 2, and this plan's fourteen commits sit on `develop` itself. A direct push to `develop` is now blocked by its own required checks, so publication has to go through a feature branch. That is handled in `## Publication and integration` below, without any history rewrite.

**Scaffolding removal.** The plan's 5.4 asks for the Phase 1.3 staging guard to be removed from `.husky/pre-commit`. There is nothing to remove: **1.3 was cut** at Phase 1 because the working tree was clean at phase start, and the guard was never added.

```
$ cat .husky/pre-commit
npx lint-staged
```

Unchanged, one line, exactly as it was before this plan. The Phase 5 exit-checklist item "1.3 scaffolding guard removed; `npx lint-staged` intact" is satisfied by the cut plus this file state.

**Uncommitted installer rework.** The plan's dirty-tree constraint required stating its status explicitly before publication rather than letting the operator discover it. It is **committed**, not uncommitted: it landed through the v2.4.8 merge `f36afd9c` before Phase 1 began. The working tree is clean.

---

## CI/CD coverage

Terminal reconciliation via existing-pipeline comparison, **bounded** as the plan's Non-Goals require: confirm this plan's own checks, record other differences, do not perform an exhaustive fourteen-field comparison across 21 workflows.

**DETECT.** Provider: GitHub Actions. 21 workflow files, all parse.

**COMPARE -- this plan's checks, confirmed:**

| Required confirmation | Result |
|---|---|
| CodeQL Python present with a per-language check name | Present. `matrix.include` carries `javascript-typescript` and `python`, both `build-mode: none`; `name: Analyze (${{ matrix.language }})`. **Extraction itself is unconfirmed** -- see below. |
| A sibling job auditing the three runtime requirement sets | Present. `audit-runtimes`, gating, covering `runtimes/{audio,diffusion,ocr}/requirements.txt`. |
| gitleaks on both triggers | Present. `secrets.yml` on `push` and `pull_request` to `main` and `develop`. |
| `installer-smoke` on paths, not cron only | Present. `pull_request` filtered to `scripts/installer/**`; the monthly cron is retained, not replaced. |
| `check-feature-drift.mjs` gating | Present. `check-feature-drift` job in `ci.yml`. |
| `docs-drift.yml` scheduled | Present. Daily at 06:00 UTC, one rolling issue. |
| New test paths run by an existing required check, and `.test.ts` so vitest collects them | Confirmed by **running** them, not assumed: `tests/unit/scripts/check-feature-drift.test.ts` (17), `tests/unit/scripts/crash-class-report.test.ts` (18), `tests/unit/docs/v2.4.9-model-acceptance.test.ts` (5), `tests/python/diffusion/test_vram_release_regression.py` (3), `desktop/tests/media-model-vram.test.ts` (11). All collected; none is `.test.mjs`. |

**Gating status against D2, confirmed exactly:**

| Check | D2 says | Actual |
|---|---|---|
| `pip-audit (model runtimes)` | gating | gating (no `continue-on-error`) |
| `gitleaks` | gating | gating |
| `Analyze (python)` | reporting-only | `continue-on-error: true` |
| `Analyze (javascript-typescript)` | unchanged | `continue-on-error: true`, as before |

**The CodeQL rename did not drop a required check.** The plan's exit checklist asks this specifically. Two independent reasons it could not have: the rendered name is byte-identical to the previous literal (`Analyze (javascript-typescript)`), and at the time of the rename **no branch protection existed at all**. Neither `Analyze` check is in the required set today, deliberately, because `continue-on-error` makes them report success regardless of findings.

**Field-by-field, recorded not applied:**

| Contract field | Observed | Disposition |
|---|---|---|
| Immutable action references | **All SHA-pinned.** `grep -rhoE 'uses: [^ ]+' \| grep -vE '@[a-f0-9]{40}'` returns nothing | PASS |
| Concurrency groups | 20 of 21 workflows; `branch-cleanup.yml` lacks one | Recorded, not applied |
| Explicit `permissions` | 9 of 21 declare at top or job level | Recorded, not applied |
| Path scoping | 3 workflows path-filtered on PRs | PASS, and see the aggregate-check gap |
| Artifact retention | Declared on the jobs this plan added (30 days) | PASS |
| Always-resolving aggregate required check | **ABSENT.** 17 individually required contexts | Recorded as `QG-3` |
| Deployment boundaries | `release.yml` / `semantic-release.yml` unchanged by this plan | Out of scope |

**The aggregate-check gap is the one with teeth, so it is stated plainly rather than filed and forgotten.** Three workflows (`installer-tests.yml`, `installer-smoke.yml`, `shell-build.yml`) are path-filtered on pull requests. A pull request touching none of their paths produces **no check at all** from them, so requiring them individually would leave a required context permanently pending and block every unrelated merge. They are therefore unguarded by branch protection today. The contract's aggregate job, which always runs and resolves its dependencies' skip states, is what closes this. Authoring it is a pipeline-topology change the Non-Goals exclude, and it is `QG-3`.

**Cross-installer parity.** The repository ships more than one installer workflow (`installer-build`, `installer-linux`, `installer-macos`), so the parity check applies. No declarative cross-installer parity checker exists in this repository, and adding one is a new gate rather than a reconciliation. Recorded here as an observation for the next cycle rather than filed as a separate gap, because `QG-3` already owns the pipeline-shape work this would sit inside.

**Post-merge behaviour.** Unverifiable until the merge happens; carried into `## Deferred verification`.

**PASS/FAIL.** The comparison concludes **PASS on this plan's own checks**, each with observable evidence above, and **records** the aggregate-gate, concurrency, permissions and parity differences without applying them. No pipeline file was changed in this duty.

---

## Tier 3 deep pass

Invoked with a blast-radius verdict first, as the runbook requires.

**Blast-radius verdict: RUN, weighted.** The artifact set is two reference documents, one agent definition, five workflow edits or additions, one rebuilt 29-entry inventory, three scripts with tests, two issue templates, **and three modified production source files** from the out-of-plan GPU fixes. That last group is what makes this a weighted run rather than a reduced one: the plan's own storage change left under D1, but the diversion put real code back on the generation hot path.

**Full procedure** for the items that can fail CI or that run in production: the GPU fixes, `check-feature-drift.mjs`, and the two gating security checks. **Reduced path** for the additive documents, templates and reporting-only workflows, each of which already carries phase-level verification.

### Full-procedure items

**The GPU fixes (highest radius).** Both were proven to fail against pre-change code before being trusted, which is the strongest evidence available without a GPU:

```
$ git checkout HEAD -- runtimes/diffusion/{vram_lifecycle.py,pipelines/real_execute.py}   # pre-change
$ python -m pytest tests/python/diffusion/test_vram_release_regression.py -q
AssertionError: expected collect-then-empty, got ['empty_cache', 'gc_collect']
FAILED ...::test_image_execute_drops_the_pipe_before_releasing_vram
FAILED ...::test_empty_cache_collects_before_returning_blocks_to_the_driver
2 failed, 1 passed
```

After restoring the fix: `3 passed`. The weakref test proves the pipeline is unreachable at sweep time; the call-order test proves collect precedes empty. The eviction fix asserts both sides of the decisive case in one run: with the catalog value (24 GB) the chat model is evicted, with the old per-pillar constant (8 GB) it is not.

**`check-feature-drift.mjs`**, exercised adversarially beyond its unit tests:

```
probe 1 duplicate region heading  -> ok: true  | errors: 0
probe 2 regex metacharacters in a feature name ("C++ (a|b)") -> ok: true | errors: 0
probe 3 traversal-shaped evidence path ("../../etc/passwd") -> ok: false (flagged missing; no read attempted)
```

Probe 2 confirms names are compared with `includes()`, not compiled as patterns, so a feature named with regex syntax cannot alter matching. Probe 3 confirms the checker only ever asks whether a path exists.

Probe 1 surfaced a **bounded limit worth stating**: with two identically-named `## ` headings, only the first slice is parsed, so names in the second are silently unenforced. Duplicate H2 headings in one README is pathological input, and the fix would be a heading-uniqueness assertion. Recorded here rather than filed, because it is a property of a README nobody has written.

**The two gating security checks**, re-run terminally:

```
$ gitleaks git --no-banner --redact -c .gitleaks.toml
INF 750 commits scanned.  INF no leaks found

$ python -m pip_audit -r runtimes/{ocr,audio,diffusion}/requirements.txt ...
ocr 21 pkgs / 0 advisories | audio 94 / 0 | diffusion 65 / 0   -> 180 packages, 0 advisories
```

### Reduced-path items

The acceptance bar, the agent definition, the inventory contract document, the two issue templates and the crash-class report each carry their phase-level verification unchanged: the job-map invariants (proven to fail on an injected duplicate), the judge's 11-finding run against the pinned v2.4.8 range, both YAML files parsing with `app-version` confirmed `required: true` by reading the parsed form, and 18 fixture tests for the report.

### Cross-phase interaction

The one real interaction is the shared file the plan warned about. Phase 2 and Phase 3 both edit `.github/workflows/ci.yml` at disjoint anchors; Phase 3's edit was sequenced after Phase 2's as instructed. Confirmed both jobs coexist and the file parses: **18 jobs**, including `audit-runtimes` and `check-feature-drift`.

**Terminal disposition: bounded clean.** No unresolved deep-pass finding. The two limits surfaced (the duplicate-heading parse and the absence of live-GPU evidence) are recorded, the latter under the pre-existing `QG-1`.

---

## Goal-vs-codebase review

Independent pass: the plan's Goal re-read first, then the tree inspected as if I had not implemented it.

**Goal restated**, verbatim from the plan header: *"Close the loop between a shipped Nexus build and the repository that produced it -- a feature inventory CI actually enforces, a written bar every new model must clear, security scanning that covers the Python half of the tree, bug reports that carry the build they came from, and databases that survive their own migrations."*

Five promises. Checked by artifact, not by intent, and **only for clauses whose owning phase landed**.

| # | Definition-of-Done clause | Owner | Verdict |
|---|---|---|---|
| 1 | A bug filed states its build version because the form required it | Phase 4 | **Met, with a deferred observation.** `app-version` is `required: true`, confirmed by parsing the form rather than reading it. The rendered New-issue page only exists after merge to the default branch. |
| 2 | The crash-class report runs, bucketizes correctly against fixtures, reports its unknown count truthfully | Phase 4 | **Met.** 18 tests; the load-bearing assertion is that an unversioned report appears in **no** version total. Per the plan's honesty note, a non-empty live distribution is deliberately not asserted. |
| 3 | A CI run fails when a feature named in README.md no longer exists in the tree | Phase 3 | **Met.** `check-feature-drift` gates, and was shown to fail on a real induced drift where the feature name still appeared 3 times in changelog prose. |
| 4 | CodeQL reports Python results with a non-zero scanned-file count, plausible against the tracked Python surface | Phase 2 | **NOT MET locally, and not claimed.** The configuration is in place; no count exists. See below. |
| 5 | A corrupted migration leaves the original intact and stops the app | — | **Deferred scope, not a miss.** D1 moved it to the v2.5.0 plan with the work that owns it. |

**Clause 4 is the honest gap in this release.** The plan's own review criterion says a count in the thousands would mean the extractor scanned a vendored virtualenv and is a miss, not a pass. I have no count at all: the CodeQL CLI was deliberately not installed, so the first Python extraction happens on the integration pull request. What exists today is configuration that *should* produce a plausible count against **308 tracked Python files** (193 installer, 55 runtimes, 57 tests, 3 other). Calling clause 4 satisfied would be exactly the "satisfied in intent" failure this review is meant to catch.

This does **not** block the handoff: it is a deferred observation with a named check, carried in `## Deferred verification` and gated at 5.10, not an unresolved finding hiding behind a green tick.

**Goal fragments not reducible to a numbered clause:**

- *"a feature inventory CI actually enforces"* -- met. The word doing the work is *actually*: the previous inventory was read by nothing.
- *"a written bar every new model must clear"* -- met, and stronger than asked. The bar is enforced by a committed test, not only by review, and it surfaced a real divergence nobody had noticed (`NI-1`).
- *"security scanning that covers the Python half of the tree"* -- **partially met.** Configured, unproven. Same evidence as clause 4.
- *"bug reports that carry the build they came from"* -- met, pending render.
- *"databases that survive their own migrations"* -- deferred to v2.5.0 by D1.

**One thing this release delivered that the Goal did not ask for**, stated so the scope is legible: two user-facing defects in shipped v2.4.8 code were fixed out of plan (`BG-17`, `BG-18`), on the operator's explicit instruction, after the Phase 1 critic found them. That is the loop the Goal describes working on its first turn.

**Verdict: the Goal landed for every clause whose phase ran, with clause 4 recorded as deferred-and-named rather than claimed.**

---

## Human testing suggestions

Last phase only. Only things automated tests cannot reach. No walkthrough is invented.

1. **File a real test issue through the new bug form.** Open the New-issue page, confirm the chooser shows "Bug report" plus the three contact links, and confirm the form **refuses to submit** with the Nexus version blank. Then submit one and check the rendered body contains `### Nexus version` with the value underneath, which is the shape `crash-class-report.mjs` parses. Close it afterwards.
2. **Run the crash-class report against that issue**: `node scripts/crash-class-report.mjs --since 2020-01-01`. Confirm it reports 1 versioned report and buckets it to the version you typed, not to the unknown bucket.
3. **Open the Security tab after the merge** and confirm Python results are present with a scanned-file count in the low hundreds. **A count in the thousands means the extractor reached a virtualenv and is a miss, not a pass.** This is the clause-4 check.
4. **Confirm the GPU handoff actually hands off**, which nothing automated here can do. On the machine with the NVIDIA card: load a chat model so it is resident, check VRAM, then run one image generation in Image Studio. Watch VRAM during and after. Before the fix the weights stayed resident after the job; afterwards the allocator should give them back and the chat model should answer immediately without a reload stall.
5. **Confirm the eviction picks the right threshold.** With a chat model resident on the 24 GB card, start a Wan 2.2 TI2V video job (24 GB floor). It should now evict the chat model rather than proceeding into CPU offload. The old behaviour was to leave it resident and run slowly.
6. **Touch an installer file in a pull request** and confirm `installer-smoke` fires on Windows and Linux and **not** macOS, then confirm it does not fire on a pull request that touches no installer path.

Items 4 and 5 are the ones worth doing first: they are the only user-visible behaviour changes in this release, and `QG-1` records that nothing in this cycle has run against a live GPU.

---

## Full-suite testing and stabilization

Local by construction. Run after every phase landed, on the complete tree.

```
### ROOT VITEST
 Test Files  61 failed | 476 passed | 3 skipped (540)
### DESKTOP VITEST
 Test Files  9 failed | 216 passed (225)
### PYTEST
313 passed in 4.43s
### INSTALLER PYTEST
(100%, no failures)
```

**Against the measured baseline at `f36afd9c`:**

| Suite | Baseline | Now | Delta |
|---|---|---|---|
| Root vitest | 61 failed / 473 passed / 537 files | 61 failed / 476 passed / 540 files | **+3 files, +3 passing, 0 new failures** |
| Desktop vitest | 9 failed / 215 passed / 224 files | 9 failed / 216 passed / 225 files | **+1 file, +1 passing, 0 new failures** |
| Python | 310 passed / 0 failed | 313 passed / 0 failed | **+3 passing** |
| Installer python | pass | pass | unchanged |

**Every failure is the known `better-sqlite3` Electron-ABI baseline on this host** (`NODE_MODULE_VERSION 146` against a required `137`). The root baseline was measured twice in this session, at HEAD with the changes stashed and again with them applied, and the failure count was identical both times. Three assertion-shaped messages in `tests/unit/storage/MemorySubsystem.test.ts` are the same cause one layer up: the subsystem catches its store's construction failure and yields `null`, so the ABI fault surfaces as `expected null not to be null`. **Do not `npm rebuild` to chase these.**

Other gates:

```
$ npm run lint                 exit 0
$ npm run check                54 findings -- 0 error, 54 warning; baseline 56 matched, 0 stale
$ npm run check:tampering      0 findings
$ npm run check:docs-layout    canonical layout OK
$ npm run check:naming         clean
$ node scripts/check-feature-drift.mjs
                               OK -- 29 entries agree with README
$ python -m ruff check runtimes
                               1 error: unused `typing.Any`, vram_lifecycle.py:35
```

That single ruff error **predates this plan** and is recorded as `WN-1` under v2.4.5. It sits in a file this plan edited, and was deliberately left: fixing it is out of scope and it belongs to an existing gap owner. The count is unchanged by this plan's edits.

Test files added this cycle, all confirmed **collected by running them**, not assumed:

| File | Tests |
|---|---:|
| `tests/unit/docs/v2.4.9-model-acceptance.test.ts` | 5 |
| `tests/unit/scripts/check-feature-drift.test.ts` | 17 |
| `tests/unit/scripts/crash-class-report.test.ts` | 18 |
| `tests/python/diffusion/test_vram_release_regression.py` | 3 |
| `desktop/tests/media-model-vram.test.ts` | 11 |
| | **54** |

None is `.test.mjs`, which `configs/vitest.config.ts` would silently never collect.

---

## Deferred verification

Everything that genuinely requires a published branch or a merged default branch. Each is named with the check that settles it, so none can be quietly dropped.

| Deferred from | Observation | Settles when |
|---|---|---|
| Phase 2 | CodeQL Python scanned-file count, plausible against 308 files (**DoD clause 4**) | First CodeQL run on the integration PR |
| Phase 2 | GitHub Security tab showing Python results | After merge to the default branch |
| Phase 2 | `pip-audit (model runtimes)` job log naming all three sources | First CI run |
| Phase 2 | `installer-smoke` firing on an installer path and not on an unrelated one | A PR touching `scripts/installer/**`, and one that does not |
| Phase 3 | Rolling drift issue: two runs against a drifted tree produce one issue updated in place; a clean run closes it | Two scheduled `docs-drift` runs |
| Phase 4 | The rendered New-issue page with the version field required | After merge to the default branch |
| Phase 5 | Post-merge workflow performs only its intended smoke/publication work and does **not** rerun the full suite | After merge |
| Phase 5 | First green run of `gitleaks` and `pip-audit (model runtimes)`, the precondition for adding them to branch protection (`QG-2`) | Integration PR |

---

## Publication and integration

**Status: NOT STARTED. Awaiting explicit operator approval.**

This is the only point in the whole plan at which anything leaves the machine. The evidence file is complete, every duty above has quoted output, and no deep-pass or Goal-review finding is unresolved without a recorded gap.

### The mechanical complication, stated before the ask

Branch protection was applied to `develop` during Phase 2, and this plan's commits sit **on `develop` itself** (`protected_checkout=yes`). A direct push to `develop` is now blocked by its own required checks. Publication therefore goes through a feature branch, which needs **no history rewrite and no reset**: create a branch at the current HEAD, push that, and open a pull request into `develop`. Local `develop` converges when the pull request merges.

### Scope, measured against the REMOTE integration branch

```
$ git fetch origin
$ git rev-list --count origin/develop..HEAD
14
$ git diff --stat origin/develop..HEAD | tail -1
 98 files changed, 12814 insertions(+), 1328 deletions(-)
```

**14 commits, not the 6 this plan produced.** Eight predate this session:

```
336aaef1 docs(v2.4.9): record the verified build evidence for both warnings
c05dc572 fix: close the two installer build warnings and the v2.4.9 code gaps
f36afd9c Merge fix/v2.4.8-video-enhancement-panel into develop
c2f0176c docs(v2.4.9): record the studio UX cycle and its open gaps
250390f1 feat(studio): bound settings by model capability, one progress bar, real errors, image editor
81fb0248 feat(installer): admit VS Code 1.137, rebuild the Welcome hero, select every caption
552a1854 docs(v2.4.9): add the VoiceStudio comparison and field-discipline plan
57b24967 fix(video): replace the seven enhancement preset cards with three plain controls
```

This plan's own six are isolated by `git diff f36afd9c..HEAD`. **They cannot be unbundled** from the pull request, because `develop` requires one and every unpushed commit on the branch travels with it. The honest move is disclosure in the pull-request description rather than silence: approval obtained against an understated scope is not approval for what actually merges.

### What the first run will exercise

This pull request is the first thing ever to exercise the branch protection applied in Phase 2, and two of its checks (`gitleaks`, `pip-audit (model runtimes)`) have never run anywhere. Both measured clean locally. If either is red, the pull request blocks, which is gating working as designed.

### Version outcome

`main` auto-releases via `semantic-release`. With Phase 5 removed by D1, every remaining slice should compute a **patch**. The six commits are typed `docs`, `docs`, `fix`, `ci`, `feat`, `feat` -- **two `feat` commits**, from Phase 3 and Phase 4, which compute a **minor**, not the patch the plan expects. Per the plan's own instruction this is a finding to surface before merging, not a version to accept silently: confirm whether `feat(inventory)` and `feat(triage)` are the intended types, or whether the release should be reconciled to `2.4.9`. Recorded here for the operator's decision at the approval gate.

**Nothing has been pushed. No pull request exists. No tag has been created.**
