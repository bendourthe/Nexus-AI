# Session History -- v2.4.10 Phase 1: Provenance evidence and runtime registration

**Date**: 2026-09-11
**Plan**: [v2.4.10-adoption-minicpm5-mistral-models.md](../../plans/v2.4.10-adoption-minicpm5-mistral-models.md)
**Phase**: 1 of 5
**Branch**: `feat/v2.4.10-adoption-minicpm5` (cut from local `develop` @ `aa418e6d`)
**Evidence**: [v2.4.10-model-evidence.md](../v2.4.10-model-evidence.md)

## Goal

Prove the pull targets exist, obtain measured provenance, and register MiniCPM5-2B in the coding runtime so Phase 2 can address it. Nothing user-visible changes in this phase.

## Sub-tasks

| Task | Outcome |
|---|---|
| T001 Probe the pull target | PASS. `openbmb/MiniCPM5-2B-GGUF` and `openbmb/MiniCPM5-2B` both HTTP 200 unauthenticated, `gated: False`, Apache-2.0, `MiniCPM5-2B-Q4_K_M.gguf` published. A connectivity control separated "wrong target" from "network down". |
| T002 Pull, hash, and date | PASS. SHA256 `ec2d5801...02fd` agrees across the HF LFS `oid`, the Ollama manifest layer digest, and `sha256sum` of the on-disk blob. 1561318368 bytes = 1.5613 GB, 0.0013 GB from the card's 1.56 GB claim. |
| T003/T004 Register in the runtime | PASS. `minicpm5:2b` added to `ModelCatalog.ts` and mirrored into `models.json`; `ModelFamily` union extended. |
| T005 `familyFormats` branch | PASS. `minicpm`-prefixed families no longer fall through to the `llama3-json` default at `modelAliases.ts:105`. |
| T006 Resolution test | PASS. Placed in `modelAliases.test.ts` (see Deviations). |
| T007 Scope guard | PASS. `scripts/check-plan-scope-guard.mjs`, `npm run check:plan-scope`, plus `tests/unit/scripts/planScopeGuard.test.ts`. Proven in both directions. |

## Decisions

- **`promptFormat: "qwen"`** -- a real decision, not implicit reuse. MiniCPM5's vendor `chat_template.jinja` renders ChatML (`<|im_start|>` / `<|im_end|>`), `ollama show` lists those as stop tokens, and this repo's `qwen` PromptFormat emits exactly that shape. No new `PromptFormatName` member was needed, so the 2.2 stop condition on `PromptFormat.ts` is not approached.
- **`releaseDate: "2026-09-06"`** -- the date the base model weights were published in the first-party repo. The plan's 2026-09-07 hypothesis came from vendor announcements; repository history offers three defensible dates and the ambiguity is recorded in full rather than resolved by invention. All three are newer than every one of the 38 existing catalog entries, so the choice does not move the entry's sort position.
- **`toolFormat: "qwen-json"`** -- placeholder only, expected to change in Phase 2 (see Plan delta).

## Plan delta

**Disposition: Incomplete -- non-blocking, with one prior contradicted.**

The plan's Phase 1 instructions were carried out as written and Phase 1's own gate is unaffected. Two things were learned during it that later phases need, and neither was available when the plan was written.

**Evidence 1 -- the plan's stated parser prior is contradicted by first-party sources.** Sub-task 2.1 tells the implementer that for "an XML-style emitter on stock `LlamaForCausalLM` the likelier match is the existing `qwen-json`, which accepts the common `<tool_call>{json}</tool_call>` envelope, or `llama3-json` for a bare-JSON body". Two first-party sources read during Phase 1 say otherwise:

- The vendor `chat_template.jinja` renders tool calls as nested XML with named attributes, `<function name="fn"><param name="p">value</param></function>`, with CDATA wrapping for values containing `<`, `&`, or newlines, and a `<tool_sep>` convention between multiple calls.
- The model card states it directly: "MiniCPM5-2B emits XML-style tool calls and SGLang's built-in `minicpm5` parser converts them to OpenAI-compatible `tool_calls` natively", invoked as `--tool-call-parser minicpm5`.

None of the five parsers in `TOOL_FORMAT_NAMES` accepts that grammar. `qwen-json` matches `<tool_call>{json}</tool_call>`, not `<function name=...>`.

**Consequence for remaining phases.** Decision 2.2 is now much more likely to resolve to **option 2** (add one grammar-named parser) than to option 1. This does not pre-empt the decision: sub-task 2.1 still runs in full, three transcripts are still captured, and all five parsers are still tested, because a chat template describes what the vendor intends and only a transcript shows what a Q4_K_M GGUF through Ollama actually emits. What changes is the expectation Phase 2 should hold going in, so a five-way verdict of "no parser matches" is read as the predicted result rather than as a probe malfunction.

The option-2 stop condition was checked ahead of time and is satisfied: `ToolCallFormat.ts` strategies are self-contained `{ name, parse(text) }` objects, so the wiring is confined to the three sites the plan names plus tests. No `HarnessSelector` profile or `PromptFormat.ts` change is implied.

**Evidence 2 -- the plan's test-baseline figure is wrong for this host.** Sub-task 1.5 says "the 36 desktop vitest failures on this host are a known better-sqlite3 Electron-ABI baseline". The measured baseline is **496 failed tests across 61 files**, not 36. This was established by stashing all Phase 1 work and re-running the pristine tree, rather than by assuming.

**Consequence for remaining phases.** Every later phase's "any NEW failure is a regression" check must compare against 496/61, not 36. The comparison method (stash, run, restore) is recorded here so later phases can repeat it instead of trusting a number.

Neither delta blocks Phase 1 or requires a plan edit: the first sharpens an expectation the plan already left to measurement, and the second corrects a figure used only for regression comparison. Both are carried as notes rather than as `DF`/`QG` gaps.

## Deviations

- `# DEVIATION:` T007 named `scripts/check_plan_scope_guard.py`; implemented as `scripts/check-plan-scope-guard.mjs`. The repo keeps this class of repo-wide gate as `scripts/check-*.mjs` wired to a `check:*` npm script with a companion test under `tests/unit/scripts/` (`check-no-devai-hub.mjs`, `check-docs-layout.mjs`, `check-feature-drift.mjs`). The plan's own instruction, "place it wherever the repo already keeps this class of check", outranks the illustrative filename.
- `# DEVIATION:` T006 named `tests/unit/core/registry/catalog.test.ts`; the resolution test was added to `tests/unit/core/registry/modelAliases.test.ts`, which is where `lookupAlias` / `foldModelId` / `aliasesFor` tests live. The plan's prose also refers to a sync test "in `catalog.test.ts`" that actually lives in `ModelCatalog.test.ts`; that test was located and confirmed passing.
- `tests/unit/core/registry/ModelCatalog.test.ts` `listFamilies` was updated to include `minicpm5`. This is an in-scope consequence of extending the closed union, not an unrelated edit.

## Test results

| Suite | Result |
|---|---|
| `npm run lint` (eslint src modules) | clean, exit 0 |
| `npm run build` (`tsc -b`) | clean, exit 0 |
| `npx vitest run tests/unit/core/registry/` | 19 files, 231 passed |
| `npx vitest run tests/unit/scripts/planScopeGuard.test.ts` | 2 passed |
| `npm test` (full) | 61 failed / 477 passed files; 496 failed / 5333 passed tests |
| baseline (same command, Phase 1 work stashed) | 61 failed / 476 passed files; 496 failed / 5329 passed tests |
| installer `pytest tests` (`PYTHONPATH=src`) | exit 0, 1472 collected, zero failures |
| `node scripts/check-plan-scope-guard.mjs` | OK (base `develop` @ `aa418e6d`) |

**Regression check**: failed counts are identical before and after (496 tests / 61 files). Passing rose 5329 -> 5333, exactly the four tests this phase added. Zero new failures. The 496 are the pre-existing better-sqlite3 `NODE_MODULE_VERSION 146 vs 137` Electron-ABI mismatch; `npm rebuild` was not run, per the plan.

## Verification expectation

Declared: a plain non-tool completion through the coding runtime returns a normal text response, and the recorded hash reproduces.

Observed:

```
$ curl -s http://localhost:11434/api/generate -d '{"model":"hf.co/openbmb/MiniCPM5-2B-GGUF:Q4_K_M",
    "prompt":"In one sentence, what is the capital of France?","stream":false,"think":false,
    "options":{"temperature":0.2,"num_predict":80}}'
done_reason: stop
eval_count:  8
response:    'The capital of France is Paris.'

$ sha256sum ~/.ollama/models/blobs/sha256-ec2d5801...02fd
ec2d5801640099e97d8d7e8003ad4d81f336e757811f03a26173dddf386602fd
```

A coherent answer, not garbage, and the hash reproduced. PASS.

## Defect found and fixed during stabilization

`scripts/check-plan-scope-guard.mjs` was written with LF endings, then rewritten by an in-place `sed` that converted it to CRLF. Vite's ESM loader rejected the CRLF `.mjs` with `SyntaxError: Invalid or unexpected token`, even though bare `node` imported it without complaint. The file is now LF, matching every other `scripts/check-*.mjs`. Worth recording because the failure mode is invisible to a manual `node` run and would have reached CI.

## CI impact (recorded, no remote CI run)

Provider: GitHub Actions (`.github/workflows/`).

| Introduced | Covered by the existing pipeline? |
|---|---|
| New npm script `check:plan-scope` | Not invoked by any workflow. It is executed by the test suite through `tests/unit/scripts/planScopeGuard.test.ts`, which the existing unit-test job runs, so the gate does execute in CI via that path. Recorded as an observation rather than a gap. |
| New test file `tests/unit/scripts/planScopeGuard.test.ts` | Yes. `tests/unit/scripts/` already holds `check-feature-drift.test.ts` and peers and is inside the unit-test glob. |
| New script `scripts/check-plan-scope-guard.mjs` | No new job needed; see above. |
| New dependency / env var / artifact | None. |

The guard resolves its base ref against `develop` then `origin/develop`, and fails closed otherwise. On a CI runner with a shallow clone, neither ref may exist, which would make the guard fail closed and red the job. This is flagged now as an input to the Phase 5 terminal reconciliation rather than fixed here, since changing a pipeline file is out of scope for this phase.

No pipeline file was changed. No remote CI was run. No push occurred.

## Next steps

Phase 2: capture at least three tool-call transcripts from the locally pulled `minicpm5:2b`, run them through all five parsers, and record decision 2.2. Go in expecting a five-way miss, per the Plan delta above.
