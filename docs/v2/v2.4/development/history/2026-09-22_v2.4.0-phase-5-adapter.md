# v2.4.0 Phase 5 session history

**Plan**: [v2.4.0-adoption-unsloth-qwen38-gaussian-splatting.md](../../plans/v2.4.0-adoption-unsloth-qwen38-gaussian-splatting.md)
**Phase**: 5, TripoSplat Adapter, Catalog, and Evidence
**Date**: 2026-09-22

## What landed

- `TripoSplatAdapter.ts` resolves weights under the models root and spawns Python with an argument array and `shell: false`. Missing weights return unavailable. The process is mocked in tests. No weights are downloaded.
- `triposplat` is an opt-in catalog row. It is not in `recommended.json`. It has no `weights.files` pin because file hashes were not observed. The job map records it as the holder of optional NVIDIA CUDA single-image splat generate.
- `scripts/bench-gaussian-splat.mjs` records a fake one-row run and a real mode that says not proven here.

## Plan delta

Incomplete, and not blocking. The phase asked for SHA-256 pins through the Hugging Face puller. Those hashes were not observed, so the row does not ship an all-zero pin. The adapter will not run, and will not download, until `model.safetensors` and `infer.py` are present locally. Upstream TripoSplat was not vendored. A fresh clone of the upstream repository was not taken in this phase.

## CI impact

New paths: `desktop/tests/tripoSplatAdapter.test.ts`, `tests/unit/core/image/splatPackaging.test.ts`, and `scripts/bench-gaussian-splat.mjs`. Existing Vitest globs already include the tests. The benchmark script is an explicit local command, not a CI job. No pipeline file was edited. No remote CI was started.

## Gitignore

0 patterns added.

## Known gaps

0 entries added in this phase. The missing weight pin is recorded here and in the catalog `licenseNote` rather than as a fake hash.

## Docs cleanup

Audit only. No scratch document was created, and no file was moved.

## Functional smoke

- Input: an empty models root. Expected: unavailable, and the process runner is not called. Observed: `tripoSplatAdapter.test.ts`.
- Input: a temp weights directory and a fake runner. Expected: a 32-byte stdout body, command `python`, args ending in `infer.py`, and no URL in the argument list. Observed: that test.
- Input: `node scripts/bench-gaussian-splat.mjs`. Expected: `validation: pass`, `gaussianCount: 1`. Observed: that JSON on win32. `--real` observed `not proven here`.
- Comparison: expected and observed matched. A live TripoSplat process was not observed.

## Verification

Adapter tests: 2 passed. Packaging, model-acceptance, and catalog tests: 61 passed. Catalog invariants reported no TripoSplat problems. Desktop `tsc --noEmit` exited 0.
