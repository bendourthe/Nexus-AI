# v2.5.0 migration-durability last-phase evidence

Migration-specific record. The shared quotes for the editor and v2.5.1 work live in `last-phase-evidence.md`. This file is the path the migration plan names.

## Architecture refactor

No migration files were moved. `npx depcruise --config configs/dependency-cruiser.cjs src core modules --output-type err` printed `x 17 dependency violations (0 errors, 17 warnings). 484 modules, 1667 dependencies cruised.` The snapshot helper is `core/storage/preMigrationSnapshot.ts`.

## Known-gaps reconciliation

A11, the signed updater, was deferred twice: once as out of scope in the comparison, and again by the D1 split that put its gating dependency in this plan. It is still not implemented.

`QG-v250-2` is closed. Node 22 job `108108818115` in run `36146482594` passed `preMigrationSnapshot.test.ts`, including "leaves the original bytes unchanged and refuses to reopen after a failed migration" and "creates no new snapshot when the chat store is already migrated". On 2026-09-26 this host loaded the published Node 24.13.0 prebuild and the same file passed 4 tests inside the root suite. The module was not rebuilt, and the prebuild is not committed.

## Living docs architecture

`npm run check:docs-layout` printed `check-docs-layout: canonical layout OK (no docs/versions|docs/archive/versions wrappers)`.

## Git-tree hygiene

`python scripts/check_release_preconditions.py --branches --repo-settings` was run on 2026-09-25. It reported `current=develop`, a clean tree at that moment, `default_branch=main`, and `default_branch_protection=observed`. No branch was deleted.

## CI/CD coverage

The comparison was not applied. There is no `fast` / `full` / `platform` / `report` / `release` profile set. That difference is `CI-v251-1`. Linux run `36153544032` and macOS run `36153548528` succeeded as dispatch-only installer rehearsals. They do not stage the desktop payload.

## Tier 3 deep pass

Bounded result for the storage change. `fix_rerun_cycles_used` is 0.

The load-bearing set is `core/storage/preMigrationSnapshot.ts`, the three stores that call it, and `tests/unit/core/storage/preMigrationSnapshot.test.ts`. None of that set took a reduced path. The byte-identical refusal and the no-snapshot-when-current cases passed on Node 22 in job `108108818115`. They did not run on this host. Rendered-surface delegates and implementation-convergence were not run (`QG-v251-3`).

## Goal-vs-codebase review

Definition of done, checked by artifact:

- A failed migration leaves the original bytes unchanged: the CI test name is "leaves the original bytes unchanged and refuses to reopen after a failed migration". It passed on Node 22. Not re-run here.
- The app stops and names the snapshot: `ChatHistoryStore` writes `*.migration-pending` and `assertNotHalfMigrated` refuses the next open. It does not auto-restore.
- Three launches with nothing pending create no snapshots: the CI test name is "creates no new snapshot when the chat store is already migrated". It passed on Node 22.
- Satisfied-in-intent is not used as a pass for the byte compare. The local Node 24 prebuild run passed the same file: 4 tests, inside the green root suite quoted below.

## Human and manual testing

On a machine whose Node ABI matches `better-sqlite3`, open a populated chat database, force a migration failure, and confirm the original file bytes match the pre-run copy and the newest snapshot path is named in the refusal. Do not rebuild the native module on a mismatched ABI to perform that check.

## Full-suite testing and stabilization

Green on this host on 2026-09-26 with the published Node 24.13.0 `better-sqlite3` prebuild installed and not committed. Root vitest exit 0: 551 files passed, 3 skipped, 5913 tests passed, 12 skipped (5925). Desktop vitest exit 0: 237 files passed, 2199 tests passed, 1 skipped (2200). The quoted blocks are in `last-phase-evidence.md` under this same heading. The Electron-as-node run was not repeated.
