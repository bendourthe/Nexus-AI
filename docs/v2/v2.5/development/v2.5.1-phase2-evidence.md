# v2.5.1 Phase 2 evidence

## Enumeration scope

Decision: (a) the sidecar wire boundary only. The checker reads `export const IPC_METHODS = [ ... ] as const` under `desktop/sidecar/src`, including a brand-new file that uses that idiom. It does not parse Rust `#[tauri::command]` or TypeScript `invoke` sites.

Hand count and checker count are both **141** quoted names in that array (26 exempt). Snapshot: `configs/command-count-snapshot.json`.

## Verification

`node scripts/check-command-parity.mjs` printed `command-parity: PASS commands 141, exempt 26, findings 0`.

`npx vitest run --config configs/vitest.config.ts tests/unit/scripts/command-parity.test.ts` -- 6 passed. Those tests cover an unmapped command in a new file, a renamed idiom that matches nothing (floor failure, no clean pass), a count below the snapshot, a double claim, a dead evidence path, and an exemption with a blank reason.

## NI-3

This checker does not close NI-3. NI-3 is a value union copied in several declaration sites. A command-to-capability map cannot see those copies.

## CI impact

`.github/workflows/ci.yml` gained `check-command-parity`. The job is not path-scoped, because a skipped path-filtered check is treated as green. The checker is a Node script with no install step. This pipeline edit is the phase deliverable.
