# v2.5.1 Phase 3 -- packaged smoke and release assets

The smoke fails when a route selector is missing, when launch fails, when a route times out, or when a connection is not loopback. The artifact list is a committed file, and CI checks it against the installer workflow names before semantic-release.

## Plan delta

**Incomplete.** A real Tauri window was not driven. See `QG-v251-2`. The judgments themselves are tested.
