# v2.5.1 Phase 2 -- command parity

Every `IPC_METHODS` entry maps to one feature id or one exemption with a reason. The checker fails when it finds zero commands or fewer than 141.

## Plan delta

**No delta.** Scope stayed on the sidecar registry, which is the option the plan recommended. NI-3 stays open.
