# ==============================================================================
# OS Metadata
# ==============================================================================
.DS_Store
Thumbs.db
Desktop.ini
desktop.ini
ehthumbs.db
$RECYCLE.BIN/
._*

# ==============================================================================
# IDE and Editor
# ==============================================================================
.idea/
*.suo
*.user
.vs/
*.swp
*.swo
*~
.project
.classpath
.settings/

# VS Code (extension development exceptions below)
.vscode/
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/extensions.json
!.vscode/settings.json

# Visual Studio state files
*.userosscache
*.sln.docstates

# ==============================================================================
# Logs and Temp
# ==============================================================================
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
temp/
tmp/
*.tmp
*.temp
.tmp-model-map-*.json

# ==============================================================================
# Secrets and Environment
# ==============================================================================
.env
.env.*
!.env.example
!.env.sample
*.pem
*.key
*.p12
*.pfx
*.cer
*_rsa
*_dsa
*_ed25519
*_ecdsa
credentials.json
secrets.json

# ==============================================================================
# Coverage and Test Output
# ==============================================================================
coverage/
.nyc_output/
htmlcov/
TEST*.xml
*.lcov

# ==============================================================================
# Build Artifacts
# ==============================================================================
dist/
build/
out/
# PyQt installer packaging sources live under build/ — these are inputs to
# PyInstaller (scripts + .spec + hooks), not build outputs, so allow them.
# (v1.9.0: the two build dirs were unified into scripts/installer/build/.)
!scripts/installer/build/
!scripts/installer/build/**
# ...but the PyInstaller workpath + the mac/linux staging dir under build/
# ARE build outputs (re-excluded after the allow rules above).
scripts/installer/build/work/
# v1.11.0 P4: generated desktop-bundle staging (embedded into NexusSetup.exe)
scripts/installer/build/desktop-payload/
scripts/installer/build/stage/

# ==============================================================================
# Archives
# ==============================================================================
*.zip
*.tar.gz
*.rar

# ==============================================================================
# Large Media
# ==============================================================================
*.mp4
*.avi
*.mov
*.wmv
*.flv
*.tiff

# ==============================================================================
# Claude Code Settings
# ==============================================================================
# Default-ignore everything under /.claude/ ...
/.claude/*
# ... then re-track the committed agent-agnostic subagent / slash-command
# artifacts (the markdown is reverse-engineered Gemma-Code-native and contains
# no Anthropic-specific bindings; per v0.9.0 Phase 3.2 / 3.3 / 3.5).
!/.claude/agents/
!/.claude/commands/
settings.local.json

# ==============================================================================
# Node.js / TypeScript (VS Code Extension)
# ==============================================================================
node_modules/
*.tsbuildinfo
.npm
.yarn/
.pnp.*
.eslintcache

# VS Code Extension specific
*.vsix
.vscode-test/
*.js.map

# ==============================================================================
# Python (Inference Backend)
# ==============================================================================
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.pyc
.python-version

# uv package manager (src/backend/ uses uv)
uv.lock
.uv/
uv.cache

# ==============================================================================
# Rust (Performance Components)
# ==============================================================================
target/
Cargo.lock

# ==============================================================================
# SQLite (Phase 5: ChatHistoryStore via better-sqlite3)
# ==============================================================================
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# ==============================================================================
# Nexus / Gemma Code agent output and session sync
# ==============================================================================
# v1.0.0 Phase 2.2: storage root moved from .gemma-code/ -> .nexus/. The legacy
# .gemma-code/ pattern stays for one cycle so projects mid-migration do not
# accidentally commit the old workspace-local cache.
.gemma-code-output/
.nexus-output/
.gemma-code/
.nexus/
project-sandboxes/

# ==============================================================================
# Go (CLI / Tooling)
# ==============================================================================
vendor/

# Compiled binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test

# ==============================================================================
# Installer Build Artifacts (Phase 7)
# ==============================================================================
scripts/installer/setup.exe
scripts/installer/backend-requirements.txt

# Generated by the audit-py CI job on each run; never committed.
scripts/installer/requirements-audit.txt

# ==============================================================================
# Python Coverage Data Files
# ==============================================================================
.coverage
coverage.xml

# ==============================================================================
# npm Auth Tokens
# ==============================================================================
.npmrc

# ==============================================================================
# Golden Task Suite (Phase 8)
# ==============================================================================
tests/golden/.worktrees/
tests/smoke/results/
tests/golden/snapshots/*/.git/
tests/golden/snapshots/*/node_modules/

# ==============================================================================
# Local build fixtures (v1.8.0 one-shot-installer Phase 1, T104)
# ==============================================================================
# Locally-built desktop bundles stashed as integration fixtures for the
# desktop provisioner tests (Phase 2, T204); machine-built binaries, never
# committed.
.local-fixtures/

# ==============================================================================
# Local A/B harness output (v1.6.0 openrouter-fusion Phase 4, OF010)
# ==============================================================================
# scripts/run-panel-ab.mjs writes per-run panel-vs-single comparison reports
# here when OLLAMA_URL is set; never committed (generated, machine-specific).
tests/benchmarks/results/

# ==============================================================================
# CI Workflow Outputs (v0.4.0 Phase 1: benchmark + golden-task gates)
# ==============================================================================
bench-results.json
bench-results.txt
golden-results-*.json
golden-report-*.md
golden-task-results.json
golden-task-report.md

# ==============================================================================
# Dependency-cruiser local graph output (v0.5.0 Phase 10)
# ==============================================================================
docs/archive/v0/v0.5/dep-graph.svg

# ==============================================================================
# Stryker mutation testing artifacts (v0.6.0 Phase 7.6)
# ==============================================================================
.stryker-tmp/
reports/stryker/

# ==============================================================================
# Debug runner logs (v0.9.0 Phase 4 sub-task 4.1)
# ==============================================================================
# scripts/debug/cli.mjs tees vitest output here; never committed.
out/debug-logs/

# ==============================================================================
# Deep-work git worktrees (v0.9.0 Phase 5 sub-task 5.1)
# ==============================================================================
# scripts/deep-work/start.mjs creates per-issue worktrees here; never committed.
worktrees/

# ==============================================================================
# HNSW vector index artifacts (v0.7.0 Phase 7)
# ==============================================================================
# Persisted under ~/.nexus/<workspace-id>/memory.hnsw (or the legacy
# ~/.gemma-code/<workspace-id>/memory.hnsw during the v1.0.0 compat window)
# at runtime; never committed. The pattern below catches any stray copies.
*.hnsw
memory.hnsw

# ==============================================================================
# Nexus desktop shell (v1.0.0 Phase 1)
# ==============================================================================
# Tauri + Vite build outputs, sidecar bundle, generated Tauri sources.
# Covered by `dist/` and `target/` above; the patterns below pin them
# explicitly so a future cleanup pass does not silently re-include them.
desktop/dist/
desktop/coverage/
desktop/sidecar/dist/
desktop/src-tauri/target/
desktop/src-tauri/gen/
desktop/src-tauri/Cargo.lock

# v2.2.0 Phase 8: the bundled Nexus-Hub catalog snapshot is a build
# artifact. It is regenerated from the local catalog on every installer build
# and its manifest changes each time, so tracking it only produces noise.
scripts/installer/build/hub-snapshot/

# v2.4.1 field repair smoke output is host-specific and may include logs.
docs/v2/v2.4/development/field-installer-repair-result.json
docs/v2/v2.4/development/installed-media-smoke-report.json
docs/v2/v2.4/development/artifacts/
docs/archive/v2/v2.4/development/field-installer-repair-result.json
docs/archive/v2/v2.4/development/installed-media-smoke-report.json
docs/archive/v2/v2.4/development/artifacts/
