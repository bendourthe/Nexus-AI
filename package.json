{
  "name": "nexus-coding",
  "displayName": "Nexus Code",
  "description": "Nexus Agentic AI Coding module. Local agentic coding assistant powered by Gemma 4 / Llama 3 and other open-source LLMs via Ollama. (Formerly Gemma Code. The package name, publisher, command IDs, and view container were renamed to `nexus-coding` / `nexus.coding.*` in v1.1.0; the legacy `gemma-code.*` command IDs continue to fire via the runtime compat shim until v1.2.0, and legacy settings keys remain resolvable via the v1.0.0 SettingsCompat shim.)",
  "version": "2.4.1",
  "publisher": "nexus-coding",
  "license": "MIT",
  "icon": "assets/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/bendourthe/Nexus-AI"
  },
  "engines": {
    "vscode": "^1.134.0",
    "node": ">=20.0.0"
  },
  "categories": [
    "AI",
    "Programming Languages",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/src/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "nexus-coding-sidebar",
          "title": "Nexus Code",
          "icon": "assets/sidebar-icon.svg"
        }
      ]
    },
    "views": {
      "nexus-coding-sidebar": [
        {
          "id": "nexus.coding.chatView",
          "name": "Chat",
          "type": "webview"
        },
        {
          "id": "nexus.coding.memoryPanel",
          "name": "Memory",
          "type": "webview"
        },
        {
          "id": "nexus.coding.traceDashboard",
          "name": "Traces",
          "type": "webview"
        }
      ]
    },
    "commands": [
      {
        "command": "nexus.coding.ping",
        "title": "Nexus Code: Ping Ollama"
      },
      {
        "command": "nexus.coding.newChat",
        "title": "Nexus Code: New Session",
        "icon": {
          "light": "assets/icon.png",
          "dark": "assets/icon.png"
        }
      },
      {
        "command": "nexus.coding.focusSidebar",
        "title": "Nexus Code: Show Sessions"
      },
      {
        "command": "nexus.coding.openSession",
        "title": "Nexus Code: Open Session"
      },
      {
        "command": "nexus.coding.detectGpu",
        "title": "Nexus Code: Detect GPU"
      },
      {
        "command": "nexus.coding.hooks.editPlanModeHook",
        "title": "Nexus Code: Edit Plan-Mode Improvement Hook"
      },
      {
        "command": "nexus.coding.selectModel",
        "title": "Nexus Code: Select Agentic Model"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "nexus.coding.newChat",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Nexus Code",
      "properties": {
        "nexus.llm.ollamaUrl": {
          "type": "string",
          "default": "http://localhost:11434",
          "description": "Ollama server URL (v1.0.0 canonical key; legacy `gemma-code.ollamaUrl` is resolved by the compat shim until v1.1.0)."
        },
        "nexus.llm.modelName": {
          "type": "string",
          "default": "gemma4:e4b",
          "description": "Owned agentic model for coding (installer ticks or Settings downloads). Use Nexus Code: Select Agentic Model; legacy `gemma-code.modelName`."
        },
        "nexus.llm.maxTokens": {
          "type": "number",
          "default": 131072,
          "description": "Maximum context tokens (Gemma 4 supports 128K for E2B/E4B, 256K for 26B/31B). v1.0.0; legacy `gemma-code.maxTokens`."
        },
        "nexus.llm.temperature": {
          "type": "number",
          "default": 1,
          "description": "Sampling temperature (Gemma 4 recommended: 1.0). v1.0.0; legacy `gemma-code.temperature`."
        },
        "nexus.llm.topP": {
          "type": "number",
          "default": 0.95,
          "description": "Top-p (nucleus) sampling threshold (Gemma 4 recommended: 0.95). v1.0.0; legacy `gemma-code.topP`."
        },
        "nexus.llm.topK": {
          "type": "number",
          "default": 64,
          "description": "Top-k sampling threshold (Gemma 4 recommended: 64). v1.0.0; legacy `gemma-code.topK`."
        },
        "nexus.llm.requestTimeout": {
          "type": "number",
          "default": 60000,
          "description": "HTTP timeout in milliseconds (v1.0.0; legacy `gemma-code.requestTimeout`)."
        },
        "nexus.llm.backend": {
          "type": "string",
          "enum": [
            "ollama",
            "lmstudio",
            "auto"
          ],
          "default": "ollama",
          "description": "LLM backend selector (v1.0.0; legacy `gemma-code.llm.backend`)."
        },
        "nexus.llm.lmstudio.baseUrl": {
          "type": "string",
          "default": "http://127.0.0.1:1234",
          "description": "LM Studio base URL (v1.0.0; legacy `gemma-code.lmstudio.baseUrl`)."
        },
        "nexus.llm.localAdapters": {
          "type": "array",
          "default": [],
          "markdownDescription": "v1.6.0 (A3) -- additional **local-only** inference-runtime adapters, discovered from validated manifests. Each manifest is `{ name, protocol, endpoint, label?, capabilities? }`. `endpoint` MUST be a loopback address (`127.0.0.0/8`, `::1`, `localhost`); a non-loopback endpoint is rejected per the AGENTS.md MCP Registry Policy (local-first, zero-outbound). Set `nexus.llm.backend` to a manifest's `name` to select it.",
          "items": {
            "type": "object",
            "required": [
              "name",
              "protocol",
              "endpoint"
            ],
            "additionalProperties": false,
            "properties": {
              "name": {
                "type": "string",
                "minLength": 1,
                "description": "Unique selector id, matched against `nexus.llm.backend`."
              },
              "label": {
                "type": "string",
                "minLength": 1,
                "description": "Human-friendly display name."
              },
              "protocol": {
                "type": "string",
                "enum": [
                  "ollama",
                  "openai"
                ],
                "description": "Wire protocol the runtime speaks (Ollama-native or OpenAI-compatible)."
              },
              "endpoint": {
                "type": "string",
                "minLength": 1,
                "description": "Loopback base URL, e.g. http://127.0.0.1:8000."
              },
              "capabilities": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "chat": {
                    "type": "boolean"
                  },
                  "embed": {
                    "type": "boolean"
                  },
                  "vision": {
                    "type": "boolean"
                  }
                }
              }
            }
          }
        },
        "nexus.llm.panelRouting": {
          "type": "boolean",
          "default": false,
          "description": "v1.6.0 (openrouter-fusion F4) -- opt-in budget-panel routing. When on, a task flagged as benefiting from higher reliability is escalated to a diverse small-model panel (fused by a local judge) instead of a single larger model that may not fit VRAM. Off by default: this is the slower path (it runs several panelists plus a judge, the local analogue of Fusion's disclosed 2-3x latency), and the local A/B (see the v1.6.0 known-gaps) has not yet measured a net quality win for small local models on coding tasks, so per the no-degradation gate it ships opt-in. Enable to trade latency for higher answer reliability on hard tasks."
        },
        "nexus.coding.editMode": {
          "type": "string",
          "enum": [
            "ask",
            "auto",
            "plan"
          ],
          "default": "ask",
          "description": "How Nexus Coding applies changes. 'ask': show diff and confirm. 'auto': apply immediately. 'plan': produce a plan first. v1.0.0; legacy `gemma-code.editMode`."
        },
        "nexus.coding.toolConfirmationMode": {
          "type": "string",
          "enum": [
            "always",
            "ask",
            "never"
          ],
          "default": "ask",
          "description": "When to prompt for confirmation before running terminal commands or editing files (v1.0.0; legacy `gemma-code.toolConfirmationMode`)."
        },
        "nexus.coding.securityPosture": {
          "type": "string",
          "enum": [
            "strict",
            "standard",
            "unattended"
          ],
          "default": "standard",
          "description": "v1.19.1 Phase 2.5 -- named security-posture dial. Strict confirms more and screens all tool output. Standard is the existing default. Unattended skips CONFIRM-tier prompts so long-running jobs can proceed; DANGEROUS tools still confirm and hard-denied commands never run. This is not a no-floor mode."
        },
        "nexus.coding.compactionUserMessageTail": {
          "type": "number",
          "default": 3,
          "minimum": 1,
          "maximum": 20,
          "description": "v1.19.1 Phase 2.4 -- number of recent human user messages EmergencyTrim must keep even under aggressive compaction."
        },
        "nexus.coding.maxAgentIterations": {
          "type": "number",
          "default": 20,
          "description": "Maximum agentic tool-use iterations per user message (v1.0.0; legacy `gemma-code.maxAgentIterations`)."
        },
        "nexus.coding.thinkingMode": {
          "type": "boolean",
          "default": true,
          "description": "Enable Gemma 4 thinking mode for chain-of-thought reasoning (v1.0.0; legacy `gemma-code.thinkingMode`)."
        },
        "nexus.coding.promptStyle": {
          "type": "string",
          "enum": [
            "concise",
            "detailed",
            "beginner"
          ],
          "default": "concise",
          "description": "Verbosity and style of the system prompt (v1.0.0; legacy `gemma-code.promptStyle`)."
        },
        "nexus.coding.thinkingModePreset": {
          "type": "string",
          "enum": [
            "nothink",
            "think",
            "think-max"
          ],
          "default": "nothink",
          "description": "Sampler/thinking-mode preset (v1.0.0; legacy `gemma-code.thinkingModePreset`)."
        },
        "nexus.coding.egressDenyExtra": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "v1.4.0 Phase 2 (A4) -- extra outbound destinations to add to the network-egress denylist enforced by the SSRF guard (fetch_page, web_search, OTLP export). Each entry is a host (no scheme/port); a denied apex domain also covers its sub-domains. Adds to the built-in defaults (cloud-metadata endpoints 169.254.169.254 / metadata.google.internal / metadata.azure.com, and paste/file-drop hosts pastebin.com / transfer.sh / 0x0.st / paste.ee / termbin.com / ix.io); it does not replace them."
        },
        "nexus.coding.terminalEnvScrub": {
          "type": "boolean",
          "default": true,
          "description": "v1.4.0 Phase 2 (A5) -- strip sensitive environment variables (API keys, tokens, secrets, cloud credentials) from the env handed to child processes spawned by run_terminal. On by default; set false to pass the full parent environment through (legacy behavior)."
        },
        "nexus.coding.terminalEnvScrubAllowlist": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "v1.4.0 Phase 2 (A5) -- exact environment-variable names that are allowed to pass through to run_terminal child processes even when they would otherwise be scrubbed by `nexus.coding.terminalEnvScrub`. Use this to opt a specific secret-bearing variable back in for a command that genuinely needs it."
        },
        "nexus.coding.execSandbox": {
          "type": "boolean",
          "default": false,
          "description": "v1.18.0 Phase 6 (OI-A1) -- wrap run_terminal in an OS process sandbox (macOS Seatbelt, Linux Landlock+seccomp, Windows job object + restricted token). Off by default. When off, or when the host cannot apply the OS backend, the command still runs under the existing confirmation/denylist/env-scrub guardrails and the UI and logs state unconfined (never silently unconfined). Headless/sidecar hosts honor NEXUS_EXEC_SANDBOX=1 as the same switch. Network is denied inside the sandbox when confinement applies; Windows does not kernel-enforce filesystem or network (partial, documented)."
        },
        "nexus.coding.parseDocument.enabled": {
          "type": "boolean",
          "default": false,
          "description": "v1.20.0 -- expose the `parse_document` agent tool, which reads a workspace PDF, image, or Office file as text. Off by default; with the toggle off the tool is not registered. PDF/image still need a document OCR model (Settings > Models). .docx / .pptx / .xlsx parse without those models. Sidecar hosts honor NEXUS_PARSE_DOCUMENT=1 or nexus.coding.parseDocument.enabled in ~/.nexus/settings.json."
        },
        "nexus.coding.parseDocument.memoryIngest.enabled": {
          "type": "boolean",
          "default": false,
          "description": "v1.16.0 Phase 4 (adoption item A6) -- store text extracted by `parse_document` as a memory observation, tagged with its source file and OCR engine. Off by default; with the toggle off no memory write is attempted. Parsed documents are untrusted content: text is secret-redacted before storage and the memory store still rejects anything carrying prompt-injection markers."
        },
        "nexus.coding.inboundClassifier.enabled": {
          "type": "boolean",
          "default": true,
          "description": "v1.5.0 Phase 3 (item 3) -- screen content returned by external-data tools (fetch_page, web_search) for indirect prompt-injection markers before it enters the agent's context. Warn-then-allow: flagged content is annotated with an untrusted-content banner and passed through, never hard-blocked or silently dropped. On by default; set false to disable the gate."
        },
        "nexus.coding.inboundClassifier.deepScan": {
          "type": "boolean",
          "default": false,
          "description": "v1.5.0 Phase 3 (item 3) -- add a local-model second opinion to the inbound classifier on top of the always-on deterministic heuristic. Off by default so no per-fetch model call is made; enable to have the local model also classify fetched content (it can only add an annotation, never block). No effect when `nexus.coding.inboundClassifier.enabled` is false."
        },
        "nexus.coding.swarmOrchestration.enabled": {
          "type": "boolean",
          "default": false,
          "description": "v1.5.0 Phase 4 (item 36) -- enable planner/critic/worker swarm orchestration for complex plan-mode requests. When on, write-capable workers run in isolated git worktrees (so concurrent writers cannot collide) and each worker's output is reviewed by a critic before it is merged into the plan; a rejected worker is retried with the critic's feedback. Concurrency stays bounded by the GPU tier's sub-agent limit (extra workers queue, they do not oversubscribe). Off by default: the orchestrator runs the legacy single-workspace, critic-less Plan-and-Execute loop."
        },
        "nexus.coding.skillOptimizer.enabled": {
          "type": "boolean",
          "default": false,
          "description": "v1.7.0 Phase 3 (S2 + S6) -- enable the local bounded-edit skill optimizer: it rolls out the train split, reflects on failing trajectories, proposes a small add/delete/replace edit to a skill .md under a textual learning-rate budget, accepts it ONLY when the held-out validation split improves, and requires explicit human approval before any skill file is overwritten (rejected edits are buffered). Off by default: per the no-degradation gate, default-on flips only when the optimizer-quality A/B (decideSkillOptimizerDefault) shows the resident model produces a net-positive edit set; that A/B has not been measured on live local models here, so the optimizer ships opt-in. Enable to run the loop (every overwrite still asks for approval)."
        },
        "nexus.coding.harnessSelector.enabled": {
          "type": "boolean",
          "default": false,
          "description": "v1.18.0 Phase 2 (OI-A5) -- enable the per-model harness selector: it derives a capability tier (weak/mid/strong) and a named family profile (concise-loop, plan-first, structured-edit, minimal, plus the tier scaffolds) for the active local model and applies that overlay (prompt style, thinking mode, system-prompt budget). Inspect or switch with /harness. Off by default: per the no-degradation gate, default-on flips only when the harness A/B (decideHarnessDefault) shows the selected scaffold beats the one-size default for a weak model on the golden split; that live A/B has not been measured here, so the selector ships opt-in. Reverse-engineered from the per-model harness thesis; no external code imported."
        },
        "nexus.llm.patientTier.enabled": {
          "type": "boolean",
          "default": false,
          "description": "v1.12.0 Phase 4 (adoption-ecosystem-2026-07 E1) -- enable the disk-offload 'patient' inference tier: a large open MoE streamed off disk via a user-registered local llama.cpp flash-MoE offload adapter on a modest/no-GPU host. Off by default and BELOW the single-GPU ceiling: trillion-class models have been measured at roughly 0.03 tokens/sec (~32 seconds/token laptop, ~19-21 seconds/token server), so a much larger per-request timeout (nexus.llm.patientTier.timeoutMs) applies and a latency warning is surfaced. Enable only when you have pointed nexus.llm.backend at a slow disk-offload backend and accept minutes-to-hours responses for async/batch work. Nexus does not bundle the offload runtime."
        },
        "nexus.llm.patientTier.timeoutMs": {
          "type": "number",
          "default": 3600000,
          "description": "v1.12.0 Phase 4 (E1) -- per-request LLM timeout (ms) applied when nexus.llm.patientTier.enabled is on. Defaults to 1 hour so a sub-1-tok/s disk-offload run is not aborted by the interactive nexus.llm.requestTimeout default. Never shorter than nexus.llm.requestTimeout."
        },
        "nexus.llm.patientTier.ramPreset": {
          "type": "string",
          "enum": [
            "laptop",
            "workstation",
            "max"
          ],
          "default": "laptop",
          "description": "v1.19.2 -- RAM-budget expectation preset for the patient tier. Laptop: ~32 s/token at ~8.24 GB peak RSS. Workstation: ~21 s/token. Max: ~19 s/token at ~224 GB peak RSS. Numbers are independently measured on a trillion-class disk-offload MoE and seeded as honest floors. Activation of this setting does not resize expert caches or grant Nexus an offload runtime -- actual cache sizing lives in the user-registered llama.cpp-lineage adapter. Changing the preset only changes the expectation copy shown in settings."
        },
        "nexus.memory.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable persistent memory system for cross-session context recall (v1.0.0; legacy `gemma-code.memoryEnabled`)."
        },
        "nexus.memory.embeddingModel": {
          "type": "string",
          "default": "embeddinggemma",
          "description": "Ollama embedding model for semantic memory search. EmbeddingGemma 300M is the v2.4.1 default (legacy key: `gemma-code.embeddingModel`)."
        },
        "nexus.memory.bm25.k1": {
          "type": "number",
          "default": 1.5,
          "minimum": 0.1,
          "maximum": 5,
          "description": "v1.1.0 Phase 5.2 -- BM25 term-frequency saturation. Textbook default 1.5; raise for short documents, lower for long-form notes."
        },
        "nexus.memory.bm25.b": {
          "type": "number",
          "default": 0.75,
          "minimum": 0,
          "maximum": 1,
          "description": "v1.1.0 Phase 5.2 -- BM25 length-normalization slope. 0 disables; 1 fully normalizes. Default 0.75."
        },
        "nexus.memory.rrf.k": {
          "type": "number",
          "default": 60,
          "minimum": 1,
          "maximum": 1000,
          "description": "v1.1.0 Phase 5.4 -- Reciprocal Rank Fusion constant. Default 60 (Cormack et al., SIGIR 2009). Lower boosts top-of-list confidence; higher smooths between rankings."
        },
        "nexus.memory.hybridMinCorpus": {
          "type": "number",
          "default": 100,
          "minimum": 0,
          "maximum": 100000,
          "description": "v1.1.0 Phase 5.5 -- minimum entry count before `MemoryHub.retrieve` switches from substring scan to the hybrid BM25 + dense + graph + RRF path. Smaller corpora stay on substring because hybrid overhead is wasteful."
        },
        "nexus.memory.consolidation.enabled": {
          "type": "boolean",
          "default": false,
          "description": "v1.1.0 Phase 9.1 -- when true, the `memory.contradiction-sweep` IdleTimeScheduler worker fires on a 1-hour cadence: it scans semantic-tier rows for high-dense-similarity / low-BM25-overlap pairs and adjudicates each via a local Ollama model, marking the loser as `superseded_by` the winner. Off by default; no LLM call is ever made when the toggle is false."
        },
        "nexus.memory.compression.enabled": {
          "type": "boolean",
          "default": false,
          "description": "v1.1.0 Phase 9.2 -- when true, `nexus memory compress --file <path>` and the `/memory-compress <path>` slash command are allowed to call the local Ollama model and write a semantic-tier observation. Off by default; with the toggle off, both surfaces short-circuit before any LLM call."
        },
        "nexus.memory.snapshotMode": {
          "type": "string",
          "enum": [
            "frozen",
            "live"
          ],
          "default": "frozen",
          "description": "Memory snapshot semantics for prefix-cache preservation (v1.0.0; legacy `gemma-code.memorySnapshotMode`)."
        },
        "nexus.mcp.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable Model Context Protocol (MCP) support (v1.0.0; legacy `gemma-code.mcpEnabled`)."
        },
        "nexus.mcp.serverMode": {
          "type": "string",
          "enum": [
            "stdio",
            "off"
          ],
          "default": "off",
          "description": "MCP server mode (v1.0.0; legacy `gemma-code.mcpServerMode`)."
        },
        "nexus.serving.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Serve Nexus's installed local models to other tools on this machine over a loopback OpenAI/Anthropic-compatible HTTP API (v1.16.0). Opt-in and off by default: with this disabled no port is bound at all."
        },
        "nexus.serving.host": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "Bind address for the local serving gateway (v1.16.0). Loopback only -- the gateway refuses to start on any address outside 127.0.0.0/8, ::1, or a loopback hostname, so local models are never exposed to the network."
        },
        "nexus.serving.port": {
          "type": "number",
          "default": 11500,
          "description": "Port for the local serving gateway (v1.16.0). The base URL other tools point at is http://<host>:<port>/v1."
        },
        "nexus.serving.token": {
          "type": "string",
          "default": "",
          "description": "Bearer token required on every local serving-gateway request (v1.16.0). Leave empty to have Nexus generate a random token on first use and surface it in Settings > Local API server for copying into another tool."
        },
        "nexus.autoDetectGpu": {
          "type": "boolean",
          "default": true,
          "description": "Auto-detect GPU and VRAM at startup (v1.0.0; legacy `gemma-code.autoDetectGpu`)."
        },
        "nexus.operationLog.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Append one Markdown-friendly line per tool call to <workspace>/.nexus/operation-log.md (v1.0.0; legacy `gemma-code.operationLog.enabled`, which still wrote to .gemma-code/)."
        },
        "nexus.curator.enabled": {
          "type": "boolean",
          "default": true,
          "description": "v1.1.0 Phase 1.7 -- when true, the curator background task runs on the IdleTimeScheduler's idle cadence (archive stale skills, dedup memory, patch frontmatter). When false, the curator is disabled entirely. The legacy edit-threshold curator-cadence fallback has been removed; the IdleTimeScheduler is now the sole entry point."
        },
        "nexus.skills.autoSync.nexus-hub": {
          "type": "boolean",
          "default": false,
          "description": "v1.1.0 Phase 8.2 (renamed from nexus.skills.autoSync.devai-hub in v1.10.0) -- when true, the sidecar registers a weekly `IdleTimeScheduler` worker that runs `nexus skills sync --apply` to refresh the Nexus-Hub catalog at ~/.nexus-ai/catalog/. Cadence: 7 days. Idle threshold: 5 minutes. Off by default; the user opts in from Settings -> Skills."
        },
        "nexus.skills.preferUpstream": {
          "type": "boolean",
          "default": false,
          "description": "v1.1.0 Phase 8.4 -- when two skills share a display name (one under `user/` and one under `devai-hub/`), surface the DevAI-Hub variant first in slash-command autocomplete. Off by default so local edits take precedence."
        },
        "nexus.skills.harvest": {
          "type": "boolean",
          "default": true,
          "description": "v0.8.0 Phase 6 / v1.1.0 -- Detect repeated tool-call sequences in episodic memory and surface a SKILL.md draft proposal on the third recurrence."
        },
        "nexus.skills.harvestMinRecurrence": {
          "type": "number",
          "default": 3,
          "minimum": 2,
          "maximum": 10,
          "description": "v0.8.0 Phase 6 / v1.1.0 -- Minimum recurrence count before a workflow becomes a skill proposal."
        },
        "nexus.skills.harvestWindowDays": {
          "type": "number",
          "default": 7,
          "minimum": 1,
          "maximum": 90,
          "description": "v0.8.0 Phase 6 / v1.1.0 -- Lookback window (in days) for the workflow detector."
        },
        "gemma-code.ollamaUrl": {
          "type": "string",
          "default": "http://localhost:11434",
          "deprecationMessage": "Use `nexus.llm.ollamaUrl` instead. Will be removed in v1.2.0.",
          "description": "Ollama server URL"
        },
        "gemma-code.modelName": {
          "type": "string",
          "default": "gemma4:e4b",
          "deprecationMessage": "Use `nexus.llm.modelName` instead. Will be removed in v1.2.0.",
          "description": "Model to use for inference"
        },
        "gemma-code.maxTokens": {
          "type": "number",
          "default": 131072,
          "description": "Maximum context tokens (Gemma 4 supports 128K for E2B/E4B, 256K for 26B/31B)",
          "deprecationMessage": "Use `nexus.llm.maxTokens` instead. Will be removed in v1.2.0."
        },
        "gemma-code.temperature": {
          "type": "number",
          "default": 1,
          "description": "Sampling temperature (Gemma 4 recommended: 1.0)",
          "deprecationMessage": "Use `nexus.llm.temperature` instead. Will be removed in v1.2.0."
        },
        "gemma-code.topP": {
          "type": "number",
          "default": 0.95,
          "description": "Top-p (nucleus) sampling threshold (Gemma 4 recommended: 0.95)",
          "deprecationMessage": "Use `nexus.llm.topP` instead. Will be removed in v1.2.0."
        },
        "gemma-code.topK": {
          "type": "number",
          "default": 64,
          "description": "Top-k sampling threshold (Gemma 4 recommended: 64)",
          "deprecationMessage": "Use `nexus.llm.topK` instead. Will be removed in v1.2.0."
        },
        "gemma-code.requestTimeout": {
          "type": "number",
          "default": 60000,
          "description": "HTTP timeout in milliseconds",
          "deprecationMessage": "Use `nexus.llm.requestTimeout` instead. Will be removed in v1.2.0."
        },
        "gemma-code.toolConfirmationMode": {
          "type": "string",
          "enum": [
            "always",
            "ask",
            "never"
          ],
          "default": "ask",
          "description": "When to prompt for confirmation before running terminal commands or editing files. 'always': always ask. 'ask': ask for destructive operations only. 'never': run without confirmation (use with caution).",
          "deprecationMessage": "Use `nexus.coding.toolConfirmationMode` instead. Will be removed in v1.2.0."
        },
        "gemma-code.maxAgentIterations": {
          "type": "number",
          "default": 20,
          "description": "Maximum number of agentic tool-use iterations per user message before the loop terminates.",
          "deprecationMessage": "Use `nexus.coding.maxAgentIterations` instead. Will be removed in v1.2.0."
        },
        "gemma-code.editMode": {
          "type": "string",
          "enum": [
            "ask",
            "auto",
            "plan"
          ],
          "enumDescriptions": [
            "Show a diff and ask for confirmation before applying any file edit.",
            "Automatically accept and apply all changes without prompting.",
            "Produce a plan before taking action and show diffs for review."
          ],
          "default": "ask",
          "description": "Controls how Gemma Code applies changes. 'ask': show diff and confirm. 'auto' (Accept): apply immediately. 'plan': produce a plan first.",
          "deprecationMessage": "Use `nexus.coding.editMode` instead. Will be removed in v1.2.0."
        },
        "gemma-code.thinkingMode": {
          "type": "boolean",
          "default": true,
          "description": "Enable Gemma 4 thinking mode (<|think|>) for chain-of-thought reasoning before tool use and complex responses.",
          "deprecationMessage": "Use `nexus.coding.thinkingMode` instead. Will be removed in v1.2.0."
        },
        "gemma-code.promptStyle": {
          "type": "string",
          "enum": [
            "concise",
            "detailed",
            "beginner"
          ],
          "enumDescriptions": [
            "Brief, direct instructions optimized for experienced developers.",
            "Thorough instructions with rationale explanations.",
            "Step-by-step instructions with technical term definitions for new developers."
          ],
          "default": "concise",
          "description": "Controls the verbosity and style of the system prompt sent to the model.",
          "deprecationMessage": "Use `nexus.coding.promptStyle` instead. Will be removed in v1.2.0."
        },
        "gemma-code.systemPromptBudgetPercent": {
          "type": "number",
          "default": 10,
          "minimum": 5,
          "maximum": 30,
          "description": "Percentage of the context window reserved for the system prompt (base instructions + tool declarations). Range: 5-30%.",
          "deprecationMessage": "Use `nexus.coding.systemPromptBudgetPercent` instead. Will be removed in v1.2.0."
        },
        "gemma-code.compactionKeepRecent": {
          "type": "number",
          "default": 10,
          "minimum": 2,
          "maximum": 50,
          "description": "Number of most-recent messages preserved during sliding-window context compaction.",
          "deprecationMessage": "Use `nexus.coding.compactionKeepRecent` instead. Will be removed in v1.2.0."
        },
        "gemma-code.compactionToolResultsKeep": {
          "type": "number",
          "default": 8,
          "minimum": 0,
          "maximum": 50,
          "description": "Number of most-recent tool results to preserve during compaction (older results are replaced with one-line summaries).",
          "deprecationMessage": "Use `nexus.coding.compactionToolResultsKeep` instead. Will be removed in v1.2.0."
        },
        "gemma-code.memoryEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable persistent memory system for cross-session context recall.",
          "deprecationMessage": "Use `nexus.memory.enabled` instead. Will be removed in v1.2.0."
        },
        "gemma-code.embeddingModel": {
          "type": "string",
          "default": "embeddinggemma",
          "description": "Ollama embedding model for semantic memory search. Set to empty string to disable semantic search.",
          "deprecationMessage": "Use `nexus.memory.embeddingModel` instead. Will be removed in v1.2.0."
        },
        "gemma-code.memoryMaxEntries": {
          "type": "number",
          "default": 10000,
          "minimum": 100,
          "maximum": 100000,
          "description": "Maximum number of memory entries before automatic pruning.",
          "deprecationMessage": "Use `nexus.memory.maxEntries` instead. Will be removed in v1.2.0."
        },
        "gemma-code.memoryCorroborationThreshold": {
          "type": "number",
          "default": 2,
          "minimum": 1,
          "maximum": 5,
          "description": "Number of independent observations required before a semantic memory is treated as a fact. Single-source observations are kept as candidates and only retrieved when no fact-tier match is available. Set to 1 to disable (legacy behavior: every observation is a fact).",
          "deprecationMessage": "Use `nexus.memory.corroborationThreshold` instead. Will be removed in v1.2.0."
        },
        "gemma-code.ollamaEmbeddingThreshold": {
          "type": "number",
          "default": 0.85,
          "minimum": 0,
          "maximum": 1,
          "description": "Minimum cosine similarity for tool-output rows whose embedding came from the configured Ollama embedding model.",
          "deprecationMessage": "Use `nexus.memory.ollamaEmbeddingThreshold` instead. Will be removed in v1.2.0."
        },
        "gemma-code.heuristicEmbeddingThreshold": {
          "type": "number",
          "default": 0.95,
          "minimum": 0,
          "maximum": 1,
          "description": "Minimum cosine similarity for tool-output rows whose embedding came from the deterministic heuristic fallback. Higher than the Ollama-tier threshold because the 128-D heuristic embedder is noisier.",
          "deprecationMessage": "Use `nexus.memory.heuristicEmbeddingThreshold` instead. Will be removed in v1.2.0."
        },
        "gemma-code.mcpEnabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable Model Context Protocol (MCP) support for interoperability with external tools.",
          "deprecationMessage": "Use `nexus.mcp.enabled` instead. Will be removed in v1.2.0."
        },
        "gemma-code.mcpServerMode": {
          "type": "string",
          "enum": [
            "stdio",
            "off"
          ],
          "default": "off",
          "description": "MCP server mode. 'stdio' exposes Gemma Code tools via MCP stdio protocol. 'off' disables.",
          "deprecationMessage": "Use `nexus.mcp.serverMode` instead. Will be removed in v1.2.0."
        },
        "gemma-code.verificationEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Automatically run a verification sub-agent after multiple file edits to check for bugs and test failures.",
          "deprecationMessage": "Use `nexus.coding.verificationEnabled` instead. Will be removed in v1.2.0."
        },
        "gemma-code.passStateGating": {
          "type": "boolean",
          "default": true,
          "description": "v0.8.0 Phase 2 -- refuse to terminate a turn via a no-tool-call 'done' response unless at least one verification-class tool call (run_terminal returning exit 0, lint, build, test, golden task) has succeeded since the last user message. When the gate trips, the agent gets one system nudge and one more iteration to run the missing check before the loop terminates. Disable for non-coding workflows or tests that cannot run real commands.",
          "deprecationMessage": "Use `nexus.coding.passStateGating` instead. Will be removed in v1.2.0."
        },
        "gemma-code.passStateGating.subAgentCredit": {
          "type": "boolean",
          "default": true,
          "description": "v0.9.0 Phase 6.2 -- when a verification / audit / testgaps / curator sub-agent returns success, credit the parent loop's pass-state gate so the parent does not also need to run a verification tool before terminating. Disable to require parent-level verification regardless of sub-agent outcomes (legacy v0.8.0 behaviour).",
          "deprecationMessage": "Use `nexus.coding.passStateGating.subAgentCredit` instead. Will be removed in v1.2.0."
        },
        "gemma-code.hooks.scanInjection": {
          "type": "boolean",
          "default": true,
          "description": "v0.9.0 Phase 6.5 -- scan ~/.gemma-code/hooks/*.md for prompt-injection patterns before folding the hook body into a system message. Hooks that trip the scanner are dropped with a logged warning. Disable if the hook intentionally contains text that overlaps with the heuristic (e.g. a security-research hook containing 'ignore previous instructions').",
          "deprecationMessage": "Use `nexus.hooks.scanInjection` instead. Will be removed in v1.2.0."
        },
        "gemma-code.memorySnapshotMode": {
          "type": "string",
          "enum": [
            "frozen",
            "live"
          ],
          "enumDescriptions": [
            "Capture Instructions.md / Memory.md / Context.md once at session start; mid-session writes go to disk but the rendered prompt keeps reading from the captured snapshot so LLM prefix caches stay warm.",
            "Read the three memory files fresh on every prompt build (v0.7.0 behaviour). Mid-session edits show up in the next turn at the cost of busting the prefix cache."
          ],
          "default": "frozen",
          "description": "v0.8.0 Phase 2 -- memory snapshot semantics for prefix-cache preservation. See ADR-0014 and src/storage/MemorySnapshot.ts.",
          "deprecationMessage": "Use `nexus.memory.snapshotMode` instead. Will be removed in v1.2.0."
        },
        "gemma-code.llm.backend": {
          "type": "string",
          "enum": [
            "ollama",
            "lmstudio",
            "auto"
          ],
          "default": "ollama",
          "description": "v0.8.0 Phase 4 -- LLM backend selector. `auto` probes LM Studio at :1234 on macOS first and falls back to Ollama at :11434. See ADR-0016.",
          "deprecationMessage": "Use `nexus.llm.backend` instead. Will be removed in v1.2.0."
        },
        "gemma-code.lmstudio.baseUrl": {
          "type": "string",
          "default": "http://127.0.0.1:1234",
          "description": "v0.8.0 Phase 4 -- LM Studio base URL. Defaults to the loopback address LM Studio exposes by default.",
          "deprecationMessage": "Use `nexus.llm.lmstudio.baseUrl` instead. Will be removed in v1.2.0."
        },
        "gemma-code.thinkingModePreset": {
          "type": "string",
          "enum": [
            "nothink",
            "think",
            "think-max"
          ],
          "default": "nothink",
          "description": "v0.8.0 Phase 4 -- Sampler/thinking-mode preset. `think` uses the Qwen/jola-tuned values (temp 0.6, top_p 0.95, top_k 20); `think-max` extends the output budget to 32K (auto-downgrades to `think` when context < 64K).",
          "deprecationMessage": "Use `nexus.coding.thinkingModePreset` instead. Will be removed in v1.2.0."
        },
        "gemma-code.memory.scoringMethod": {
          "type": "string",
          "enum": [
            "rrf",
            "weighted"
          ],
          "default": "rrf",
          "description": "v0.8.0 Phase 4 -- Hybrid memory scoring method. `rrf` fuses vector + lexical + recency via reciprocal-rank fusion (k=60). `weighted` blends 50/30/20.",
          "deprecationMessage": "Use `nexus.memory.scoringMethod` instead. Will be removed in v1.2.0."
        },
        "gemma-code.trace.autoEnable": {
          "type": "boolean",
          "default": false,
          "description": "v0.8.0 Phase 4 -- Pre-enable the `/trace` JSONL bug-report file at session start. Default off so disk cost is opt-in.",
          "deprecationMessage": "Use `nexus.trace.autoEnable` instead. Will be removed in v1.2.0."
        },
        "gemma-code.memory.anticipatoryCache": {
          "type": "boolean",
          "default": false,
          "description": "v0.8.0 Phase 6 -- Opt-in anticipatory context cache. When enabled, prefetches likely-relevant memory entries on editor change and tool completion (30s warmth window). Surfaces in the Memory panel under \"anticipated context\".",
          "deprecationMessage": "Use `nexus.memory.anticipatoryCache` instead. Will be removed in v1.2.0."
        },
        "gemma-code.skills.harvest": {
          "type": "boolean",
          "default": true,
          "description": "v0.8.0 Phase 6 -- Detect repeated tool-call sequences in episodic memory and surface a SKILL.md draft proposal on the third recurrence.",
          "deprecationMessage": "Use `nexus.skills.harvest` instead. Will be removed in v1.2.0."
        },
        "gemma-code.skills.harvestMinRecurrence": {
          "type": "number",
          "default": 3,
          "minimum": 2,
          "maximum": 10,
          "description": "v0.8.0 Phase 6 -- Minimum recurrence count before a workflow becomes a skill proposal.",
          "deprecationMessage": "Use `nexus.skills.harvestMinRecurrence` instead. Will be removed in v1.2.0."
        },
        "gemma-code.skills.harvestWindowDays": {
          "type": "number",
          "default": 7,
          "minimum": 1,
          "maximum": 90,
          "description": "v0.8.0 Phase 6 -- Lookback window (in days) for the workflow detector.",
          "deprecationMessage": "Use `nexus.skills.harvestWindowDays` instead. Will be removed in v1.2.0."
        },
        "gemma-code.verificationThreshold": {
          "type": "number",
          "default": 3,
          "minimum": 1,
          "maximum": 20,
          "description": "Number of file edits in a session before automatic verification triggers.",
          "deprecationMessage": "Use `nexus.coding.verificationThreshold` instead. Will be removed in v1.2.0."
        },
        "gemma-code.subAgentMaxIterations": {
          "type": "number",
          "default": 10,
          "minimum": 1,
          "maximum": 30,
          "description": "Maximum tool-use iterations for sub-agents (verification, research, planning).",
          "deprecationMessage": "Use `nexus.coding.subAgentMaxIterations` instead. Will be removed in v1.2.0."
        },
        "gemma-code.autoDetectGpu": {
          "type": "boolean",
          "default": true,
          "description": "Auto-detect GPU and VRAM at startup to select optimal model configuration.",
          "deprecationMessage": "Use `nexus.autoDetectGpu` instead. Will be removed in v1.2.0."
        },
        "gemma-code.gpuTierOverride": {
          "type": [
            "number",
            "null"
          ],
          "enum": [
            null,
            1,
            2,
            3
          ],
          "enumDescriptions": [
            "Auto-detect (default)",
            "Tier 1: Constrained (6-8 GB VRAM)",
            "Tier 2: Balanced (12-16 GB VRAM)",
            "Tier 3: Full (24+ GB VRAM)"
          ],
          "default": null,
          "description": "Override auto-detected GPU tier. Set to null for auto-detection.",
          "deprecationMessage": "Use `nexus.gpuTierOverride` instead. Will be removed in v1.2.0."
        },
        "gemma-code.permissionOverrides": {
          "type": "object",
          "default": {},
          "description": "Override default permission tiers for individual tools. Values: 0 = auto-approve, 1 = confirm, 2 = dangerous. Floor: tools whose baseline tier requires confirmation (delete_file, run_terminal, fetch_page, web_search, write_file, edit_file, create_file, MCP tools) cannot be dropped to 0; the override is silently clamped to 1 with a logger warning.",
          "deprecationMessage": "Use `nexus.coding.permissionOverrides` instead. Will be removed in v1.2.0."
        },
        "gemma-code.mcpExposedTools": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "read_file",
            "list_directory",
            "grep_codebase"
          ],
          "description": "Built-in tools exposed to external MCP clients when the Gemma Code MCP server is enabled. Defaults to the read-only subset; broaden cautiously. Confirmation-tier tools still require user approval per call.",
          "deprecationMessage": "Use `nexus.mcp.exposedTools` instead. Will be removed in v1.2.0."
        },
        "gemma-code.otlpEnabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable optional OTLP trace export to an external collector (Jaeger, Grafana Tempo). Off by default for offline-first usage.",
          "deprecationMessage": "Use `nexus.otlp.enabled` instead. Will be removed in v1.2.0."
        },
        "gemma-code.otlpEndpoint": {
          "type": "string",
          "default": "http://localhost:4318/v1/traces",
          "description": "OTLP HTTP endpoint for trace export.",
          "deprecationMessage": "Use `nexus.otlp.endpoint` instead. Will be removed in v1.2.0."
        },
        "gemma-code.otlpHeaders": {
          "type": "string",
          "default": "",
          "description": "Comma-separated key=value headers for OTLP export requests (e.g. 'Authorization=Bearer token,X-Custom=value').",
          "deprecationMessage": "Use `nexus.otlp.headers` instead. Will be removed in v1.2.0."
        },
        "gemma-code.secretPathDenyExtra": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Extra glob-style patterns (relative to workspace root) that should be treated as secret paths and blocked from file/list/grep tools unless `allow_secrets: true` is passed with per-call confirmation.",
          "deprecationMessage": "Use `nexus.secretPathDenyExtra` instead. Will be removed in v1.2.0."
        },
        "gemma-code.operationLog.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Append one Markdown-friendly line per tool call to <workspace>/.gemma-code/operation-log.md (default off). Records only metadata: tool name, outcome, optional path, session id; never tool inputs. Trade-off: small disk-cost and visibility into past actions vs. privacy of having a persistent audit trail.",
          "deprecationMessage": "Use `nexus.operationLog.enabled` instead. Will be removed in v1.2.0."
        },
        "gemma-code.memoryAutoArchive": {
          "type": "string",
          "enum": [
            "off",
            "weekly",
            "monthly"
          ],
          "enumDescriptions": [
            "Never auto-archive (default). Run /memory archive manually when desired.",
            "Take a snapshot of Instructions.md / Memory.md / Context.md when the latest archive is older than 7 days.",
            "Take a snapshot when the latest archive is older than 30 days."
          ],
          "default": "off",
          "description": "v0.7.0 Phase 2 -- schedule for automatic memory file snapshots into ~/.gemma-code/memory/<workspace-id>/Archive/<YYYY-MM-DD>/. Auto-archive runs silently on session start when due.",
          "deprecationMessage": "Use `nexus.memory.autoArchive` instead. Will be removed in v1.2.0."
        },
        "gemma-code.contextLimitsPerModel": {
          "type": "object",
          "default": {},
          "description": "v0.7.0 Phase 3 -- per-model overrides for max context tokens. Keys are model names (e.g. 'gemma4:e4b'), values are objects with optional 'maxTokens' (number) and 'minContextLimit' (number). Falls back to gemma-code.maxTokens when no override applies.",
          "deprecationMessage": "Use `nexus.coding.contextLimitsPerModel` instead. Will be removed in v1.2.0."
        },
        "gemma-code.compactionProtectedTools": {
          "type": "array",
          "default": [
            "compress_range",
            "compress_message",
            "verify",
            "research",
            "memory",
            "write_file",
            "edit_file",
            "create_file",
            "delete_file"
          ],
          "items": {
            "type": "string"
          },
          "description": "v0.7.0 Phase 3 -- tool names that every compaction strategy (deduplication, purge-errors, compress) must skip. Protects causally-load-bearing or destructive tool calls.",
          "deprecationMessage": "Use `nexus.coding.compactionProtectedTools` instead. Will be removed in v1.2.0."
        },
        "gemma-code.compactionErrorPurgeTurns": {
          "type": "number",
          "default": 4,
          "minimum": 1,
          "maximum": 50,
          "description": "v0.7.0 Phase 3 -- after this many user-message turns elapse since an errored tool call, its args are purged (the error result message itself is preserved verbatim).",
          "deprecationMessage": "Use `nexus.coding.compactionErrorPurgeTurns` instead. Will be removed in v1.2.0."
        },
        "gemma-code.compactionProtectedFilePatterns": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          },
          "description": "v0.7.0 Phase 3 -- substring patterns; tool calls whose args contain a matching path are exempt from deduplication.",
          "deprecationMessage": "Use `nexus.coding.compactionProtectedFilePatterns` instead. Will be removed in v1.2.0."
        },
        "gemma-code.compactExperimentalMessageMode": {
          "type": "boolean",
          "default": false,
          "description": "v0.7.0 Phase 3 -- when true, registers the experimental compress_message tool. Less surgical than compress_range and may fragment causally-linked tool sequences.",
          "deprecationMessage": "Use `nexus.coding.compactExperimentalMessageMode` instead. Will be removed in v1.2.0."
        },
        "gemma-code.memoryHnswThreshold": {
          "type": "number",
          "default": 1000,
          "minimum": 0,
          "maximum": 1000000,
          "description": "v0.7.0 Phase 7 (C32) -- row-count threshold for activating the optional HNSW vector index over memory embeddings. When the entry count exceeds this value AND `hnswlib-node` is loadable, MemoryStore switches from the FTS5-pre-filtered linear scan to the persistent ANN index. Set to 0 to disable HNSW (linear-scan fallback only).",
          "deprecationMessage": "Use `nexus.memory.hnswThreshold` instead. Will be removed in v1.2.0."
        },
        "gemma-code.workers.audit.enabled": {
          "type": "boolean",
          "default": false,
          "description": "v0.7.0 Phase 7 (C34) -- when true, every time the file-edit threshold trips the audit background worker invokes `gemma-check --json` on the changed files and posts findings as a chat message. Off by default.",
          "deprecationMessage": "Use `nexus.workers.audit.enabled` instead. Will be removed in v1.2.0."
        },
        "gemma-code.workers.testgaps.enabled": {
          "type": "boolean",
          "default": false,
          "description": "v0.7.0 Phase 7 (C34) -- when true, every time the file-edit threshold trips the testgaps background worker runs `vitest --coverage --json` on the test files matching the changed source and posts uncovered branches as a chat message. Off by default.",
          "deprecationMessage": "Use `nexus.workers.testgaps.enabled` instead. Will be removed in v1.2.0."
        },
        "gemma-code.cacheEvictionStrategy": {
          "type": "string",
          "enum": [
            "lru",
            "lfu",
            "arc",
            "wtinylfu",
            "clock"
          ],
          "enumDescriptions": [
            "Least-Recently-Used (default; preserves v0.4.0 behavior).",
            "Least-Frequently-Used: keeps popular paths longer; one-shot reads age out fast.",
            "Adaptive Replacement Cache: balances recency and frequency without a tuned parameter.",
            "Window-TinyLFU: small recency window plus frequency-gated admission to a main region.",
            "Clock (Second-Chance): approximates LRU at lower bookkeeping cost."
          ],
          "default": "lru",
          "description": "Eviction policy for the in-process tool-output cache. All strategies are pure JS (no native modules).",
          "deprecationMessage": "Use `nexus.coding.cacheEvictionStrategy` instead. Will be removed in v1.2.0."
        }
      }
    }
  },
  "bin": {
    "nexus": "./bin/nexus.mjs",
    "nexus-check": "./bin/nexus-check.mjs",
    "gemma-check": "./bin/gemma-check-compat.mjs",
    "nexus-image": "./bin/nexus-image.mjs",
    "nexus-video": "./bin/nexus-video.mjs"
  },
  "workspaces": [
    "desktop"
  ],
  "scripts": {
    "generate:golden-tasks": "node scripts/generate-golden-tasks.mjs",
    "prebuild": "npm run generate:golden-tasks",
    "build": "tsc -b",
    "dev:shell": "npm run dev:shell --workspace @nexus/desktop",
    "build:shell": "npm run build:shell --workspace @nexus/desktop",
    "build:sidecar": "npm run build:sidecar --workspace @nexus/desktop",
    "lint:shell": "npm run lint --workspace @nexus/desktop",
    "test:shell": "npm run test --workspace @nexus/desktop",
    "test:shell:coverage": "npm run test:coverage --workspace @nexus/desktop",
    "watch": "tsc -b -w",
    "dev": "tsc -b -w",
    "pretest": "npm run generate:golden-tasks",
    "test": "vitest run --config configs/vitest.config.ts",
    "t": "node scripts/test.mjs",
    "test:integration": "vitest run --config configs/vitest.config.ts --reporter=verbose tests/integration",
    "test:vsix-runtime": "node scripts/verify-vsix-runtime.mjs",
    "lint": "eslint src modules",
    "package": "powershell -NonInteractive -ExecutionPolicy Bypass -File scripts/build-vsix.ps1",
    "package:quick": "node scripts/warn-package-quick.mjs && vsce package",
    "check": "node bin/nexus-check.mjs --baseline configs/nexus-check-baseline.json",
    "check:raw": "node bin/nexus-check.mjs",
    "check:prompts": "node bin/nexus-check.mjs --rule prompt-no-ascii-violation --rule prompt-oversized --rule prompt-trailing-whitespace --rule prompt-bom --rule skill-duplicate-name modules/coding/chat/prompts modules/coding/skills/catalog",
    "check:tampering": "node bin/nexus-check.mjs --rule no-focused-tests --rule no-skipped-tests-without-reason --rule no-tautological-assertion --rule no-commented-out-assertion --rule no-disabled-ci-check tests .github/workflows",
    "cleanup:scan": "node scripts/cleanup-scanner.mjs",
    "cleanup:scan:json": "node scripts/cleanup-scanner.mjs --format=json",
    "bench": "vitest bench --run --config configs/vitest.config.ts",
    "deps:check": "depcruise --config configs/dependency-cruiser.cjs src core modules tests",
    "check-architecture": "depcruise --config configs/dependency-cruiser.cjs src core modules",
    "deps:graph": "depcruise --config configs/dependency-cruiser.cjs --output-type dot src | dot -Tsvg > docs/archive/v0/v0.5/dep-graph.svg",
    "catalog": "node scripts/generate-catalog.mjs",
    "catalog:check": "node scripts/generate-catalog.mjs && git diff --exit-code docs/index.md",
    "perm-tier": "node scripts/generate-tool-permission-table.mjs",
    "perm-tier:check": "node scripts/generate-tool-permission-table.mjs --check",
    "security:gen": "node scripts/generate-tool-permission-table.mjs",
    "security:check": "node scripts/generate-tool-permission-table.mjs --check",
    "check:audit-prod": "node scripts/check-prod-audit.mjs",
    "check:naming": "node scripts/check-no-devai-hub.mjs",
    "check:docs-layout": "node scripts/check-docs-layout.mjs",
    "check:plan-scope": "node scripts/check-plan-scope-guard.mjs",
    "docs:handbooks": "node scripts/generate-handbooks.mjs",
    "docs:handbooks:check": "node scripts/generate-handbooks.mjs --check",
    "mutate": "stryker run configs/stryker.config.json",
    "debug": "node scripts/debug/cli.mjs",
    "work": "node scripts/work.mjs",
    "deep-work": "node scripts/deep-work/cli.mjs",
    "agent-batch": "node scripts/agent-batch/cli.mjs",
    "review": "node scripts/review/cli.mjs",
    "prepare": "husky"
  },
  "lint-staged": {
    "src/**/*.ts": [
      "eslint --max-warnings=0"
    ],
    "modules/**/*.ts": [
      "eslint --max-warnings=0"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "@electron/rebuild": "^4.0.3",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^11.0.1",
    "@stryker-mutator/core": "^8.7.0",
    "@stryker-mutator/vitest-runner": "^8.7.0",
    "@types/better-sqlite3": "^7.6.13",
    "@types/diff": "^5.2.3",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.90.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "@vitest/coverage-v8": "^2.1.9",
    "@vscode/test-electron": "2.5.2",
    "@vscode/vsce": "^2.24.0",
    "dependency-cruiser": "^16.10.4",
    "eslint": "^8.57.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "msw": "^2.13.4",
    "semantic-release": "^25.0.0",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "better-sqlite3": "12.11.1",
    "diff": "^5.2.2",
    "highlight.js": "^11.11.1",
    "isomorphic-dompurify": "^3.9.0",
    "marked": "^12.0.0",
    "minimatch": "^10.2.5",
    "node-html-parser": "^6.1.13",
    "tiktoken": "^1.0.17",
    "tree-sitter-wasms": "^0.1.13",
    "typescript": "^5.4.0",
    "web-tree-sitter": "^0.25.10",
    "zod": "^3.23.8"
  },
  "optionalDependencies": {
    "@huggingface/transformers": "^4.2.0",
    "hnswlib-node": "^3.0.0"
  },
  "overrides": {
    "qs": "^6.16.0",
    "hono": "^4.12.34",
    "dompurify": "^3.4.13",
    "undici": "^7.29.0",
    "@hono/node-server": "^1.19.15",
    "fast-uri": "^3.1.7",
    "ip-address": "^10.3.1",
    "react-router": "7.18.2"
  }
}
